# lending-hub: the hub's committed `lending` book, lending timing, REB_STEP debug effects, Htlc* jurisdictionId

Spec: og (core/) is the authority. Tests are in `pure/diff/lending-hub.test.ts`. Every test is a MATCH test that runs og live, or checks against og's own helpers.

| ID | Item | og source | Rewrite | Test | Status |
|---|---|---|---|---|---|
| LH-1 | Hub-side lending book producer. On a hub (`profile.isHub`), each committed Account lending tx that names this hub moves `state.lending`. `lending_fund` opens a pool. `lending_borrow_request` picks the cheapest open pool (by bps, then createdAt, then positionId), opens an `opening` loan (og `buildLendingLoanId`, `computeLendingInterest`, `dueAt = now + termMs`) and returns a `lending_credit` grant (projected credit + principal). The grant activates the loan. `lending_repay` checks the exact remaining amount, marks the loan `closing` and returns a revoke (projected credit minus principal, floored at 0). The revoke repays the pool, or only stamps a `defaulted` loan. `lending_close_request` closes an empty pool, or checks the hub's outCapacity and returns a `lending_close_payout`. The payout closes the pool. `now` = max(Account frame time, Entity frame time). The book is created by the first lending tx that names the hub (og ensureLendingState). Every og refusal (23 codes) is a plain Error, so the whole Account input is refused. | tx/handlers/account/committed-lending-followup.ts, committed-lending-close.ts, extensions/lending.ts | `lendingFollowups`, `committedLendingFollowups` (runs on the ACKed own frame and the signed peer frame before the HTLC and settle followups); returned txs go through `queueReturned` (og applyLocalAccountEffects) | 3000 random hub books x committed frames vs og applyCommittedLendingFollowup: accept/refuse message, book, returned txs (grant, revoke, payout, 23 refusal codes). og buildLendingLoanId / computeLendingInterest over 200 random inputs. | FIXED |
| LH-2 | Peer frames carrying lending txs reach the hub | og applies any committed Account tx | `entityAcceptsPeerTx` refused them (`not_l0`) | end-to-end test | FIXED: the six lending kinds are accepted |
| LH-3 | End-to-end lifecycle: lendingOffer, then lendingBorrow (grant, and the credit line moves on both replicas), then lendingRepay (revoke, pool repaid with interest), then lendingClosePosition (payout, pool closed). An unpaid loan then defaults at its derived deadline through the local scheduledWake: `lending_overdue` releases the pool and revokes the credit line. | payments/lending.ts, the above, scheduler/derived-deadlines.ts | applyRuntime + convertOutput, `localScheduledWake` | Runtime test: og loan id, interest, term, openedAt = hub frame time, book states, credit on both sides | FIXED |
| LH-4 | Unfunded repay timing (runtime-2 R2-10b, settle-jsubmit SJ-17) | account/local-tx-admission.ts | `admitAt` no longer trial-folds (it queues like og). An unfunded repay is queued and fails when the Account frame is built. | runtime-2 lending test (400 random) | FIXED (already fixed by the admission rework; the finding rows were stale) |
| LH-5 | HtlcReceived / HtlcFinalized `jurisdictionId` and key order | committed-frame-followups.ts jurisdictionIdFor, committed-htlc-followups.ts getJurisdictionId, protocol/htlc/events.ts | `htlcEventFields` takes `secret` and `jurisdictionId` in og's key order. `htlcJurisdictionId` = `jurisdictionConfig.name` trimmed. It is passed through `paybookFollowups`, `resolveFollowup` and `secretFollowup`. | 400 random routes vs og applyCommittedAccountFrameFollowups + applyHtlcSecretFollowups: JSON byte-equal events | FIXED. og's fallback to the Runtime's `env.activeJurisdiction` when the Entity config has no name is not ported. The rewrite Runtime has no active-J notion (owned by the runtime J registry agent). |
| LH-6 | og REB_STEP debug candidate effects (rebalance.ts, j-events-account-settled.ts, account/consensus/helpers.ts, j-events/claim.ts) | runtime/observability/env-events.ts | not emitted | none | NOT OBSERVABLE: every REB_STEP payload is `level: 'info'`, and og's effect sink relays only warn/error debug effects. They reach no event stream, state or hash. Only the Rust shadow-wire cross-check reads them. No port. |
| LH-7 | Followup order across the two committed frames of one input | committed-input.ts applyCommittedFrameTransactions | per frame, og runs lending, then settlement/HTLC. The rewrite runs lending for both frames, then HTLC, then settle. | none | REMAINING (minor): only an input that commits both an own and a peer frame, with lending and HTLC/settle txs in the first, would order the returned Account txs differently in the mempool. The book and credit projection do not change (HTLC/settle return no credit txs). |
