# runtime-j: J replica registry, cursors, J submit ledger, registration authority

og is the authority: core/ and jurisdictions/ at 566c850. Every test is in `pure/diff/runtime-j.test.ts` and runs og live through `applyRuntimeTx(env, tx, { isReplay: true })`.

| ID | og | Rewrite | Finding | Test | Status |
|---|---|---|---|---|---|
| RJ-1 | jurisdiction-import.ts `importJ` / `completeImportJ`, snapshot.ts J view | `importJ`, `completeImportJ`, `durableInfrastructure.pendingJurisdictionImports` | Missing. The rewrite now has the same pending-import staging, completion checks and refusal codes, plus a bound `importReplica` jurisdiction. | MATCH: randomized imports and completions | FIXED |
| RJ-2 | watcher-cursor.ts `advanceJWatcherCursor` | `advanceJWatcherCursor` | Missing. The rewrite now has the monotone cursor rules and codes. | MATCH: randomized | FIXED |
| RJ-3 | j-submit-state.ts / j-submit-result.ts `retryJSubmit`, `recordJSubmitResult`, `recordGovernanceJSubmitResult`, `retryEntityProviderAction`, `recordEntityProviderActionSubmitResult` | ReplicaLocal `jSubmitState` / `entityProviderActionSubmitState` | Missing. The rewrite now has the ledger transitions, the attempt ids and the failure classes. | MATCH: randomized J-submit ledger; EP attempt id; `classifyRuntimeJBatchFailure`. The EP retry path is covered only through the attempt id and the split, because a full EP intent with a certified board is heavy to build. | FIXED |
| RJ-4 | Draft.jOutputs → `pendingCommittedJOutbox`, `registerPendingJOutbox` | `splitJOutbox`, `registerPendingJOutbox` | Committed jOutputs were not routed into the jOutbox; they now are. | MATCH: randomized split / register | FIXED |
| RJ-5 | runtime/mempool/propose-accounts-now.ts `assertProposeAccountsNowTxAuthorized` | applyRuntime ingress check | The rewrite accepted an external `proposeAccountsNow`. It now refuses it with `PROPOSE_ACCOUNTS_NOW_EXTERNAL_INGRESS_REJECTED`. | MATCH | FIXED |
| RJ-6 | jurisdiction/machine/local-history `observeJRange` / `rewindJHistory`, canonical J event normalizers and hashes | `observeJRange`, `rewindJHistory`, `EVENT_NORMALIZERS`, `jEventsHash` | Missing. The rewrite now has the same history, certified-anchor checks and normalizers, and the events / dispute-evidence hashes match og. og's check of the signed-lock range on a rewind is unreachable in pure: pure frames never carry a `j_event` range tx. | MATCH: 100 randomized runs, plus hash equality | FIXED (signed-lock branch documented as unreachable) |
| RJ-7 | jurisdiction/machine/registration-evidence `recordAuthenticatedJAuthority` | `recordAuthenticatedJAuthority`, RLP decode, MPT receipt proof, `durableInfrastructure.certifiedRegistrationEvidence` | Missing. The rewrite now has the same receipt proof, raw-log, envelope, witness-signature, Tron and conflict checks, in og's order and with og's error strings. | MATCH: randomized evidence, including the digests from `buildRegistrationEvidenceDigest` | FIXED |
| RJ-8 | runtime/tx/tx-handlers.ts `assertNumberedReplicaImportAuthority` | `importBoundReplica` numbered branch | The rewrite always refused a numbered importReplica. It now requires registration evidence whose board matches. | MATCH | FIXED |
| RJ-9 | runtime/registration/numbered-registration-intent.ts `recordNumberedRegistrationIntent` / `resolveNumberedRegistrationIntent` | still `runtime_tx_unsupported` | Not ported. It needs a parser for signed EVM raw transactions (ethers `Transaction.from`: legacy EIP-155 and types 1 and 2, sender recovery), ABI encoding of `registerNumberedEntitiesBatch(bytes[])` calldata over `encodeBoard` bytes, and the durable `numberedRegistrationIntents` map. | none | FIXED in entity-j.md (EJ-4, EJ-5): MATCH against og and ethers `Transaction.from` |
| RJ-10 | entity/consensus/authority/board-handover.ts (`boardHandover` EntityTx) | only listed in `PROTOCOL_TXS` | The handover of a pending board config inside the entity frame (leader state, pending config, `withBoardAuthority`) is not ported. It belongs to entity consensus, not the runtime J layer. | none | FIXED in entity-j.md (EJ-6): `boardHandover` with the frame config derived in `foldTxs`; consensus signing parts REMAINING there |
| RJ-11 | SJ-11 (settle.ts continuation J actions) | `materializeContinuation` | Unchanged. It needs Entity-level r2r / r2c / j_broadcast, which the rewrite routes through the Host J layer by design. | none | FIXED in entity-j.md (EJ-1, EJ-2): Entity-level r2r / r2e / r2c / j_broadcast on the committed jBatchState |
