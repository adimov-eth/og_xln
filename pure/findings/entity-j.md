# entity-j: Entity-level J txs, j_event, boardHandover, numbered registration intents

og (core/ and jurisdictions/ at 566c850) is the authority. Every test is in `pure/diff/entity-j.test.ts` and runs og live: the j-batch handlers, `applyJEvent`, `getBoardHandoverFrameConfig` + `handleBoardHandoverEntityTx`, `applyRuntimeTx`, and ethers v6 `Transaction.from`.

The Entity txs work on og's committed `jBatchState` (`state.committed["jBatchState"]`, og-shaped with numeric rows). They share the jBatch functions with the Host J layer, which are shape-agnostic.

| ID | og | Rewrite | Finding | Test | Status |
|---|---|---|---|---|---|
| EJ-1 | entity/tx/handlers/j-batch/{r2r,r2e,e2r,r2c,j-broadcast,j-rebroadcast,j-abort-sent-batch,j-clear-batch,mint-reserves}.ts | `entityR2R`, `entityR2E`, `entityE2R`, `entityR2C`, `entityJBroadcast`, `entityJRebroadcast`, `entityJAbort`, `entityJClear`, `entityMint` | These were not Entity txs. J work reached the Host J layer only as `JOp`s. They now queue, seal, resend, abort and clear the committed jBatchState. They produce og's messages, J outputs, the jBatch hash to sign, and the Account rebalance latch release. | MATCH (randomized, 40 runs x 12 steps): verdict, jBatchState, messages, J outputs, encoded batch, hashesToSign, Account submittedAt. MATCH: `batchOfOg` bytes against og `encodeJBatch`. MATCH: j_broadcast refusals and notes. | FIXED |
| EJ-2 | settle.ts continuation actions; scheduled wake j_broadcast / j_abort_sent_batch (SJ-11, RR-3, RJ-11) | `materializeContinuation`, `foldWake` | `SETTLEMENT_CONTINUATION_J_ACTIONS_NOT_PORTED`, `J_BROADCAST_ENTITY_TX_NOT_PORTED` and `J_ABORT_SENT_BATCH_ENTITY_TX_NOT_PORTED` are removed. Continuation r2r / r2e / r2c / j_broadcast and the wake's J txs now run through the EJ-1 handlers. | The handlers are covered by EJ-1. There is no og-live test of the continuation composition itself. | FIXED |
| EJ-3 | entity/tx/j-events.ts `applyJEvent` over machine/{range-budget, j-event-range-validation, history-consensus, local-history} | `entityJEvent` (`j_event` EntityTx) | Missing. Ported: the 10 MiB range budget; the active-proposer envelope (jurisdiction ref, heights, strict canonical blocks, eventsHash, rangeHash, history root, EOA signature); reconciliation with the certified head (no-op, AHEAD, rebase); a per-block history fold with `lastFinalizedJHeight`; the finality commit (`jHistoryFinality`, `certifiedBoardState` finality); `mergeJEventClaimOps`; and admission of active-Account claims. Handlers ported: board registry, ReserveUpdated, Debt*, AccountSettled, HankoBatchProcessed (finalize and auto j_broadcast, or quarantine), EntityProvider actions, ExternalWallet*. Debt refusal messages now carry og's full detail. | MATCH (randomized, 45 runs x 8 signed ranges, with envelope defects): verdict and code, reserves, debts, jBatchState, jHistoryFinality, certifiedBoardState, externalWallet, messages, dirty Accounts, self outputs. | FIXED |
| EJ-4 | runtime/registration/numbered-registration-intent.ts (RJ-9) | `recordNumberedRegistrationIntent`, `resolveNumberedRegistrationIntent`, durable `numberedRegistrationIntents` | Missing (`runtime_tx_unsupported`). Ported: og's parse order and codes, the request hash, the durable map (placed before `certifiedRegistrationEvidence` in the durable view), and quarantine or completion from certified evidence. `runtime_tx_unsupported` is gone and `applyRuntimeTxStep` is exhaustive. | MATCH (randomized, 40 runs): decisions and codes, the durable store, and durable-view digests. | FIXED |
| EJ-5 | ethers v6 `Transaction.from` and ABI `registerNumberedEntitiesBatch(bytes[])` | `parseEvmTx` (@noble/curves + keccak only), `numberedRegistrationCalldata` | Missing. Ported: lenient RLP; legacy EIP-155 and pre-155; EIP-2930; EIP-1559; lazy sender recovery; ethers' serialize-time checks. | MATCH (randomized, 1500 signed txs with mutations): refusal, hash, sender, chain, nonce, to, value, data. MATCH: calldata against `iface.encodeFunctionData`. | FIXED, except types 3 and 4 (see EJ-R4) |
| EJ-6 | entity/consensus/authority/board-handover.ts `getBoardHandoverFrameConfig`, entity/tx/handlers/board-handover.ts (RJ-10, BH-1) | `handoverFrameConfig` in `foldTxs`, `entityBoardHandover`, `consensusConfigIssue` | Missing. `foldTxs` derives the authorized config from the normalized pre-frame state, as og does inside consensus. It checks the exact frame [j_event, boardHandover], the canonical config, the mode, the self BoardActivated chain from the committed board hash, and the config hash. The handler requires the certified BoardActivated record and matching hashes, then installs the quorum and `leaderState` (validators[0], view 0, height + 1). A handover frame skips the post-tx phases (og `finishAuthorityTransitionOnly`). og's `validateConsensusConfig` messages are ported verbatim. | MATCH (randomized, 60 runs x 20 variants: chains, bad previous hash, hash mismatch, no activation, case, mode, thresholds, duplicates, outsiders, shape, count, unregistered, wrong registration): frame verdict and code, handler verdict, board, leader, registry. | FIXED |
| EJ-R1 | j-events.ts DisputeStarted / DisputeFinalized / CounterDisputeRegistered / HashLadderRevealRegistered Entity handlers, plus `flushDeferredHashLadderReveals` | `finalizedJEvent` default | The Entity-level j_event refuses these with `J_EVENT_<type>_ENTITY_HANDLER_NOT_PORTED`. They go through the Host J layer (`JOp` j_event, `hostDisputeJBatch`, entity-txs-3 T3-8). Porting them is about 1000 lines of og Account-dispute state and is owned by the dispute work. | none | REMAINING: dispute scope |
| EJ-R2 | j-events-observations `applySecretRevealedJEvent`, which calls j-events-htlc `applyKnownHtlcSecret` | `finalizedJEvent` default | This needs the paybook route, the inbound htlc_resolve claims and the cross-j relay output. Cross-j legs belong to the parallel cross-j owners. | none | REMAINING: cross-j / HTLC scope |
| EJ-R3 | Consensus parts of the handover: frame-signature authority from the handover config (`getEntityFrameConsensusConfig`), leader validation, the proposal policy that isolates handover frames, the j-prefix round, `isSelfBoardAuthorityTransitionFrame`, and profile re-certification (`computeEntityProfileHash`) | none | The rewrite derives the config inside `foldTxs` only. Frame signing and verification still use the committed quorum. The proposer does not isolate [j_event, boardHandover] frames from mempool work. There is no profile hash. | none | REMAINING: entity-consensus scope (proposal, commit and profile) |
| EJ-R4 | ethers `Transaction.from` for type 3 (blob) and type 4 (EIP-7702) | `parseEvmTx` | Refused. og's numbered registration payer is a plain call, so these types never apply. | Excluded from the randomized test | REMAINING: out of the registration path |
| EJ-R5 | Nested Entity validators in a handed-over board (og `toBoardEntityId` of a 32-byte validator) | `entityBoardHandover` | The rewrite's quorum is keyed by EOA, so the handover refuses with `BOARD_HANDOVER_ENTITY_VALIDATOR_NOT_PORTED`. og's encodeBoard refusals other than `BOARD_PROPOSER_EOA_REQUIRED` keep the rewrite's `lazyBoardEncoding` codes; og's config validation rejects those inputs first. | none | REMAINING: nested-board quorums are not modelled |
