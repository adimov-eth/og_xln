# cross-j: og cross-jurisdiction extension, HTLC envelope and Account outputs vs pure/xln.ts

Tests: `pure/diff/cross-j.test.ts`. Run from `pure/` with `bun test diff/cross-j.test.ts`: 15 pass, 0 fail.

Every test is `MATCH:` and runs against live og (`core/`, HEAD 566c850). There are three kinds of test:

- **Pure-function tests.** They call the og export and the rewrite function on the same random input. A throw from og counts as a reject. When both sides accept, the values must match as `stableJson`, a port of og `safeStringify`.
- **Account-level lockstep tests.** They reuse the `ogHarness` pattern from `account-tx.test.ts`. A persistent og replica is seeded from the rewrite's committed view and driven through the real transition overlay. Both sides must agree on accept or reject. Every accepted tx must give the same `accountStateRoot`.
- **Output tests.** They go through og `applyAccountTxMutation`, so the og outcome and `candidateEffects` are compared too.

All randomness comes from a seeded mulberry32.

## What was ported

| og source | rewrite | Status |
|---|---|---|
| `protocol/htlc/hash-ladder.ts`: `buildHashLadderProof`, `revealHashLadder`, `decodeHashLadderBinary`, `verifyHashLadderBinary` | `buildHashLadderProof`, `revealHashLadder`, `decodeHashLadderBinary`, `verifyHashLadderBinary` | FIXED. 40 seeds × 9 ratios, plus tampered and malformed binaries. |
| `extensions/cross-j/index.ts`: route hash (43-field ABI tuple), canonical route, clone, pull id / binding, private seed, signed amount, status machine, exact fill progress (validate/apply/committed amounts/proof ratio), close proof, prepared route, pull reveal | `crossRouteHash`, `canonicalCrossRoute`, `cloneCrossRoute`, `crossPullId`, `crossPullBinding`, `crossPrivateSeed`, `crossSignedAmount`, `transitionCrossStatus`/`crossTransitionAllowed`/`compareCrossStatus`, `crossFillProgress`/`applyCrossFill`/`crossFillAmounts`/`crossProofRatio`, `buildCrossCloseProof`, `prepareCrossRoute`, `crossPullReveal` | FIXED. 150 random routes cover invalid stacks, same-stack routes, dispute-config overflow and optional fields. 400 fill inputs are split between consistent and random. |
| `extensions/cross-j/market.ts`: canonical market, venue, book owner | `crossMarket`, `crossMarketForLegs`, `crossBookOwner`, `stackIdOf` | FIXED. This includes `CROSS_J_REQUIRES_DISTINCT_STACKS`. |
| `account/tx/handlers/settlement/pull.ts`: `cross_pull_lock` (`getPullLockAdmissionError`, payer-side hold, `PullCommitment` row at jHeight) and `cross_pull_close` (evidence, hub-only authority, hold release, claimed move, int512 check, source offer retirement) | `AccountTx` `cross_pull_lock` / `cross_pull_close` with the og data shape; `crossPullLock`, `crossPullClose`, `AccountBody.pulls`, `PullRow`, holds in `totalsOn`, and `pulls` committed in `project()` | FIXED. The account-level `unchosen: cross_open` hole is gone. `AccountKinds` marks both txs as bilateral. Coverage: 60 random sequences (lock, offer, resolve, cancel, close) with random corruptions and replays, both legs, ratios 0 through 65535, plus a hold and release check. |
| `account/tx/handlers/swap/offer/{admission,quantization,cross-j-binding,commit}.ts`, cross-j branch: separate same-j (32) and cross-j (18) limits, market key by venue and side, maker/source-hub proposer, no fee authority, lot-aligned exact route amounts, paired source pull, no second hold, stored public route | `swapOffer` / `crossJOffer` / `offerMarketKey`; `SwapOffer.crossJurisdiction` | FIXED. This is covered by the lockstep above. The same-j path now counts only same-j offers toward its 32 and keys markets like og. |
| `swap/resolve/validation.ts`: resolving a cross-j offer is refused | `swapResolve` | FIXED |
| `account/pull-registry-settlement.ts`: `findExactSignedProofBodyPull` (DeltaTransformer batch ABI decode), `resolveFinalizedCrossJurisdictionRouteLeg`, `resolveFinalizedPullFillRatio` | `findSignedProofBodyPull` (with a strict ethers-equivalent batch decoder), `finalizedRouteLeg`, `finalizedPullFillRatio` | FIXED. 500 random batches, some corrupted by truncation, odd hex, flipped offsets or counts, or huge indices, plus 400 window, record and leg cases. |
| `account/tx/handlers/htlc/lock.ts` `envelope` → `assertOpaqueHtlcCiphertext` → `envelopeHash` (sha256 of the decoded packed bytes), checked before capacity | `htlc_lock.envelope` (`HtlcEnvelope`), `htlcEnvelopeHash`, canonical `decodeBase64`/`encodeBase64`, `HtlcLock.envelopeHash` committed by `ogLockRow`; the wire field `htlc_lock.deliveryMode` was added | FIXED. The rewrite-only `encryptedPackage` is removed. Coverage: 600 random envelopes (wrong keys or version, unpadded or non-canonical base64, url-safe characters, size bounds) and 200 lockstep locks with equal roots. |
| apply-result outcomes `htlc_error`, `swap_cancel_requested`, `swap_cancelled`; `mutation.ts` `request_collateral_committed` runtimeEvent; `direct-payment.ts` `directPaymentForward` | `Effect` gains `htlc_error`, `swap_cancel_requested`, `swap_cancelled`, `request_collateral_committed` and `direct_payment_forward` (all perspective-free); `AccountKinds`.effects and `EffectOf` are updated | FIXED. 40 random sequences go through `applyAccountTxMutation` and compare the og outcome plus candidateEffects with the rewrite effects. The forward is emitted as data, and only the named `trustedGatewayEntityId` acts on it, which matches og emitting it only on the gateway side. |
| replica shadow `rebalance.submittedAtByToken`: `setRebalanceSubmittedAt` (entity-update.ts), deletion on full refund (refund.ts) and on a J-finality collateral increase against a pending request (finality.ts), leaf `submittedAtByTokenRoot` (state-root.ts) | `AccountBody.submittedAt`, `setRebalanceSubmittedAt`, `submittedAtRoot`; `rebalanceRefund` / `finalizeSettled` clear it; `installedAccount` commits its root instead of `ZERO_WORD` | FIXED. 120 random refunds compare the og shadow `rootHash()` and the account root. A finality grid covers partial cover, full cover, no cover and an untouched token. |

## Wave 3: entity-cross-j (Entity HTLC onion routing, Account outputs above the Account)

Tests: `pure/diff/entity-cross-j.test.ts`. Run from `pure/` with `bun test diff/entity-cross-j.test.ts`: 13 pass, 0 fail. Every test is `MATCH:` against live og at HEAD 566c850, with seeded mulberry32 randomness. og throwing counts as a reject; accepted values must be equal as `stableJson`, or as key-sorted JSON where og and the rewrite build objects in different key orders.

| Item | og source | rewrite | Status |
|---|---|---|---|
| HTLC onion crypto | `protocol/htlc/multi-recipient.ts` `encryptOpaqueHtlcBytes` / `decryptOpaqueHtlcBytes` (X25519, HKDF-SHA256 salted `xln:htlc-opaque:aes-gcm:hkdf-salt`, AES-256-GCM with the context as AAD, nonce `sha256(eph‖recipient‖context)[0:12]`) | `encryptOpaqueHtlc`, `decryptOpaqueHtlc`; `@noble/ciphers/aes.js` imported exactly as og does | FIXED. 300 random inputs, including tampered ones: byte-identical ciphertext for the same ephemeral key, and the same accept/reject. |
| Onion layer codec | `codec/onion.ts` (XLON v2 final/forward layers, XLMR v1 inner ciphertext) | `encodeOnionLayer`, `decodeOnionLayer`, `OnionLayer` | FIXED. 600 random and malformed layers. |
| Envelope context, onion construction, route quote, deadline window | `codec/envelope.ts` `computeHtlcEnvelopeContextHash`, `createOnionEnvelopes`; `pathfinding/{htlc-quote,fees}.ts`; `payments/delivery.ts` `resolvePaymentDeadlineWindow` | `htlcEnvelopeContextHash`, `createOnionEnvelopes`, `quoteHtlcRoute`, `routingIndex`, `directionalFeePpm`, `requiredInbound`, `paymentDeadlineWindow`, `hopTimelock`, `hopRevealHeight` | FIXED. 500 fee, inbound and window cases; 300 quotes; 80 full onions peeled hop by hop against og. |
| Entity `htlcPayment` origination | `entity/paybook/payment-admission.ts` (`hashRawHtlcPaymentTx`, `materializeOriginatedHtlcPayments`, `assertOriginatedHtlcPayments`, `validatePreparedHtlcPayment`), `prepared-context-validation.ts` (originated half), `tx/handlers/htlc/payment.ts`, `test-secret-capability.ts` | `EntityTx` `htlcPayment`, `htlcPaymentTxHash`, `materializeOriginated`, `assertOriginated`, `validatePreparedHtlcPayment`, `preparedOriginOf`, `htlcPaymentStep`; `HtlcProposerInfra` (gossip profiles, liveness, deterministic test secret or proposer entropy); frame `entityContext.gossipProfiles` / `peerAssertions` / `htlc.originated` | FIXED. 250 random payments, profiles and paybooks. An end-to-end check shows the paybook entry, its collection root and the first-hop `htlc_lock` wire equal og `handleHtlcPayment`'s. |
| Paybook in Entity state and root | `entity/types.ts` `PaybookEntry`, `state/persistent-collection-map.ts` `entityCollectionCommitment` (`paybookHashlock` keys) | `EntityState.paybook`, `Paybook`, `paybookSection`, `entityCollectionCommitment(entries, "paybookHashlock")` | FIXED. It is checked against og's commitment. |
| Inbound onion peel and prepared entries | `entity/paybook/materialize-context.ts` (`buildInboundBinding`, `materializeInboundEntry`, `materializeForwardOutcome`, `canonicalizeInboundEntries`, `assertHtlcPreparedInfraContext`), `prepared-context-validation.ts` (entries half) | `inboundHtlcEntries`, `preparedEntryOf`, `PreparedHtlcEntry`; proposer materialization in `startProposal`; validator replay (`assertInboundEntries`) takes liveness only from `peerAssertions`; `onlineObserver` (og `createOnlineObservation`); `entityEncryptionPrivateKey` (the Runtime derives it from its encryption seed) | FIXED. 240 mutated inbound locks reach every og outcome class: forward, final, `decrypt_failed`, `ciphertext_invalid`, `next_hop_account_missing`, `next_hop_offline`, `insufficient_capacity`, `fee_below_policy`, `deadline_unsafe`. The mutations also cover a wrong key, a bad lock id and a duplicate input, and the entries must equal og `materializeHtlcPreparedInfraContext`'s. A 2-of-2 hub's second validator replays the entries. |
| Forward / final / reject, secret and error propagation, fees | `tx/handlers/account/committed-htlc-followups.ts` (`applyCommittedHtlcLockFollowup`, `applyHtlcTimeoutFollowups` = `htlc_error` → upstream `downstream_error`, `applyHtlcSecretFollowups` with the pending fee and the 120 s secret-ACK deadline), `committed-frame-followups.ts` (`applyCommittedHtlcResolveFollowup`, settled legs), `paybook/lifecycle.ts` | `paybookFollowups`, `lockFollowup`, `resolveFollowup`, `timeoutFollowup`, `secretFollowup`, `htlcFollowups`, which run per committed frame: our own frame on the peer's ACK, then the peer's frame on our signature | FIXED. 400 random paybooks and committed frames go through an og driver that follows `committed-input.ts` order. The paybook, `feesEarned` and returned Account txs must be equal, and so must accept/reject. End to end, Alice → Bob → Carol settles: every paybook entry terminates and Bob earns og's quoted fee. An offline next hop fails back to Alice with balances untouched. |
| Entity → Account work queue | `consensus/frame/tx-effects.ts` `applyLocalAccountEffects` (each returned tx is admitted alone; a missing, frozen or refusing Account drops it) | `queueReturned` → `admitAt` | FIXED for HTLC returns. The rewrite's `admitAt` still validates the body at enqueue while og validates at proposal; see orderbook-watchtower.md "Admission timing". That divergence is shared and predates this work. |
| Peer frames carrying HTLC / collateral txs; consumed Account outputs | og applies any committed Account tx; outcomes go to follow-ups or runtime events | `entityAcceptsPeerTx` (L0 + `htlc_lock` + `htlc_resolve` + `request_collateral`); `ENTITY_CONSUMED_EFFECTS` (`direct_payment_forward`, `htlc_error`, `forward_secret`, `request_collateral_committed`, `swap_cancel_requested`, `swap_cancelled`) | FIXED. A request_collateral round trip now commits on both Entities. Before, it was refused as `not_l0`. |
| `onion` hole | — | removed from `HoleNames`, `EntityTxNames` and the one-Account Host route; `htlcPayment` is an Entity tx | FIXED |
| Route discovery for `htlcPayment` with an empty route | `infra-context.ts` `getNetworkGraph().findPaths` | `findPaths` (gossip graph + PathFinder port) | FIXED. See entity-lane.md. |
| HTLC runtime events (`HtlcForwardAccepted`, `HtlcReceived`, `HtlcFinalized`, `HtlcFailed`) and `request_collateral_committed` | `candidateEffects` `runtimeEvent` | emitted | FIXED (runtime-final.md RF-6, RF-7): `RuntimeStep.events`. Was: The rewrite's Runtime step has no event channel. og events are observability, not consensus state. Adding a channel means threading it through every `Draft` constructor, which is the runtime region. |
| Book cancel on `swap_cancel_requested` | `consensus/frame/application.ts` `collectSwapEvents` → `processOrderbookCancels` | the effect is consumed; there is no book to cancel in | FIXED (consensus-final.md): a cancel request removes the resting offer (book-admission.test.ts "a cancel request removes a resting offer"). |
| Secret-ACK deadline dispute (scheduler due hook), `persistVerifiedPaymentSecret` on dispute paths | `entity/scheduler/due-hooks.ts`, `tx/handlers/account/dispute-input.ts` | the deadline is recorded (`secretAckPending` / `secretAckDeadlineAt`); no hook acts on it | FIXED. The due hook is MATCHed in scheduler-disputes. og handleUnsafeAccountFrame (persistVerifiedPaymentSecret, upstream resolve, ACK deadline, prepareDispute, broadcast latch) is ported as `unsafeAccountFrame` (b21994f). MATCH: 200 random unsafe frames (disputes-final.md, MATCH in diff/disputes-final.test.ts). |
| Keypair check when no inbound lock | og `assertEntityEncryptionKeypair` runs whenever a frame has HTLC txs, including resolve-only frames | the key is required only when an enveloped inbound lock must be decrypted | FIXED (consensus-final.md): the Entity encryption keypair is checked on every proposal and replay (consensus-final.test.ts keypair MATCH, 200 frames). |
| Entity cross-j handlers and collections | `entity/tx/handlers/cross-j/*` | `crossPrepare` / `crossMaterialize` / `crossRegister`, the `runtimeOutput` lane, both collections in the root | FIXED. Setup, lane and collections as before; clear / pullClose / sweep (cross-j-final.md); salvage and sibling dispute (disputes-final.md); book-order txs (cross-book.md). |
| `flushDeferredHashLadderReveals` | `tx/j-events-htlc/index.ts` | not called | FIXED. See cross-j-final.md row 13 (disputes-final.md, MATCH in diff/disputes-final.test.ts). |

## Remaining (earlier waves, statuses updated)

| Item | Status | Reason |
|---|---|---|
| Entity `prepareCrossJurisdictionSwap` / `registerCrossJurisdictionSwap` and the rest of `entity/tx/handlers/cross-j/*` | FIXED | Setup, the command lane and the collections (entity-lane.md); clear / pullClose / sweep (cross-j-final.md); salvage and sibling dispute (disputes-final.md); book-order txs (cross-book.md). |
| Entity `htlcPayment` with onion routing | FIXED | See the wave 3 table. The `onion` hole is removed. |
| Consuming the new Account outputs above the Account | FIXED | FIXED: gateway forward, `htlc_error` propagation along the route, secret propagation, `request_collateral_committed` / swap cancel outcomes consumed. Runtime events FIXED (runtime-final.md RF-6). Book cancel FIXED (consensus-final.md). |
| same-j `swapOfferUpsert` / `swapOfferRemove` / `swapCancelRequest` candidate effects and the `swap_offer_created` outcome | REMAINING | FIXED (consensus-final.md): the Entity book consumes them (book-admission.test.ts "same-j hub matcher", "hub order book inside entity consensus"). |
| Cross-j offer price ticks | FIXED | Matches under the one-tick price step that the rewrite already assumes for every pair policy (`priceTicksOf`). |
