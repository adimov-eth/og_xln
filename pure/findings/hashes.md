# hashes-codecs: og vs pure/xln.ts

Tests: `pure/diff/hashes.test.ts`. Run it from `pure/` with `bun test diff/hashes.test.ts`. Result: 45 pass, about 4.6k expects, seeded mulberry32 PRNG (`0xC0FFEE`). Every DIVERGES test passes by asserting the difference it observed. The og side is always og's own function, or the typechain ABI generated from the `.sol` files. ethers `solidityPacked` stands in for og only where og has no exported function; that happens once, for `Account.sol _encodeDisputeHash`.

## Findings

| id | severity | og file:line | rewrite xln.ts:line | what differs | proven by test? |
|---|---|---|---|---|---|
| H1 | critical | `jurisdictions/contracts/Types.sol:150` (`Int512[] offdeltas`), `core/protocol/dispute/proof-body.ts:127`, `core/protocol/dispute/proof-builder.ts:89,413` | `proofBodyAbi` 489, `encodeProofBodyBytes` 506, `proofBodyHash` 1428 | The rewrite encodes `ProofBody.offdeltas` as `int256[]`. The contract and og encode them as `tuple(int256 high,uint256 low)[]` (Int512). For every body with at least one token, `proofBodyHash` differs from og `hashProofBodyStruct` and from the on-chain `keccak256(abi.encode(finalProofbody))` at `Account.sol:1381`. A dispute proof the rewrite signs therefore cannot be verified on chain. The two agree only for token-free bodies. | yes: `DIVERGES: proofBodyHash(int256[] offdeltas) != og …` and `MATCH: token-free bodies …` |
| H2 | critical | `Types.sol:95-101` (`SignedAmount` diffs), `core/hanko/onchain-domain.ts:159-199`, `proof-builder.ts:495` | `diffAbi` 490, `encodeCooperativeUpdateHash` 589-591 | `SettlementDiff` fields are `SignedAmount{bool negative,uint256 magnitude}` on chain and in og. The rewrite uses `int256`. Every cooperative-update (settlement) Hanko digest with at least one diff differs from og `createSettlementHashWithNonce`. The rewrite also cannot represent the full ±(2^256-1) SignedAmount range (it throws `int256 out of range`). | yes: `DIVERGES: encodeCooperativeUpdateHash …` |
| H3 | critical | `Types.sol:333-345` via 95/144, `core/jurisdiction/machine/batch/index.ts:497-512` | `batchAbi` 492-504, `encodeBatch` 505 | These diverge because of H1 and H2. Any `processBatch` payload with a settlement diff, or with a dispute start, counter or finalization whose proof body has a token, differs from og `encodeJBatch`, and the contract would decode it wrongly or revert. Batches without such entries are byte-identical. | yes: `DIVERGES: any batch with a non-empty settlement diff …` and `MATCH: encodeBatch == og encodeJBatch …` |
| H4 | critical | `Depository.sol:37`, `Types.sol:104-110` (`Int512 ondelta`), `core/jurisdiction/adapter/events/j-event-payloads.ts:53` (`decodeInt512`) | `J_EVENT_SIGNATURES.AccountSettled` 510, `readSettled` 533-545, `encodeAccountSettledData` 571 | The rewrite's AccountSettled signature has `int256 ondelta`. The contract's is `(int256,uint256)`. topic0 differs, so `readJEvents` silently drops every real AccountSettled log: j-claims never observe on-chain settlements. The rewrite's own `encodeAccountSettledData` emits a layout the contract never produces. | yes: `DIVERGES: AccountSettled …` and `DIVERGES: readJEvents silently drops …` |
| H5 | high | `core/account/commitment/state-root.ts:222` | `preparedRoot` 407-416 (commitments section) | og binds `settlementWorkspace` (with its Hankos removed) into the `commitments` section of the Account state root. The rewrite has no such field. Any account with a live settlement workspace gets a different `accountStateRoot`, and so a different frame hash, from og. | **FIXED** (account-tx fix): `CommittedAccountState.settlementWorkspace` is committed through `workspaceWithoutHankos`, which also strips the post-proof Hankos. Proven by `MATCH (H5): og binds settlementWorkspace …` (50 random workspaces) and by the account-tx settlement lockstep, which compares the og overlay `accountStateRoot` after every upsert, clear and submit. |
| H6 | high | `core/entity/consensus/state-root.ts:50-82,444-510,485` | `entityStateRoot` 2098-2110, `EMPTY_ENTITY_PAYBOOK` 2068 | The rewrite's Entity root commits only three sections: `accounts`, `config`, and `paybook` with `feesEarned` fixed at 0. og commits every present `ENTITY_STATE_ROOT_FIELDS` member (`entityId`, `height`, `timestamp`, `reserves`, `nonces`, `lastFinalizedJHeight`, `profile`, …), and each of them moves the root. The oracle goldens `0x1a37f4d7…` and `0x72ac0104…` are real og outputs, but only for a synthetic og EntityState stripped to those three fields. No real og EntityState hashes like this. | yes: `MATCH (golden provenance) …` and `DIVERGES: every real og EntityState carries more root sections …` |
| H7 | medium | `state-root.ts:314-342` (`projectAccountConsensusState`) | `accountLeaf` 2087-2096 | The Entity account leaf always commits `currentFrameHash` and has none of og's optional committed fields: `counterparty*Hanko` digests, `currentDispute*`, `counterpartyDispute*`, `disputePrepare`, `activeDispute`, `publicPinned`, `counterpartySettlementHankos`, and `shadow.activeQuote/pendingRequest/rejectedFrameEvidence`. When og has any of them, the leaf differs. og also omits `currentFrameHash` when no `currentFrame` exists. For the fields the rewrite does model, leaves match on 50 random states. | yes: `DIVERGES: og leaf omits currentFrameHash …`; `MATCH: entity account leaf …` |
| H8 | medium | `core/hanko/claims.ts:154`, `HankoVerifier.sol:323` | `verifyHankoLocal` 705-732 (used by the board quorum at 2220) | `verifyHankoLocal` has no first-member-must-be-EOA check. It also lacks the duplicate-member, threshold-greater-than-board-power, placeholder-equals-signer and zero r/s checks. It accepts a board Hanko that both og `verifyCanonicalHanko` and `HankoVerifier.sol` reject. `verifyAccountHanko`, the stricter rewrite verifier, agrees with og. | yes: `DIVERGES: verifyHankoLocal …` |
| H9 | medium | `core/protocol/serialization/binary-codec.ts:210-213`; `core/entity/consensus/frame.ts:405` | `walkBinary` 2015-2035, `entityFrameHash` 2150-2161 | og's binary codec refuses `-0`, unsafe integers and non-finite numbers. It also refuses a non-canonical `stateRoot`/`authorityRoot`: anything other than lowercase `0x` plus 64 hex characters, so uppercase fails too. The rewrite hashes all of these, lowercasing the roots. If peers disagree on the input, the rewrite produces a hash where og throws. | yes: `DIVERGES: non-canonical numbers …`, `DIVERGES: malformed or UPPERCASE stateRoot …` |
| H10 | low | `core/hanko/codec.ts:83` (`asHankoBytes32`) | `bytes32Of` 784-789, `verifyAccountHanko` 856 | `verifyAccountHanko` accepts the expected entity as a decimal string or as short hex (it pads). og requires `0x` plus 64 hex characters, so for the same Hanko og rejects and the rewrite accepts. | yes: `DIVERGES: verifyAccountHanko accepts a numeric/short expected entity id …` |
| H11 | low | `core/hanko/codec.ts:277-289` | `packSignatures` 633-638 | og `packHankoSignatures` refuses `v∉{27,28}`, zero r or s, and high-s. The rewrite packs anything, with `v≠28` giving bit 0. Callers normalize v, so this is latent. | yes: `DIVERGES (EXTRA laxness): packSignatures …` |
| H12 | low | `core/hanko/onchain-domain.ts:127,143` | `encodeBatchHash` 592-593 | og refuses `chainId==0` and the zero depository address. The rewrite hashes them. It also takes the domain separator as a parameter, where og has a fixed `keccak("XLN_DEPOSITORY_HANKO_V1")`. | yes: `DIVERGES (EXTRA laxness): encodeBatchHash …` |
| H13 | low | `core/account/settlement/witness-projection.ts:53-60` | `withoutHankoWitness` 442-449 | For a `settle_transition` with kind `hanko` and no `postProof`, og throws a TypeError from `delete unsigned.data.postProof.hanko`. The rewrite hashes it. This only affects malformed input. | yes: `DIVERGES (malformed input): settle_transition …` |
| H14 | low (cosmetic) | `core/jurisdiction/adapter/events/contract-codec.ts:11` | `encodeAccountKey` 576-579 | Same bytes, but the rewrite keeps the input's hex case and og returns lowercase. It is hashed as bytes, so no hash is affected. | yes |
| H15 | low (EXTRA) | none (og uses RLP/msgpack) | `canon` 137-148, `hashEntityState` 1992, `hashAccountState` 967 | `canon` is rewrite-only text. It encodes a `Set` or a `Uint8Array` as a plain object, so `canon(new Set([1,2])) === canon({})`. It feeds `EntityFrame.stateHash`, which is outside the og-compatible `entityFrameHash`. | yes: `EXTRA: canon() …` |
| H16 | low (code-reading) | `state-root.ts:354-369` (validators kept in config order) | `rootConfig` 2280-2283 | The rewrite always sorts validators ascending and always sets `mode: "proposer-based"`. og commits `config.validators` in stored order. An og entity whose validators are not sorted hashes differently. | no (code reading) |

## Golden hashes hardcoded in `pure/oracle.test.ts`

All of these were reproduced by og functions in `diff/hashes.test.ts`, in the tests `golden hashes hardcoded …` and `MATCH (golden provenance)`:
- **Account frame:** `0x4820…` from og `computeFrameHash`; this one is also og's own golden. Settlement `0x31c1e688…` and moved `0x2bbd9706…` also come from og `computeFrameHash`. og's golden test only asserts equality and inequality for these, not the literals.
- **j-claim frame `0x11cbf820…`:** og `computeFrameHash` over the proposed frame. Its `accountStateRoot` equals og `computeAccountStateRoot` of the same committed view.
- **Pending root `0x32a2477f…`:** og `createAccountJClaimRecord`, then `applyAccountJClaimInsert` twice with `createAccountJClaimProof`, using the events hash from `canonicalJurisdictionEventsHash`. The one-claim root also matches.
- **Entity-state-root `0x1a37f4d7…` and `0x72ac0104…`:** og `computeCanonicalEntityConsensusStateHash`, but only on an EntityState containing just `config`, `accounts` and `paybook` (see H6).
- **Entity frame `0xbf89…`:** og's own golden.

## Coverage: og behaviours confirmed as MATCH

- **RLP canonical value:** `encodeCanonicalValue` equals og `encodeAccountStateValue` and `encodeAccountStateValueOracle`. Checked on 500 random nested values (bigint ±2^255, uint256 max, -0, maps with many mixed keys, sets, undefined-valued keys, unicode), plus edge scalars. Both refuse lone surrogates.
- **Flat integrity digest:** `flatDigest` equals `computeCanonicalMerkleRoot(ns, …, 'integrity')` on 200 random section lists.
- **Radix-16 Patricia map root:** `mapRoot` equals og `PersistentAccountStateMap.rootHash`. Checked on 400 random maps with 0 to 40 keys, numeric and string keys, `MAX_SAFE_INTEGER`, and mixed non-colliding keys. Both refuse prefix collisions, nested collections, and leaves over 10,000 bytes.
- **Account state commitment:** `accountStateCommitment` equals og `computeAccountStateRoot` on 200 random states without a settlement workspace. These include mixed-case ids, seeds and roots, zero and checksummed depositories, and random j-claim accumulators. Both refuse a bad EIP-55 mixed-case depository.
- **Account frame hash:** `accountFrameHash` equals og `computeFrameHash` on 300 random frames, including stripping of settle_transition Hanko witnesses. Both refuse an unsafe `rebalance_policy.policyVersion`.
- **Event topics:** `HankoBatchProcessed`, `ReserveUpdated`, `DisputeStarted` and `DisputeFinalized` equal the Depository ABI. `readJEvents` decodes contract-encoded logs of those four kinds correctly (50 random each, including uint256 max and mixed case).
- **Batch encoding and batch hash:** `encodeBatch` equals og `encodeJBatch` for 200 batches without settlements or disputes: R2R, R2C, C2R, external tokens, reveals, hash-ladder with `bytes32[4]`, and `uint8`/`uint16` fields. The empty batch also matches. `encodeBatchHash` equals og `computeBatchHankoHash` (100 random).
- **Dispute digests:** `encodeDisputeProofHash` plus `encodeAccountKey` equals og `createDisputeProofHashWithNonce` (200 random, mixed case, max nonce). `encodeDisputeHash` equals the `Account.sol _encodeDisputeHash` / `_argumentCommitment` packing (100 random).
- **Hanko:** Several functions match their og counterparts:
  - `encodeLazyEntityId` equals `lazySingleSignerEntityId`.
  - `encodeHanko65` equals `recoverShortHankoEntityId` for v in {0, 1, 27, 28}; both reject high-s.
  - `encodeHankoEnvelope` and `packSignatures` produce the same bytes as og for canonical input.
  - `verifyAccountHanko` accept/reject and target equal og `verifyCanonicalHanko` on 300 random board Hankos: random thresholds and weights, placeholders, duplicate indexes, weight 0 or over 65535, wrong target, registered-board authority, unused placeholders. The nested-claim and unused-claim cases also agree.
- **`EMPTY_J_ROOT`** equals og `EMPTY_ACCOUNT_J_CLAIM_ROOT`.
- **Entity frame hash:** `entityFrameHash` equals og `createEntityFrameHashFromStateRoot` on 200 random frames. These cover plain txs, the `accountInput` commitment digest, events, and hex projection of strings of 34+ lowercase hex characters versus shorter, odd-length or uppercase ones.
- **Entity account leaf:** matches og `computeEntityAccountValueHash` through the full root, on 50 random multi-account states restricted to the fields the rewrite models.
