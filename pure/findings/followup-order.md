# followup-order: committed-frame followups, cross-j admission timing, active jurisdiction

og (core/ + jurisdictions/) is the spec. The tests are in `pure/diff/followup-order.test.ts`, one MATCH test against live og `applySuccessfulAccountInput` + `admitLocalAccountTx` over 600 random cases.

| # | og item | og file | Rewrite | Status |
|---|---|---|---|---|
| 1 | Committed-frame followup ORDER: per frame (own frame, then the received frame), each tx's lending followup and HTLC resolve, then per tx the settlement auto-approval, the cross-j followup, the receiver's HTLC lock followup and the same-j swap output cursor or the cross-j swap offer | committed-input.ts `applyCommittedFrameTransactions`, committed-frame-followups.ts | `committedFollowups` replaces `committedLendingFollowups` / `htlcFollowups` / `crossFollowups` / `committedSettleFollowups`. The paybook split is `paybookFrameFollowups` + `paybookTailFollowups`. | FIXED (0698770). MATCH: per-account mempools, worklist order, runtime events, swap events, lending, paybook and crontab hooks, with frames that mix lending, HTLC lock/resolve, same-j and cross-j swaps, and forwards. |
| 2 | Direct-payment forwards run after all frame followups (applyCommittedHtlcFollowups: forwards, timeouts, preimages). A missing forward Account is a silent drop at admission. | committed-input.ts | `forwardLeg` in `committedFollowups` | FIXED (0698770). |
| 3 | Returned Account txs are admitted after the whole EntityTx, one at a time in order (dedup via the admission fingerprint). The worklist follows admission order. | tx-effects.ts `applyLocalAccountEffects` | deferred `queueReturned` loop and `touched` order | FIXED (0698770). |
| 4 | Same-j swap outputs are consumed in signed tx order, with ACCOUNT_SWAP_OUTPUT_MISSING / KIND_MISMATCH / ID_MISMATCH / UNCONSUMED invariants. Created events interleave with cross-j created events. | committed-input.ts `consumeSameJurisdictionSwapOutput` | the cursor in `committedFollowups` | FIXED (0698770). |
| 5 | Own-frame runtime events (for example `request_collateral`) survive answering the peer frame | committed-input.ts | `answerFrame` concatenates `runtimeEvents` | FIXED (0698770). |
| 6 | Htlc* `jurisdictionId` = `config.jurisdiction.name \|\| env.activeJurisdiction` | committed-frame-followups.ts `jurisdictionIdFor` | `FoldContext` / `EntityContext.activeJurisdiction` from `Runtime.activeJurisdiction` (set by importJ); `htlcJurisdictionId` | FIXED (4142f5f). The test covers the `jid:Arrakis`, `jid:local` and `jid:eth-main` cases. |
| 7 | `queueInitialHubPolicies`: a hub on inbound genesis (height 1) queues a `rebalance_policy` per `add_delta` token, in ascending order. ACCOUNT_GENESIS_COMMIT_REQUIRED. | committed-input.ts | the `createdAccount` branch of `committedFollowups`; `entityAcceptsPeerTx` now accepts `rebalance_policy` (og applies any committed Account tx) | FIXED (a4af38d). |
| 8 | `scheduleCommittedAccountWork`: the `hub-rebalance-kick` hook when the peer has REBALANCE work and the task's lastRun < now | committed-input.ts | the hook in `committedFollowups` | FIXED (a4af38d). |
| 9 | Runtime cross-j atomic admission and sibling cohort gating | runtime/frame/cross-j/atomic-admission.ts, entity-routing.ts | none | REMAINING. It needs transport provenance on routed inputs (`runtimeId`, `sourceRuntimeFrame`, `atomicCrossJurisdictionPair`), which the rewrite's `RoutedEntityInput` lacks, plus a reducer frame rollback: roughly 1,500 og lines across entity-routing / atomic-admission / entity-input-output / reducer. It changes the Runtime outbox contract every test uses, so it is out of scope for a localised edit. |
| 10 | Account-work timing for queued cross-j legs at H+1; the same-frame local drain of cross-j outputs | entity-input-output.ts `drainImmediateCrossJurisdictionOutputs`, runtime "account-work" commands, `selectCrossJOpeningAccountProposalTxs` (reads the sibling replica from `env.state.eReplicas`) | none | REMAINING. It needs Runtime-level sibling-replica reads and an account-work command loop in `applyRuntime`, and depends on #9. The rewrite's `proposableChild` is also simpler than og `accountHasProposableMempool`. |
| 11 | `accountOutputVerified: true` on same-j created swap events | committed-input.ts | the rewrite's `SwapOfferEvent` has no such field | REMAINING (orderbook area). The test strips this field before comparing. |
| 12 | Hash of the next proposed frame | — | — | NOT COMPARED DIRECTLY. Account state, mempool and worklist order are equal, so proposal inputs are equal. The frame hash itself is covered by the existing account-frame diff tests. |
