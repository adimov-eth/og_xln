# settle-jsubmit: Entity settlement orchestration, J submit lifecycle, r2c fee, remaining RuntimeTx kinds

og is the authority: core/ and jurisdictions/ at 566c850. Every test is in `pure/diff/settle-jsubmit.test.ts` and runs og live.

| ID | og | Rewrite | Finding | Test | Status |
|---|---|---|---|---|---|
| SJ-1 | entity/tx/handlers/j-batch/j-rebroadcast.ts `handleJRebroadcast` | `jRebroadcast`, JOp `j_rebroadcast` | Missing. og re-signs the in-flight `sentBatch` at the same nonce with a gas bump and hands back `jTx` and `hashToSign` (context `...:rebroadcast`). | MATCH: 150 random sent batches | FIXED |
| SJ-2 | j-batch/j-abort-sent-batch.ts `handleJAbortSentBatch` | `jAbortSentBatch`, JOp `j_abort_sent_batch` | Missing. og drops `sentBatch`, can requeue it into the current draft (recovery batches), and releases the rebalance latches it held. | MATCH: 150 cases | FIXED |
| SJ-3 | j-batch/j-clear-batch.ts `handleJClearBatch` | `jClearBatch`, JOp `j_clear_batch` | Missing. og clears the draft batch and the latches its C2R/R2C rows held. | MATCH: 100 cases | FIXED |
| SJ-4 | j-batch/mint-reserves.ts `handleMintReserves` | `mintReservesTx`, JOp `mintReserves` | Missing. og emits a `mint` jTx. | MATCH | FIXED |
| SJ-5 | j-events-batch.ts `applyHankoBatchProcessedEvent` (autoBroadcast) and the jTx / hashToSign outputs | HostEffect `j_submit` / `j_broadcast_request`; `applyJ` returns `{ j, effects, release }` | The Host had no J-submit output. It now emits the jTx and hashToSign, the autoBroadcast `j_broadcast` request, and latch releases, including on the external_finality path. | Host effects test vs og outputs | FIXED |
| SJ-6 | entity/tx/handlers/payments/settle.ts `handleSettlePropose / Update / Reject / Approve` | `settlePropose / settleUpdate / settleReject / settleApprove` in foldTx | Missing. The rewrite now has the same refusal codes (plain Error vs rejectFailure), the same queued Account `settle_transition` (upsert / clear), status events, and `deferredAccountProposals` with og's collection commitment. | MATCH: 400 random txs × random workspaces (none / unsigned / signed / submitted / corrupt, pending transition, unknown peer) | FIXED |
| SJ-7 | settle.ts `canAutoApproveWorkspace` | `canAutoApproveWorkspace` | Ported. | MATCH: 300 random workspaces | FIXED |
| SJ-8 | consensus/frame/application.ts `refreshStaleUncommittedSettlementHankos`, `materializeDeferredSettlementApprovals`; settle.ts `buildSettlementHankoDraft` | `materializeSettlements`, `settlementHankoDraft` in foldTxs before proposeAccounts | Ported. A stale-nonce hanko is dropped and re-deferred. An idle Account's deferred approval becomes its own hanko transition with manifest placeholders and settlement / dispute hashes to sign (`Draft.signing` feeds `hashesToSignOf`). | covered in SJ-6 (materialized approvals; og frame-level corruption and hash-mismatch throws) | FIXED |
| SJ-9 | settle.ts `handleSettleExecute`, `prepareSettlementExecution`, `verifySettlementExecutionHankos` | `settleExecute`, `prepareExecution` | Ported. Gates: skip, reject-disposition pending (`entity_command`), executor mismatch, and missing counterparty Hanko. Then the recompiled-target checks, verification of all three Hankos, the committed jBatch row, and the queued `submit`. | MATCH: 300 random execute gates | FIXED |
| SJ-10 | jurisdiction/machine/batch/index.ts `batchAddSettlement` | `addSettlementRow` on the og-shaped committed `jBatchState` | Ported: full rows, the pure-C2R shortcut into `collateralToReserve`, exact-retry no-op, `J_BATCH_SETTLEMENT_CONFLICT`, and limits. | MATCH: 300 random row sequences | FIXED |
| SJ-11 | settle.ts settlement continuations (`settlementContinuations`, select / materialize) | `materializeContinuation` | Only the waiting, discard and execute-with-no-J-actions paths are ported. When og's continuation carries follow-up r2r / r2e / r2c actions or `broadcast`, it runs them as Entity txs in the same frame. The rewrite carries those only as Host `JOp`s, so it refuses with `SETTLEMENT_CONTINUATION_J_ACTIONS_NOT_PORTED`. | none | REMAINING: needs Entity-level r2r / r2c / j_broadcast (the rewrite routes them through the Host J layer by design, see entity-txs-3) |
| SJ-12 | consensus/frame/application.ts proposePendingAccountFrames events | `proposeAccounts` | A bug: an Account proposal replaced the Draft and dropped the frame's status events. Fixed; the frame's events now survive. | SJ-6 events equality | FIXED |
| SJ-13 | j-batch/r2c.ts `collectRebalanceFee` | `queueR2C(..., fee)`, JOp `r2c.rebalanceFee` | Missing. og core has no writer of `shadow.rebalance.activeQuote` (only `clearRebalanceActiveQuote` deletes it). So from consensus, a quoted deposit is always refused with a status ("no active quote" locally, "unsupported" for a remote receiver) and never reaches the jBatch. The rewrite does the same. | MATCH: 300 random deposits | FIXED (the quote-present branch, the direct_payment fee and quote clearing are unreachable in og consensus) |
| SJ-14 | runtime/tx/tx-handlers.ts observeJRange, advanceJWatcherCursor, rewindJHistory (local-history, watcher-cursor, history-ingress) | `applyRuntimeTx` → `runtime_tx_unsupported` | The rewrite Runtime has no J replica registry (contracts, blockNumber cursor) and no per-replica `jHistory` / `lockedFrame`. That is about 1.5k og lines, plus new Runtime state that other areas hash and construct. | auth MATCH only (runtime-2) | REMAINING: not localisable to this area |
| SJ-15 | runtime/j-submit/{j-submit-state,j-submit-result}.ts retryJSubmit, recordJSubmitResult | same | Needs og's Runtime J-submit ledger (per-replica submit state, attempt/result records, retry scheduling). The Entity-level lifecycle (SJ-1..5) is ported, but the Runtime submit ledger is not. | auth MATCH only | REMAINING: Runtime J-submit state not in the rewrite |
| SJ-16 | runtime/j-submit/jurisdiction-import.ts importJ, completeImportJ | same | Needs og's jurisdiction import intents and the live J adapter registry. `Runtime.jurisdictions` is only a name set. | auth MATCH only | REMAINING: J import registry not in the rewrite |
| SJ-17 | account/local-tx-admission.ts (enqueue does not pre-apply); committed-lending-followup.ts | `admitAt` trial-folds | Account admission timing: og queues and refuses at the Account frame, while the rewrite refuses at enqueue. The settle test counts these cases as `admission` (an upsert whose holds exceed capacity). The hub-side lending follow-up needs og's `state.lending` book. | counted in SJ-6 | REMAINING: ER-15 admission timing (owned by the admission-timing agent); lending book not ported |
| SJ-18 | settlement Hanko verification against the certified board | `pendingVerify(ctx.verify)` | Counterparty settlement / N+1 Hankos are verified with the rewrite's `Verify`. og resolves the signer's certified board. | n/a | REMAINING: certified-board registry (parallel owner) |
| SJ-19 | finalize latches (`finalizeQueued`) in j-abort / j-clear | `JLatchRelease` | Release is ported. The rewrite never sets `finalizeQueued`, so that part of the release is a no-op. | SJ-2 / SJ-3 | REMAINING: scheduler / disputes owner |
