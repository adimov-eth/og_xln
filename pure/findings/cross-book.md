# cross-book: the cross-jurisdiction hub order book

og (`core/` at 566c850) is the spec. Every row marked FIXED has a `MATCH:` test in `pure/diff/cross-book.test.ts` (or `entity-lane.test.ts` where noted) that runs live og.

| Area | og | rewrite before | Status |
|---|---|---|---|
| `crossJurisdictionBookAdmissions` as live state | `extensions/cross-j/orderbook.ts` merge / resolving / closed / admission error / typed failure; the committed collection root | a committed radix section only | FIXED (cbd960d, b98ca65). The admissions are an `EntityState` field: encoded, and committed through `entityCollectionCommitment`. 60 random admission streams match og's entries, collection roots and errors. |
| USD risk caps | `crossJurisdictionLegUsdMicros`, `getCrossJurisdictionLocalUsdCapError`: validator-leg selection, the $6.5m cap, and unpriced tokens being permissionless. Non-stable tokens are priced through `orderbookExt` books and `hubProfile.referenceTokenId` | missing; the materialize cap ignored `orderbookExt` | FIXED (cbd960d, aadc575). 600 + 600 random cases match. In entity-lane, materialize now prices through the Entity's `orderbookExt`, and random ext prices cover both the priced and the stable branch of the 6.5m `risk_reject`. |
| Market offer / fill / cancel instructions | `buildCrossJurisdictionMarketOffer`, remaining amounts, execution price and amounts, `buildCrossJurisdictionFillInstruction`, `buildCrossJurisdictionCancelInstruction` | missing | FIXED (cbd960d). 500 random progressed routes match. |
| Cross matcher pass | `orderbook/cross/*` inside `processOrderbookSwaps` and the cross branch of `processOrderbookCancels`. It includes og's overlay aliasing: after a resting commit, later trades on that pair land in the published book | same-j only | FIXED (cbd960d). The aliasing is reproduced as `CrossPass.aliased`. 200 random hub states (local and remote admitted rows, incoming offers, cancels) match og's fills, cancel instructions, resolves, books and halts. |
| `collectOffersForMatching` cross branch | a cross-j offer is matched only at `crossJurisdictionBookOwnerRef`, through `admitOrderbookOfferForMatching`: a working route, a live Account, pending or risk_reject waits, and anything else halts | cross-j offers skipped | FIXED (b98ca65). This is `offersForMatching` with `admitCrossForMatching`, and the matcher hub view carries the admissions and the route mirror. |
| Frame order | `applyPostEntityTxPhases`: committed cancels close still-visible cross admissions (`committed_cancel`), then `routeRemoteCrossJurisdictionBookCancels` (which runs even without `orderbookExt`), then local cancels, then matching. Matching also runs when any `cross:` pair exists. Cross fills are applied after the books are installed | same-j order only | FIXED (b98ca65). `bookPhase(d, timestamp)` follows og's order. The existing book-admission integration tests stay green. |
| `admitCrossJurisdictionBookOrder` | `book-order.ts` handleAdmit: wrong owner, duplicate closed/resolving (hash fence), mirror transition and stale-same-route merge, a committed offer event from the local Account or from the route alone, and pending / risk_reject / invalid | missing | FIXED (b98ca65). 400 random book owners match og's admissions, mirror, messages, created offer event and halts. |
| Book fill progress | `applyCrossJurisdictionBookFillToState`: resize, re-materialize or remove the row; close the admission (`fill_closed` / `cancel_request`); keep the remote mirror coherent. `applyCrossJurisdictionOrderbookFill` then serves the source hub locally or emits `crossJurisdictionFillNotice` | missing | FIXED (b98ca65). 500 random worlds, covering direct progress and matcher instructions, match og's state, outputs and halts. For the fill-validation halts only the code prefix is compared: `applyCrossFill` reports `CROSS_J_FILL_SEQ`-style reasons where og formats its own text, and the accept/reject decision is the same. |
| `removeCrossJurisdictionBookOrder` | route fences, admission-hash fence, row removal, an ack to the source hub carrying the book's own progress, and the admission close | missing | FIXED (b98ca65). 400 random cases match og, including the ack output. |
| `crossJurisdictionFillNotice` | `applySourceHubCrossJurisdictionFillProgress`: duplicate, stale conflict, clear already requested, a terminal fill removing the local book row, and a self `requestCrossJurisdictionClear` output | missing | FIXED (b98ca65). 400 random mirrors match og. |
| `crossJurisdictionBookOrderRemoved` (non-dispute) | `book-removal-ack.ts`: hub, state and hash fences; a terminal mirror is a no-op; the admission closes and the cancel progress runs from the later of the carried and mirrored progress | missing | FIXED (b98ca65 + test). 300 random acks match og. |
| `crossJurisdictionBookOrderRemoved` (dispute branch) | when the Account is `dispute_preparing` with a pending removal: `confirmDisputeBookRemoval`, then `draftPreparedDisputeStartIfReady` | missing | FIXED (1abcef3). `disputeRemovalAck` confirms the removal, then `draftPreparedStart`. MATCH: 200 random ACKs (disputes-final.md, MATCH in diff/disputes-final.test.ts). |
| `routeRemoteCrossJurisdictionBookCancels` | a cancel whose book lives on a sibling hub becomes a `removeCrossJurisdictionBookOrder` to that hub, and the admission is marked resolving | missing | FIXED (b98ca65). 300 random source hubs match og's local cancels, outputs, admissions and halts. |
| runtimeOutput authorization of the book txs | `auth/authorization.ts`: the semantic route is the stored swap by `orderId` for fillNotice, remove and requestClear; book owner and source hub authority; the `requestCrossJurisdictionClear` self-continuation lane | only the setup txs | FIXED (b98ca65). 600 random envelopes match og's exact error strings. |
| `requestCrossJurisdictionClear` handler, clear materialization, `crossPullClose` followup, salvage, force sibling dispute, sweep | scheduler-driven cross-j txs | ported | FIXED. Clear, clear materialization, crossPullClose and sweep MATCH og (cross-j-final.md); salvage and force sibling dispute MATCH og (disputes-final.md). |
| Peer-side acceptance of paired `cross_pull_lock` / `swap_offer` legs, atomic admission, sibling cohort gating | `cross-j-proposer-materialization.ts` opening cohorts; `queueBookAdmissionOnCommittedPull` (committed pull, then `admitCrossJurisdictionBookOrder`) | committed-pull followup ported (`committedCrossFollowup`, scheduleHook) | FIXED. The committed cross_pull_lock / cross_pull_close followups and peer acceptance were fixed in cross-j-final.md. Sibling cohort gating is runtime-final RF-14. Runtime atomic admission is runtime-final RF-15. |
| Account-work timing (og proposes queued cross-j legs at H+1) | the Runtime's account-work input proposes after the registering frame commits | proposed in the same frame | FIXED (runtime-final RF-14). A registration frame is a cross-j setup phase and proposes no Account frame; og account-work proposes the legs at H+1 in the same Runtime frame. |
| `SwapMatched` runtime event for cross fills | a candidate effect | emitted by `bookPhase` from `tradesMatched` over every matcher book, cross pairs included | FIXED (final-sweep.md). og counts trade-count deltas over all `bookUpdates` in commitOrderbookMatchResult, cross pairs included; the rewrite's `bookPhase` emits `SwapMatched` from `tradesMatched(ext, match.books)`, which is the same sum. MATCH: cross-book.test.ts "hub cross matcher" now asserts og's count equals `tradesMatched` on all 200 hub states, and requires a case with cross fills and a non-zero count. |
