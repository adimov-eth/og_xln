# book-admission: Account admission timing, proposeAccountsNow, and the hub order book inside entity consensus

og (`core/` at 566c850) is the spec. Every row marked FIXED has a `MATCH:` test in `pure/diff/book-admission.test.ts` that runs og live.

| Area | og | rewrite before | Status |
|---|---|---|---|
| Admission timing | `account/input/local-tx-admission.ts` `applyAccountEnqueue`: `assertAccountTxsAdmissible` (policyVersion, set_credit_limit tokenId, profile), lifecycle dedup by fingerprint against mempool + pending frame, j_event_claim planning (duplicate / conflict row rejection), mempool limit. Bodies are validated only at proposal. | `admitAt` ran `admissionFold`, which refused txs when they were enqueued (capacity, `SWAP_PRICE_TICKS_MISMATCH`, ...) | FIXED (cc7154c). `admit`/`admitAt` only queue; frozen Accounts stay suppressed (og tx-effects). 300 random batches match og `applyAccountEnqueue`, plus j-claim admission. Tests that relied on refusal at enqueue time were updated in account-consensus, entity-runtime, j-layer, orderbook-watchtower and runtime-2. |
| proposeAccountsNow | `handlers/account/propose-accounts-now.ts` + `assertProposeAccountsNowMatchesState`: active leader, version 1, 1..1000 lowercase, strictly ascending ids; re-emits a clone of `pendingAccountInput` per counterparty | missing; the rewrite kept no `pendingAccountInput` bytes | FIXED (a6bc7d2). `Candidate.sent` keeps the exact ack_frame (ack + frame + disputeHanko). It is excluded from the root, as in og. 300 random markers match og's error codes, and the re-emitted bytes equal the sent ack_frame. |
| `EntityState.orderbookExt` + root section | `OrderbookExtState` {books, orderPairs (derived), pairDimensions, referrals, hubProfile}; `state-root.ts projectOrderbookConsensusState` = {books: pairId→`computeBookCommitmentHash`, pairDimensions, hubProfile, referrals} | missing | FIXED (ec80f1e). `orderPairs` is derived and never stored. 40 random ext states (books built by random commands) match og `computeCanonicalEntityConsensusStateHash`. |
| `initOrderbookExt` | `system/basic.ts handleInitOrderbookExtEntityTx`: no-op when the ext exists or the spread does not sum to 10000; `ORDERBOOK_USD_QUOTE_AUTHORITY_INVALID` halt | missing | FIXED (ec80f1e). 200 random txs match og's no-op, halt and hubProfile. |
| Account swap outputs | `account/tx/same-j-swap-output.ts`: swap_offer → `swapOfferUpsert`; swap_resolve → upsert or `swapOfferRemove`; swap_cancel_request → `swapCancelRequest`; `committed-input.ts` turns them into swapOffersCreated / Cancelled / CancelRequests | swap_offer emitted nothing; the Entity dropped swap effects | FIXED (b65b44f). New `swap_offer_upsert` effect (post-transition snapshot). Committed frames' swap effects are collected per Account into `Draft.swaps`. |
| Peer frames carrying swap txs | accepted (profile) | `entityAcceptsPeerTx` refused them (`not_l0`) | FIXED (b65b44f). swap_offer / swap_cancel_request / swap_resolve are accepted. |
| Post-tx phase | `application.ts applyPostEntityTxPhases`: `applyCommittedSwapCancelsToOrderbook` → `applySwapCancelRequests` (`processOrderbookCancels`, per-tx enqueue) → `applyOrderbookMatching` (`collectOffersForMatching`, `processOrderbookSwaps`, `admitOrderbookAccountTxBatch`, trade-count regression, `replaceOrderbookPair`) → proposals | missing | FIXED (b65b44f). `bookPhase` runs in `foldTxs` before `proposeAccounts`, so proposer and validators replay it identically. Halts are `entity_invariant` with og's messages. An integration test runs 3 entities: a maker rests, a crossing taker fills at the resting price, both resolves commit and the book empties; a cancel request clears a resting offer. |
| Same-j matcher | `handlers/account/orderbook/{index,queue,helpers,same/*}.ts`: materialization rejects, pair-dimension lock, ±30% band plus one out-of-band sweep per pair, keep-identical/drain, STP/post-only/IOC/FOK, fee-authority check (`fee-authorization-exceeded`), taker fee from `swapTakerFeeBps`, USD reference ask, resume of touched pairs, `queueUniqueSwapResolve` | only the book primitives existed | FIXED (b65b44f). 40 random streams × 8 passes match og `processOrderbookSwaps`, `processOrderbookCancels` and `applyCommittedSwapCancelsToOrderbook`, comparing exact swap_resolve txs, book commitments, book orders and pair dimensions. swap_resolve now carries og's `comment`, `restingGiveTokenId` and `restingWantTokenId`, which enter the frame hash. |
| Cross-j book | `processCrossJurisdictionOrderbookOffers`, cross fills, `routeRemoteCrossJurisdictionBookCancels`, cross-j cancel instruction, `markCrossJurisdictionBookAdmissionClosed`, risk caps (`getTypedCrossJurisdictionBookAdmissionFailure` risk_reject, the $6.5m USD boundary) | none | REMAINING. After the merge `orderbookExt` and `crossJurisdictionSwaps` / `crossJurisdictionAuthorizations` are live state, but `crossJurisdictionBookAdmissions` is still only a committed radix section, and og's cross pass (orderbook/cross/*, handlers/cross-j/book-order.ts, cross fills, remote cancel routing, admission-closed marking, risk caps) is a whole subsystem beyond a quick wire-up. A cross-j offer is skipped by the same-j pass, as og does. |
| SwapMatched runtime event | `commitOrderbookMatchResult` pushes a `runtimeEvent` candidate effect | emitted in bookPhase into Draft.runtimeEvents (tradesMatched) | FIXED. MATCH: the count equals og's over og's own bookUpdates in the 40 random hub streams, and the hub's matching frame emits it once |
| `PROPOSE_ACCOUNTS_NOW_EXTERNAL_INGRESS_REJECTED` | runtime ingress refuses an external proposeAccountsNow | not checked at ingress | REMAINING. This is a runtime-layer check owned by the runtime tx-kinds agent. The entity handler is FIXED. |
| og consistency halts on swap outputs | `ACCOUNT_SWAP_OUTPUT_MISSING/KIND_MISMATCH/UNCONSUMED`, `classifyCommittedSwapCancels` | n/a | NOTE. The rewrite derives the events from the committed transition itself, so these cannot diverge; og's cross-j cancel request also lands in swapCancelRequests, the same as here. |
| `debugRebuildProjectionOnly` | storage rebuild mode of the matcher | n/a | NOTE. It is not consensus. Live mode is ported. |
