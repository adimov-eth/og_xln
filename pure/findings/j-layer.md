# j-layer: og vs pure/xln.ts

Tests: `pure/diff/j-layer.test.ts` (17 tests). Run it from `pure/` with `bun test diff/j-layer.test.ts`. They use a seeded PRNG. Every MATCH test runs live og code from `core/` or the typechain ABI. The two PORT tests cover og functions that are module-private or that need og's runtime jurisdiction registry.

## Findings

| id | severity | og file | rewrite xln.ts | what differed | proven by test | status |
|---|---|---|---|---|---|---|
| J1 | high | `core/jurisdiction/adapter/events/*` (`j-event-payloads.ts`, `depository-event-codec.ts`), `machine/event-catalog.ts` | `J_EVENT_SIGNATURES`, `readJEvents` ~674 | Ingress covered only some Depository events. Missing: `DebtCreated`, `DebtEnforced`, `DebtForgiven`, `CounterDisputeRegistered`, `HashLadderRevealRegistered`, the EntityProvider events, and og's per-event normalizers and refusals. | MATCH: signatures/topic0 for every consensus event; 100 random logs vs `rawEventToJEvents` | FIXED |
| J2 | high | `core/jurisdiction/adapter/rpc-public.ts` (`decodeDispute*EvidenceCalldata`, `resolveDispute*Evidence`) | `decodeBatch`, `disputeProofEvidence`, `finalizationEvidence`, `withDisputeCalldata` ~806 | There was no `initialProofbody` / finalization evidence from `processBatch` or `watchtowerCounterDispute` calldata. | MATCH: decodeBatch vs `decodeJBatch` (safe-integer refusals included); evidence vs og on 60 rounds; watchtower pair | FIXED |
| J3 | high | `core/account/j-claims/j-claim-accumulator.ts`, `j-claim-transition.ts` (`prepareAccountJClaimTx`) | `JClaimProof`, `claimTree` / `claimPath` ~2418, `stampClaims` ~2648 | Proofs had no branch nodes. The proposer could not include the finalizing second `j_event_claim` in the same frame. Events were not in og metadata order and duplicate keys were not refused. | MATCH: 30 random multi-claim frames from either side vs `prepareAccountJClaimTx` + `handleJEventClaim` + `computeFrameHash`; a tampered proof is refused like `verifyAccountJClaimProof` | FIXED |
| J4 | medium | `core/account/settlement/*` (`activatePostSettlementProof` per finalized claim) | `promoteSettled` ~2595 (`finalized` list) | When two claims finalized in one frame, only the last jNonce was considered, so og's extra `nextProofNonce` bump was missed. | MATCH: AT-10c in `account-tx.test.ts` | FIXED |
| J5 | medium | `core/account/consensus/index.ts` (stale settlement Hanko, `ACCOUNT_INPUT_FRAME_STALE_SETTLEMENT_HANKO`) | `staleHankoNonce` / `minimumSafeNonce` ~2804, `planWindow`, `admitPeerFrame` ~3070 | A refreshable stale settlement Hanko halted the proposer instead of being deferred for retry. On receipt it was reported as `dispute_required` instead of the plain stale-Hanko reject. | MATCH: AT-10b in `account-tx.test.ts` | FIXED |
| J6 | high | `core/entity/tx/j-events.ts`, `j-events-account-settled.ts`, `j-events-account.ts` (`mergeJEventClaimOps`), `j-events-observations/*` | `entityJEvents` ~833, `observeJBlocks` ~912, `applyDebtEvent` ~864, `withBatchNonces` ~823 | There was no Entity-side handling of J events. Missing: AccountSettled expansion per Entity and token, own-reserve updates, claim suppression for missing or non-active Accounts, the claim merge and order, and the debt ledger. | MATCH: 80 logs vs `rawEventToJEvents`; 40 blocks vs og handlers + `mergeJEventClaimOps`; 60 debt sequences vs `applyDebtJEvent`. PORT: `enrichDisputeBatchNonces` | FIXED |
| J7 | medium | `core/entity/tx/j-events.ts` (`resolveFinalizationEvidence` → `external_finality`) | `disputeStartedInput` ~942, `disputeFinalizedInput` ~952 | There were no builders turning DisputeStarted / DisputeFinalized into Account `external_finality` inputs. | none (og helper is module-private and needs the full `applyJEvent` pipeline) | PARTIAL: builders ported. REMAINING: no live og test; not yet wired into EntityTx (the entity-tx agent owns that dispatch); og proof-body contract-limit checks skipped |
| J8 | high (ER-16) | `core/jurisdiction/machine/batch/{index,reserve-simulation}.ts`, `entity/tx/handlers/j-batch/{r2r,r2c,r2e,e2r,j-broadcast,j-batch-reserve-admission}.ts` | `JBatchState`, `simulateBatchReserves` ~1015, `reserveCandidateIssue` ~1098, `queueR2R/R2E/E2R/R2C` ~1123, `takeBroadcastBatch`, `jBroadcast` ~1161; Host `JOp` / `JState` / `applyJ` ~4066 | `applyJ` moved reserves at once, checked raw reserve instead of debt-aware spendable reserve, and had EXTRA `c2r` plus a local escrow. og only queues ops into `jBatchState`. `j_broadcast` seals the batch at `entityNonce+1` (dispute work first, recovery batches first) for the quorum Hanko, and reserves move only through finalized J events. | MATCH: 400 drafts vs `simulateDraftBatchReserveAvailability`; 25×60-op sequences vs `handleR2R` / `handleR2C` / `handleR2E`; 200 drafts vs og `takeBroadcastBatch`; batch hash vs `computeBatchHankoHash(encodeJBatch)`; Host ER-16 test. PORT: `handleJBroadcast` state commit | FIXED. `c2r` and escrow removed (nothing depended on them) |
| J9 | medium | `core/entity/tx/j-events-batch.ts` (`applyHankoBatchProcessedEvent`) | `applyHankoBatchProcessed` ~1182 | There was no HankoBatchProcessed handling. Missing: finalizing the pending batch, quarantining it when a different hash consumes its nonce, the nonce sync, and the auto-broadcast follow-up. | MATCH: 60 lifecycles vs `applyHankoBatchProcessedEvent` (exact, other hash, lower / higher nonce, other Entity; `autoBroadcast` equals og's queued `j_broadcast`) | FIXED |

## Remaining (not in this pass)

- `flushDeferredHashLadderReveals` (cross-j / HTLC area) is not called from `jBroadcast` or `applyHankoBatchProcessed`. REMAINING, and today it changes no behaviour: it only walks the `crossJurisdictionSwaps` routes, which no rewrite state holds yet. It must land together with the Entity cross-j collections. See cross-j.md, wave 3.
- The `r2c` rebalance-fee path (`rebalanceQuoteId`) is not ported. That needs the account rebalance quote state.
- `j_rebroadcast`, `j_abort_sent_batch`, `j_clear_batch` and `mintReserves` are not ported.
- The Host drops `jBroadcast`'s `jTx`, its `hashToSign`, and `applyHankoBatchProcessed`'s `autoBroadcast` flag. HostEffect has no J-submit or quorum-sign effect yet, and that is the runtime agent's area. The sealed batch stays in `jBatch.sentBatch`.
- The Host's `j_event` updates reserves, debts and jBatch only. The `j_event_claim` ops that `observeJBlocks` produces are not yet admitted into the Account mempool.
- `simulateBatchReserves` keeps og's `DRAFT_BATCH_DEFICIT_WITHOUT_ORIGIN` invariant throw. It cannot be reached.
