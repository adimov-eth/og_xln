# consensus-final: Entity/Account consensus, admission, boards, orderbook and wire shapes

og (core/ at the pinned checkout) is the authority. New tests are in `pure/diff/consensus-final.test.ts` unless another file is named; every one runs og live and uses a seeded prng.

| ID | og | Rewrite | Finding | Test | Status |
|---|---|---|---|---|---|
| CF-1 (cross-j 48) | entity/consensus/proposal/start.ts `assertEntityEncryptionKeypair` | `startProposal`, `replayFrame` | The Entity encryption keypair was checked only on inbound HTLC paths. og checks it on every proposal and replay. | MATCH: 200 random frames with good, missing, mismatched and malformed keypairs | FIXED |
| CF-2 (EJ-R3 profile) | entity/profile/profile-descriptor.ts `computeEntityProfileHash` | `entityProfileHash`, `profileHashToSign` in `foldTxs` | There was no profile hash. The frame now signs the descriptor hash at genesis, whenever it changes, and always on a handover frame. The descriptor is capped at 960,602 bytes and capabilities are ranked like og. | MATCH: 150 random states, plus the genesis / changed / handover frame test | FIXED |
| CF-3 (EJ-R4, EJ-5) | ethers `Transaction.from` for type 3 (blob) and type 4 (set-code) | `parseEvmTx`, `evmBlobSidecar`, `evmAuthorizationList` | Types 3 and 4 were refused. Blob sidecars (4844 and 7594 network forms, versioned hash = sha256(commitment) with byte 0 = 1) and authorization lists are now parsed. ethers does not check high-s on authorization rows, and neither does the rewrite. | MATCH: 1500 random typed txs | FIXED |
| CF-4 (EJ-R3 policy) | entity/consensus/proposal/policy.ts `selectProposableEntityTxs`, `isSelfBoardAuthorityTransitionFrame`, `findCounterpartyBoardActivationConflict`; jurisdiction/machine/range-budget.ts | `selectProposable`, `selfAuthorityTransitionFrame`, `counterpartyBoardActivationConflict`, `jRangeBudgetPrefix`, `frameTxBudgetPrefix` | The proposer did not isolate handover frames. Selection now works like og: a self board range isolates itself, plus its one handover when it moves off the config board; an uncertified board proposes nothing; counterparty activations take priority; then the 10 MiB J-range budget and the 10,000-tx / 50 MB frame budgets apply. A validator refuses a mixed counterparty-activation frame (`PROPOSAL_COUNTERPARTY_BOARD_ACTIVATION_MIXED`). | MATCH: 400 conflict frames; 300 random mempools covering every og reason and error; multi-MiB J-range cases | FIXED |
| CF-5 (EJ-R3 authority) | entity/consensus/authority/board-handover.ts `getEntityFrameConsensusConfig`, `withBoardAuthority`; proposal/start.ts | `authorityReplica`, `frameQuorum`, `handoverLeaderState`, `proposeSelected`, `preauthenticate`, `heldQuorum` | Frames were signed and verified under the committed quorum. A handover frame now uses the handover config: leader validators[0], view 0. Precommits and the quorum use the post-frame board. The proposal goes to the new board, and a single-signer handover commits and notifies the retired board. | entity-j.test.ts RJ-10 (66 runs); boards.test.ts ER-4b; the full suite | FIXED |
| CF-6 (EJ-R3 frame hash) | entity/consensus/frame.ts `canonicalJEventDataForFrameHash` | `jEventDataForHash` in `entityTxForHash` | The frame hash committed raw j_event data. It now commits og's canonical range projection (version tag, lowercased text, integer heights, canonical events) and refuses a range without `blocks` or `rangeHash` (`ENTITY_FRAME_J_EVENT_RANGE_REQUIRED`). | MATCH (og createEntityFrameHashFromStateRoot): 400 random ranges | FIXED |
| CF-7 (EJ-R5) | entity/factory.ts `toBoardEntityId`; handover config validation | `quorumOfConfig`, `configBoardHash`, `entityBoardHandover` | A nested Entity validator (a 32-byte id) was refused with `BOARD_HANDOVER_ENTITY_VALIDATOR_NOT_PORTED`. It now joins the quorum. A nested proposer is refused as og refuses it. | entity-j.test.ts RJ-10 variants `nested` (ok) and `nestedFirst` (refused) | FIXED |
| CF-8 (SJ-18) | account/tx/handlers/settlement/transition.ts `verifySettlementHankoHankos`; entity/account/account-consensus-context.ts | `SettlementCtx.registeredBoardHash`, `hankoWorkspace`, `admitPeerFrame` | Settlement Hankos were verified without authority. The post-settlement proof is now checked under the source's certified board with `allowPreviousBoard: true`, and the settlement Hanko with `false`. The receiver passes its `counterpartyCertifiedBoard`. When no board is passed, og resolves one from the source's local replicas, which in the rewrite is the Runtime `Verify`'s job. Proposal selection already withholds frames from an uncertified self board, so og's MISSING/MISMATCH throws cannot occur on the proposer side. | account-tx.test.ts "SJ-18": og and the rewrite get the same (hanko, authority) calls, with and without a registered board | FIXED |
| CF-9 (RR-12) | og commits a received Account frame at once | `answerFrame` inside the `ack_frame` accountInput | Stale. The rewrite answers and commits a received frame in the same Entity tx, so no Account is ever `received` when a refresh hook reads it. | 40 random payment rounds between three Entities: after every Runtime step no Account is `received`, and each ack_frame leaves the non-proposing receiver's head at the frame height | FIXED |
| CF-10 (stale rows) | T3-9, SJ-17, orderbook-watchtower 19/20, ER-4/14/15/18/24, AC-13, AC-13b, H7, entity-lane 30, cross-j 46/59, entity-consensus-2 31/32/34/37, account-tx 19/20/22/29/55, RJ-10 | n/a | Already ported by earlier merges. Each row now cites its MATCH test in its own findings file. | as cited in each file | FIXED |
| CF-R1 (EJ-R3 j-prefix) | entity/consensus/j-prefix/*, jurisdiction/machine/history/j-prefix-consensus.ts `assertFrameJPrefix` (about 1,500 lines) | `jPrefixAttestationsInput`, `ensureLocalJPrefix`, `jPrefixSelection`, `assertProposalPrefix`, `jPrefixFrameChecks`, `afterCommit`; EntityFrame `jPrefixCertificate`, replica `jPrefixRound`, `EntityContext.jHistory` | og requires a `jPrefixCertificate` on every frame that carries a j_event (`J_PREFIX_CERTIFICATE_MISSING`), and on every frame of a registered Entity. Each validator checks the certificate against its own replica-local `jHistory` and runs an attestation round in `jPrefixRound`. The rewrite accepted j_event frames without a certificate. It now runs og's whole J prefix: the attestation lane, local head signing, the round certificate and certified j_event at proposal (a missing required certificate blocks selection, a frozen base rolls an empty frame), the proposer and validator range and prefix checks (reject, defer on a lagging history, catch-up tolerates a stronger local prefix), the certificate in the frame hash, pruning and re-signing after commit. See runtime-final.md RF-11. | runtime-final.test.ts "the per-frame J prefix": four randomized MATCH tests of the core against og j-prefix-consensus.ts, and "40 validator Runtimes run the J prefix through Entity consensus" (committed certificate, certified j_event and frame hash equal og's; the local history is pruned like og) | FIXED |
| CF-R2 (cross-j 49/56) | dispute / cross-j recovery | n/a | Closed by the dispute agent (disputes-final.md). | diff/disputes-final.test.ts | FIXED (disputes-final.md) |
