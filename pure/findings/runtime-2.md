# runtime-2: Runtime input model, Runtime WAL, lending, oracle types

og (`core/` at 566c850) is the spec. Tests: `pure/diff/runtime-2.test.ts`; every MATCH test there runs live og.

| ID | og source | Finding | Test | Status |
|---|---|---|---|---|
| R2-1 | core/runtime/types.ts (RuntimeInput, RuntimeTx) | The rewrite's `RuntimeInput = create \| receive` host model is replaced by og's `{runtimeTxs, entityInputs, jInputs}`, with all 16 og RuntimeTx kinds typed by og field names. `RoutedEntityInput` carries og's `from`. | entity-runtime + runtime-2 suites | FIXED |
| R2-2 | core/runtime/tx/internal-tx-auth.ts, checkpoint/barrier.ts, command/frontier-auth.ts | Capability authorization: every kind except importReplica and importJ needs a process-local capability (the exact tx object, as og's Symbol markers do) or replay. The error codes are og's. | MATCH: all kinds vs `assertRuntimeTxCapabilitiesAuthorized`; marked vs cloned tx | FIXED |
| R2-3 | core/runtime/frame/intake (validateRuntimeInputShapeAndLimits, advanceAppliedRuntimeFrame) | Shape limits: the checkpoint barrier must be alone, plus the J-input limits. The height advances only when the frame did work. A RuntimeTx failure refuses the whole frame. | MATCH barrier-alone and no-work tests | FIXED |
| R2-4 | core/runtime/command/frontier.ts | recordRuntimeAdapterCommand: validation, pruning of expired lanes, contiguous sequence, capacity of 1024 lanes, observedHeight. | MATCH: 600 random markers vs `applyRuntimeAdapterCommandMarker` (code + frontier map) | FIXED |
| R2-5 | core/entity/consensus/input/merge.ts | mergeEntityInputs: lane keys (base / tx-origin / precommit), exact-replay skip, accountInput dedup, and precommit bundle merge. Duplicate-signer and equivocation refuse the frame. A conflicting proposal is appended last. | MATCH: 500 random batches vs og `mergeEntityInputs` | FIXED |
| R2-5b | merge.ts runtimeOutput / cross-j / j-prefix / leader-vote lanes, scheduledWake priority, j_event dedup | Ported: see runtime-final RF-13 (and RF-11 for the j-prefix lane). | runtime-final "Entity-input lanes" MATCH (randomized, 600 batches vs og mergeEntityInputs) | FIXED (runtime-final RF-13) |
| R2-6 | core/runtime/tx/tx-handlers.ts importReplica | Checks in og order: identity, jurisdiction binding, board membership, proposer flag, numbered→evidence, lazy id = hashBoard(encodeBoard). Validator resolution covers bytes32, EIP-55, and a 33/65-byte public key. Also the seed format, X25519(HKDF) key vs siblings, and the retained seed. The replica is then reused, imported from a checkpoint, or created at genesis. | MATCH: 600 random boards vs `hashBoard(encodeBoard)`; key vs og derive fns; flow codes | FIXED (lazy entities) |
| R2-6b | importReplica numbered entities; profile/position gossip | Numbered registration evidence, profile and position side effects. | code; runtime-final.test.ts genesis MATCH | FIXED (runtime-j.md RJ-8 evidence; runtime-final.md RF-1 profile/position) |
| R2-7 | tx-handlers.ts: observeJRange, advanceJWatcherCursor, rewindJHistory, retryJSubmit, recordJSubmitResult, record/resolveNumberedRegistrationIntent, recordAuthenticatedJAuthority, EntityProvider action x2, recordGovernanceJSubmitResult, importJ, completeImportJ | These kinds are typed and capability-checked, and the handler returns `runtime_tx_unsupported`. | runtime-j.test.ts MATCH suites | FIXED (stale: runtime-j.md RJ-1..RJ-9, runtime-final.md RF-2) |
| R2-8 (ER-22) | core/storage/hashes.ts, canonical-hash.ts, replica-meta-digest.ts, wal/outbox-payload.ts, docs/wal.md | The WAL row is og RuntimeFrame. It covers computeStorageFrameHash (0x03-msgpack integrity digest, domain `xln.storage.frame`, sorted Entity hashes) and the canonical Runtime hash (keccak v2). It also covers postStateHash, per-component digests, the replicaMeta digest and the ordered outbox digest, chained from ZERO_FRAME_HASH. | MATCH: random vs all five og functions; a committed row rehashes under og | FIXED |
| R2-8b | storage/replica (certifiedFrameHeadDigest, lineage links, leader votes, J submit state), buildReplayVerifiableRuntimePostStateView components | The rewrite commits the replica-meta fields and Runtime components it holds, using og's formulas. og's other fields/components are absent. The outbox row bytes are the rewrite's EntityOutput, not og RoutedEntityInput. | code | PARTIAL (runtime-final RF-16). The replica-meta rows now commit og `leaderVotes`, `pendingLeaderCertificate` and `jPrefixRound`, and are byte-equal to og (MATCH). Three parts are REMAINING. (1) `certifiedFrameHeadDigest`: the rewrite replica keeps only `head` (height, frame hash), not og's `certifiedFrameHead` link (stateRoot, authorityRoot, leader, hashesToSign, collectedSigs, hankos, postAuthority); commit installation in the Entity consensus handlers would have to retain it. (2) A leader vote's `preparedFrame` encodes the rewrite PreparedFrame, not og's wire EntityFrame. (3) Outbox rows encode the rewrite EntityOutput, not og RoutedEntityInput (`entityTxs` / `proposedFrame` / `hashPrecommits` wire fields). (2) and (3) need og's EntityInput / EntityFrame wire codec, which is owned by the Entity consensus area. | |
| R2-9 (ER-23) | core/storage/read/verify.ts + replay | recoverRuntime checks the height chain, prevFrameHash, the canonical hash and the frame hash. It replays each input with replay capabilities, and the frameHash must reproduce. The outbox is bound positionally. The codes are og's STORAGE_VERIFY_*, STORAGE_REPLAY_POST_STATE_MISMATCH and STORAGE_RECOVERY_OUTBOX_MISMATCH. | tamper tests | FIXED |
| R2-10 (ER-17) | core/entity/tx/handlers/payments/lending.ts | Entity lendingOffer/Borrow/Repay/ClosePosition validate og's intent ids, terms and bps. They queue the account lending_* tx on the hub Account and wake validators[0]. A failure refuses the whole input. The host pool / applyLending is removed. | MATCH: 400 random txs vs og handlers; specific codes | FIXED |
| R2-10b | local-tx-admission.ts; committed-lending-followup.ts | og enqueue does not pre-apply, but the rewrite's admitAt does, so an unfunded repay is refused at enqueue (counted in the test). The hub-side lending follow-up and state.lending book are not ported. | counted in random test | FIXED (lending-hub.md LH-1, LH-4): admitAt queues without applying like og (an unfunded repay fails at the Account frame); the hub follow-up and state.lending book are ported |
| R2-11 | pure/oracle.test.ts | Branded TokenId/Hash: uses the tokenId() constructor and a Hash cast for og's 0x jBlockHash. | tsc 0 errors; 19 pass | FIXED |
