# runtime-final: Runtime transport, scheduling and events

og is the authority: core/ and jurisdictions/ at 566c850. The tests are in `pure/diff/runtime-final.test.ts` unless another file is named. Every MATCH test runs og live.

| ID | og | Rewrite | Finding | Test | Status |
|---|---|---|---|---|---|
| RF-1 | runtime/tx/tx-handlers.ts `buildGenesisReplica` / `buildCheckpointReplica` (R2-6b, boards RG-1 side effects) | `importBoundReplica` | The genesis Entity committed only the encryption key and the J height. og also commits the default profile (`profileName.trim()` or `Entity <last 4>`, `isHub: false`), the crontab, the jurisdiction's default swap pairs (Tron stacks add TRX/SUN), and empty nonces, proposals, reserves, deferred proposals and book admissions. The genesis root therefore differed from og's. The rewrite now builds the same genesis state. `position` is validator-local (`ReplicaLocal.position`): genesis defaults it to the active jurisdiction or `default`, and a checkpoint import defaults it to the bound jurisdiction name. | MATCH (randomized): genesis and sibling roots equal `computeCanonicalEntityConsensusStateHash`; positions equal og's | FIXED |
| RF-2 | R2-7, settle-jsubmit SJ-14 / SJ-15 / SJ-16, boards RG-2: the J RuntimeTx handlers | `applyRuntimeTxStep` | Stale. Every handler now exists and runs og's rules. | runtime-j.test.ts: "importJ / completeImportJ / advanceJWatcherCursor sequences", "retryJSubmit / recordJSubmitResult frames", "watcher pages, reorgs ... rewinds", "MPT receipt proofs ...", EP attempt id; entity-j.test.ts for the numbered registration intents (EJ-4 / EJ-5) | FIXED (runtime-j.md RJ-1..RJ-9) |
| RF-3 | boards EP-6: Entity J outputs reach the Runtime J outbox | `splitJOutbox` / `registerPendingJOutbox` in `applyRuntime` | Stale. | runtime-j.test.ts "batches, governance proposals and maintenance jTxs split and register identically, across frames" | FIXED (runtime-j.md RJ-4) |
| RF-4 | book-admission `PROPOSE_ACCOUNTS_NOW_EXTERNAL_INGRESS_REJECTED` | `applyRuntime` ingress check | Stale. | runtime-j.test.ts "an unmarked proposeAccountsNow outside replay refuses the whole Runtime frame" | FIXED (runtime-j.md RJ-5) |
| RF-5 | boards RG-1 / R2-6b numbered importReplica | `importBoundReplica` numbered branch | Stale for the evidence check. RF-1 closes the profile and position side effects. | runtime-j.test.ts "a numbered importReplica needs registration evidence for its exact board" | FIXED (runtime-j.md RJ-8, RF-1) |
| RF-6 | observability/env-events.ts `publishEntityCandidateEffects` (`env.emit` of committed `runtimeEvent` effects); commit/finalization.ts `installCommittedState` | `RuntimeStep.events`, `CommitEffects.runtimeEvents` | The rewrite had no Runtime event channel: committed Draft `runtimeEvents` were dropped. `RuntimeStep.events` now carries every committed frame's runtime events, published once on each committing validator replica, in commit order. Frames that only propose publish nothing. | "og semantics: runtime events publish only at commit, once on every committing validator replica"; the draft-level events are MATCHed against og in entity-cross-j (Htlc*), lending-hub (HtlcReceived/Finalized), followup-order, scheduler-disputes (AccountOpening), book-admission (SwapMatched) and entity-j "Entity-level j_event" (JEventReceived through og `applyEntityTx`) | FIXED |
| RF-7 | account/tx/handlers/j-events/claim.ts `account_settled_finalized_bilateral`; account/tx/mutation.ts `request_collateral_committed` | Account effect `account_settled_finalized_bilateral`, `accountRuntimeEvents` | The bilateral finalization of a `j_event_claim` emitted no event. It now emits og's event with the first settled token's row after finality. Both Account events map to og's payload from the local side. | MATCH (randomized): claim sequences through og `handleJEventClaim`; request_collateral through og `applyAccountTxMutation` | FIXED |
| RF-8 | account/tx/handlers/* `ApplyAccountTxResult.events`; account/consensus (proposal/finalize.ts `🚀`, index.ts `🤝`, incoming/ack-commit.ts `✅` and stale ACK, collision.ts LEFT-WINS / ROLLBACK / `📥`, replay.ts re-sent / rebuilt ACK, preflight.ts stale frame, board-hanko-refresh.ts `🔐`); entity committed-input.ts / application.ts `addMessages` (entity-txs-3 T3-10) | `accountTxMessages`, `frameTxMessages`, AccountOutput `message`, `routedRaw` | The Account machine said nothing, so the Entity frame certified none of og's Account lines. Each Account tx now has og's handler lines (from the proposer's side, perspective-aware for payments), and the Account machine emits og's consensus lines as `message` outputs. The Entity turns them into certified frame events in og's order: ACK lines, then the frame's replayed handler lines and `🤝 Accepted frame`, then `🚀 Proposed frame` from the proposal phase. | MATCH (randomized): account-tx.test.ts lockstep sequences for payments (both sides), HTLC, swaps, rebalance, lending (random side) and settle_transition against og handler `events`; the j_event_claim sequence in this file (retained / idempotent / stale / finalized). MATCH: account-consensus.test.ts empty frame, LEFT and RIGHT collision, duplicate frame (re-sent / rebuilt) and stale ACK against og `applyAccountInput` `events`; entity-consensus-2 board refresh | FIXED |
| RF-9 | account/consensus/helpers.ts `runPostFrameAutoRebalanceCheck` (index.ts after a received frame commits, ack-commit.ts `queuePostAckWork`) | `postCommitRebalance` in `commitOwn` / `createAck`; `DoorContext.autoRebalance` = no `hubRebalanceConfig` | After a frame commits, the rewrite never queued og's automatic `request_collateral` (only `setRebalancePolicy` did). The commit now appends og's `checkAutoRebalance` txs to the Account mempool on a non-hub Entity, and says `🔄 Auto-rebalance queued n tx(s) after ACK commit` or `... after frame commit`. | MATCH (randomized): "a full round says og's lines ... queues exactly og runPostFrameAutoRebalanceCheck's request_collateral" (hub and non-hub, random deltas, fee terms and policies) | FIXED |
| RF-10 | runtime/mempool/wake.ts generateHookPingsWithDeps; scheduled-wake.ts createDueScheduledWakeInputs, nextReplicaDeadline, assertScheduledWakeTxAuthorized; entity consensus `lastConsensusProgressAt` writes (scheduler-disputes row 9) | `runtimeWake`, `forgedIngress`, `consensusProgressed`, ReplicaLocal `lastConsensusProgressAt` | Nothing drove the Entity scheduler: the rewrite had localScheduledWake / localTimeoutVote but no Runtime tick, took an external scheduledWake at ingress, and never tracked a validator's last consensus progress. `runtimeWake(rt, now, queued)` now returns og's tick input: each due replica in (dueAt, entityId, signerId) order gets the leader's scheduledWake or a validator's timeout vote (none while one is queued), then the due J submit and EntityProvider action retries, plus the local marker set. Outside replay, a scheduledWake or proposeAccountsNow the tick did not mark is refused (`SCHEDULED_WAKE_EXTERNAL_INGRESS_REJECTED`). The Runtime records `lastConsensusProgressAt` at og's write points. | MATCH (randomized): 300 random Runtimes against og createDueScheduledWakeInputs (wakes, votes, queued skips, order); forged vs marked wake against og assertScheduledWakeTxAuthorized, and the marked wake's frame runs the due hook | FIXED |
| RF-11 | jurisdiction/machine/history/j-prefix-consensus.ts; entity/consensus/j-prefix/{prefix-round, prefix-input, prefix-validation}.ts; proposal/selection.ts, start.ts `assertProposalPrefix` / `shouldStartProposal`; proposal/input.ts and commit/catch-up.ts J checks; commit/finalization.ts `pruneReplicaFinalizedJHistory` + `runLocalPostCommitHooks`; leader `hasEntityLeaderWork`; input/merge.ts j-prefix lane (consensus-final CF-R1, settle-jsubmit SJ-14 consumer) | the J-prefix block after `applyEntityJEvent`, `jPrefixAttestations` EntityInput, `ensureLocalJPrefix`, `jPrefixSelection`, `heldSelection`, `assertProposalPrefix`, `jPrefixFrameChecks`, `preparedJPrefix`, `afterCommit`, `replicaJHistory`, `mergeKey` j-prefix lane | The validator-local J history was recorded but never used: frames carried any j_event with no certificate. The rewrite now ports og's J prefix end to end. Validators sign a head for the next Entity height when they have local consensus work (or on a J-prefix input) and send it to the other validators. The leader certifies the highest weighted common prefix and puts the certified j_event first in the mempool (replacing any other). A registered Entity, or a validator with a pending local J event, proposes nothing without a certificate, and a frozen base rolls one empty frame. The proposer checks its own ranges and prefix. Validators reject a range unlike their history, defer when behind (`PROPOSAL_J_PREFIX_HISTORY_WAIT` / `COMMIT_J_PREFIX_HISTORY_WAIT`), and catch-up tolerates a stronger local prefix. The certificate is in the frame hash and the wire frame. A commit clears the committed round, prunes the local history, re-signs due J work, and wakes the certified leader. Attestation inputs merge one per lane (`ENTITY_INPUT_J_PREFIX_MUST_BE_SPLIT` / `_EQUIVOCATION`). The round is frozen while a frame is held. og's defer is a `j_prefix` error with disposition `defer` (the rewrite has no defer queue). A watcher rewind into a range the locked frame signed is refused (`J_HISTORY_SIGNED_LOCK_REORG`). | MATCH (randomized): local attestations, round merges with tampering and equivocation, out-of-round votes, and 300 frames through `assertFrameJPrefix` + `getJEventRangeValidationError` against og; 40 three-validator Runtimes with random boards, histories, lag and forks: the committed certificate equals og `buildJPrefixCertificate`, the certified j_event og `buildCertifiedJPrefixTx`, every committed frame hash og `createEntityFrameHashFromStateRoot`, and the pruned history og `pruneFinalizedValidatorJHistory` ; 200 rewinds against a locked frame vs og `rewindJHistoryRuntimeTx` | FIXED |
| RF-12 | coordinator item: og genesis / importReplica defaults (profile, swapTradingPairs, paybook, crontab) | `importBoundReplica` | Already closed by RF-1: the genesis Entity root equals og `computeCanonicalEntityConsensusStateHash`. og's empty paybook commits nothing, so the roots agree. | RF-1 MATCH "profile name, swap pairs, crontab and position -- the genesis Entity root equals og computeCanonicalEntityConsensusStateHash" | FIXED (RF-1) |
| RF-13 | entity/consensus/input/merge.ts `mergeEntityInputs`: entityInputMergeKey runtime-output / cross-j-source-consumer lanes, effect boundaries, mergeJEventTxs, mergeExactAccountInputReplays, prioritizeScheduledWakeTransactions, applyCausalEntityInputOrder, prioritizeEntityConsensusInputs + hasVerifiedEntityCommitPrecertificate (runtime-2 R2-5b) | `mergeEntityInputs`, `mergeKey`, `laneKey`, `mergeJEvents`, `prioritizeConsensusInputs`, `verifiedCommit` | The rewrite merged all tx inputs of one Entity/signer into one envelope. It kept repeated J observations, did not order the scheduled wake, and put timeout votes and proposals in arrival order. Now each runtimeOutput is its own envelope, keyed by sender and exact tx, and is an effect boundary that ordinary lanes never merge across. A source-pull consumer is its own lane that runs after the ACK. A signed J observation is kept once per lane. Exact Account replays collapse. The scheduled wake runs first, and conflicting wakes refuse the frame (`SCHEDULED_WAKE_CONFLICTING_INPUTS`). Within one consensus race (Entity, signer, height) the order is: verified commit, then timeout vote, then unverified proposal. A verified commit replaces an unverified copy of the same frame. | MATCH (randomized): 600 batches of txs / J events / wakes / runtimeOutput envelopes / timeout votes / proposals against og `mergeEntityInputs` (refusal codes, lanes, order, tx contents) | FIXED |
| RF-14 | runtime/mempool/entity-inputs.ts `applyMergedEntityInputs` + `createDeferredProposalBatch`; admit/entity-input-output.ts `routeCommittedEntityOutputs`, `drainImmediateCrossJurisdictionOutputs`, `queueCommittedAccountWork` / `collectReadyLocalAccountWorkTargets`; entity/consensus/input/consensus.ts `trustedLocalRuntimeProtocol` (`cross-j` / `account-work`) and `isProposalDeferrableEntityInput`; proposal/selection.ts `shouldKeepPreparedEntityFrame`; frame/application.ts `crossJSetupPhase`; transition/cross-j-proposer-materialization.ts `selectCrossJOpeningAccountProposalTxs` (followup-order 10, cross-book 23, entity-lane 35-36) | `entityInputBatch`, `readyAccountWorkTargets`, `EntityContext.lane` / `siblings`, `trustedCrossCommand`, `accountWorkProposal`, `crossOpeningSelection`, `foldTxs` setup phase | Before this fix, every `txs` input proposed at once, and outputs only left in the outbox. A registering frame proposed its own queued Account legs, and an opening proposal ignored its sibling. The Runtime now runs og's cascade. (1) Plain `txs` inputs only admit (a non-leader still forwards), and each touched replica proposes once at the end, in first-touch order. (2) After every applied input, cross-j commands to a local replica are coalesced per Entity frame and applied in the same frame as trusted `cross-j` commands. A trusted command must be single-signer and proposer-held, and it takes exactly the command txs (after a certified J range, never through the mempool). If they are not all selected, or the frame does not commit, the frame is refused. (3) Every commit that account work did not cause pokes each ready local active validator with no frame in flight. The poke proposes only Account work, and an Entity frame that proposes no Account frame is discarded. Cycles and cascades past og's budgets refuse the frame. (4) A cross-j setup frame (materialize / register) proposes no Account frame, and one that also carries an Account transition is refused. (5) An Account holding a cross-j opening proposes exactly the cohort its sibling replica can match. It waits while the reciprocal is missing, and a missing sibling replica or Account halts like og. The flush and local-event inputs are not recorded in the applied RuntimeInput (og drops them from appliedEntityInputs). | MATCH (randomized): 800 opening Accounts vs og `selectCrossJOpeningAccountProposalTxs` (cohort, wait, halt codes); 300 Runtimes vs og `collectReadyLocalAccountWorkTargets`; og `isProposalDeferrableEntityInput` per lane; entity-lane scenario: the source user's command drains into the hub in the same frame, the registration queues legs without proposing, and the Runtime's account-work halts exactly as og does without the sibling hub | FIXED |
| RF-15 | runtime/frame/cross-j/atomic-admission.ts `markPotentialAtomicCrossJInputPairs`, `admitAtomicCrossJAccountInputs` (`coalesceExactAtomicProposalRetries`, `groupAtomicPairsFirst`), `atomicCrossJPairIndexesThatDidNotCommit`; delivery/topology/entity-routing.ts `selectPotentialCrossJAccountInputPairs`, `selectMatchedCrossJAccountInputPairs` (opening authorization, hash-ladder uniqueness, `removeRejectedCrossJAccountInputsByIndex`); admit/entity-input-atomic.ts `applyAtomicEntityInputPair`; frame/cross-j/evidence.ts `markCommittedAtomicCrossJAckOutputs`; entity/consensus/input/merge.ts provenance keys and `isExactTransactionReplay`; proposal/selection.ts `requiredEntityTx` (followup-order 9, cross-j-final 9, cross-book 22, entity-lane 34) | `RoutedEntityInput.runtimeId` / `sourceRuntimeFrame` / `atomicCrossJurisdictionPair`, `EntityOutput.atomicCrossJurisdictionPair`, `potentialCrossPairs`, `markPotentialCrossPairs`, `matchedCrossPairs`, `admitAtomicCrossPairs`, `markCommittedAckOutputs`, the atomic branch of `entityInputBatch`, `EntityContext.required` | Before this fix, routed inputs had no transport provenance, and any cross-j Account leg reached Account consensus alone. The rewrite now follows og. (1) Provenance: `RoutedEntityInput` carries og `runtimeId`, `sourceRuntimeFrame` and the cohort marker. The merge keys a runtimeOutput by sender, Runtime, source frame and tx. A marked leg is keyed by cohort and source frame; a missing source frame refuses the frame with ENTITY_INPUT_ATOMIC_CROSS_J_SOURCE_FRAME_MISSING. An exact replay must match the marker, origin, Runtime and source frame. `convertOutput` carries an output's marker. (2) Before the merge, a transported pair that is structurally complete is marked as one proposal cohort. (3) Admission. Transport retries of one cohort keep the latest delivery. Every cross-j Account leg must form an exact two-leg cohort: same phase and route set, same origin and source frame, sibling Entities, paired pulls and closes, and an authorized opening (identical `intent` authorizations on both siblings, exact participants, a valid prepared route, and one hash ladder per route across the Runtime). A hub reads ACK legs against its pending frame. Every other leg is stripped from its input, and a replay refuses the frame (RUNTIME_REPLAY_CROSS_J_ACCOUNT_PAIR_INVALID). Admitted pairs are ordered first, as adjacent marked inputs. (4) Application. Earlier deferred proposals flush first. Both legs then propose with their one accountInput required (RUNTIME_CROSS_J_ATOMIC_ACCOUNT_INPUT_NOT_SELECTED). Both must commit the expected Account frame. Otherwise both replicas roll back, each leg records CROSS_J_ACCOUNT_PAIR_NOT_COMMITTED, and the rest of each input is applied without the Account legs; a replay refuses the frame instead. (5) The two ACKs of a committed proposal pair (exactly one distinct output each) carry the pair's ACK marker, or the frame is refused. Divergences: og's security-incident telemetry is not kept, because it is observability rather than replayed state. A pair-leg failure other than an invariant is recorded as NOT_COMMITTED (og splits out PROTOCOL_REJECTED for malformed remote ingress). A rewrite Account keeps only its committed head, not the committed frame's txs. So an exact replay of an already-committed ACK counts as committed only when it carries its ACK cohort marker; og also requires that committed frame to hold a cross-j pull or close. | MATCH (randomized): 800 batches vs og `selectPotentialCrossJAccountInputPairs` (both frame policies) and `markPotentialAtomicCrossJInputPairs`; 800 batches over hub/spoke Runtimes vs og `selectMatchedCrossJAccountInputPairs` (pairs, rejected legs with reason and detail, retained inputs); 500 batches vs og `admitAtomicCrossJAccountInputs`, live and in replay; 400 outboxes vs og `markCommittedAtomicCrossJAckOutputs`; og `mergeEntityInputs` refuses a marked leg without a source frame. Runtime: a close pair that cannot commit rolls back both legs and re-applies the rest, a lone leg is stripped, and both replay paths refuse the frame | FIXED |
| RF-16 | storage/replica/replicas.ts `buildStorageLiveReplicaMetaCommitment` (runtime-2 R2-8b) | `replicaMetaRows` | Before this fix, the replica-meta rows dropped og `leaderVotes`, `pendingLeaderCertificate` and `jPrefixRound`, so the Runtime replica-meta digest differed from og whenever a replica held one. They are now committed in og's field order, with og's presence rule (an empty vote map still counts). Still absent: `certifiedFrameHeadDigest` (the rewrite keeps no certified lineage link), a vote's `preparedFrame` in og wire form, and og-shaped outbox rows (see runtime-2 R2-8b). | MATCH (randomized): 300 Runtimes. Every row key and value byte, and the digest, equal og `buildStorageLiveReplicaMetaCommitment`, with votes, certificates and J-prefix rounds present in more than 200 replicas | FIXED (R2-8b remainder REMAINING) |
