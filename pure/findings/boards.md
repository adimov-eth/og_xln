# Boards: certified-board registry, board binding, EntityProvider actions

og spec: `core/jurisdiction/machine/board-registry`, `core/hanko/signing.ts`, `core/entity/command`,
`core/entity/tx/handlers/{entity-provider-action,control-board-proposal}.ts`, `core/entity/tx/j-events-{board,entity-provider-action}.ts`,
`core/entity/tx/state-effects/board-rotation-hanko-refresh.ts`, `core/runtime/registration/*`. Tests: `pure/diff/boards.test.ts` (all MATCH, og run live).

| ID | Behaviour | og | Status | Notes |
|----|-----------|----|--------|-------|
| BR-1 | Certified-board registry: a Patricia trie of board records keyed by (stack, entity), with node hashing, records, proofs, finality and reachable nodes | jurisdiction/machine/board-registry | FIXED | `applyBoardRegistryEvent`, `lookupBoardRecord`, `boardProof`/`verifyBoardProof`, `advanceBoardFinality`, `reachableBoardNodes`. 150 random event sequences match og on state, new nodes, errors, lookups and proofs. |
| BR-2 | Entity registry: `certifiedBoardState` is committed in the root, and the nodes live outside it (`boardNodes`) | entity/tx/j-events-board.ts, state-root.ts | FIXED | `applyBoardJEvent` returns og's status message. The root matches `computeCanonicalEntityConsensusStateHash`. |
| ER-4b | Quorum board binding: the id must equal the lazy board hash, or the certified record must equal the board | hanko/signing.ts assertQuorumBoardBinding | FIXED | Enforced in `signManifest` (proposer and validator replay) and in `quorumHanko`. Fixtures in entity-runtime, entity-consensus-2 and oracle now use og-valid lazy ids. 200 random boards match og. |
| BR-3 | Entity command board: a certified record gives the epoch; a missing record is REQUIRED and a different board is CONFIG_MISMATCH | entity/command resolveEntityCommandBoard | FIXED | `commandBoard` reads `observerBoardRecord`. |
| EP-1 | `entityProviderTransfer` / `entityProviderReleaseControlShares`: pending intent, action hash, J output, `entityProviderAction` hashesToSign | entity/tx/handlers/entity-provider-action.ts | FIXED | Draft gained `hashes` and `jOutputs`. The hashes are signed in the frame manifest (sorted together with the Account hashes; a duplicate is fatal). `JurisdictionConfig.name` was added; it is not committed. 40 random runs match og. |
| EP-2 | `entityProviderCancelAction` | same | FIXED | The stored intent is re-validated (envelope, epoch, hash) as og does. |
| EP-3 | EntityProviderActionExecuted / Cancelled receipts | j-events-entity-provider-action.ts | FIXED | `applyEntityProviderActionJEvent`. |
| EP-4 | A pending action expires at a local BoardActivated with a newer epoch | j-events-board.ts | FIXED | Inside `applyBoardJEvent`. |
| EP-5 | `entityProviderProposeControlBoard` (CONTROL proposal hash, supporter Hankos verified against their certified boards) and `entityProviderActivateBoard` | control-board-proposal.ts | FIXED | `boardProposalHash`. 60 random proposals and activations match og, including a real 2-of-3 supporter Hanko. |
| EP-6 | The Entity's J outputs reach `RuntimeStep.jOutbox` | entity frame jOutputs, then the runtime J outbox | REMAINING | Only the committed draft carries `Draft.jOutputs`. Routing them needs an EntityOutput or Apply channel for J inputs, which belongs to the J-submit lifecycle owner. og's runtime lookup of the named J replica (`ENTITY_JURISDICTION_UNAVAILABLE`) is a host check. |
| AC-13b-recv | Account inputs get `counterpartyCertifiedBoard` from the registry | entity/tx/handlers/account/input-phases.ts | FIXED | `board_hanko_refresh` and the ack/preflight Hanko authority now use the sender's record; without a record the refresh still refuses with certified_board_missing. |
| AC-13b-send | Board-rotation refresh producer: on a local BoardActivated, mark Accounts `boardHankoRefreshMigration`, schedule the `board_hanko_refresh` crontab hook, then build at most 32 refresh AccountInputs plus accountFrame/dispute hashes per frame, and for a foreign activation schedule the 24h counterparty deadline hook | board-rotation-hanko-refresh.ts (468 lines), scheduler/board-hanko-refresh-hook.ts | FIXED | Ported with the Account head keeping both frame Hankos, og proposer lookup (counterpartyProposer) and the committed marker; see rebalance-refresh.md RR-4 to RR-8 (MATCH vs og executeCrontab, applyCertifiedBoardJEvent and scheduleChangedAccountBoardHankoRefreshes). |
| BH-1 | `boardHandover`: a frame of exactly [j_event, boardHandover] installs the activated board and resets leaderState | entity/tx/handlers/board-handover.ts + consensus frame config | FIXED | entity-j.md EJ-6 (MATCH, og run live); the consensus-side parts are FIXED in consensus-final.md (EJ-R3; the j-prefix round stays REMAINING) |
| RG-1 | Numbered `importReplica` with certified registration evidence (`env.infrastructure.certifiedRegistrationEvidence`) | runtime/registration/*, jurisdiction/machine/registration-evidence | REMAINING | Needs receipt MPT proofs, a runtime witness signature, JReplica contracts and confirmation depth, which are not in the rewrite Runtime (it has only `jurisdictions: Set<string>`). `importReplica` still refuses with NUMBERED_REPLICA_REGISTRATION_EVIDENCE_MISSING, which is og's verdict when no evidence is present. |
| RG-2 | RuntimeTx `recordNumberedRegistrationIntent`, `resolveNumberedRegistrationIntent`, `recordAuthenticatedJAuthority`, `retryEntityProviderAction`, `recordEntityProviderActionSubmitResult`, `recordGovernanceJSubmitResult` | runtime/registration/*, j-submit | REMAINING | These still return runtime_tx_unsupported. They are the same registration and J-submit subsystems as RG-1 and EP-6. |
