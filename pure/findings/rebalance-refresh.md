# rebalance-refresh: hub rebalance task, board Hanko refresh sending chain, lending_overdue hook

Spec: og (core/) is the authority. Tests are in `pure/diff/rebalance-refresh.test.ts`. Every test is a MATCH test that runs og live on randomized inputs.

| ID | Item | og source | Rewrite | Test | Status |
|---|---|---|---|---|---|
| RR-1 | Hub rebalance handler. It covers the tokenless raw override guard, stale sent-batch abort (`j_abort_sent_batch`, requeue), R2C ranking by amount/fee/time with fee-policy/prepaid checks funded from reserves (at most 10), submitted-at markers, C2R `settle_propose` (at most 10) and `settle_execute` for ready workspaces, and `j_broadcast` unless manual. The old invariant CRONTAB_HUB_REBALANCE_NOT_PORTED is removed. | entity/scheduler/rebalance.ts | `hubRebalance`, `addCommittedR2C`, `executeCrontab` | 400 random hubs vs og executeCrontab: outputs, jBatchState, lastRun, markers, halts | FIXED |
| RR-2 | Pending work for the periodic task (og ACCOUNT_WORK_REBALANCE plus sentBatch) | consensus/account/work-index.ts, scheduler/index.ts crontabTaskHasPendingWork | `rebalanceAccountIds`, `crontabTaskHasPendingWork(state, replicas)` | same test | FIXED |
| RR-3 | The wake's `j_broadcast` / `j_abort_sent_batch` as Entity txs | tx/handlers/j-batch/* | foldWake refuses with `J_BROADCAST_ENTITY_TX_NOT_PORTED` / `J_ABORT_SENT_BATCH_ENTITY_TX_NOT_PORTED` | none | FIXED in entity-j.md (EJ-1, EJ-2): the wake runs j_broadcast / j_abort_sent_batch as Entity txs |
| RR-4 | board_hanko_refresh hook. It covers the cursor, pending-only filter, at most 32 per frame, re-arm now (hasMore) or +60s (route unavailable), markers `issued` / `output-route-unavailable` / `bilateral-frame-uncertified` / `certified-frame-invalid` / `bilateral-*-dispute`, the refresh AccountInput and the accountFrame/dispute hashesToSign. The old invariant SCHEDULED_BOARD_HANKO_REFRESH_NOT_PORTED is removed. | scheduler/board-hanko-refresh-hook.ts, tx/state-effects/board-rotation-hanko-refresh.ts | `boardRefreshHook`, `refreshDraft`; `CrontabRun.sent/hashes` flow out through foldWake | 150 random hooks (with more than 32 Accounts) vs og executeCrontab: outputs, proposer, hashes, markers, hooks | FIXED |
| RR-5 | The Account keeps its own frame Hanko. og's currentFrameHanko / counterpartyFrameHanko are both read from the installed head certificate. | types/account.ts | `headHankos` (certifiedBy own/peer) | RR-4 test | FIXED |
| RR-6 | Proposer lookup: the peer frame Hanko is verified against the observer's certified board records, and the signer is the target claim's first member | entity/account/account-counterparty-route.ts | `counterpartyProposer`, `checkAccountHanko` (og validateBoardAuthority callback, `firstMember`) | RR-4 test (signerId of every og output) | FIXED |
| RR-7 | BoardActivated tail. A local activation marks certified Accounts `pending`, clears genesis markers, and arms or cancels the hook. A peer activation arms the 24h `counterparty-board-hanko-refresh` deadline and adds og's message. | tx/j-events-board.ts | `applyBoardJEvent(state, event, block, replicas, now)` returns accountReplicas | 120 random activations vs og applyCertifiedBoardJEvent: markers, hooks, messages | FIXED |
| RR-8 | Frame-end re-arm: a changed Account still needing its marker's refresh re-arms the hook for the latest activation, and a marker-only change never re-arms | scheduler/board-hanko-refresh-hook.ts scheduleChangedAccountBoardHankoRefreshes | `rearmBoardRefreshes` at the end of foldTxs | 300 random frame ends vs og | FIXED. The rewrite leaves our own Hankos out of the evidence tuple, because og attaches them only after commit and never inside one frame. |
| RR-9 | lending_overdue: the derived deadline for each active loan, then default, pool release, and a `lending_credit` revoke to the projected hub credit minus the principal (og projectedHubCreditLimit over the pending frame, mempool and earlier revokes). The revoke is admitted before the wake's self txs. | scheduler/derived-deadlines.ts, tx/handlers/account/committed-lending-close.ts, extensions/lending.ts | `derivedDeadlines`, `lendingOverdue`, `projectedHubCredit`, `CrontabRun.accountTxs` to foldWake `queueReturned` | 300 random lending books vs og collectDerivedDeadlines + executeCrontab: deadlines, book, revokes | FIXED over an og-shaped committed `lending` book. The hub-side book producer (og committed-lending-followup.ts: pools on lending_fund, loans on borrow, repay, close) is not ported, so only an imported og state carries loans. That belongs to the lending area. |
| RR-10 | Refresh output routing field | cross-j-outputs.ts buildAccountEntityOutput | EntityOutput `{to, tx}` has no signerId; `counterpartyProposer` gives og's signerId | RR-4 test | REPRESENTATIONAL: the output shape carries no signer. The proposer only decides the marker reason and is computed identically. |
| RR-11 | og REB_STEP debug candidate effects from the rebalance handler | scheduler/rebalance.ts | not emitted | none | REMAINING: debug-only runtime effects, outside state and hashes |
| RR-12 | A `received` Account (og commits a received frame at once). currentFrameOf reads the candidate frame while headHankos reads the previous head certificate. | types/account.ts | `refreshDraft` | not covered | REMAINING: tests use committed (`open`) Accounts. A refresh that fires while an Account sits in `received` would verify the peer Hanko against the wrong frame and mark it `output-route-unavailable`, which retries in 60s. |
