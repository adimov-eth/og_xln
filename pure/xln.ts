import { secp256k1 } from "@noble/curves/secp256k1";
import { x25519 } from "@noble/curves/ed25519";
import { gcm } from "@noble/ciphers/aes.js";
import { hkdf } from "@noble/hashes/hkdf";
import { sha256 } from "@noble/hashes/sha2";
import { keccak_256 } from "@noble/hashes/sha3";
import { bytesToHex as nobleHex } from "@noble/hashes/utils";
import { Packr, addExtension } from "msgpackr";


// ---- vocabulary: brands, tagged unions, Result ----

declare const __brand: unique symbol;
export type Brand<T, B extends string> = T & { readonly [__brand]: { readonly [K in B]: B } };
export type Flat<T> = { readonly [K in keyof T]: T[K] } & {};
export type Tagged<Tag extends string, Extra extends object = {}> =
  Tag extends unknown ? Flat<{ readonly _tag: Tag } & Extra> : never;
export type Of<T extends { readonly _tag: string }, K extends T["_tag"]> =
  Extract<T, { readonly _tag: K }>;
export type Eq<A, B> = [A] extends [B] ? ([B] extends [A] ? true : false) : false;
export const tag = <T extends string>(_tag: T) =>
  <X extends object = {}>(x: X = {} as X): Tagged<T, X> => ({ _tag, ...x }) as Tagged<T, X>;
/** `{k: v}` when defined, `{}` otherwise — absence and undefined are one value (§4.2). */
export const opt = <K extends string, V>(k: K, v: V | undefined): { readonly [P in K]?: V } =>
  (v === undefined ? {} : { [k]: v }) as { readonly [P in K]?: V };


export type Result<T, E> =
  | { readonly ok: true; readonly value: T }
  | { readonly ok: false; readonly error: E };
export const ok = <T>(value: T): Result<T, never> => ({ ok: true, value });
export const err = <E>(error: E): Result<never, E> => ({ ok: false, error });
export const assertNever = (x: never): never => { throw new Error(`unreachable: ${String(x)}`); };
export const map = <T, U, E>(r: Result<T, E>, f: (t: T) => U): Result<U, E> =>
  (r.ok ? ok(f(r.value)) : r);
export const chain = <T, U, E, F>(r: Result<T, E>, f: (t: T) => Result<U, F>): Result<U, E | F> =>
  (r.ok ? f(r.value) : r);
export const mapErr = <T, E, F>(r: Result<T, E>, f: (e: E) => F): Result<T, F> =>
  (r.ok ? r : err(f(r.error)));
export const guard = <E>(pass: boolean, e: E): Result<void, E> => (pass ? ok(undefined) : err(e));
export const unwrapOr = <T, E>(r: Result<T, E>, f: (e: E) => T): T => (r.ok ? r.value : f(r.error));
/** Folds left to right; the first refusal is the answer and later items are not visited. */
export const foldResult = <S, X, E>(
  xs: Iterable<X>, init: S, f: (s: S, x: X, i: number) => Result<S, E>,
): Result<S, E> =>
  [...xs].reduce<Result<S, E>>((acc, x, i) => (acc.ok ? f(acc.value, x, i) : acc), ok(init));
export const mapAccumResult = <S, X, Y, E>(
  xs: Iterable<X>, init: S, f: (s: S, x: X, i: number) => Result<readonly [S, Y], E>,
): Result<readonly [S, readonly Y[]], E> => {
  type Acc = readonly [S, readonly Y[]];
  const stepOnce = ([s, ys]: Acc, x: X, i: number): Result<Acc, E> =>
    map(f(s, x, i), ([next, y]) => [next, [...ys, y]] as const);
  return foldResult<Acc, X, E>(xs, [init, []], stepOnce);
};
export const mapAccum = <S, X, Y>(
  xs: Iterable<X>, init: S, f: (s: S, x: X, i: number) => readonly [S, Y],
): readonly [S, readonly Y[]] =>
  unwrapOr(mapAccumResult(xs, init, (s, x, i) => ok(f(s, x, i))), assertNever);
export const traverse = <X, Y, E>(
  xs: Iterable<X>, f: (x: X, i: number) => Result<Y, E>,
): Result<readonly Y[], E> => {
  const collect = (_: undefined, x: X, i: number) => map(f(x, i), (y) => [undefined, y] as const);
  return map(mapAccumResult(xs, undefined, collect), ([, ys]) => ys);
};
/** First refusal among eagerly evaluated checks, in order. */
export const checks = <E>(...rs: readonly Result<unknown, E>[]): Result<void, E> =>
  foldResult(rs, undefined as void, (_, r) => map(r, () => undefined));
type Values<R> = { readonly [K in keyof R]: R[K] extends Result<infer V, unknown> ? V : never };
type Errors<R> = R[keyof R] extends Result<unknown, infer E> ? E : never;
export const all = <R extends Record<string, Result<unknown, unknown>>>(
  cs: R,
): Result<Values<R>, Errors<R>> => {
  const collect = (acc: Record<string, unknown>, [k, r]: readonly [string, unknown]) =>
    map(r as Result<unknown, Errors<R>>, (v) => ({ ...acc, [k]: v }));
  return foldResult(Object.entries(cs), {}, collect) as Result<Values<R>, Errors<R>>;
};
/** The first answer among lazily asked questions; later questions are never asked once one answers. */
export const firstDefined = <T>(...asks: readonly (() => T | undefined)[]): T | undefined =>
  asks.reduce<T | undefined>((found, ask) => found ?? ask(), undefined);
/** A refusable step over a value that may be absent; absence passes through as absence. */
export const whenDefined = <T, U, E>(v: T | undefined, f: (t: T) => Result<U, E>): Result<U | undefined, E> =>
  v === undefined ? ok(undefined) : f(v);
/** `every` over a refusable predicate: the first `false` or the first refusal ends it; later items are not asked. */
export const everyResult = <X, E>(xs: Iterable<X>, pass: (x: X) => Result<boolean, E>): Result<boolean, E> =>
  foldResult(xs, true, (all, x) => (all ? pass(x) : ok(false)));
/** `some` over a refusable predicate: the first `true` or the first refusal ends it; later items are not asked. */
export const someResult = <X, E>(xs: Iterable<X>, pass: (x: X) => Result<boolean, E>): Result<boolean, E> =>
  foldResult(xs, false, (any, x) => (any ? ok(true) : pass(x)));


// ---- matching: exhaustive tables keyed by a discriminant ----

export const arm = <A extends object, K extends keyof A>(arms: A, k: K): A[K] =>
  (Object.hasOwn(arms, k) ? arms[k] : assertNever(k as never));
export const matchBy = <Key extends string, T extends Record<Key, PropertyKey>, R>(
  key: Key, value: T, arms: { [K in T[Key]]: (v: Extract<T, Record<Key, K>>) => R },
): R => arm(arms, value[key] as T[Key])(value as never);
export const match = <T extends { readonly _tag: string }, R>(
  value: T, arms: { [K in T["_tag"]]: (v: Of<T, K>) => R },
): R => matchBy("_tag", value, arms);
export const total = <K extends string, F>(names: readonly K[], f: F): { readonly [P in K]: F } =>
  Object.fromEntries(names.map((n) => [n, f])) as { readonly [P in K]: F };
export type Kinds<K extends string, Row> = { readonly [T in K]: Row };


// ---- replica grammars: which phase an event may move a replica to ----

type Table = { readonly [event: string]: { readonly [phase: string]: readonly string[] } };
export type Grammar = Readonly<{
  table: Table; replica: Tagged<string>; input: { readonly kind: string };
  ctx: { readonly [e: string]: unknown }; output: unknown; error: unknown;
}>;
export type Phase<G extends Grammar> = G["replica"]["_tag"];
export type Event<G extends Grammar> = keyof G["table"] & string;
export type At<G extends Grammar, P extends string> = Of<G["replica"], P>;
export type InputFor<G extends Grammar, E extends string> = Extract<G["input"], { readonly kind: E }>;
export type Next<G extends Grammar, P extends Phase<G>, E extends Event<G>> =
  P extends keyof G["table"][E] ? Extract<G["table"][E][P], readonly Phase<G>[]>[number] : never;
export type Apply<R, O> = { readonly replica: R; readonly outputs: readonly O[] };
export type Handler<G extends Grammar, E extends Event<G>, P extends Phase<G>> = (
  replica: At<G, P>, input: InputFor<G, E>, ctx: G["ctx"][E],
) => Result<Apply<At<G, Next<G, P, E>>, G["output"]>, G["error"]>;
/** A phase the table allows gets a handler; a phase it forbids gets the refusal to return. */
export type Cases<G extends Grammar, E extends Event<G>> = {
  readonly [P in Phase<G>]: P extends keyof G["table"][E] ? Handler<G, E, P> : G["error"];
};
export const grammar = <G extends Grammar>(_table: G["table"]) =>
  <E extends Event<G>, C extends Cases<G, E>>(_event: E, cases: C) =>
    (
      replica: G["replica"], input: InputFor<G, E>, ctx: G["ctx"][E],
    ): Result<Apply<G["replica"], G["output"]>, G["error"]> => {
      const branch = arm(cases, replica._tag as keyof C) as unknown;
      return typeof branch === "function" ? branch(replica, input, ctx) : err(branch as G["error"]);
    };
export const done = <R, O>(replica: R, outputs: readonly O[] = []): Apply<R, O> =>
  ({ replica, outputs });

const everyPhaseStays = {
  open: ["open"], proposed: ["proposed"], received: ["received"], preparing: ["preparing"],
  disputed: ["disputed"],
} as const;
const everyPhaseDisputes = {
  open: ["disputed"], proposed: ["disputed"], received: ["disputed"], preparing: ["disputed"],
  disputed: ["disputed"],
} as const;
export const AccountTransition = {
  propose: { open: ["open", "proposed"] },
  ack: {
    open: ["open"], proposed: ["open", "proposed"], received: ["open", "received"],
    preparing: ["preparing"], disputed: ["disputed"],
  },
  ack_frame: {
    open: ["open", "received"], proposed: ["open", "proposed", "received"], received: ["received"],
    preparing: ["preparing"], disputed: ["disputed"],
  },
  freeze: {
    open: ["preparing", "disputed"], proposed: ["preparing", "disputed"],
    received: ["preparing", "disputed"], preparing: ["preparing", "disputed"],
    disputed: ["disputed"],
  },
  dispute: everyPhaseStays,
  board_hanko_refresh: everyPhaseStays,
  resume: { preparing: ["open"] },
  external_finality: everyPhaseDisputes,
} as const;
export const EntityTransition = {
  txs: { open: ["open", "proposed"], proposed: ["open", "proposed"], locked: ["open", "locked"] },
  proposal: {
    open: ["open", "locked"], proposed: ["open", "proposed"], locked: ["open", "locked"],
  },
  precommit: { open: ["open"], proposed: ["open", "proposed"], locked: ["open", "locked"] },
  leaderTimeoutVote: {
    open: ["open", "proposed", "locked"], proposed: ["open", "proposed"],
    locked: ["open", "proposed", "locked"],
  },
  jPrefixAttestations: {
    open: ["open", "proposed"], proposed: ["open", "proposed"], locked: ["open", "locked"],
  },
} as const;


export const AccountTxNames = [
  "add_delta", "set_credit_limit", "payment", "htlc_lock", "htlc_resolve",
  "swap_offer", "swap_cancel_request", "swap_resolve", "settle_transition", "j_event_claim",
  "cross_pull_lock", "cross_pull_close", "request_collateral", "rebalance_refund", "rebalance_policy",
  "lending_fund", "lending_borrow_request", "lending_repay", "lending_credit",
  "lending_close_request", "lending_close_payout",
] as const;
export const LendingTxNames = ["lendingOffer", "lendingBorrow", "lendingRepay", "lendingClosePosition"] as const;
export const EntityTxNames = ["directPayment", "placeSwapOffer"] as const;
export const AccountInputKinds = ["dispute", "board_hanko_refresh"] as const;
export const EntityInputKinds = ["leaderTimeoutVote", "jPrefixAttestations"] as const;


// ---- layers: one step of a state machine, folded strictly or leniently ----

export type Step<S, Eff> = { readonly state: S; readonly effects: readonly Eff[] };
export const step = <S, Eff = never>(state: S, effects: readonly Eff[] = []): Step<S, Eff> =>
  ({ state, effects });
export type Layer<S, X, C, Eff, E> = (s: S, x: X, c: C) => Result<Step<S, Eff>, E>;
/** Strict fold: one refusal refuses the frame. */
export const strictFold = <S, X, C, Eff, E>(apply: Layer<S, X, C, Eff, E>) =>
  (s: S, xs: Iterable<X>, c: C): Result<Step<S, Eff>, E> => {
    const applyOne = (cur: S, x: X) =>
      map(apply(cur, x, c), (r) => [r.state, r.effects] as const);
    return map(mapAccumResult(xs, s, applyOne), ([state, effects]) => step(state, effects.flat()));
  };
export type Lenient<S, X, Eff, E> = Step<S, Eff> & {
  readonly included: readonly X[];
  readonly refused: readonly { readonly index: number; readonly error: E }[];
};
type LenientItem<X, Eff, E> =
  | { readonly _tag: "folded"; readonly x: X; readonly effects: readonly Eff[] }
  | { readonly _tag: "refused"; readonly index: number; readonly error: E };
/** Lenient fold: a refused item is skipped, the rest go on. */
export const lenientFold = <S, X, C, Eff, E>(apply: Layer<S, X, C, Eff, E>) =>
  (s: S, xs: Iterable<X>, c: C): Lenient<S, X, Eff, E> => {
    const applyOne = (cur: S, x: X, index: number): readonly [S, LenientItem<X, Eff, E>] => {
      const r = apply(cur, x, c);
      return r.ok
        ? [r.value.state, tag("folded")({ x, effects: r.value.effects })]
        : [cur, tag("refused")({ index, error: r.error })];
    };
    const [state, items] = mapAccum(xs, s, applyOne);
    const folded = items.flatMap((item) => (item._tag === "folded" ? [item] : []));
    const refused = items.flatMap((item) => (item._tag === "refused" ? [item] : []));
    return {
      state, effects: folded.flatMap((f) => f.effects), included: folded.map((f) => f.x),
      refused: refused.map(({ index, error }) => ({ index, error })),
    };
  };


// ---- immutable collections ----

export const mapSet = <K, V>(m: ReadonlyMap<K, V>, k: K, v: V): ReadonlyMap<K, V> =>
  new Map([...m, [k, v]]);
export const mapDelete = <K, V>(m: ReadonlyMap<K, V>, k: K): ReadonlyMap<K, V> =>
  new Map([...m].filter(([key]) => key !== k));
/** Adds d to the count at k; a count that reaches zero leaves the map. */
export const bump = <K>(m: ReadonlyMap<K, bigint>, k: K, d: bigint): ReadonlyMap<K, bigint> => {
  const v = (m.get(k) ?? 0n) + d;
  return v === 0n ? mapDelete(m, k) : mapSet(m, k, v);
};
/** Keeps the first item for each key, drops keys already taken; keyless items always stay. */
export const firstBy = <X, K>(
  xs: Iterable<X>, key: (x: X) => K | undefined, taken: Iterable<K> = [],
): readonly X[] => {
  const items = [...xs];
  const keys = items.map(key);
  const blocked = new Set(taken);
  const firstAt = new Map(keys.map((k, i) => [k, i] as const).reverse());
  const kept = (i: number): boolean => {
    const k = keys[i];
    return k === undefined || (!blocked.has(k) && firstAt.get(k) === i);
  };
  return items.filter((_, i) => kept(i));
};


// ---- canonical text: the deterministic spelling every hash in the rewrite is taken over ----

export const asc = <T extends string | number | bigint>(a: T, b: T): number => (a < b ? -1 : a > b ? 1 : 0);
const sortedBy = <X>(xs: readonly X[], key: (x: X) => string): readonly X[] =>
  xs.map((x) => [key(x), x] as const).toSorted(([a], [b]) => asc(a, b)).map(([, x]) => x);
const sortWith = <X>(xs: Iterable<X>, cmp: (a: X, b: X) => number): readonly X[] =>
  [...xs].toSorted(cmp);
const lengthPrefixed = (kind: string, body: string): string => `${kind}${body.length}:${body}`;
const countOf = (n: number): string => lengthPrefixed("d", String(n));
const canonMap = (m: ReadonlyMap<unknown, unknown>): string => {
  const rows = sortedBy([...m].map(([k, v]) => [canon(k), v] as const), ([k]) => k);
  return `m${countOf(rows.length)}${rows.map(([k, v]) => k + canon(v)).join("")}`;
};
const canonSet = (s: ReadonlySet<unknown>): string => {
  const rows = [...s].map(canon).toSorted(asc);
  return `S${countOf(rows.length)}${rows.join("")}`;
};
/** Undefined fields are absent, so `{a: undefined}` and `{}` spell the same. */
const canonObject = (o: Record<string, unknown>): string => {
  const keys = Object.keys(o).filter((k) => o[k] !== undefined).toSorted();
  return `o${countOf(keys.length)}${keys.map((k) => lengthPrefixed("s", k) + canon(o[k])).join("")}`;
};
/** Every value gets a type letter and a length, so no two different values share a spelling. */
export const canon = (v: unknown): string => {
  switch (true) {
    case v === undefined: return "u";
    case v === null: return "n";
    case typeof v === "boolean": return v ? "T" : "F";
    case typeof v === "bigint": return lengthPrefixed("i", v.toString());
    case typeof v === "string": return lengthPrefixed("s", v);
    case typeof v === "number": return lengthPrefixed("d", String(v));
    case v instanceof Map: return canonMap(v);
    case Array.isArray(v): return `a${countOf(v.length)}${v.map(canon).join("")}`;
    case v instanceof Set: return canonSet(v);
    case v instanceof Uint8Array: return lengthPrefixed("b", nobleHex(v));
    case typeof v === "object": return canonObject(v as Record<string, unknown>);
    default: return "u";
  }
};


// ---- hex, words and bytes ----

const nibble = (code: number): number => {
  const lower = code | 32;
  switch (true) {
    case code >= 48 && code <= 57: return code - 48;
    case lower >= 97 && lower <= 102: return lower - 87;
    default: return -1;
  }
};
export const hexBody = (h: string): string => (/^0[xX]/.test(h) ? h.slice(2) : h);
export const hexToBytes = (hex: string): Uint8Array => {
  const body = hexBody(hex);
  if (body.length % 2 !== 0) throw new Error(`odd hex length ${body.length}`);
  const byteAt = (_: unknown, i: number): number => {
    const hi = nibble(body.charCodeAt(2 * i));
    const lo = nibble(body.charCodeAt(2 * i + 1));
    if (hi < 0 || lo < 0) throw new Error(`bad hex at ${i}`);
    return (hi << 4) | lo;
  };
  return Uint8Array.from({ length: body.length / 2 }, byteAt);
};
export const bytesToHex = (b: Uint8Array): string => `0x${nobleHex(b)}`;
export const parseHex = (h: string): Uint8Array | null => {
  const body = hexBody(h);
  return body.length % 2 === 0 && /^[0-9a-fA-F]*$/.test(body) ? hexToBytes(h) : null;
};
export const joinHex = (parts: readonly string[]): string => `0x${parts.map(hexBody).join("")}`;
export const concat = (parts: readonly Uint8Array[]): Uint8Array => {
  const [size, starts] = mapAccum(parts, 0, (offset, p) => [offset + p.length, offset] as const);
  const joined = new Uint8Array(size);
  parts.forEach((p, i) => joined.set(p, starts[i]));
  return joined;
};
export const utf8 = (s: string): Uint8Array => new TextEncoder().encode(s);
export const keccak256 = (b: Uint8Array): Uint8Array => keccak_256(b);
export const keccak256Hex = (b: Uint8Array): string => bytesToHex(keccak256(b));
export const wordOf = (n: bigint): Uint8Array => {
  if (n < 0n || n >= 1n << 256n) throw new Error("uint256 out of range");
  return hexToBytes(n.toString(16).padStart(64, "0"));
};
const INT256_MIN = -(1n << 255n), INT256_MAX = (1n << 255n) - 1n;
/** Two's complement: a negative n is written as 2^256 + n. */
export const wordOfSigned = (n: bigint): Uint8Array => {
  if (n < INT256_MIN || n > INT256_MAX) throw new Error("int256 out of range");
  return wordOf(n < 0n ? (1n << 256n) + n : n);
};
/** The 32-byte word at `at`; bytes past the end of the buffer read as zero. */
export const wordAt = (buf: Uint8Array, at: number): bigint =>
  BigInt(bytesToHex(Uint8Array.from({ length: 32 }, (_, i) => buf[at + i] ?? 0)));
const sized = (n: number) => (hex: string): Uint8Array => {
  const b = hexToBytes(hex);
  if (b.length !== n) throw new Error(`${n}-byte value is ${b.length} bytes`);
  return b;
};
const addressBytes = sized(20);
export const bytes32 = sized(32);
export const addressWord = (a: string): Uint8Array => concat([new Uint8Array(12), addressBytes(a)]);
const uint32Bytes = (n: number): Uint8Array => {
  if (!Number.isInteger(n) || n < 0 || n > 0xffff_ffff) throw new Error("uint32 out of range");
  return Uint8Array.of(n >>> 24, (n >>> 16) & 0xff, (n >>> 8) & 0xff, n & 0xff);
};


// ---- Solidity ABI: encoding, and typed cursors for decoding ----

export type Abi =
  | Tagged<"uint" | "int", { value: bigint }>
  | Tagged<"bool", { value: boolean }>
  | Tagged<"address" | "bytes32" | "bytes", { value: string }>
  | Tagged<"array" | "tuple", { value: readonly Abi[] }>;
export const A = {
  uint: (value: bigint): Abi => ({ _tag: "uint", value }),
  int: (value: bigint): Abi => ({ _tag: "int", value }),
  bool: (value: boolean): Abi => ({ _tag: "bool", value }),
  address: (value: string): Abi => ({ _tag: "address", value }),
  b32: (value: string): Abi => ({ _tag: "bytes32", value }),
  bytes: (value: string): Abi => ({ _tag: "bytes", value }),
  array: (value: readonly Abi[]): Abi => ({ _tag: "array", value }),
  tuple: (value: readonly Abi[]): Abi => ({ _tag: "tuple", value }),
} as const;
const arr = <X>(xs: readonly X[], f: (x: X) => Abi): Abi => A.array(xs.map((x) => f(x)));
const dynamic = (v: Abi): boolean => {
  switch (v._tag) {
    case "bytes": case "array": return true;
    case "tuple": return v.value.some(dynamic);
    default: return false;
  }
};
const padRight = (b: Uint8Array): Uint8Array => {
  const padding = Math.ceil(b.length / 32) * 32 - b.length;
  return padding === 0 ? b : concat([b, new Uint8Array(padding)]);
};
const uintWordHex = (n: bigint): string => nobleHex(wordOf(n));
const encodeValue = (v: Abi): string => match(v, {
  uint: (x) => uintWordHex(x.value),
  int: (x) => nobleHex(wordOfSigned(x.value)),
  bool: (x) => uintWordHex(x.value ? 1n : 0n),
  address: (x) => nobleHex(addressWord(x.value)),
  bytes32: (x) => nobleHex(bytes32(x.value)),
  bytes: (x) => {
    const b = hexToBytes(x.value);
    return uintWordHex(BigInt(b.length)) + nobleHex(padRight(b));
  },
  array: (x) => uintWordHex(BigInt(x.value.length)) + encodeSequence(x.value),
  tuple: (x) => encodeSequence(x.value),
});
/** Static values sit in the head; a dynamic value sits in the tail and its head holds the offset. */
const encodeSequence = (vs: readonly Abi[]): string => {
  const parts = vs.map((v) => ({ dynamic: dynamic(v), hex: encodeValue(v) }));
  const headSize = parts.reduce((n, p) => n + (p.dynamic ? 32 : p.hex.length / 2), 0);
  const [, heads] = mapAccum(parts, headSize, (tailAt, p) => p.dynamic
    ? [tailAt + p.hex.length / 2, uintWordHex(BigInt(tailAt))] as const
    : [tailAt, p.hex] as const);
  const tails = parts.filter((p) => p.dynamic).map((p) => p.hex);
  return [...heads, ...tails].join("");
};
export const abiEncodeHex = (vs: readonly Abi[]): string => `0x${encodeSequence(vs)}`;
export const abiEncode = (vs: readonly Abi[]): Uint8Array => hexToBytes(abiEncodeHex(vs));
export type Packed =
  | Tagged<"uint256", { value: bigint }>
  | Tagged<"uint32", { value: number }>
  | Tagged<"bool", { value: boolean }>
  | Tagged<"address" | "bytes32" | "bytes", { value: string }>;
const packedBytes = (p: Packed): Uint8Array => match(p, {
  uint256: (x) => wordOf(x.value), uint32: (x) => uint32Bytes(x.value),
  bool: (x) => Uint8Array.of(x.value ? 1 : 0), address: (x) => addressBytes(x.value),
  bytes32: (x) => bytes32(x.value), bytes: (x) => hexToBytes(x.value),
});
export const encodePacked = (parts: readonly Packed[]): Uint8Array => concat(parts.map(packedBytes));

/** A cursor at the head of a tuple. */
export type AbiTuple = Brand<number, "AbiTuple">;
/** A cursor at the length word of a dynamic array or byte string. */
export type AbiLength = Brand<number, "AbiLength">;
const T = (n: number): AbiTuple => n as AbiTuple, L = (n: number): AbiLength => n as AbiLength;
const itemAt = (l: AbiLength, i: number): number => l + 32 + i * 32;
export const abiRoot = (): AbiTuple => T(0);
export const abiCursorOk = (h: AbiTuple): boolean => Number.isFinite(h) && h >= 0;
export const abiTupleRef = (buf: Uint8Array, h: AbiTuple, slot: number): AbiTuple =>
  T(h + Number(wordAt(buf, h + slot)));
export const abiLengthRef = (buf: Uint8Array, h: AbiTuple, slot: number): AbiLength =>
  L(h + Number(wordAt(buf, h + slot)));
export const abiWord = (buf: Uint8Array, h: AbiTuple, slot: number): bigint => wordAt(buf, h + slot);
export const abiTupleBytes = (buf: Uint8Array, h: AbiTuple, slot: number): Uint8Array =>
  buf.subarray(h + slot, h + slot + 32);
export const abiLengthWord = (buf: Uint8Array, l: AbiLength): bigint => wordAt(buf, l);
export const abiBytes = (buf: Uint8Array, l: AbiLength): Uint8Array =>
  buf.subarray(l + 32, l + 32 + Number(wordAt(buf, l)));
/** Whether `count` items of `stride` bytes fit after the length word; none always fit. */
export const abiFits = (buf: Uint8Array, l: AbiLength, count: bigint, stride: number): boolean => {
  if (!Number.isFinite(l) || l < 0) return false;
  const start = l + 32;
  return start > buf.length ? count === 0n : count * BigInt(stride) <= BigInt(buf.length - start);
};
export const abiBytesElement = (buf: Uint8Array, l: AbiLength, i: number): AbiLength =>
  L(l + 32 + Number(wordAt(buf, itemAt(l, i))));
export const abiTupleElement = (buf: Uint8Array, l: AbiLength, i: number): AbiTuple =>
  T(l + 32 + Number(wordAt(buf, itemAt(l, i))));
export const abiInlineTuple = (l: AbiLength, i: number, stride: number): AbiTuple =>
  T(l + 32 + i * stride);
export const abiStaticWord = (buf: Uint8Array, l: AbiLength, i: number): bigint =>
  wordAt(buf, itemAt(l, i));
export const abiStaticBytes = (buf: Uint8Array, l: AbiLength, i: number): Uint8Array =>
  buf.subarray(itemAt(l, i), itemAt(l, i) + 32);


// ---- signatures and identities ----

export type RawSignature = Readonly<{
  r: bigint; s: bigint; recovery: number; publicKey: Uint8Array;
}>;
export const signRaw = (h: Uint8Array, privateKey: Uint8Array): RawSignature => {
  const s = secp256k1.sign(h, privateKey, { prehash: false, lowS: true });
  const publicKey = s.recoverPublicKey(h).toRawBytes(false);
  return { r: s.r, s: s.s, recovery: s.recovery, publicKey };
};
export const recoverPublicKey = (
  h: Uint8Array, r: Uint8Array, s: Uint8Array, bit: number,
): Uint8Array | null => {
  try {
    const signature = secp256k1.Signature.fromCompact(concat([r, s])).addRecoveryBit(bit);
    return signature.recoverPublicKey(h).toRawBytes(false);
  } catch {
    return null;
  }
};
export type EntityId = Brand<string, "EntityId">;
export type TokenId = Brand<string, "TokenId">;
export type Address = Brand<string, "Address">;
export type Hash = Brand<string, "Hash">;
export type Signature = Brand<string, "Signature">;
export type HostRoot = Brand<Hash, "HostRoot">;
export type RuntimeFrameHash = Brand<Hash, "RuntimeFrameHash">;
export type EntityStateHash = Brand<Hash, "EntityStateHash">;
export type EntityFrameHash = Brand<Hash, "EntityFrameHash">;
export type AccountId = Brand<{ readonly left: EntityId; readonly right: EntityId }, "AccountId">;
/** Separator characters are refused because ids are joined into composite keys. */
const branded = <T extends string, E extends string>(s: string, e: E): Result<T, E> =>
  (s.length === 0 || /[|;=,]/.test(s) ? err(e) : ok(s as T));
export const entityId = (s: string): Result<EntityId, "invalid_entity_id"> =>
  branded(s, "invalid_entity_id");
export const address = (s: string): Result<Address, "invalid_address"> =>
  branded(s, "invalid_address");
export const tokenId = (s: unknown): Result<TokenId, "invalid_token_id"> => {
  const isUint16Text = typeof s === "string" && /^(0|[1-9][0-9]{0,4})$/.test(s) && Number(s) <= 65_535;
  return isUint16Text ? ok(s as TokenId) : err("invalid_token_id");
};
export const hash = (s: string): Result<Hash, "invalid_hash"> =>
  (/^[0-9a-f]{64}$/.test(s) ? ok(s as Hash) : err("invalid_hash"));
export const signature = (s: string): Result<Signature, "invalid_signature"> => {
  const evenHex = /^[0-9a-f]+$/.test(s) && s.length >= 2 && s.length % 2 === 0;
  return evenHex && !/^0+$/.test(s) ? ok(s as Signature) : err("invalid_signature");
};
export const ZERO_HASH = "0".repeat(64) as Hash;
export const ZERO_WORD = `0x${ZERO_HASH}`;
export const WORD = /^0x[0-9a-fA-F]{64}$/;
export const keccakUtf8 = (s: string): Hash => nobleHex(keccak_256(utf8(s))) as Hash;
export const sameHex = (a: string | undefined, b: string | undefined): boolean =>
  a !== undefined && b !== undefined && a.toLowerCase() === b.toLowerCase();
/** Lowercase, and pad a short hex word to 32 bytes. Equal-length hex then compares as an integer. */
const normalizeId = (id: string): string => {
  const raw = id.toLowerCase();
  const hex = raw.slice(2);
  const shortHexWord = raw.startsWith("0x") && /^[0-9a-f]*$/.test(hex) && hex.length < 64;
  return shortHexWord ? `0x${hex.padStart(64, "0")}` : raw;
};
const before = (a: string, b: string): boolean => normalizeId(a) < normalizeId(b);
const sameId = (a: string, b: string): boolean => normalizeId(a) === normalizeId(b);
/** The lower id is always the left side, so both parties name the Account the same way. */
export const accountId = (a: EntityId, b: EntityId): Result<AccountId, Tagged<"same_entity">> => {
  if (sameId(a, b)) return err({ _tag: "same_entity" });
  const sides = before(a, b) ? { left: a, right: b } : { left: b, right: a };
  return ok(sides as AccountId);
};
export const isLeft = (party: EntityId, id: AccountId): boolean => party === id.left;
export const at = <T>(left: T, right: T, onLeft: boolean): T => (onLeft ? left : right);
export const other = (onLeft: boolean): boolean => !onLeft;
export type Party = { readonly self: EntityId; readonly peer: EntityId; readonly left: boolean };
export const counterpartyOf = (id: AccountId, self: EntityId): EntityId | undefined => {
  switch (self) {
    case id.left: return id.right;
    case id.right: return id.left;
    default: return undefined;
  }
};
export const partyOf = (
  id: AccountId, self: EntityId,
): Result<Party, Tagged<"unknown_signer", { entity: EntityId }>> => {
  const peer = counterpartyOf(id, self);
  if (peer === undefined) return err({ _tag: "unknown_signer", entity: self });
  return ok({ self, peer, left: isLeft(self, id) });
};
/** Whoever holds a proposed frame proposed it; whoever holds a received frame did not. */
export const proposerIsLeft = (
  held: { readonly _tag: "proposed" | "received" }, party: Party,
): boolean => (held._tag === "proposed" ? party.left : other(party.left));


// ---- canonical value encoding: RLP over typed nodes, and the flat integrity digest ----

export type CanonicalValueError = Tagged<"non_finite_number" | "unsupported_type" | "invalid_utf8">;
type Rlp = Uint8Array | readonly Rlp[];
const magnitude = (v: bigint): Uint8Array => {
  const h = v.toString(16);
  return hexToBytes(h.length % 2 === 0 ? h : `0${h}`);
};
/** RLP's three spellings: a lone low byte is itself, a short body gets one prefix, a long one its length. */
const payload = (body: Uint8Array, list: boolean): Uint8Array => {
  const loneLowByte = !list && body.length === 1 && (body[0] ?? 0x80) < 0x80;
  if (loneLowByte) return body;
  if (body.length <= 55) return concat([Uint8Array.of((list ? 0xc0 : 0x80) + body.length), body]);
  const size = magnitude(BigInt(body.length));
  return concat([Uint8Array.of((list ? 0xf7 : 0xb7) + size.length), size, body]);
};
const rlp = (n: Rlp): Uint8Array =>
  (n instanceof Uint8Array ? payload(n, false) : payload(concat(n.map(rlp)), true));
const LONE_SURROGATE = /[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?<![\uD800-\uDBFF])[\uDC00-\uDFFF]/;
const text = (v: string): Result<Uint8Array, CanonicalValueError> =>
  (LONE_SURROGATE.test(v) ? err({ _tag: "invalid_utf8" }) : ok(utf8(v)));
const labelled = (label: string, ...rest: readonly Rlp[]): Rlp => [utf8(label), ...rest];
const numberNode = (v: number): Result<Rlp, CanonicalValueError> =>
  (Number.isFinite(v) ? ok(labelled("number", utf8(String(v)))) : err({ _tag: "non_finite_number" }));
const bigintNode = (v: bigint): Rlp =>
  labelled("bigint", Uint8Array.of(v < 0n ? 1 : 0), magnitude(v < 0n ? -v : v));
const mapNode = (m: ReadonlyMap<unknown, unknown>): Result<Rlp, CanonicalValueError> => {
  const rows = traverse(m, ([k, v]) => traverse([k, v], cnode));
  const byKey = (row: readonly Rlp[]): string => bytesToHex(rlp(row[0] ?? []));
  return map(rows, (rs) => labelled("map", ...sortedBy(rs, byKey)));
};
const setNode = (s: ReadonlySet<unknown>): Result<Rlp, CanonicalValueError> =>
  map(traverse(s, cnode), (ns) => labelled("set", ...sortedBy(ns, (n) => bytesToHex(rlp(n)))));
const objectNode = (o: Record<string, unknown>): Result<Rlp, CanonicalValueError> => {
  const keys = Object.keys(o).toSorted(asc).filter((k) => o[k] !== undefined);
  const field = (k: string) => chain(text(k), (name) => map(cnode(o[k]), (n): Rlp => [name, n]));
  return map(traverse(keys, field), (fields) => labelled("object", ...fields));
};
const cnode = (v: unknown): Result<Rlp, CanonicalValueError> => {
  switch (true) {
    case v === null: return ok(labelled("null"));
    case typeof v === "boolean": return ok(labelled("bool", Uint8Array.of(v ? 1 : 0)));
    case typeof v === "number": return numberNode(v);
    case typeof v === "bigint": return ok(bigintNode(v));
    case typeof v === "string": return map(text(v), (b) => labelled("string", b));
    case Array.isArray(v): return map(traverse(v, cnode), (ns) => labelled("array", ...ns));
    case v instanceof Map: return mapNode(v);
    case v instanceof Set: return setNode(v);
    case typeof v === "object": return objectNode(v as Record<string, unknown>);
    default: return err({ _tag: "unsupported_type" });
  }
};
export const encodeCanonicalValue = (v: unknown): Result<Uint8Array, CanonicalValueError> =>
  map(cnode(v), rlp);
/** og state-root.ts:133-161: the frame hash and the state commitment both use this, under two namespaces. */
export const flatDigest = (
  namespace: string, sections: readonly (readonly [string, unknown])[],
): Result<string, CanonicalValueError> => {
  const leafOf = ([path, value]: readonly [string, unknown]) => map(encodeCanonicalValue(value), (enc) => {
    const key = sha256(utf8(`xln.${namespace}.${path}`));
    return { key, hex: bytesToHex(key), digest: sha256(enc) };
  });
  return map(traverse(sections, leafOf), (leaves) => {
    const ordered = sortedBy(leaves, (l) => l.hex).flatMap((l) => [l.key, l.digest]);
    return bytesToHex(sha256(concat([utf8("xln.flat-digest.v1"), ...ordered])));
  });
};


// ---- the committed Account state and its commitment root ----

export type CommittedDelta = Readonly<{
  tokenId: number; collateral: bigint; ondelta: bigint; offdelta: bigint; leftCreditLimit: bigint;
  rightCreditLimit: bigint; leftAllowance: bigint; rightAllowance: bigint; leftHold: bigint;
  rightHold: bigint;
}>;
export type JClaimAccumulator = { readonly version: 1; readonly root: string; readonly count: bigint };
export type CommittedMap = ReadonlyMap<number | string, unknown>;
export type Domain = { readonly chainId: number; readonly depositoryAddress: string };
export type DisputeConfig = Readonly<{ leftResponseSeconds: number; rightResponseSeconds: number }>;
export type CommittedAccountState = Readonly<{
  domain: Domain; leftEntity: string; rightEntity: string; watchSeed: string;
  disputeConfig: DisputeConfig; jNonce: number; lastFinalizedJHeight: number;
  leftPendingJClaims: JClaimAccumulator; rightPendingJClaims: JClaimAccumulator;
  deltas: ReadonlyMap<number, CommittedDelta>; locks: CommittedMap; pulls: CommittedMap;
  swapOffers: CommittedMap; subcontracts: CommittedMap; lendingIntents: CommittedMap;
  requestedRebalance: CommittedMap; requestedRebalanceFeeState: CommittedMap;
  rebalanceFeePolicies: CommittedMap; settlementWorkspace?: SettlementWorkspace | undefined;
}>;
export type CommitmentError =
  | CanonicalValueError
  | Tagged<"bad_domain" | "bad_j_claims" | "bad_key" | "key_prefix_collision">
  | Tagged<"nested_collection" | "leaf_too_large">;
const u16 = (n: number): Uint8Array => Uint8Array.of((n >> 8) & 0xff, n & 0xff);
const u32 = (n: number): Uint8Array => Uint8Array.of((n >>> 24) & 0xff, (n >>> 16) & 0xff, (n >>> 8) & 0xff, n & 0xff);
const prefixed = (t: string): Uint8Array => concat([u16(utf8(t).length), utf8(t)]);
const LEAF = prefixed("xln.storage.merkle.leaf.v1");
const BRANCH = prefixed("xln.storage.merkle.branch.v1");
const EXTENSION = prefixed("xln.storage.merkle.extension.v1");
const RADIX_16 = Uint8Array.of(0x10);
export const EMPTY_J_ROOT = bytesToHex(keccak256(utf8("xln.account-j-claim.empty.v1")));
const keyBytes = (key: number | string): Result<Uint8Array, CommitmentError> => {
  if (typeof key === "number") {
    return Number.isSafeInteger(key) && key >= 0 ? ok(wordOf(BigInt(key))) : err({ _tag: "bad_key" });
  }
  const body = utf8(key);
  return body.length > 0xffff ? err({ _tag: "bad_key" }) : ok(concat([u16(body.length), body]));
};
export type PreparedLeaf = { readonly key: Uint8Array; readonly value: Uint8Array };
type Leaf = { readonly nibbles: readonly number[]; readonly key: Uint8Array; readonly digest: Uint8Array };
/** A radix-16 trie: a branch's path is the nibbles every leaf under it shares. */
type MNode =
  | Tagged<"leaf", { leaf: Leaf }>
  | Tagged<"branch", { path: readonly number[]; children: ReadonlyMap<number, MNode> }>;
const nibblesOf = (b: Uint8Array): number[] => [...b].flatMap((x) => [x >> 4, x & 0x0f]);
/** The first depth from `depth` on where the leaves disagree. */
const splitDepth = (leaves: readonly Leaf[], first: Leaf, depth: number): number => {
  const agreeAt = (d: number): boolean => leaves.every((l) => l.nibbles[d] === first.nibbles[d]);
  const offset = first.nibbles.slice(depth).findIndex((_, i) => !agreeAt(depth + i));
  return offset === -1 ? first.nibbles.length : depth + offset;
};
const build = (leaves: readonly Leaf[], depth: number): MNode => {
  const [first] = leaves;
  if (first === undefined) throw new Error("unreachable: build is called with at least one leaf");
  if (leaves.length === 1) return { _tag: "leaf", leaf: first };
  const split = splitDepth(leaves, first, depth);
  const slotOf = (l: Leaf): number => l.nibbles[split] ?? 0;
  const slots = [...new Set(leaves.map(slotOf))];
  const childAt = (slot: number) => build(leaves.filter((l) => slotOf(l) === slot), split + 1);
  const children = new Map(slots.map((slot) => [slot, childAt(slot)] as const));
  return { _tag: "branch", path: first.nibbles.slice(0, split), children };
};
const pack = (ns: readonly number[]): Uint8Array =>
  Uint8Array.from({ length: Math.ceil(ns.length / 2) }, (_, i) => ((ns[2 * i] ?? 0) << 4) | (ns[2 * i + 1] ?? 0));
const nodeHash = (node: MNode): Uint8Array => match(node, {
  leaf: ({ leaf }) => sha256(concat([LEAF, leaf.key, leaf.digest])),
  branch: (b) => {
    const bySlot = [...b.children].toSorted(([x], [y]) => x - y);
    const edges = bySlot.flatMap(([slot, child]) => [Uint8Array.of(slot), edgeHash(b.path, child)]);
    return sha256(concat([BRANCH, RADIX_16, ...edges]));
  },
});
/** An edge that skips nibbles is an extension node over the child. */
const edgeHash = (parentPath: readonly number[], child: MNode): Uint8Array => {
  if (child._tag === "leaf") return nodeHash(child);
  const gap = child.path.slice(parentPath.length + 1);
  if (gap.length === 0) return nodeHash(child);
  return sha256(concat([EXTENSION, RADIX_16, u16(gap.length), pack(gap), nodeHash(child)]));
};
const holdsCollection = (v: unknown): boolean => {
  switch (true) {
    case v instanceof Map || v instanceof Set: return true;
    case Array.isArray(v): return v.some(holdsCollection);
    case v !== null && typeof v === "object": return Object.values(v).some(holdsCollection);
    default: return false;
  }
};
const isPrefix = (short: Uint8Array, long: Uint8Array): boolean =>
  short.length <= long.length && short.every((b, i) => b === long[i]);
const MAX_LEAF_BYTES = 10_000;
/** A leaf holds one flat value; a collection inside it would escape the trie's per-key proofs. */
export const prepareLeaf = (key: number | string, value: unknown): Result<PreparedLeaf, CommitmentError> =>
  chain(keyBytes(key), (k): Result<PreparedLeaf, CommitmentError> => {
    if (holdsCollection(value)) return err({ _tag: "nested_collection" });
    return chain(encodeCanonicalValue(value), (v): Result<PreparedLeaf, CommitmentError> =>
      (v.length > MAX_LEAF_BYTES ? err({ _tag: "leaf_too_large" }) : ok({ key: k, value: v })));
  });
export type PreparedMap = readonly PreparedLeaf[];
/** Keys are sorted, so a key that prefixes another sits right before it. */
export const prepareMap = (m: CommittedMap): Result<PreparedMap, CommitmentError> =>
  chain(traverse(m, ([k, v]) => prepareLeaf(k, v)), (prepared) => {
    const unique = [...new Map(prepared.map((l) => [nobleHex(l.key), l])).values()];
    const leaves = sortedBy(unique, (l) => nobleHex(l.key));
    const prefixesNext = (cur: PreparedLeaf, i: number): boolean =>
      i > 0 && isPrefix(leaves[i - 1]?.key ?? cur.key, cur.key);
    return leaves.some(prefixesNext) ? err({ _tag: "key_prefix_collision" }) : ok(leaves);
  });
/** The root is always a branch: a lone leaf or a pathed branch is lifted under its first nibble. */
const sealRadix = (leaves: readonly Leaf[]): string => {
  if (leaves.length === 0) return ZERO_WORD;
  const top = build(leaves, 0);
  const lift = (slot: number): MNode => ({ _tag: "branch", path: [], children: new Map([[slot, top]]) });
  const root = match<MNode, MNode>(top, {
    leaf: ({ leaf }) => lift(leaf.nibbles[0] ?? 0),
    branch: (b) => (b.path.length === 0 ? top : lift(b.path[0] ?? 0)),
  });
  return bytesToHex(nodeHash(root));
};
const preparedMapRoot = (m: PreparedMap): string =>
  sealRadix(m.map((leaf) => ({ nibbles: nibblesOf(leaf.key), key: leaf.key, digest: sha256(leaf.value) })));
export const mapRoot = (m: CommittedMap): Result<string, CommitmentError> =>
  map(prepareMap(m), preparedMapRoot);
/** A mixed-case address must carry its EIP-55 checksum; the committed spelling is lowercase. */
export const domainOf = (d: Domain): Result<Domain, CommitmentError> => {
  const { chainId, depositoryAddress: a } = d;
  const shaped = Number.isSafeInteger(chainId) && chainId > 0 && /^0x[0-9a-fA-F]{40}$/.test(a);
  if (!shaped) return err({ _tag: "bad_domain" });
  const body = a.slice(2);
  const mixedCase = /[a-f]/.test(body) && /[A-F]/.test(body);
  if (mixedCase && checksum(a) !== a) return err({ _tag: "bad_domain" });
  return ok({ chainId, depositoryAddress: `0x${body.toLowerCase()}` });
};
/** An accumulator is empty exactly when its root is the empty root. */
const jClaims = (c: JClaimAccumulator): Result<JClaimAccumulator, CommitmentError> => {
  const root = c.root.trim().toLowerCase();
  const countFits = c.count >= 0n && c.count < 1n << 64n;
  const emptyAgrees = (root === EMPTY_J_ROOT) === (c.count === 0n);
  const shaped = /^0x[0-9a-f]{64}$/.test(root) && countFits && emptyAgrees;
  return shaped ? ok({ version: 1, root, count: c.count }) : err({ _tag: "bad_j_claims" });
};
const MAP_NAMES = [
  "deltas", "locks", "pulls", "swapOffers", "subcontracts", "lendingIntents",
  "requestedRebalance", "requestedRebalanceFeeState", "rebalanceFeePolicies",
] as const;
type MapName = (typeof MAP_NAMES)[number];
export type PreparedCommitment = Readonly<{
  state: CommittedAccountState; domain: Domain; left: JClaimAccumulator; right: JClaimAccumulator;
  maps: { readonly [K in MapName]: PreparedMap };
}>;
export const prepareState = (
  state: CommittedAccountState,
): Result<PreparedCommitment, CommitmentError> => {
  type PreparedMaps = { [K in MapName]: Result<PreparedMap, CommitmentError> };
  const maps = Object.fromEntries(MAP_NAMES.map((n) => [n, prepareMap(state[n])])) as PreparedMaps;
  const domain = domainOf(state.domain);
  const left = jClaims(state.leftPendingJClaims);
  const right = jClaims(state.rightPendingJClaims);
  return map(all({ domain, left, right, ...maps }), ({ domain, left, right, ...maps }) =>
    ({ state, domain, left, right, maps }));
};
export const preparedRoot = (prepared: PreparedCommitment): Result<string, CommitmentError> => {
  const { state, domain, left, right, maps } = prepared;
  const root = (n: MapName): string => preparedMapRoot(maps[n]);
  const identity = {
    chainId: domain.chainId, depositoryAddress: domain.depositoryAddress,
    leftEntity: state.leftEntity.toLowerCase(), rightEntity: state.rightEntity.toLowerCase(),
    watchSeed: state.watchSeed.toLowerCase(),
  };
  const financial = { deltasRoot: root("deltas"), jNonce: state.jNonce, disputeConfig: state.disputeConfig };
  const commitments = {
    locksRoot: root("locks"), pullsRoot: root("pulls"), swapOffersRoot: root("swapOffers"),
    subcontractsRoot: root("subcontracts"), lendingIntentsRoot: root("lendingIntents"),
    settlementWorkspace: workspaceWithoutHankos(state.settlementWorkspace),
  };
  const jurisdiction = {
    lastFinalizedJHeight: state.lastFinalizedJHeight, leftPendingJClaims: left,
    rightPendingJClaims: right,
  };
  const rebalance = {
    requestedRebalanceRoot: root("requestedRebalance"),
    requestedRebalanceFeeStateRoot: root("requestedRebalanceFeeState"),
    rebalanceFeePoliciesRoot: root("rebalanceFeePolicies"),
  };
  return flatDigest("account.state", [
    ["identity", identity], ["financial", financial], ["commitments", commitments],
    ["jurisdiction", jurisdiction], ["rebalance", rebalance],
  ]);
};
/** Structural equality over plain data; collections compare by identity only. */
const sameValue = (a: unknown, b: unknown): boolean => {
  if (a === b) return true;
  if (typeof a !== "object" || typeof b !== "object" || a === null || b === null) return false;
  const collection = a instanceof Map || a instanceof Set || b instanceof Map || b instanceof Set;
  if (collection || Array.isArray(a) !== Array.isArray(b)) return false;
  const ka = Object.keys(a), kb = Object.keys(b);
  const sameField = (k: string): boolean => Object.hasOwn(b, k) && sameValue(Reflect.get(a, k), Reflect.get(b, k));
  return ka.length === kb.length && ka.every(sameField);
};
const oneKeyKind = (m: CommittedMap): boolean => new Set([...m.keys()].map((k) => typeof k)).size <= 1;
/**
 * Only changed leaves are re-prepared. Number keys are fixed 32-byte words and string keys carry their
 * length, so keys of one kind never prefix each other; only a new key in a mixed map can collide.
 */
const prepareMapChanges = (before: CommittedMap, after: CommittedMap): Result<void, CommitmentError> => {
  if (before === after) return ok(undefined);
  const changed = [...after].filter(([k, v]) => {
    const was = before.get(k);
    return was === undefined || !sameValue(was, v);
  });
  const onlyOldKeys = changed.every(([k]) => before.has(k));
  return chain(traverse(changed, ([k, v]) => prepareLeaf(k, v)), () =>
    (onlyOldKeys || oneKeyKind(after) ? ok(undefined) : map(prepareMap(after), () => undefined)));
};
export const prepareChanges = (
  before: CommittedAccountState, after: CommittedAccountState,
): Result<void, CommitmentError> => {
  const domain = sameValue(before.domain, after.domain) ? ok(undefined) : domainOf(after.domain);
  const claimSides = ["leftPendingJClaims", "rightPendingJClaims"] as const;
  const claims = claimSides.map((side) =>
    (sameValue(before[side], after[side]) ? ok(undefined) : jClaims(after[side])));
  const maps = MAP_NAMES.map((n) => prepareMapChanges(before[n], after[n]));
  return checks(domain, ...claims, ...maps);
};
export const accountStateCommitment = (state: CommittedAccountState): Result<string, CommitmentError> =>
  chain(prepareState(state), preparedRoot);


// ---- the Account frame hash ----

export type WireTx = { readonly type: string; readonly data: unknown };
export type AccountFrameInputs = Readonly<{
  height: number; timestamp: number; jHeight: number; prevFrameHash: string;
  accountStateRoot: string; accountTxs: readonly WireTx[];
}>;
export type FrameHashError =
  | CanonicalValueError
  | Tagged<"policy_version" | "settle_witness_shape">
  | ClaimError;
type Fields = Record<string, unknown>;
const isRecord = (v: unknown): v is Fields => v !== null && typeof v === "object";
const fieldOf = (data: unknown, name: string): unknown =>
  (isRecord(data) && Object.hasOwn(data, name) ? data[name] : undefined);
/** Hankos are signed after the frame, so the frame hash is taken over the settlement without them. */
const withoutHankoWitness = (tx: WireTx): WireTx => {
  const hankoSettlement = tx.type === "settle_transition" && fieldOf(tx.data, "kind") === "hanko";
  if (!hankoSettlement || !isRecord(tx.data)) return tx;
  const { settlementHanko: _settlementHanko, ...rest } = tx.data;
  const post = rest.postProof;
  if (!isRecord(post) || Array.isArray(post)) return { type: tx.type, data: rest };
  const { hanko: _hanko, ...postWithoutHanko } = post;
  return { type: tx.type, data: { ...rest, postProof: postWithoutHanko } };
};
/**
 * og refuses to hash these before it gets that far: a policy without a version, and a settlement
 * og's witness projection (witness-projection.ts:53) would dereference as `data.postProof.hanko`.
 */
const projectionRefusal = (tx: WireTx): Result<void, FrameHashError> => {
  const version = fieldOf(tx.data, "policyVersion");
  const badVersion = typeof version !== "number" || !Number.isSafeInteger(version) || version < 0;
  if (tx.type === "rebalance_policy" && badVersion) return err({ _tag: "policy_version" });
  const noData = tx.data === null || tx.data === undefined;
  const hankoWithoutPost = fieldOf(tx.data, "kind") === "hanko" && (fieldOf(tx.data, "postProof") ?? null) === null;
  if (tx.type === "settle_transition" && (noData || hankoWithoutPost)) return err({ _tag: "settle_witness_shape" });
  return ok(undefined);
};
export const ownWire = (tx: { readonly type: string }): WireTx => {
  const { type, ...data } = tx;
  return { type, data };
};
export const txRefusal = (own: { readonly type: string }): Result<void, FrameHashError> => {
  const tx = ownWire(own);
  return chain(projectionRefusal(tx), () => map(encodeCanonicalValue(tx), () => undefined));
};
export const accountFrameHash = (f: AccountFrameInputs): Result<string, FrameHashError> => {
  const transition = { height: f.height, timestamp: f.timestamp, jHeight: f.jHeight, prevFrameHash: f.prevFrameHash };
  return chain(traverse(f.accountTxs, projectionRefusal), () => flatDigest("account.frame", [
    ["transition", transition], ["transactions", f.accountTxs.map(withoutHankoWitness)],
    ["accountStateRoot", f.accountStateRoot],
  ]));
};
type AmountTx = { readonly type: string; readonly data: { readonly tokenId: number; readonly amount: string } };
export const encodeFrameHash = (
  f: Omit<AccountFrameInputs, "accountTxs"> & { readonly accountTxs: readonly AmountTx[] },
): string => {
  const accountTxs = f.accountTxs.map((tx) =>
    ({ type: tx.type, data: { tokenId: tx.data.tokenId, amount: BigInt(tx.data.amount) } }));
  return unwrapOr(accountFrameHash({ ...f, accountTxs }), (e) => {
    throw new Error(`frame hash cannot encode: ${e._tag}`);
  });
};


// ---- Depository batch: the calldata an Entity submits to its jurisdiction ----

export type JAllowance = Readonly<{
  deltaIndex: bigint; rightAllowance: bigint; leftAllowance: bigint;
}>;
export type TransformerClause = Readonly<{
  transformerAddress: string; encodedBatch: string; allowances: readonly JAllowance[];
}>;
export type ProofBody = Readonly<{
  watchSeed: string; leftResponseSeconds: bigint; rightResponseSeconds: bigint;
  offdeltas: readonly bigint[]; tokenIds: readonly bigint[];
  transformers: readonly TransformerClause[];
}>;
export type SettlementDiff = Readonly<{
  tokenId: bigint; leftDiff: bigint; rightDiff: bigint; collateralDiff: bigint; ondeltaDiff: bigint;
}>;
export type Settlement = Readonly<{
  leftEntity: string; rightEntity: string; diffs: readonly SettlementDiff[];
  forgiveDebtsInTokenIds: readonly bigint[]; sig: string; nonce: bigint;
}>;
export type DisputeStart = Readonly<{
  counterentity: string; nonce: bigint; proposerIsLeft: boolean; proofbodyHash: string;
  initialProofbody: ProofBody; watchSeed: string; sig: string; starterInitialArguments: string;
  starterCounterArguments: string; starterCounterProofCommitment: string;
}>;
export type CounterDispute = Readonly<{
  counterentity: string; initialNonce: bigint; initialProofbodyHash: string; counterNonce: bigint;
  proposerIsLeft: boolean; counterProofbody: ProofBody; sig: string;
}>;
export type DisputeFinalization = Readonly<{
  counterentity: string; initialNonce: bigint; finalNonce: bigint; proposerIsLeft: boolean;
  initialProofbodyHash: string; finalProofbody: ProofBody; starterArguments: string;
  otherArguments: string; sig: string; startedByLeft: boolean; cooperative: boolean;
}>;
export type HashLadderWitness = Readonly<{
  fillRatio: bigint; fullSecret: string; reveals: readonly [string, string, string, string];
}>;
type ReserveTransfer = { readonly receivingEntity: string; readonly tokenId: bigint; readonly amount: bigint };
type ReserveToCollateral = Readonly<{
  tokenId: bigint; receivingEntity: string;
  pairs: readonly { readonly entity: string; readonly amount: bigint }[];
}>;
type CollateralToReserve = Readonly<{
  counterparty: string; tokenId: bigint; amount: bigint; nonce: bigint; sig: string;
}>;
type ExternalTokenToReserve = Readonly<{
  entity: string; contractAddress: string; externalTokenId: bigint; tokenType: bigint;
  internalTokenId: bigint; amount: bigint;
}>;
type RevealSecret = { readonly transformer: string; readonly secret: string };
type HashLadderRegistration = Readonly<{
  counterpartyEntity: string; targetRole: boolean; fullHash: string; partialRoot: string;
  witness: HashLadderWitness;
}>;
export type Batch = Readonly<{
  reserveToReserve: readonly ReserveTransfer[]; reserveToCollateral: readonly ReserveToCollateral[];
  collateralToReserve: readonly CollateralToReserve[]; settlements: readonly Settlement[];
  disputeStarts: readonly DisputeStart[]; counterDisputes: readonly CounterDispute[];
  disputeFinalizations: readonly DisputeFinalization[];
  externalTokenToReserve: readonly ExternalTokenToReserve[];
  reserveToExternalToken: readonly ReserveTransfer[]; revealSecrets: readonly RevealSecret[];
  hashLadderRegistrations: readonly HashLadderRegistration[];
}>;
export const BATCH_FIELDS = [
  "reserveToReserve", "reserveToCollateral", "collateralToReserve", "settlements",
  "disputeStarts", "counterDisputes", "disputeFinalizations", "externalTokenToReserve",
  "reserveToExternalToken", "revealSecrets", "hashLadderRegistrations",
] as const;
export const emptyBatch = (): Batch =>
  Object.fromEntries(BATCH_FIELDS.map((f) => [f, []])) as unknown as Batch;
const t = A.tuple;
const allowanceAbi = (r: JAllowance): Abi =>
  t([A.uint(r.deltaIndex), A.uint(r.rightAllowance), A.uint(r.leftAllowance)]);
const transformerAbi = (c: TransformerClause): Abi =>
  t([A.address(c.transformerAddress), A.bytes(c.encodedBatch), arr(c.allowances, allowanceAbi)]);
/** Types.sol `Int512{int256 high; uint256 low}`: a value outside int512 fails the int256 word. */
const int512Abi = (n: bigint): Abi => t([A.int(n >> 256n), A.uint(n & ((1n << 256n) - 1n))]);
/** Types.sol `SignedAmount{bool negative; uint256 magnitude}`: zero is never negative. */
const signedAmountAbi = (n: bigint): Abi => t([A.bool(n < 0n), A.uint(n < 0n ? -n : n)]);
const proofBodyAbi = (b: ProofBody): Abi => t([
  A.b32(b.watchSeed), A.uint(b.leftResponseSeconds), A.uint(b.rightResponseSeconds),
  arr(b.offdeltas, int512Abi), arr(b.tokenIds, A.uint), arr(b.transformers, transformerAbi),
]);
const diffAbi = (d: SettlementDiff): Abi => t([
  A.uint(d.tokenId), signedAmountAbi(d.leftDiff), signedAmountAbi(d.rightDiff),
  signedAmountAbi(d.collateralDiff), signedAmountAbi(d.ondeltaDiff),
]);
const witnessAbi = (w: HashLadderWitness): Abi =>
  t([A.uint(w.fillRatio), A.b32(w.fullSecret), ...w.reveals.map((r) => A.b32(r))]);
const reserveTransferAbi = (r: ReserveTransfer): Abi =>
  t([A.b32(r.receivingEntity), A.uint(r.tokenId), A.uint(r.amount)]);
const reserveToCollateralAbi = (r: ReserveToCollateral): Abi => {
  const pairs = arr(r.pairs, (p) => t([A.b32(p.entity), A.uint(p.amount)]));
  return t([A.uint(r.tokenId), A.b32(r.receivingEntity), pairs]);
};
const collateralToReserveAbi = (r: CollateralToReserve): Abi =>
  t([A.b32(r.counterparty), A.uint(r.tokenId), A.uint(r.amount), A.uint(r.nonce), A.bytes(r.sig)]);
const settlementAbi = (r: Settlement): Abi => t([
  A.b32(r.leftEntity), A.b32(r.rightEntity), arr(r.diffs, diffAbi),
  arr(r.forgiveDebtsInTokenIds, A.uint), A.bytes(r.sig), A.uint(r.nonce),
]);
const disputeStartAbi = (r: DisputeStart): Abi => t([
  A.b32(r.counterentity), A.uint(r.nonce), A.bool(r.proposerIsLeft), A.b32(r.proofbodyHash),
  proofBodyAbi(r.initialProofbody), A.b32(r.watchSeed), A.bytes(r.sig),
  A.bytes(r.starterInitialArguments), A.bytes(r.starterCounterArguments),
  A.b32(r.starterCounterProofCommitment),
]);
const counterDisputeAbi = (r: CounterDispute): Abi => t([
  A.b32(r.counterentity), A.uint(r.initialNonce), A.b32(r.initialProofbodyHash),
  A.uint(r.counterNonce), A.bool(r.proposerIsLeft), proofBodyAbi(r.counterProofbody),
  A.bytes(r.sig),
]);
const disputeFinalizationAbi = (r: DisputeFinalization): Abi => t([
  A.b32(r.counterentity), A.uint(r.initialNonce), A.uint(r.finalNonce), A.bool(r.proposerIsLeft),
  A.b32(r.initialProofbodyHash), proofBodyAbi(r.finalProofbody), A.bytes(r.starterArguments),
  A.bytes(r.otherArguments), A.bytes(r.sig), A.bool(r.startedByLeft), A.bool(r.cooperative),
]);
const externalTokenToReserveAbi = (r: ExternalTokenToReserve): Abi => t([
  A.b32(r.entity), A.address(r.contractAddress), A.uint(r.externalTokenId), A.uint(r.tokenType),
  A.uint(r.internalTokenId), A.uint(r.amount),
]);
const revealSecretAbi = (r: RevealSecret): Abi => t([A.address(r.transformer), A.b32(r.secret)]);
const hashLadderRegistrationAbi = (r: HashLadderRegistration): Abi => t([
  A.b32(r.counterpartyEntity), A.bool(r.targetRole), A.b32(r.fullHash), A.b32(r.partialRoot),
  witnessAbi(r.witness),
]);
const batchAbi = (b: Batch): Abi => t([
  arr(b.reserveToReserve, reserveTransferAbi), arr(b.reserveToCollateral, reserveToCollateralAbi),
  arr(b.collateralToReserve, collateralToReserveAbi), arr(b.settlements, settlementAbi),
  arr(b.disputeStarts, disputeStartAbi), arr(b.counterDisputes, counterDisputeAbi),
  arr(b.disputeFinalizations, disputeFinalizationAbi),
  arr(b.externalTokenToReserve, externalTokenToReserveAbi),
  arr(b.reserveToExternalToken, reserveTransferAbi), arr(b.revealSecrets, revealSecretAbi),
  arr(b.hashLadderRegistrations, hashLadderRegistrationAbi),
]);
export const encodeBatch = (b: Batch): string => abiEncodeHex([batchAbi(b)]);
export const encodeProofBodyBytes = (b: ProofBody): string => abiEncodeHex([proofBodyAbi(b)]);


// ---- J events: the Depository and EntityProvider logs an Entity reads back ----

export const J_EVENT_SIGNATURES = {
  HankoBatchProcessed: "HankoBatchProcessed(bytes32,bytes32,uint256)",
  ReserveUpdated: "ReserveUpdated(bytes32,uint256,uint256)",
  AccountSettled:
    "AccountSettled((bytes32,bytes32,(uint256,uint256,uint256,uint256,(int256,uint256))[],uint256)[])",
  DisputeStarted:
    "DisputeStarted(bytes32,bytes32,uint256,bool,bytes32,bytes32,bytes,bytes,bytes32,uint256,uint256,uint32,uint32)",
  DisputeFinalized: "DisputeFinalized(bytes32,bytes32,uint256,bytes32,bytes32)",
  SecretRevealed: "SecretRevealed(bytes32,bytes32,bytes32)",
  CounterDisputeRegistered: "CounterDisputeRegistered(bytes32,bytes32,uint256,bool,bytes32)",
  HashLadderRevealRegistered:
    "HashLadderRevealRegistered(bytes32,bytes32,bytes32,uint16,bytes32,bytes32[4],bool,uint256)",
  DebtCreated: "DebtCreated(bytes32,bytes32,uint256,(uint256,uint256),uint256)",
  DebtEnforced: "DebtEnforced(bytes32,bytes32,uint256,uint256,(uint256,uint256),uint256)",
  DebtForgiven: "DebtForgiven(bytes32,bytes32,uint256,(uint256,uint256),uint256)",
  FoundationBootstrapped: "FoundationBootstrapped(address,bytes32,uint256,uint256)",
  EntityRegistered: "EntityRegistered(bytes32,uint256,bytes32)",
  BoardActivated: "BoardActivated(bytes32,bytes32,bytes32,uint256)",
  EntityProviderActionExecuted: "EntityProviderActionExecuted(bytes32,uint256,bytes32,uint8)",
  EntityProviderActionCancelled: "EntityProviderActionCancelled(bytes32,uint256,bytes32,uint8,bytes32)",
} as const;
export type JEventName = keyof typeof J_EVENT_SIGNATURES;
const J_EVENT_NAMES = Object.keys(J_EVENT_SIGNATURES) as readonly JEventName[];
const eventTopics = Object.fromEntries(J_EVENT_NAMES.map((n) =>
  [n, keccak256Hex(utf8(J_EVENT_SIGNATURES[n]))])) as Readonly<Record<JEventName, string>>;
export const jEventTopic = (n: JEventName): string => eventTopics[n];
const eventByTopic = new Map(J_EVENT_NAMES.map((n) => [jEventTopic(n).toLowerCase(), n] as const));
/** og JEventMetadata: the EVM log position; a token row's `eventIndex` orders one log's rows. */
export type JEventMeta = Readonly<{
  blockNumber?: number | undefined; blockHash?: string | undefined;
  transactionHash?: string | undefined; logIndex?: number | undefined;
}>;
export type TokenSettlement = Readonly<{
  tokenId: bigint; leftReserve: bigint; rightReserve: bigint; collateral: bigint; ondelta: bigint;
  eventIndex?: number | undefined;
}>;
export type AccountSettlement = Readonly<{
  left: string; right: string; tokens: readonly TokenSettlement[]; nonce: bigint;
  meta?: JEventMeta | undefined;
}>;
/** og DisputeFinalizationEvidence: the reducer sidecar decoded from the finalizing transaction. */
export type DisputeFinalizationEvidence = Readonly<{
  sender: string; counterentity: string; initialNonce: bigint; finalNonce: bigint;
  initialProofbodyHash: string; finalProofbodyHash: string; proposerIsLeft: boolean;
  leftArguments: string; rightArguments: string; startedByLeft: boolean; sig: string;
}>;
type HankoBatchProcessed = Readonly<{
  type: "HankoBatchProcessed"; entityId: string; batchHash: string; nonce: bigint;
}>;
type ReserveUpdated = Readonly<{
  type: "ReserveUpdated"; entity: string; tokenId: bigint; newBalance: bigint;
}>;
type AccountSettled = { readonly type: "AccountSettled"; readonly settled: readonly AccountSettlement[] };
type DisputeStarted = Readonly<{
  type: "DisputeStarted"; sender: string; counterentity: string; nonce: bigint;
  proposerIsLeft: boolean; proofbodyHash: string; watchSeed: string;
  starterInitialArguments: string; starterCounterArguments: string;
  starterCounterProofCommitment: string; disputeTimeout: bigint; disputeStartTimestamp: bigint;
  leftResponseSeconds: bigint; rightResponseSeconds: bigint;
  initialProofbody?: ProofBody | undefined; batchNonce?: number | undefined;
}>;
type DisputeFinalized = Readonly<{
  type: "DisputeFinalized"; sender: string; counterentity: string; nonce: bigint;
  finalProofbodyHash: string; finalizationEvidenceHash: string;
  finalProofbody?: ProofBody | undefined; initialProofbodyHash?: string | undefined;
  evidence?: DisputeFinalizationEvidence | undefined; batchNonce?: number | undefined;
}>;
type SecretRevealed = Readonly<{
  type: "SecretRevealed"; hashlock: string; revealer: string; secret: string;
}>;
type CounterDisputeRegistered = Readonly<{
  type: "CounterDisputeRegistered"; sender: string; counterentity: string; nonce: bigint;
  proposerIsLeft: boolean; proofbodyHash: string; counterProofbody?: ProofBody | undefined;
}>;
type HashLadderRevealRegistered = Readonly<{
  type: "HashLadderRevealRegistered"; entity: string; counterpartyEntity: string;
  ladderHash: string; fillRatio: number; fullSecret: string;
  reveals: readonly [string, string, string, string]; targetRole: boolean; revealedAt: bigint;
}>;
type DebtCreated = Readonly<{
  type: "DebtCreated"; debtor: string; creditor: string; tokenId: bigint; amount: bigint;
  debtIndex: bigint;
}>;
type DebtEnforced = Readonly<{
  type: "DebtEnforced"; debtor: string; creditor: string; tokenId: bigint; amountPaid: bigint;
  remainingAmount: bigint; newDebtIndex: bigint;
}>;
type DebtForgiven = Readonly<{
  type: "DebtForgiven"; debtor: string; creditor: string; tokenId: bigint; amountForgiven: bigint;
  debtIndex: bigint;
}>;
type FoundationBootstrapped = Readonly<{
  type: "FoundationBootstrapped"; recipient: string; boardHash: string; controlTokenId: bigint;
  dividendTokenId: bigint;
}>;
type EntityRegistered = Readonly<{
  type: "EntityRegistered"; entityId: string; entityNumber: bigint; boardHash: string;
}>;
type BoardActivated = Readonly<{
  type: "BoardActivated"; entityId: string; previousBoardHash: string; newBoardHash: string;
  previousBoardValidUntil: bigint;
}>;
type EntityProviderActionExecuted = Readonly<{
  type: "EntityProviderActionExecuted"; entityId: string; actionNonce: bigint; actionHash: string;
  actionKind: 0 | 1;
}>;
type EntityProviderActionCancelled = Readonly<{
  type: "EntityProviderActionCancelled"; entityId: string; actionNonce: bigint;
  cancelledActionHash: string; cancelledActionKind: 0 | 1; cancelHash: string;
}>;
export type JEventClaimBody =
  | HankoBatchProcessed
  | ReserveUpdated
  | AccountSettled
  | DisputeStarted
  | DisputeFinalized
  | SecretRevealed
  | CounterDisputeRegistered
  | HashLadderRevealRegistered
  | DebtCreated
  | DebtEnforced
  | DebtForgiven
  | FoundationBootstrapped
  | EntityRegistered
  | BoardActivated
  | EntityProviderActionExecuted
  | EntityProviderActionCancelled;
/** A decoded canonical log with its chain coordinates when the transport supplied them. */
export type JEvent = JEventClaimBody & { readonly meta?: JEventMeta | undefined };
export type ChainLog = { readonly topics: readonly string[]; readonly data: string } & JEventMeta;
const wordHex = (topic: string): string => `0x${hexBody(topic).toLowerCase().padStart(64, "0")}`;
const signedWord = (n: bigint): bigint => (n >= 1n << 255n ? n - (1n << 256n) : n);
const SAFE_INT = BigInt(Number.MAX_SAFE_INTEGER);
/** og event-normalizers.ts: a normalizer returning null makes og ingress throw this. */
const invalidPayload = (name: JEventName): never => {
  throw new Error(`J_EVENT_CANONICAL_PAYLOAD_INVALID:${name}`);
};
/** ethers AddressCoder: toBeHex(word, 20) refuses a word wider than 20 bytes. */
const addressFromWord = (n: bigint): string => {
  if (n >> 160n !== 0n) throw new Error("address word exceeds 20 bytes");
  return `0x${n.toString(16).padStart(40, "0")}`;
};
const readSettled = (buf: Uint8Array): readonly AccountSettlement[] => {
  if (buf.length < 64) return [];
  const countAt = (h: AbiLength, stride: number): number => {
    const count = abiLengthWord(buf, h);
    if (!abiFits(buf, h, count, stride)) throw new Error("AccountSettled log count does not fit");
    return Number(count);
  };
  const tokenAt = (tokens: AbiLength, k: number): TokenSettlement => {
    const at = abiInlineTuple(tokens, k, 192);
    const word = (slot: number): bigint => abiWord(buf, at, slot);
    const ondelta = (signedWord(word(128)) << 256n) + word(160);
    return { tokenId: word(0), leftReserve: word(32), rightReserve: word(64), collateral: word(96), ondelta };
  };
  const rowAt = (rows: AbiLength, i: number): AccountSettlement => {
    const row = abiTupleElement(buf, rows, i);
    const tokensAt = abiLengthRef(buf, row, 64);
    const tokens = Array.from({ length: countAt(tokensAt, 192) }, (_, k) => tokenAt(tokensAt, k));
    const left = wordHex(bytesToHex(abiTupleBytes(buf, row, 0)));
    const right = wordHex(bytesToHex(abiTupleBytes(buf, row, 32)));
    return { left, right, tokens, nonce: abiWord(buf, row, 96) };
  };
  const rows = abiLengthRef(buf, abiRoot(), 0);
  return Array.from({ length: countAt(rows, 32) }, (_, i) => rowAt(rows, i));
};

/** One log's topics and data words, read the way ethers' coders read them. */
type LogReader = {
  /** The first `n` indexed topics after the signature topic; a missing one throws. */
  readonly topics: (n: number) => readonly string[];
  readonly word: (slot: number) => bigint;
  readonly hash: (slot: number) => string;
  /** ethers' bool coder: any non-zero word is true. */
  readonly flag: (slot: number) => boolean;
  /** The dynamic `bytes` whose offset sits in head slot `slot`. */
  readonly bytes: (slot: number) => string;
  /** og normalizeInt: a safe integer, at least `min`. */
  readonly safe: (n: bigint, min?: bigint) => bigint;
  readonly invalid: () => never;
  readonly buf: Uint8Array;
};
const logReader = (log: ChainLog, name: JEventName, buf: Uint8Array): LogReader => {
  const head = abiRoot();
  const topics = (n: number): readonly string[] => {
    const indexed = log.topics.slice(1, 1 + n);
    if (indexed.length < n) throw new Error(`${name} log is missing an indexed topic`);
    return indexed.map(wordHex);
  };
  const word = (slot: number): bigint => abiWord(buf, head, 32 * slot);
  const hash = (slot: number): string => wordHex(bytesToHex(abiTupleBytes(buf, head, 32 * slot)));
  const bytes = (slot: number): string => bytesToHex(abiBytes(buf, abiLengthRef(buf, head, 32 * slot)));
  const safe = (n: bigint, min = 0n): bigint => (n >= min && n <= SAFE_INT ? n : invalidPayload(name));
  const invalid = (): never => invalidPayload(name);
  return { topics, word, hash, flag: (slot) => word(slot) !== 0n, bytes, safe, invalid, buf };
};
const UINT32_MASK = 0xffff_ffffn;
/** og j-event-payloads.ts: a positive safe clock whose timeout is start plus both response windows. */
const disputeClockValid = (start: bigint, timeout: bigint, left: bigint, right: bigint): boolean => {
  const positiveSafe = (n: bigint): boolean => n > 0n && n <= SAFE_INT;
  return positiveSafe(timeout) && positiveSafe(start) && timeout >= start && timeout === start + left + right;
};
/** The uint8 action kind of an EntityProvider action: 0 or 1. */
const actionKindOf = (r: LogReader, slot: number): 0 | 1 => {
  switch (r.word(slot) & 0xffn) {
    case 0n: return 0;
    case 1n: return 1;
    default: return r.invalid();
  }
};
type JEventReaders = { readonly [K in JEventName]: (r: LogReader) => Extract<JEventClaimBody, { type: K }> };
const readers: JEventReaders = {
  HankoBatchProcessed: (r) => {
    const [entityId = "", batchHash = ""] = r.topics(2);
    return { type: "HankoBatchProcessed", entityId, batchHash, nonce: r.safe(r.word(0), 1n) };
  },
  ReserveUpdated: (r) => {
    const [entity = "", token = "0x0"] = r.topics(2);
    return { type: "ReserveUpdated", entity, tokenId: r.safe(BigInt(token)), newBalance: r.word(0) };
  },
  AccountSettled: (r) => ({ type: "AccountSettled", settled: readSettled(r.buf) }),
  DisputeStarted: (r) => {
    const [sender = "", counterentity = "", nonce = "0x0"] = r.topics(3);
    const timeout = r.word(6), start = r.word(7);
    const left = r.word(8) & UINT32_MASK, right = r.word(9) & UINT32_MASK;
    if (!disputeClockValid(start, timeout, left, right)) {
      throw new Error(`J_EVENT_DISPUTE_CLOCK_INVALID:${start}:${timeout}:${left}:${right}`);
    }
    return {
      type: "DisputeStarted", sender, counterentity, nonce: BigInt(nonce),
      proposerIsLeft: r.flag(0), proofbodyHash: r.hash(1), watchSeed: r.hash(2),
      starterInitialArguments: r.bytes(3), starterCounterArguments: r.bytes(4),
      starterCounterProofCommitment: r.hash(5), disputeTimeout: timeout,
      disputeStartTimestamp: start, leftResponseSeconds: left, rightResponseSeconds: right,
    };
  },
  DisputeFinalized: (r) => {
    const [sender = "", counterentity = "", nonce = "0x0"] = r.topics(3);
    return {
      type: "DisputeFinalized", sender, counterentity, nonce: BigInt(nonce),
      finalProofbodyHash: r.hash(0), finalizationEvidenceHash: r.hash(1),
    };
  },
  SecretRevealed: (r) => {
    const [hashlock = "", revealer = ""] = r.topics(2);
    return { type: "SecretRevealed", hashlock, revealer, secret: r.hash(0) };
  },
  CounterDisputeRegistered: (r) => {
    const [sender = "", counterentity = "", nonce = "0x0"] = r.topics(3);
    return {
      type: "CounterDisputeRegistered", sender, counterentity, nonce: r.safe(BigInt(nonce)),
      proposerIsLeft: r.flag(0), proofbodyHash: r.hash(1),
    };
  },
  HashLadderRevealRegistered: (r) => {
    const [entity = "", counterpartyEntity = ""] = r.topics(2);
    const fillRatio = r.word(1) & 0xffffn;
    // og hashLadderRevealRegistered: a zero ratio reveals nothing.
    if (fillRatio === 0n) return r.invalid();
    return {
      type: "HashLadderRevealRegistered", entity, counterpartyEntity, ladderHash: r.hash(0),
      fillRatio: Number(fillRatio), fullSecret: r.hash(2),
      reveals: [r.hash(3), r.hash(4), r.hash(5), r.hash(6)], targetRole: r.flag(7),
      revealedAt: r.safe(r.word(8), 1n),
    };
  },
  DebtCreated: (r) => {
    const [debtor = "", creditor = "", token = "0x0"] = r.topics(3);
    const tokenId = r.safe(BigInt(token));
    const amount = (r.word(0) << 256n) + r.word(1);
    return { type: "DebtCreated", debtor, creditor, tokenId, amount, debtIndex: r.safe(r.word(2)) };
  },
  DebtEnforced: (r) => {
    const [debtor = "", creditor = "", token = "0x0"] = r.topics(3);
    return {
      type: "DebtEnforced", debtor, creditor, tokenId: r.safe(BigInt(token)), amountPaid: r.word(0),
      remainingAmount: (r.word(1) << 256n) + r.word(2), newDebtIndex: r.safe(r.word(3)),
    };
  },
  DebtForgiven: (r) => {
    const [debtor = "", creditor = "", token = "0x0"] = r.topics(3);
    return {
      type: "DebtForgiven", debtor, creditor, tokenId: r.safe(BigInt(token)),
      amountForgiven: (r.word(0) << 256n) + r.word(1), debtIndex: r.safe(r.word(2)),
    };
  },
  FoundationBootstrapped: (r) => {
    const [recipient = "0x0", boardHash = ""] = r.topics(2);
    return {
      type: "FoundationBootstrapped", recipient: addressFromWord(BigInt(recipient)), boardHash,
      controlTokenId: r.word(0), dividendTokenId: r.word(1),
    };
  },
  EntityRegistered: (r) => {
    const [entityId = "", entityNumber = "0x0"] = r.topics(2);
    return { type: "EntityRegistered", entityId, entityNumber: BigInt(entityNumber), boardHash: r.hash(0) };
  },
  BoardActivated: (r) => {
    const [entityId = ""] = r.topics(1);
    const until = r.word(2);
    // og boardActivated: the previous board keeps an exclusive, non-zero validity boundary.
    if (until <= 0n) return r.invalid();
    return {
      type: "BoardActivated", entityId, previousBoardHash: r.hash(0), newBoardHash: r.hash(1),
      previousBoardValidUntil: until,
    };
  },
  EntityProviderActionExecuted: (r) => {
    const [entityId = "", actionNonce = "0x0", actionHash = ""] = r.topics(3);
    const nonce = BigInt(actionNonce);
    if (nonce < 1n) return r.invalid();
    const actionKind = actionKindOf(r, 0);
    return { type: "EntityProviderActionExecuted", entityId, actionNonce: nonce, actionHash, actionKind };
  },
  EntityProviderActionCancelled: (r) => {
    const [entityId = "", actionNonce = "0x0", cancelledActionHash = ""] = r.topics(3);
    const nonce = BigInt(actionNonce);
    if (nonce < 1n) return r.invalid();
    return {
      type: "EntityProviderActionCancelled", entityId, actionNonce: nonce, cancelledActionHash,
      cancelledActionKind: actionKindOf(r, 0), cancelHash: r.hash(1),
    };
  },
};
/** The data is decoded before the topic is looked at, so malformed data throws even for an unknown log. */
const readOne = (log: ChainLog): JEventClaimBody | undefined => {
  const buf = hexToBytes(log.data);
  const topic = log.topics[0]?.toLowerCase();
  const name = topic === undefined ? undefined : eventByTopic.get(topic);
  return name === undefined ? undefined : readers[name](logReader(log, name, buf));
};
const logMeta = (l: ChainLog): JEventMeta | undefined => {
  const meta: JEventMeta = {
    ...opt("blockNumber", l.blockNumber), ...opt("blockHash", l.blockHash),
    ...opt("transactionHash", l.transactionHash), ...opt("logIndex", l.logIndex),
  };
  return Object.keys(meta).length === 0 ? undefined : meta;
};
/** og decodeJEventLog + event-normalizers.ts: an unknown topic is ignored, a malformed payload throws. */
export const readJEvents = (logs: readonly ChainLog[]): readonly JEvent[] =>
  logs.flatMap((log): JEvent[] => {
    const body = readOne(log);
    if (body === undefined) return [];
    const meta = logMeta(log);
    return [meta === undefined ? body : { ...body, meta }];
  });
export const readJEventVector = (inputs: { readonly logs: readonly ChainLog[] }): unknown => {
  const bigintsAsText = (_k: string, v: unknown) => (typeof v === "bigint" ? v.toString() : v);
  return JSON.parse(JSON.stringify(readJEvents(inputs.logs), bigintsAsText));
};
export const encodeAccountSettledData = (settled: readonly AccountSettlement[]): string => {
  const tokenAbi = (x: TokenSettlement): Abi => t([
    A.uint(x.tokenId), A.uint(x.leftReserve), A.uint(x.rightReserve), A.uint(x.collateral),
    int512Abi(x.ondelta),
  ]);
  const rowAbi = (r: AccountSettlement): Abi =>
    t([A.b32(r.left), A.b32(r.right), arr(r.tokens, tokenAbi), A.uint(r.nonce)]);
  return abiEncodeHex([arr(settled, rowAbi)]);
};


// ---- decoding Depository calldata by schema ----

/** ethers semantics: uint words as-is, bool any non-zero word, address refuses high bits. */
type AbiType =
  | "uint" | "u8" | "u16" | "u32" | "int" | "bool" | "address" | "b32" | "bytes"
  | { readonly array: AbiType }
  | { readonly tuple: readonly AbiType[] };
type AbiValue = bigint | boolean | string | readonly AbiValue[];
const abiDyn = (ty: AbiType): boolean => {
  if (typeof ty === "string") return ty === "bytes";
  return "array" in ty ? true : ty.tuple.some(abiDyn);
};
/** A static tuple is laid out inline; everything else takes one head word. */
const abiHead = (ty: AbiType): number => {
  const staticTuple = typeof ty !== "string" && "tuple" in ty && !abiDyn(ty);
  return staticTuple ? ty.tuple.reduce((n, c) => n + abiHead(c), 0) : 32;
};
const NARROW = { u8: 8n, u16: 16n, u32: 32n } as const;
const abiSig = (ty: AbiType): string => {
  if (typeof ty !== "string") return "array" in ty ? `${abiSig(ty.array)}[]` : `(${ty.tuple.map(abiSig).join(",")})`;
  switch (ty) {
    case "b32": return "bytes32";
    case "uint": return "uint256";
    case "int": return "int256";
    case "u8": case "u16": case "u32": return `uint${NARROW[ty]}`;
    default: return ty;
  }
};
/** A pointer or length past the buffer throws, as ethers does. */
const abiRead = (buf: Uint8Array, at: number): bigint => {
  if (!Number.isSafeInteger(at) || at < 0 || at + 32 > buf.length) throw new Error("ABI_DECODE_OVERRUN");
  return wordAt(buf, at);
};
/** The raw 32 bytes at `at`, bounds-checked like any word read. */
const abiWordBytes = (buf: Uint8Array, at: number): Uint8Array => {
  abiRead(buf, at);
  return buf.subarray(at, at + 32);
};
const abiBytesAt = (buf: Uint8Array, at: number): string => {
  const n = abiRead(buf, at);
  if (n > BigInt(buf.length - at - 32)) throw new Error("ABI_DECODE_OVERRUN");
  return bytesToHex(buf.subarray(at + 32, at + 32 + Number(n)));
};
const abiArrayAt = (buf: Uint8Array, item: AbiType, at: number): readonly AbiValue[] => {
  const n = abiRead(buf, at);
  if (n * 32n > BigInt(buf.length - at - 32)) throw new Error("ABI_DECODE_OVERRUN");
  const base = at + 32;
  return Array.from({ length: Number(n) }, (_, i) => abiSlot(buf, item, base, base + i * abiHead(item)));
};
const abiDecodeAt = (buf: Uint8Array, ty: AbiType, at: number): AbiValue => {
  if (typeof ty !== "string") return "array" in ty ? abiArrayAt(buf, ty.array, at) : abiSequence(buf, ty.tuple, at);
  switch (ty) {
    case "uint": return abiRead(buf, at);
    case "u8": case "u16": case "u32": return abiRead(buf, at) & ((1n << NARROW[ty]) - 1n);
    case "int": return signedWord(abiRead(buf, at));
    case "bool": return abiRead(buf, at) !== 0n;
    case "address": return addressFromWord(abiRead(buf, at));
    case "b32": return bytesToHex(abiWordBytes(buf, at));
    case "bytes": return abiBytesAt(buf, at);
  }
};
/** A dynamic value's head slot holds its offset from the start of the enclosing sequence. */
const abiSlot = (buf: Uint8Array, ty: AbiType, base: number, slotAt: number): AbiValue =>
  (abiDyn(ty) ? abiDecodeAt(buf, ty, base + Number(abiRead(buf, slotAt))) : abiDecodeAt(buf, ty, slotAt));
const abiSequence = (buf: Uint8Array, tys: readonly AbiType[], base: number): readonly AbiValue[] => {
  const [, slots] = mapAccum(tys, base, (at, ty) => [at + abiHead(ty), at] as const);
  return tys.map((ty, i) => abiSlot(buf, ty, base, slots[i] ?? base));
};
const S_PROOF: AbiType = { tuple: [
  "b32", "u32", "u32", { array: { tuple: ["int", "uint"] } }, { array: "uint" },
  { array: { tuple: ["address", "bytes", { array: { tuple: ["uint", "uint", "uint"] } }] } },
] };
const S_SIGNED: AbiType = { tuple: ["bool", "uint"] };
const S_FINAL: AbiType = {
  tuple: ["b32", "uint", "uint", "bool", "b32", S_PROOF, "bytes", "bytes", "bytes", "bool", "bool"],
};
const S_SETTLEMENT: AbiType = { tuple: [
  "b32", "b32", { array: { tuple: ["uint", S_SIGNED, S_SIGNED, S_SIGNED, S_SIGNED] } },
  { array: "uint" }, "bytes", "uint",
] };
const S_BATCH: AbiType = { tuple: [
  { array: { tuple: ["b32", "uint", "uint"] } },
  { array: { tuple: ["uint", "b32", { array: { tuple: ["b32", "uint"] } }] } },
  { array: { tuple: ["b32", "uint", "uint", "uint", "bytes"] } }, { array: S_SETTLEMENT },
  { array: { tuple: ["b32", "uint", "bool", "b32", S_PROOF, "b32", "bytes", "bytes", "bytes", "b32"] } },
  { array: { tuple: ["b32", "uint", "b32", "uint", "bool", S_PROOF, "bytes"] } },
  { array: S_FINAL }, { array: { tuple: ["b32", "address", "uint", "u8", "uint", "uint"] } },
  { array: { tuple: ["b32", "uint", "uint"] } }, { array: { tuple: ["address", "b32"] } },
  { array: { tuple: ["b32", "bool", "b32", "b32", { tuple: ["u16", "b32", "b32", "b32", "b32", "b32"] }] } },
] };
type Row = readonly AbiValue[];
const rows = (v: AbiValue | undefined): readonly Row[] => (v ?? []) as readonly Row[];
const big = (v: AbiValue | undefined): bigint => v as bigint;
const str = (v: AbiValue | undefined): string => v as string;
const yes = (v: AbiValue | undefined): boolean => v as boolean;
/** og validateJBatch `integer`: token ids, nonces, windows and ratios are safe integers. */
const int = (v: AbiValue | undefined): bigint => {
  const n = big(v);
  if (n > SAFE_INT) throw new Error("J_BATCH_ABI_RESULT_INTEGER");
  return n;
};
/** og decodeSignedAmount: a negative zero is not a SignedAmount. */
const signedOf = (v: AbiValue | undefined): bigint => {
  const [negative, magnitude] = v as Row;
  if (negative === true && magnitude === 0n) throw new Error("ABI_MONEY_NEGATIVE_ZERO");
  return negative === true ? -big(magnitude) : big(magnitude);
};
const allowanceOf = ([d, ra, la]: Row): JAllowance =>
  ({ deltaIndex: big(d), rightAllowance: big(ra), leftAllowance: big(la) });
const transformerOf = ([a, e, al]: Row): TransformerClause =>
  ({ transformerAddress: str(a), encodedBatch: str(e), allowances: rows(al).map(allowanceOf) });
const decodedProofBody = (v: AbiValue | undefined): ProofBody => {
  const [watchSeed, left, right, offdeltas, tokenIds, transformers] = v as Row;
  return {
    watchSeed: str(watchSeed), leftResponseSeconds: int(left), rightResponseSeconds: int(right),
    offdeltas: rows(offdeltas).map(([hi, lo]) => (big(hi) << 256n) + big(lo)),
    tokenIds: tokenIds as readonly bigint[], transformers: rows(transformers).map(transformerOf),
  };
};
const finalOf = (v: Row): DisputeFinalization => {
  const [counterentity, initialNonce, finalNonce, proposerIsLeft, initialProofbodyHash] = v;
  const [, , , , , finalProofbody, starterArguments, otherArguments, sig, startedByLeft, cooperative] = v;
  return {
    counterentity: str(counterentity), initialNonce: int(initialNonce), finalNonce: int(finalNonce),
    proposerIsLeft: yes(proposerIsLeft), initialProofbodyHash: str(initialProofbodyHash),
    finalProofbody: decodedProofBody(finalProofbody), starterArguments: str(starterArguments),
    otherArguments: str(otherArguments), sig: str(sig), startedByLeft: yes(startedByLeft),
    cooperative: yes(cooperative),
  };
};
const reserveTransferOf = ([e, tk, n]: Row): ReserveTransfer =>
  ({ receivingEntity: str(e), tokenId: int(tk), amount: big(n) });
const reserveToCollateralOf = ([tk, e, pairs]: Row): ReserveToCollateral => ({
  tokenId: int(tk), receivingEntity: str(e),
  pairs: rows(pairs).map(([p, n]) => ({ entity: str(p), amount: big(n) })),
});
const collateralToReserveOf = ([c, tk, n, nonce, sig]: Row): CollateralToReserve =>
  ({ counterparty: str(c), tokenId: int(tk), amount: big(n), nonce: int(nonce), sig: str(sig) });
const diffOf = ([tk, ld, rd, cd, od]: Row): SettlementDiff => ({
  tokenId: int(tk), leftDiff: signedOf(ld), rightDiff: signedOf(rd), collateralDiff: signedOf(cd),
  ondeltaDiff: signedOf(od),
});
const settlementOfRow = ([l, r, diffs, forgive, sig, nonce]: Row): Settlement => ({
  leftEntity: str(l), rightEntity: str(r), diffs: rows(diffs).map(diffOf),
  forgiveDebtsInTokenIds: (forgive as readonly AbiValue[]).map(int), sig: str(sig),
  nonce: int(nonce),
});
const disputeStartOf = ([c, nonce, left, hash, body, seed, sig, ia, ca, commit]: Row): DisputeStart => ({
  counterentity: str(c), nonce: int(nonce), proposerIsLeft: yes(left), proofbodyHash: str(hash),
  initialProofbody: decodedProofBody(body), watchSeed: str(seed), sig: str(sig),
  starterInitialArguments: str(ia), starterCounterArguments: str(ca),
  starterCounterProofCommitment: str(commit),
});
const counterDisputeOf = ([c, initialNonce, initialHash, counterNonce, left, body, sig]: Row): CounterDispute => ({
  counterentity: str(c), initialNonce: int(initialNonce), initialProofbodyHash: str(initialHash),
  counterNonce: int(counterNonce), proposerIsLeft: yes(left),
  counterProofbody: decodedProofBody(body), sig: str(sig),
});
const externalTokenToReserveOf = ([e, a, x, ty, tk, n]: Row): ExternalTokenToReserve => ({
  entity: str(e), contractAddress: str(a), externalTokenId: big(x), tokenType: int(ty),
  internalTokenId: int(tk), amount: big(n),
});
const hashLadderRegistrationOf = ([c, role, full, partial, witness]: Row): HashLadderRegistration => {
  const [fillRatio, fullSecret, r0, r1, r2, r3] = witness as Row;
  const reveals = [str(r0), str(r1), str(r2), str(r3)] as const;
  return {
    counterpartyEntity: str(c), targetRole: yes(role), fullHash: str(full),
    partialRoot: str(partial),
    witness: { fillRatio: int(fillRatio), fullSecret: str(fullSecret), reveals },
  };
};
/** og decodeJBatch: the Depository Types.Batch tuple, SignedAmounts and Int512s decoded to integers. */
export const decodeBatch = (encoded: string): Batch => {
  const [batch] = abiSequence(hexToBytes(encoded), [S_BATCH], 0);
  const [r2r, r2c, c2r, settlements, starts, counters, finals, e2r, r2e, reveals, ladders] = batch as Row;
  return {
    reserveToReserve: rows(r2r).map(reserveTransferOf),
    reserveToCollateral: rows(r2c).map(reserveToCollateralOf),
    collateralToReserve: rows(c2r).map(collateralToReserveOf),
    settlements: rows(settlements).map(settlementOfRow),
    disputeStarts: rows(starts).map(disputeStartOf),
    counterDisputes: rows(counters).map(counterDisputeOf),
    disputeFinalizations: rows(finals).map(finalOf),
    externalTokenToReserve: rows(e2r).map(externalTokenToReserveOf),
    reserveToExternalToken: rows(r2e).map(reserveTransferOf),
    revealSecrets: rows(reveals).map(([a, s]) => ({ transformer: str(a), secret: str(s) })),
    hashLadderRegistrations: rows(ladders).map(hashLadderRegistrationOf),
  };
};
const selectorOf = (sig: string): string => keccak256Hex(utf8(sig)).slice(0, 10);
const PROCESS_BATCH = ["bytes", "bytes", "uint"] as const;
const WATCHTOWER_COUNTER: readonly AbiType[] = ["b32", S_FINAL, "uint", "uint", "bytes"];
export const PROCESS_BATCH_SELECTOR = selectorOf("processBatch(bytes,bytes,uint256)");
export const WATCHTOWER_COUNTER_DISPUTE_SELECTOR =
  selectorOf(`watchtowerCounterDispute(${WATCHTOWER_COUNTER.map(abiSig).join(",")})`);
type DisputeCall = Tagged<"batch", { batch: Batch }> | Tagged<"watchtower", { proof: DisputeFinalization }>;
const decodeDisputeCall = (selector: string, args: Uint8Array, code: string): DisputeCall | undefined => {
  switch (selector) {
    case PROCESS_BATCH_SELECTOR: {
      const encoded = str(abiSequence(args, PROCESS_BATCH, 0)[0]);
      if (encoded === "0x") throw new Error(`${code}_BATCH_CALLDATA_MISSING`);
      return { _tag: "batch", batch: decodeBatch(encoded) };
    }
    case WATCHTOWER_COUNTER_DISPUTE_SELECTOR:
      return { _tag: "watchtower", proof: finalOf(abiSequence(args, WATCHTOWER_COUNTER, 0)[1] as Row) };
    default:
      return undefined;
  }
};
/** og depositoryTransactionInterface.parseTransaction over the two dispute-carrying Depository calls. */
const disputeCall = (calldata: string, code: string): DisputeCall => {
  const selector = calldata.slice(0, 10).toLowerCase();
  const args = hexToBytes(`0x${calldata.slice(10)}`);
  const decoded = (() => {
    try {
      return decodeDisputeCall(selector, args, code);
    } catch (e) {
      const message = e instanceof Error ? e.message : String(e);
      throw new Error(message.startsWith(code) ? message : `${code}_CALLDATA_DECODE_FAILED:${message}`);
    }
  })();
  if (decoded === undefined) throw new Error(`${code}_CALLDATA_DECODE_FAILED:${code}_CALLDATA_UNKNOWN`);
  return decoded;
};
export type DisputeProofEvidence = Readonly<{
  eventName: "DisputeStarted" | "CounterDisputeRegistered" | "DisputeFinalized";
  counterentity: string; nonce: bigint; proposerIsLeft: boolean; proofbodyHash: string;
  initialNonce?: bigint | undefined; initialProofbodyHash?: string | undefined;
  proofbody: ProofBody;
}>;
const startEvidence = (s: DisputeStart): DisputeProofEvidence => ({
  eventName: "DisputeStarted", counterentity: s.counterentity.toLowerCase(), nonce: s.nonce,
  proposerIsLeft: s.proposerIsLeft, proofbodyHash: s.proofbodyHash.toLowerCase(),
  proofbody: s.initialProofbody,
});
const counterEvidence = (c: CounterDispute): DisputeProofEvidence => ({
  eventName: "CounterDisputeRegistered", counterentity: c.counterentity.toLowerCase(),
  nonce: c.counterNonce, proposerIsLeft: c.proposerIsLeft,
  proofbodyHash: proofBodyHash(c.counterProofbody), proofbody: c.counterProofbody,
});
const finalizedEvidence = (f: DisputeFinalization): DisputeProofEvidence => ({
  eventName: "DisputeFinalized", counterentity: f.counterentity.toLowerCase(), nonce: f.finalNonce,
  proposerIsLeft: f.proposerIsLeft, proofbodyHash: proofBodyHash(f.finalProofbody),
  initialNonce: f.initialNonce, initialProofbodyHash: f.initialProofbodyHash.toLowerCase(),
  proofbody: f.finalProofbody,
});
/**
 * og decodeDisputeProofBodyEvidenceCalldata: every signed ProofBody a dispute transaction carries.
 * A start keeps its declared hash; the others are recomputed. A watchtower counter-dispute both
 * registers the counter and finalizes.
 */
export const disputeProofEvidence = (calldata: string): readonly DisputeProofEvidence[] =>
  match(disputeCall(calldata, "J_DISPUTE_PROOFBODY"), {
    batch: ({ batch }): readonly DisputeProofEvidence[] => [
      ...batch.disputeStarts.map(startEvidence), ...batch.counterDisputes.map(counterEvidence),
      ...batch.disputeFinalizations.map(finalizedEvidence),
    ],
    watchtower: ({ proof }): readonly DisputeProofEvidence[] => {
      const finalized = finalizedEvidence(proof);
      return [{ ...finalized, eventName: "CounterDisputeRegistered" }, finalized];
    },
  });
export type TxFinalizationEvidence = Omit<DisputeFinalizationEvidence, "sender" | "finalProofbodyHash">;
const txEvidenceOf = (f: DisputeFinalization): TxFinalizationEvidence => {
  const starter = f.starterArguments.toLowerCase(), other = f.otherArguments.toLowerCase();
  return {
    counterentity: f.counterentity.toLowerCase(), initialNonce: f.initialNonce,
    finalNonce: f.finalNonce, initialProofbodyHash: f.initialProofbodyHash.toLowerCase(),
    proposerIsLeft: f.proposerIsLeft, leftArguments: f.startedByLeft ? starter : other,
    rightArguments: f.startedByLeft ? other : starter, startedByLeft: f.startedByLeft,
    sig: f.sig.toLowerCase(),
  };
};
/** og decodeDisputeFinalizationEvidenceCalldata. */
export const finalizationEvidence = (calldata: string): readonly TxFinalizationEvidence[] =>
  match(disputeCall(calldata, "J_DISPUTE_FINALIZATION"), {
    batch: ({ batch }) => batch.disputeFinalizations.map(txEvidenceOf),
    watchtower: ({ proof }) => [txEvidenceOf(proof)],
  });
/** og hashFinalizationEvidence: the commitment DisputeFinalized logs as finalizationEvidenceHash. */
export const finalizationEvidenceHash = (c: TxFinalizationEvidence): string => {
  const starter = c.startedByLeft ? c.leftArguments : c.rightArguments;
  const other = c.startedByLeft ? c.rightArguments : c.leftArguments;
  const hashOf = (h: string): string => keccak256Hex(hexToBytes(h));
  return keccak256Hex(abiEncode([
    A.b32(c.initialProofbodyHash), A.uint(c.finalNonce), A.bool(c.proposerIsLeft),
    A.bool(c.startedByLeft), A.b32(hashOf(starter)), A.b32(hashOf(other)), A.b32(hashOf(c.sig)),
  ]));
};
type DisputeEvent = DisputeStarted | CounterDisputeRegistered | DisputeFinalized;
const isDisputeEvent = (e: JEvent): e is DisputeEvent & JEvent =>
  e.type === "DisputeStarted" || e.type === "CounterDisputeRegistered" || e.type === "DisputeFinalized";
/** The calldata's ProofBody this dispute event declares: same kind, counterparty, nonce and hash. */
const declaredProofEvidence = (event: DisputeEvent, calldata: string): DisputeProofEvidence => {
  const counter = event.counterentity.toLowerCase();
  const finalized = event.type === "DisputeFinalized";
  const declared = (finalized ? event.finalProofbodyHash : event.proofbodyHash).toLowerCase();
  const declares = (c: DisputeProofEvidence): boolean =>
    c.eventName === event.type
    && c.counterentity === counter
    && (finalized ? c.initialNonce : c.nonce) === event.nonce
    && c.proofbodyHash.toLowerCase() === declared
    && (finalized || c.proposerIsLeft === event.proposerIsLeft);
  const found = disputeProofEvidence(calldata).find(declares);
  if (found === undefined) throw new Error(`J_DISPUTE_PROOFBODY_EVIDENCE_NOT_FOUND:${event.type}:${event.nonce}`);
  return found;
};
const withFinalizationEvidence = (
  event: DisputeFinalized & JEvent, calldata: string, proofbody: ProofBody,
): JEvent => {
  const tx = event.meta?.transactionHash?.toLowerCase() ?? "";
  const candidates = finalizationEvidence(calldata);
  if (candidates.length === 0) throw new Error(`J_DISPUTE_FINALIZATION_EVIDENCE_EMPTY:${tx}`);
  const committed = (c: TxFinalizationEvidence): boolean =>
    c.counterentity === event.counterentity.toLowerCase()
    && c.initialNonce === event.nonce
    && finalizationEvidenceHash(c) === event.finalizationEvidenceHash.toLowerCase();
  const matched = candidates.find(committed);
  if (matched === undefined) throw new Error(`J_DISPUTE_FINALIZATION_EVIDENCE_NOT_FOUND:${tx}`);
  const evidence = {
    sender: event.sender.toLowerCase(), ...matched,
    finalProofbodyHash: event.finalProofbodyHash.toLowerCase(),
  };
  return { ...event, finalProofbody: proofbody, initialProofbodyHash: matched.initialProofbodyHash, evidence };
};
/**
 * og decodeCanonicalContractEvent: a dispute event takes its ProofBody (and a finalization its
 * reducer evidence) from the emitting transaction's calldata; missing evidence throws.
 */
export const withDisputeCalldata = (event: JEvent, calldata: string): JEvent => {
  if (!isDisputeEvent(event)) return event;
  const { proofbody } = declaredProofEvidence(event, calldata);
  switch (event.type) {
    case "DisputeStarted": return { ...event, initialProofbody: proofbody };
    case "CounterDisputeRegistered": return { ...event, counterProofbody: proofbody };
    case "DisputeFinalized": return withFinalizationEvidence(event, calldata, proofbody);
  }
};

// ---- what one Entity derives from finalized J events: og core/entity/tx/j-events*.ts, j-events-observations/* ----
//
// An Entity reads each finalized J block for three things: its own reserves, its debt ledger, and
// AccountSettled rows for Accounts it holds, which become j_event_claim txs for Account consensus.
// Dispute events become the Account's finality inputs.

const txKey = (e: JEvent, entity: string): string =>
  `${(e.meta?.transactionHash ?? "").toLowerCase()}:${entity.toLowerCase()}`;
/**
 * og event-observation.ts enrichDisputeBatchNonces: a dispute event takes the HankoBatchProcessed
 * nonce its sender's batch logged in the same transaction.
 */
export const withBatchNonces = (events: readonly JEvent[]): readonly JEvent[] => {
  const logged = events.flatMap((e) => {
    const keyed = e.type === "HankoBatchProcessed" && (e.meta?.transactionHash ?? "") !== "" && e.entityId !== "";
    return keyed ? [[txKey(e, e.entityId), e.nonce] as const] : [];
  });
  const nonces = new Map(logged);
  return events.map((e): JEvent => {
    if (e.type !== "DisputeStarted" && e.type !== "DisputeFinalized") return e;
    const nonce = nonces.get(txKey(e, e.sender));
    return nonce === undefined ? e : { ...e, batchNonce: Number(nonce) };
  });
};
/**
 * og expandAccountSettled + withTransportMetadata: one AccountSettled per (settlement naming the
 * Entity, token); eventIndex only when the log yields several.
 */
export const entityJEvents = (events: readonly JEvent[], entity: string): readonly JEvent[] =>
  events.flatMap((e): JEvent[] => {
    if (e.type !== "AccountSettled") return [e];
    const me = entity.toLowerCase();
    const mine = e.settled.filter((s) => s.left.toLowerCase() === me || s.right.toLowerCase() === me);
    const rows = mine.flatMap((s) => s.tokens.map((token) => ({ s, token })));
    if (rows.length === 0) throw new Error("J_EVENT_CANONICAL_PAYLOAD_EMPTY:AccountSettled");
    const meta = opt("meta", e.meta);
    return rows.map(({ s, token }, i): JEvent => {
      const indexed = rows.length > 1 ? { ...token, eventIndex: i } : token;
      const settled = { left: s.left, right: s.right, nonce: s.nonce, tokens: [indexed], ...meta };
      return { type: "AccountSettled", settled: [settled], ...meta };
    });
  });

/** og types/finance/debt.ts DebtEntry: an open debt; retired entries leave the ledger. */
export type DebtEntry = Readonly<{
  debtId: string; tokenId: number; debtor: string; creditor: string; counterparty: string;
  direction: "out" | "in"; createdAmount: bigint; paidAmount: bigint; remainingAmount: bigint;
  createdDebtIndex: number; currentDebtIndex: number; status: "open"; createdAtBlock: number;
  createdTxHash: string; lastUpdatedBlock: number; lastUpdatedTxHash: string;
  lastEventType: "DebtCreated" | "DebtEnforced";
}>;
type DebtBook = ReadonlyMap<number, ReadonlyMap<string, DebtEntry>>;
/** og EntityState outDebtsByToken / inDebtsByToken. */
export type DebtLedger = { readonly out: DebtBook; readonly in: DebtBook };
export const EMPTY_DEBTS: DebtLedger = { out: new Map(), in: new Map() };
export type JObserveError = Tagged<"j_observe", { reason: string }>;
const observeErr = (reason: string): Result<never, JObserveError> => err({ _tag: "j_observe", reason });
type DebtEvent = Extract<JEvent, { readonly type: "DebtCreated" | "DebtEnforced" | "DebtForgiven" }>;
/** Whether the observing Entity owes this debt, is owed it, or is not a party. */
const debtSide = (me: string, debtor: string, creditor: string): "out" | "in" | undefined => {
  switch (me) {
    case debtor.toLowerCase(): return "out";
    case creditor.toLowerCase(): return "in";
    default: return undefined;
  }
};
const debtsOf = (l: DebtLedger, d: DebtEntry): ReadonlyMap<string, DebtEntry> =>
  l[d.direction].get(d.tokenId) ?? new Map();
const putDebt = (l: DebtLedger, d: DebtEntry): DebtLedger => {
  const bucket = mapSet(debtsOf(l, d), d.debtId, d);
  return { ...l, [d.direction]: mapSet(l[d.direction], d.tokenId, bucket) };
};
const retireDebt = (l: DebtLedger, d: DebtEntry): DebtLedger => {
  const bucket = mapDelete(debtsOf(l, d), d.debtId);
  const book = bucket.size === 0 ? mapDelete(l[d.direction], d.tokenId) : mapSet(l[d.direction], d.tokenId, bucket);
  return { ...l, [d.direction]: book };
};
/**
 * og findEarliestOutstandingDebt: the open debt of this pair at the `preferred` index, else the
 * oldest by (created index, block, id).
 */
const earliestDebt = (
  l: DebtLedger, dir: "out" | "in", e: DebtEvent, preferred?: number,
): DebtEntry | undefined => {
  const betweenPair = (d: DebtEntry): boolean => d.status === "open"
    && d.debtor.toLowerCase() === e.debtor.toLowerCase()
    && d.creditor.toLowerCase() === e.creditor.toLowerCase();
  const open = [...(l[dir].get(Number(e.tokenId))?.values() ?? [])].filter(betweenPair);
  if (preferred !== undefined) return open.find((d) => d.currentDebtIndex === preferred);
  const oldestFirst = (x: DebtEntry, y: DebtEntry): number => x.createdDebtIndex - y.createdDebtIndex
    || x.createdAtBlock - y.createdAtBlock
    || asc(x.debtId, y.debtId);
  return open.toSorted(oldestFirst)[0];
};
type DebtContext = { readonly dir: "out" | "in"; readonly block: number; readonly txHash: string };
/** A replayed DebtCreated is accepted only when it matches the untouched entry it created. */
const debtCreated = (
  l: DebtLedger, e: Extract<DebtEvent, { type: "DebtCreated" }>, { dir, block, txHash }: DebtContext,
): Result<DebtLedger, JObserveError> => {
  const tokenId = Number(e.tokenId), index = Number(e.debtIndex);
  const debtId = `${e.debtor.toLowerCase()}:${tokenId}:${index}:${block}:${txHash.toLowerCase()}`;
  const held = l[dir].get(tokenId)?.get(debtId);
  if (held !== undefined) {
    const untouched = held.lastEventType === "DebtCreated" && held.createdAmount === e.amount
      && held.remainingAmount === e.amount && held.paidAmount === 0n;
    return untouched ? ok(l) : observeErr(`DEBT_CREATED_ID_CONFLICT:${debtId}`);
  }
  return ok(putDebt(l, {
    debtId, tokenId, debtor: e.debtor, creditor: e.creditor,
    counterparty: dir === "out" ? e.creditor : e.debtor, direction: dir,
    createdAmount: e.amount, paidAmount: 0n, remainingAmount: e.amount,
    createdDebtIndex: index, currentDebtIndex: index, status: "open",
    createdAtBlock: block, createdTxHash: txHash, lastUpdatedBlock: block, lastUpdatedTxHash: txHash,
    lastEventType: "DebtCreated",
  }));
};
/** Forgiveness names the debt by index and must forgive exactly what remains. */
const debtForgiven = (
  l: DebtLedger, e: Extract<DebtEvent, { type: "DebtForgiven" }>, { dir }: DebtContext,
): Result<DebtLedger, JObserveError> => {
  const index = Number(e.debtIndex);
  const d = earliestDebt(l, dir, e, index);
  const missing = `missing-open-debt:index=${index}:${Number(e.tokenId)}:${e.debtor}:${e.creditor}`;
  if (d === undefined) return observeErr(`DEBT_LEDGER_DIVERGENCE:DebtForgiven:${missing}`);
  if (e.amountForgiven !== d.remainingAmount) {
    return observeErr(`DEBT_FORGIVEN_AMOUNT_MISMATCH:${d.debtId}:expected=${d.remainingAmount}:actual=${e.amountForgiven}`);
  }
  return ok(retireDebt(l, d));
};
/** Enforcement pays the oldest debt; a fully paid debt retires and the chain's debt index moves on. */
const debtEnforced = (
  l: DebtLedger, e: Extract<DebtEvent, { type: "DebtEnforced" }>, { dir, block, txHash }: DebtContext,
): Result<DebtLedger, JObserveError> => {
  const d = earliestDebt(l, dir, e);
  const missing = `missing-open-debt:${Number(e.tokenId)}:${e.debtor}:${e.creditor}`;
  if (d === undefined) return observeErr(`DEBT_LEDGER_DIVERGENCE:DebtEnforced:${missing}`);
  const paysDown = e.amountPaid > 0n && e.remainingAmount >= 0n && e.amountPaid + e.remainingAmount === d.remainingAmount;
  if (!paysDown) {
    return observeErr(`DEBT_ENFORCED_AMOUNT_MISMATCH:${d.debtId}:before=${d.remainingAmount}:paid=${e.amountPaid}:after=${e.remainingAmount}`);
  }
  const paidOff = e.remainingAmount === 0n;
  const expectedIndex = paidOff ? d.currentDebtIndex + 1 : d.currentDebtIndex;
  const newIndex = Number(e.newDebtIndex);
  if (newIndex !== expectedIndex) {
    return observeErr(`DEBT_ENFORCED_INDEX_MISMATCH:${d.debtId}:expected=${expectedIndex}:actual=${newIndex}`);
  }
  if (paidOff) return ok(retireDebt(l, d));
  return ok(putDebt(l, {
    ...d, paidAmount: d.paidAmount + e.amountPaid, remainingAmount: e.remainingAmount,
    currentDebtIndex: newIndex, lastUpdatedBlock: block, lastUpdatedTxHash: txHash,
    lastEventType: "DebtEnforced",
  }));
};
/** og j-events-observations/debt.ts: a debt event updates the ledger only when this Entity is a party. */
export const applyDebtEvent = (
  l: DebtLedger, entity: string, e: DebtEvent,
): Result<DebtLedger, JObserveError> => {
  if (e.type === "DebtCreated" && e.amount <= 0n) return observeErr(`DEBT_CREATED_AMOUNT_INVALID:${e.amount}`);
  const dir = debtSide(entity.toLowerCase(), e.debtor, e.creditor);
  if (dir === undefined) return ok(l);
  const context = { dir, block: e.meta?.blockNumber ?? 0, txHash: e.meta?.transactionHash ?? "" };
  switch (e.type) {
    case "DebtCreated": return debtCreated(l, e, context);
    case "DebtForgiven": return debtForgiven(l, e, context);
    case "DebtEnforced": return debtEnforced(l, e, context);
  }
};

/** The Entity-side view og's J handlers read: reserves, debts, and which Accounts are active. */
export type JObserver = Readonly<{
  entityId: string; reserves: ReadonlyMap<number, bigint>; debts: DebtLedger;
  accounts: ReadonlyMap<string, { readonly active: boolean }>;
}>;
export type JClaimOp = { readonly accountId: string; readonly tx: TxOf<"j_event_claim"> };
export type JObservation = { readonly observer: JObserver; readonly claims: readonly JClaimOp[] };
export type JBlock = { readonly blockNumber: number; readonly events: readonly JEvent[] };
const settledRow = (row: AccountSettlement): Result<SettledEvent, JObserveError> => {
  const reason = "J_EVENT_CANONICAL_PAYLOAD_INVALID:AccountSettled";
  const token = row.tokens[0];
  if (token === undefined) return observeErr(reason);
  return mapErr(settledEvent(row, token), () => ({ _tag: "j_observe", reason }));
};
const perToken = (row: AccountSettlement): readonly AccountSettlement[] =>
  row.tokens.map((token) => ({ ...row, tokens: [token] }));
/** A claim's events in canonical order; the same settlement twice is a duplicate, not two events. */
const canonicalClaim = (op: JClaimOp): Result<JClaimOp, JObserveError> => {
  const checked = traverse(op.tx.events.flatMap(perToken), (row) => map(settledRow(row), (event) => ({ row, event })));
  return chain(checked, (rows) => {
    const sorted = rows.toSorted((x, y) => compareSettled(x.event, y.event));
    const keys = sorted.map((r) => settledKey(r.event));
    if (new Set(keys).size !== keys.length) return observeErr("ACCOUNT_J_CLAIM_EVENT_DUPLICATE");
    return ok({ ...op, tx: { ...op.tx, events: sorted.map((r) => r.row) } });
  });
};
/**
 * og mergeJEventClaimOps: one claim per (account, jHeight, block) at its first position, events in
 * canonical order, claims ordered by account, height, block.
 */
export const mergeClaimOps = (ops: readonly JClaimOp[]): Result<readonly JClaimOp[], JObserveError> => {
  const claimKey = (op: JClaimOp): string =>
    `${op.accountId.toLowerCase()}:${op.tx.jHeight}:${op.tx.jBlockHash.toLowerCase()}`;
  const merged = firstBy(ops, claimKey).map((first) => {
    const events = ops.filter((op) => claimKey(op) === claimKey(first)).flatMap((op) => op.tx.events);
    return { ...first, tx: { ...first.tx, events } };
  });
  const byAccountHeightBlock = (x: JClaimOp, y: JClaimOp): number => asc(x.accountId, y.accountId)
    || Number(x.tx.jHeight - y.tx.jHeight)
    || asc(x.tx.jBlockHash, y.tx.jBlockHash);
  return map(traverse(merged, canonicalClaim), (claims) => claims.toSorted(byAccountHeightBlock));
};
type SettledToken = { readonly row: AccountSettlement; readonly token: TokenSettlement };
/**
 * A settled token moves this Entity's reserve; with an active Account it also queues a claim at
 * the J height the row (or its log, or its block) was observed at.
 */
const observeSettledToken = (me: string, block: JBlock, e: JEvent) =>
  (acc: JObservation, { row, token }: SettledToken): JObservation => {
    const isLeft = row.left.toLowerCase() === me, isRight = row.right.toLowerCase() === me;
    if (!isLeft && !isRight) return acc;
    const counterparty = isLeft ? row.right : row.left;
    const reserve = isLeft ? token.leftReserve : token.rightReserve;
    const observer = { ...acc.observer, reserves: mapSet(acc.observer.reserves, Number(token.tokenId), reserve) };
    const account = observer.accounts.get(counterparty);
    if (account === undefined || !account.active) return { ...acc, observer };
    const meta = row.meta ?? e.meta;
    const jHeight = BigInt(row.meta?.blockNumber ?? e.meta?.blockNumber ?? block.blockNumber);
    const claim: TxOf<"j_event_claim"> = {
      type: "j_event_claim", jHeight, jBlockHash: (meta?.blockHash ?? "") as Hash,
      events: [{ ...row, tokens: [token], ...opt("meta", meta) }], observedAt: jHeight,
    };
    return { observer, claims: [...acc.claims, { accountId: counterparty, tx: claim }] };
  };
const observeEvent = (me: string, entityId: string) =>
  (acc: JObservation, { block, e }: { readonly block: JBlock; readonly e: JEvent }): Result<JObservation, JObserveError> => {
    const s = acc.observer;
    switch (e.type) {
      case "ReserveUpdated": {
        if (e.entity.toLowerCase() !== me) return ok(acc);
        return ok({ ...acc, observer: { ...s, reserves: mapSet(s.reserves, Number(e.tokenId), e.newBalance) } });
      }
      case "DebtCreated": case "DebtEnforced": case "DebtForgiven":
        return map(applyDebtEvent(s.debts, entityId, e), (debts) => ({ ...acc, observer: { ...s, debts } }));
      case "AccountSettled": {
        const tokens = e.settled.flatMap((row) => row.tokens.map((token) => ({ row, token })));
        return ok(tokens.reduce(observeSettledToken(me, block, e), acc));
      }
      default:
        return ok(acc);
    }
  };
/**
 * og applyJRangeBlocks over the reserve, settlement and debt handlers: own reserves and debts
 * update; an AccountSettled with an active Account queues a j_event_claim for Account consensus.
 */
export const observeJBlocks = (o: JObserver, blocks: readonly JBlock[]): Result<JObservation, JObserveError> => {
  const events = blocks.flatMap((block) => block.events.map((e) => ({ block, e })));
  const start: JObservation = { observer: o, claims: [] };
  const seen = foldResult(events, start, observeEvent(o.entityId.toLowerCase(), o.entityId));
  return chain(seen, ({ observer, claims }) => map(mergeClaimOps(claims), (merged) => ({ observer, claims: merged })));
};

const SAFE_UINT = BigInt(Number.MAX_SAFE_INTEGER);
const boundaryUint = (n: bigint, code: string): Result<number, JObserveError> =>
  (n >= 0n && n <= SAFE_UINT ? ok(Number(n)) : observeErr(code));
/** og requireFrozenAccountProofBody: the calldata ProofBody hashes to the logged hash and is the frozen one. */
const frozenProofBody = (
  body: ProofBody | undefined, logged: string, frozenHash: string,
): Result<ProofBody, JObserveError> => {
  const want = logged.trim().toLowerCase();
  if (!/^0x[0-9a-f]{64}$/.test(want)) return observeErr("J_EVENT_DISPUTE_FINAL_PROOFBODY_HASH_INVALID");
  if (body === undefined) return observeErr("J_EVENT_DISPUTE_FINAL_PROOFBODY_INVALID");
  if (proofBodyHash(body).toLowerCase() !== want) return observeErr("J_EVENT_DISPUTE_FINAL_PROOFBODY_HASH_MISMATCH");
  if (frozenHash.toLowerCase() !== want) return observeErr("DISPUTE_FROZEN_ACCOUNT_STATE_MISMATCH");
  return ok(body);
};
/** og applyStartedDisputeAccountInput: the Account's dispute_started finality (jNonce = the logged nonce). */
export const disputeStartedInput = (
  e: Extract<JEvent, { readonly type: "DisputeStarted" }> & { readonly batchNonce?: number | undefined },
  observedBlockNumber: number,
  frozenHash: string,
): Result<DisputeStartedFinality, JObserveError> => {
  const checked = all({
    body: frozenProofBody(e.initialProofbody, e.proofbodyHash, frozenHash),
    initialNonce: boundaryUint(e.nonce, "J_EVENT_DISPUTE_NONCE_INVALID"),
    left: boundaryUint(e.leftResponseSeconds, "J_EVENT_DISPUTE_LEFT_RESPONSE_SECONDS_INVALID"),
    right: boundaryUint(e.rightResponseSeconds, "J_EVENT_DISPUTE_RIGHT_RESPONSE_SECONDS_INVALID"),
  });
  return map(checked, ({ initialNonce, left, right }): DisputeStartedFinality => ({
    kind: "dispute_started", starterEntityId: e.sender.toLowerCase(), initialProofbodyHash: e.proofbodyHash,
    initialNonce, initialProposerIsLeft: e.proposerIsLeft, disputeTimeout: Number(e.disputeTimeout),
    disputeStartTimestamp: Number(e.disputeStartTimestamp), leftResponseSeconds: left,
    rightResponseSeconds: right, jNonce: initialNonce,
    starterInitialArguments: e.starterInitialArguments || "0x",
    starterCounterArguments: e.starterCounterArguments || "0x",
    starterCounterProofCommitment: e.starterCounterProofCommitment,
    observedBlockNumber, ...opt("batchNonce", e.batchNonce),
  }));
};
/** The Account dispute context og's resolveFinalizationEvidence reads. */
export type FinalizingDispute = Readonly<{
  jNonce: number; initialProposerIsLeft?: boolean | undefined;
  selectedCounter?: { readonly nonce: number; readonly proposerIsLeft: boolean; readonly proofbodyHash: string } | undefined;
}>;
type DisputeFinalizedEvent = Extract<JEvent, { readonly type: "DisputeFinalized" }>;
/** Evidence counts only when it describes this very finalization. */
const matchingEvidence = (e: DisputeFinalizedEvent, finalHash: string, initialHash: string) => {
  const ev = e.evidence;
  const describes = ev !== undefined
    && ev.sender.toLowerCase() === e.sender.toLowerCase()
    && ev.counterentity.toLowerCase() === e.counterentity.toLowerCase()
    && ev.initialNonce === e.nonce
    && ev.initialProofbodyHash.toLowerCase() === initialHash
    && ev.finalProofbodyHash.toLowerCase() === finalHash;
  return describes ? ev : undefined;
};
/**
 * og resolveFinalizationEvidence: the nonce the finalization spent. Finalizing on the initial body
 * without evidence, or with an unsigned initial unilateral that was not a selected counter-proof,
 * spends one more nonce than the initial one.
 */
const finalizedNonce = (
  e: DisputeFinalizedEvent, d: FinalizingDispute, initialNonce: number,
): Result<number, JObserveError> => {
  const finalHash = e.finalProofbodyHash.trim().toLowerCase();
  const initialHash = (e.initialProofbodyHash ?? "").toLowerCase();
  const onInitial = finalHash === initialHash;
  const oneMore = initialNonce >= Number.MAX_SAFE_INTEGER
    ? observeErr("J_EVENT_DISPUTE_FINAL_NONCE_OVERFLOW")
    : ok(initialNonce + 1);
  const ev = matchingEvidence(e, finalHash, initialHash);
  if (ev === undefined) return onInitial ? oneMore : ok(initialNonce);
  return chain(boundaryUint(ev.finalNonce, "J_EVENT_DISPUTE_FINAL_NONCE_INVALID"), (finalNonce) => {
    const sel = d.selectedCounter;
    const selected = sel !== undefined && sel.nonce === finalNonce
      && sel.proposerIsLeft === ev.proposerIsLeft && sel.proofbodyHash.toLowerCase() === finalHash;
    const unsigned = ev.sig.toLowerCase() === "" || ev.sig.toLowerCase() === "0x";
    const initialUnilateral = unsigned && !selected && finalNonce === initialNonce
      && ev.proposerIsLeft === d.initialProposerIsLeft && onInitial;
    return initialUnilateral ? oneMore : ok(finalNonce);
  });
};
/** og applyDisputeFinalizedJEvent: the finalized jNonce, and the token ids of the finalized body. */
export const disputeFinalizedInput = (
  e: DisputeFinalizedEvent, d: FinalizingDispute, frozenHash: string,
): Result<Extract<AccountFinality, { kind: "dispute_finalized" }>, JObserveError> =>
  chain(frozenProofBody(e.finalProofbody, e.finalProofbodyHash, frozenHash), (body) =>
    chain(boundaryUint(e.nonce, "J_EVENT_DISPUTE_INITIAL_NONCE_INVALID"), (initialNonce) =>
      map(finalizedNonce(e, d, initialNonce), (n) => ({
        kind: "dispute_finalized" as const,
        finalizedJNonce: Math.max(d.jNonce, n),
        finalizedTokenIds: body.tokenIds.map(Number),
      }))));
// ---- the J batch: og core/jurisdiction/machine/batch/*, entity/tx/handlers/j-batch/*, j-events-batch.ts ----
//
// An Entity accumulates Depository ops in a draft batch. Broadcasting seals the draft (recovered
// work first, dispute work ahead of the rest) at the next entity nonce and leaves it in flight as
// the sent batch until the chain's HankoBatchProcessed event finalizes or quarantines it.

/** og JTx batch `feeOverrides` (types/jurisdiction-runtime.ts). */
export type FeeOverrides = Readonly<{
  gasBumpBps?: number | undefined; maxFeePerGasWei?: string | undefined;
  maxPriorityFeePerGasWei?: string | undefined;
}>;
export type SentJBatch = Readonly<{
  batch: Batch; batchHash: string; encodedBatch: string; entityNonce: number; firstSubmittedAt: number;
  lastSubmittedAt: number; submitAttempts: number;
  terminalFailure?: { readonly message: string; readonly failedAt: number } | undefined;
  feeOverrides?: FeeOverrides | undefined;
}>;
/** og JBatchState: the editable draft, the in-flight batch, recovered work, the chain's entity nonce. */
export type JBatchState = Readonly<{
  batch: Batch; jurisdiction: null; lastBroadcast: number; broadcastCount: number; failedAttempts: number;
  status: "empty" | "accumulating" | "sent" | "failed"; sentBatch?: SentJBatch | undefined;
  recoveryBatches?: readonly Batch[] | undefined; autoBroadcastDraft?: boolean | undefined;
  entityNonce?: number | undefined;
}>;
export const initJBatch = (): JBatchState =>
  ({ batch: emptyBatch(), jurisdiction: null, lastBroadcast: 0, broadcastCount: 0, failedAttempts: 0,
    status: "empty" });
export const J_BATCH_LIMITS = {
  maxTotalOps: 50, maxSettlements: 32, maxSettlementDiffs: 32, maxSettlementForgivenessIds: 32,
  maxDisputeStarts: 8, maxCounterDisputes: 8, maxDisputeFinalizations: 1, maxReserveToCollateralPairs: 64,
  maxReserveToCollateralPairsTotal: 256, maxSecretReveals: 32, maxHashLadderRegistrations: 32,
} as const;
export const batchOpCount = (b: Batch): number => BATCH_FIELDS.reduce((n, f) => n + b[f].length, 0);
const batchEmpty = (b: Batch): boolean => batchOpCount(b) === 0;
export const hasJBatchWork = (s: JBatchState): boolean =>
  (s.recoveryBatches ?? []).some((b) => !batchEmpty(b)) || !batchEmpty(s.batch);
export type JBatchError = Tagged<"j_batch", { reason: string }>;
const batchErr = (reason: string): Result<never, JBatchError> => err({ _tag: "j_batch", reason });
const pairCount = (b: Batch): number => b.reserveToCollateral.reduce((n, op) => n + op.pairs.length, 0);
const overLimit = (name: string, n: number, max: number): string | undefined =>
  (n > max ? `${name} ${n}/${max}` : undefined);
const reserveToCollateralIssues = (op: ReserveToCollateral, i: number): readonly (string | undefined)[] => {
  const at = `reserveToCollateral[${i}]`;
  return [
    op.tokenId <= 0n || op.tokenId > SAFE_UINT ? `${at}.tokenId must be a positive safe integer` : undefined,
    op.pairs.length === 0 ? `${at}.pairs must not be empty` : undefined,
    op.pairs.some((p) => p.amount <= 0n) ? `${at}.pairs amounts must be positive` : undefined,
    overLimit(`${at}.pairs`, op.pairs.length, J_BATCH_LIMITS.maxReserveToCollateralPairs),
  ];
};
const settlementIssues = (s: Settlement, i: number): readonly (string | undefined)[] => [
  overLimit(`settlements[${i}].diffs`, s.diffs.length, J_BATCH_LIMITS.maxSettlementDiffs),
  overLimit(`settlements[${i}].forgiveDebtsInTokenIds`, s.forgiveDebtsInTokenIds.length,
    J_BATCH_LIMITS.maxSettlementForgivenessIds),
];
/** og getJBatchContractLimitIssue: the first limit the Depository contract would refuse. */
export const jBatchLimitIssue = (b: Batch): string | undefined => {
  const L = J_BATCH_LIMITS;
  const issues = [
    overLimit("total ops", batchOpCount(b), L.maxTotalOps),
    overLimit("settlements", b.settlements.length, L.maxSettlements),
    overLimit("disputeStarts", b.disputeStarts.length, L.maxDisputeStarts),
    overLimit("counterDisputes", b.counterDisputes.length, L.maxCounterDisputes),
    overLimit("disputeFinalizations", b.disputeFinalizations.length, L.maxDisputeFinalizations),
    overLimit("revealSecrets", b.revealSecrets.length, L.maxSecretReveals),
    overLimit("hashLadderRegistrations", b.hashLadderRegistrations.length, L.maxHashLadderRegistrations),
    ...b.reserveToCollateral.flatMap(reserveToCollateralIssues),
    overLimit("reserveToCollateral.pairs total", pairCount(b), L.maxReserveToCollateralPairsTotal),
    ...b.settlements.flatMap(settlementIssues),
  ];
  return issues.find((issue) => issue !== undefined);
};

// -- reserve simulation: what Depository._processBatch would do to the initiator's own reserves --

export type ReserveOpType = "reserveToReserve" | "settlement" | "reserveToCollateral" | "reserveToExternalToken";
/** og DraftBatchReserveIssue: the op whose outflow reverts the whole batch on chain. */
export type ReserveIssue = Readonly<{
  tokenId: number; opType: ReserveOpType; opIndex: number; failureMode: "batchRevert"; requiredAmount: bigint;
  availableAfterDebt: bigint; debtClaimPaid: bigint; remainingDebtAfterSweep: bigint; unrepaidDeficit: bigint;
}>;
export type ReserveSimulation = Readonly<{
  issues: readonly ReserveIssue[]; reservesByToken: ReadonlyMap<number, bigint>;
  outgoingDebtByToken: ReadonlyMap<number, bigint>; deficitByToken: ReadonlyMap<number, bigint>;
}>;
/** og getOpenOutgoingDebtTotals: the open outgoing debt per token. */
export const openOutgoingDebtTotals = (book: DebtBook): ReadonlyMap<number, bigint> => {
  const openTotal = (bucket: ReadonlyMap<string, DebtEntry>): bigint =>
    [...bucket.values()].reduce((n, d) => (d.status === "open" ? n + d.remainingAmount : n), 0n);
  return new Map([...book].flatMap(([tokenId, bucket]) => {
    const total = openTotal(bucket);
    return total > 0n ? [[tokenId, total] as const] : [];
  }));
};
type DebtSweep = Readonly<{ availableAfterDebt: bigint; debtClaimPaid: bigint; remainingDebtAfterSweep: bigint }>;
type OpRef = Readonly<{ opType: ReserveOpType; opIndex: number; requiredAmount: bigint }>;
/** The initiator's books while the batch runs: a deficit is flash credit that must be repaid by the end. */
type ReserveBooks = Readonly<{
  reserves: ReadonlyMap<number, bigint>; debt: ReadonlyMap<number, bigint>;
  deficit: ReadonlyMap<number, bigint>; origins: ReadonlyMap<number, DebtSweep & OpRef>;
  issues: readonly ReserveIssue[];
}>;
const amountOf = (m: ReadonlyMap<number, bigint>, t: number): bigint => m.get(t) ?? 0n;
const withAmount = (m: ReadonlyMap<number, bigint>, t: number, a: bigint): ReadonlyMap<number, bigint> =>
  (a === 0n ? mapDelete(m, t) : mapSet(m, t, a));
const smaller = (a: bigint, b: bigint): bigint => (a < b ? a : b);
/** An inflow repays any open deficit first; the rest lands in reserve. */
const credit = (books: ReserveBooks, t: number, a: bigint): ReserveBooks => {
  if (a <= 0n) return books;
  const owed = amountOf(books.deficit, t);
  const repaid = smaller(a, owed);
  const deficit = withAmount(books.deficit, t, owed - repaid);
  const rest = a - repaid;
  return rest > 0n
    ? { ...books, deficit, reserves: withAmount(books.reserves, t, amountOf(books.reserves, t) + rest) }
    : { ...books, deficit };
};
type Debit = Readonly<{ books: ReserveBooks; outcome: "spent" | "deficit" | "rejected" }>;
/** An outflow beyond reserve opens a deficit, but only for an initiator with no debt left. */
const debit = (books: ReserveBooks, t: number, a: bigint, remainingDebt: bigint): Debit => {
  const reserve = amountOf(books.reserves, t);
  if (reserve >= a) {
    return { books: { ...books, reserves: withAmount(books.reserves, t, reserve - a) }, outcome: "spent" };
  }
  if (remainingDebt !== 0n) return { books, outcome: "rejected" };
  const deficit = withAmount(books.deficit, t, amountOf(books.deficit, t) + (a - reserve));
  return { books: { ...books, deficit, reserves: withAmount(books.reserves, t, 0n) }, outcome: "deficit" };
};
/** Before any outflow the Depository pays the token's debt, oldest first, out of reserve. */
const sweep = (books: ReserveBooks, t: number): { readonly books: ReserveBooks; readonly swept: DebtSweep } => {
  const reserve = amountOf(books.reserves, t), owed = amountOf(books.debt, t);
  const paid = smaller(reserve, owed);
  const reserves = withAmount(books.reserves, t, reserve - paid);
  const debt = withAmount(books.debt, t, owed - paid);
  const swept = { availableAfterDebt: reserve - paid, debtClaimPaid: paid, remainingDebtAfterSweep: owed - paid };
  return { books: { ...books, reserves, debt }, swept };
};
/** The first op that opened a token's deficit is the one blamed if it is never repaid. */
const noteOrigin = (books: ReserveBooks, t: number, swept: DebtSweep, op: OpRef): ReserveBooks =>
  (books.origins.has(t) ? books : { ...books, origins: mapSet(books.origins, t, { ...swept, ...op }) });
const revert = (books: ReserveBooks, t: number, swept: DebtSweep, op: OpRef, unrepaidDeficit: bigint): ReserveBooks => {
  const { opType, opIndex, requiredAmount } = op;
  const issue: ReserveIssue = {
    tokenId: t, opType, opIndex, failureMode: "batchRevert", requiredAmount, ...swept, unrepaidDeficit,
  };
  return { ...books, issues: [...books.issues, issue] };
};
type Spend = { readonly books: ReserveBooks; readonly spent: boolean };
const spendReserve = (books: ReserveBooks, t: number, op: OpRef): Spend => {
  const swept = sweep(books, t);
  const paid = debit(swept.books, t, op.requiredAmount, swept.swept.remainingDebtAfterSweep);
  switch (paid.outcome) {
    case "rejected": return { books: revert(paid.books, t, swept.swept, op, 0n), spent: false };
    case "deficit": return { books: noteOrigin(paid.books, t, swept.swept, op), spent: true };
    case "spent": return { books: paid.books, spent: true };
  }
};
/** Settlements sweep every token they debit before any settlement runs, so the sweeps are tallied first. */
const settlementSweeps = (books: ReserveBooks, settlements: readonly Settlement[], ownDiff: OwnDiff) => {
  const debited = settlements.flatMap((s) => s.diffs.filter((d) => ownDiff(s, d) < 0n));
  const start = { books, sweeps: new Map<number, DebtSweep>() as ReadonlyMap<number, DebtSweep> };
  return debited.reduce((acc, d) => {
    const t = Number(d.tokenId);
    const { books: next, swept } = sweep(acc.books, t);
    const paidBefore = acc.sweeps.get(t)?.debtClaimPaid ?? 0n;
    const tally = { ...swept, debtClaimPaid: paidBefore + swept.debtClaimPaid };
    return { books: next, sweeps: mapSet(acc.sweeps, t, tally) };
  }, start);
};
type OwnDiff = (s: Settlement, d: SettlementDiff) => bigint;
const applySettlement = (sweeps: ReadonlyMap<number, DebtSweep>, ownDiff: OwnDiff, me: string) =>
  (books: ReserveBooks, s: Settlement, i: number): ReserveBooks => {
    if (s.leftEntity.toLowerCase() !== me && s.rightEntity.toLowerCase() !== me) return books;
    const headroom = (t: number): bigint => {
      const reserve = amountOf(books.reserves, t), debt = amountOf(books.debt, t);
      return reserve > debt ? reserve - debt : 0n;
    };
    const snapshot = (b: ReserveBooks, t: number, available: bigint): DebtSweep => ({
      availableAfterDebt: available, debtClaimPaid: sweeps.get(t)?.debtClaimPaid ?? 0n,
      remainingDebtAfterSweep: amountOf(b.debt, t),
    });
    // A debtor cannot draw flash credit, so an uncovered debit reverts the whole settlement.
    const blocked = s.diffs.find((d) => {
      const own = ownDiff(s, d), t = Number(d.tokenId);
      return own < 0n && headroom(t) < -own && amountOf(books.debt, t) !== 0n;
    });
    if (blocked !== undefined) {
      const t = Number(blocked.tokenId);
      const op = { opType: "settlement" as const, opIndex: i, requiredAmount: -ownDiff(s, blocked) };
      return revert(books, t, snapshot(books, t, headroom(t)), op, 0n);
    }
    return s.diffs.reduce((b, d) => {
      const own = ownDiff(s, d), t = Number(d.tokenId);
      if (own >= 0n) return credit(b, t, own);
      const before = snapshot(b, t, amountOf(b.reserves, t));
      const paid = debit(b, t, -own, before.remainingDebtAfterSweep);
      const op = { opType: "settlement" as const, opIndex: i, requiredAmount: -own };
      return paid.outcome === "deficit" ? noteOrigin(paid.books, t, before, op) : paid.books;
    }, books);
  };
/** A deficit still open at the end reverts the batch, blamed on the op that opened it. */
const unrepaidDeficit = (books: ReserveBooks): ReserveBooks => {
  const open = [...books.deficit].find(([, d]) => d !== 0n);
  if (open === undefined) return books;
  const [t, owed] = open;
  const origin = books.origins.get(t);
  if (origin === undefined) throw new Error(`DRAFT_BATCH_DEFICIT_WITHOUT_ORIGIN:${t}`);
  const { opType, opIndex, requiredAmount, ...swept } = origin;
  return revert(books, t, swept, { opType, opIndex, requiredAmount }, owed);
};
/**
 * og simulateDraftBatchReserveAvailability: Depository._processBatch for the initiator's reserves,
 * op kinds in the contract's order. A batch with any issue leaves the books as they were.
 */
export const simulateBatchReserves = (
  entity: string, reserves: ReadonlyMap<number, bigint>, batch: Batch, outgoingDebt: ReadonlyMap<number, bigint>,
): ReserveSimulation => {
  const me = entity.toLowerCase();
  const start: ReserveBooks = { reserves, debt: outgoingDebt, deficit: new Map(), origins: new Map(), issues: [] };
  const ownDiff: OwnDiff = (s, d) => {
    if (s.leftEntity.toLowerCase() === me) return d.leftDiff;
    return s.rightEntity.toLowerCase() === me ? d.rightDiff : 0n;
  };
  const externalIn = batch.externalTokenToReserve
    .filter((op) => (op.entity === "" ? me : op.entity.toLowerCase()) === me)
    .reduce((b, op) => credit(b, Number(op.internalTokenId), op.amount), start);
  const reserveTransfers = batch.reserveToReserve.reduce((b, op, i) => {
    const t = Number(op.tokenId);
    const out = spendReserve(b, t, { opType: "reserveToReserve", opIndex: i, requiredAmount: op.amount });
    return out.spent && op.receivingEntity.toLowerCase() === me ? credit(out.books, t, op.amount) : out.books;
  }, externalIn);
  const collateralIn = batch.collateralToReserve
    .reduce((b, op) => credit(b, Number(op.tokenId), op.amount), reserveTransfers);
  const { books: swept, sweeps } = settlementSweeps(collateralIn, batch.settlements, ownDiff);
  const settled = batch.settlements.reduce(applySettlement(sweeps, ownDiff, me), swept);
  const deposited = batch.reserveToCollateral.reduce((b, op, i) => {
    const requiredAmount = op.pairs.reduce((n, p) => n + p.amount, 0n);
    return spendReserve(b, Number(op.tokenId), { opType: "reserveToCollateral", opIndex: i, requiredAmount }).books;
  }, settled);
  const withdrawn = batch.reserveToExternalToken.reduce((b, op, i) => {
    const ref = { opType: "reserveToExternalToken" as const, opIndex: i, requiredAmount: op.amount };
    return spendReserve(b, Number(op.tokenId), ref).books;
  }, deposited);
  const end = unrepaidDeficit(withdrawn);
  if (end.issues.length > 0) {
    const untouched = { reservesByToken: reserves, outgoingDebtByToken: outgoingDebt, deficitByToken: new Map() };
    return { issues: end.issues, ...untouched };
  }
  return { issues: [], reservesByToken: end.reserves, outgoingDebtByToken: end.debt, deficitByToken: end.deficit };
};

// -- queueing reserve ops into the draft --

export type ReserveCandidate =
  | Readonly<{ type: "reserveToReserve"; receivingEntity: string; tokenId: number; amount: bigint }>
  | Readonly<{ type: "reserveToExternalToken"; receivingEntity: string; tokenId: number; amount: bigint }>
  | Readonly<{
    type: "reserveToCollateral"; receivingEntity: string; counterparty: string; tokenId: number; amount: bigint;
  }>;
/** og appendCandidate: an R2C joins its (receivingEntity, tokenId) entry, adding to or opening its pair. */
const appendCandidate = (b: Batch, c: ReserveCandidate): { readonly batch: Batch; readonly index: number } => {
  if (c.type !== "reserveToCollateral") {
    const op = { receivingEntity: c.receivingEntity, tokenId: BigInt(c.tokenId), amount: c.amount };
    return { batch: { ...b, [c.type]: [...b[c.type], op] }, index: b[c.type].length };
  }
  const tokenId = BigInt(c.tokenId);
  const sameEntry = (op: ReserveToCollateral): boolean => op.receivingEntity === c.receivingEntity && op.tokenId === tokenId;
  const at = b.reserveToCollateral.findIndex(sameEntry);
  const pair = { entity: c.counterparty, amount: c.amount };
  if (at < 0) {
    const entry = { tokenId, receivingEntity: c.receivingEntity, pairs: [pair] };
    const index = b.reserveToCollateral.length;
    return { batch: { ...b, reserveToCollateral: [...b.reserveToCollateral, entry] }, index };
  }
  const entry = b.reserveToCollateral[at]!;
  const hasPair = entry.pairs.some((p) => p.entity === c.counterparty);
  const pairs = hasPair
    ? entry.pairs.map((p) => (p.entity === c.counterparty ? { ...p, amount: p.amount + c.amount } : p))
    : [...entry.pairs, pair];
  const reserveToCollateral = b.reserveToCollateral.map((x, i) => (i === at ? { ...x, pairs } : x));
  return { batch: { ...b, reserveToCollateral }, index: at };
};
/** The Entity-side view og's j-batch handlers read. */
export type JEntity = Readonly<{
  entityId: string; reserves: ReadonlyMap<number, bigint>; debts: DebtLedger;
  jBatch?: JBatchState | undefined; accounts: ReadonlySet<string>;
}>;
/** og getReserveCandidateIssue: simulate the draft plus the candidate; its own issue, else the first. */
export const reserveCandidateIssue = (e: JEntity, c: ReserveCandidate): ReserveIssue | undefined => {
  const { batch, index } = appendCandidate(e.jBatch?.batch ?? emptyBatch(), c);
  const sim = simulateBatchReserves(e.entityId, e.reserves, batch, openOutgoingDebtTotals(e.debts.out));
  return sim.issues.find((i) => i.opType === c.type && i.opIndex === index) ?? sim.issues[0];
};
const accumulating = (s: JBatchState, batch: Batch): JBatchState =>
  ({ ...s, batch, status: s.status === "empty" ? "accumulating" : s.status });
const batchRoom = (b: Batch, op: string): Result<void, JBatchError> =>
  guard(batchOpCount(b) + 1 <= J_BATCH_LIMITS.maxTotalOps,
    { _tag: "j_batch", reason: `J_BATCH_LIMIT_EXCEEDED: ${op}` });
type AppendedField = "reserveToReserve" | "reserveToExternalToken" | "externalTokenToReserve";
/** og batchAddReserveToReserve / ToExternal / ExternalTokenToReserve: one op within the 50-op limit. */
const addOp = <F extends AppendedField>(
  s: JBatchState, field: F, op: Batch[F][number],
): Result<JBatchState, JBatchError> =>
  map(batchRoom(s.batch, field), () => accumulating(s, { ...s.batch, [field]: [...s.batch[field], op] }));
/** og batchAddReserveToCollateral: aggregate into the (receivingEntity, tokenId) entry within the pair limits. */
export const addReserveToCollateral = (
  s: JBatchState, entity: string, counterparty: string, tokenId: number, amount: bigint,
): Result<JBatchState, JBatchError> => {
  if (amount <= 0n) return batchErr("R2C_AMOUNT_MUST_BE_POSITIVE");
  if (!Number.isSafeInteger(tokenId) || tokenId <= 0) return batchErr("R2C_TOKEN_ID_INVALID");
  if (entity === "" || counterparty === "" || entity === counterparty) return batchErr("R2C_ACCOUNT_PARTIES_INVALID");
  const L = J_BATCH_LIMITS;
  const candidate = { type: "reserveToCollateral" as const, receivingEntity: entity, counterparty, tokenId, amount };
  const added = (): Result<JBatchState, JBatchError> => ok(accumulating(s, appendCandidate(s.batch, candidate).batch));
  const sameEntry = (op: ReserveToCollateral): boolean => op.receivingEntity === entity && op.tokenId === BigInt(tokenId);
  const entry = s.batch.reserveToCollateral.find(sameEntry);
  // Topping up an existing pair adds no op and no pair, so no limit can refuse it.
  if (entry !== undefined && entry.pairs.some((p) => p.entity === counterparty)) return added();
  if (entry === undefined && batchOpCount(s.batch) + 1 > L.maxTotalOps) {
    return batchErr("J_BATCH_LIMIT_EXCEEDED: reserveToCollateral");
  }
  if (pairCount(s.batch) + 1 > L.maxReserveToCollateralPairsTotal) {
    return batchErr("J_BATCH_LIMIT_EXCEEDED: reserveToCollateral.pairs total");
  }
  if (entry !== undefined && entry.pairs.length + 1 > L.maxReserveToCollateralPairs) {
    return batchErr("J_BATCH_LIMIT_EXCEEDED: reserveToCollateral.pairs");
  }
  return added();
};
/** `note`: og's addMessage text when the handler refuses without throwing (the Entity stays as it was). */
export type JQueued = { readonly jBatch: JBatchState; readonly note?: string | undefined };
const insufficient = (issue: ReserveIssue, amount: bigint, tokenId: number): string =>
  `Insufficient spendable reserve: have ${issue.availableAfterDebt}, need ${amount} token ${tokenId}`;
/** og handleR2R: debt-aware reserve admission, then queue; reserves move when the batch finalizes. */
export const queueR2R = (e: JEntity, to: string, tokenId: number, amount: bigint): Result<JBatchState, JBatchError> => {
  const issue = reserveCandidateIssue(e, { type: "reserveToReserve", receivingEntity: to, tokenId, amount });
  if (issue !== undefined) return batchErr(insufficient(issue, amount, tokenId));
  return addOp(e.jBatch ?? initJBatch(), "reserveToReserve", { receivingEntity: to, tokenId: BigInt(tokenId), amount });
};
/** og handleR2E. */
export const queueR2E = (
  e: JEntity, receivingEntity: string, tokenId: number, amount: bigint,
): Result<JBatchState, JBatchError> => {
  const issue = reserveCandidateIssue(e, { type: "reserveToExternalToken", receivingEntity, tokenId, amount });
  if (issue !== undefined) return batchErr(insufficient(issue, amount, tokenId));
  const op = { receivingEntity, tokenId: BigInt(tokenId), amount };
  return addOp(e.jBatch ?? initJBatch(), "reserveToExternalToken", op);
};
export type ExternalDeposit = Readonly<{
  contractAddress: string; amount: bigint; tokenType?: number | undefined;
  externalTokenId?: bigint | undefined; internalTokenId?: number | undefined;
}>;
/** og handleE2R: a non-zero token contract and a positive amount, credited when the batch executes. */
export const queueE2R = (e: JEntity, x: ExternalDeposit): Result<JBatchState, JBatchError> => {
  if (usableAddress(x.contractAddress) === null) {
    return batchErr(`Invalid external token contract: ${x.contractAddress}`);
  }
  if (x.amount <= 0n) return batchErr("External → Reserve amount must be positive");
  return addOp(e.jBatch ?? initJBatch(), "externalTokenToReserve", {
    entity: e.entityId, contractAddress: x.contractAddress, externalTokenId: x.externalTokenId ?? 0n,
    tokenType: BigInt(x.tokenType ?? 0), internalTokenId: BigInt(x.internalTokenId ?? 0), amount: x.amount,
  });
};
/**
 * og r2c.ts collectRebalanceFee: a deposit carrying `rebalanceQuoteId` needs the Account's accepted
 * `shadow.rebalance.activeQuote`. og core never writes activeQuote (only clears it), so from
 * consensus the quote is always absent and the deposit is refused with a note.
 */
export type RebalanceFee = Readonly<{
  rebalanceQuoteId: number; rebalanceFeeAmount?: bigint | undefined; rebalanceFeeTokenId?: number | undefined;
}>;
/** og handleR2C (no rebalance quote): every refusal is a note, and the draft stays as it was. */
export const queueR2C = (
  e: JEntity, counterparty: string, tokenId: number, amount: bigint, receivingEntityId?: string, fee?: RebalanceFee,
): Result<JQueued, JBatchError> => {
  const receiving = (receivingEntityId ?? e.entityId).trim().toLowerCase();
  const local = receiving === e.entityId.trim().toLowerCase();
  const unchanged = e.jBatch ?? initJBatch();
  const refused = (note: string): Result<JQueued, JBatchError> => ok({ jBatch: unchanged, note });
  if (amount <= 0n || !Number.isSafeInteger(tokenId) || tokenId <= 0) {
    return refused("Collateral deposit requires a positive amount and registered tokenId");
  }
  if (receiving === "" || receiving === counterparty.toLowerCase()) {
    return refused("Collateral deposit requires two distinct non-empty entities");
  }
  const candidate = { type: "reserveToCollateral" as const, receivingEntity: receiving, counterparty, tokenId, amount };
  const issue = reserveCandidateIssue(e, candidate);
  if (issue !== undefined) {
    const shortfall = `have ${issue.availableAfterDebt}, need ${amount} token ${tokenId}`;
    return refused(`Insufficient spendable reserve for collateral deposit: ${shortfall}`);
  }
  if (local && !e.accounts.has(counterparty)) return refused("Cannot deposit collateral: no account");
  if (fee !== undefined) {
    return refused(local
      ? `Rebalance fee: no active quote for ${counterparty.slice(-4)}`
      : "Rebalance fee unsupported for remote reserve → account deposits");
  }
  return map(addReserveToCollateral(unchanged, receiving, counterparty, tokenId, amount), (jBatch) => ({ jBatch }));
};

// -- broadcasting, finalizing and recovering the sent batch --

type BroadcastSplit = { readonly selected: Batch; readonly remainder: Batch; readonly disputePriority: boolean };
/**
 * og takeBroadcastBatch: dispute work broadcasts alone and first. Hash-ladder registrations ride
 * with the starts they support; otherwise one finalization goes at a time.
 */
export const takeBroadcastBatch = (b: Batch): BroadcastSplit => {
  const disputeWork = b.disputeStarts.length + b.counterDisputes.length + b.disputeFinalizations.length > 0;
  if (!disputeWork) return { selected: b, remainder: emptyBatch(), disputePriority: false };
  const base = { disputeStarts: b.disputeStarts, counterDisputes: b.counterDisputes, revealSecrets: b.revealSecrets };
  const taken = { disputeStarts: [], counterDisputes: [], revealSecrets: [] };
  if (b.hashLadderRegistrations.length > 0) {
    const selected = { ...emptyBatch(), ...base, hashLadderRegistrations: b.hashLadderRegistrations };
    return { selected, remainder: { ...b, ...taken, hashLadderRegistrations: [] }, disputePriority: true };
  }
  const selected = { ...emptyBatch(), ...base, disputeFinalizations: b.disputeFinalizations.slice(0, 1) };
  const remainder = { ...b, ...taken, disputeFinalizations: b.disputeFinalizations.slice(1) };
  return { selected, remainder, disputePriority: true };
};
/** og JTx `batch` as j_broadcast emits it, before the quorum Hanko is attached. */
export type JBatchTx = Readonly<{
  type: "batch"; entityId: string; timestamp: number;
  data: Readonly<{
    batch: Batch; batchHash: string; encodedBatch: string; entityNonce: number; batchGeneration: number;
    batchSize: number; signerId: string; feeOverrides?: FeeOverrides | undefined;
  }>;
}>;
/** og HashToSign of type "jBatch": the Entity quorum signs the batch hash for its Hanko. */
export type JBatchHashToSign = { readonly hash: string; readonly type: "jBatch"; readonly context: string };
export type Broadcast = Readonly<{
  jBatch: JBatchState; jTx?: JBatchTx | undefined; hashToSign?: JBatchHashToSign | undefined; note?: string | undefined;
}>;
type BroadcastContext = Readonly<{
  entityId: string; chainId: number; depository: string; signerId: string; timestamp: number;
}>;
const unusableDepository = (ctx: BroadcastContext): boolean =>
  !domainOf({ chainId: ctx.chainId, depositoryAddress: ctx.depository }).ok || /^0x0{40}$/.test(ctx.depository);
const sealedHash = (ctx: BroadcastContext, encodedBatch: string, nonce: number): string =>
  encodeBatchHash({ chainId: ctx.chainId, depository: ctx.depository, encodedBatch, nonce: String(nonce) });
/** Recovered work goes first: a non-empty recovery batch is sealed before the draft. */
const nextToSeal = (s: JBatchState) => {
  const recovery = s.recoveryBatches?.[0];
  const fromRecovery = recovery !== undefined && !batchEmpty(recovery);
  return { fromRecovery, ...takeBroadcastBatch(fromRecovery ? recovery : s.batch) };
};
/** What stays behind once `selected` is sealed: its remainder goes back where it came from. */
const parkRemainder = (s: JBatchState, fromRecovery: boolean, remainder: Batch): JBatchState => {
  const later = (s.recoveryBatches ?? []).slice(1);
  const queue = !fromRecovery ? s.recoveryBatches : batchEmpty(remainder) ? later : [remainder, ...later];
  const { recoveryBatches: _r, autoBroadcastDraft: _a, ...rest } = s;
  const parked = { ...rest, batch: fromRecovery ? s.batch : remainder };
  return queue === undefined || queue.length === 0 ? parked : { ...parked, recoveryBatches: queue };
};
/**
 * og handleJBroadcast: refuse while a batch is in flight; skip an empty draft; seal the next batch
 * at entityNonce + 1, park the rest, and hand its batch hash to the quorum.
 */
export const jBroadcast = (
  s: JBatchState | undefined, ctx: BroadcastContext & { readonly feeOverrides?: FeeOverrides | undefined },
): Result<Broadcast, JBatchError> => {
  if (s === undefined) return batchErr("No jBatchState found for j_broadcast");
  const sent = s.sentBatch;
  if (sent !== undefined) {
    return batchErr(`Cannot broadcast: sentBatch pending nonce=${sent.entityNonce} attempts=${sent.submitAttempts}`);
  }
  if (!hasJBatchWork(s)) return ok({ jBatch: s, note: "j_broadcast skipped: jBatch is empty" });
  if (ctx.chainId === 0) return ok({ jBatch: s, note: "Missing chainId" });
  if (ctx.signerId === "") return ok({ jBatch: s, note: "No signerId available" });
  const { fromRecovery, selected, remainder } = nextToSeal(s);
  if (unusableDepository(ctx)) return batchErr(`INVALID_HANKO_DOMAIN:${ctx.chainId}:${ctx.depository}`);
  const limit = jBatchLimitIssue(selected);
  if (limit !== undefined) return batchErr(`J_BATCH_LIMIT_EXCEEDED: j_broadcast: ${limit}`);
  return map(encodeJBatch(selected), (encodedBatch): Broadcast => {
    const nonce = (s.entityNonce ?? 0) + 1;
    const batchHash = sealedHash(ctx, encodedBatch, nonce);
    const generation = s.broadcastCount + 1;
    const fee = ctx.feeOverrides === undefined ? {} : { feeOverrides: { ...ctx.feeOverrides } };
    const parked = parkRemainder(s, fromRecovery, remainder);
    const sentBatch: SentJBatch = {
      batch: selected, batchHash, encodedBatch, entityNonce: nonce, firstSubmittedAt: ctx.timestamp,
      lastSubmittedAt: 0, submitAttempts: 0, ...fee,
    };
    const jBatch: JBatchState = {
      ...parked, ...(hasJBatchWork(parked) ? { autoBroadcastDraft: true } : {}),
      sentBatch, broadcastCount: generation, lastBroadcast: ctx.timestamp, status: "sent",
    };
    const data = {
      batch: selected, batchHash, encodedBatch, entityNonce: nonce, batchGeneration: generation, ...fee,
      batchSize: batchOpCount(selected), signerId: ctx.signerId,
    };
    const jTx: JBatchTx = { type: "batch", entityId: ctx.entityId, data, timestamp: ctx.timestamp };
    const context = `jBatch:${ctx.entityId.slice(-4)}:nonce:${nonce}`;
    return { jBatch, jTx, hashToSign: { hash: batchHash, type: "jBatch", context } };
  });
};
/**
 * `autoBroadcast`: og finalizePendingBatch queues a follow-up j_broadcast when the sealed batch
 * parked work behind it.
 */
export type BatchProcessed = { readonly jBatch: JBatchState | undefined; readonly autoBroadcast: boolean };
/**
 * og applyHankoBatchProcessedEvent: the chain nonce is authoritative. The exact pending (nonce,
 * hash) finalizes; a different hash at or past the pending nonce quarantines the pending batch.
 */
export const applyHankoBatchProcessed = (
  s: JBatchState | undefined, entity: string, e: Extract<JEvent, { readonly type: "HankoBatchProcessed" }>,
  timestamp: number,
): Result<BatchProcessed, JBatchError> => {
  if (e.entityId.toLowerCase() !== entity.toLowerCase()) return ok({ jBatch: s, autoBroadcast: false });
  const hash = e.batchHash.trim().toLowerCase();
  if (!/^0x[0-9a-f]{64}$/.test(hash)) return batchErr("J_BATCH_EVENT_BATCH_HASH_INVALID");
  if (e.nonce < 1n || e.nonce > SAFE_UINT) return batchErr("J_BATCH_EVENT_NONCE_INVALID");
  const cur = s ?? initJBatch();
  const nonce = Number(e.nonce);
  const synced = Math.max(cur.entityNonce ?? 0, nonce);
  const sent = cur.sentBatch;
  const finalizesPending = sent !== undefined && sent.entityNonce === nonce && sent.batchHash.toLowerCase() === hash;
  if (finalizesPending) {
    const { sentBatch: _s, ...rest } = cur;
    const jBatch: JBatchState = { ...rest, status: hasJBatchWork(cur) ? "accumulating" : "empty", entityNonce: synced };
    return ok({ jBatch, autoBroadcast: jBatch.autoBroadcastDraft === true && hasJBatchWork(jBatch) });
  }
  if (sent === undefined || nonce < sent.entityNonce) {
    return ok({ jBatch: { ...cur, entityNonce: synced }, autoBroadcast: false });
  }
  const pending = `pending=${sent.batchHash.toLowerCase()}:pendingNonce=${sent.entityNonce}:finalizedNonce=${nonce}`;
  const message = `J_BATCH_NONCE_CONSUMED_BY_DIFFERENT_HASH:${hash}:${pending}`;
  const quarantined = { ...sent, terminalFailure: { message, failedAt: timestamp } };
  const jBatch: JBatchState = { ...cur, entityNonce: synced, status: "failed", sentBatch: quarantined };
  return ok({ autoBroadcast: false, jBatch });
};
/** og normalizeGasBumpBps: floored and clamped to [0, 20000]; a non-finite bump is dropped. */
const gasBump = (v: number | undefined): number | undefined =>
  (v === undefined || !Number.isFinite(Number(v)) ? undefined : Math.min(20_000, Math.max(0, Math.floor(Number(v)))));
/**
 * og handleJRebroadcast: resend the sent batch exactly as stored, re-sealed at its own nonce with
 * the next generation; an empty stale sent batch is dropped instead.
 */
export const jRebroadcast = (
  s: JBatchState | undefined, ctx: BroadcastContext & { readonly gasBumpBps?: number | undefined },
): Result<Broadcast, JBatchError> => {
  const sent = s?.sentBatch;
  if (s === undefined || sent === undefined) {
    return ok({ jBatch: s ?? initJBatch(), note: "⚠️ j_rebroadcast skipped: no sentBatch" });
  }
  if (sent.terminalFailure !== undefined) {
    const failure = sent.terminalFailure.message;
    return batchErr(`❌ Cannot rebroadcast quarantined jBatch nonce=${sent.entityNonce}: ${failure}`);
  }
  if (ctx.signerId === "") return batchErr("❌ No signerId available for j_rebroadcast");
  const bump = gasBump(ctx.gasBumpBps);
  if (ctx.chainId === 0) return ok({ jBatch: s, note: "❌ Missing chainId for j_rebroadcast" });
  if (unusableDepository(ctx)) {
    return ok({ jBatch: s, note: "❌ Jurisdiction unavailable for j_rebroadcast: INVALID_DEPOSITORY_ADDRESS" });
  }
  const generation = s.broadcastCount + 1;
  if (batchEmpty(sent.batch)) {
    const { sentBatch: _s, ...rest } = s;
    const note = `🧹 j_rebroadcast cleared empty stale sentBatch nonce=${sent.entityNonce}`;
    return ok({ jBatch: { ...rest, status: batchEmpty(s.batch) ? "empty" : "accumulating" }, note });
  }
  return map(encodeJBatch(sent.batch), (encodedBatch): Broadcast => {
    const batchHash = sealedHash(ctx, encodedBatch, sent.entityNonce);
    const fee = bump === undefined ? {} : { feeOverrides: { gasBumpBps: bump } };
    const sentBatch = { ...sent, batchHash, encodedBatch, ...fee };
    const jBatch: JBatchState = {
      ...s, sentBatch, lastBroadcast: ctx.timestamp, broadcastCount: generation, status: "sent",
    };
    const data = {
      batch: sent.batch, batchHash, encodedBatch, entityNonce: sent.entityNonce, batchGeneration: generation,
      batchSize: batchOpCount(sent.batch), signerId: ctx.signerId, ...fee,
    };
    const context = `jBatch:${ctx.entityId.slice(-4)}:nonce:${sent.entityNonce}:rebroadcast`;
    return {
      jBatch,
      note: `📤 Rebroadcast intent queued nonce=${sent.entityNonce}${bump === undefined ? "" : ` bump=${bump}bps`}`,
      jTx: { type: "batch", entityId: ctx.entityId, data, timestamp: ctx.timestamp },
      hashToSign: { hash: batchHash, type: "jBatch", context },
    };
  });
};
/** Latches a recovery releases on the Entity's Accounts: R2C submitted markers and finalize latches. */
export type JLatchRelease = Readonly<{
  submitted: readonly { readonly accountId: string; readonly tokenId: number }[]; finalizers: readonly string[];
}>;
export type JRecovered = Readonly<{ jBatch: JBatchState | undefined; note: string; release: JLatchRelease }>;
const NOTHING_RELEASED: JLatchRelease = { submitted: [], finalizers: [] };
const finalizersOf = (b: Batch): readonly string[] =>
  b.disputeFinalizations.map((op) => op.counterentity.toLowerCase());
/**
 * og handleJAbortSentBatch: requeue (the default) drops dispute finalizations and C2Rs the Account's
 * jNonce already spent, then parks the rest as the first recovery batch; drop instead releases the
 * R2C submitted markers.
 */
export const jAbortSentBatch = (
  s: JBatchState | undefined,
  x: { readonly requeueToCurrent?: boolean | undefined; readonly reason?: string | undefined },
  jNonceOf: (counterparty: string) => number,
): JRecovered => {
  const sent = s?.sentBatch;
  if (s === undefined || sent === undefined) {
    return { jBatch: s, note: "⚠️ No sentBatch to abort", release: NOTHING_RELEASED };
  }
  const requeue = x.requeueToCurrent !== false;
  const notYetSpent = (op: CollateralToReserve): boolean => op.nonce > BigInt(jNonceOf(op.counterparty.toLowerCase()));
  const unspent = sent.batch.collateralToReserve.filter(notYetSpent);
  const kept: Batch = { ...sent.batch, disputeFinalizations: [], collateralToReserve: unspent };
  const { sentBatch: _s, recoveryBatches, ...rest } = s;
  const queue = requeue && !batchEmpty(kept) ? [kept, ...(recoveryBatches ?? [])] : recoveryBatches;
  const parked: JBatchState = { ...rest, ...(queue === undefined ? {} : { recoveryBatches: queue }) };
  const submitted = requeue ? [] : sent.batch.reserveToCollateral.flatMap((op) =>
    op.pairs.map((p) => ({ accountId: p.entity.toLowerCase(), tokenId: Number(op.tokenId) })));
  const how = requeue ? " (requeued to current)" : " (dropped)";
  const why = x.reason ? ` (${x.reason})` : "";
  const note = `🛑 Aborted sentBatch nonce=${sent.entityNonce} ops=${batchOpCount(sent.batch)}${how}${why}`;
  const finalizers = [...new Set(finalizersOf(sent.batch))];
  const jBatch: JBatchState = { ...parked, status: hasJBatchWork(parked) ? "accumulating" : "empty" };
  return { jBatch, note, release: { submitted, finalizers } };
};
/**
 * og handleJClearBatch: drop the draft, the sent batch and every recovery batch; release every
 * Account's submitted markers and the dropped finalize latches.
 */
export const jClearBatch = (
  s: JBatchState | undefined, x: { readonly reason?: string | undefined },
  submittedTokens: ReadonlyMap<string, readonly number[]>,
): JRecovered => {
  if (s === undefined) return { jBatch: s, note: "⚠️ No jBatchState to clear", release: NOTHING_RELEASED };
  const recovery = s.recoveryBatches ?? [];
  const sentBatches = s.sentBatch === undefined ? [] : [s.sentBatch.batch];
  const finalizers = [...new Set([s.batch, ...sentBatches, ...recovery].flatMap(finalizersOf))];
  const submitted = [...submittedTokens].flatMap(([accountId, tokens]) =>
    tokens.toSorted((a, b) => a - b).map((tokenId) => ({ accountId, tokenId })));
  const recoveryOps = recovery.reduce((n, b) => n + batchOpCount(b), 0);
  const note = [
    `🗑️ Cleared jBatch current=${batchOpCount(s.batch)}`,
    s.sentBatch === undefined ? "" : ` [sentBatch=${batchOpCount(s.sentBatch.batch)} ops]`,
    recoveryOps > 0 ? ` [recoveryBatch=${recoveryOps} ops]` : "",
    x.reason ? ` (${x.reason})` : "",
    submitted.length > 0 ? `; reset ${submitted.length} submitted rebalance marker(s)` : "",
  ].join("");
  const { sentBatch: _s, recoveryBatches: _r, ...rest } = s;
  return { jBatch: { ...rest, batch: emptyBatch(), status: "empty" }, note, release: { submitted, finalizers } };
};
/** og JTx `mint` (handleMintReserves): a direct admin mint, outside the batch. */
export type JMintTx = Readonly<{
  type: "mint"; entityId: string; timestamp: number;
  data: { readonly entityId: string; readonly tokenId: number; readonly amount: bigint };
}>;
export const mintReservesTx = (
  entityId: string, tokenId: number, amount: bigint, timestamp: number,
): { readonly jTx: JMintTx; readonly note: string } => ({
  jTx: { type: "mint", entityId, data: { entityId, tokenId, amount }, timestamp },
  note: `💰 Minting ${amount} of token ${tokenId}`,
});

// ---- Depository digests: what the contract hashes before it checks a signature ----

type Word = string;
/** og computeAccountKey (contract-codec.ts:6): two bytes32 words, ordered and packed lowercase. */
export const encodeAccountKey = (
  { e1, e2 }: { readonly e1: Word; readonly e2: Word },
): { readonly lesserThenGreater: string; readonly greaterThenLesser: string } => {
  const bytes32Text = (e: string): string => {
    if (!/^0[xX][0-9a-fA-F]{64}$/.test(e)) throw new Error(`account key entity is not bytes32: ${e}`);
    return e.toLowerCase();
  };
  const a = bytes32Text(e1), b = bytes32Text(e2);
  const [lesser, greater] = a < b ? [a, b] : [b, a];
  return { lesserThenGreater: joinHex([lesser, greater]), greaterThenLesser: joinHex([greater, lesser]) };
};
type AllowanceText = { readonly deltaIndex: string; readonly rightAllowance: string; readonly leftAllowance: string };
type ProofBodyText = Readonly<{
  watchSeed: Word; leftResponseSeconds: number; rightResponseSeconds: number; offdeltas: readonly string[];
  tokenIds: readonly string[];
  transformers: readonly Readonly<{
    transformerAddress: string; encodedBatch: string; allowances: readonly AllowanceText[];
  }>[];
}>;
const proofBodyOfText = (b: ProofBodyText): ProofBody => ({
  watchSeed: b.watchSeed,
  leftResponseSeconds: BigInt(b.leftResponseSeconds),
  rightResponseSeconds: BigInt(b.rightResponseSeconds),
  offdeltas: b.offdeltas.map((x) => BigInt(x)),
  tokenIds: b.tokenIds.map((x) => BigInt(x)),
  transformers: b.transformers.map((c) => ({
    transformerAddress: c.transformerAddress,
    encodedBatch: c.encodedBatch,
    allowances: c.allowances.map((a) => ({
      deltaIndex: BigInt(a.deltaIndex),
      rightAllowance: BigInt(a.rightAllowance),
      leftAllowance: BigInt(a.leftAllowance),
    })),
  })),
});
export const encodeProofBody = (inputs: { readonly proofBody: ProofBodyText }): string =>
  keccak256Hex(hexToBytes(encodeProofBodyBytes(proofBodyOfText(inputs.proofBody))));
/** og requireDepositoryDomain (onchain-domain.ts:139): chainId > 0 and a real depository, or no digest. */
const depositoryDomain = (chainId: number, depository: string): bigint => {
  const usable = domainOf({ chainId, depositoryAddress: depository }).ok && !/^0x0{40}$/.test(depository);
  if (!usable) throw new Error(`INVALID_HANKO_DOMAIN:${chainId}:${depository}`);
  return BigInt(chainId);
};
export const DEPOSITORY_BATCH_HANKO_DOMAIN = keccak256Hex(utf8("XLN_DEPOSITORY_HANKO_V1"));
type SignedMessageHeader = Readonly<{
  messageType: number; chainId: number; contractAddress: string; accountKey: string; nonce: string;
}>;
/** Every Account message the Depository signs starts with the same domain-bound header. */
const signedMessageHeader = (i: SignedMessageHeader): readonly Abi[] => [
  A.uint(BigInt(i.messageType)),
  A.uint(depositoryDomain(i.chainId, i.contractAddress)),
  A.address(i.contractAddress),
  A.bytes(i.accountKey),
  A.uint(BigInt(i.nonce)),
];
export const encodeDisputeProofHash = (
  i: SignedMessageHeader & Readonly<{ proposerIsLeft: boolean; proofbodyHash: Word; watchSeed: Word }>,
): string => keccak256Hex(abiEncode([
  ...signedMessageHeader(i), A.bool(i.proposerIsLeft), A.b32(i.proofbodyHash), A.b32(i.watchSeed),
]));
type DiffText = Readonly<{
  tokenId: string; leftDiff: string; rightDiff: string; collateralDiff: string; ondeltaDiff: string;
}>;
const diffOfText = (d: DiffText): SettlementDiff => ({
  tokenId: BigInt(d.tokenId), leftDiff: BigInt(d.leftDiff), rightDiff: BigInt(d.rightDiff),
  collateralDiff: BigInt(d.collateralDiff), ondeltaDiff: BigInt(d.ondeltaDiff),
});
export const encodeCooperativeUpdateHash = (
  i: SignedMessageHeader & Readonly<{ diffs: readonly DiffText[]; forgiveDebtsInTokenIds: readonly string[] }>,
): string => keccak256Hex(abiEncode([
  ...signedMessageHeader(i),
  arr(i.diffs, (d) => diffAbi(diffOfText(d))),
  arr(i.forgiveDebtsInTokenIds, (id) => A.uint(BigInt(id))),
]));
export const encodeBatchHash = (
  i: Readonly<{ chainId: number; depository: string; encodedBatch: string; nonce: string }>,
): string => keccak256Hex(encodePacked([
  { _tag: "bytes32", value: DEPOSITORY_BATCH_HANKO_DOMAIN },
  { _tag: "uint256", value: depositoryDomain(i.chainId, i.depository) },
  { _tag: "address", value: i.depository },
  { _tag: "bytes", value: i.encodedBatch },
  { _tag: "uint256", value: BigInt(i.nonce) },
]));
type DisputeCase = Readonly<{
  nonce: string; startedByLeft: boolean; initialProposerIsLeft: boolean; timeout: string;
  leftResponseSeconds: number; rightResponseSeconds: number; proofbodyHash: Word; disputeStartTimestamp: string;
  starterInitialArguments: string; starterCounterArguments: string; starterCounterProofCommitment: Word;
}>;
/** The on-chain dispute record's hash; the trailing zero fields are the counter-dispute slots, still empty. */
export const encodeDisputeHash = ({ cases }: { readonly cases: readonly DisputeCase[] }): readonly string[] =>
  cases.map((c) => {
    const commitment = (args: string): string =>
      keccak256Hex(abiEncode([A.bytes(args), A.bool(c.startedByLeft), A.uint(BigInt(c.disputeStartTimestamp))]));
    return keccak256Hex(encodePacked([
      { _tag: "uint256", value: BigInt(c.nonce) },
      { _tag: "bool", value: c.startedByLeft },
      { _tag: "bool", value: c.initialProposerIsLeft },
      { _tag: "uint256", value: BigInt(c.timeout) },
      { _tag: "uint32", value: c.leftResponseSeconds },
      { _tag: "uint32", value: c.rightResponseSeconds },
      { _tag: "bytes32", value: c.proofbodyHash },
      { _tag: "uint256", value: BigInt(c.disputeStartTimestamp) },
      { _tag: "bytes32", value: commitment(c.starterInitialArguments) },
      { _tag: "bytes32", value: commitment(c.starterCounterArguments) },
      { _tag: "bytes32", value: c.starterCounterProofCommitment },
      { _tag: "uint256", value: 0n },
      { _tag: "bytes32", value: ZERO_WORD },
      { _tag: "bool", value: false },
    ]));
  });


// ---- Hanko: an Entity's signature, og protocol/hanko/* ----
//
// A Hanko carries packed ECDSA signatures, placeholders for board members who did not sign, and
// claims. A claim is one Entity's board: members by index (placeholders, then signers, then earlier
// claims) with weights and a threshold. The last claim is the signing Entity; the claims before it
// are nested Entities that vote in it. A claim's board hashes to its entity id when the Entity is
// lazy; otherwise the board must be the registered one.

const HALF_ORDER = secp256k1.CURVE.n >> 1n;
export type Board = Readonly<{
  votingThreshold: number; entityIds: readonly string[]; votingPowers: readonly number[];
  boardChangeDelay: number; controlChangeDelay: number; dividendChangeDelay: number;
}>;
type Verdict = { readonly entityId: string; readonly valid: boolean };
const INVALID: Verdict = { entityId: ZERO_WORD, valid: false };
export type HankoError = Tagged<
  | "duplicate_signer" | "duplicate_entity_index" | "duplicate_claim_entity" | "claim_order" | "weight" | "threshold"
  | "unused_signature" | "unused_placeholder" | "unused_claim" | "packed" | "decode"
>;
export type HankoClaimInput = Readonly<{
  entityId: string; entityIndexes: readonly number[]; weights: readonly number[]; threshold: number;
  boardChangeDelay: number; controlChangeDelay: number; dividendChangeDelay: number;
}>;
export type HankoEnvelope = Readonly<{
  placeholders: readonly string[]; packedSignatures: Uint8Array; claims: readonly HankoClaimInput[];
  memberSignatures: readonly string[];
}>;
type Delays = Readonly<{ boardChangeDelay: bigint; controlChangeDelay: bigint; dividendChangeDelay: bigint }>;
type HankoClaim = Delays & Readonly<{
  entityId: string; entityIndexes: readonly bigint[]; weights: readonly bigint[]; threshold: bigint;
}>;
type HankoBytes = Readonly<{
  placeholders: readonly string[]; packedSignatures: Uint8Array; claims: readonly HankoClaim[];
  memberSignatures: readonly Uint8Array[];
}>;
export type RawSig = { readonly r: Uint8Array; readonly s: Uint8Array; readonly v: number };
const boardAbi = (b: Board): Abi => t([
  A.uint(BigInt(b.votingThreshold)),
  arr(b.entityIds, A.b32),
  arr(b.votingPowers, (p) => A.uint(BigInt(p))),
  A.uint(BigInt(b.boardChangeDelay)),
  A.uint(BigInt(b.controlChangeDelay)),
  A.uint(BigInt(b.dividendChangeDelay)),
]);
const boardHashOf = (b: Board): string => keccak256Hex(abiEncode([boardAbi(b)]));
const boardOf = (threshold: bigint, ids: readonly string[], powers: readonly number[], c: Delays): string =>
  boardHashOf({
    votingThreshold: Number(threshold), entityIds: ids, votingPowers: powers,
    boardChangeDelay: Number(c.boardChangeDelay), controlChangeDelay: Number(c.controlChangeDelay),
    dividendChangeDelay: Number(c.dividendChangeDelay),
  });
const addressAsId = (a: string): string => bytesToHex(addressWord(a));
/** EIP-55: a hex letter is upper case when the matching nibble of the address hash is 8 or more. */
export const checksum = (a: string): string => {
  const hex = a.slice(2).toLowerCase();
  const h = keccak256(utf8(hex));
  const nibbleAt = (i: number): number => ((h[i >> 1] ?? 0) >> (i % 2 === 0 ? 4 : 0)) & 0xf;
  return `0x${[...hex].map((c, i) => (nibbleAt(i) >= 8 ? c.toUpperCase() : c)).join("")}`;
};
export const addressOf = (publicKey: Uint8Array): string =>
  checksum(bytesToHex(keccak256(publicKey.slice(1)).slice(12)));
/** A lazy Entity's id is the hash of its one-signer board. */
export const encodeLazyEntityId = ({ signer }: { readonly signer: string }): string => boardHashOf({
  votingThreshold: 1, entityIds: [addressAsId(signer)], votingPowers: [1],
  boardChangeDelay: 0, controlChangeDelay: 0, dividendChangeDelay: 0,
});
export const encodeBoardHash = ({ board }: { readonly board: Board }): string => boardHashOf(board);
export const encodeBoardBytes = (board: Board): string => abiEncodeHex([boardAbi(board)]);
const claimAbi = (c: HankoClaim): Abi => t([
  A.b32(c.entityId),
  arr(c.entityIndexes, A.uint),
  arr(c.weights, A.uint),
  A.uint(c.threshold),
  A.uint(c.boardChangeDelay),
  A.uint(c.controlChangeDelay),
  A.uint(c.dividendChangeDelay),
]);
const claimWords = (c: HankoClaimInput): HankoClaim => ({
  entityId: c.entityId,
  entityIndexes: c.entityIndexes.map((x) => BigInt(x)),
  weights: c.weights.map((x) => BigInt(x)),
  threshold: BigInt(c.threshold),
  boardChangeDelay: BigInt(c.boardChangeDelay),
  controlChangeDelay: BigInt(c.controlChangeDelay),
  dividendChangeDelay: BigInt(c.dividendChangeDelay),
});
const envelopeHex = (h: HankoBytes): string => abiEncodeHex([t([
  arr(h.placeholders, A.b32),
  A.bytes(bytesToHex(h.packedSignatures)),
  arr(h.claims, claimAbi),
  arr(h.memberSignatures, (s) => A.bytes(bytesToHex(s))),
])]);
export const encodeHankoEnvelope = (h: HankoEnvelope): string => envelopeHex({
  placeholders: h.placeholders,
  packedSignatures: h.packedSignatures,
  claims: h.claims.map(claimWords),
  memberSignatures: h.memberSignatures.map((s) => hexToBytes(s)),
});
const isZeroWord = (w: Uint8Array): boolean => w.every((b) => b === 0);
const lowS = (s: Uint8Array): boolean => wordAt(s, 0) <= HALF_ORDER;
/** og assertCanonicalSignature (codec.ts:277): 32-byte r and s, v in {27,28}, non-zero r and s, low s. */
const canonicalSig = (sig: RawSig): boolean => sig.r.length === 32 && sig.s.length === 32
  && (sig.v === 27 || sig.v === 28) && !isZeroWord(sig.r) && !isZeroWord(sig.s) && lowS(sig.s);
/** r and s of every signature, then one recovery bit per signature, eight to a byte. */
export const packSignatures = (sigs: readonly RawSig[]): Uint8Array => {
  if (sigs.length === 0) return new Uint8Array();
  sigs.forEach((sig, i) => {
    if (!canonicalSig(sig)) throw new Error(`HANKO_SIGNATURE_NON_CANONICAL:${i}`);
  });
  const bitsByte = (byte: number): number => sigs.slice(byte * 8, byte * 8 + 8)
    .reduce((bits, sig, k) => (sig.v === 28 ? bits | (1 << k) : bits), 0);
  const bits = Uint8Array.from({ length: Math.ceil(sigs.length / 8) }, (_, byte) => bitsByte(byte));
  return concat([...sigs.flatMap((sig) => [sig.r, sig.s]), bits]);
};
/** How many signatures a packed blob of this length holds; null when no count fits exactly. */
const packedCount = (byteLength: number): number | null => {
  if (byteLength === 0) return 0;
  const count = Math.floor((byteLength * 8) / 513);
  return count === 0 || count * 64 + Math.ceil(count / 8) !== byteLength ? null : count;
};
const packedPaddingClear = (packed: Uint8Array, count: number): boolean =>
  count % 8 === 0 || ((packed[packed.length - 1] ?? 0) >> (count % 8)) === 0;
type PackedSig = { readonly r: Uint8Array; readonly s: Uint8Array; readonly bit: number };
const packedAt = (packed: Uint8Array, count: number, i: number): PackedSig => ({
  r: packed.subarray(i * 64, i * 64 + 32),
  s: packed.subarray(i * 64 + 32, i * 64 + 64),
  bit: ((packed[count * 64 + (i >> 3)] ?? 0) >> (i & 7)) & 1,
});
const chainV = (v: number): number => (v < 27 ? v + 27 : v);
export const recoverRawSigner = (hashHex: string, signatureHex: string): string | null => {
  const raw = parseHex(signatureHex), h = parseHex(hashHex);
  if (raw === null || h === null || raw.length !== 65 || h.length !== 32) return null;
  const s = raw.subarray(32, 64), v = chainV(raw[64] ?? 0);
  if ((v !== 27 && v !== 28) || !lowS(s)) return null;
  const key = recoverPublicKey(h, raw.subarray(0, 32), s, v - 27);
  return key === null ? null : addressOf(key);
};
export const encodeHanko65 = (
  i: Readonly<{ hash: string; hanko: string; registration: null }>,
): Verdict => {
  const signer = recoverRawSigner(i.hash, i.hanko);
  return signer === null ? INVALID : { entityId: encodeLazyEntityId({ signer }), valid: true };
};
export type Registration = { readonly encodedBoard: string; readonly entityId: string };
/** og verifyCanonicalHanko: a registration pins the target Entity and its board; without one there is no target. */
export const verifyHankoLocal = (
  hankoHex: string, hashHex: string, registration: Registration | null,
): Result<Verdict, HankoError> => {
  const encodedBoard = registration === null ? null : parseHex(registration.encodedBoard);
  if (registration !== null && encodedBoard === null) return err({ _tag: "decode" });
  const boardHash = encodedBoard === null ? undefined : keccak256Hex(encodedBoard);
  const verified = verifyAccountHanko(hankoHex, hashHex, registration?.entityId ?? "", boardHash);
  return ok(verified.ok ? { entityId: verified.value.entityId, valid: true } : INVALID);
};
const verdictOf = (r: Result<Verdict, HankoError>): Verdict => unwrapOr(r, () => INVALID);
export const encodeHankoBytes = (
  i: Readonly<{ encodedBoard: string; entityId: string; hash: string; twoOfThree: string; oneOfThree: string }>,
): { readonly twoOfThree: Verdict; readonly oneOfThree: Verdict } => ({
  twoOfThree: verdictOf(verifyHankoLocal(i.twoOfThree, i.hash, i)),
  oneOfThree: verdictOf(verifyHankoLocal(i.oneOfThree, i.hash, i)),
});
/** The canonical signature a board member gave, if any; of two spellings of one signer, the last wins. */
const signatureFor = (signedBy: ReadonlyMap<string, string>, entityWord: string): RawSig | null => {
  const addr = bytesToHex(hexToBytes(entityWord).subarray(12));
  const found = [...signedBy].findLast(([signer]) => sameHex(signer, addr))?.[1];
  if (found === undefined) return null;
  const raw = hexToBytes(found.startsWith("0x") ? found : `0x${found}`);
  if (raw.length !== 65) return null;
  const sig = { r: raw.subarray(0, 32), s: raw.subarray(32, 64), v: chainV(raw[64] ?? 0) };
  return canonicalSig(sig) ? sig : null;
};
/** A single-claim Hanko over the board: members who signed go in the packed signatures, the rest are placeholders. */
export const encodeBoardHanko = (
  board: Board & { readonly entityId: string }, signedBy: ReadonlyMap<string, string>,
): string => {
  const slots = board.entityIds.map((id) => ({ id, sig: signatureFor(signedBy, id) }));
  const placeholders = slots.flatMap((s) => (s.sig === null ? [s.id] : []));
  const sigs = slots.flatMap((s) => (s.sig === null ? [] : [s.sig]));
  const indexOf = (slot: (typeof slots)[number], i: number): number => {
    const sameKindBefore = slots.slice(0, i).filter((b) => (b.sig === null) === (slot.sig === null)).length;
    return slot.sig === null ? sameKindBefore : placeholders.length + sameKindBefore;
  };
  const claim = {
    entityId: board.entityId, entityIndexes: slots.map(indexOf), weights: board.votingPowers,
    threshold: board.votingThreshold, boardChangeDelay: board.boardChangeDelay,
    controlChangeDelay: board.controlChangeDelay, dividendChangeDelay: board.dividendChangeDelay,
  };
  const packedSignatures = packSignatures(sigs);
  return encodeHankoEnvelope({ placeholders, packedSignatures, memberSignatures: [], claims: [claim] });
};
export const boardVotingPower = (board: Board, signedBy: ReadonlyMap<string, string>): number => {
  const powerOf = (id: string, i: number): number =>
    (signatureFor(signedBy, id) === null ? 0 : board.votingPowers[i] ?? 0);
  return board.entityIds.reduce((sum, id, i) => sum + powerOf(id, i), 0);
};
const listOf = <X>(buf: Uint8Array, at: AbiLength, read: (i: number) => X): Result<X[], HankoError> => {
  const count = abiLengthWord(buf, at);
  if (!abiFits(buf, at, count, 32)) return err({ _tag: "decode" });
  return ok(Array.from({ length: Number(count) }, (_, i) => read(i)));
};
const wordsAt = (buf: Uint8Array, at: AbiTuple, slot: number): Result<bigint[], HankoError> => {
  const list = abiLengthRef(buf, at, slot);
  return listOf(buf, list, (i) => abiStaticWord(buf, list, i));
};
/** null when the claim's cursor itself is out of range: og then decodes the whole Hanko as empty. */
const decodeClaim = (buf: Uint8Array, at: AbiTuple): Result<HankoClaim, HankoError> | null => {
  if (!abiCursorOk(at)) return null;
  return chain(wordsAt(buf, at, 32), (entityIndexes) => map(wordsAt(buf, at, 64), (weights): HankoClaim => ({
    entityId: bytesToHex(abiTupleBytes(buf, at, 0)),
    entityIndexes,
    weights,
    threshold: abiWord(buf, at, 96),
    boardChangeDelay: abiWord(buf, at, 128),
    controlChangeDelay: abiWord(buf, at, 160),
    dividendChangeDelay: abiWord(buf, at, 192),
  })));
};
const NO_HANKO: HankoBytes = { placeholders: [], packedSignatures: new Uint8Array(), claims: [], memberSignatures: [] };
const decodeHanko = (buf: Uint8Array): Result<HankoBytes, HankoError> => {
  const body = abiTupleRef(buf, abiRoot(), 0);
  if (!abiCursorOk(body)) return ok(NO_HANKO);
  const placeholdersAt = abiLengthRef(buf, body, 0);
  const signaturesAt = abiLengthRef(buf, body, 32);
  const claimsAt = abiLengthRef(buf, body, 64);
  const membersAt = abiLengthRef(buf, body, 96);
  const placeholders = listOf(buf, placeholdersAt, (i) => bytesToHex(abiStaticBytes(buf, placeholdersAt, i)));
  return chain(placeholders, (placeholders) =>
    chain(listOf(buf, claimsAt, (i) => decodeClaim(buf, abiTupleElement(buf, claimsAt, i))), (decoded) => {
      const firstBad = decoded.find((c) => c === null || !c.ok);
      if (firstBad === null) return ok(NO_HANKO);
      if (firstBad !== undefined && !firstBad.ok) return firstBad;
      const claims = decoded.flatMap((c) => (c !== null && c.ok ? [c.value] : []));
      const members = listOf(buf, membersAt, (i) => abiBytes(buf, abiBytesElement(buf, membersAt, i)));
      return map(members, (memberSignatures): HankoBytes =>
        ({ placeholders, packedSignatures: abiBytes(buf, signaturesAt), claims, memberSignatures }));
    }));
};
export type AccountHankoTag =
  | "expected_entity" | "digest" | "decode" | "too_large" | "member_signatures_shape" | "packed_length"
  | "claim_shape" | "non_canonical" | "claim_required" | "member_signature" | "duplicate_placeholder"
  | "duplicate_claim_entity" | "packed_padding" | "signature_non_canonical" | "recovery_failed"
  | "duplicate_signer" | "signature_required" | "placeholder_signer" | "threshold" | "entity_index"
  | "duplicate_entity_index" | "weight" | "placeholder_claim" | "claim_order" | "first_member"
  | "duplicate_member" | "threshold_power" | "quorum" | "unused_claim" | "unused_placeholder"
  | "unused_signature" | "authority" | "target";
export type AccountHankoError = { readonly _tag: AccountHankoTag };
export type AccountHankoVerdict = { readonly entityId: string; readonly signers: readonly string[] };
const ACCOUNT_HANKO_MAX_BYTES = 64 * 1024, MAX_ENTITIES = 256, MAX_CLAIMS = 64;
const MAX_MEMBERS_PER_CLAIM = 256, MAX_TOTAL_MEMBERS = 1024, MAX_MEMBER_SIGNATURES = 8;
const MAX_POWER = 0xffffn, MAX_DELAY = 0xffff_ffffn;
const MAX_SAFE_INDEX = BigInt(Number.MAX_SAFE_INTEGER), ADDRESS_MAX = (1n << 160n) - 1n;
const hankoRefuse = (_tag: AccountHankoTag): Result<never, AccountHankoError> => err({ _tag });
/** og asHankoBytes32 (codec.ts:83): exactly `0x` + 64 hex, lowercased. */
const bytes32Of = (text: string): string | null => (/^0x[0-9a-f]{64}$/i.test(text) ? text.toLowerCase() : null);
const unique = (xs: readonly string[]): boolean => new Set(xs).size === xs.length;
export const isAddressId = (id: string): boolean => {
  const v = BigInt(id);
  return v > 0n && v <= ADDRESS_MAX;
};
/** The contract's size limits, checked claim by claim so the first oversized claim is the one named. */
const contractShape = (h: HankoBytes): Result<void, AccountHankoError> => {
  const members = h.memberSignatures, signatures = packedCount(h.packedSignatures.length);
  if (members.length !== 0 && members.length !== h.placeholders.length) return hankoRefuse("member_signatures_shape");
  if (members.filter((s) => s.length > 0).length > MAX_MEMBER_SIGNATURES) return hankoRefuse("too_large");
  if (signatures === null) return hankoRefuse("packed_length");
  const entities = h.placeholders.length + signatures + h.claims.length;
  const tooMany = h.claims.length > MAX_CLAIMS || entities > MAX_ENTITIES
    || h.placeholders.length > MAX_ENTITIES || signatures > MAX_ENTITIES;
  if (tooMany) return hankoRefuse("too_large");
  const [, membersSoFar] = mapAccum(h.claims, 0, (sum, c) => {
    const total = sum + c.entityIndexes.length;
    return [total, total] as const;
  });
  const claimIssue = (c: HankoClaim, i: number): readonly (AccountHankoTag | undefined)[] => {
    const n = c.entityIndexes.length;
    const badShape = n === 0 || n !== c.weights.length || n > MAX_MEMBERS_PER_CLAIM;
    return [badShape ? "claim_shape" : undefined, (membersSoFar[i] ?? 0) > MAX_TOTAL_MEMBERS ? "too_large" : undefined];
  };
  const issue = h.claims.flatMap(claimIssue).find((x) => x !== undefined);
  return issue === undefined ? ok(undefined) : hankoRefuse(issue);
};
/** A Hanko is canonical when re-encoding its decoded form gives back the same bytes. */
const canonicalHanko = (h: HankoBytes, bytes: Uint8Array): boolean => {
  const delaysFit = (c: HankoClaim): boolean =>
    c.boardChangeDelay <= MAX_DELAY && c.controlChangeDelay <= MAX_DELAY && c.dividendChangeDelay <= MAX_DELAY;
  return h.claims.every((c) => c.entityId.length === 66 && delaysFit(c)) && envelopeHex(h) === bytesToHex(bytes);
};
const recoverAccountSigners = (
  digest: Uint8Array, packed: Uint8Array,
): Result<readonly string[], AccountHankoError> => {
  const count = packedCount(packed.length) ?? 0;
  if (!packedPaddingClear(packed, count)) return hankoRefuse("packed_padding");
  const parts = Array.from({ length: count }, (_, i) => packedAt(packed, count, i));
  const nonCanonical = parts.some(({ r, s }) => isZeroWord(r) || isZeroWord(s) || !lowS(s));
  if (nonCanonical) return hankoRefuse("signature_non_canonical");
  return foldResult(parts, [] as readonly string[], (signers, { r, s, bit }) => {
    const key = recoverPublicKey(digest, r, s, bit);
    if (key === null) return hankoRefuse("recovery_failed");
    const signer = addressOf(key).toLowerCase();
    return signers.includes(signer) ? hankoRefuse("duplicate_signer") : ok([...signers, signer]);
  });
};
type ResolvedClaim = Readonly<{
  entityId: string; boardHash: string; threshold: bigint; votingPower: bigint; referenced: readonly number[];
  used: readonly number[]; firstMember: string;
}>;
/** One board member: its id, its weight, whether its weight counts toward quorum, and the claim it nests. */
type Member = Readonly<{ id: string; weight: bigint; votes: boolean; nested?: number | undefined }>;
/**
 * Placeholders did not sign, so they carry no vote; signers and earlier claims do. A placeholder
 * may not stand in for an Entity that an earlier claim already proves.
 */
const memberOf = (h: HankoBytes, signerIds: readonly string[], claimIndex: number) =>
  (index: number, m: number): Result<Member, AccountHankoError> => {
    const weight = h.claims[claimIndex]?.weights[m] ?? 0n;
    if (weight <= 0n || weight > MAX_POWER) return hankoRefuse("weight");
    const placeholders = h.placeholders.length, firstClaim = placeholders + signerIds.length;
    const member = ((): Result<Member, AccountHankoError> => {
      if (index < placeholders) {
        const id = h.placeholders[index] ?? "";
        const provenEarlier = h.claims.slice(0, claimIndex).some((e) => e.entityId === id);
        return provenEarlier ? hankoRefuse("placeholder_claim") : ok({ id, weight, votes: false });
      }
      if (index < firstClaim) return ok({ id: signerIds[index - placeholders] ?? "", weight, votes: true });
      const nested = index - firstClaim;
      if (nested >= claimIndex) return hankoRefuse("claim_order");
      return ok({ id: h.claims[nested]?.entityId ?? "", weight, votes: true, nested });
    })();
    return chain(member, (x) =>
      (m === 0 && (index >= firstClaim || !isAddressId(x.id)) ? hankoRefuse("first_member") : ok(x)));
  };
const resolveAccountClaim = (
  h: HankoBytes, signerIds: readonly string[], claimIndex: number,
): Result<ResolvedClaim, AccountHankoError> => {
  const claim = h.claims[claimIndex];
  if (claim === undefined) return hankoRefuse("claim_shape");
  if (claim.threshold <= 0n || claim.threshold > MAX_POWER) return hankoRefuse("threshold");
  const totalCount = BigInt(h.placeholders.length + signerIds.length + h.claims.length);
  if (claim.entityIndexes.some((i) => i > MAX_SAFE_INDEX || i >= totalCount)) return hankoRefuse("entity_index");
  const indexes = claim.entityIndexes.map(Number);
  if (!unique(indexes.map(String))) return hankoRefuse("duplicate_entity_index");
  const members = traverse(indexes, memberOf(h, signerIds, claimIndex));
  return chain(members, (members): Result<ResolvedClaim, AccountHankoError> => {
    const ids = members.map((x) => x.id);
    if (!unique(ids)) return hankoRefuse("duplicate_member");
    if (claim.threshold > claim.weights.reduce((sum, w) => sum + w, 0n)) return hankoRefuse("threshold_power");
    const votingPower = members.reduce((sum, x) => (x.votes ? sum + x.weight : sum), 0n);
    const referenced = members.flatMap((x) => (x.nested === undefined ? [] : [x.nested]));
    const boardHash = boardOf(claim.threshold, ids, members.map((x) => Number(x.weight)), claim);
    return ok({
      entityId: claim.entityId, boardHash, threshold: claim.threshold, votingPower, referenced,
      used: indexes, firstMember: ids[0] ?? "",
    });
  });
};
/** Every claim must be reachable from the target, and every placeholder and signature used by some claim. */
const accountReachability = (
  placeholders: number, signatures: number, claims: readonly ResolvedClaim[],
): Result<void, AccountHankoError> => {
  // A claim only nests earlier claims, so one pass from the last claim down finds everything reachable.
  const reachable = claims.reduceRight(
    (set, c, i) => (set.has(i) ? new Set([...set, ...c.referenced]) : set),
    new Set([claims.length - 1]),
  );
  const used = new Set(claims.flatMap((c) => c.used));
  const unusedFrom = (start: number, count: number): boolean =>
    Array.from({ length: count }, (_, i) => start + i).some((i) => !used.has(i));
  if (reachable.size !== claims.length) return hankoRefuse("unused_claim");
  if (unusedFrom(0, placeholders)) return hankoRefuse("unused_placeholder");
  if (unusedFrom(placeholders, signatures)) return hankoRefuse("unused_signature");
  return ok(undefined);
};
/**
 * og verifyCanonicalHanko. An empty expected entity is og's absent `expectedTargetEntityId`: the
 * last claim is the target and only self-hashed boards authorize.
 */
export const verifyAccountHanko = (
  hanko: string, digest: string, expectedEntityId: string, registeredBoardHash?: string,
): Result<AccountHankoVerdict, AccountHankoError> => {
  const target = expectedEntityId === "" ? undefined : bytes32Of(expectedEntityId);
  const registered = registeredBoardHash?.trim().toLowerCase();
  const isRegistered = (entityId: string, boardHash: string): Result<boolean, never> =>
    ok(entityId === target && boardHash === registered);
  const verdict = checkAccountHanko(hanko, digest, expectedEntityId, isRegistered);
  return map(verdict, ({ entityId, signers }) => ({ entityId, signers }));
};
/** The decoded Hanko if its bytes are well formed, canonical and within the contract's limits. */
const decodedAccountHanko = (hanko: string): Result<HankoBytes, AccountHankoError> => {
  if (!/^0[xX](?:[0-9a-fA-F]{2})*$/.test(hanko)) return hankoRefuse("decode");
  const bytes = hexToBytes(hanko);
  if (bytes.length > ACCOUNT_HANKO_MAX_BYTES) return hankoRefuse("too_large");
  const decoded = decodeHanko(bytes);
  if (!decoded.ok) return hankoRefuse("non_canonical");
  const env = decoded.value;
  const shape = contractShape(env);
  if (!shape.ok) return shape;
  if (!canonicalHanko(env, bytes)) return hankoRefuse("non_canonical");
  if (env.claims.length === 0) return hankoRefuse("claim_required");
  if (env.memberSignatures.some((s) => s.length > 0)) return hankoRefuse("member_signature");
  if (!unique(env.placeholders)) return hankoRefuse("duplicate_placeholder");
  if (!unique(env.claims.map((c) => c.entityId))) return hankoRefuse("duplicate_claim_entity");
  return ok(env);
};
/** Whether a claim's board is the one certified for its Entity; a refusal is a fault that aborts the whole check. */
type BoardAuthorizer<F> = (entityId: string, boardHash: string) => Result<boolean, F>;
/** Claims that resolve, reach quorum, are all reachable and sit on authorized boards, checked in og's order. */
const acceptedClaims = <F>(
  env: HankoBytes, signerIds: readonly string[], authorize: BoardAuthorizer<F>,
): Result<readonly ResolvedClaim[], AccountHankoError | F> => {
  const authorized = (c: ResolvedClaim): Result<boolean, F> =>
    (c.entityId === c.boardHash ? ok(true) : authorize(c.entityId, c.boardHash));
  const resolved = traverse(env.claims, (_, i) => resolveAccountClaim(env, signerIds, i));
  return chain(resolved, (claims) => {
    if (claims.some((c) => c.votingPower < c.threshold)) return hankoRefuse("quorum");
    const reachable = accountReachability(env.placeholders.length, signerIds.length, claims);
    return chain(reachable, () =>
      chain(everyResult(claims, authorized), (all) => (all ? ok(claims) : hankoRefuse("authority"))));
  });
};
/**
 * og verifyCanonicalHanko with og's `validateBoardAuthority` callback for every claim whose board is
 * not self-hashed; also yields the target claim's first member.
 */
export const checkAccountHanko = <F = never>(
  hanko: string, digest: string, expectedEntityId: string, authorize: BoardAuthorizer<F>,
): Result<AccountHankoVerdict & { readonly firstMember: string }, AccountHankoError | F> => {
  const target = expectedEntityId === "" ? undefined : bytes32Of(expectedEntityId);
  if (target === null) return hankoRefuse("expected_entity");
  if (!/^0[xX][0-9a-fA-F]{64}$/.test(digest)) return hankoRefuse("digest");
  return chain(decodedAccountHanko(hanko), (env) =>
    chain(recoverAccountSigners(hexToBytes(digest), env.packedSignatures), (signers) => {
      if (signers.length === 0) return hankoRefuse("signature_required");
      const signerIds = signers.map(addressAsId);
      if (env.placeholders.some((p) => signerIds.includes(p))) return hankoRefuse("placeholder_signer");
      return chain(acceptedClaims(env, signerIds, authorize), (claims) => {
        const last = claims.at(-1);
        if (last === undefined || (target !== undefined && last.entityId !== target)) return hankoRefuse("target");
        return ok({ entityId: last.entityId, signers, firstMember: last.firstMember });
      });
    }));
};
/** A lazy Entity's Hanko: one signature, one claim of one member with weight and threshold 1. */
export const encodeLazyAccountHanko = (entityIdText: string, sig: string): Result<string, AccountHankoError> => {
  const id = bytes32Of(entityIdText), raw = parseHex(sig);
  if (id === null) return hankoRefuse("expected_entity");
  if (raw === null || raw.length !== 65) return hankoRefuse("decode");
  const v = raw[64] ?? 0, r = raw.subarray(0, 32), s = raw.subarray(32, 64);
  const bit = v === 0 || v === 27 ? 0 : v === 1 || v === 28 ? 1 : null;
  if (bit === null || isZeroWord(r) || isZeroWord(s) || !lowS(s)) return hankoRefuse("signature_non_canonical");
  const claim: HankoClaim = {
    entityId: id, entityIndexes: [0n], weights: [1n], threshold: 1n,
    boardChangeDelay: 0n, controlChangeDelay: 0n, dividendChangeDelay: 0n,
  };
  const packedSignatures = packSignatures([{ r, s, v: 27 + bit }]);
  return ok(envelopeHex({ placeholders: [], packedSignatures, memberSignatures: [], claims: [claim] }));
};


// ---- fill ratios: how much of a hash-ladder payment a revealed ratio releases ----

export const MAX_FILL = 65535;
export type RatioError = Tagged<"bad_ratio" | "e12" | "ratio_mismatch" | "leg_mismatch" | "hub_authorship">;
export type RatioRecord = { readonly fillRatio: number; readonly revealedAt: bigint };
export const validRatio = (r: number): boolean => Number.isInteger(r) && r >= 0 && r <= MAX_FILL;
export const floorRatio = (amount: bigint, r: number): Result<bigint, RatioError> =>
  (validRatio(r) ? ok((amount * BigInt(r)) / BigInt(MAX_FILL)) : err({ _tag: "bad_ratio" }));
export const inRevealWindow = (revealedAt: bigint, s: bigint, w: bigint): boolean =>
  revealedAt >= s && revealedAt <= s + w;
export const timelyRatio = (record: RatioRecord | undefined, s: bigint, w: bigint): number =>
  (record !== undefined && inRevealWindow(record.revealedAt, s, w) ? record.fillRatio : 0);
export const effectiveRatio = (claimed: number, timely: number): Result<number, RatioError> =>
  (validRatio(claimed) && validRatio(timely) ? ok(Math.max(claimed, timely)) : err({ _tag: "bad_ratio" }));
export const deltaMove = (amount: bigint, effective: number, already: bigint): Result<bigint, RatioError> =>
  map(floorRatio(amount, effective), (filled) => filled - already);
/**
 * A source-role reveal is fixed once made (a different ratio is e12); a target-role reveal may only
 * raise the ratio.
 */
export const revealSlot = (
  prev: RatioRecord | undefined,
  next: { readonly fillRatio: number; readonly revealedAt: bigint; readonly targetRole: boolean },
): Result<RatioRecord, RatioError> => {
  if (!validRatio(next.fillRatio)) return err({ _tag: "bad_ratio" });
  const fresh: RatioRecord = { fillRatio: next.fillRatio, revealedAt: next.revealedAt };
  if (prev === undefined) return ok(fresh);
  if (!next.targetRole) return prev.fillRatio === next.fillRatio ? ok(prev) : err({ _tag: "e12" });
  return next.fillRatio < prev.fillRatio ? err({ _tag: "e12" }) : ok(fresh);
};
export const uncollateralizedCredit = (hubDebtToUser: bigint, collateral: bigint): bigint =>
  (hubDebtToUser > collateral ? hubDebtToUser - collateral : 0n);


// ---- cross-jurisdiction kernel: og protocol/htlc/hash-ladder.ts, extensions/cross-j/{index,market,status,prepared-route}.ts ----
// A cross-J route swaps an amount on a source stack for an amount on a target stack through one hub. Both legs
// are pulls locked by the same hash ladder, so revealing a fill ratio on one stack lets it be claimed on the other.
export type CrossError = Tagged<"cross_j", { reason: string }>;
const crossErr = (reason: string): Result<never, CrossError> => err({ _tag: "cross_j", reason });

/** og protocol/serialization safeStringify: sorted keys, undefined dropped, bigint tagged, array holes kept as null. */
const jsonNode = (v: unknown): unknown => {
  if (v === undefined || typeof v === "function" || typeof v === "symbol") return undefined;
  if (v === null || typeof v === "string" || typeof v === "number" || typeof v === "boolean") return v;
  if (typeof v === "bigint") return { __xlnType: "BigInt", value: v.toString() };
  if (Array.isArray(v)) return v.map((x) => jsonNode(x) ?? null);
  return Object.fromEntries(jsonEntries(v as Record<string, unknown>));
};
const jsonEntries = (r: Record<string, unknown>): readonly (readonly [string, unknown])[] =>
  Object.keys(r).toSorted(asc).flatMap((k) => {
    const x = jsonNode(r[k]);
    return x === undefined ? [] : [[k, x] as const];
  });
export const stableJson = (v: unknown): string => JSON.stringify(jsonNode(v) ?? null);

// ---- hash ladder ----
// Ratio r (of 65535) is claimed by revealing, for each hex digit of r, a preimage that many hash steps below the
// committed 15-step root. Hashing forward is free and backward is impossible, so a reveal proves at most its ratio.
// The full ratio is claimed by the full secret alone.
const LADDER_NIBBLE_MAX = 15;
export type Reveals = readonly [string, string, string, string];
type Nibble = 0 | 1 | 2 | 3;
const fourOf = <X>(f: (i: Nibble) => X): readonly [X, X, X, X] => [f(0), f(1), f(2), f(3)];
const ladderHash = (node: string): string => keccak256Hex(hexToBytes(node));
const ladderSteps = (node: string, steps: number): string =>
  (steps > 0 ? ladderSteps(ladderHash(node), steps - 1) : node);
const clampRatio = (v: unknown): number => Math.max(0, Math.min(MAX_FILL, Math.floor(Number(v) || 0)));
const ladderDigits = (ratio: number): readonly [number, number, number, number] => {
  const r = clampRatio(ratio);
  return fourOf((i) => (r >> (12 - 4 * i)) & 0x0f);
};
const ladderRoot = (roots: readonly string[]): string => keccak256Hex(concat(roots.map(hexToBytes)));
export type HashLadderCommitment = Readonly<{ fullHash: string; partialRoot: string }>;
export type HashLadderProof = HashLadderCommitment & Readonly<{ fullSecret: string; nibbleBases: Reveals }>;
export type HashLadderReveal = Readonly<{ fillRatio: number; binary: string; fullSecret?: string; reveals?: Reveals }>;
export type DecodedLadder = Readonly<{ fillRatio: number; fullSecret?: string; reveals?: Reveals }>;
/** og buildHashLadderProof: secrets are keccak(`${seed}:full|n0..n3`). */
export const buildHashLadderProof = (seed: string): HashLadderProof => {
  const secretFor = (suffix: string): string => keccak256Hex(utf8(`${seed}:${suffix}`));
  const fullSecret = secretFor("full");
  const nibbleBases = fourOf((i) => secretFor(`n${i}`));
  const partialRoot = ladderRoot(nibbleBases.map((b) => ladderSteps(b, LADDER_NIBBLE_MAX)));
  return { fullSecret, nibbleBases, fullHash: ladderHash(fullSecret), partialRoot };
};
/** og revealHashLadder: 0 → `0x`, 65535 → the full secret, else uint16 ratio ‖ four nibble reveals. */
export const revealHashLadder = (p: HashLadderProof, fillRatio: number): HashLadderReveal => {
  const ratio = clampRatio(fillRatio);
  switch (ratio) {
    case 0: return { fillRatio: 0, binary: "0x" };
    case MAX_FILL: return { fillRatio: ratio, binary: p.fullSecret, fullSecret: p.fullSecret };
    default: return partialReveal(p, ratio);
  }
};
const partialReveal = (p: HashLadderProof, ratio: number): HashLadderReveal => {
  const digits = ladderDigits(ratio);
  const reveals = fourOf((i) => ladderSteps(p.nibbleBases[i], LADDER_NIBBLE_MAX - digits[i]));
  const binary = `0x${ratio.toString(16).padStart(4, "0")}${reveals.map((r) => r.slice(2)).join("")}`;
  return { fillRatio: ratio, binary, reveals };
};
/** og decodeHashLadderBinary. */
export const decodeHashLadderBinary = (binary?: string): Result<DecodedLadder, CrossError> => {
  const value = String(binary || "0x").toLowerCase();
  if (value === "0x") return ok({ fillRatio: 0 });
  if (!value.startsWith("0x") || value.length % 2 !== 0) return crossErr("HASHLADDER_BINARY_INVALID_HEX");
  const size = (value.length - 2) / 2;
  if (size === 32) {
    if (!WORD.test(value)) return crossErr("HASHLADDER_FULL_BINARY_INVALID");
    return ok({ fillRatio: MAX_FILL, fullSecret: value });
  }
  if (size !== 130) return crossErr(`HASHLADDER_BINARY_INVALID_LENGTH:${size}`);
  const fillRatio = Number.parseInt(value.slice(2, 6), 16);
  const partial = Number.isInteger(fillRatio) && fillRatio > 0 && fillRatio < MAX_FILL;
  if (!partial) return crossErr(`HASHLADDER_PARTIAL_BINARY_RATIO_INVALID:${fillRatio}`);
  const reveals = fourOf((i) => `0x${value.slice(6 + i * 64, 70 + i * 64)}`);
  if (!reveals.every((r) => WORD.test(r))) return crossErr("HASHLADDER_PARTIAL_BINARY_REVEALS_INVALID");
  return ok({ fillRatio, reveals });
};
const opensCommitment = (c: HashLadderCommitment, d: DecodedLadder): boolean => {
  const ratio = clampRatio(d.fillRatio);
  switch (ratio) {
    case 0: return true;
    case MAX_FILL: return fullSecretOpens(c, d.fullSecret);
    default: return revealsOpen(c, ratio, d.reveals);
  }
};
const fullSecretOpens = (c: HashLadderCommitment, secret: string | undefined): boolean =>
  secret !== undefined && WORD.test(secret) && ladderHash(secret).toLowerCase() === c.fullHash.toLowerCase();
const revealsOpen = (c: HashLadderCommitment, ratio: number, reveals: Reveals | undefined): boolean => {
  if (reveals === undefined || reveals.length !== 4) return false;
  const digits = ladderDigits(ratio);
  const roots = reveals.map((r, i) => ladderSteps(r, digits[i] ?? 0));
  return ladderRoot(roots).toLowerCase() === c.partialRoot.toLowerCase();
};
/** og verifyHashLadderBinary: decode, then the reveal must open the commitment at exactly its ratio. */
export const verifyHashLadderBinary = (c: HashLadderCommitment, binary?: string): Result<DecodedLadder, CrossError> =>
  chain(decodeHashLadderBinary(binary), (d) =>
    (opensCommitment(c, d) ? ok(d) : crossErr("HASHLADDER_BINARY_VERIFY_FAILED")));

// ---- the route: og types/cross-jurisdiction.ts ----
export const CROSS_STATUSES = [
  "intent", "target_prepared", "resting", "partially_filled", "clear_requested", "clearing",
  "settled", "cancelled", "expired",
] as const;
export type CrossStatus = (typeof CROSS_STATUSES)[number];
export type CrossLegRole = "source" | "target";
const CROSS_LEG_ROLES: readonly CrossLegRole[] = ["source", "target"];
export type CrossLeg = Readonly<{
  jurisdiction: string; entityId: string; counterpartyEntityId: string; tokenId: number; amount: bigint;
}>;
export type CrossPullLeg = Readonly<{
  pullId: string; tokenId: number; amount: bigint; signedAmount: bigint; fullHash: string; partialRoot: string;
}>;
type CloseMode = "full" | "partial_cancel_remainder" | "pure_cancel";
export type CrossCloseProof = Readonly<{
  orderId: string; routeHash: string; sourcePullId: string; targetPullId: string; fillRatio: number;
  cumulativeSourceAmount: bigint; cumulativeTargetAmount: bigint; binaryHash: string; closeMode: CloseMode;
}>;
export type CrossRouteDomain = Readonly<{
  protocol: "xln-cross-j"; hashSchema: "route-domain"; sourceStackId: string; targetStackId: string;
  sourceEntityProviderAddress?: string; targetEntityProviderAddress?: string;
  sourceDeltaTransformerAddress?: string; targetDeltaTransformerAddress?: string;
  sourceAssetRef: string; targetAssetRef: string;
}>;
export type CrossTimePolicy = Readonly<{
  runtimeClock: "unix_ms"; settlementClock: "unix_seconds"; deadlineConversion: "floor_ms_to_unix_seconds";
  runtimeExpiresAtMs: number; finalityPolicy: "independent_beneficiary_windows_pull_sum_finality";
}>;
export type CrossPullBinding = Readonly<{
  orderId: string; routeHash: string; leg: CrossLegRole; status?: CrossStatus;
}>;
type CrossRecord = Readonly<{ fillRatio: number; revealedAt: number }>;
type CrossPendingReveal = Readonly<{ fillRatio: number; fullSecret: string; reveals: Reveals }>;
type ClearingPolicy = "manual" | "full_fill" | "cancel_and_clear";
type RiskMode = "fully_collateralized" | "partially_collateralized" | "credit_line" | "unsecured_internalized";
/** og CrossJurisdictionSwapRoute, field for field. */
export type CrossRoute = Readonly<{
  orderId: string; routeHash?: string; bookOwnerEntityId?: string; venueId?: string;
  sourceSignerId?: string; sourceHubSignerId?: string; targetHubSignerId?: string; targetSignerId?: string;
  bookHubSignerId?: string;
  makerEntityId: string; hubEntityId: string; source: CrossLeg; target: CrossLeg;
  sourceDisputeConfig: DisputeConfig; targetDisputeConfig: DisputeConfig;
  sourcePull?: CrossPullLeg; targetPull?: CrossPullLeg;
  sourceCloseProof?: CrossCloseProof; targetCloseProof?: CrossCloseProof;
  priceTicks?: bigint; fillSeq?: number; cumulativeFillRatio?: number;
  fillNumerator?: bigint; fillDenominator?: bigint; filledSourceAmount?: bigint; filledTargetAmount?: bigint;
  pendingClearRequestedAt?: number; domain?: CrossRouteDomain; timePolicy?: CrossTimePolicy;
  clearingPolicy?: ClearingPolicy; riskMode?: RiskMode;
  claimedRatio?: number; sourceRegistryFillRatio?: number; targetRegistryFillRatio?: number;
  sourceRegistryRecord?: CrossRecord; targetRegistryRecord?: CrossRecord;
  pendingSourceRegistryReveal?: CrossPendingReveal; pendingTargetRegistryReveal?: CrossPendingReveal;
  sourceClaimed?: bigint; targetClaimed?: bigint; status: CrossStatus; createdAt: number; updatedAt: number;
  expiresAt?: number; settledAt?: number; error?: string; memo?: string;
}>;
type MutableRoute = { -readonly [K in keyof CrossRoute]: CrossRoute[K] };

// ---- route status: og extensions/cross-j/status.ts ----
const CROSS_RANK: Readonly<Record<CrossStatus, number>> = {
  intent: 10, target_prepared: 20, resting: 40, partially_filled: 50, clear_requested: 60, clearing: 70,
  settled: 120, cancelled: 120, expired: 120,
};
const CROSS_NEXT: Readonly<Record<CrossStatus, readonly CrossStatus[]>> = {
  intent: ["intent", "target_prepared", "resting", "cancelled", "expired"],
  target_prepared: ["target_prepared", "resting", "clearing", "cancelled", "expired"],
  resting: ["resting", "partially_filled", "clear_requested", "clearing", "cancelled", "expired"],
  partially_filled: ["partially_filled", "clear_requested", "clearing", "cancelled", "expired"],
  clear_requested: ["clear_requested", "clearing", "cancelled", "expired"],
  clearing: ["clearing", "settled", "cancelled", "expired"],
  settled: ["settled"],
  cancelled: ["cancelled"],
  expired: ["expired"],
};
export const isCrossStatus = (v: unknown): v is CrossStatus => (CROSS_STATUSES as readonly unknown[]).includes(v);
export const isCrossTerminal = (s: CrossStatus | undefined): boolean =>
  s === "settled" || s === "cancelled" || s === "expired";
export const compareCrossStatus = (current: CrossStatus | undefined, next: CrossStatus | undefined): number =>
  (CROSS_RANK[next || "intent"] ?? 0) - (CROSS_RANK[current || "intent"] ?? 0);
export const crossTransitionAllowed = (current: CrossStatus | undefined, next: CrossStatus | undefined): boolean =>
  !current || !next || (CROSS_NEXT[current]?.includes(next) ?? false);
export const transitionCrossStatus = (
  r: CrossRoute, next: CrossStatus, updatedAt: number,
): Result<CrossRoute, CrossError> => {
  if (!crossTransitionAllowed(r.status, next)) return crossErr("CROSS_J_ROUTE_TRANSITION_INVALID");
  return ok({ ...r, status: next, updatedAt });
};
export const isCrossExpired = (r: CrossRoute, now: number): boolean => {
  const at = Number(r.expiresAt || 0);
  return Number.isFinite(at) && at > 0 && at <= now;
};

// ---- market: og extensions/cross-j/market.ts ----
// A stack is one (chain, Depository) pair, labelled `stack:<chainId>:<depository>`. The market pairs two assets;
// the reference stable is always the quote, and the hub on the lower stack owns the book.
const lowerText = (v: unknown): string => String(v || "").toLowerCase();
const trimLower = (v: unknown): string => String(v ?? "").trim().toLowerCase();
type Stack = Readonly<{ chainId: number; depositoryAddress: string }>;
const STACK_LABEL = /^stack:(\d+):(0x[0-9a-fA-F]{40})$/;
const stackOf = (j: unknown): Stack | undefined => {
  if (typeof j !== "string") return undefined;
  const m = STACK_LABEL.exec(j.trim());
  const chainId = Number(m?.[1]);
  if (m === null || !Number.isSafeInteger(chainId) || chainId <= 0) return undefined;
  return { chainId, depositoryAddress: (m[2] ?? "").toLowerCase() };
};
const stackLabel = (s: Stack): string => `stack:${s.chainId}:${s.depositoryAddress}`;
/** og getJurisdictionStackId: the stack label of an Account domain. */
export const stackIdOf = (d: { readonly chainId?: unknown; readonly depositoryAddress?: unknown }): string => {
  const dep = typeof d.depositoryAddress === "string" ? d.depositoryAddress.trim().toLowerCase() : "";
  const chainId = Number(d.chainId);
  switch (true) {
    case dep === "": return "";
    case Number.isSafeInteger(chainId) && chainId > 0: return `stack:${chainId}:${dep}`;
    default: return `stack:${dep}`;
  }
};
const assetKey = (j: string, tokenId: number): Result<string, CrossError> => {
  const s = stackOf(j);
  if (s === undefined) return crossErr("CROSS_J_MARKET_JURISDICTION_INVALID");
  return ok(`${stackLabel(s)}:${Math.floor(Number(tokenId) || 0)}`);
};
export type CrossMarket = Readonly<{
  sourceKey: string; targetKey: string; baseKey: string; quoteKey: string; sourceIsBase: boolean; venueId: string;
}>;
export const crossMarketForLegs = (
  sourceJ: string, sourceToken: number, targetJ: string, targetToken: number,
): Result<CrossMarket, CrossError> =>
  chain(assetKey(sourceJ, sourceToken), (sourceKey) =>
    map(assetKey(targetJ, targetToken), (targetKey) => marketOf(sourceKey, targetKey, sourceToken, targetToken)));
/** With exactly one reference stable, it is the quote; otherwise the lower asset key is the base. */
const marketOf = (sourceKey: string, targetKey: string, sourceToken: number, targetToken: number): CrossMarket => {
  const sourceStable = REFERENCE_STABLES.has(sourceToken);
  const targetStable = REFERENCE_STABLES.has(targetToken);
  const sourceIsBase = sourceStable !== targetStable ? !sourceStable : sourceKey <= targetKey;
  const [baseKey, quoteKey] = sourceIsBase ? [sourceKey, targetKey] : [targetKey, sourceKey];
  return { sourceKey, targetKey, baseKey, quoteKey, sourceIsBase, venueId: `cross:${baseKey}/${quoteKey}` };
};
export const crossMarket = (r: Pick<CrossRoute, "source" | "target">): Result<CrossMarket, CrossError> =>
  crossMarketForLegs(r.source.jurisdiction, r.source.tokenId, r.target.jurisdiction, r.target.tokenId);
export const crossBookOwnerForLegs = (
  sourceJ: string, sourceHub: string, targetJ: string, targetHub: string,
): Result<string, CrossError> => {
  const s = stackOf(sourceJ);
  const t = stackOf(targetJ);
  if (s === undefined || t === undefined) return crossErr("CROSS_J_BOOK_JURISDICTION_INVALID");
  if (s.chainId === t.chainId && s.depositoryAddress === t.depositoryAddress) {
    return crossErr("CROSS_J_REQUIRES_DISTINCT_STACKS");
  }
  const sourceIsLower = s.chainId < t.chainId || (s.chainId === t.chainId && s.depositoryAddress < t.depositoryAddress);
  return ok(lowerText(sourceIsLower ? sourceHub : targetHub));
};
export const crossBookOwner = (r: Pick<CrossRoute, "source" | "target">): Result<string, CrossError> =>
  crossBookOwnerForLegs(r.source.jurisdiction, r.source.counterpartyEntityId, r.target.jurisdiction, r.target.entityId);

// ---- policy defaults: the dispute windows, domain and clocks a route hash commits to ----
const uint32Seconds = (v: unknown): number | undefined => {
  const n = Number(v);
  return Number.isSafeInteger(n) && n >= 0 && n <= MAX_UINT32 ? n : undefined;
};
/** og canonicalAccountDisputeConfig: Number-coerced uint32 windows, total at most one year. */
const canonDisputeConfig = (c: unknown): Result<DisputeConfig, CrossError> => {
  if (!c || typeof c !== "object") return crossErr("ACCOUNT_DISPUTE_CONFIG_INVALID");
  const leftResponseSeconds = uint32Seconds((c as DisputeConfig).leftResponseSeconds);
  const rightResponseSeconds = uint32Seconds((c as DisputeConfig).rightResponseSeconds);
  if (leftResponseSeconds === undefined || rightResponseSeconds === undefined) {
    return crossErr("ACCOUNT_DISPUTE_RESPONSE_SECONDS_INVALID");
  }
  if (leftResponseSeconds + rightResponseSeconds > MAX_DISPUTE_SECONDS) {
    return crossErr("ACCOUNT_DISPUTE_RESPONSE_TOTAL_EXCEEDED");
  }
  return ok({ leftResponseSeconds, rightResponseSeconds });
};
const optAddress = (v: unknown): string | undefined => {
  const t = trimLower(v);
  return /^0x[0-9a-f]{40}$/.test(t) ? t : undefined;
};
const routeDomain = (r: CrossRoute): CrossRouteDomain => {
  const d = r.domain;
  const assetRef = (leg: CrossLeg, given: string | undefined): string =>
    String(given || `${trimLower(leg.jurisdiction)}:${Math.floor(Number(leg.tokenId))}`).trim().toLowerCase();
  return {
    protocol: "xln-cross-j", hashSchema: "route-domain",
    sourceStackId: trimLower(d?.sourceStackId || r.source.jurisdiction),
    targetStackId: trimLower(d?.targetStackId || r.target.jurisdiction),
    ...opt("sourceEntityProviderAddress", optAddress(d?.sourceEntityProviderAddress)),
    ...opt("targetEntityProviderAddress", optAddress(d?.targetEntityProviderAddress)),
    ...opt("sourceDeltaTransformerAddress", optAddress(d?.sourceDeltaTransformerAddress)),
    ...opt("targetDeltaTransformerAddress", optAddress(d?.targetDeltaTransformerAddress)),
    sourceAssetRef: assetRef(r.source, d?.sourceAssetRef),
    targetAssetRef: assetRef(r.target, d?.targetAssetRef),
  };
};
const timePolicyAt = (runtimeExpiresAtMs: number): CrossTimePolicy => ({
  runtimeClock: "unix_ms", settlementClock: "unix_seconds", deadlineConversion: "floor_ms_to_unix_seconds",
  runtimeExpiresAtMs, finalityPolicy: "independent_beneficiary_windows_pull_sum_finality",
});
const routeTimePolicy = (r: CrossRoute): Result<CrossTimePolicy, CrossError> => {
  const at = Math.floor(Number(r.timePolicy?.runtimeExpiresAtMs ?? r.expiresAt ?? 0));
  return !Number.isFinite(at) || at < 0 ? crossErr("CROSS_J_TIME_POLICY_EXPIRES_INVALID") : ok(timePolicyAt(at));
};
type DefaultedRoute = CrossRoute & Readonly<{
  domain: CrossRouteDomain; timePolicy: CrossTimePolicy; riskMode: RiskMode;
}>;
/** og withCrossJurisdictionPolicyDefaults. */
const policyDefaults = (r: CrossRoute): Result<DefaultedRoute, CrossError> => {
  const policies = all({
    sourceDisputeConfig: canonDisputeConfig(r.sourceDisputeConfig),
    targetDisputeConfig: canonDisputeConfig(r.targetDisputeConfig),
    timePolicy: routeTimePolicy(r),
  });
  return map(policies, ({ sourceDisputeConfig, targetDisputeConfig, timePolicy }) => ({
    ...r, sourceDisputeConfig, targetDisputeConfig,
    riskMode: r.riskMode || "fully_collateralized", domain: routeDomain(r), timePolicy,
  }));
};

// ---- route hash: og deriveCrossJurisdictionRouteHash ----
// keccak of a 43-slot ABI tuple over the policy-defaulted route. A slot og's encoder refuses (a lone surrogate,
// an out-of-range or non-integer number) is undefined here, and any such slot refuses the whole hash.
const bigintOf = (v: unknown): bigint | undefined => {
  try {
    return BigInt(v as bigint);
  } catch {
    return undefined;
  }
};
const textSlot = (v: unknown): Abi | undefined => {
  const s = String(v);
  return LONE_SURROGATE.test(s) ? undefined : A.bytes(bytesToHex(utf8(s)));
};
const idSlot = (v: unknown): Abi | undefined => textSlot(lowerText(v));
const uintSlot = (bits: number) => (v: unknown): Abi | undefined => {
  const n = bigintOf(v);
  return n === undefined || n < 0n || n >= 1n << BigInt(bits) ? undefined : A.uint(n);
};
const intSlot = (v: unknown): Abi | undefined => {
  const n = bigintOf(v);
  return n === undefined || n < -(1n << 255n) || n >= 1n << 255n ? undefined : A.int(n);
};
const uint256Slot = uintSlot(256);
const uint32Slot = uintSlot(32);
const routeHashSlots = (r: DefaultedRoute): readonly (Abi | undefined)[] => {
  const { domain: d, timePolicy: tp } = r;
  const venue = [
    textSlot(r.orderId || ""), idSlot(r.bookOwnerEntityId || r.source.counterpartyEntityId || r.hubEntityId),
    textSlot(r.venueId || ""), idSlot(r.makerEntityId), idSlot(r.hubEntityId),
  ];
  const signers = [r.sourceSignerId, r.sourceHubSignerId, r.targetHubSignerId, r.targetSignerId, r.bookHubSignerId];
  const leg = (l: CrossLeg): readonly (Abi | undefined)[] => [
    textSlot(trimLower(l.jurisdiction || "")), idSlot(l.entityId), idSlot(l.counterpartyEntityId),
    uint256Slot(String(Math.floor(Number(l.tokenId)))), uint256Slot(l.amount),
  ];
  const pricing = [
    A.bool(r.priceTicks !== undefined), intSlot(r.priceTicks ?? 0n),
    uint256Slot(Math.floor(Number(r.expiresAt ?? 0))), textSlot(String(r.riskMode || "")),
  ];
  const domain = [
    d.protocol, d.hashSchema, d.sourceStackId, d.targetStackId,
    d.sourceEntityProviderAddress || "", d.targetEntityProviderAddress || "",
    d.sourceDeltaTransformerAddress || "", d.targetDeltaTransformerAddress || "",
    d.sourceAssetRef, d.targetAssetRef,
  ];
  const clocks = [
    textSlot(tp.runtimeClock), textSlot(tp.settlementClock), textSlot(tp.deadlineConversion),
    uint256Slot(tp.runtimeExpiresAtMs), textSlot(tp.finalityPolicy),
  ];
  const windows = [
    r.sourceDisputeConfig.leftResponseSeconds, r.sourceDisputeConfig.rightResponseSeconds,
    r.targetDisputeConfig.leftResponseSeconds, r.targetDisputeConfig.rightResponseSeconds,
  ];
  return [
    ...venue, ...signers.map((s) => idSlot(s || "")), ...leg(r.source), ...leg(r.target),
    ...pricing, ...domain.map(textSlot), ...clocks, ...windows.map(uint32Slot),
  ];
};
export const crossRouteHash = (route: CrossRoute): Result<string, CrossError> =>
  chain(policyDefaults(route), (r) => {
    const slots = routeHashSlots(r);
    const encodable = slots.every((s): s is Abi => s !== undefined);
    return encodable ? ok(keccak256Hex(abiEncode(slots))) : crossErr("CROSS_J_ROUTE_HASH_ENCODING");
  });
/**
 * og withCanonicalCrossJurisdictionRouteHash: venue defaults (book owner, venue, hub), then policy defaults,
 * then fully collateralized only, then the hash; a supplied hash must agree.
 */
export const canonicalCrossRoute = (route: CrossRoute): Result<CrossRoute, CrossError> => {
  const owner = route.bookOwnerEntityId ? ok(route.bookOwnerEntityId) : crossBookOwner(route);
  return chain(owner, (owner) => {
    const venue = route.venueId ? ok(route.venueId) : map(crossMarket(route), (m) => m.venueId);
    return chain(venue, (venueId) => {
      const bookOwnerEntityId = lowerText(owner);
      const hubEntityId = route.hubEntityId || bookOwnerEntityId;
      return chain(policyDefaults({ ...route, bookOwnerEntityId, venueId, hubEntityId }), sealRouteHash);
    });
  });
};
const sealRouteHash = (r: CrossRoute): Result<CrossRoute, CrossError> => {
  const fullyCollateralized = (r.riskMode || "fully_collateralized") === "fully_collateralized";
  if (!fullyCollateralized) return crossErr("CROSS_J_RISK_MODE_UNSUPPORTED");
  return chain(crossRouteHash(r), (routeHash) => {
    const disagrees = r.routeHash && String(r.routeHash).toLowerCase() !== routeHash.toLowerCase();
    return disagrees ? crossErr("CROSS_J_ROUTE_HASH_MISMATCH") : ok({ ...r, routeHash });
  });
};
/** og canonicalAccountDisputeConfig's thrown text for one route clock, or undefined when it is valid. */
const disputeConfigIssue = (c: unknown): string | undefined => {
  if (!c || typeof c !== "object") return `ACCOUNT_DISPUTE_CONFIG_INVALID:${String(c)}`;
  const { leftResponseSeconds: l, rightResponseSeconds: r } = c as DisputeConfig;
  switch (true) {
    case uint32Seconds(l) === undefined: return `ACCOUNT_DISPUTE_LEFT_RESPONSE_SECONDS_INVALID:${String(l)}`;
    case uint32Seconds(r) === undefined: return `ACCOUNT_DISPUTE_RIGHT_RESPONSE_SECONDS_INVALID:${String(r)}`;
    case Number(l) + Number(r) > MAX_DISPUTE_SECONDS:
      return `ACCOUNT_DISPUTE_RESPONSE_TOTAL_EXCEEDED:${Number(l) + Number(r)}`;
    default: return undefined;
  }
};
/** The first route clock's thrown text, in the order given. */
const disputeConfigText = (configs: readonly unknown[]): string | undefined =>
  configs.map(disputeConfigIssue).find((x) => x !== undefined);
/** og's thrown message for a canonicalCrossRoute refusal; the rewrite's reasons are bare codes. */
export const crossRouteErrorText = (route: CrossRoute, reason: string): string => {
  switch (reason) {
    case "CROSS_J_BOOK_JURISDICTION_INVALID":
    case "CROSS_J_MARKET_JURISDICTION_INVALID": {
      const jurisdictions = [route.source.jurisdiction, route.target.jurisdiction];
      const bad = jurisdictions.findIndex((j) => stackOf(j) === undefined);
      return bad < 0 ? reason : `${reason}:${trimLower(jurisdictions[bad])}`;
    }
    case "CROSS_J_REQUIRES_DISTINCT_STACKS": {
      const s = stackOf(route.source.jurisdiction);
      return s === undefined ? reason : `${reason}:${stackLabel(s)}`;
    }
    case "ACCOUNT_DISPUTE_CONFIG_INVALID":
    case "ACCOUNT_DISPUTE_RESPONSE_SECONDS_INVALID":
    case "ACCOUNT_DISPUTE_RESPONSE_TOTAL_EXCEEDED": {
      return disputeConfigText([route.sourceDisputeConfig, route.targetDisputeConfig]) ?? reason;
    }
    case "CROSS_J_TIME_POLICY_EXPIRES_INVALID":
    case "CROSS_J_ROUTE_HASH_MISMATCH": return `${reason}:${route.orderId}`;
    case "CROSS_J_RISK_MODE_UNSUPPORTED": return `${reason}:${route.orderId}:${route.riskMode}`;
    default: return reason;
  }
};

// ---- identities derived from the route hash ----
const hashOrDerive = (r: CrossRoute): Result<string, CrossError> => (r.routeHash ? ok(r.routeHash) : crossRouteHash(r));
/** og deriveCrossJurisdictionPullId. */
export const crossPullId = (r: CrossRoute, leg: CrossLegRole): Result<string, CrossError> =>
  map(hashOrDerive(r), (h) => keccak256Hex(utf8(`xln:cross-j:pull-id:v1:${h}:${leg}`)));
/** og deriveCrossJurisdictionPrivateSeed: the ladder seed is private to the runtime seed and bound to the route. */
export const crossPrivateSeed = (runtimeSeed: string | undefined, r: CrossRoute): Result<string, CrossError> => {
  const seed = String(runtimeSeed || "").trim();
  if (seed === "") return crossErr("CRYPTO_DETERMINISM_VIOLATION");
  return map(hashOrDerive(r), (h) => keccak256Hex(utf8(`xln:cross-j:hashladder-private-seed:v1:${seed}:${h}`)));
};
/** og signedCrossJurisdictionAmountForBeneficiary: positive when the beneficiary is the left entity. */
export const crossSignedAmount = (beneficiary: string, counterparty: string, amount: bigint): bigint =>
  (before(lowerText(beneficiary), lowerText(counterparty)) ? amount : -amount);

// ---- clones: og cloneCrossJurisdiction*, every field normalized and blanks dropped ----
const optText = (v: unknown): string | undefined => {
  const t = String(v ?? "").trim();
  return t === "" ? undefined : t;
};
const optNum = (v: unknown): number | undefined => (v === undefined || v === null ? undefined : Number(v));
const optBig = (v: unknown): bigint | undefined => (v === undefined || v === null ? undefined : BigInt(v as bigint));
/** f(x) when x is present (truthy), matching og's `x ? {...} : {}` spreads. */
const given = <X, Y>(x: X | undefined, f: (x: X) => Y): Y | undefined => (x ? f(x) : undefined);
const cloneLeg = (l: CrossLeg): CrossLeg => ({
  jurisdiction: String(l.jurisdiction || ""), entityId: String(l.entityId || ""),
  counterpartyEntityId: String(l.counterpartyEntityId || ""), tokenId: Number(l.tokenId), amount: BigInt(l.amount),
});
const clonePull = (p: CrossPullLeg | undefined): CrossPullLeg | undefined => (p === undefined ? undefined : {
  pullId: String(p.pullId || ""), tokenId: Number(p.tokenId), amount: BigInt(p.amount),
  signedAmount: BigInt(p.signedAmount), fullHash: String(p.fullHash || ""), partialRoot: String(p.partialRoot || ""),
});
const CLOSE_MODES: readonly unknown[] = ["full", "partial_cancel_remainder", "pure_cancel"];
/** og cloneCrossJurisdictionCloseProof: exact uint16 ratio and a known close mode. */
export const cloneCloseProof = (p: CrossCloseProof): Result<CrossCloseProof, CrossError> => {
  const fillRatio = Number(p.fillRatio);
  if (!Number.isSafeInteger(fillRatio) || fillRatio < 0 || fillRatio > MAX_FILL) {
    return crossErr("CROSS_J_CLOSE_PROOF_FILL_RATIO_INVALID");
  }
  if (!CLOSE_MODES.includes(p.closeMode)) return crossErr("CROSS_J_CLOSE_PROOF_MODE_INVALID");
  return ok({
    orderId: String(p.orderId || ""), routeHash: String(p.routeHash || ""),
    sourcePullId: String(p.sourcePullId || ""), targetPullId: String(p.targetPullId || ""), fillRatio,
    cumulativeSourceAmount: BigInt(p.cumulativeSourceAmount ?? 0n),
    cumulativeTargetAmount: BigInt(p.cumulativeTargetAmount ?? 0n),
    binaryHash: String(p.binaryHash || ""), closeMode: p.closeMode,
  });
};
const cloneDisputeConfig = (c: DisputeConfig): DisputeConfig => ({
  leftResponseSeconds: Number(c.leftResponseSeconds), rightResponseSeconds: Number(c.rightResponseSeconds),
});
const cloneDomain = (d: CrossRouteDomain): CrossRouteDomain => ({
  protocol: "xln-cross-j", hashSchema: "route-domain",
  sourceStackId: String(d.sourceStackId || ""), targetStackId: String(d.targetStackId || ""),
  ...opt("sourceEntityProviderAddress", given(d.sourceEntityProviderAddress, String)),
  ...opt("targetEntityProviderAddress", given(d.targetEntityProviderAddress, String)),
  ...opt("sourceDeltaTransformerAddress", given(d.sourceDeltaTransformerAddress, String)),
  ...opt("targetDeltaTransformerAddress", given(d.targetDeltaTransformerAddress, String)),
  sourceAssetRef: String(d.sourceAssetRef || ""), targetAssetRef: String(d.targetAssetRef || ""),
});
const cloneRecord = (x: CrossRecord): CrossRecord => ({ fillRatio: x.fillRatio, revealedAt: x.revealedAt });
const cloneReveal = (x: CrossPendingReveal): CrossPendingReveal => ({
  fillRatio: x.fillRatio, fullSecret: x.fullSecret, reveals: [...x.reveals],
});
const cloneOptionalProof = (p: CrossCloseProof | undefined): Result<CrossCloseProof | undefined, CrossError> =>
  (p ? cloneCloseProof(p) : ok(undefined));
/** og cloneCrossJurisdictionRoute: the public route with every field normalized and blanks dropped. */
export const cloneCrossRoute = (r: CrossRoute): Result<CrossRoute, CrossError> => {
  if (!isCrossStatus(r.status)) return crossErr("CROSS_J_ROUTE_STATUS_INVALID");
  const proofs = all({
    sourceCloseProof: cloneOptionalProof(r.sourceCloseProof),
    targetCloseProof: cloneOptionalProof(r.targetCloseProof),
  });
  return map(proofs, ({ sourceCloseProof, targetCloseProof }): CrossRoute => ({
    orderId: String(r.orderId || ""), makerEntityId: String(r.makerEntityId || ""),
    hubEntityId: String(r.hubEntityId || ""), source: cloneLeg(r.source), target: cloneLeg(r.target),
    sourceDisputeConfig: cloneDisputeConfig(r.sourceDisputeConfig),
    targetDisputeConfig: cloneDisputeConfig(r.targetDisputeConfig),
    status: r.status, createdAt: Number(r.createdAt || 0), updatedAt: Number(r.updatedAt || 0),
    ...opt("routeHash", optText(r.routeHash)),
    ...opt("bookOwnerEntityId", optText(r.bookOwnerEntityId)),
    ...opt("venueId", optText(r.venueId)),
    ...opt("sourceSignerId", optText(r.sourceSignerId)),
    ...opt("sourceHubSignerId", optText(r.sourceHubSignerId)),
    ...opt("targetHubSignerId", optText(r.targetHubSignerId)),
    ...opt("targetSignerId", optText(r.targetSignerId)),
    ...opt("bookHubSignerId", optText(r.bookHubSignerId)),
    ...opt("sourcePull", clonePull(r.sourcePull)),
    ...opt("targetPull", clonePull(r.targetPull)),
    ...opt("sourceCloseProof", sourceCloseProof),
    ...opt("targetCloseProof", targetCloseProof),
    ...opt("priceTicks", optBig(r.priceTicks)),
    ...opt("fillSeq", optNum(r.fillSeq)),
    ...opt("cumulativeFillRatio", optNum(r.cumulativeFillRatio)),
    ...opt("fillNumerator", optBig(r.fillNumerator)),
    ...opt("fillDenominator", optBig(r.fillDenominator)),
    ...opt("filledSourceAmount", optBig(r.filledSourceAmount)),
    ...opt("filledTargetAmount", optBig(r.filledTargetAmount)),
    ...opt("pendingClearRequestedAt", optNum(r.pendingClearRequestedAt)),
    ...opt("domain", given(r.domain, cloneDomain)),
    ...opt("timePolicy", given(r.timePolicy, (tp) => timePolicyAt(Number(tp.runtimeExpiresAtMs || 0)))),
    ...opt("clearingPolicy", r.clearingPolicy || undefined),
    ...opt("riskMode", r.riskMode || undefined),
    ...opt("claimedRatio", optNum(r.claimedRatio)),
    ...opt("sourceRegistryFillRatio", optNum(r.sourceRegistryFillRatio)),
    ...opt("targetRegistryFillRatio", optNum(r.targetRegistryFillRatio)),
    ...opt("sourceRegistryRecord", given(r.sourceRegistryRecord, cloneRecord)),
    ...opt("targetRegistryRecord", given(r.targetRegistryRecord, cloneRecord)),
    ...opt("pendingSourceRegistryReveal", given(r.pendingSourceRegistryReveal, cloneReveal)),
    ...opt("pendingTargetRegistryReveal", given(r.pendingTargetRegistryReveal, cloneReveal)),
    ...opt("sourceClaimed", optBig(r.sourceClaimed)),
    ...opt("targetClaimed", optBig(r.targetClaimed)),
    ...opt("expiresAt", optNum(r.expiresAt)),
    ...opt("settledAt", optNum(r.settledAt)),
    ...opt("error", optText(r.error)),
    ...opt("memo", optText(r.memo)),
  }));
};
/** og cloneCrossJurisdictionPullBinding. */
export const cloneCrossBinding = (b: CrossPullBinding): Result<CrossPullBinding, CrossError> => {
  if (b.status !== undefined && !isCrossStatus(b.status)) return crossErr("CROSS_J_ROUTE_STATUS_INVALID");
  const orderId = String(b.orderId || "");
  const routeHash = String(b.routeHash || "");
  return ok({ orderId, routeHash, leg: b.leg, ...opt("status", b.status) });
};
/** og buildCrossJurisdictionPullBinding: the opening binding of one leg to the canonical route. */
export const crossPullBinding = (route: CrossRoute, leg: CrossLegRole): Result<CrossPullBinding, CrossError> =>
  chain(canonicalCrossRoute(route), (c) =>
    chain(hashOrDerive(c), (routeHash) => cloneCrossBinding({ orderId: c.orderId, routeHash, leg, status: c.status })));
/** og hashCrossJurisdictionCloseBinary: keccak of the ladder reveal bytes. */
export const crossCloseBinaryHash = (binary: string): Result<string, CrossError> => {
  const text = String(binary || "0x");
  const bytes = parseHex(text);
  return bytes === null || !text.startsWith("0x") ? crossErr("CROSS_J_CLOSE_BINARY_INVALID") : ok(keccak256Hex(bytes));
};

// ---- fill progress ----
// Cumulative amounts are exact n/d scalings of the leg totals; the uint16 ratio is only the dispute projection,
// so any coarse ratio or amount a route carries must agree with the exact one.
type ExactRatio = Readonly<{ numerator: bigint; denominator: bigint }>;
type ExactFields = Readonly<{ fillNumerator?: bigint | undefined; fillDenominator?: bigint | undefined }>;
const readExactRatio = (x: ExactFields): Result<ExactRatio | undefined, CrossError> => {
  const { fillNumerator: numerator, fillDenominator: denominator } = x;
  if (numerator === undefined && denominator === undefined) return ok(undefined);
  if (numerator === undefined || denominator === undefined) return crossErr("CROSS_J_EXACT_FILL_RATIO_INCOMPLETE");
  const proper = denominator > 0n && numerator >= 0n && numerator <= denominator;
  if (!proper) return crossErr("CROSS_J_EXACT_FILL_RATIO_INVALID");
  return ok({ numerator, denominator });
};
const scaleExact = (total: bigint, r: ExactRatio): bigint =>
  (r.numerator >= r.denominator ? total : (total * r.numerator) / r.denominator);
type ProofRatioInput = ExactFields & Readonly<{
  cumulativeFillRatio?: number | undefined; claimedRatio?: number | undefined;
}>;
/** og getCrossJurisdictionCommittedProofRatio: the uint16 projection of the exact ratio. */
export const crossProofRatio = (x: ProofRatioInput): Result<number, CrossError> =>
  chain(readExactRatio(x), (exact): Result<number, CrossError> => {
    const coarse = [x.cumulativeFillRatio, x.claimedRatio];
    if (exact === undefined) {
      const claimsFill = Math.max(...coarse.map(clampRatio)) > 0;
      return claimsFill ? crossErr("CROSS_J_EXACT_FILL_RATIO_REQUIRED") : ok(0);
    }
    const derived = fillRatioOf({ n: exact.numerator, d: exact.denominator });
    const disagrees = coarse.some((v) => v !== undefined && clampRatio(v) !== derived);
    return disagrees ? crossErr("CROSS_J_COARSE_EXACT_RATIO_MISMATCH") : ok(derived);
  });
export type CrossFillAmounts = Readonly<{
  sourceTotal: bigint; targetTotal: bigint; filledSourceAmount: bigint; filledTargetAmount: bigint; fillRatio: number;
}>;
/** og getCrossJurisdictionCommittedFillAmounts. */
export const crossFillAmounts = (r: CrossRoute): Result<CrossFillAmounts, CrossError> =>
  chain(readExactRatio(r), (exact) => {
    const sourceTotal = BigInt(r.source.amount);
    const targetTotal = BigInt(r.target.amount);
    const recorded = [r.filledSourceAmount, r.filledTargetAmount, r.sourceClaimed, r.targetClaimed];
    if (exact === undefined && recorded.some((v) => v !== undefined && v !== 0n)) {
      return crossErr("CROSS_J_EXACT_FILL_RATIO_REQUIRED");
    }
    const filledSourceAmount = exact ? scaleExact(sourceTotal, exact) : 0n;
    const filledTargetAmount = exact ? scaleExact(targetTotal, exact) : 0n;
    const expected = [filledSourceAmount, filledTargetAmount, filledSourceAmount, filledTargetAmount];
    return chain(crossProofRatio(r), (fillRatio) => {
      const disagrees = recorded.some((have, i) => have !== undefined && have !== expected[i]);
      if (disagrees) return crossErr("CROSS_J_COMMITTED_AMOUNT_MISMATCH");
      return ok({ sourceTotal, targetTotal, filledSourceAmount, filledTargetAmount, fillRatio });
    });
  });
export const hasCrossCommittedFill = (r: CrossRoute): Result<boolean, CrossError> =>
  map(crossFillAmounts(r), (c) => c.fillRatio > 0 || c.filledSourceAmount > 0n || c.filledTargetAmount > 0n);
export const isCrossFillTerminal = (
  r: CrossRoute, x: Readonly<{ nextRatio: number; cancelRemainder?: boolean | undefined }>,
): Result<boolean, CrossError> =>
  map(crossFillAmounts(r), (c) =>
    x.nextRatio >= MAX_FILL || c.filledSourceAmount >= BigInt(r.source.amount)
    || c.filledTargetAmount >= BigInt(r.target.amount) || Boolean(x.cancelRemainder));
export type CrossFillInput = ProofRatioInput & Readonly<{
  fillSeq?: number | undefined; cumulativeFillRatio: number;
  incrementalSourceAmount?: bigint | undefined; incrementalTargetAmount?: bigint | undefined;
  cumulativeSourceAmount?: bigint | undefined; cumulativeTargetAmount?: bigint | undefined;
}>;
type FillStep = Readonly<{
  cumulativeSourceAmount: bigint; cumulativeTargetAmount: bigint;
  incrementalSourceAmount: bigint; incrementalTargetAmount: bigint;
}>;
export type CrossFillProgress = FillStep & Readonly<{
  fillSeq: number; previousRatio: number; nextRatio: number; fillNumerator: bigint; fillDenominator: bigint;
  previousSourceAmount: bigint; previousTargetAmount: bigint;
}>;
/** The new cumulative amounts at the exact ratio and what they add; both must grow and echoes must agree. */
const fillStep = (
  r: CrossRoute, x: CrossFillInput, exact: ExactRatio, committed: CrossFillAmounts,
): Result<FillStep, CrossError> => {
  const cumulativeSourceAmount = scaleExact(BigInt(r.source.amount), exact);
  const cumulativeTargetAmount = scaleExact(BigInt(r.target.amount), exact);
  const incrementalSourceAmount = cumulativeSourceAmount - committed.filledSourceAmount;
  const incrementalTargetAmount = cumulativeTargetAmount - committed.filledTargetAmount;
  if (incrementalSourceAmount <= 0n || incrementalTargetAmount <= 0n) return crossErr("CROSS_J_FILL_NO_INCREMENT");
  const echoes = [
    [x.cumulativeSourceAmount, cumulativeSourceAmount], [x.cumulativeTargetAmount, cumulativeTargetAmount],
    [x.incrementalSourceAmount, incrementalSourceAmount], [x.incrementalTargetAmount, incrementalTargetAmount],
  ] as const;
  const disagrees = echoes.some(([echo, actual]) => echo !== undefined && echo !== actual);
  if (disagrees) return crossErr("CROSS_J_FILL_AMOUNT_MISMATCH");
  return ok({ cumulativeSourceAmount, cumulativeTargetAmount, incrementalSourceAmount, incrementalTargetAmount });
};
/** og validateCrossJurisdictionFillProgress: the next sequence, an exact ratio strictly above the committed one. */
export const crossFillProgress = (r: CrossRoute, x: CrossFillInput): Result<CrossFillProgress, CrossError> => {
  const prevSeq = Math.max(0, Math.floor(Number(r.fillSeq ?? 0) || 0));
  const nextSeq = x.fillSeq === undefined ? prevSeq + 1 : Math.floor(Number(x.fillSeq));
  if (!Number.isInteger(nextSeq) || nextSeq !== prevSeq + 1) return crossErr("CROSS_J_FILL_SEQ");
  return chain(readExactRatio(x), (exact) => {
    if (exact === undefined) return crossErr("CROSS_J_EXACT_FILL_RATIO_REQUIRED");
    const { cumulativeFillRatio, fillNumerator, fillDenominator } = x;
    const nextOnly = { cumulativeFillRatio, fillNumerator, fillDenominator };
    const ratios = all({ previousRatio: crossProofRatio(r), nextRatio: crossProofRatio(nextOnly) });
    return chain(ratios, ({ previousRatio, nextRatio }) => {
      if (nextRatio <= previousRatio) return crossErr("CROSS_J_FILL_NON_MONOTONIC");
      if (BigInt(r.source.amount) <= 0n || BigInt(r.target.amount) <= 0n) return crossErr("CROSS_J_FILL_ROUTE_AMOUNT");
      return chain(crossFillAmounts(r), (committed) =>
        map(fillStep(r, x, exact, committed), (step): CrossFillProgress => ({
          fillSeq: nextSeq, previousRatio, nextRatio,
          fillNumerator: exact.numerator, fillDenominator: exact.denominator,
          previousSourceAmount: committed.filledSourceAmount, previousTargetAmount: committed.filledTargetAmount,
          ...step,
        })));
    });
  });
};
/** og applyCrossJurisdictionFillProgress: the validated progress written onto the route. */
export const applyCrossFill = (r: CrossRoute, x: CrossFillInput, updatedAt: number): Result<CrossRoute, CrossError> =>
  map(crossFillProgress(r, x), (f) => ({
    ...r, fillSeq: f.fillSeq, cumulativeFillRatio: f.nextRatio,
    fillNumerator: f.fillNumerator, fillDenominator: f.fillDenominator, claimedRatio: f.nextRatio,
    filledSourceAmount: f.cumulativeSourceAmount, filledTargetAmount: f.cumulativeTargetAmount,
    sourceClaimed: f.cumulativeSourceAmount, targetClaimed: f.cumulativeTargetAmount,
    status: f.nextRatio >= MAX_FILL ? "clear_requested" : "partially_filled", updatedAt,
  }));
/** og withCrossJurisdictionCloseProofProgress. */
export const withCloseProofProgress = (r: CrossRoute, p: CrossCloseProof, updatedAt: number): CrossRoute => ({
  ...r, cumulativeFillRatio: p.fillRatio,
  fillNumerator: BigInt(p.fillRatio), fillDenominator: BigInt(MAX_FILL), claimedRatio: p.fillRatio,
  filledSourceAmount: p.cumulativeSourceAmount, filledTargetAmount: p.cumulativeTargetAmount,
  sourceClaimed: p.cumulativeSourceAmount, targetClaimed: p.cumulativeTargetAmount, updatedAt,
});
const closeModeAt = (ratio: number): CloseMode => {
  switch (true) {
    case ratio >= MAX_FILL: return "full";
    case ratio <= 0: return "pure_cancel";
    default: return "partial_cancel_remainder";
  }
};
/** og buildCrossJurisdictionCloseProof: the Hub's exact source+target close cohort at the committed ratio. */
export const buildCrossCloseProof = (route: CrossRoute, binary: string): Result<CrossCloseProof, CrossError> =>
  chain(canonicalCrossRoute(route), (c) => {
    const { sourcePull, targetPull } = c;
    if (!sourcePull || !targetPull) return crossErr("CROSS_J_CLOSE_PROOF_PULLS_MISSING");
    const parts = all({
      fill: crossFillAmounts(c), routeHash: hashOrDerive(c), binaryHash: crossCloseBinaryHash(binary),
    });
    return chain(parts, ({ fill, routeHash, binaryHash }) => cloneCloseProof({
      orderId: c.orderId, routeHash, sourcePullId: sourcePull.pullId, targetPullId: targetPull.pullId,
      fillRatio: fill.fillRatio, cumulativeSourceAmount: fill.filledSourceAmount,
      cumulativeTargetAmount: fill.filledTargetAmount, binaryHash, closeMode: closeModeAt(fill.fillRatio),
    }));
  });

// ---- prepared route: og extensions/cross-j/prepared-route.ts ----
const CROSS_DEFAULT_BOOK_TTL_MS = 60_000;
/** og buildPreparedCrossJurisdictionRoute: book TTL, canonical hash, a useful asset route, both pulls on one ladder. */
export const prepareCrossRoute = (
  route: CrossRoute, o: Readonly<{ runtimeSeed?: string | undefined; now: number }>,
): Result<CrossRoute, CrossError> => {
  const now = Math.floor(Number(o.now || 0));
  if (!Number.isFinite(now) || now <= 0) return crossErr("CROSS_J_NOW_INVALID");
  const expiresAt = Math.floor(Number(route.expiresAt ?? now + CROSS_DEFAULT_BOOK_TTL_MS));
  if (!Number.isFinite(expiresAt) || expiresAt <= now) return crossErr("CROSS_J_EXPIRES_AT_INVALID");
  return chain(canonicalCrossRoute({ ...route, expiresAt }), (c) => {
    const sameAsset = trimLower(c.source.jurisdiction) === trimLower(c.target.jurisdiction)
      && Number(c.source.tokenId) === Number(c.target.tokenId);
    if (sameAsset) return crossErr("CROSS_J_SAME_JURISDICTION_TOKEN_INVALID");
    const ids = all({
      seed: crossPrivateSeed(o.runtimeSeed, c),
      sourcePullId: crossPullId(c, "source"),
      targetPullId: crossPullId(c, "target"),
    });
    return map(ids, ({ seed, sourcePullId, targetPullId }): CrossRoute => {
      const ladder = buildHashLadderProof(seed);
      const pullOf = (leg: CrossLeg, pullId: string): CrossPullLeg => {
        const amount = BigInt(leg.amount);
        const signedAmount = crossSignedAmount(leg.counterpartyEntityId, leg.entityId, amount);
        const { fullHash, partialRoot } = ladder;
        return { pullId, tokenId: Number(leg.tokenId), amount, signedAmount, fullHash, partialRoot };
      };
      return {
        ...c, sourcePull: pullOf(c.source, sourcePullId), targetPull: pullOf(c.target, targetPullId),
        status: "target_prepared", updatedAt: now, expiresAt,
      };
    });
  });
};
/** og buildCrossJurisdictionPullReveal: the ladder reveal at a ratio, from the route's private seed. */
export const crossPullReveal = (fillRatio: number, privateSeed: string): Result<HashLadderReveal, CrossError> => {
  const seed = String(privateSeed || "").trim();
  if (seed === "") return crossErr("CROSS_J_HASHLADDER_PRIVATE_SEED_MISSING");
  return ok(revealHashLadder(buildHashLadderProof(seed), fillRatio));
};
// ---- pull registry settlement: og account/pull-registry-settlement.ts ----
// After a dispute finalizes, a cross-J leg is paid by the pull the DeltaTransformer finds in the signed proof body,
// at the ratio that pull claimed, raised by a registry reveal made inside the beneficiary's own response window.
export type SignedProofBodyPull = Readonly<{
  amount: bigint; claimedRatio: number; targetRole: boolean; fullHash: string; partialRoot: string;
}>;
export type HashLadderRegistryRecord = Readonly<{ fillRatio: number; revealedAt: number }>;
const BATCH_INVALID = crossErr("CROSS_J_FINAL_DELTA_BATCH_INVALID");
const MAX_SAFE_WORD = BigInt(Number.MAX_SAFE_INTEGER);
/** An ABI offset or count: past the end of the data, too large to be a JS index, or a usable index. */
type AbiIndex = Tagged<"overrun"> | Tagged<"unsafe"> | Tagged<"at", { value: number }>;
const wordWithin = (b: Uint8Array, at: number): bigint | undefined =>
  (at + 32 > b.length ? undefined : BigInt(bytesToHex(b.subarray(at, at + 32))));
const indexAt = (b: Uint8Array, at: number): AbiIndex => {
  const w = wordWithin(b, at);
  if (w === undefined) return { _tag: "overrun" };
  return w > MAX_SAFE_WORD ? { _tag: "unsafe" } : { _tag: "at", value: Number(w) };
};
/** One array of the batch tuple. ethers keeps an unsafe count as an unread error value, so that section is `unread`. */
type BatchSection = Tagged<"unread"> | Tagged<"rows", { items: Uint8Array; count: number }>;
const batchSection = (tuple: Uint8Array, slot: number, rowWords: number): Result<BatchSection, CrossError> => {
  const offset = indexAt(tuple, slot * 32);
  if (offset._tag !== "at") return BATCH_INVALID;
  const body = tuple.subarray(offset.value);
  const count = indexAt(body, 0);
  switch (count._tag) {
    case "overrun": return BATCH_INVALID;
    case "unsafe": return ok({ _tag: "unread" });
    case "at": {
      const end = 32 + count.value * rowWords * 32;
      if (count.value * 32 > body.length || end > body.length) return BATCH_INVALID;
      return ok({ _tag: "rows", items: body.subarray(32, end), count: count.value });
    }
  }
};
/** (payment[], swap[], pull[]) with rows of 5, 5 and 7 words. */
const BATCH_SECTIONS = [[0, 5], [1, 5], [2, 7]] as const;
const PULL_ROW_WORDS = 7;
/** A pull row: word 1 is the sign, 2 the magnitude, 3 the uint16 ratio, 4–5 the ladder hashes, 6 the role. */
const pullRow = (items: Uint8Array, i: number): Result<SignedProofBodyPull, CrossError> => {
  const base = i * PULL_ROW_WORDS * 32;
  const word = (k: number): bigint => wordWithin(items, base + k * 32) ?? 0n;
  const hash = (k: number): string => bytesToHex(items.subarray(base + k * 32, base + k * 32 + 32)).toLowerCase();
  const negative = word(1) !== 0n;
  const magnitude = word(2);
  if (negative && magnitude === 0n) return crossErr("ABI_MONEY_NEGATIVE_ZERO");
  return ok({
    amount: negative ? -magnitude : magnitude, claimedRatio: Number(word(3) & 0xffffn),
    fullHash: hash(4), partialRoot: hash(5), targetRole: word(6) !== 0n,
  });
};
/** The DeltaTransformer batch decoded as ethers' AbiCoder does; only the pulls are read. */
const decodeBatchPulls = (hex: string): Result<readonly SignedProofBodyPull[], CrossError> => {
  const data = /^0x([0-9a-fA-F]{2})*$/.test(hex) ? parseHex(hex) : null;
  if (data === null) return BATCH_INVALID;
  const top = indexAt(data, 0);
  if (top._tag !== "at") return BATCH_INVALID;
  const tuple = data.subarray(top.value);
  const sections = traverse(BATCH_SECTIONS, ([slot, rowWords]) => batchSection(tuple, slot, rowWords));
  return chain(sections, ([, , pulls]) => {
    if (pulls?._tag !== "rows") return BATCH_INVALID;
    return traverse(Array.from({ length: pulls.count }, (_, i) => i), (i) => pullRow(pulls.items, i));
  });
};
/** ethers isAddress over a hex address: 0x optional; a mixed-case spelling must be its EIP-55 checksum. */
const isAddressText = (a: string): boolean => {
  if (!/^(0x)?[0-9a-fA-F]{40}$/.test(a)) return false;
  const body = a.startsWith("0x") ? a.slice(2) : a;
  const mixedCase = /[a-f]/.test(body) && /[A-F]/.test(body);
  return !mixedCase || checksum(`0x${body}`) === `0x${body}`;
};
const signedProofBodyPulls = (
  proofbody: Pick<ProofBody, "transformers">, transformerAddress: string,
): Result<readonly SignedProofBodyPull[], CrossError> => {
  if (!isAddressText(transformerAddress)) return crossErr("CROSS_J_FINAL_DELTA_TRANSFORMER_ADDRESS_INVALID");
  const canonical = transformerAddress.toLowerCase();
  const clauses = proofbody.transformers.filter((t) => String(t.transformerAddress).toLowerCase() === canonical);
  if (clauses.length === 0) return crossErr("CROSS_J_FINAL_DELTA_TRANSFORMER_MISSING");
  return map(traverse(clauses, (t) => decodeBatchPulls(t.encodedBatch)), (rows) => rows.flat());
};
/** og findExactSignedProofBodyPull: the one signed pull with this role, ladder and signed amount; two is ambiguous. */
export const findSignedProofBodyPull = (
  proofbody: Pick<ProofBody, "transformers">, expected: CrossPullLeg, targetRole: boolean, transformerAddress: string,
): Result<SignedProofBodyPull | undefined, CrossError> =>
  chain(signedProofBodyPulls(proofbody, transformerAddress), (pulls) => {
    const isExpected = (p: SignedProofBodyPull): boolean =>
      p.targetRole === targetRole && p.amount === expected.signedAmount
      && p.fullHash === expected.fullHash.toLowerCase() && p.partialRoot === expected.partialRoot.toLowerCase();
    const hits = pulls.filter(isExpected);
    return hits.length > 1 ? crossErr("CROSS_J_FINAL_PULL_AMBIGUOUS") : ok(hits[0]);
  });
const safeUintOf = (v: unknown, max: number): number | undefined => {
  const n = Number(v);
  return Number.isSafeInteger(n) && n >= 0 && n <= max ? n : undefined;
};
type FinalizedLegQuery = Readonly<{
  route: Pick<CrossRoute, "orderId" | "source" | "target">; self: string; counterparty: string;
  localStack?: string | undefined;
}>;
/** og resolveFinalizedCrossJurisdictionRouteLeg: the leg between this Account's entities, on its stack. */
export const finalizedRouteLeg = (x: FinalizedLegQuery): Result<CrossLegRole | undefined, CrossError> => {
  const self = x.self.toLowerCase();
  const peer = x.counterparty.toLowerCase();
  const joinsThisAccount = (l: CrossLeg): boolean => {
    const e = l.entityId.toLowerCase();
    const c = l.counterpartyEntityId.toLowerCase();
    return (e === self && c === peer) || (e === peer && c === self);
  };
  const candidates = CROSS_LEG_ROLES.filter((role) => joinsThisAccount(x.route[role]));
  if (candidates.length === 0) return ok(undefined);
  if (!x.localStack) return crossErr("CROSS_J_FINALITY_JURISDICTION_MISSING");
  const stack = x.localStack.toLowerCase();
  const onThisStack = candidates.filter((role) => x.route[role].jurisdiction.toLowerCase() === stack);
  switch (onThisStack.length) {
    case 0: return crossErr("CROSS_J_FINALITY_LEG_MISSING");
    case 1: return ok(onThisStack[0]);
    default: return crossErr("CROSS_J_FINALITY_LEG_AMBIGUOUS");
  }
};
type DisputeClock = Readonly<{ disputeStartTimestamp?: unknown; disputeTimeout?: unknown }>;
type ProofWindows = Pick<ProofBody, "leftResponseSeconds" | "rightResponseSeconds">;
type DisputeWindows = Readonly<{ start: number; left: number; right: number }>;
/** The active dispute's start and each side's window; its timeout must be start + left + right. */
const disputeWindows = (
  active: DisputeClock | undefined, proofbody: ProofWindows,
): Result<DisputeWindows, CrossError> => {
  const left = safeUintOf(proofbody.leftResponseSeconds, MAX_UINT32);
  const right = safeUintOf(proofbody.rightResponseSeconds, MAX_UINT32);
  const start = safeUintOf(active?.disputeStartTimestamp, Number.MAX_SAFE_INTEGER);
  const timeout = safeUintOf(active?.disputeTimeout, Number.MAX_SAFE_INTEGER);
  if (left === undefined || right === undefined || start === undefined || timeout === undefined) {
    return crossErr("CROSS_J_FINAL_WINDOW_INVALID");
  }
  if (timeout !== start + left + right) return crossErr("CROSS_J_FINAL_CLOCK_MISMATCH");
  return ok({ start, left, right });
};
/** The signed ratio, raised by a higher registry reveal inside the beneficiary's window (left for a positive pull). */
const raisedByRecord = (
  pull: SignedProofBodyPull, w: DisputeWindows, record: HashLadderRegistryRecord | undefined,
): Result<number, CrossError> => {
  if (record === undefined) return ok(pull.claimedRatio);
  const ratio = safeUintOf(record.fillRatio, MAX_FILL);
  const revealedAt = safeUintOf(record.revealedAt, Number.MAX_SAFE_INTEGER);
  if (ratio === undefined || revealedAt === undefined) return crossErr("CROSS_J_REGISTRY_RECORD_INVALID");
  const window = pull.amount > 0n ? w.left : w.right;
  const inWindow = revealedAt >= w.start && revealedAt <= w.start + window;
  return ok(inWindow && ratio > pull.claimedRatio ? ratio : pull.claimedRatio);
};
/** og resolveFinalizedPullFillRatio (DeltaTransformer.applyPull). */
export const finalizedPullFillRatio = (x: Readonly<{
  active?: DisputeClock | undefined; proofbody: Pick<ProofBody, "transformers"> & ProofWindows;
  transformerAddress: string; expectedPull: CrossPullLeg; targetRole: boolean;
  record?: HashLadderRegistryRecord | undefined;
}>): Result<number, CrossError> => {
  const found = findSignedProofBodyPull(x.proofbody, x.expectedPull, x.targetRole, x.transformerAddress);
  return chain(found, (pull): Result<number, CrossError> => {
    if (pull === undefined) return crossErr("CROSS_J_FINAL_PULL_MISSING");
    return chain(disputeWindows(x.active, x.proofbody), (w) => raisedByRecord(pull, w, x.record));
  });
};


// ---- account deltas ----
// One Delta per token. `ondelta` moves only on-chain, `offdelta` off-chain; their sum is how far the balance has
// moved right of the collateral split. Each side can pay up to its share plus the credit the other side extends.
export type Delta = Readonly<{
  tokenId: TokenId; collateral: bigint; ondelta: bigint; offdelta: bigint;
  leftCreditLimit: bigint; rightCreditLimit: bigint;
}>;
export type AccountState = Readonly<{ id: AccountId; deltas: ReadonlyMap<TokenId, Delta> }>;
export type AccountError =
  | Tagged<"insufficient_capacity", { available: bigint; requested: bigint }>
  | Tagged<"negative_collateral" | "negative_credit_limit" | "negative_transfer">
  | Tagged<"credit_limit_too_large" | "payment_too_large" | "non_positive_payment">;
/** og direct-payment.ts:30, lock.ts:49: either sender moves up to the full uint256 magnitude of SignedAmount. */
export const MAX_PAYMENT_AMOUNT = (1n << 256n) - 1n;
const inPaymentRange = (v: bigint): boolean => v >= 1n && v <= MAX_PAYMENT_AMOUNT;
export const INT512_MIN = -(1n << 511n);
export const INT512_MAX = (1n << 511n) - 1n;
export const MAX_CREDIT_LIMIT = (1n << 256n) - 1n;
const floor0 = (n: bigint): bigint => (n > 0n ? n : 0n);
export const zeroDelta = (tokenId: TokenId): Delta => ({
  tokenId, collateral: 0n, ondelta: 0n, offdelta: 0n, leftCreditLimit: 0n, rightCreditLimit: 0n,
});
export const genesisAccount = (id: AccountId): AccountState => ({ id, deltas: new Map() });
export const getDelta = (s: AccountState, tk: TokenId): Delta => s.deltas.get(tk) ?? zeroDelta(tk);
export const setDelta = (s: AccountState, d: Delta): AccountState => ({ ...s, deltas: mapSet(s.deltas, d.tokenId, d) });
export const updateDelta = <E>(
  s: AccountState, tk: TokenId, f: (d: Delta) => Result<Delta, E>,
): Result<AccountState, E> => map(f(getDelta(s, tk)), (d) => setDelta(s, d));
export const totalDelta = (d: Delta): bigint => d.ondelta + d.offdelta;
export const leftCanPay = (d: Delta): bigint => floor0(totalDelta(d) + d.leftCreditLimit);
export const rightCanPay = (d: Delta): bigint => floor0(d.collateral + d.rightCreditLimit - totalDelta(d));
export const outCapacity = (d: Delta, payerIsLeft: boolean, held: bigint): bigint =>
  floor0(at(leftCanPay, rightCanPay, payerIsLeft)(d) - held);
type SideAmounts = Readonly<{ left: bigint; right: bigint }>;
/** The holds a settlement diff takes: a negative diff holds its magnitude against that side's room. */
export const chargeSettlement = (
  d: Delta,
  diff: Readonly<{ leftDiff: bigint; rightDiff: bigint; collateralDiff: bigint }>,
  held: SideAmounts,
): Result<SideAmounts, AccountError> => {
  const left = diff.leftDiff < 0n ? -diff.leftDiff : 0n;
  const right = diff.rightDiff < 0n ? -diff.rightDiff : 0n;
  const leftRoom = outCapacity(d, true, held.left);
  const rightRoom = outCapacity(d, false, held.right);
  // A reserve moving into collateral is funded on-chain, so it is not checked against outgoing room.
  const leftFunds = diff.leftDiff < 0n && diff.collateralDiff > 0n;
  const rightFunds = diff.rightDiff < 0n && diff.collateralDiff > 0n;
  const short = (available: bigint, requested: bigint): Result<never, AccountError> =>
    err({ _tag: "insufficient_capacity", available, requested });
  if (!leftFunds && left > leftRoom) return short(leftRoom, left);
  if (!rightFunds && right > rightRoom) return short(rightRoom, right);
  return ok({ left, right });
};
/** Left pays negative, right pays positive. Zero stays zero. A negative amount is refused. */
export const offdeltaChange = (payerIsLeft: boolean, amount: bigint): Result<bigint, AccountError> =>
  (amount < 0n ? err({ _tag: "negative_transfer" }) : ok(payerIsLeft ? -amount : amount));
/** Unchecked move: releases of prior holds (htlc resolve, swap give). */
export const shift = (d: Delta, by: bigint): Delta => ({ ...d, offdelta: d.offdelta + by });
/** Checked move: `offdeltaChange` picks the sign; holds reduce room. */
export const move = (d: Delta, by: bigint, held: bigint): Result<Delta, AccountError> => {
  if (by === 0n) return ok(d);
  const payerIsLeft = by < 0n;
  const amount = payerIsLeft ? -by : by;
  const available = outCapacity(d, payerIsLeft, held);
  if (amount > MAX_PAYMENT_AMOUNT) return err({ _tag: "payment_too_large" });
  return amount > available ? err({ _tag: "insufficient_capacity", available, requested: amount }) : ok(shift(d, by));
};
export const settle = (d: Delta, collateral: bigint, ondelta: bigint): Delta => ({ ...d, collateral, ondelta });
/** A side sets the credit it extends, which is the limit on the other side. */
export const setCreditLimit = (d: Delta, limit: bigint, byLeft: boolean): Result<Delta, AccountError> => {
  switch (true) {
    case limit < 0n: return err({ _tag: "negative_credit_limit" });
    case limit > MAX_CREDIT_LIMIT: return err({ _tag: "credit_limit_too_large" });
    case other(byLeft): return ok({ ...d, leftCreditLimit: limit });
    default: return ok({ ...d, rightCreditLimit: limit });
  }
};
export const addCollateral = (d: Delta, amount: bigint): Result<Delta, AccountError> =>
  (amount < 0n ? err({ _tag: "negative_collateral" }) : ok(settle(d, d.collateral + amount, d.ondelta)));
export const applyCollateralFixture = (
  s: AccountState, tk: TokenId, amount: bigint,
): Result<AccountState, AccountError> =>
  updateDelta(s, tk, (d) => addCollateral(d, amount));
type SignableDelta = Pick<Delta, "tokenId" | "offdelta" | "leftCreditLimit" | "rightCreditLimit">;
const signableDelta = (d: Delta): SignableDelta => ({
  tokenId: d.tokenId, offdelta: d.offdelta, leftCreditLimit: d.leftCreditLimit, rightCreditLimit: d.rightCreditLimit,
});
type SignableAccount = Readonly<{ id: AccountId; deltas: ReadonlyMap<TokenId, SignableDelta> }>;
export const signableAccount = (s: AccountState): SignableAccount =>
  ({ id: s.id, deltas: new Map([...s.deltas].map(([k, d]) => [k, signableDelta(d)])) });
export const hashAccountState = (s: AccountState): Hash => keccakUtf8(canon(signableAccount(s)));


// ---- account terms and envelopes ----
// Terms are what both sides agreed when the Account opened: the Depository domain, the watchtower seed and the
// dispute windows. Every message between the sides carries an envelope that must repeat them.
export type AccountTerms = Readonly<{ domain: Domain; watchSeed: string; disputeConfig: DisputeConfig }>;
export type TermsError = Tagged<"bad_domain" | "bad_watch_seed" | "bad_dispute_config">;
const MAX_UINT32 = 0xffff_ffff;
const MAX_DISPUTE_SECONDS = 365 * 24 * 60 * 60;
export const disputeConfigOf = (config: unknown): Result<DisputeConfig, TermsError> => {
  if (typeof config !== "object" || config === null) return err({ _tag: "bad_dispute_config" });
  const { leftResponseSeconds: left, rightResponseSeconds: right } = config as { readonly [k: string]: unknown };
  const isWindow = (n: unknown): n is number =>
    typeof n === "number" && Number.isSafeInteger(n) && n >= 0 && n <= MAX_UINT32;
  if (!isWindow(left) || !isWindow(right)) return err({ _tag: "bad_dispute_config" });
  if (left + right > MAX_DISPUTE_SECONDS) return err({ _tag: "bad_dispute_config" });
  return ok({ leftResponseSeconds: left, rightResponseSeconds: right });
};
export const isWatchSeed = (seed: unknown): seed is string => typeof seed === "string" && WORD.test(seed);
export const accountTerms = (terms: AccountTerms): Result<AccountTerms, TermsError> => {
  const domain = mapErr(domainOf(terms.domain), (): TermsError => ({ _tag: "bad_domain" }));
  return chain(domain, (domain): Result<AccountTerms, TermsError> => {
    if (!isWatchSeed(terms.watchSeed)) return err({ _tag: "bad_watch_seed" });
    const watchSeed = terms.watchSeed.toLowerCase();
    return map(disputeConfigOf(terms.disputeConfig), (disputeConfig) => ({ domain, watchSeed, disputeConfig }));
  });
};
export type Delivery = Tagged<"local"> | Tagged<"received", { from: EntityId }>;
export type AccountEnvelope = Readonly<{
  fromEntityId: EntityId; toEntityId: EntityId; domain: Domain; disputeConfig: DisputeConfig;
  watchSeed?: string | undefined;
}>;
export type EnvelopeError = Tagged<
  | "domain_invalid" | "dispute_config_invalid" | "dispute_config_mismatch" | "party_mismatch"
  | "domain_mismatch" | "watch_seed_invalid" | "watch_seed_mismatch"
>;
export const envelopeOf = (terms: AccountTerms, party: Party): AccountEnvelope => ({
  fromEntityId: party.self, toEntityId: party.peer,
  domain: terms.domain, disputeConfig: terms.disputeConfig, watchSeed: terms.watchSeed,
});
export const namesEntity = (v: unknown, entity: EntityId): boolean => typeof v === "string" && sameHex(v, entity);
type Check = Result<void, EnvelopeError>;
const PARTY_MISMATCH: Check = err({ _tag: "party_mismatch" });
/** A local envelope comes from us; a received one from the sender, to us, and not from us. */
export const deliveredBy = (e: AccountEnvelope, self: EntityId, delivery: Delivery): Check => match(delivery, {
  local: (): Check => (namesEntity(e.fromEntityId, self) ? ok(undefined) : PARTY_MISMATCH),
  received: ({ from }): Check => {
    const fromSender = namesEntity(e.fromEntityId, from) && !namesEntity(e.fromEntityId, self);
    return fromSender && namesEntity(e.toEntityId, self) ? ok(undefined) : PARTY_MISMATCH;
  },
});
export const localOnly = (delivery: Delivery): Check =>
  match(delivery, { local: (): Check => ok(undefined), received: (): Check => PARTY_MISMATCH });
const domainShaped = (d: unknown): boolean => {
  if (typeof d !== "object" || d === null) return false;
  const r = d as Record<string, unknown>;
  return Number.isSafeInteger(r["chainId"]) && typeof r["depositoryAddress"] === "string";
};
/** The envelope repeats our terms and runs between the Account's two entities; yields the sender. */
export const checkEnvelope = (
  id: AccountId, terms: AccountTerms, e: AccountEnvelope,
): Result<EntityId, EnvelopeError> => {
  if (!domainShaped(e.domain)) return err({ _tag: "domain_invalid" });
  const config = disputeConfigOf(e.disputeConfig);
  if (!config.ok) return err({ _tag: "dispute_config_invalid" });
  const held = terms.disputeConfig;
  const sameWindows = config.value.leftResponseSeconds === held.leftResponseSeconds
    && config.value.rightResponseSeconds === held.rightResponseSeconds;
  if (!sameWindows) return err({ _tag: "dispute_config_mismatch" });
  const leftToRight = namesEntity(e.fromEntityId, id.left) && namesEntity(e.toEntityId, id.right);
  const rightToLeft = namesEntity(e.fromEntityId, id.right) && namesEntity(e.toEntityId, id.left);
  if (!leftToRight && !rightToLeft) return err({ _tag: "party_mismatch" });
  const sameDomain = e.domain.chainId === terms.domain.chainId
    && sameHex(e.domain.depositoryAddress, terms.domain.depositoryAddress);
  if (!sameDomain) return err({ _tag: "domain_mismatch" });
  if (e.watchSeed !== undefined && !isWatchSeed(e.watchSeed)) return err({ _tag: "watch_seed_invalid" });
  if (e.watchSeed !== undefined && !sameHex(e.watchSeed, terms.watchSeed)) return err({ _tag: "watch_seed_mismatch" });
  return ok(leftToRight ? id.left : id.right);
};


// ---- the account body: its refusals, its context and what it tells the parent Entity ----
export type ClaimError = Tagged<
  "claim_height" | "claim_events" | "claim_block" | "claim_entity" | "claim_conflict" | "claim_proof"
>;
/** og SettlementHankoNonceRejection: `account` is the derived minimum safe nonce, `workspace` the first-signature pin. */
export type SettlementNonceMismatch = Readonly<{ supplied: number; required: number; basis: "account" | "workspace" }>;
export type BodyError =
  | AccountError | RatioError | Uncommitted | ClaimError
  | Tagged<"settlement_frozen" | "settled_pair" | "settled_nonce">
  | Tagged<"lock_id" | "htlc_envelope" | "htlc_expired" | "htlc_lock_capacity" | "hold_overflow" | "offdelta_range">
  | Tagged<"duplicate" | "missing" | "not_maker" | "before_deadline" | "preimage" | "not_counterparty">
  | Tagged<"index" | "too_many_rows">
  | Tagged<"token_id", { tokenId: string }>
  | Tagged<"settlement", { reason: string; nonce?: SettlementNonceMismatch | undefined }>
  | Tagged<"swap", { reason: string }>
  | Tagged<"rebalance", { reason: string }>
  | Tagged<"lending", { reason: string }>
  | Tagged<"payment_route", { reason: string }>
  | CrossError;
/**
 * The replica's settlement authority, which og passes through AccountConsensusContext: its Hanko verifier and the
 * dispute-proof nonce floor (the max of nextProofNonce, current+1 and counterparty+1).
 * `registeredBoardHash` is og resolveSettlementBoardAuthority's passed board (the counterpartyCertifiedBoard).
 * Without it `boardAuthority` (og's fallback over the source Entity's local replicas) resolves the board; with
 * neither, the Hankos are verified without a pinned board.
 */
export type SettlementCtx = Readonly<{
  verify: Verify; proofNonceFloor: number; deltaTransformer?: DeltaTransformerRef | undefined;
  registeredBoardHash?: string | undefined; boardAuthority?: BoardAuthority | undefined;
}>;
/**
 * og resolveSettlementBoardAuthority with no certified board passed (entity/account/account-consensus-context.ts),
 * bound to the Runtime's local replicas: the source Entity's certified board, undefined for a lazy board or no
 * local replica, or og's thrown text (which og's settlement handler turns into the tx's refusal).
 */
export type BoardAuthority = (sourceEntityId: string) => Result<string | undefined, string>;
export type FoldCtx = Readonly<{
  byLeft: boolean; nowMs: bigint; jHeight: bigint; accountHeight: bigint; settlement?: SettlementCtx | undefined;
}>;
/**
 * Account outputs to the parent Entity (og apply-result outcomes and AccountOutput candidate effects), free of
 * perspective: the consumer adds its own side (og fills entityId/accountId from proofHeader, and only the gateway
 * forwards a trusted payment).
 */
export type Effect =
  | Tagged<"forward_secret", { hashlock: string; secret: string }>
  | Tagged<"htlc_error", { lockId: string; hashlock: string; tokenId: number; amount: bigint; reason?: string }>
  | Tagged<"swap_cancel_requested", { offerId: string }>
  | Tagged<"swap_cancelled", { offerId: string; makerId: string }>
  /** og same-j-swap-output.ts swapOfferUpsert: the committed same-j offer, with the Account's left/right entities. */
  | Tagged<"swap_offer_upsert", { offer: SwapOffer; left: string; right: string }>
  | Tagged<"request_collateral_committed", {
    tokenId: number; requestedAmount: bigint; prepaidFee: bigint; requestedAt: number;
  }>
  /** og j-events/claim.ts: a j_event_claim finalized bilaterally, with the first settled token's row as it now stands. */
  | Tagged<"account_settled_finalized_bilateral", {
    tokenId: number; jHeight: number; collateral: bigint; ondelta: bigint;
  }>
  | Tagged<"direct_payment_forward", {
    tokenId: number; amount: bigint; route: readonly string[]; description?: string; trustedGatewayEntityId: string;
  }>;
const MAX_ROWS = 128;


// ---- HTLC locks and their opaque envelopes ----
export type HtlcLock = Readonly<{
  lockId: string; hashlock: string; timelock: bigint; revealBeforeHeight: bigint; amount: bigint; tokenId: TokenId;
  senderIsLeft: boolean; createdHeight: bigint; createdTimestamp: bigint; envelopeHash?: string | undefined;
}>;
/**
 * og protocol/htlc/multi-recipient.ts OpaqueHtlcCiphertext: exactly {version, ciphertext}, canonical padded base64
 * of ephemeralKey(32) || AES-GCM body || tag(16).
 */
export type HtlcEnvelope = Readonly<{ version: "xln:htlc-opaque:aes-gcm"; ciphertext: string }>;
const HTLC_ENVELOPE_VERSION = "xln:htlc-opaque:aes-gcm";
const MAX_HTLC_BINARY_LAYER_BYTES = Math.floor(((100_000_000 - 1_000_000) * 3) / 4);
const MAX_HTLC_PACKED_BYTES = 32 + MAX_HTLC_BINARY_LAYER_BYTES + 16;
// Base64 packs three bytes into four 6-bit sextets; a short final group is padded with `=`.
const B64 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
const CANONICAL_BASE64 = /^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/;
const chunksOf = <X>(xs: readonly X[], size: number): readonly (readonly X[])[] =>
  Array.from({ length: Math.ceil(xs.length / size) }, (_, i) => xs.slice(i * size, (i + 1) * size));
const sextetsToBytes = (sextets: readonly number[]): readonly number[] => {
  const n = sextets.reduce((acc, s, i) => acc | (s << (18 - 6 * i)), 0);
  return [(n >> 16) & 0xff, (n >> 8) & 0xff, n & 0xff].slice(0, sextets.length - 1);
};
const bytesToSextets = (bytes: readonly number[]): string => {
  const n = ((bytes[0] ?? 0) << 16) | ((bytes[1] ?? 0) << 8) | (bytes[2] ?? 0);
  const chars = [18, 12, 6, 0].map((shift) => B64[(n >> shift) & 63] ?? "");
  return chars.slice(0, bytes.length + 1).join("").padEnd(4, "=");
};
/** og serialization/base64.ts decodeBase64Bytes: padded RFC 4648 only; any non-canonical spelling is refused. */
export const decodeBase64 = (text: string): Uint8Array | null => {
  if (text.length % 4 !== 0 || !CANONICAL_BASE64.test(text)) return null;
  const sextets = [...text.replace(/=+$/, "")].map((ch) => B64.indexOf(ch));
  const bytes = Uint8Array.from(chunksOf(sextets, 4).flatMap(sextetsToBytes));
  return encodeBase64(bytes) === text ? bytes : null;
};
export const encodeBase64 = (bytes: Uint8Array): string => chunksOf([...bytes], 3).map(bytesToSextets).join("");
/** og assertOpaqueHtlcCiphertext + hashOpaqueHtlcCiphertext: the envelopeHash is sha256 of the packed bytes. */
export const htlcEnvelopeHash = (v: unknown): string | null => {
  if (!v || typeof v !== "object" || Array.isArray(v)) return null;
  const rec = v as Record<string, unknown>;
  const keys = Object.keys(rec);
  const ct = rec["ciphertext"];
  const exactKeys = keys.length === 2 && keys.includes("ciphertext") && keys.includes("version");
  if (!exactKeys || rec["version"] !== HTLC_ENVELOPE_VERSION) return null;
  if (typeof ct !== "string" || ct.length === 0 || ct.length > Math.ceil(MAX_HTLC_PACKED_BYTES / 3) * 4) return null;
  const packed = decodeBase64(ct);
  if (packed === null || packed.length < 48 || packed.length > MAX_HTLC_PACKED_BYTES) return null;
  return bytesToHex(sha256(packed));
};
// ---- og HTLC onion: protocol/htlc/{multi-recipient,utils}.ts, codec/{binary,onion,envelope}.ts, pathfinding/{htlc-quote,fees}.ts, payments/delivery.ts ----
// A multi-hop payment carries an onion: one sealed layer per hop, each readable only by that hop's entity key and
// bound to the lock it arrived on. A hop learns its next hop and forward amount; the last learns the secret.
export type OnionError = Tagged<"onion", { code: string }>;
const onionErr = (code: string): Result<never, OnionError> => err({ _tag: "onion", code });
const onionTry = <T>(code: string, f: () => T): Result<T, OnionError> => {
  try {
    return ok(f());
  } catch {
    return onionErr(code);
  }
};

// ---- sealing: X25519 key agreement, HKDF-SHA256, AES-256-GCM ----
const AUTHENTICATION_FAILED = "HTLC_CIPHERTEXT_AUTHENTICATION_FAILED";
/** og multi-recipient.ts keyBytes: trimmed, optional 0x, 64 hex digits, never all zero. */
const x25519KeyBytes = (value: string, code: string): Result<Uint8Array, OnionError> => {
  const t = String(value || "").trim();
  const body = (t.startsWith("0x") ? t.slice(2) : t).toLowerCase();
  if (!/^[0-9a-f]{64}$/.test(body)) return onionErr(code);
  const bytes = hexToBytes(body);
  return bytes.every((b) => b === 0) ? onionErr(code) : ok(bytes);
};
const onionContext = (contextHash: string): Result<Uint8Array, OnionError> => {
  const n = String(contextHash || "").trim().toLowerCase();
  if (!/^0x[0-9a-f]{64}$/.test(n)) return onionErr("HTLC_ENCRYPTION_CONTEXT_HASH_INVALID");
  return ok(utf8(`${HTLC_ENVELOPE_VERSION}:${n}`));
};
const onionAeadKey = (shared: Uint8Array, context: Uint8Array): Result<Uint8Array, OnionError> => {
  if (shared.every((b) => b === 0)) return onionErr("HTLC_X25519_LOW_ORDER_SHARED_SECRET");
  const salt = sha256(utf8(`${HTLC_ENVELOPE_VERSION}:hkdf-salt`));
  return ok(hkdf(sha256, shared, salt, context, 32));
};
const onionNonce = (ephemeral: Uint8Array, recipient: Uint8Array, context: Uint8Array): Uint8Array =>
  sha256(concat([ephemeral, recipient, context])).slice(0, 12);
const opaqueEnvelope = (ciphertext: string): Result<HtlcEnvelope, OnionError> => {
  const envelope: HtlcEnvelope = { version: HTLC_ENVELOPE_VERSION, ciphertext };
  return htlcEnvelopeHash(envelope) === null ? onionErr("HTLC_OPAQUE_CIPHERTEXT_INVALID") : ok(envelope);
};
const envelopeBytes = (envelope: HtlcEnvelope): Uint8Array | null =>
  (htlcEnvelopeHash(envelope) === null ? null : decodeBase64(envelope.ciphertext));
/** og encryptOpaqueHtlcBytes: an ephemeral key agreed with the recipient; packed ephemeralKey ‖ body ‖ tag. */
export const encryptOpaqueHtlc = (
  plaintext: Uint8Array, recipientPublicKey: string, contextHash: string, ephemeralPrivateKey: string,
): Result<HtlcEnvelope, OnionError> => {
  if (plaintext.length > MAX_HTLC_BINARY_LAYER_BYTES) return onionErr("HTLC_ENCRYPTION_PLAINTEXT_TOO_LARGE");
  const keys = all({
    recipient: x25519KeyBytes(recipientPublicKey, "HTLC_ENTITY_ENCRYPTION_PUBLIC_KEY_INVALID"),
    secret: x25519KeyBytes(ephemeralPrivateKey, "HTLC_EPHEMERAL_PRIVATE_KEY_INVALID"),
  });
  return chain(keys, ({ recipient, secret }) => {
    const exchange = onionTry("HTLC_X25519_FAILED", () => ({
      ephemeral: x25519.getPublicKey(secret), shared: x25519.getSharedSecret(secret, recipient),
    }));
    return chain(exchange, ({ ephemeral, shared }) =>
      chain(onionContext(contextHash), (context) =>
        chain(onionAeadKey(shared, context), (key) => {
          const body = gcm(key, onionNonce(ephemeral, recipient, context), context).encrypt(plaintext);
          return opaqueEnvelope(encodeBase64(concat([ephemeral, body])));
        })));
  });
};
/** Any X25519 or low-order failure reads as an authentication failure, so a bad ciphertext reveals nothing more. */
const openingKey = (priv: Uint8Array, ephemeral: Uint8Array, context: Uint8Array): Result<Uint8Array, OnionError> => {
  const shared = onionTry(AUTHENTICATION_FAILED, () => x25519.getSharedSecret(priv, ephemeral));
  const key = chain(shared, (s) => onionAeadKey(s, context));
  return mapErr(key, (): OnionError => ({ _tag: "onion", code: AUTHENTICATION_FAILED }));
};
/** og decryptOpaqueHtlcBytes: the entity keypair must match, and the body must authenticate under the context. */
export const decryptOpaqueHtlc = (
  envelope: HtlcEnvelope, entityPublicKey: string, entityPrivateKey: string, contextHash: string,
): Result<Uint8Array, OnionError> => {
  const packed = envelopeBytes(envelope);
  if (packed === null) return onionErr("HTLC_OPAQUE_CIPHERTEXT_INVALID");
  const ephemeral = packed.slice(0, 32);
  const body = packed.slice(32);
  const keys = all({
    pub: x25519KeyBytes(entityPublicKey, "HTLC_ENTITY_ENCRYPTION_PUBLIC_KEY_INVALID"),
    priv: x25519KeyBytes(entityPrivateKey, "HTLC_ENTITY_ENCRYPTION_PRIVATE_KEY_INVALID"),
  });
  return chain(keys, ({ pub, priv }) => {
    const keypairMatches = bytesToHex(x25519.getPublicKey(priv)) === bytesToHex(pub);
    if (!keypairMatches) return onionErr("HTLC_ENTITY_ENCRYPTION_KEYPAIR_MISMATCH");
    return chain(onionContext(contextHash), (context) =>
      chain(openingKey(priv, ephemeral, context), (key) => {
        const cipher = gcm(key, onionNonce(ephemeral, pub, context), context);
        const opened = onionTry(AUTHENTICATION_FAILED, () => cipher.decrypt(body));
        return chain(opened, (plain) =>
          (plain.length > MAX_HTLC_BINARY_LAYER_BYTES ? onionErr("HTLC_DECRYPTED_PLAINTEXT_TOO_LARGE") : ok(plain)));
      }));
  });
};

// ---- layer codec: og codec/{binary,onion}.ts ----
// A layer is magic `XLON`, version 2, then kind 1 (final: secret, flags, optional note and start time) or kind 2
// (forward: next hop, amount, and the next layer's packed ciphertext framed as `XLMR`, version 1).
/** og codec/onion.ts DecodedOnionLayer. */
export type FinalLayer = Readonly<{
  finalRecipient: true; secret: string; description?: string | undefined; startedAtMs?: number | undefined;
}>;
export type ForwardLayer = Readonly<{ nextHop: string; innerEnvelope: HtlcEnvelope; forwardAmount: string }>;
export type OnionLayer = FinalLayer | ForwardLayer;
const ONION_MAGIC = Uint8Array.of(0x58, 0x4c, 0x4f, 0x4e);
const CIPHERTEXT_MAGIC = Uint8Array.of(0x58, 0x4c, 0x4d, 0x52);
const ONION_VERSION = 2;
const LAYER_FINAL = 1;
const LAYER_FORWARD = 2;
const FLAG_DESCRIPTION = 1;
const FLAG_STARTED_AT = 2;
const beBytes = (value: bigint, width: number): Uint8Array =>
  Uint8Array.from({ length: width }, (_, i) => Number((value >> BigInt(8 * (width - 1 - i))) & 0xffn));
const beValue = (bytes: Uint8Array): bigint => bytes.reduce((n, b) => (n << 8n) | BigInt(b), 0n);
/** og BinaryWriter: every part must encode, and the whole stays within the layer limit. */
const onionWrite = (parts: readonly (Uint8Array | null)[], code: string): Result<Uint8Array, OnionError> => {
  const encoded = parts.flatMap((p) => (p === null ? [] : [p]));
  if (encoded.length !== parts.length) return onionErr(code);
  const out = concat(encoded);
  return out.length > MAX_HTLC_BINARY_LAYER_BYTES ? onionErr(code) : ok(out);
};
const u16Text = (s: string): Uint8Array | null => {
  const b = utf8(s);
  return b.length > 0xffff ? null : concat([beBytes(BigInt(b.length), 2), b]);
};
const u32Sized = (b: Uint8Array): Uint8Array | null =>
  (b.length > 0xffffffff ? null : concat([beBytes(BigInt(b.length), 4), b]));
const exactHex = (value: string, width: number): Uint8Array | null => {
  const t = String(value || "").trim();
  return /^0x(?:[0-9a-f]{2})*$/i.test(t) && t.length === 2 + width * 2 ? hexToBytes(t) : null;
};
const LAYER_TOO_LARGE = "HTLC_ONION_LAYER_TOO_LARGE";
const layerHead = concat([ONION_MAGIC, Uint8Array.of(ONION_VERSION)]);
const encodeFinalLayer = (layer: FinalLayer): Result<Uint8Array, OnionError> => {
  const secret = exactHex(layer.secret, 32);
  if (secret === null) return onionErr("HTLC_ONION_FINAL_SECRET_INVALID");
  const { description: note, startedAtMs: at } = layer;
  if (at !== undefined && (!Number.isSafeInteger(at) || at <= 0)) return onionErr("HTLC_ONION_STARTED_AT_INVALID");
  const flags = (note !== undefined ? FLAG_DESCRIPTION : 0) | (at !== undefined ? FLAG_STARTED_AT : 0);
  return onionWrite([
    layerHead, Uint8Array.of(LAYER_FINAL), secret, Uint8Array.of(flags),
    note === undefined ? new Uint8Array() : u16Text(note),
    at === undefined ? new Uint8Array() : beBytes(BigInt(at), 8),
  ], LAYER_TOO_LARGE);
};
const encodeForwardLayer = (layer: ForwardLayer): Result<Uint8Array, OnionError> => {
  const amount = onionTry("HTLC_ONION_FORWARD_AMOUNT_INVALID", () => BigInt(layer.forwardAmount));
  const inRange = amount.ok && amount.value > 0n && amount.value <= UINT256_MAX;
  if (!amount.ok || !inRange) return onionErr("HTLC_ONION_FORWARD_AMOUNT_INVALID");
  const packed = envelopeBytes(layer.innerEnvelope);
  if (packed === null) return onionErr("HTLC_OPAQUE_CIPHERTEXT_INVALID");
  const framed = onionWrite([CIPHERTEXT_MAGIC, Uint8Array.of(1), u32Sized(packed)], "HTLC_CIPHERTEXT_BINARY_TOO_LARGE");
  return chain(framed, (inner) => onionWrite([
    layerHead, Uint8Array.of(LAYER_FORWARD), u16Text(layer.nextHop), beBytes(amount.value, 32), u32Sized(inner),
  ], LAYER_TOO_LARGE));
};
export const encodeOnionLayer = (layer: OnionLayer): Result<Uint8Array, OnionError> =>
  ("finalRecipient" in layer ? encodeFinalLayer(layer) : encodeForwardLayer(layer));
/** og BinaryReader: every read is bounded; a read yields its value and where the next one starts. */
type Read<T> = Readonly<{ value: T; next: number }> | null;
const readBytes = (input: Uint8Array, at: number, n: number): Read<Uint8Array> =>
  (n > MAX_HTLC_BINARY_LAYER_BYTES || at + n > input.length ? null : { value: input.slice(at, at + n), next: at + n });
const readUint = (input: Uint8Array, at: number, n: number): Read<number> => {
  const b = readBytes(input, at, n);
  return b === null ? null : { value: Number(beValue(b.value)), next: b.next };
};
const readSized = (input: Uint8Array, at: number): Read<Uint8Array> => {
  const n = readUint(input, at, 4);
  return n === null ? null : readBytes(input, n.next, n.value);
};
const fatalUtf8 = (b: Uint8Array): string | null => {
  try {
    return new TextDecoder("utf-8", { fatal: true }).decode(b);
  } catch {
    return null;
  }
};
const readText = (input: Uint8Array, at: number): Read<string> => {
  const n = readUint(input, at, 2);
  const b = n === null ? null : readBytes(input, n.next, n.value);
  const text = b === null ? null : fatalUtf8(b.value);
  return b === null || text === null ? null : { value: text, next: b.next };
};
const startsWith = (bytes: Uint8Array, magic: Uint8Array): boolean =>
  bytes.length >= magic.length && magic.every((b, i) => bytes[i] === b);
const decodeOpaqueCiphertext = (bytes: Uint8Array): Result<HtlcEnvelope, OnionError> => {
  const invalid = onionErr("HTLC_CIPHERTEXT_BINARY_INVALID");
  const version = startsWith(bytes, CIPHERTEXT_MAGIC) ? readUint(bytes, 4, 1) : null;
  if (bytes.length > MAX_HTLC_BINARY_LAYER_BYTES || version === null || version.value !== 1) return invalid;
  const body = readSized(bytes, version.next);
  return body === null || body.next !== bytes.length ? invalid : opaqueEnvelope(encodeBase64(body.value));
};
const LAYER_INVALID = onionErr("HTLC_ONION_LAYER_INVALID");
const decodeFinalLayer = (bytes: Uint8Array, at: number): Result<OnionLayer, OnionError> => {
  const secret = readBytes(bytes, at, 32);
  const flags = secret === null ? null : readUint(bytes, secret.next, 1);
  const knownFlags = FLAG_DESCRIPTION | FLAG_STARTED_AT;
  if (secret === null || flags === null || (flags.value & ~knownFlags) !== 0) return LAYER_INVALID;
  const description = (flags.value & FLAG_DESCRIPTION) !== 0
    ? readText(bytes, flags.next)
    : { value: undefined, next: flags.next };
  if (description === null) return LAYER_INVALID;
  const started = (flags.value & FLAG_STARTED_AT) !== 0
    ? readBytes(bytes, description.next, 8)
    : { value: undefined, next: description.next };
  if (started === null || started.next !== bytes.length) return LAYER_INVALID;
  const startedAt = started.value === undefined ? undefined : beValue(started.value);
  if (startedAt !== undefined && startedAt > BigInt(Number.MAX_SAFE_INTEGER)) return LAYER_INVALID;
  return ok({
    finalRecipient: true, secret: bytesToHex(secret.value),
    ...opt("description", description.value),
    ...opt("startedAtMs", startedAt === undefined ? undefined : Number(startedAt)),
  });
};
const decodeForwardLayer = (bytes: Uint8Array, at: number): Result<OnionLayer, OnionError> => {
  const nextHop = readText(bytes, at);
  const amount = nextHop === null ? null : readBytes(bytes, nextHop.next, 32);
  const inner = amount === null ? null : readSized(bytes, amount.next);
  if (nextHop === null || amount === null || inner === null || beValue(amount.value) <= 0n) return LAYER_INVALID;
  return chain(decodeOpaqueCiphertext(inner.value), (innerEnvelope) => {
    if (inner.next !== bytes.length) return LAYER_INVALID;
    return ok({ nextHop: nextHop.value, innerEnvelope, forwardAmount: beValue(amount.value).toString() });
  });
};
export const decodeOnionLayer = (bytes: Uint8Array): Result<OnionLayer, OnionError> => {
  if (bytes.length > MAX_HTLC_BINARY_LAYER_BYTES || !startsWith(bytes, ONION_MAGIC)) return LAYER_INVALID;
  const version = readUint(bytes, 4, 1);
  if (version === null) return LAYER_INVALID;
  if (version.value !== ONION_VERSION) return onionErr("HTLC_ONION_LAYER_VERSION_INVALID");
  switch (readUint(bytes, 5, 1)?.value) {
    case LAYER_FINAL: return decodeFinalLayer(bytes, 6);
    case LAYER_FORWARD: return decodeForwardLayer(bytes, 6);
    default: return LAYER_INVALID;
  }
};

// ---- deadlines: og config/constants.ts, payments/delivery.ts ----
// The source's lock gets the whole window; each forward shortens the timelock by one delta and reveals one step
// of blocks earlier, so every hop can still claim upstream after its downstream reveals.
export const HTLC_TIMELOCK_DELTA_MS = 10_000;
export const HTLC_REVEAL_DELTA_BLOCKS = 3;
export const HTLC_MIN_FORWARD_TIMELOCK_MS = 20_000;
export const HTLC_MAX_HOPS = 100;
const INSTANT_PAYMENT_EXPIRY_MS = 120_000;
const INSTANT_PAYMENT_EXPIRY_BLOCKS = 50;
const ASYNC_PAYMENT_EXPIRY_MS = 24 * 60 * 60 * 1000;
const ASYNC_PAYMENT_EXPIRY_BLOCKS = Math.ceil(ASYNC_PAYMENT_EXPIRY_MS / 5_000);
export type ConditionalMode = "instant" | "async";
type DeadlineWindow = Readonly<{ baseTimelock: bigint; baseHeight: number }>;
/** og resolvePaymentDeadlineWindow (callers validate their inputs with toJHeight / toUnixMs). */
export const paymentDeadlineWindow = (
  mode: ConditionalMode, jHeight: number, timestamp: number, totalHops: number,
): Result<DeadlineWindow, OnionError> => {
  const isAsync = mode === "async";
  const baseHeight = jHeight + (isAsync ? ASYNC_PAYMENT_EXPIRY_BLOCKS : INSTANT_PAYMENT_EXPIRY_BLOCKS);
  if (!Number.isSafeInteger(baseHeight) || baseHeight < 0) return onionErr("PROTOCOL_J_HEIGHT_INVALID");
  const minimum = totalHops * HTLC_TIMELOCK_DELTA_MS + HTLC_MIN_FORWARD_TIMELOCK_MS;
  const expiry = Math.max(isAsync ? ASYNC_PAYMENT_EXPIRY_MS : INSTANT_PAYMENT_EXPIRY_MS, minimum);
  return ok({ baseTimelock: BigInt(timestamp + expiry), baseHeight });
};
/** og calculateHopTimelock / calculateHopRevealHeight. */
export const hopTimelock = (base: bigint, hopIndex: number): bigint =>
  base - BigInt(hopIndex) * BigInt(HTLC_TIMELOCK_DELTA_MS);
export const hopRevealHeight = (baseHeight: number, hopIndex: number, totalHops: number): number =>
  baseHeight + (totalHops - hopIndex) * HTLC_REVEAL_DELTA_BLOCKS;

// ---- building the onion: og createOnionEnvelopes ----
/** og computeHtlcEnvelopeContextHash: the fixed binary AAD binding one layer to its lock. */
export type HtlcEnvelopeContext = Readonly<{
  fromEntityId: string; toEntityId: string; domain: Domain; hashlock: string; tokenId: number;
  amount: bigint; timelock: bigint; revealBeforeHeight: number;
}>;
export const htlcEnvelopeContextHash = (c: HtlcEnvelopeContext): Result<string, OnionError> => {
  const hex = (value: string, width: number, code: string): Result<Uint8Array, OnionError> => {
    const body = /^0[xX]/.test(value) ? value.slice(2) : value;
    return body.length === width * 2 && /^[0-9a-fA-F]*$/.test(body) ? ok(hexToBytes(body)) : onionErr(code);
  };
  const u64 = (v: number, code: string): Result<Uint8Array, OnionError> =>
    (Number.isSafeInteger(v) && v >= 0 ? ok(beBytes(BigInt(v), 8)) : onionErr(code));
  const u256 = (v: bigint, code: string): Result<Uint8Array, OnionError> =>
    (v >= 0n && v <= UINT256_MAX ? ok(beBytes(v, 32)) : onionErr(code));
  const fields = [
    hex(c.fromEntityId.toLowerCase(), 32, "HTLC_CONTEXT_FROM_INVALID"),
    hex(c.toEntityId.toLowerCase(), 32, "HTLC_CONTEXT_TO_INVALID"),
    u64(c.domain.chainId, "HTLC_CONTEXT_CHAIN_INVALID"),
    hex(c.domain.depositoryAddress.toLowerCase(), 20, "HTLC_CONTEXT_DEPOSITORY_INVALID"),
    hex(c.hashlock.toLowerCase(), 32, "HTLC_CONTEXT_HASHLOCK_INVALID"),
    u64(c.tokenId, "HTLC_CONTEXT_TOKEN_INVALID"),
    u256(c.amount, "HTLC_CONTEXT_AMOUNT_INVALID"),
    u256(c.timelock, "HTLC_CONTEXT_TIMELOCK_INVALID"),
    u64(c.revealBeforeHeight, "HTLC_CONTEXT_REVEAL_INVALID"),
  ];
  const tag = utf8("xln:htlc-envelope-context:binary");
  return map(traverse(fields, (r) => r), (parts) => keccak256Hex(concat([tag, ...parts])));
};
/** og createOnionEnvelopes' lock binding: the source's first-hop lock. */
export type HtlcEnvelopeBinding = Readonly<{
  hashlock: string; tokenId: number; senderLockAmount: bigint; timelock: bigint; revealBeforeHeight: number;
}>;
/** og route loop rule: unique entities, or a self route with at least two unique intermediates not the source. */
const routeLoopFree = (route: readonly string[]): boolean => {
  if (route[0] !== route[route.length - 1]) return new Set(route).size === route.length;
  const inner = route.slice(1, -1);
  return inner.length >= 2 && new Set(route).size === route.length - 1 && new Set(inner).size === inner.length;
};
/**
 * og createOnionEnvelopes: the final layer carries the secret (plus note and start time); each earlier hop wraps
 * {nextHop, innerEnvelope, forwardAmount}. Layer i is sealed to route[i] under the context of the lock
 * route[i-1] → route[i], with the proposer's ephemeral key `ephemeralAt(i)`.
 */
export const createOnionEnvelopes = (
  route: readonly string[], secret: string, keys: ReadonlyMap<string, string>, domains: readonly Domain[],
  forwards: ReadonlyMap<string, bigint>, description: string | undefined, startedAtMs: number | undefined,
  binding: HtlcEnvelopeBinding, ephemeralAt: (hopIndex: number) => string,
): Result<HtlcEnvelope, OnionError> => {
  const routable = route.length >= 2 && route.length - 1 <= HTLC_MAX_HOPS && routeLoopFree(route);
  if (!routable) return onionErr("HTLC_ONION_ROUTE_INVALID");
  if (domains.length !== route.length - 1) return onionErr("HTLC_ACCOUNT_DOMAIN_COUNT_MISMATCH");
  const contextAt = (i: number): Result<string, OnionError> => {
    const inbound = i === 1 ? binding.senderLockAmount : forwards.get(route[i - 1] ?? "");
    const domain = domains[i - 1];
    if (inbound === undefined || domain === undefined) return onionErr("HTLC_ONION_INBOUND_AMOUNT_MISSING");
    return htlcEnvelopeContextHash({
      fromEntityId: route[i - 1] ?? "", toEntityId: route[i] ?? "", domain,
      hashlock: binding.hashlock, tokenId: binding.tokenId, amount: inbound,
      timelock: hopTimelock(binding.timelock, i - 1),
      revealBeforeHeight: binding.revealBeforeHeight - (i - 1) * HTLC_REVEAL_DELTA_BLOCKS,
    });
  };
  const sealAt = (i: number, layer: OnionLayer): Result<HtlcEnvelope, OnionError> => {
    const key = keys.get(route[i] ?? "");
    if (key === undefined || key === "") return onionErr("HTLC_ONION_KEY_MISSING");
    return chain(encodeOnionLayer(layer), (bytes) =>
      chain(contextAt(i), (context) => encryptOpaqueHtlc(bytes, key, context, ephemeralAt(i))));
  };
  const wrapAt = (inner: HtlcEnvelope, i: number): Result<HtlcEnvelope, OnionError> => {
    const forward = forwards.get(route[i] ?? "");
    if (forward === undefined) return onionErr("HTLC_ONION_FORWARD_AMOUNT_MISSING");
    return sealAt(i, { nextHop: route[i + 1] ?? "", innerEnvelope: inner, forwardAmount: forward.toString() });
  };
  const last = route.length - 1;
  const finalLayer: FinalLayer = {
    finalRecipient: true, secret, ...opt("description", description || undefined), ...opt("startedAtMs", startedAtMs),
  };
  const intermediariesInward = Array.from({ length: last - 1 }, (_, k) => last - 1 - k);
  return chain(sealAt(last, finalLayer), (sealed) => foldResult(intermediariesInward, sealed, wrapAt));
};

// ---- quoting: og pathfinding/{htlc-quote,fees}.ts ----
// Walking back from the recipient, each intermediary must receive enough that, after its fee, it forwards what the
// next hop needs. The fee is its base plus a ppm rate that rises with how much of its lane is already used.
/** og gossip Profile fields the HTLC router reads (pathfinding/htlc-quote.ts RoutingProfile). */
type TokenCapacity = Readonly<{ inCapacity: bigint; outCapacity: bigint }>;
export type RoutingAccount = Readonly<{
  counterpartyId: string; domain: Domain; tokenCapacities: ReadonlyMap<number, TokenCapacity>;
}>;
export type RoutingProfile = Readonly<{
  entityId: string; accounts: readonly RoutingAccount[]; entityEncryptionPublicKey: string;
  metadata: Readonly<{ routingFeePPM?: number | undefined; baseFee?: bigint | undefined }>;
}>;
/** og buildRoutingProfileIndex + lookupUniqueRoutingProfile: exactly one Profile per (lowercased) entity. */
export type RoutingIndex = ReadonlyMap<string, readonly RoutingProfile[]>;
export const routingIndex = (profiles: readonly RoutingProfile[]): RoutingIndex =>
  Map.groupBy(profiles, (p) => p.entityId.toLowerCase());
export const uniqueProfile = (index: RoutingIndex, entity: string): Result<RoutingProfile, OnionError> => {
  const id = entity.toLowerCase();
  const found = index.get(id) ?? [];
  const [only] = found;
  const matchCount = `HTLC_PAYMENT_PROFILE_MATCH_COUNT:${id}:${found.length}`;
  if (found.length !== 1 || only === undefined) return onionErr(matchCount);
  return ok(only);
};
const MAX_ROUTING_FEE_PPM = 999_999;
/** og sanitizeFeePPM: a whole ppm in [0, 999999]. */
const sanitizeFeePpm = (raw: unknown, fallback = 1): number => {
  const n = Number(raw);
  if (!Number.isFinite(n)) return fallback;
  const v = Math.floor(n);
  return v < 0 ? 0 : Math.min(v, MAX_ROUTING_FEE_PPM);
};
/** og calculateDirectionalFeePPM: the base rate raised by the lane's used share, capped at 50%, in 5% buckets. */
export const directionalFeePpm = (basePpm: number, outCapacity: bigint, inCapacity: bigint): number => {
  const basePpmClean = sanitizeFeePpm(basePpm, 1);
  const base = BigInt(basePpmClean);
  const out = floor0(outCapacity);
  const total = out + floor0(inCapacity);
  if (total <= 0n) return basePpmClean;
  const usedPpm = ((total - out) * 1_000_000n) / total;
  const bucketed = ((usedPpm > 500_000n ? 500_000n : usedPpm) / 50_000n) * 50_000n;
  const raised = base + (base * bucketed) / 1_000_000n;
  return Math.min(MAX_ROUTING_FEE_PPM, Math.max(0, Math.floor(Number(raised))));
};
const wholePpm = (ppm: number): number => (Number.isFinite(ppm) && ppm >= 0 ? Math.floor(ppm) : 0);
/** og calculateHtlcForwardAmount: amountIn − (baseFee + ⌊amountIn·ppm/1e6⌋); the fee must stay below it. */
const htlcForward = (amountIn: bigint, ppm: number, baseFee: bigint): Result<bigint, OnionError> => {
  if (amountIn <= 0n) return onionErr("HTLC_AMOUNT_NOT_POSITIVE");
  const fee = baseFee + (amountIn * BigInt(wholePpm(ppm))) / 1_000_000n;
  return fee >= amountIn ? onionErr("HTLC_FEE_EXCEEDS_AMOUNT") : ok(amountIn - fee);
};
/** og calculateRequiredInboundForDesiredForward: the least inbound whose forward covers `desired`. */
export const requiredInbound = (desired: bigint, feePpm: number, baseFee: bigint): Result<bigint, OnionError> => {
  if (desired <= 0n) return onionErr("HTLC_AMOUNT_NOT_POSITIVE");
  const ppm = wholePpm(feePpm);
  if (ppm === 0 && baseFee === 0n) return ok(desired);
  const covers = (x: bigint): Result<boolean, OnionError> => map(htlcForward(x, ppm, baseFee), (out) => out >= desired);
  // Double until the inbound covers, then bisect down to the least one that does.
  const growFrom = (high: bigint): Result<bigint, OnionError> =>
    chain(covers(high), (enough) => (enough ? ok(high) : growFrom(high * 2n)));
  const bisect = (low: bigint, high: bigint): Result<bigint, OnionError> => {
    if (low >= high) return ok(low);
    const mid = (low + high) / 2n;
    return chain(covers(mid), (enough) => (enough ? bisect(low, mid) : bisect(mid + 1n, high)));
  };
  const least = desired + baseFee;
  return chain(growFrom(least), (high) => bisect(least, high));
};
type LaneCapacity = Readonly<{ out: bigint; in: bigint }>;
/** The hop's capacity toward `to`: from its own Profile, else mirrored from the peer's. */
const laneCapacity = (
  index: RoutingIndex, tokenId: number, from: string, to: string,
): Result<LaneCapacity, OnionError> => {
  const notAdvertised = onionErr(`HTLC_PAYMENT_PROFILE_TOKEN_NOT_ADVERTISED:${from}:${to}:${tokenId}`);
  return chain(uniqueProfile(index, from), (own) => {
    const row = own.accounts.find((a) => a.counterpartyId.toLowerCase() === to);
    if (row !== undefined) {
      const cap = row.tokenCapacities.get(tokenId);
      return cap === undefined ? notAdvertised : ok({ out: cap.outCapacity, in: cap.inCapacity });
    }
    return chain(uniqueProfile(index, to), (peer) => {
      const mirror = peer.accounts.find((a) => a.counterpartyId.toLowerCase() === from);
      if (mirror === undefined) return onionErr(`HTLC_PAYMENT_PROFILE_ACCOUNT_MISSING:${from}:${to}`);
      const cap = mirror.tokenCapacities.get(tokenId);
      return cap === undefined ? notAdvertised : ok({ out: cap.inCapacity, in: cap.outCapacity });
    });
  });
};
export type RouteQuote = Readonly<{ senderLockAmount: bigint; hopForwardAmounts: ReadonlyMap<string, bigint> }>;
type QuoteWalk = Readonly<{ inbound: bigint; forwards: ReadonlyMap<string, bigint> }>;
/** og quoteHtlcPaymentRouteWithIndex. */
export const quoteHtlcRoute = (
  index: RoutingIndex, route: readonly string[], tokenId: number, recipientAmount: bigint,
): Result<RouteQuote, OnionError> => {
  const chargeHop = (walk: QuoteWalk, i: number): Result<QuoteWalk, OnionError> => {
    const hop = route[i] ?? "";
    const next = route[i + 1] ?? "";
    const quoted = all({ profile: uniqueProfile(index, hop), lane: laneCapacity(index, tokenId, hop, next) });
    return chain(quoted, ({ profile, lane }) => {
      const ppm = directionalFeePpm(sanitizeFeePpm(profile.metadata.routingFeePPM ?? 1, 1), lane.out, lane.in);
      const baseFee = floor0(profile.metadata.baseFee ?? 0n);
      const forwards = mapSet(walk.forwards, hop, walk.inbound);
      return map(requiredInbound(walk.inbound, ppm, baseFee), (inbound) => ({ inbound, forwards }));
    });
  };
  const intermediariesInward = Array.from({ length: Math.max(0, route.length - 2) }, (_, k) => route.length - 2 - k);
  const start: QuoteWalk = { inbound: recipientAmount, forwards: new Map() };
  const walked = foldResult(intermediariesInward, start, chargeHop);
  return map(walked, (q) => ({ senderLockAmount: q.inbound, hopForwardAmounts: q.forwards }));
};
// ---- og pathfinding/{graph,pathfinding,capacity}.ts: the gossip network graph and the Dijkstra route finder behind htlcPayment route discovery ----
// Gossip Profiles advertise each entity's Accounts with per-token capacities. One advertised row gives both
// directions of a lane; the finder searches loop-free paths cheapest first and keeps those every lane can carry.
export type AccountEdge = Readonly<{
  from: string; to: string; tokenId: number; capacity: bigint; baseFee: bigint; feePPM: number; disabled: boolean;
}>;
type LaneBalance = Readonly<{ outbound: bigint; inbound: bigint }>;
export type NetworkGraph = Readonly<{
  nodes: ReadonlySet<string>; edges: ReadonlyMap<string, readonly AccountEdge[]>;
  accountCapacities: ReadonlyMap<string, LaneBalance>;
}>;
type RouteHop = Readonly<{ from: string; to: string; fee: bigint; feePPM: number }>;
export type PaymentRoute = Readonly<{
  path: readonly string[]; hops: readonly RouteHop[]; totalFee: bigint; totalAmount: bigint; probability: number;
}>;
type GraphCaps = Readonly<{ inCapacity: bigint; outCapacity: bigint }>;
type GraphAccount = Readonly<{ counterpartyId: string; caps: (tokenId: number) => GraphCaps | null }>;
type GraphProfile = Readonly<{
  entityId: string; name: string; isHub: boolean; routingFeePPM: unknown; baseFee: bigint;
  accounts: readonly GraphAccount[];
}>;
/** og getTokenCapacity: a Map keyed by number or string, or a Record keyed by string; false or null is none. */
const capsReader = (caps: unknown) => (tokenId: number): GraphCaps | null => {
  if (caps === null || caps === undefined || typeof caps !== "object") return null;
  const raw = caps instanceof Map
    ? (caps.get(tokenId) ?? caps.get(String(tokenId)))
    : (caps as Fields)[String(tokenId)];
  return raw === null || raw === undefined || raw === false ? null : (raw as GraphCaps);
};
/** og Profile fields buildNetworkGraph reads. */
const graphProfileOf = (b: Binary): GraphProfile | null => {
  if (b === null || typeof b !== "object" || Array.isArray(b) || b instanceof Map) return null;
  const p = b as Fields;
  const meta = (p["metadata"] ?? {}) as Fields;
  const accounts = Array.isArray(p["accounts"]) ? (p["accounts"] as readonly Fields[]) : [];
  return {
    entityId: String(p["entityId"] ?? ""), name: typeof p["name"] === "string" ? p["name"] : "",
    isHub: meta["isHub"] === true, routingFeePPM: meta["routingFeePPM"],
    baseFee: typeof meta["baseFee"] === "bigint" ? meta["baseFee"] : 0n,
    accounts: accounts.map((a) => ({
      counterpartyId: String(a["counterpartyId"] ?? ""), caps: capsReader(a["tokenCapacities"]),
    })),
  };
};
/** A hub is routable only with a name and a finite non-negative fee rate. */
const unroutableHub = (p: GraphProfile): boolean => {
  const ppm = Number(p.routingFeePPM);
  return p.isHub && !(p.name.trim() !== "" && Number.isFinite(ppm) && ppm >= 0);
};
type Funding = Readonly<{ sourceEntityId: string; accountId: string }>;
type GraphScope = Readonly<{
  profiles: ReadonlyMap<string, GraphProfile>; nodes: ReadonlySet<string>;
  tokenId: number; funding: Funding | undefined;
}>;
/** Lanes seen so far, and the edges this Profile adds: mirrored ones first, then its own. */
type ProfileWalk = Readonly<{
  capacities: ReadonlyMap<string, LaneBalance>; mirrors: readonly AccountEdge[]; owns: readonly AccountEdge[];
}>;
const graphLaneKey = (from: string, to: string, tokenId: number): string => `${from}:${to}:${tokenId}`;
/** A lane is usable with capacity, or when it is the funding Account the source will top up. */
const usableLane = (g: GraphScope, from: string, to: string, capacity: bigint): boolean =>
  capacity > 0n || (g.funding?.sourceEntityId === from && g.funding.accountId === to);
const laneEdge = (
  g: GraphScope, from: string, to: string, fee: Readonly<{ base: bigint; ppm: unknown }>, send: bigint, receive: bigint,
): AccountEdge => ({
  from, to, tokenId: g.tokenId, capacity: send, baseFee: floor0(fee.base),
  feePPM: directionalFeePpm(sanitizeFeePpm(fee.ppm, 1), send, receive), disabled: false,
});
/** One advertised row: our lane always, the mirror lane unless its own side was already advertised. */
const sightAccount = (g: GraphScope, profile: GraphProfile) => (w: ProfileWalk, account: GraphAccount): ProfileWalk => {
  const from = profile.entityId;
  const to = account.counterpartyId;
  const peer = g.profiles.get(to);
  if (!g.nodes.has(to) || (peer !== undefined && unroutableHub(peer))) return w;
  const cap = account.caps(g.tokenId);
  if (cap === null) return w;
  const ownFee = { base: profile.baseFee, ppm: profile.routingFeePPM };
  const own = usableLane(g, from, to, cap.outCapacity)
    ? [laneEdge(g, from, to, ownFee, cap.outCapacity, cap.inCapacity)]
    : [];
  const owns = [...w.owns, ...own];
  const ours = { outbound: cap.outCapacity, inbound: cap.inCapacity };
  const capacities = mapSet(w.capacities, graphLaneKey(from, to, g.tokenId), ours);
  const mirrorKey = graphLaneKey(to, from, g.tokenId);
  if (capacities.has(mirrorKey)) return { ...w, capacities, owns };
  const peerFee = { base: peer?.baseFee ?? 0n, ppm: peer?.routingFeePPM ?? 1 };
  const mirror = usableLane(g, to, from, cap.inCapacity)
    ? [laneEdge(g, to, from, peerFee, cap.inCapacity, cap.outCapacity)]
    : [];
  return {
    capacities: mapSet(capacities, mirrorKey, { outbound: cap.inCapacity, inbound: cap.outCapacity }),
    mirrors: [...w.mirrors, ...mirror], owns,
  };
};
type GraphWalk = Readonly<{ capacities: ReadonlyMap<string, LaneBalance>; edges: readonly AccountEdge[] }>;
const sightProfile = (g: GraphScope) => (walk: GraphWalk, profile: GraphProfile): GraphWalk => {
  if (unroutableHub(profile)) return walk;
  const start: ProfileWalk = { capacities: walk.capacities, mirrors: [], owns: [] };
  const seen = profile.accounts.reduce(sightAccount(g, profile), start);
  return { capacities: seen.capacities, edges: [...walk.edges, ...seen.mirrors, ...seen.owns] };
};
/** og buildNetworkGraph: a later Profile for an entity replaces the earlier one in its place (og Map.set). */
export const buildNetworkGraph = (profileRows: readonly Binary[], tokenId: number, funding?: Funding): NetworkGraph => {
  const parsed = profileRows.flatMap((b) => graphProfileOf(b) ?? []);
  const profiles = new Map(parsed.map((p) => [p.entityId, p] as const));
  const nodes = new Set(profiles.keys());
  const g: GraphScope = { profiles, nodes, tokenId, funding };
  const walked = [...profiles.values()].reduce(sightProfile(g), { capacities: new Map(), edges: [] });
  return { nodes, edges: Map.groupBy(walked.edges, (e) => e.from), accountCapacities: walked.capacities };
};
const graphEdge = (g: NetworkGraph, from: string, to: string, tokenId: number): AccountEdge | undefined =>
  (g.edges.get(from) ?? []).find((e) => e.to === to && e.tokenId === tokenId);
const pathEdges = (g: NetworkGraph, path: readonly string[], tokenId: number): readonly AccountEdge[] | null => {
  const edges = path.slice(1).map((to, i) => graphEdge(g, path[i] ?? "", to, tokenId));
  const usable = (e: AccountEdge | undefined): e is AccountEdge =>
    e !== undefined && Number.isInteger(e.feePPM) && e.feePPM >= 0 && e.feePPM < 1_000_000;
  return edges.every(usable) ? edges : null;
};
/** og PathFinder.buildRoute: quoteHtlcPaymentRoute's fee inversion over graph edges; the sender pays itself no fee. */
const buildPaymentRoute = (
  g: NetworkGraph, path: readonly string[], amount: bigint, tokenId: number,
): Result<PaymentRoute | null, OnionError> => {
  const edges = path.length < 2 ? null : pathEdges(g, path, tokenId);
  if (edges === null) return ok(null);
  // What each edge carries: the amount on the last, and on each earlier one what the next forwarder needs.
  const carried = foldResult(edges.slice(1).toReversed(), [amount] as readonly bigint[], (later, e) =>
    map(requiredInbound(later[0] ?? amount, e.feePPM, e.baseFee), (need) => [need, ...later]));
  return map(carried, (amounts) => {
    const at = (i: number): bigint => amounts[i] ?? 0n;
    const feeAt = (i: number): bigint => (i === 0 ? 0n : at(i - 1) - at(i));
    const hops = edges.map((e, i) => ({ from: e.from, to: e.to, fee: feeAt(i), feePPM: e.feePPM }));
    const likelihood = edges.reduce(
      (p, e, i) => (e.capacity > 0n ? p * Math.exp((-2 * Number(at(i))) / Number(e.capacity)) : p), 1);
    const totalAmount = at(0);
    const probability = Math.max(0.01, Math.min(1, likelihood));
    return { path, hops, totalFee: totalAmount - amount, totalAmount, probability };
  });
};
const MAX_SEARCH_POPS = 4_096;
type SearchEntry = Readonly<{ cost: bigint; node: string; path: readonly string[] }>;
type Search = Readonly<{ queue: readonly SearchEntry[]; routes: readonly PaymentRoute[] }>;
/**
 * og PathFinder.findRoutes: cheapest-first search (stable sort, at most 4096 pops) over loop-free paths. A complete
 * route must fit every lane once each forwarder keeps its fee; the funding Account's first lane is exempt.
 */
export const findPaymentRoutes = (
  g: NetworkGraph, source: string, target: string, amount: bigint, tokenId: number,
  maxRoutes = 100, fundingAccountId?: string,
): Result<readonly PaymentRoute[], OnionError> => {
  if (source === target || !g.nodes.has(source) || !g.nodes.has(target)) return ok([]);
  const funded = fundingAccountId !== undefined && fundingAccountId !== "";
  const fits = (r: PaymentRoute): boolean => {
    const [, remaining] = mapAccum(r.hops, r.totalAmount, (left, hop) => [left - hop.fee, left - hop.fee] as const);
    return r.hops.every((hop, i) => {
      const e = graphEdge(g, hop.from, hop.to, tokenId);
      return e !== undefined && ((i === 0 && funded) || (remaining[i] ?? 0n) <= e.capacity);
    });
  };
  const extend = (current: SearchEntry, e: AccountEdge): Result<SearchEntry | null, OnionError> => {
    const fromFundedSource = current.node === source && funded;
    if (fromFundedSource && e.to !== fundingAccountId) return ok(null);
    if (e.tokenId !== tokenId || e.disabled || current.path.includes(e.to)) return ok(null);
    if (!fromFundedSource && amount > e.capacity) return ok(null);
    const path = [...current.path, e.to];
    return map(buildPaymentRoute(g, path, amount, tokenId), (prefix) =>
      (prefix === null ? null : { cost: prefix.totalFee, node: e.to, path }));
  };
  const pop = (s: Search): Result<Search, OnionError> => {
    const [current, ...rest] = s.queue.toSorted((a, b) => asc(a.cost, b.cost));
    if (current === undefined) return ok(s);
    if (current.node === target) {
      return map(buildPaymentRoute(g, current.path, amount, tokenId), (r) =>
        ({ queue: rest, routes: r !== null && fits(r) ? [...s.routes, r] : s.routes }));
    }
    const next = traverse(g.edges.get(current.node) ?? [], (e) => extend(current, e));
    return map(next, (entries) => ({ queue: [...rest, ...entries.flatMap((x) => x ?? [])], routes: s.routes }));
  };
  const searching = (s: Search): boolean => s.queue.length > 0 && s.routes.length < maxRoutes;
  const start: Search = { queue: [{ cost: 0n, node: source, path: [source] }], routes: [] };
  const pops = Array.from({ length: MAX_SEARCH_POPS }, (_, i) => i);
  const searched = foldResult(pops, start, (s) => (searching(s) ? pop(s) : ok(s)));
  return map(searched, (s) => s.routes.toSorted((a, b) => asc(a.totalFee, b.totalFee)));
};
/** og gossip getNetworkGraph().findPaths(source, target, amount ?? 1n, tokenId = 1, fundingAccountId). */
export const findPaths = (
  profiles: readonly Binary[], source: string, target: string, amount = 1n, tokenId = 1, fundingAccountId?: string,
): Result<readonly PaymentRoute[], OnionError> => {
  const funding = fundingAccountId ? { sourceEntityId: source, accountId: fundingAccountId } : undefined;
  const graph = buildNetworkGraph(profiles, tokenId, funding);
  return findPaymentRoutes(graph, source, target, amount, tokenId, 100, fundingAccountId);
};


// ---- account body: og types/account.ts ----
// Beyond the deltas, both sides commit to HTLC locks, swap offers, cross-J pulls, rebalance requests and fee
// policies, lending replay guards, pending J claims and the settlement workspace. Together that is the body.
/**
 * og types/account.ts SwapOffer (same-jurisdiction): quantized amounts, canonical price and the maker's signed fee
 * authority.
 */
export type SwapOffer = Readonly<{
  offerId: string; giveTokenId: TokenId; giveTokenDecimals: number; giveAmount: bigint;
  wantTokenId: TokenId; wantTokenDecimals: number; wantAmount: bigint;
  maxFee: bigint; minNetReceive: bigint; priceTicks: bigint; timeInForce?: number | undefined;
  makerIsLeft: boolean; createdHeight: number; quantizedGive: bigint; quantizedWant: bigint;
  crossJurisdiction?: CrossRoute | undefined;
}>;
/**
 * og types/account.ts PullCommitment: a cross-j pull's committed row; its |amount| is held on the payer side until
 * cross_pull_close.
 */
export type PullRow = Readonly<{
  pullId: string; tokenId: number; amount: bigint; claimedRatio: number; claimedAmount: bigint;
  fullHash: string; partialRoot: string; crossJurisdiction: CrossPullBinding;
  createdHeight: number; createdTimestamp: number;
}>;
/** og AccountTx swap_offer data (same-jurisdiction). */
export type SwapOfferTerms = Readonly<{
  offerId: string; giveTokenId: TokenId; giveTokenDecimals: number; giveAmount: bigint;
  wantTokenId: TokenId; wantTokenDecimals: number; wantAmount: bigint;
  maxFee: bigint; minNetReceive: bigint; priceTicks?: bigint | undefined; timeInForce?: number | undefined;
  crossJurisdiction?: CrossRoute | undefined;
}>;
/** og AccountTx swap_resolve data. */
export type SwapResolveTerms = Readonly<{
  offerId: string; fillRatio: number; cancelRemainder: boolean;
  fillNumerator?: bigint | undefined; fillDenominator?: bigint | undefined;
  feeTokenId?: TokenId | undefined; feeAmount?: bigint | undefined;
  executionGiveAmount?: bigint | undefined; executionWantAmount?: bigint | undefined;
  restingPriceTicks?: bigint | undefined;
  restingGiveAmount?: bigint | undefined; restingWantAmount?: bigint | undefined;
  restingQuantizedGive?: bigint | undefined; restingQuantizedWant?: bigint | undefined;
  /** og matcher annotations: signed into the frame, never read by the Account transition. */
  comment?: string | undefined; restingGiveTokenId?: TokenId | undefined; restingWantTokenId?: TokenId | undefined;
}>;
/**
 * og types/finance/rebalance.ts: one side's committed fee terms, the bilateral register, and a prepaid request's fee
 * state.
 */
export type RebalanceFeeSnapshot = Readonly<{
  policyVersion: number; baseFee: bigint; liquidityFeeBps: bigint; gasFee: bigint; updatedAt: number;
}>;
export type BilateralFeePolicy = Readonly<{ left?: RebalanceFeeSnapshot; right?: RebalanceFeeSnapshot }>;
/** og types/account.ts AccountLendingIntentKind: the committed replay guard of each Account-level lending intent. */
export type LendingIntentKind =
  | "fund" | "borrow" | "repay" | "credit-grant" | "credit-revoke" | "close-request" | "close-payout";
export type RefundReason = "policy_mismatch" | "timeout" | "fee_too_low" | "manual";
export type RebalanceRequestFeeState = Readonly<{
  requestId: string; feeTokenId: number; feePaidUpfront: bigint; requestedAmount: bigint; policyVersion: number;
  requestedAt: number; requestedByLeft: boolean;
  refund?: Readonly<{ reason: RefundReason; refundedAmount: bigint }>;
}>;
/**
 * og types/finance/account-j-claims.ts AccountJClaimRecord / Node / Proof (root-to-leaf path; empty for the EMPTY
 * root).
 */
export type JClaimRecord = Readonly<{
  version: 1; accountKey: string; side: "left" | "right"; jHeight: number; jBlockHash: string; eventsHash: string;
}>;
export type JClaimNode =
  | Readonly<{ version: 1; type: "leaf"; key: string; record: JClaimRecord }>
  | Readonly<{ version: 1; type: "branch"; bit: number; left: string; right: string }>;
export type JClaimProof = Readonly<{ version: 1; nodes: readonly JClaimNode[] }>;
export type ClaimRow = Readonly<{ onLeft: boolean; jHeight: bigint; jBlockHash: string; eventsHash: string }>;
/**
 * og types/account.ts SettlementOp / SettlementWorkspace, field for field: the workspace (minus Hankos) is committed in
 * the Account root.
 */
export type SettlementOp =
  | Readonly<{ type: "r2c" | "c2r" | "r2r"; tokenId: number; amount: bigint }>
  | Readonly<{ type: "forgive"; tokenId: number }>
  | Readonly<{
    type: "rawDiff"; tokenId: number; leftDiff: bigint; rightDiff: bigint; collateralDiff: bigint; ondeltaDiff: bigint;
  }>;
export type WorkspaceDiff = Readonly<{
  tokenId: number; leftDiff: bigint; rightDiff: bigint; collateralDiff: bigint; ondeltaDiff: bigint;
}>;
export type PostSettlementProof = Readonly<{
  leftHanko?: string | undefined; rightHanko?: string | undefined; disputeHash: string; proofBodyHash: string;
  nonce: number; proposerIsLeft: boolean;
}>;
export type SettlementWorkspace = Readonly<{
  workspaceHash: string; ops: readonly SettlementOp[];
  compiledDiffs?: readonly WorkspaceDiff[] | undefined; compiledForgiveTokenIds?: readonly number[] | undefined;
  leftHanko?: string | undefined; rightHanko?: string | undefined; settlementHash?: string | undefined;
  lastModifiedByLeft: boolean; status: "draft" | "awaiting_counterparty" | "ready_to_submit" | "submitted";
  memo?: string | undefined; revision: number; createdAt: number; lastUpdatedAt: number;
  executorIsLeft: boolean; nonceAtSign?: number | undefined;
  postSettlementDisputeProof?: PostSettlementProof | undefined;
}>;
export type AccountBody = Readonly<{
  account: AccountState; terms: AccountTerms;
  locks: ReadonlyMap<string, HtlcLock>; offers: ReadonlyMap<string, SwapOffer>;
  requested: ReadonlyMap<TokenId, bigint>; requestFees: ReadonlyMap<TokenId, RebalanceRequestFeeState>;
  feePolicies: ReadonlyMap<TokenId, BilateralFeePolicy>; lendingIntents: ReadonlyMap<string, LendingIntentKind>;
  claimRows?: readonly ClaimRow[] | undefined; finalizedJHeight: bigint; jNonce: number;
  settlement?: SettlementWorkspace | undefined; pulls?: ReadonlyMap<string, PullRow> | undefined;
  /**
   * og replica shadow.rebalance.submittedAtByToken: local J-batch submission marker per token; outside the Account
   * state root, committed in the Entity's account leaf.
   */
  submittedAt?: ReadonlyMap<number, number> | undefined;
}>;
/** og envelope/entity-update.ts setRebalanceSubmittedAt: set, or (undefined) clear, one token's submission marker. */
export const setRebalanceSubmittedAt = (
  a: AccountBody, tokenId: number, submittedAt: number | undefined,
): AccountBody => {
  const markers = a.submittedAt ?? new Map<number, number>();
  const next = submittedAt === undefined ? mapDelete(markers, tokenId) : mapSet(markers, tokenId, submittedAt);
  return { ...a, submittedAt: next };
};
const clearSubmittedAt = (a: AccountBody, tk: TokenId): AccountBody =>
  (a.submittedAt?.has(Number(tk)) ? { ...a, submittedAt: mapDelete(a.submittedAt, Number(tk)) } : a);
/** og state-root.ts submittedAtByTokenRoot: the shadow map's root, as the Entity account leaf commits it. */
export const submittedAtRoot = (a: AccountBody): Result<string, CommitmentError> =>
  mapRoot(a.submittedAt ?? new Map<number, number>());
export type AccountStep<E extends Effect = Effect> = Step<AccountBody, E>;
type BodyStep<E extends Effect = never> = Result<AccountStep<E>, BodyError>;

// ---- account transactions: og types/account.ts AccountTx ----
type PaymentTx = Readonly<{
  type: "payment"; tokenId: TokenId; amount: bigint; route?: readonly string[] | undefined;
  description?: string | undefined; fromEntityId?: string | undefined; toEntityId?: string | undefined;
  deliveryMode?: "direct" | "trusted" | undefined; trustedGatewayEntityId?: string | undefined;
}>;
type HtlcLockTx = Readonly<{
  type: "htlc_lock"; lockId: string; hashlock: string; timelock: bigint; revealBeforeHeight: bigint;
  amount: bigint; tokenId: TokenId; deliveryMode?: "instant" | "async" | undefined; envelope?: HtlcEnvelope | undefined;
}>;
type LendingTx =
  | Readonly<{
    type: "lending_fund"; positionId: string; hubEntityId: string; lenderEntityId: string; tokenId: TokenId;
    amount: bigint; termId: string; interestBps: number;
  }>
  | Readonly<{
    type: "lending_borrow_request"; requestId: string; hubEntityId: string; borrowerEntityId: string;
    tokenId: TokenId; amount: bigint; termId: string; maxInterestBps: number;
  }>
  | Readonly<{
    type: "lending_repay"; loanId: string; hubEntityId: string; borrowerEntityId: string;
    tokenId: TokenId; amount: bigint;
  }>
  | Readonly<{
    type: "lending_credit"; action: "grant" | "revoke"; loanId: string; hubEntityId: string;
    borrowerEntityId: string; tokenId: TokenId; creditLimit: bigint;
  }>
  | Readonly<{ type: "lending_close_request"; positionId: string; hubEntityId: string; lenderEntityId: string }>
  | Readonly<{
    type: "lending_close_payout"; positionId: string; hubEntityId: string; lenderEntityId: string;
    tokenId: TokenId; amount: bigint;
  }>;
type RebalanceTx =
  | Readonly<{
    type: "request_collateral"; tokenId: TokenId; amount: bigint; feeTokenId?: TokenId | undefined;
    feeAmount: bigint; policyVersion: number;
  }>
  | Readonly<{
    type: "rebalance_refund"; requestId: string; requestTokenId: TokenId; amount: bigint; reason: RefundReason;
  }>
  | Readonly<{
    type: "rebalance_policy"; tokenId: TokenId; policyVersion: number; baseFee: bigint;
    liquidityFeeBps: bigint; gasFee: bigint;
  }>;
type CrossPullTx =
  | Readonly<{
    type: "cross_pull_lock"; pullId: string; tokenId: TokenId; amount: bigint; fullHash: string; partialRoot: string;
    crossJurisdiction: CrossPullBinding; crossJurisdictionRoute: CrossRoute;
  }>
  | Readonly<{ type: "cross_pull_close"; pullId: string; binary: string; proof: CrossCloseProof }>;
type JEventClaimTx = Readonly<{
  type: "j_event_claim"; jHeight: bigint; jBlockHash: Hash; events: readonly AccountSettlement[]; observedAt: bigint;
  leftProof?: JClaimProof | undefined; rightProof?: JClaimProof | undefined;
}>;
type PostProofClaim = Readonly<{
  nonce: number; proposerIsLeft: boolean; proofBodyHash: string; disputeHash: string; hanko?: string | undefined;
}>;
type SettleTransitionTx =
  | Readonly<{
    type: "settle_transition"; kind: "upsert"; revision: number; previousWorkspaceHash?: string | undefined;
    ops: readonly SettlementOp[]; executorIsLeft: boolean; memo?: string | undefined;
  }>
  | Readonly<{ type: "settle_transition"; kind: "submit" | "clear"; revision: number; workspaceHash: string }>
  | Readonly<{
    type: "settle_transition"; kind: "hanko"; revision: number; workspaceHash: string; settlementNonce: number;
    settlementHash: string; settlementHanko?: string | undefined; postProof: PostProofClaim;
  }>;
export type AccountTx =
  | Readonly<{ type: "add_delta"; tokenId: TokenId }>
  | Readonly<{ type: "set_credit_limit"; tokenId: TokenId; limit: bigint }>
  | PaymentTx
  | HtlcLockTx
  | Readonly<{ type: "htlc_resolve"; lockId: string; outcome: "secret"; secret: string }>
  | Readonly<{ type: "htlc_resolve"; lockId: string; outcome: "error"; reason?: string | undefined }>
  | (Readonly<{ type: "swap_offer" }> & SwapOfferTerms)
  | Readonly<{ type: "swap_cancel_request"; offerId: string }>
  | (Readonly<{ type: "swap_resolve" }> & SwapResolveTerms)
  | RebalanceTx
  | LendingTx
  | CrossPullTx
  | JEventClaimTx
  | SettleTransitionTx;
export type TxOf<K extends AccountTx["type"]> = Extract<AccountTx, { readonly type: K }>;
export type WireAccountTx = AccountTx;
type WireTxOf<K extends AccountTx["type"]> = Extract<WireAccountTx, { readonly type: K }>;
const OG_TOKEN_FIELDS = [
  "tokenId", "giveTokenId", "wantTokenId", "feeTokenId", "requestTokenId", "restingGiveTokenId", "restingWantTokenId",
] as const;
/**
 * og wire AccountTx fields: token ids (and htlc revealBeforeHeight) are JS numbers, never the rewrite's decimal
 * strings.
 */
export const wireOf = (tx: WireAccountTx): { readonly type: string } => {
  const fields: Record<string, unknown> = { ...tx };
  const numericTokens = OG_TOKEN_FIELDS.flatMap((k) =>
    (typeof fields[k] === "string" ? [[k, Number(fields[k])] as const] : []));
  const height = tx.type === "htlc_lock" ? { revealBeforeHeight: Number(tx.revealBeforeHeight) } : {};
  return { ...fields, ...Object.fromEntries(numericTokens), ...height } as { readonly type: string };
};
/** Per tx kind: whether it may ride in a level-0 frame, whether it may repeat, and which effects it can emit. */
export type KindRow = Readonly<{ l0: boolean; repeatable: boolean; effects: readonly Effect["_tag"][] }>;
const kind = <const T extends readonly Effect["_tag"][]>(l0: boolean, repeatable: boolean, ...effects: T) =>
  ({ l0, repeatable, effects });
export const AccountKinds = {
  add_delta: kind(true, false),
  set_credit_limit: kind(true, false),
  payment: kind(true, true, "direct_payment_forward"),
  htlc_lock: kind(false, false),
  htlc_resolve: kind(false, false, "forward_secret", "htlc_error"),
  swap_offer: kind(false, false, "swap_offer_upsert"),
  swap_cancel_request: kind(false, false, "swap_cancel_requested"),
  swap_resolve: kind(false, false, "swap_cancelled", "swap_offer_upsert"),
  settle_transition: kind(false, false),
  request_collateral: kind(false, false, "request_collateral_committed"),
  rebalance_refund: kind(false, false),
  rebalance_policy: kind(false, false),
  lending_fund: kind(false, false),
  lending_borrow_request: kind(false, false),
  lending_repay: kind(false, false),
  lending_credit: kind(false, false),
  lending_close_request: kind(false, false),
  lending_close_payout: kind(false, false),
  cross_pull_lock: kind(false, false),
  cross_pull_close: kind(false, false),
  j_event_claim: kind(false, false, "account_settled_finalized_bilateral"),
} as const satisfies Kinds<AccountTx["type"], KindRow>;
export type L0Tx = TxOf<"add_delta" | "set_credit_limit" | "payment">;
export type EffectOf<K extends AccountTx["type"]> = Of<Effect, (typeof AccountKinds)[K]["effects"][number]>;
export const isL0Tx = (tx: WireAccountTx): tx is L0Tx => arm(AccountKinds, tx.type).l0;
export const genesisAccountBody = (account: AccountState, terms: AccountTerms): AccountBody => ({
  account, terms, locks: new Map(), offers: new Map(), requested: new Map(), requestFees: new Map(),
  feePolicies: new Map(), lendingIntents: new Map(), finalizedJHeight: 0n, jNonce: 0,
});

// ---- spending room ----
const putState = (a: AccountBody, account: AccountState): AccountBody => ({ ...a, account });
/** Everything a side has held back on a token: its lock holds plus its allowances. */
export const holds = (a: AccountBody, tk: TokenId, byLeft: boolean): bigint => {
  const s = sideTotals(a, tk);
  return at(s.leftHold + s.leftAllowance, s.rightHold + s.rightAllowance, byLeft);
};
const ensureRoom = (a: AccountBody, tk: TokenId, amount: bigint, byLeft: boolean): Result<void, BodyError> => {
  const available = outCapacity(getDelta(a.account, tk), byLeft, holds(a, tk, byLeft));
  return amount > available ? err({ _tag: "insufficient_capacity", available, requested: amount }) : ok(undefined);
};
/** A side's total hold on one token stays within one payment (og's uint256 hold). */
const holdRoom = (a: AccountBody, tk: TokenId, amount: bigint, onLeft: boolean): Result<void, BodyError> => {
  const totals = sideTotals(a, tk);
  const held = at(totals.leftHold, totals.rightHold, onLeft);
  return held + amount > MAX_PAYMENT_AMOUNT ? err({ _tag: "hold_overflow" }) : ok(undefined);
};
const positive = (amount: bigint): Result<void, BodyError> => guard(amount > 0n, { _tag: "non_positive_payment" });
const spend = (a: AccountBody, tk: TokenId, amount: bigint, byLeft: boolean): Result<AccountBody, BodyError> =>
  chain(positive(amount), () => chain(offdeltaChange(byLeft, amount), (by) => {
    const moved = move(getDelta(a.account, tk), by, holds(a, tk, byLeft));
    return chain(moved, (d) => map(representable(a, d), () => putState(a, setDelta(a.account, d))));
  }));
/**
 * og protocol/htlc/utils.ts:73: keccak256 of the 32-byte secret (abi.encode(bytes32)); anything else is not a secret.
 */
export const hashHtlcSecret = (secret: string): string | null =>
  (/^0x[0-9a-fA-F]{64}$/.test(secret) ? keccak256Hex(hexToBytes(secret)) : null);
/** og htlc-deadline.ts: expired once jHeight passes revealBeforeHeight or the timestamp reaches timelock. */
export const htlcExpired = (
  l: Pick<HtlcLock, "timelock" | "revealBeforeHeight">, ctx: Pick<FoldCtx, "nowMs" | "jHeight">,
): boolean => ctx.jHeight > l.revealBeforeHeight || ctx.nowMs >= l.timelock;
export const MAX_ACCOUNT_HTLC_LOCKS = 32;
type LockMove = Readonly<{ senderIsLeft: boolean; amount: bigint }>;
/**
 * og delta-utils.ts getOffdeltaRepresentationError: the first int512 bound offdelta crosses once every live lock on
 * the token moves (the one being removed aside, the one being added included), or null.
 */
const offdeltaOutside = (a: AccountBody, d: Delta, added?: LockMove, removedLockId?: string): bigint | null => {
  const live = [...a.locks].filter(([id, l]) => l.tokenId === d.tokenId && id !== removedLockId).map(([, l]) => l);
  const moves: readonly LockMove[] = added === undefined ? live : [...live, added];
  const sentBy = (left: boolean): bigint =>
    moves.filter((m) => m.senderIsLeft === left).reduce((sum, m) => sum + m.amount, 0n);
  const lowest = d.offdelta - sentBy(true), highest = d.offdelta + sentBy(false);
  return lowest < INT512_MIN ? lowest : highest > INT512_MAX ? highest : null;
};
/** og delta-utils.ts getOffdeltaRepresentationError: offdelta plus every live lock movement stays in int512. */
const representable = (a: AccountBody, d: Delta, added?: LockMove): Result<void, BodyError> =>
  guard(offdeltaOutside(a, d, added) === null, { _tag: "offdelta_range" });
const MISSING: Result<never, BodyError> = err({ _tag: "missing" });

// ---- J claims: og j-claim-transition.ts, j-events/{claim,finality}.ts ----
// Each side claims the AccountSettled events it saw at a J height. A claim waits for the peer's matching claim;
// when both agree, the events are final: each token row takes the chain's collateral and ondelta.
const sameAccount = (row: AccountSettlement, id: AccountId): boolean => row.left === id.left && row.right === id.right;
const CLAIM_UINT64 = (1n << 64n) - 1n;
const claimHeight = (h: bigint): Result<bigint, ClaimError> =>
  (h >= 1n && h <= CLAIM_UINT64 && h <= BigInt(Number.MAX_SAFE_INTEGER) ? ok(h) : err({ _tag: "claim_height" }));
const claimBlock = (h: string): Result<string, ClaimError> =>
  (WORD.test(h) ? ok(h.toLowerCase()) : err({ _tag: "claim_block" }));
type SettledData = Readonly<{
  leftEntity: string; rightEntity: string; tokenId: number; leftReserve: string; rightReserve: string;
  collateral: string; ondelta: string; nonce: number;
}>;
/**
 * og JurisdictionEvent AccountSettled with normalizeMetadata's EVM position (hashes kept as given; the key lowercases
 * them).
 */
type SettledEvent = Readonly<{
  blockNumber?: number; blockHash?: string; transactionHash?: string; logIndex?: number; eventIndex?: number;
  type: "AccountSettled"; data: SettledData;
}>;
const settledPayload = (d: SettledData): string => [
  "AccountSettled", d.leftEntity, d.rightEntity, d.tokenId,
  d.leftReserve, d.rightReserve, d.collateral, d.ondelta, d.nonce,
].join(":");
/** og event-normalization.ts canonicalJurisdictionEventKey. */
const settledKey = (e: SettledEvent): string => JSON.stringify([
  e.blockNumber ?? null, e.blockHash?.toLowerCase() ?? null, e.transactionHash?.toLowerCase() ?? null,
  e.logIndex ?? null, e.eventIndex ?? null, settledPayload(e.data),
]);
/** Present positions order by value; a present one sorts before an absent one. */
const optIndex = (x: number | undefined, y: number | undefined): number => {
  switch (true) {
    case x !== undefined && y !== undefined: return x - y;
    case x !== undefined: return -1;
    case y !== undefined: return 1;
    default: return 0;
  }
};
/** og compareCanonicalJurisdictionEvents: EVM execution order first; the payload only orders synthetic ties. */
const compareSettled = (x: SettledEvent, y: SettledEvent): number =>
  optIndex(x.blockNumber, y.blockNumber) || optIndex(x.logIndex, y.logIndex) || optIndex(x.eventIndex, y.eventIndex)
  || asc(x.transactionHash?.toLowerCase() ?? "", y.transactionHash?.toLowerCase() ?? "")
  || asc(settledPayload(x.data), settledPayload(y.data));
const isPosition = (n: number | undefined): n is number => n !== undefined && Number.isInteger(n) && n >= 0;
const nonBlank = (s: string | undefined): s is string => typeof s === "string" && s.trim() !== "";
/** og normalizeMetadata: integer block/log/event positions (log/event non-negative) and non-blank hashes. */
const settledMeta = (
  m: JEventMeta | undefined, eventIndex: number | undefined,
): Omit<SettledEvent, "type" | "data"> => ({
  ...opt("blockNumber", m?.blockNumber !== undefined && Number.isInteger(m.blockNumber) ? m.blockNumber : undefined),
  ...opt("blockHash", nonBlank(m?.blockHash) ? m?.blockHash : undefined),
  ...opt("transactionHash", nonBlank(m?.transactionHash) ? m?.transactionHash : undefined),
  ...opt("logIndex", isPosition(m?.logIndex) ? m?.logIndex : undefined),
  ...opt("eventIndex", isPosition(eventIndex) ? eventIndex : undefined),
});
const settledEvent = (row: AccountSettlement, token: TokenSettlement): Result<SettledEvent, ClaimError> => {
  if (row.nonce < 0n || row.nonce > BigInt(Number.MAX_SAFE_INTEGER)) return err({ _tag: "claim_events" });
  const data = {
    leftEntity: row.left.toLowerCase(), rightEntity: row.right.toLowerCase(), tokenId: Number(token.tokenId),
    leftReserve: token.leftReserve.toString(), rightReserve: token.rightReserve.toString(),
    collateral: token.collateral.toString(), ondelta: token.ondelta.toString(), nonce: Number(row.nonce),
  };
  return ok({ ...settledMeta(row.meta, token.eventIndex), type: "AccountSettled", data });
};
type ClaimEvidence = Readonly<{ eventsHash: string; events: readonly SettledEvent[] }>;
/** og canonicalEvents + canonicalJurisdictionEventsHash: sorted, duplicate-free, keccak of the key list. */
const claimEvidence = (events: readonly AccountSettlement[]): Result<ClaimEvidence, ClaimError> => {
  const rows = events.flatMap((row) => row.tokens.map((token) => ({ row, token })));
  if (rows.length === 0) return err({ _tag: "claim_events" });
  return chain(traverse(rows, ({ row, token }) => settledEvent(row, token)), (built) => {
    const sorted = built.toSorted(compareSettled);
    const keys = sorted.map(settledKey);
    if (new Set(keys).size !== keys.length) return err({ _tag: "claim_events" });
    return ok({ eventsHash: keccak256Hex(utf8(JSON.stringify(keys))), events: sorted });
  });
};
const claimParts = (tx: TxOf<"j_event_claim">) =>
  all({ jHeight: claimHeight(tx.jHeight), jBlockHash: claimBlock(tx.jBlockHash), evidence: claimEvidence(tx.events) });
type ClaimFrame = Readonly<{
  version: "xln:account-j-event-claim-frame:v1"; jHeight: number; jBlockHash: string; eventsHash: string;
  events: readonly SettledEvent[];
}>;
const claimFrame = (tx: TxOf<"j_event_claim">): Result<ClaimFrame, ClaimError> =>
  map(claimParts(tx), ({ jHeight, jBlockHash, evidence }) => ({
    version: "xln:account-j-event-claim-frame:v1", jHeight: Number(jHeight), jBlockHash,
    eventsHash: evidence.eventsHash, events: evidence.events,
  }));
const claimRowOf = (tx: TxOf<"j_event_claim">, onLeft: boolean): Result<ClaimRow, ClaimError> =>
  map(claimParts(tx), ({ jHeight, jBlockHash, evidence }) =>
    ({ onLeft, jHeight, jBlockHash, eventsHash: evidence.eventsHash }));
/**
 * og types/account.ts HtlcLock as committed: numeric token/height/timestamp fields, and envelopeHash only when the lock
 * carried an envelope.
 */
const ogLockRow = (l: HtlcLock): Record<string, unknown> => {
  const { envelopeHash, ...rest } = l;
  return {
    ...rest, revealBeforeHeight: Number(l.revealBeforeHeight), tokenId: Number(l.tokenId),
    createdHeight: Number(l.createdHeight), createdTimestamp: Number(l.createdTimestamp),
    ...opt("envelopeHash", envelopeHash),
  };
};
const sameEvidence = (x: ClaimRow, y: ClaimRow): boolean =>
  x.jBlockHash === y.jBlockHash && x.eventsHash === y.eventsHash;
const pruneThrough = (rows: readonly ClaimRow[], height: bigint): readonly ClaimRow[] | undefined => {
  const kept = rows.filter((r) => r.jHeight > height);
  return kept.length === 0 ? undefined : kept;
};
/** og finality.ts: every AccountSettled names this pair and nonces never regress; the last one becomes jNonce. */
const settledNonce = (a: AccountBody, events: readonly SettledEvent[]): Result<number, BodyError> => {
  const left = a.account.id.left.toLowerCase();
  const right = a.account.id.right.toLowerCase();
  return foldResult(events, a.jNonce, (prev, e): Result<number, BodyError> => {
    switch (true) {
      case e.data.leftEntity !== left || e.data.rightEntity !== right: return err({ _tag: "settled_pair" });
      case e.data.nonce < prev: return err({ _tag: "settled_nonce" });
      default: return ok(e.data.nonce);
    }
  });
};
const dropRequest = (b: AccountBody, tk: TokenId): AccountBody =>
  ({ ...b, requested: mapDelete(b.requested, tk), requestFees: mapDelete(b.requestFees, tk) });
/**
 * og finality.ts: collateral arriving against a pending request pays it down (or clears it and its fee state) and
 * clears the submission marker.
 */
const creditRequest = (b: AccountBody, tk: TokenId, increase: bigint): AccountBody => {
  const requested = b.requested.get(tk) ?? 0n;
  if (requested <= 0n || increase <= 0n) return b;
  const paidDown = requested > increase
    ? { ...b, requested: mapSet(b.requested, tk, requested - increase) }
    : dropRequest(b, tk);
  return clearSubmittedAt(paidDown, tk);
};
type AccountFold = Result<AccountBody, BodyError>;
/** One token row takes the chain's collateral and ondelta. */
const applySettledRow = (b: AccountBody, e: SettledEvent): Result<AccountBody, BodyError> =>
  chain(mapErr(tokenId(String(e.data.tokenId)), (): BodyError => ({ _tag: "index" })), (tk): AccountFold => {
    const fresh = !b.account.deltas.has(tk);
    if (fresh && e.data.tokenId === 0) return err({ _tag: "index" });
    if (fresh && b.account.deltas.size + 1 > MAX_ROWS) return err({ _tag: "too_many_rows" });
    const was = getDelta(b.account, tk);
    const now = settle(was, BigInt(e.data.collateral), BigInt(e.data.ondelta));
    return ok(creditRequest(putState(b, setDelta(b.account, now)), tk, floor0(now.collateral - was.collateral)));
  });
const finalizeSettled = (
  a: AccountBody, events: readonly SettledEvent[], dt?: DeltaTransformerRef,
): Result<AccountBody, BodyError> =>
  chain(settledNonce(a, events), (jNonce) =>
    chain(foldResult(events, a, applySettledRow), (b) =>
      map(activateWorkspace(b, jNonce, dt), (c) => ({ ...c, jNonce }))));
type ClaimCursor = Readonly<{ claimRows?: readonly ClaimRow[] | undefined; finalizedJHeight: bigint }>;
type ClaimStep = Readonly<{ claimRows: readonly ClaimRow[] | undefined; finalizedJHeight: bigint; finalizes: boolean }>;
/**
 * og applyAccountJClaimTransition on the pending rows: a conflict on either side refuses; a stale claim prunes;
 * the first side waits; the peer's matching record at any pending height finalizes.
 */
const claimStep = (c: ClaimCursor, own: ClaimRow): Result<ClaimStep, ClaimError> => {
  const held = c.claimRows ?? [];
  const peer: ClaimRow = { ...own, onLeft: !own.onLeft };
  const member = (
    r: ClaimRow,
  ): ClaimRow | undefined => held.find((h) => h.onLeft === r.onLeft && h.jHeight === r.jHeight);
  const ownHeld = member(own);
  const peerHeld = member(peer);
  const conflicts = (ownHeld !== undefined && !sameEvidence(ownHeld, own))
    || (peerHeld !== undefined && !sameEvidence(peerHeld, peer));
  if (conflicts) return err({ _tag: "claim_conflict" });
  const finalizedJHeight = c.finalizedJHeight;
  switch (true) {
    case own.jHeight <= finalizedJHeight:
      return ok({ claimRows: pruneThrough(held, finalizedJHeight), finalizedJHeight, finalizes: false });
    case peerHeld === undefined: {
      const claimRows = ownHeld === undefined ? [...held, own] : c.claimRows;
      return ok({ claimRows, finalizedJHeight, finalizes: false });
    }
    default:
      return ok({ claimRows: pruneThrough(held, own.jHeight), finalizedJHeight: own.jHeight, finalizes: true });
  }
};
type SettledFinal = Of<Effect, "account_settled_finalized_bilateral">;
/** og claim.ts: the first AccountSettled token (else 1) and its row after finality. */
const settledFinal = (b: AccountBody, events: readonly SettledEvent[], jHeight: bigint): SettledFinal => {
  const firstToken = Number(events.find((e) => e.type === "AccountSettled")?.data.tokenId ?? 1);
  const row = [...b.account.deltas].find(([k]) => Number(k) === firstToken)?.[1];
  return {
    _tag: "account_settled_finalized_bilateral", tokenId: firstToken, jHeight: Number(jHeight),
    collateral: row?.collateral ?? 0n, ondelta: row?.ondelta ?? 0n,
  };
};
const claimJ = (a: AccountBody, tx: TxOf<"j_event_claim">, ctx: FoldCtx): BodyStep<SettledFinal> =>
  chain(claimRowOf(tx, ctx.byLeft), (own) => chain(claimStep(a, own), (s): BodyStep<SettledFinal> => {
    if (!s.finalizes) {
      return ok(step<AccountBody, SettledFinal>(s.claimRows === a.claimRows ? a : { ...a, claimRows: s.claimRows }));
    }
    const dt = ctx.settlement?.deltaTransformer;
    return chain(claimEvidence(tx.events), ({ events }) => map(finalizeSettled(a, events, dt), (b) =>
      step<AccountBody, SettledFinal>(
        { ...b, claimRows: s.claimRows, finalizedJHeight: s.finalizedJHeight },
        [settledFinal(b, events, tx.jHeight)],
      )));
  }));

// ---- settlement workspace: og protocol/settlement/operations.ts, account/settlement/transition.ts ----
// Both sides draft one cooperative settlement: a list of ops compiled into per-token diffs. Once either side signs,
// the Account freezes until the settlement lands. Signing binds the settlement hash at a nonce and the dispute proof
// at the next nonce, so if the settlement never lands the post-settlement state can still be disputed.
const settleErr = (reason: string): Result<never, BodyError> => err({ _tag: "settlement", reason });
const settlementToken = (t: unknown): t is number =>
  typeof t === "number" && Number.isSafeInteger(t) && t >= 0 && t <= 65_535;
const MAX_SETTLEMENT_DIFFS = 32;
type DiffParts = Omit<WorkspaceDiff, "tokenId">;
type AmountOp = Extract<SettlementOp, { readonly amount: bigint }>;
const noDiff: DiffParts = { leftDiff: 0n, rightDiff: 0n, collateralDiff: 0n, ondeltaDiff: 0n };
/**
 * What one amount op moves, seen from the proposer: r2c moves its reserve into collateral, c2r back out, r2r pays
 * the other side. Only the left side's collateral moves also shift ondelta.
 */
const amountOpDiff = (op: AmountOp, proposerIsLeft: boolean): DiffParts => {
  const n = op.amount;
  switch (op.type) {
    case "r2c": return proposerIsLeft
      ? { ...noDiff, leftDiff: -n, collateralDiff: n, ondeltaDiff: n }
      : { ...noDiff, rightDiff: -n, collateralDiff: n };
    case "c2r": return proposerIsLeft
      ? { ...noDiff, leftDiff: n, collateralDiff: -n, ondeltaDiff: -n }
      : { ...noDiff, rightDiff: n, collateralDiff: -n };
    case "r2r": return proposerIsLeft
      ? { ...noDiff, leftDiff: -n, rightDiff: n }
      : { ...noDiff, leftDiff: n, rightDiff: -n };
  }
};
const addDiff = (d: WorkspaceDiff, x: DiffParts): WorkspaceDiff => ({
  tokenId: d.tokenId, leftDiff: d.leftDiff + x.leftDiff, rightDiff: d.rightDiff + x.rightDiff,
  collateralDiff: d.collateralDiff + x.collateralDiff, ondeltaDiff: d.ondeltaDiff + x.ondeltaDiff,
});
type Compiled = Readonly<{ diffs: readonly WorkspaceDiff[]; forgive: readonly number[] }>;
type Compiling = Readonly<{ diffs: ReadonlyMap<number, WorkspaceDiff>; forgive: readonly number[] }>;
const compileOp = (proposerIsLeft: boolean) => (c: Compiling, op: SettlementOp): Result<Compiling, BodyError> => {
  if (!settlementToken(op.tokenId)) return settleErr("SETTLEMENT_TOKEN_INVALID");
  const merged = (x: DiffParts): Compiling => {
    const d = c.diffs.get(op.tokenId) ?? { tokenId: op.tokenId, ...noDiff };
    return { ...c, diffs: mapSet(c.diffs, op.tokenId, addDiff(d, x)) };
  };
  switch (op.type) {
    case "forgive":
      if (c.forgive.includes(op.tokenId)) return settleErr("SETTLEMENT_DUPLICATE_FORGIVENESS_TOKEN");
      return ok({ ...c, forgive: [...c.forgive, op.tokenId] });
    case "rawDiff": return ok(merged(op));
    case "r2c": case "c2r": case "r2r": return ok(merged(amountOpDiff(op, proposerIsLeft)));
    default: return settleErr("SETTLEMENT_UNKNOWN_OP_TYPE");
  }
};
/** og compileOps: ops merge per token in first-seen order; every diff conserves value and fits SignedAmount. */
export const compileOps = (ops: readonly SettlementOp[], proposerIsLeft: boolean): Result<Compiled, BodyError> => {
  const start: Compiling = { diffs: new Map(), forgive: [] };
  return chain(foldResult(ops, start, compileOp(proposerIsLeft)), (c) => {
    const diffs = [...c.diffs.values()];
    const wide = (v: bigint): boolean => v < -MAX_PAYMENT_AMOUNT || v > MAX_PAYMENT_AMOUNT;
    const anyWide = diffs.some((d) =>
      wide(d.leftDiff) || wide(d.rightDiff) || wide(d.collateralDiff) || wide(d.ondeltaDiff));
    if (anyWide) return settleErr("SETTLEMENT_SIGNED_AMOUNT_RANGE");
    if (diffs.some((d) => d.leftDiff + d.rightDiff + d.collateralDiff !== 0n)) {
      return settleErr("SETTLEMENT_INVARIANT_VIOLATION");
    }
    if (diffs.length > MAX_SETTLEMENT_DIFFS) return settleErr("SETTLEMENT_DIFF_LIMIT_EXCEEDED");
    if (c.forgive.length > MAX_SETTLEMENT_DIFFS) return settleErr("SETTLEMENT_FORGIVENESS_LIMIT_EXCEEDED");
    return ok({ diffs, forgive: c.forgive });
  });
};
const opShapeIssue = (op: SettlementOp): string | undefined => {
  if (!settlementToken(op.tokenId)) return "SETTLEMENT_TOKEN_INVALID";
  switch (op.type) {
    case "r2c": case "c2r": case "r2r":
      return typeof op.amount !== "bigint" || op.amount <= 0n ? "SETTLEMENT_WORKSPACE_AMOUNT_INVALID" : undefined;
    case "rawDiff": {
      const parts = [op.leftDiff, op.rightDiff, op.collateralDiff, op.ondeltaDiff];
      return parts.some((v) => typeof v !== "bigint") ? "SETTLEMENT_WORKSPACE_RAW_DIFF_INVALID" : undefined;
    }
    case "forgive": return undefined;
    default: return "SETTLEMENT_WORKSPACE_OP_INVALID";
  }
};
const checkOps = (ops: readonly SettlementOp[]): Result<void, BodyError> => {
  if (!Array.isArray(ops) || ops.length === 0) return settleErr("SETTLEMENT_WORKSPACE_OPS_EMPTY");
  const issue = ops.map(opShapeIssue).find((x) => x !== undefined);
  return issue === undefined ? ok(undefined) : settleErr(issue);
};
const WORKSPACE_LEAF_KEY = keccak256(utf8("xln.settlement.workspace.body"));
type WorkspaceBody = Pick<SettlementWorkspace, "revision" | "ops" | "lastModifiedByLeft" | "executorIsLeft" | "memo">;
/**
 * og createSettlementWorkspaceHash: the one-leaf keccak radix root of the canonical body under
 * `xln.settlement.workspace.body`.
 */
export const workspaceHashOf = (id: AccountId, w: WorkspaceBody): Result<string, BodyError> => {
  const body = encodeCanonicalValue({
    domain: "xln:settlement-workspace:v1", leftEntity: id.left.toLowerCase(), rightEntity: id.right.toLowerCase(),
    revision: w.revision, ops: w.ops, lastModifiedByLeft: w.lastModifiedByLeft, executorIsLeft: w.executorIsLeft,
    memo: w.memo,
  });
  const encoded = mapErr(body, (): BodyError => ({ _tag: "settlement", reason: "SETTLEMENT_WORKSPACE_ENCODING" }));
  return map(encoded, (enc) => keccak256Hex(concat([LEAF, WORKSPACE_LEAF_KEY, enc])));
};
/** og witness-projection.ts: the committed workspace keeps every decision and target, never the Hanko bytes. */
export const workspaceWithoutHankos = (w: SettlementWorkspace | undefined): unknown => {
  if (w === undefined) return undefined;
  const { leftHanko: _l, rightHanko: _r, postSettlementDisputeProof: post, ...rest } = w;
  if (post === undefined) return rest;
  const { leftHanko: _pl, rightHanko: _pr, ...postRest } = post;
  return { ...rest, postSettlementDisputeProof: postRest };
};
const signedWorkspace = (w: SettlementWorkspace): boolean =>
  w.settlementHash !== undefined || w.leftHanko !== undefined || w.rightHanko !== undefined
  || w.postSettlementDisputeProof !== undefined;
const workspaceDiffs = (w: SettlementWorkspace): readonly WorkspaceDiff[] =>
  unwrapOr(map(compileOps(w.ops, w.lastModifiedByLeft), (c) => c.diffs), () => []);
/**
 * og getSignedSettlementWorkspaceTxError: a signed workspace freezes the Account except J claims and settlement
 * hanko/submit.
 */
const settlementFreeze = (a: AccountBody, tx: WireAccountTx): Result<void, BodyError> => {
  const w = a.settlement;
  const finishesSettlement = tx.type === "settle_transition" && (tx.kind === "hanko" || tx.kind === "submit");
  if (w === undefined || !signedWorkspace(w) || tx.type === "j_event_claim" || finishesSettlement) return ok(undefined);
  return err({ _tag: "settlement_frozen" });
};
/**
 * og planWorkspaceHoldAdd: a negative side diff holds on an existing row, checked against capacity unless it funds
 * collateral.
 */
const workspaceRoom = (a: AccountBody, diffs: readonly WorkspaceDiff[]): Result<void, BodyError> =>
  foldResult(diffs, undefined as void, (_, diff): Result<void, BodyError> => {
    if (diff.leftDiff >= 0n && diff.rightDiff >= 0n) return ok(undefined);
    const tk = String(diff.tokenId) as TokenId;
    const d = a.account.deltas.get(tk);
    if (d === undefined) return settleErr("SETTLEMENT_HOLD_DELTA_MISSING");
    const t = sideTotals(a, tk);
    const held = { left: holds(a, tk, true), right: holds(a, tk, false) };
    return chain(chargeSettlement(d, diff, held), (plan) => {
      const overflows = t.leftHold + plan.left > MAX_PAYMENT_AMOUNT || t.rightHold + plan.right > MAX_PAYMENT_AMOUNT;
      return overflows ? settleErr("HOLD_ADD_OVERFLOW") : ok(undefined);
    });
  });
const WORKSPACE_HASH = WORD;
const currentWorkspace = (a: AccountBody, revision: number, hash: string): Result<SettlementWorkspace, BodyError> => {
  if (!Number.isSafeInteger(revision) || revision < 1) return settleErr("SETTLEMENT_WORKSPACE_VERSION_INVALID");
  const w = a.settlement;
  if (w === undefined) return settleErr("SETTLEMENT_WORKSPACE_MISSING");
  if (typeof hash !== "string" || !WORKSPACE_HASH.test(hash)) {
    return settleErr("SETTLEMENT_WORKSPACE_TARGET_HASH_INVALID");
  }
  if (w.revision !== revision) return settleErr("SETTLEMENT_WORKSPACE_VERSION_MISMATCH");
  return sameHex(w.workspaceHash, hash) ? ok(w) : settleErr("SETTLEMENT_WORKSPACE_TARGET_HASH_MISMATCH");
};
type UpsertTx = Extract<SettleTransitionTx, { readonly kind: "upsert" }>;
type HankoTx = Extract<SettleTransitionTx, { readonly kind: "hanko" }>;
/** Revision 1 starts a workspace; each later revision links to the current unsigned one by its hash. */
const workspaceLink = (cur: SettlementWorkspace | undefined, x: UpsertTx): Result<void, BodyError> => {
  const prev = x.previousWorkspaceHash;
  if (x.revision === 1) {
    if (cur !== undefined) return settleErr("SETTLEMENT_WORKSPACE_ALREADY_EXISTS");
    return prev === undefined ? ok(undefined) : settleErr("SETTLEMENT_WORKSPACE_PREVIOUS_HASH_UNEXPECTED");
  }
  if (cur === undefined) return settleErr("SETTLEMENT_WORKSPACE_PREVIOUS_MISSING");
  if (cur.leftHanko !== undefined || cur.rightHanko !== undefined) {
    return settleErr("SETTLEMENT_WORKSPACE_SIGNED_UPDATE_FORBIDDEN");
  }
  if (cur.revision + 1 !== x.revision) return settleErr("SETTLEMENT_WORKSPACE_NON_CONTIGUOUS_VERSION");
  if (prev === undefined || !WORKSPACE_HASH.test(prev)) return settleErr("SETTLEMENT_WORKSPACE_PREVIOUS_HASH_INVALID");
  return sameHex(cur.workspaceHash, prev) ? ok(undefined) : settleErr("SETTLEMENT_WORKSPACE_PREVIOUS_HASH_MISMATCH");
};
const upsertWorkspace = (a: AccountBody, x: UpsertTx, ctx: FoldCtx): BodyStep => {
  if (!Number.isSafeInteger(x.revision) || x.revision < 1) return settleErr("SETTLEMENT_WORKSPACE_VERSION_INVALID");
  return chain(checkOps(x.ops), () => {
    if (typeof x.executorIsLeft !== "boolean") return settleErr("SETTLEMENT_WORKSPACE_EXECUTOR_INVALID");
    return chain(compileOps(x.ops, ctx.byLeft), ({ diffs }) => {
      const cur = a.settlement;
      const now = Number(ctx.nowMs);
      const body = {
        ops: x.ops.map((op) => ({ ...op })), lastModifiedByLeft: ctx.byLeft, status: "awaiting_counterparty" as const,
        memo: x.memo, revision: x.revision, createdAt: cur?.createdAt ?? now, lastUpdatedAt: now,
        executorIsLeft: x.executorIsLeft,
      };
      return chain(workspaceLink(cur, x), () => chain(workspaceHashOf(a.account.id, body), (workspaceHash) =>
        map(workspaceRoom({ ...a, settlement: undefined }, diffs), () =>
          step({ ...a, settlement: { workspaceHash, ...body } }))));
    });
  });
};
type ProjectedRows = ReadonlyMap<number, CommittedDelta>;
const zeroCommittedDelta = (tokenId: number): CommittedDelta => ({
  tokenId, collateral: 0n, ondelta: 0n, offdelta: 0n, leftCreditLimit: 0n, rightCreditLimit: 0n,
  leftAllowance: 0n, rightAllowance: 0n, leftHold: 0n, rightHold: 0n,
});
/** The rows with this token present: a missing one is added as a zero row, within the row limit. */
const withRow = (rows: ProjectedRows, tokenId: number): Result<ProjectedRows, BodyError> => {
  if (rows.has(tokenId)) return ok(rows);
  if (tokenId === 0) return settleErr("TOKEN_ID_INVALID");
  if (rows.size + 1 > MAX_ROWS) return settleErr("ACCOUNT_DELTA_ROW_LIMIT_EXCEEDED");
  return ok(mapSet(rows, tokenId, zeroCommittedDelta(tokenId)));
};
const applyDiffRow = (rows: ProjectedRows, diff: WorkspaceDiff): Result<ProjectedRows, BodyError> =>
  chain(withRow(rows, diff.tokenId), (rows) => {
    const d = rows.get(diff.tokenId) ?? zeroCommittedDelta(diff.tokenId);
    const collateral = d.collateral + diff.collateralDiff;
    const ondelta = d.ondelta + diff.ondeltaDiff;
    if (collateral < 0n || collateral > MAX_PAYMENT_AMOUNT) return settleErr("SETTLEMENT_PROJECTED_COLLATERAL_RANGE");
    if (ondelta < INT512_MIN || ondelta > INT512_MAX) return settleErr("SETTLEMENT_PROJECTED_ONDELTA_RANGE");
    return ok(mapSet(rows, diff.tokenId, { ...d, collateral, ondelta }));
  });
const settlementReason = (reason: string): BodyError => ({ _tag: "settlement", reason });
const proofError = (e: ProofError): BodyError => settlementReason(e._tag === "transformer" ? e.code : e._tag);
/** og settlement-projection.ts: the post-settlement rows the dispute proof at nonce N+1 commits to. */
const projectedProofHash = (
  a: AccountBody, diffs: readonly WorkspaceDiff[], forgive: readonly number[], dt?: DeltaTransformerRef,
): Result<string, BodyError> =>
  chain(mapErr(committedView(a), uncommitted), (view) => {
    const settled = foldResult(diffs, view.deltas as ProjectedRows, applyDiffRow);
    const projected = chain(settled, (rows) => foldResult(forgive, rows, withRow));
    return chain(projected, (deltas) =>
      map(mapErr(accountProofBody({ ...view, deltas }, dt), proofError), proofBodyHash));
  });
const postDisputeHash = (
  a: AccountBody, bodyHash: string, nonce: number, proposerIsLeft: boolean,
): Result<string, BodyError> =>
  chain(mapErr(committedView(a), uncommitted), (view) =>
    mapErr(accountDisputeHash(view, bodyHash, nonce, proposerIsLeft), (e) => settlementReason(e._tag)));
const settlementHashOf = (
  a: AccountBody, diffs: readonly WorkspaceDiff[], forgive: readonly number[], nonce: number,
): Result<string, BodyError> => {
  const { domain } = a.terms;
  const { left, right } = a.account.id;
  if (/^0x0{40}$/i.test(domain.depositoryAddress) || !WORD.test(left) || !WORD.test(right)) {
    return settleErr("SETTLEMENT_HASH_DOMAIN");
  }
  const diffText = (d: WorkspaceDiff) => ({
    tokenId: String(d.tokenId), leftDiff: d.leftDiff.toString(), rightDiff: d.rightDiff.toString(),
    collateralDiff: d.collateralDiff.toString(), ondeltaDiff: d.ondeltaDiff.toString(),
  });
  try {
    return ok(encodeCooperativeUpdateHash({
      messageType: 0, chainId: domain.chainId, contractAddress: domain.depositoryAddress,
      accountKey: joinHex([left, right]), nonce: String(nonce),
      diffs: diffs.map(diffText), forgiveDebtsInTokenIds: forgive.map(String),
    }));
  } catch {
    return settleErr("SETTLEMENT_HASH_ENCODING");
  }
};
const exactHanko = (
  h: string | undefined,
): h is string => typeof h === "string" && h !== "0x" && /^0x(?:[0-9a-fA-F]{2})*$/.test(h);

// ---- signing: og prepare/verify/commitSettlementHanko ----
/** The signing nonce: pinned at first signature, else the account's minimum safe nonce. */
const hankoNonce = (
  a: AccountBody, w: SettlementWorkspace, auth: SettlementCtx, nonce: number,
): Result<void, BodyError> => {
  if (!Number.isSafeInteger(nonce) || nonce < 1) return settleErr("SETTLEMENT_HANKO_NONCE_INVALID");
  const floor = Math.max(a.jNonce + 1, auth.proofNonceFloor);
  if (floor >= Number.MAX_SAFE_INTEGER) return settleErr("SETTLEMENT_NONCE_EXHAUSTED");
  const required = w.nonceAtSign ?? floor;
  if (nonce === required) return ok(undefined);
  const basis = w.nonceAtSign === undefined ? "account" : "workspace";
  const mismatch = { supplied: nonce, required, basis } as const;
  return err({ _tag: "settlement", reason: "SETTLEMENT_HANKO_NONCE_MISMATCH", nonce: mismatch });
};
/** The settlement hash recomputed at the nonce must equal the one signed and any one already pinned. */
const agreedSettlementHash = (
  a: AccountBody, w: SettlementWorkspace, claimed: string, c: Compiled, nonce: number,
): Result<string, BodyError> =>
  chain(settlementHashOf(a, c.diffs, c.forgive, nonce), (settlementHash) => {
    if (typeof claimed !== "string" || !WORKSPACE_HASH.test(claimed)) return settleErr("SETTLEMENT_HANKO_HASH_INVALID");
    if (!sameHex(claimed, settlementHash)) return settleErr("SETTLEMENT_HANKO_HASH_MISMATCH");
    if (w.settlementHash !== undefined && !sameHex(w.settlementHash, settlementHash)) {
      return settleErr("SETTLEMENT_HANKO_PINNED_HASH_MISMATCH");
    }
    return ok(settlementHash);
  });
type PostTargets = Readonly<{ bodyHash: string; disputeHash: string }>;
/** The N+1 dispute proof: nonce, body hash and dispute hash recomputed, each equal to the claim and to any pin. */
const agreedPostProof = (
  a: AccountBody, w: SettlementWorkspace, post: PostProofClaim, nonce: number, c: Compiled,
  dt: DeltaTransformerRef | undefined,
): Result<PostTargets, BodyError> => {
  if (!Number.isSafeInteger(post.nonce) || post.nonce < 1) return settleErr("POST_SETTLEMENT_PROOF_NONCE_INVALID");
  if (post.nonce !== nonce + 1) return settleErr("POST_SETTLEMENT_PROOF_NONCE_MISMATCH");
  return chain(projectedProofHash(a, c.diffs, c.forgive, dt), (bodyHash) => {
    if (!sameHex(post.proofBodyHash, bodyHash)) return settleErr("POST_SETTLEMENT_PROOF_BODY_HASH_MISMATCH");
    return chain(postDisputeHash(a, bodyHash, post.nonce, post.proposerIsLeft), (disputeHash) => {
      if (!sameHex(post.disputeHash, disputeHash)) return settleErr("POST_SETTLEMENT_DISPUTE_HASH_MISMATCH");
      const pin = w.postSettlementDisputeProof;
      const pinAgrees = pin === undefined || (pin.nonce === post.nonce && sameHex(pin.proofBodyHash, bodyHash)
        && sameHex(pin.disputeHash, disputeHash) && pin.proposerIsLeft === post.proposerIsLeft);
      return pinAgrees ? ok({ bodyHash, disputeHash }) : settleErr("POST_SETTLEMENT_PROOF_PIN_MISMATCH");
    });
  });
};
/**
 * og verifySettlementHankoHankos: the board authority resolves before any Hanko is read. The post-settlement proof
 * admits the previous board's grace (Account.sol:1063); the cooperative settlement Hanko is current-board only
 * (Account.sol:894), and only the non-executor gives one. A side may not sign twice with different bytes.
 */
const verifiedHankos = (
  a: AccountBody, w: SettlementWorkspace, x: HankoTx, auth: SettlementCtx, byLeft: boolean,
  hashes: Readonly<{ settlement: string; dispute: string }>,
): Result<void, BodyError> => {
  const source = (byLeft ? a.account.id.left : a.account.id.right) as EntityId;
  const resolved = auth.registeredBoardHash
    ? ok(auth.registeredBoardHash)
    : auth.boardAuthority?.(source) ?? ok(undefined);
  if (!resolved.ok) return settleErr(resolved.error);
  const board = opt("registeredBoardHash", resolved.value);
  const postHanko = x.postProof.hanko;
  if (!exactHanko(postHanko)) return settleErr("POST_SETTLEMENT_PROOF_HANKO_MISSING");
  if (!auth.verify(hashes.dispute, postHanko, source, { ...board, allowPreviousBoard: true })) {
    return settleErr("POST_SETTLEMENT_PROOF_HANKO_INVALID");
  }
  const executor = w.executorIsLeft === byLeft;
  const settlementHanko = x.settlementHanko;
  if (executor && settlementHanko !== undefined) return settleErr("SETTLEMENT_EXECUTOR_HANKO_FORBIDDEN");
  if (!executor && !exactHanko(settlementHanko)) return settleErr("SETTLEMENT_NONEXECUTOR_HANKO_MISSING");
  const settlementValid = settlementHanko === undefined
    || auth.verify(hashes.settlement, settlementHanko, source, { ...board, allowPreviousBoard: false });
  if (!executor && !settlementValid) return settleErr("SETTLEMENT_NONEXECUTOR_HANKO_INVALID");
  const pinnedOwn = byLeft ? w.postSettlementDisputeProof?.leftHanko : w.postSettlementDisputeProof?.rightHanko;
  const ownSettlement = byLeft ? w.leftHanko : w.rightHanko;
  if (pinnedOwn !== undefined && !sameHex(pinnedOwn, postHanko)) return settleErr("POST_SETTLEMENT_PROOF_EQUIVOCATION");
  if (settlementHanko !== undefined && ownSettlement !== undefined && !sameHex(ownSettlement, settlementHanko)) {
    return settleErr("SETTLEMENT_HANKO_EQUIVOCATION");
  }
  return ok(undefined);
};
type SignedTargets = Compiled & PostTargets & Readonly<{ nonce: number; settlementHash: string }>;
/** The workspace with this side's signatures pinned; ready once the non-executor and both proof sides have signed. */
const withSignatures = (
  w: SettlementWorkspace, x: HankoTx, byLeft: boolean, t: SignedTargets, nowMs: bigint,
): SettlementWorkspace => {
  const post = x.postProof;
  const pinned = w.postSettlementDisputeProof;
  const settlementHanko = x.settlementHanko;
  const proof: PostSettlementProof = {
    disputeHash: t.disputeHash, proofBodyHash: t.bodyHash, nonce: post.nonce, proposerIsLeft: post.proposerIsLeft,
    leftHanko: byLeft ? post.hanko : pinned?.leftHanko, rightHanko: byLeft ? pinned?.rightHanko : post.hanko,
  };
  const leftHanko = byLeft && settlementHanko !== undefined ? settlementHanko : w.leftHanko;
  const rightHanko = !byLeft && settlementHanko !== undefined ? settlementHanko : w.rightHanko;
  const nonExecutorSigned = (w.executorIsLeft ? rightHanko : leftHanko) !== undefined;
  const ready = nonExecutorSigned && proof.leftHanko !== undefined && proof.rightHanko !== undefined;
  return {
    ...w, compiledDiffs: t.diffs, compiledForgiveTokenIds: t.forgive, nonceAtSign: t.nonce,
    settlementHash: t.settlementHash, postSettlementDisputeProof: proof, leftHanko, rightHanko,
    status: ready ? "ready_to_submit" : "awaiting_counterparty", lastUpdatedAt: Number(nowMs),
  };
};
/**
 * og prepare/verify/commitSettlementHanko: exact workspace, exact nonce, recomputed targets, both authorities, then one
 * write.
 */
const hankoWorkspace = (a: AccountBody, x: HankoTx, ctx: FoldCtx): BodyStep => {
  const auth = ctx.settlement;
  if (auth === undefined) return settleErr("SETTLEMENT_HANKO_CONTEXT_MISSING");
  return chain(currentWorkspace(a, x.revision, x.workspaceHash), (w) => {
    if (w.status === "submitted") return settleErr("SETTLEMENT_HANKO_SUBMITTED_FORBIDDEN");
    const nonce = x.settlementNonce;
    return chain(hankoNonce(a, w, auth, nonce), () =>
      chain(compileOps(w.ops, w.lastModifiedByLeft), (compiled) =>
        chain(agreedSettlementHash(a, w, x.settlementHash, compiled, nonce), (settlementHash) =>
          chain(agreedPostProof(a, w, x.postProof, nonce, compiled, auth.deltaTransformer), (post) => {
            const hashes = { settlement: settlementHash, dispute: post.disputeHash };
            const targets: SignedTargets = { ...compiled, ...post, nonce, settlementHash };
            return map(verifiedHankos(a, w, x, auth, ctx.byLeft, hashes), () =>
              step({ ...a, settlement: withSignatures(w, x, ctx.byLeft, targets, ctx.nowMs) }));
          }))));
  });
};
type PostProofTarget = Readonly<{ proofBodyHash: string; disputeHash: string; nonce: number; proposerIsLeft: boolean }>;
/**
 * og buildSettlementHankoDraft / prepareSettlementExecution targets: compiled diffs, the settlement hash at `nonce`,
 * and the N+1 proof.
 */
export type SettlementTargets = Compiled & Readonly<{ settlementHash: string; postProof: PostProofTarget }>;
export const settlementTargets = (
  a: AccountBody, w: SettlementWorkspace, nonce: number, proposerIsLeft: boolean, dt?: DeltaTransformerRef,
): Result<SettlementTargets, BodyError> =>
  chain(compileOps(w.ops, w.lastModifiedByLeft), ({ diffs, forgive }) =>
    chain(settlementHashOf(a, diffs, forgive, nonce), (settlementHash) =>
      chain(projectedProofHash(a, diffs, forgive, dt), (proofBodyHash) =>
        map(postDisputeHash(a, proofBodyHash, nonce + 1, proposerIsLeft), (disputeHash) => ({
          diffs, forgive, settlementHash, postProof: { proofBodyHash, disputeHash, nonce: nonce + 1, proposerIsLeft },
        })))));
/**
 * og assertCanonicalSettlementWorkspace: the stored hash is a 32-byte word equal to the recomputed body hash (returned
 * lowercase).
 */
export const canonicalWorkspaceHash = (a: AccountBody, w: SettlementWorkspace): Result<string, BodyError> => {
  if (typeof w.workspaceHash !== "string" || !WORKSPACE_HASH.test(w.workspaceHash)) {
    return settleErr("SETTLEMENT_WORKSPACE_HASH_INVALID");
  }
  return chain(workspaceHashOf(a.account.id, w), (h) => {
    const stored = w.workspaceHash.toLowerCase();
    const computed = h.toLowerCase();
    return stored === computed ? ok(computed) : settleErr(`SETTLEMENT_WORKSPACE_HASH_CORRUPTION:${stored}:${computed}`);
  });
};
/** og getNextSettlementNonce (getMinimumSafeSettlementNonce): the first proof nonce both sides may still sign. */
export const nextSettlementNonce = (r: Pick<AccountReplica, "state" | "dispute">): number =>
  Math.max(r.state.jNonce + 1, proofNonceFloor(r.dispute));
const unsignedWorkspace = (w: SettlementWorkspace): boolean =>
  (w.status === "draft" || w.status === "awaiting_counterparty") && w.compiledDiffs === undefined
  && w.compiledForgiveTokenIds === undefined && !signedWorkspace(w) && w.nonceAtSign === undefined;
/** The executor submits once the counterparty and both proof sides have signed. */
const submitWorkspace = (a: AccountBody, w: SettlementWorkspace, ctx: FoldCtx): BodyStep => {
  if (w.status === "submitted") return settleErr("SETTLEMENT_WORKSPACE_ALREADY_SUBMITTED");
  if (ctx.byLeft !== w.executorIsLeft) return settleErr("SETTLEMENT_SUBMIT_EXECUTOR_MISMATCH");
  if ((ctx.byLeft ? w.rightHanko : w.leftHanko) === undefined) {
    return settleErr("SETTLEMENT_SUBMIT_COUNTERPARTY_HANKO_MISSING");
  }
  const proof = w.postSettlementDisputeProof;
  if (w.status !== "ready_to_submit" || proof?.leftHanko === undefined || proof.rightHanko === undefined) {
    return settleErr("SETTLEMENT_SUBMIT_POST_PROOF_INCOMPLETE");
  }
  return ok(step({ ...a, settlement: { ...w, status: "submitted", lastUpdatedAt: Number(ctx.nowMs) } }));
};
/** Only an unsigned, unsubmitted workspace can be cleared. */
const clearWorkspace = (a: AccountBody, w: SettlementWorkspace): BodyStep => {
  if (w.status === "submitted") return settleErr("SETTLEMENT_CLEAR_SUBMITTED_FORBIDDEN");
  if (!unsignedWorkspace(w)) return settleErr("SETTLEMENT_CLEAR_SIGNED_FORBIDDEN");
  return ok(step({ ...a, settlement: undefined }));
};
const settleTransition = (a: AccountBody, x: TxOf<"settle_transition">, ctx: FoldCtx): BodyStep => {
  switch (x.kind) {
    case "upsert": return upsertWorkspace(a, x, ctx);
    case "hanko": return hankoWorkspace(a, x, ctx);
    case "submit": return chain(currentWorkspace(a, x.revision, x.workspaceHash), (w) => submitWorkspace(a, w, ctx));
    default: return chain(currentWorkspace(a, x.revision, x.workspaceHash), (w) => clearWorkspace(a, w));
  }
};
/**
 * og finality.ts activatePostSettlementProof, body side: an unsigned workspace clears; a signed one clears once its
 * nonce is final, after its N+1 proof checks out.
 */
const activateWorkspace = (
  b: AccountBody, finalizedNonce: number, dt?: DeltaTransformerRef,
): Result<AccountBody, BodyError> => {
  const w = b.settlement;
  if (w === undefined) return ok(b);
  if (!signedWorkspace(w)) return ok({ ...b, settlement: undefined });
  const signed = w.nonceAtSign;
  if (signed === undefined || !Number.isSafeInteger(signed) || signed < 1) {
    return settleErr("SETTLEMENT_SIGNED_NONCE_MISSING");
  }
  if (finalizedNonce < signed) return ok(b);
  if (finalizedNonce > signed) return ok({ ...b, settlement: undefined });
  const p = w.postSettlementDisputeProof;
  if (p === undefined) return settleErr("POST_SETTLEMENT_PROOF_MISSING");
  if (p.nonce !== signed + 1) return settleErr("POST_SETTLEMENT_PROOF_NONCE_MISMATCH");
  if (!p.leftHanko || !p.rightHanko) return settleErr("POST_SETTLEMENT_PROOF_HANKO_MISSING");
  if (p.disputeHash === "" || p.proofBodyHash === "") return settleErr("POST_SETTLEMENT_DISPUTE_HASH_MISSING");
  return chain(projectedProofHash(b, [], [], dt), (h) =>
    (sameHex(h, p.proofBodyHash)
      ? ok({ ...b, settlement: undefined })
      : settleErr("POST_SETTLEMENT_FINALIZED_PROOF_BODY_MISMATCH")));
};
// ---- swaps: og handlers/swap/{offer,resolve}, swap-net-authorization.ts, orderbook/types.ts quantization ----
// A maker rests an offer to give one token for another; the counterparty resolves it with a fill. Same-j offers hold
// the maker's give until they close; cross-j offers settle through their paired pulls and only rest here.
const swapErr = (reason: string): Result<never, BodyError> => err({ _tag: "swap", reason });
const swapIssue = (issue: string | undefined): Result<void, BodyError> =>
  issue === undefined ? ok(undefined) : swapErr(issue);
const PRICE_SCALE = 10_000n;
const noteErr = <T,>(r: Result<T, CrossError>): Result<T, BodyError> => r;
const MAX_ACCOUNT_SWAP_OFFERS = 50, MAX_ACCOUNT_SAME_J_SWAP_OFFERS = 32, MAX_ACCOUNT_CROSS_J_SWAP_OFFERS = 18;
const MAX_SWAP_OFFERS_PER_SIDE_PER_MARKET = 32;
const decimalsOk = (d: number): boolean => Number.isSafeInteger(d) && d >= 0 && d <= 255;

// ---- swap market geometry: pair orientation, lots and price ticks ----
/** og account/utils.ts REFERENCE_STABLE_TOKEN_IDS (USDC, USDT): always the quote of a pair. */
const REFERENCE_STABLES: ReadonlySet<number> = new Set([1, 3]);
/** og getSwapPairOrientation: a reference stable is the quote; between two of a kind the lower id is the base. */
const pairBase = (a: number, b: number): number => {
  const aStable = REFERENCE_STABLES.has(a), bStable = REFERENCE_STABLES.has(b);
  switch (true) {
    case aStable && !bStable: return b;
    case bStable && !aStable: return a;
    default: return Math.min(a, b);
  }
};
/** og deriveSide: 1 sells the base (the give token is the pair's base), 0 buys it. */
export const swapSide = (give: number, want: number): 0 | 1 => (give === pairBase(give, want) ? 1 : 0);
const pow10 = (d: number): bigint => 10n ** BigInt(d);
/** Base amounts trade in lots of 10^(decimals - 6), so every token trades at micro-unit granularity. */
const lotScale = (baseDecimals: number): bigint => pow10(Math.max(0, baseDecimals - 6));
const gcd = (x: bigint, y: bigint): bigint => (y === 0n ? x : gcd(y, x % y));
const ceilDiv = (n: bigint, d: bigint): bigint => (n + d - 1n) / d;
/** The quote a base amount is worth at a price, rounded down. */
const quoteAt = (bd: number, qd: number, base: bigint, price: bigint): bigint =>
  base <= 0n || price <= 0n ? 0n : (base * price * pow10(qd)) / (PRICE_SCALE * pow10(bd));
/** The fewest base lots whose quote at this price is a whole number of quote units. */
const exactQuoteLots = (bd: number, qd: number, price: bigint): bigint => {
  const den = PRICE_SCALE * pow10(bd);
  return den / gcd(lotScale(bd) * price * pow10(qd), den);
};
/** An offer seen from its market: which side it takes and the base and quote decimals. */
type SwapDims = Readonly<{ side: 0 | 1; bd: number; qd: number }>;
type DimsSource = Pick<SwapOffer, "giveTokenId" | "wantTokenId" | "giveTokenDecimals" | "wantTokenDecimals">;
const dimsAt = (side: 0 | 1, o: Omit<DimsSource, "giveTokenId" | "wantTokenId">): SwapDims =>
  side === 1
    ? { side, bd: o.giveTokenDecimals, qd: o.wantTokenDecimals }
    : { side, bd: o.wantTokenDecimals, qd: o.giveTokenDecimals };
const swapDims = (o: DimsSource): SwapDims => dimsAt(swapSide(Number(o.giveTokenId), Number(o.wantTokenId)), o);
type MarketAmounts = Readonly<{ base: bigint; quote: bigint }>;
type OfferAmounts = Readonly<{ give: bigint; want: bigint }>;
const toMarket = (d: SwapDims, give: bigint, want: bigint): MarketAmounts =>
  d.side === 1 ? { base: give, quote: want } : { base: want, quote: give };
const toOffer = (d: SwapDims, base: bigint, quote: bigint): OfferAmounts =>
  d.side === 1 ? { give: base, want: quote } : { give: quote, want: base };
/**
 * og computePriceTicksForBaseQuoteDecimals under the one-tick step every og pair policy uses: sells round up, buys
 * down, so neither side is quoted a better price than it asked for.
 */
const priceTicksOf = (d: SwapDims, base: bigint, quote: bigint): bigint => {
  if (base <= 0n || quote <= 0n) return 0n;
  const n = quote * pow10(d.bd) * PRICE_SCALE;
  const den = base * pow10(d.qd);
  const roundsUp = d.side === 1 && n % den > 0n;
  const price = n / den + (roundsUp ? 1n : 0n);
  return price > 0n ? price : 0n;
};
/** og prepareSwapOrderWithDimensions: the canonical price, or undefined when the order quantizes to nothing. */
const preparedPrice = (d: SwapDims, base: bigint, quote: bigint): bigint | undefined => {
  const lot = lotScale(d.bd);
  if (base < lot || quote <= 0n) return undefined;
  const price = priceTicksOf(d, base, quote);
  if (price <= 0n) return undefined;
  const unit = lot * exactQuoteLots(d.bd, d.qd, price);
  const quantizedBase = (base / unit) * unit;
  return quantizedBase <= 0n || quoteAt(d.bd, d.qd, quantizedBase, price) <= 0n ? undefined : price;
};
/** og requantizeRemainingSwapBaseAtPriceForDimensions: the whole-unit part of a remaining base, back in offer terms. */
const requantizeRemaining = (d: SwapDims, base: bigint, price: bigint): OfferAmounts | undefined => {
  if (base <= 0n || price <= 0n) return undefined;
  const unit = lotScale(d.bd) * exactQuoteLots(d.bd, d.qd, price);
  const quantizedBase = (base / unit) * unit;
  if (quantizedBase <= 0n) return undefined;
  const quote = quoteAt(d.bd, d.qd, quantizedBase, price);
  return quote <= 0n ? undefined : toOffer(d, quantizedBase, quote);
};

// ---- swap net authorization: og account/swap/swap-net-authorization.ts ----
// The maker caps the fee it pays and floors what it nets, for the whole offer. Each fill may spend only its pro-rata
// share of that authority, and requantizing the offer takes the removed share's authority with it.
type NetAuth = Readonly<{ maxFee: bigint; minNetReceive: bigint }>;
type Authorized = NetAuth & Readonly<{ giveAmount: bigint; wantAmount: bigint }>;
type Fill = Readonly<{ give: bigint; want: bigint; fee: bigint }>;
const NO_FILL: Fill = { give: 0n, want: 0n, fee: 0n };
const offerAuthError = (o: Authorized): string | undefined => {
  const positive = (v: unknown): boolean => typeof v === "bigint" && v > 0n;
  const withinWant = (v: unknown): boolean => typeof v === "bigint" && v >= 0n && v <= o.wantAmount;
  switch (true) {
    case !positive(o.giveAmount) || !positive(o.wantAmount): return "SWAP_NET_AUTH_OFFER_AMOUNT_INVALID";
    case !withinWant(o.maxFee): return "SWAP_NET_AUTH_MAX_FEE_INVALID";
    case !withinWant(o.minNetReceive): return "SWAP_NET_AUTH_MIN_RECEIVE_INVALID";
    default: return undefined;
  }
};
/** The share of the offer a fill uses up: its give progress, or its want progress when that is larger and it closes. */
const usedShare = (o: Authorized, f: Fill, closes: boolean): Readonly<{ num: bigint; den: bigint }> => {
  const byGive = { num: f.give, den: o.giveAmount };
  const wantProgress = f.want < o.wantAmount ? f.want : o.wantAmount;
  return closes && wantProgress * o.giveAmount > f.give * o.wantAmount
    ? { num: wantProgress, den: o.wantAmount }
    : byGive;
};
/** og assertSwapNetAuthorization: the fill's fee and net receive stay inside its share of the maker's authority. */
const netAuthError = (o: Authorized, f: Fill, closes: boolean): string | undefined => {
  const bad = offerAuthError(o);
  if (bad !== undefined) return bad;
  if (f.give < 0n || f.give > o.giveAmount) return "SWAP_NET_AUTH_FILL_GIVE_INVALID";
  if (f.want < 0n || f.fee < 0n || f.fee > f.want || (f.want > 0n && f.fee >= f.want)) {
    return "SWAP_NET_AUTH_FILL_WANT_INVALID";
  }
  const { num, den } = usedShare(o, f, closes);
  if (f.fee > (o.maxFee * num) / den) return "SWAP_NET_AUTH_MAX_FEE_EXCEEDED";
  if (f.want - f.fee < ceilDiv(o.minNetReceive * num, den)) return "SWAP_NET_AUTH_MIN_RECEIVE_NOT_MET";
  return undefined;
};
/** og requantizeSwapNetAuthorization: the removed give share takes its pro-rata fee and receive authority with it. */
const requantizeAuth = (o: Authorized, give: bigint, want: bigint): Result<NetAuth, BodyError> => {
  const bad = offerAuthError(o);
  if (bad !== undefined) return swapErr(bad);
  if (give <= 0n || give > o.giveAmount || want <= 0n) return swapErr("SWAP_NET_AUTH_REMAINDER_INVALID");
  const removed = o.giveAmount - give;
  const auth: NetAuth = {
    maxFee: o.maxFee - (o.maxFee * removed) / o.giveAmount,
    minNetReceive: o.minNetReceive - ceilDiv(o.minNetReceive * removed, o.giveAmount),
  };
  return map(swapIssue(offerAuthError({ giveAmount: give, wantAmount: want, ...auth })), () => auth);
};

// ---- swap offer: og swap/offer admission, quantization, capacity and hold ----
type OfferTx = TxOf<"swap_offer">;
type UpsertEffect = Of<Effect, "swap_offer_upsert">;
/** og swap-limits.ts: a colon-free id not already resting, under the account caps for its venue. */
const offerSlot = (a: AccountBody, x: OfferTx): Result<void, BodyError> => {
  const offers = [...a.offers.values()];
  const sameJ = offers.filter((o) => o.crossJurisdiction === undefined).length;
  const crossJ = offers.length - sameJ;
  const isCrossJ = x.crossJurisdiction !== undefined;
  switch (true) {
    case x.offerId.includes(":"): return swapErr("SWAP_OFFER_ID_COLON");
    case a.offers.has(x.offerId): return err({ _tag: "duplicate" });
    case offers.length >= MAX_ACCOUNT_SWAP_OFFERS: return swapErr("SWAP_OFFER_LIMIT");
    case !isCrossJ && sameJ >= MAX_ACCOUNT_SAME_J_SWAP_OFFERS: return swapErr("SWAP_SAME_J_OFFER_LIMIT");
    case isCrossJ && crossJ >= MAX_ACCOUNT_CROSS_J_SWAP_OFFERS: return swapErr("SWAP_CROSS_J_OFFER_LIMIT");
    default: return ok(undefined);
  }
};
/** og admission.ts: sane decimals and amounts, and a fee cap below the want with a positive net floor. */
const offerTerms = (x: OfferTx): Result<void, BodyError> => {
  switch (true) {
    case !decimalsOk(x.giveTokenDecimals) || !decimalsOk(x.wantTokenDecimals):
      return swapErr("SWAP_TOKEN_DECIMALS_INVALID");
    case !inPaymentRange(x.giveAmount) || !inPaymentRange(x.wantAmount): return swapErr("SWAP_OFFER_AMOUNT_INVALID");
    case x.maxFee >= x.wantAmount || x.minNetReceive <= 0n: return swapErr("SWAP_NET_AUTH_INITIAL_TERMS_INVALID");
    default: return swapIssue(netAuthError(x, NO_FILL, false));
  }
};
/**
 * og admission.ts venue rules: a same-j offer swaps two distinct tokens; a cross-j offer settles gross through its
 * paired pulls, so it carries no fee authority, and its route must already be prepared and resting.
 */
const offerVenue = (x: OfferTx): Result<void, BodyError> => {
  const route = x.crossJurisdiction;
  if (route === undefined) return x.giveTokenId === x.wantTokenId ? swapErr("SWAP_SAME_TOKEN") : ok(undefined);
  if (x.maxFee !== 0n || x.minNetReceive !== x.wantAmount) return swapErr("CROSS_J_SWAP_NET_AUTH_INVALID");
  const prepared = route.status === "resting" && route.sourcePull && route.targetPull;
  return prepared ? ok(undefined) : swapErr("CROSS_J_SWAP_NOT_PREPARED");
};
const timeInForce = (x: OfferTx): Result<void, BodyError> =>
  x.timeInForce === undefined || [0, 1, 2].includes(x.timeInForce)
    ? ok(undefined)
    : swapErr("SWAP_TIME_IN_FORCE_INVALID");
/** og cross-j admission: the maker is a party to this Account, and the proposer is the maker or its source peer. */
const crossJMaker = (a: AccountBody, route: CrossRoute, ctx: FoldCtx): Result<boolean, BodyError> => {
  const { left, right } = a.account.id;
  const proposer = at(left, right, ctx.byLeft);
  const makerIsLeft = lowerText(route.makerEntityId) === lowerText(left);
  const party = makerIsLeft || lowerText(route.makerEntityId) === lowerText(right);
  const proposers = [route.makerEntityId, route.source.counterpartyEntityId];
  const proposes = proposers.some((e) => lowerText(e) === lowerText(proposer));
  return party && proposes ? ok(makerIsLeft) : swapErr("CROSS_J_SWAP_PROPOSER");
};
/** og swap-limits.ts accountSwapMarketKey: same-j offers by token direction, cross-j offers by venue and side. */
const offerMarketKey = (
  o: Pick<SwapOfferTerms, "giveTokenId" | "wantTokenId" | "crossJurisdiction">,
): Result<string, BodyError> => {
  const route = o.crossJurisdiction;
  if (route === undefined) return ok(`same:${Number(o.giveTokenId)}>${Number(o.wantTokenId)}`);
  return map(noteErr(crossMarket(route)), (m) => `${m.venueId}:${m.sourceIsBase ? "base>quote" : "quote>base"}`);
};
/** og swap-limits.ts: one maker rests at most MAX_SWAP_OFFERS_PER_SIDE_PER_MARKET offers in one market. */
const marketRoom = (a: AccountBody, x: OfferTx, makerIsLeft: boolean): Result<void, BodyError> => {
  const makers = [...a.offers.values()].filter((o) => o.makerIsLeft === makerIsLeft);
  return chain(offerMarketKey(x), (key) => chain(traverse(makers, offerMarketKey), (keys) => {
    const resting = keys.filter((k) => k === key).length;
    return resting >= MAX_SWAP_OFFERS_PER_SIDE_PER_MARKET ? swapErr("SWAP_MARKET_OFFER_LIMIT") : ok(undefined);
  }));
};
const swapOffer = (a: AccountBody, x: OfferTx, ctx: FoldCtx): BodyStep<UpsertEffect> => {
  const route = x.crossJurisdiction;
  const admitted = checks(offerSlot(a, x), offerTerms(x), offerVenue(x), timeInForce(x));
  const maker = (): Result<boolean, BodyError> => (route === undefined ? ok(ctx.byLeft) : crossJMaker(a, route, ctx));
  return chain(admitted, () => chain(maker(), (makerIsLeft) => chain(marketRoom(a, x, makerIsLeft), () =>
    route === undefined ? sameJOffer(a, x, ctx, makerIsLeft) : crossJOffer(a, x, route, ctx, makerIsLeft))));
};
type Resting = NetAuth & OfferAmounts & Readonly<{ priceTicks: bigint }>;
/** The offer both venues commit: the maker's tokens at the quantized amounts, price and authority. */
const restingOffer = (x: OfferTx, r: Resting, makerIsLeft: boolean, ctx: FoldCtx): SwapOffer => ({
  offerId: x.offerId,
  giveTokenId: x.giveTokenId, giveTokenDecimals: x.giveTokenDecimals, giveAmount: r.give,
  wantTokenId: x.wantTokenId, wantTokenDecimals: x.wantTokenDecimals, wantAmount: r.want,
  maxFee: r.maxFee, minNetReceive: r.minNetReceive, priceTicks: r.priceTicks, ...opt("timeInForce", x.timeInForce),
  makerIsLeft,
  // og mutation.ts passes the frame's jHeight as the swap handlers' currentHeight.
  createdHeight: Number(ctx.jHeight), quantizedGive: r.give, quantizedWant: r.want,
});
/** og cross-j-binding.ts: the source pull this offer pairs with carries the route's token, amount, hashes and order. */
const pairedSourcePull = (
  a: AccountBody, x: OfferTx, route: CrossRoute, canonical: CrossRoute,
): Result<void, BodyError> => {
  const sp = route.sourcePull;
  const paired = sp === undefined ? undefined : a.pulls?.get(sp.pullId);
  if (sp === undefined || paired === undefined) return swapErr("CROSS_J_SWAP_SOURCE_PULL_MISSING");
  const sameAmount = paired.tokenId === sp.tokenId && paired.tokenId === Number(x.giveTokenId)
    && paired.amount === sp.signedAmount;
  const sameHashes = lowerText(paired.fullHash) === lowerText(sp.fullHash)
    && lowerText(paired.partialRoot) === lowerText(sp.partialRoot);
  if (!sameAmount || !sameHashes) return swapErr("CROSS_J_SWAP_SOURCE_PULL_MISMATCH");
  const b = paired.crossJurisdiction;
  const bound = b && b.leg === "source" && b.orderId === canonical.orderId
    && lowerText(b.routeHash) === lowerText(canonical.routeHash);
  return bound ? ok(undefined) : swapErr("CROSS_J_SWAP_SOURCE_BINDING_MISMATCH");
};
/**
 * og quantization.ts + cross-j-binding.ts + commit.ts for a cross-j offer: the route's exact amounts on its canonical
 * side, a lot-aligned base, and the paired source pull. The pull already holds the give, so there is no second hold.
 */
const crossJOffer = (a: AccountBody, x: OfferTx, route: CrossRoute, ctx: FoldCtx, makerIsLeft: boolean): BodyStep =>
  chain(noteErr(crossMarket(route)), (m) => {
    const d = dimsAt(m.sourceIsBase ? 1 : 0, x);
    const { base, quote } = toMarket(d, x.giveAmount, x.wantAmount);
    const lot = lotScale(d.bd);
    const priceTicks = priceTicksOf(d, base, quote);
    const sameAmounts = (): boolean =>
      x.giveAmount === BigInt(route.source.amount) && x.wantAmount === BigInt(route.target.amount);
    switch (true) {
      case base < lot: return swapErr("SWAP_ORDER_BELOW_LOT");
      case base % lot !== 0n: return swapErr("CROSS_J_SWAP_LOT_ALIGNMENT");
      case priceTicks <= 0n: return swapErr("SWAP_PRICE_INVALID");
      case !sameAmounts(): return swapErr("CROSS_J_SWAP_AMOUNT_CHANGED");
    }
    const paired = chain(noteErr(canonicalCrossRoute(route)), (canonical) => pairedSourcePull(a, x, route, canonical));
    const committed = chain(paired, () => all({
      auth: requantizeAuth(x, x.giveAmount, x.wantAmount),
      publicRoute: noteErr(cloneCrossRoute(route)),
    }));
    return map(committed, ({ auth, publicRoute }) => {
      const resting = { ...auth, give: x.giveAmount, want: x.wantAmount, priceTicks };
      const offer: SwapOffer = { ...restingOffer(x, resting, makerIsLeft, ctx), crossJurisdiction: publicRoute };
      return step({ ...a, offers: mapSet(a.offers, x.offerId, offer) });
    });
  });
/** og price check: a maker-supplied tick may differ from the canonical price by at most one tick. */
const offerPrice = (d: SwapDims, amounts: MarketAmounts, input: bigint | undefined): Result<bigint, BodyError> => {
  const prepared = preparedPrice(d, amounts.base, amounts.quote);
  if (prepared === undefined) return swapErr("SWAP_PRICE_INVALID");
  if (input === undefined) return ok(prepared);
  if (input <= 0n) return swapErr("SWAP_PRICE_TICKS_INVALID");
  const drift = input > prepared ? input - prepared : prepared - input;
  return drift > 1n ? swapErr("SWAP_PRICE_TICKS_MISMATCH") : ok(input);
};
/** og commit.ts: the maker has room for the give, and its total hold stays within one payment. */
const makerHold = (a: AccountBody, token: TokenId, give: bigint, makerIsLeft: boolean): Result<void, BodyError> =>
  chain(ensureRoom(a, token, give, makerIsLeft), () => holdRoom(a, token, give, makerIsLeft));
/** og same-j quantization and commit: canonical price, lot-quantized amounts, capacity, and the maker's give hold. */
const sameJOffer = (a: AccountBody, x: OfferTx, ctx: FoldCtx, makerIsLeft: boolean): BodyStep<UpsertEffect> => {
  const d = swapDims(x);
  const amounts = toMarket(d, x.giveAmount, x.wantAmount);
  const lot = lotScale(d.bd);
  if (amounts.base < lot) return swapErr("SWAP_ORDER_BELOW_LOT");
  return chain(offerPrice(d, amounts, x.priceTicks), (priceTicks) => {
    const base = (amounts.base / lot) * lot;
    const { give, want } = toOffer(d, base, quoteAt(d.bd, d.qd, base, priceTicks));
    if (!inPaymentRange(give) || !inPaymentRange(want)) return swapErr("SWAP_QUANTIZED_AMOUNT_INVALID");
    const hold = makerHold(a, x.giveTokenId, give, makerIsLeft);
    const held = chain(requantizeAuth(x, give, want), (auth) => map(hold, () => auth));
    return map(held, (auth) => {
      const offer = restingOffer(x, { ...auth, give, want, priceTicks }, makerIsLeft, ctx);
      return step({ ...a, offers: mapSet(a.offers, x.offerId, offer) }, [upserted(a, offer)]);
    });
  });
};
/** og collectSameJurisdictionSwapOutputs: a same-j offer that survives reports its committed snapshot. */
const upserted = (a: AccountBody, offer: SwapOffer): UpsertEffect =>
  ({ _tag: "swap_offer_upsert", offer, left: a.account.id.left, right: a.account.id.right });

// ---- swap resolve: og swap/resolve ----
// The counterparty fills a resting same-j offer: at or above the maker's limit, inside its fee authority, with room
// for the want. The maker's give moves across, the want less the fee comes back, and a whole-unit remainder rests on.
type ResolveTx = TxOf<"swap_resolve">;
type ResolveEffect = Of<Effect, "swap_cancelled" | "swap_offer_upsert">;
/** og orderbook/swap-execution.ts deriveExactSwapFillRatio: the executed share of the quantized give, reduced. */
const exactFillRatio = (qG: bigint, fG: bigint): { readonly n: bigint; readonly d: bigint } => {
  if (qG <= 0n || fG <= 0n) return { n: 0n, d: 1n };
  if (fG >= qG) return { n: 1n, d: 1n };
  const g = gcd(fG, qG);
  return { n: fG / g, d: qG / g };
};
/**
 * og exactFillRatioToUint16: the smallest ratio c whose floor share reaches the exact fill. Since n is whole,
 * floor(d·c / MAX_FILL) ≥ n exactly when c ≥ n·MAX_FILL / d, so c is that bound rounded up.
 */
const fillRatioOf = (r: { readonly n: bigint; readonly d: bigint }): number => {
  switch (true) {
    case r.n <= 0n: return 0;
    case r.n >= r.d: return MAX_FILL;
    default: return Number(ceilDiv(r.n * BigInt(MAX_FILL), r.d));
  }
};
/** og resolve: every resting term the taker quotes must be the offer's own. */
const quotesResting = (x: ResolveTx, o: SwapOffer): boolean => {
  const quoted = [
    [x.restingGiveAmount, o.giveAmount], [x.restingWantAmount, o.wantAmount],
    [x.restingQuantizedGive, o.quantizedGive], [x.restingQuantizedWant, o.quantizedWant],
    [x.restingPriceTicks, o.priceTicks],
  ] as const;
  return quoted.every(([theirs, ours]) => theirs === undefined || theirs === ours);
};
type Execution = Readonly<{ provided: boolean; give: bigint; want: bigint; ratio: number }>;
/** og resolve execution: both explicit amounts, whose exact share fixes the ratio; without them the ratio must be 0. */
const executionOf = (x: ResolveTx, quantizedGive: bigint): Result<Execution, BodyError> => {
  const give = x.executionGiveAmount, want = x.executionWantAmount;
  if (give !== undefined && want !== undefined) {
    return ok({ provided: true, give, want, ratio: fillRatioOf(exactFillRatio(quantizedGive, give)) });
  }
  if (give !== undefined || want !== undefined) return swapErr("SWAP_EXECUTION_PARTIAL");
  if (x.fillRatio > 0) return swapErr("SWAP_EXECUTION_REQUIRED");
  return ok({ provided: false, give: 0n, want: 0n, ratio: x.fillRatio });
};
/** og resolve exact ratio: an n/d the taker quotes must be exactly the executed share of the quantized give. */
const exactRatio = (x: ResolveTx, quantizedGive: bigint, give: bigint): Result<void, BodyError> => {
  const n = x.fillNumerator, d = x.fillDenominator;
  if (n === undefined && d === undefined) return ok(undefined);
  if (n === undefined || d === undefined) return swapErr("SWAP_EXACT_RATIO_PARTIAL");
  if (d <= 0n || n < 0n || n > d) return swapErr("SWAP_EXACT_RATIO_RANGE");
  return n * quantizedGive === give * d ? ok(undefined) : swapErr("SWAP_EXACT_RATIO_MISMATCH");
};
/** og resolve fee: in the want token, only on a real fill, and always less than the want it comes out of. */
const feeOf = (x: ResolveTx, o: SwapOffer, e: Execution): Result<bigint, BodyError> => {
  const fee = x.feeAmount ?? 0n;
  const wrongToken = (x.feeTokenId ?? o.wantTokenId) !== o.wantTokenId;
  const invalid = fee < 0n || (fee > 0n && (e.give <= 0n || wrongToken)) || (fee >= e.want && e.want > 0n);
  return invalid ? swapErr("SWAP_FEE_INVALID") : ok(fee);
};
/** og resolve execution checks: a positive fill within the offer, at or above the maker's limit, at its own ratio. */
const executionFits = (x: ResolveTx, o: SwapOffer, e: Execution): Result<void, BodyError> => {
  const fills = e.give > 0n || e.want > 0n;
  switch (true) {
    case !e.provided: return ok(undefined);
    case fills && (e.give <= 0n || e.want <= 0n): return swapErr("SWAP_EXECUTION_NOT_POSITIVE");
    case x.fillRatio !== e.ratio: return swapErr("SWAP_FILL_RATIO_MISMATCH");
    case fills && e.give > o.quantizedGive: return swapErr("SWAP_EXECUTION_ABOVE_OFFER");
    case fills && e.want * o.quantizedGive < e.give * o.quantizedWant: return swapErr("SWAP_MAKER_LIMIT_PRICE");
    case e.ratio > 0 && (!inPaymentRange(e.give) || !inPaymentRange(e.want)): return swapErr("SWAP_FILL_AMOUNT_BOUNDS");
    default: return ok(undefined);
  }
};
type Taken = Readonly<{ fill: Fill; ratio: number }>;
/** og resolve admission: the counterparty quotes the resting terms and fills inside the maker's price and authority. */
const takerFill = (o: SwapOffer, x: ResolveTx, ctx: FoldCtx): Result<Taken, BodyError> => {
  if (o.crossJurisdiction !== undefined) return swapErr("SWAP_RESOLVE_CROSS_J");
  if (!quotesResting(x, o)) return swapErr("SWAP_RESTING_TERMS_MISMATCH");
  if (ctx.byLeft === o.makerIsLeft) return err({ _tag: "not_counterparty" });
  if (!Number.isInteger(x.fillRatio) || x.fillRatio < 0 || x.fillRatio > MAX_FILL) return err({ _tag: "bad_ratio" });
  return chain(executionOf(x, o.quantizedGive), (e) =>
    chain(exactRatio(x, o.quantizedGive, e.give), () => chain(feeOf(x, o, e), (fee) => {
      const fill: Fill = { give: e.give, want: e.want, fee };
      const fits = chain(swapIssue(netAuthError(o, fill, x.cancelRemainder)), () => executionFits(x, o, e));
      return map(fits, () => ({ fill, ratio: e.ratio }));
    })));
};
/**
 * og resolve settlement: the maker's give moves to the taker and the want, less the fee, moves back. The offer
 * leaves the book here; holds follow live offers, so its filled give and any requantization dust are released.
 */
const settleFill = (a: AccountBody, o: SwapOffer, f: Fill): Result<AccountBody, BodyError> => {
  const fromMaker = (n: bigint): bigint => (o.makerIsLeft ? -n : n);
  const takerRoom = f.want > 0n ? ensureRoom(a, o.wantTokenId, f.want, other(o.makerIsLeft)) : ok(undefined);
  return chain(takerRoom, () => {
    const giveRow = shift(getDelta(a.account, o.giveTokenId), fromMaker(f.give));
    const wantRow = shift(getDelta(a.account, o.wantTokenId), -fromMaker(f.want - f.fee));
    return chain(checks(representable(a, giveRow), representable(a, wantRow)), () => {
      const closed: AccountBody = { ...a, offers: mapDelete(a.offers, o.offerId) };
      return ok(putState(closed, setDelta(setDelta(a.account, giveRow), wantRow)));
    });
  });
};
/** og remainder.ts: the unfilled part, requantized at the offer's price with its share of the authority. */
const remainderOf = (o: SwapOffer, f: Fill): Result<SwapOffer | undefined, BodyError> => {
  const d = swapDims(o);
  const unfilledGive = o.quantizedGive - f.give;
  const next = requantizeRemaining(d, toMarket(d, unfilledGive, o.quantizedWant - f.want).base, o.priceTicks);
  if (next === undefined) return ok(undefined);
  if (unfilledGive - next.give < 0n) return swapErr("SWAP_REMAINDER_EXCEEDS_HOLD");
  return map(requantizeAuth(o, next.give, next.want), (auth): SwapOffer => ({
    ...o, giveAmount: next.give, wantAmount: next.want, ...auth, quantizedGive: next.give, quantizedWant: next.want,
  }));
};
const swapResolve = (a: AccountBody, x: ResolveTx, ctx: FoldCtx): BodyStep<ResolveEffect> => {
  const offer = a.offers.get(x.offerId);
  if (offer === undefined) return MISSING;
  const { left, right } = a.account.id;
  // og remainder.ts closeSwapOffer: every removal of the resting offer reports swap_cancelled with the maker's entity.
  const makerId = at(left, right, offer.makerIsLeft);
  const cancelled = { _tag: "swap_cancelled" as const, offerId: offer.offerId, makerId };
  return chain(takerFill(offer, x, ctx), ({ fill, ratio }) => chain(settleFill(a, offer, fill), (moved) => {
    const closes = x.cancelRemainder || x.fillRatio === 0 || ratio === MAX_FILL;
    const rest = closes ? ok(undefined) : remainderOf(offer, fill);
    return map(rest, (r): Step<AccountBody, ResolveEffect> => r === undefined
      ? step(moved, [cancelled])
      : step({ ...moved, offers: mapSet(moved.offers, offer.offerId, r) }, [upserted(a, r)]));
  }));
};
// ---- rebalance: og handlers/rebalance/{request-collateral,refund,policy}.ts ----
// A side short of collateral asks the hub to deposit more, prepaying the hub's fee. The request stays until the
// collateral lands or the hub refunds the whole fee. Each side also publishes versioned fee terms for such requests.
const rebalanceErr = (reason: string): Result<never, BodyError> => err({ _tag: "rebalance", reason });
type CollateralRequested = Of<Effect, "request_collateral_committed">;
/** og request-collateral.ts: a positive amount, a non-negative fee and a known policy version, on a live token. */
const requestTermsIssue = (a: AccountBody, x: TxOf<"request_collateral">): string | undefined => {
  switch (true) {
    case x.amount <= 0n: return "REQUEST_COLLATERAL_AMOUNT";
    case x.feeAmount < 0n: return "REQUEST_COLLATERAL_FEE";
    case !Number.isFinite(x.policyVersion) || x.policyVersion < 1: return "REQUEST_COLLATERAL_POLICY_VERSION";
    case !a.account.deltas.has(x.tokenId): return "REQUEST_COLLATERAL_NO_DELTA";
    default: return undefined;
  }
};
/** A fee paid in the requested token comes out of the requested amount. */
const requestNetAmount = (x: TxOf<"request_collateral">): bigint =>
  (x.feeTokenId ?? x.tokenId) === x.tokenId ? floor0(x.amount - x.feeAmount) : x.amount;
/** og request-collateral.ts: the requester prepays the fee now, and the request opens for its net amount. */
const requestCollateral = (
  a: AccountBody, x: TxOf<"request_collateral">, ctx: FoldCtx,
): BodyStep<CollateralRequested> => {
  const terms = requestTermsIssue(a, x);
  if (terms !== undefined) return rebalanceErr(terms);
  // One immutable request per token: a repeat while one is open changes nothing.
  if ((a.requested.get(x.tokenId) ?? 0n) > 0n) return ok(step(a));
  const feeToken = x.feeTokenId ?? x.tokenId;
  if (x.feeAmount <= 0n) return rebalanceErr("REQUEST_COLLATERAL_FEE_ZERO");
  if (!a.account.deltas.has(feeToken)) return rebalanceErr("REQUEST_COLLATERAL_NO_FEE_DELTA");
  const amount = requestNetAmount(x);
  if (amount <= 0n) return ok(step(a));
  const requestedAt = Number(ctx.nowMs);
  const fees: RebalanceRequestFeeState = {
    requestId: `rebalance:${ctx.byLeft ? "left" : "right"}:${Number(x.tokenId)}:${ctx.accountHeight}`,
    feeTokenId: Number(feeToken), feePaidUpfront: x.feeAmount, requestedAmount: amount,
    policyVersion: x.policyVersion, requestedAt, requestedByLeft: ctx.byLeft,
  };
  // og mutation.ts applyCollateralRequest: a freshly created request reports request_collateral_committed.
  const committed: CollateralRequested = {
    _tag: "request_collateral_committed", tokenId: Number(x.tokenId), requestedAmount: amount,
    prepaidFee: x.feeAmount, requestedAt,
  };
  const opened = (b: AccountBody): AccountBody => ({
    ...b, requested: mapSet(b.requested, x.tokenId, amount), requestFees: mapSet(b.requestFees, x.tokenId, fees),
  });
  return map(spend(a, feeToken, x.feeAmount, ctx.byLeft), (paid) => step(opened(paid), [committed]));
};
/** og refund.ts: the hub returns prepaid fee, partially or in full; a full refund clears the request. */
const rebalanceRefund = (a: AccountBody, x: TxOf<"rebalance_refund">, ctx: FoldCtx): BodyStep => {
  if (!x.requestId || x.amount <= 0n) return rebalanceErr("REBALANCE_REFUND_INPUT");
  const tk = x.requestTokenId;
  const fees = a.requestFees.get(tk);
  const open = (a.requested.get(tk) ?? 0n) > 0n;
  if (fees === undefined || !open || fees.requestId !== x.requestId) return rebalanceErr("REBALANCE_REFUND_NOT_FOUND");
  const refunded = fees.refund?.refundedAmount ?? 0n;
  const outstanding = fees.feePaidUpfront - refunded;
  const feeToken = String(fees.feeTokenId) as TokenId;
  switch (true) {
    case ctx.byLeft === fees.requestedByLeft: return rebalanceErr("REBALANCE_REFUND_SELF");
    case fees.refund !== undefined && fees.refund.reason !== x.reason: return rebalanceErr("REBALANCE_REFUND_REASON");
    case outstanding <= 0n || x.amount > outstanding: return rebalanceErr("REBALANCE_REFUND_OUTSTANDING");
    case !a.account.deltas.has(feeToken): return rebalanceErr("REBALANCE_REFUND_NO_FEE_DELTA");
  }
  const total = refunded + x.amount;
  const refund = { reason: x.reason, refundedAmount: total };
  const partly = (b: AccountBody): AccountBody =>
    ({ ...b, requestFees: mapSet(b.requestFees, tk, { ...fees, refund }) });
  // og refund.ts: a full refund also clears the shadow submission marker, so a new request may enter a J-batch.
  const fully = (b: AccountBody): AccountBody => clearSubmittedAt(dropRequest(b, tk), tk);
  const settle = total === fees.feePaidUpfront ? fully : partly;
  return map(spend(a, feeToken, x.amount, ctx.byLeft), (paid) => step(settle(paid)));
};
const samePolicyTerms = (current: RebalanceFeeSnapshot | undefined, x: TxOf<"rebalance_policy">): boolean =>
  current !== undefined && current.baseFee === x.baseFee
  && current.liquidityFeeBps === x.liquidityFeeBps && current.gasFee === x.gasFee;
/** Where a published version stands against the side's current one. */
const policyStanding = (current: RebalanceFeeSnapshot | undefined, version: number): "stale" | "current" | "newer" => {
  if (current === undefined || version > current.policyVersion) return "newer";
  return version < current.policyVersion ? "stale" : "current";
};
const policyTermsIssue = (x: TxOf<"rebalance_policy">, ts: number): string | undefined => {
  const token = Number(x.tokenId);
  const fees = [x.baseFee, x.liquidityFeeBps, x.gasFee];
  switch (true) {
    case !Number.isSafeInteger(token) || token <= 0 || token > 65_535: return "REBALANCE_POLICY_TOKEN";
    case !Number.isSafeInteger(x.policyVersion) || x.policyVersion <= 0: return "REBALANCE_POLICY_VERSION";
    case fees.some((f) => typeof f !== "bigint"): return "REBALANCE_POLICY_FEE_TYPES";
    case !Number.isSafeInteger(ts) || ts <= 0: return "REBALANCE_POLICY_TIMESTAMP";
    case fees.some((f) => f < 0n) || x.liquidityFeeBps > 10_000n: return "REBALANCE_POLICY_FEE_TERMS";
    default: return undefined;
  }
};
/**
 * og policy.ts: each side publishes versioned fee terms. An older version is ignored; a changed term under the same
 * version is equivocation.
 */
const rebalancePolicy = (a: AccountBody, x: TxOf<"rebalance_policy">, ctx: FoldCtx): BodyStep => {
  const ts = Number(ctx.nowMs);
  const issue = policyTermsIssue(x, ts);
  if (issue !== undefined) return rebalanceErr(issue);
  if (!a.account.deltas.has(x.tokenId)) return rebalanceErr("REBALANCE_POLICY_NO_DELTA");
  const held = a.feePolicies.get(x.tokenId);
  const current = at(held?.left, held?.right, ctx.byLeft);
  const next: RebalanceFeeSnapshot = {
    policyVersion: x.policyVersion, baseFee: x.baseFee, liquidityFeeBps: x.liquidityFeeBps, gasFee: x.gasFee,
    updatedAt: ts,
  };
  const published = { ...held, ...(ctx.byLeft ? { left: next } : { right: next }) };
  switch (policyStanding(current, x.policyVersion)) {
    case "stale": return ok(step(a));
    case "current": return samePolicyTerms(current, x) ? ok(step(a)) : rebalanceErr("REBALANCE_POLICY_EQUIVOCATION");
    case "newer": return ok(step({ ...a, feePolicies: mapSet(a.feePolicies, x.tokenId, published) }));
  }
};

// ---- Account-level lending: og handlers/balance/lending.ts ----
// Lending lives in the entities' books; the Account only moves the money and remembers each intent id once, so a
// replayed intent can never pay twice. Every tx is proposed by the party its role names, against the other one.
const lendingErr = (reason: string): Result<never, BodyError> => err({ _tag: "lending", reason });
const LENDING_ENTITY = /^0x[0-9a-f]{64}$/, LENDING_INTENT = /^(?:lend|borrow|loan)-[0-9a-f]{16}$/;
const lower = (v: unknown): string => String(v || "").trim().toLowerCase();
type AccountLendingTx = TxOf<
  | "lending_fund" | "lending_borrow_request" | "lending_repay"
  | "lending_credit" | "lending_close_request" | "lending_close_payout"
>;
/** og deriveDelta outOwnCredit: the proposer's own credit line it has not drawn yet. */
const ownCreditLeft = (d: Delta, isLeft: boolean): bigint => {
  const t = d.ondelta + d.offdelta;
  return isLeft ? floor0(d.leftCreditLimit - floor0(-t)) : floor0(d.rightCreditLimit - floor0(t - d.collateral));
};
/** The intent a lending tx acts on, the party that must propose it and in which role, and the party across. */
type LendingRole = Readonly<{
  id: string; prefix: "lend" | "borrow" | "loan";
  actor: string; actorRole: "lender" | "borrower" | "hub"; counterparty: string;
}>;
/** Lenders fund and close positions, borrowers request and repay loans, the hub credits loans and pays out. */
const lendingRole = (x: AccountLendingTx): LendingRole => {
  switch (x.type) {
    case "lending_fund": case "lending_close_request": return {
      id: x.positionId, prefix: "lend", actor: x.lenderEntityId, actorRole: "lender", counterparty: x.hubEntityId,
    };
    case "lending_borrow_request": return {
      id: x.requestId, prefix: "borrow", actor: x.borrowerEntityId, actorRole: "borrower", counterparty: x.hubEntityId,
    };
    case "lending_repay": return {
      id: x.loanId, prefix: "loan", actor: x.borrowerEntityId, actorRole: "borrower", counterparty: x.hubEntityId,
    };
    case "lending_credit": return {
      id: x.loanId, prefix: "loan", actor: x.hubEntityId, actorRole: "hub", counterparty: x.borrowerEntityId,
    };
    case "lending_close_payout": return {
      id: x.positionId, prefix: "lend", actor: x.hubEntityId, actorRole: "hub", counterparty: x.lenderEntityId,
    };
  }
};
/** The key each intent is remembered under, so it can act only once. */
const intentKey = (x: AccountLendingTx): string => {
  switch (x.type) {
    case "lending_fund": return `fund:${lower(x.positionId)}`;
    case "lending_borrow_request": return `borrow:${lower(x.requestId)}`;
    case "lending_repay": return `repay:${lower(x.loanId)}`;
    case "lending_credit": return `${x.action === "grant" ? "grant" : "revoke"}:${lower(x.loanId)}`;
    case "lending_close_request": return `close:${lower(x.positionId)}`;
    case "lending_close_payout": return `payout:${lower(x.positionId)}`;
  }
};
/** og requireIntentId, requireRole (the claimed actor is the frame proposer) and requireCounterparty. */
const lendingParties = (a: AccountBody, byLeft: boolean, role: LendingRole): Result<void, BodyError> => {
  const intent = lower(role.id);
  const left = lower(a.account.id.left), right = lower(a.account.id.right);
  const proposer = at(left, right, byLeft);
  const peer = proposer === left ? right : left;
  const claimed = lower(role.actor);
  switch (true) {
    case !LENDING_INTENT.test(intent) || !intent.startsWith(`${role.prefix}-`):
      return lendingErr("LENDING_INTENT_ID_INVALID");
    case !LENDING_ENTITY.test(claimed): return lendingErr("LENDING_ROLE_INVALID");
    case claimed !== proposer: return lendingErr("LENDING_ROLE_NOT_PROPOSER");
    case lower(role.counterparty) !== peer: return lendingErr("LENDING_COUNTERPARTY_INVALID");
    default: return ok(undefined);
  }
};
const LENDING_TERMS: ReadonlySet<unknown> = new Set(["1h", "1d", "1m"]);
const interestOk = (v: unknown): boolean => {
  const bps = Math.floor(Number(v));
  return Number.isFinite(bps) && bps >= 0 && bps <= 10_000;
};
const loanTerms = (termId: unknown, interestBps: unknown): Result<void, BodyError> => {
  if (!LENDING_TERMS.has(termId)) return lendingErr("LENDING_INVALID_TERM");
  return interestOk(interestBps) ? ok(undefined) : lendingErr("LENDING_INVALID_INTEREST_BPS");
};
const positiveLoanAmount = (v: bigint): Result<void, BodyError> =>
  v <= 0n ? lendingErr("LENDING_AMOUNT_MUST_BE_POSITIVE") : ok(undefined);
const intentFree = (b: AccountBody, key: string): Result<void, BodyError> =>
  b.lendingIntents.has(key) ? lendingErr("LENDING_INTENT_REPLAY") : ok(undefined);
const recordIntent = (b: AccountBody, key: string, kind: LendingIntentKind): BodyStep =>
  map(intentFree(b, key), () => step({ ...b, lendingIntents: mapSet(b.lendingIntents, key, kind) }));
/** The proposer pays under a fresh intent: a replay is refused before any money moves. */
const payIntent = (
  a: AccountBody, byLeft: boolean, tk: TokenId, amount: bigint, key: string, kind: LendingIntentKind,
): BodyStep => chain(intentFree(a, key), () => chain(spend(a, tk, amount, byLeft), (b) => recordIntent(b, key, kind)));
/** og lending fund: only owned funds may fund the pool; unused own credit does not count. */
const ownsFunds = (a: AccountBody, x: TxOf<"lending_fund">, byLeft: boolean): boolean => {
  const d = a.account.deltas.get(x.tokenId);
  // outCapacity already excludes holds and allowances.
  const spendable = (delta: Delta): bigint => outCapacity(delta, byLeft, holds(a, x.tokenId, byLeft));
  return d !== undefined && x.amount + ownCreditLeft(d, byLeft) <= spendable(d);
};
const fundPool = (a: AccountBody, x: TxOf<"lending_fund">, byLeft: boolean): BodyStep => {
  const key = intentKey(x);
  return chain(checks(loanTerms(x.termId, x.interestBps), intentFree(a, key)), () => ownsFunds(a, x, byLeft)
    ? payIntent(a, byLeft, x.tokenId, x.amount, key, "fund")
    : lendingErr("LENDING_FUND_OWNED_BALANCE_INSUFFICIENT"));
};
/** og lending credit: the hub sets the borrower's credit line, granting or revoking it once per loan. */
const creditLoan = (a: AccountBody, x: TxOf<"lending_credit">, byLeft: boolean): BodyStep => {
  if (x.creditLimit < 0n) return lendingErr("LENDING_CREDIT_LIMIT_NEGATIVE");
  const kind = x.action === "grant" ? "credit-grant" : "credit-revoke";
  return chain(updateDelta(a.account, x.tokenId, (d) => setCreditLimit(d, x.creditLimit, byLeft)), (s) =>
    recordIntent(putState(a, s), intentKey(x), kind));
};
const lendingStep = (a: AccountBody, x: AccountLendingTx, byLeft: boolean): BodyStep => {
  const key = intentKey(x);
  switch (x.type) {
    case "lending_fund": return chain(positiveLoanAmount(x.amount), () => fundPool(a, x, byLeft));
    case "lending_borrow_request": return chain(positiveLoanAmount(x.amount), () =>
      chain(loanTerms(x.termId, x.maxInterestBps), () => recordIntent(a, key, "borrow")));
    case "lending_repay": return chain(positiveLoanAmount(x.amount), () =>
      payIntent(a, byLeft, x.tokenId, x.amount, key, "repay"));
    case "lending_credit": return creditLoan(a, x, byLeft);
    case "lending_close_request": return recordIntent(a, key, "close-request");
    case "lending_close_payout": return chain(positiveLoanAmount(x.amount), () =>
      payIntent(a, byLeft, x.tokenId, x.amount, key, "close-payout"));
  }
};
const lending = (a: AccountBody, x: AccountLendingTx, ctx: FoldCtx): BodyStep =>
  chain(lendingParties(a, ctx.byLeft, lendingRole(x)), () => lendingStep(a, x, ctx.byLeft));

// ---- direct payment envelope: og handlers/balance/direct-payment.ts ----
// og validatePaymentEnvelope, resolvePaymentParties and validatePaymentRoute. A direct payment names only its
// recipient. A trusted one runs through a gateway: to the gateway with the final recipient after it, or, when the
// gateway itself pays, straight to the recipient as the last leg.
const MAX_ROUTE_HOPS = 100;
type PaymentMode = NonNullable<TxOf<"payment">["deliveryMode"]>;
const routeIssue = (
  mode: PaymentMode, route: readonly string[], from: string, to: string, gateway: string,
): string | undefined => {
  const onlyRecipient = route.length === 1 && lowerText(route[0]) === to;
  if (mode === "direct") return onlyRecipient ? undefined : "DIRECT_ROUTE";
  const g = lowerText(gateway);
  if (from === g) return onlyRecipient ? undefined : "GATEWAY_FINAL_LEG";
  const final = lowerText(route[1]);
  const viaGateway = to === g && route.length === 2 && lowerText(route[0]) === to;
  return viaGateway && final !== "" && final !== g && final !== from ? undefined : "TRUSTED_ROUTE";
};
/** Absent route/deliveryMode is the rewrite's own bilateral payment: route [recipient], direct. */
const paymentRoute = (a: AccountBody, x: TxOf<"payment">, byLeft: boolean): Result<void, BodyError> => {
  const bad = (reason: string): Result<never, BodyError> => err({ _tag: "payment_route", reason });
  const { left, right } = a.account.id;
  const from = at(left, right, byLeft).toLowerCase(), to = at(right, left, byLeft).toLowerCase();
  const route = x.route ?? [to];
  const mode = x.deliveryMode ?? "direct";
  const gateway = x.trustedGatewayEntityId;
  const misdirected = (x.fromEntityId && x.fromEntityId.toLowerCase() !== from)
    || (x.toEntityId && x.toEntityId.toLowerCase() !== to);
  switch (true) {
    case x.amount < 1n || x.amount > MAX_PAYMENT_AMOUNT: return err({ _tag: "non_positive_payment" });
    case route.length === 0 || route.length > MAX_ROUTE_HOPS: return bad("ROUTE_LENGTH");
    case mode !== "direct" && mode !== "trusted": return bad("DELIVERY_MODE");
    case mode === "direct" && gateway !== undefined: return bad("DIRECT_WITH_GATEWAY");
    case mode === "trusted" && !gateway: return bad("TRUSTED_WITHOUT_GATEWAY");
    case Boolean(misdirected): return bad("DIRECTION");
  }
  const issue = routeIssue(mode, route, from, to, String(gateway));
  return issue === undefined ? ok(undefined) : bad(issue);
};
// ---- cross-j pulls: og handlers/settlement/pull.ts ----
// A cross-j order pays through two pulls, one per jurisdiction. Opening one holds its amount on the payer side;
// closing it with a hash-ladder proof moves the claimed share to the beneficiary and releases the rest.
const absBig = (v: bigint): bigint => (v < 0n ? -v : v);
const crossReject = (reason: string): Result<never, BodyError> => err({ _tag: "cross_j", reason });
const NO_PULLS: ReadonlyMap<string, PullRow> = new Map();
const pullsOf = (a: AccountBody): ReadonlyMap<string, PullRow> => a.pulls ?? NO_PULLS;
type PullLockTx = TxOf<"cross_pull_lock">;
/** Every field a route gains once it fills, closes or settles; an opening pull must see none of them. */
const PROGRESS_FIELDS = [
  "sourceCloseProof", "targetCloseProof", "fillSeq", "cumulativeFillRatio", "fillNumerator", "fillDenominator",
  "filledSourceAmount", "filledTargetAmount", "pendingClearRequestedAt", "claimedRatio", "sourceClaimed",
  "targetClaimed", "settledAt",
] as const satisfies readonly (keyof CrossRoute)[];
type RestingPull = Readonly<{ route: CrossRoute; binding: CrossPullBinding }>;
/** og validateCrossJurisdictionPullRoute, first half: a canonical route and binding, both resting with no progress. */
const restingPullRoute = (x: PullLockTx): Result<RestingPull, BodyError> => {
  const binding = x.crossJurisdiction, supplied = x.crossJurisdictionRoute;
  if (!binding || !supplied) return crossReject("CROSS_J_PULL_ROUTE_REQUIRED");
  const canonical = canonicalCrossRoute(supplied);
  if (!canonical.ok) return crossReject("CROSS_J_PULL_ROUTE_INVALID");
  const route = canonical.value;
  if (stableJson(route) !== stableJson(supplied)) return crossReject("CROSS_J_PULL_ROUTE_NOT_CANONICAL");
  const untouched = PROGRESS_FIELDS.every((k) => route[k] === undefined);
  const resting = route.status === "resting" && binding.status === "resting" && untouched;
  return resting ? ok({ route, binding }) : crossReject("CROSS_J_PULL_NOT_RESTING");
};
/** The pull opens exactly the route leg's prepared pull: same id, token, signed amount and hash material. */
const sameLegPull = (x: PullLockTx, pull: CrossPullLeg | undefined): boolean =>
  pull !== undefined && x.pullId === pull.pullId && Number(x.tokenId) === pull.tokenId && x.amount === pull.signedAmount
  && lowerText(x.fullHash) === lowerText(pull.fullHash) && lowerText(x.partialRoot) === lowerText(pull.partialRoot);
/**
 * og validateCrossJurisdictionPullRoute, second half: the binding is the route's own for its leg, the pull is that
 * leg's, and the leg's endpoints and jurisdiction are this Account's.
 */
const boundPull = (a: AccountBody, x: PullLockTx, { route, binding }: RestingPull): Result<void, BodyError> => {
  if (binding.leg !== "source" && binding.leg !== "target") return crossReject("CROSS_J_PULL_LEG_INVALID");
  const expected = crossPullBinding(route, binding.leg);
  const routeOwn = expected.ok && stableJson(binding) === stableJson(expected.value);
  if (!routeOwn) return crossReject("CROSS_J_PULL_BINDING_MISMATCH");
  const leg = route[binding.leg];
  if (!sameLegPull(x, route[`${binding.leg}Pull`])) return crossReject("CROSS_J_PULL_TERMS_MISMATCH");
  const ends = new Set([a.account.id.left.toLowerCase(), a.account.id.right.toLowerCase()]);
  const spans = ends.has(leg.entityId.toLowerCase()) && ends.has(leg.counterpartyEntityId.toLowerCase());
  if (!spans) return crossReject("CROSS_J_PULL_ENDPOINTS");
  const here = stackIdOf(a.terms.domain) === leg.jurisdiction.toLowerCase();
  return here ? ok(undefined) : crossReject("CROSS_J_PULL_JURISDICTION");
};
/** og pull slot: a colon-free new id under the pull caps, with 32-byte hash material. */
const pullSlot = (pulls: ReadonlyMap<string, PullRow>, x: PullLockTx): Result<void, BodyError> => {
  const crossPulls = [...pulls.values()].filter((p) => p.crossJurisdiction).length;
  switch (true) {
    case !x.pullId || x.pullId.includes(":"): return crossReject("CROSS_J_PULL_ID_INVALID");
    case pulls.has(x.pullId): return err({ _tag: "duplicate" });
    case pulls.size >= MAX_ACCOUNT_SWAP_OFFERS: return crossReject("CROSS_J_PULL_LIMIT");
    case Boolean(x.crossJurisdiction) && crossPulls >= MAX_ACCOUNT_CROSS_J_SWAP_OFFERS:
      return crossReject("CROSS_J_PULL_CROSS_LIMIT");
    case !WORD.test(x.fullHash) || !WORD.test(x.partialRoot): return crossReject("CROSS_J_PULL_HASH_INVALID");
    default: return ok(undefined);
  }
};
/** A live pull sharing hash material with this one, unless both belong to the same cross-j order. */
const collidingPull = (pulls: ReadonlyMap<string, PullRow>, x: PullLockTx): PullRow | undefined => {
  const orderOf = (b: { readonly orderId?: string } | undefined): string => String(b?.orderId || "").trim();
  const orderId = orderOf(x.crossJurisdiction);
  const sameOrder = (p: PullRow): boolean => orderId !== "" && orderOf(p.crossJurisdiction) === orderId;
  const shares = (p: PullRow): boolean =>
    lowerText(p.fullHash) === lowerText(x.fullHash) || lowerText(p.partialRoot) === lowerText(x.partialRoot);
  return [...pulls.values()].find((p) => !sameOrder(p) && shares(p));
};
/** og pull amount: a uint16 token, a non-zero amount within one payment, and room on the payer side. */
const pullAmount = (a: AccountBody, x: PullLockTx): Result<void, BodyError> => {
  const tk = Number(x.tokenId);
  switch (true) {
    case !Number.isSafeInteger(tk) || tk < 0 || tk > 65_535: return crossReject("CROSS_J_PULL_TOKEN_INVALID");
    case x.amount === 0n: return crossReject("CROSS_J_PULL_AMOUNT_ZERO");
    case absBig(x.amount) > MAX_PAYMENT_AMOUNT: return crossReject("CROSS_J_PULL_AMOUNT_RANGE");
    default: return ensureRoom(a, x.tokenId, absBig(x.amount), x.amount < 0n);
  }
};
/** og getPullLockAdmissionError: route, slot, unique hash material, then token, amount and payer capacity. */
const pullAdmission = (a: AccountBody, x: PullLockTx): Result<void, BodyError> => {
  const pulls = pullsOf(a);
  const unique = (): Result<void, BodyError> =>
    collidingPull(pulls, x) === undefined ? ok(undefined) : crossReject("CROSS_J_PULL_HASH_COLLISION");
  const route = chain(restingPullRoute(x), (resting) => boundPull(a, x, resting));
  return chain(route, () => chain(pullSlot(pulls, x), () => chain(unique(), () => pullAmount(a, x))));
};
/** og getPullLockAdmissionError's wording of a cross-j refusal. */
const pullRefusalText = (a: AccountBody, x: PullLockTx, reason: string): string => {
  switch (reason) {
    case "CROSS_J_PULL_ROUTE_REQUIRED": return "Cross-j pull requires both route and binding";
    case "CROSS_J_PULL_ROUTE_INVALID": {
      const c = canonicalCrossRoute(x.crossJurisdictionRoute);
      const why = c.ok ? reason : crossRouteErrorText(x.crossJurisdictionRoute, c.error.reason);
      return `Cross-j pull route invalid: ${why}`;
    }
    case "CROSS_J_PULL_ROUTE_NOT_CANONICAL": return "Cross-j pull route is not canonical";
    case "CROSS_J_PULL_NOT_RESTING": return "Cross-j pull opening must be a zero-progress resting route";
    case "CROSS_J_PULL_LEG_INVALID": return "Cross-j pull binding leg invalid";
    case "CROSS_J_PULL_BINDING_MISMATCH": return "Cross-j pull binding does not match route";
    case "CROSS_J_PULL_TERMS_MISMATCH": return "Cross-j pull terms do not match route";
    case "CROSS_J_PULL_ENDPOINTS": return "Cross-j pull Account endpoints do not match route leg";
    case "CROSS_J_PULL_JURISDICTION": return "Cross-j pull jurisdiction does not match Account domain";
    case "CROSS_J_PULL_ID_INVALID": return "Invalid pullId";
    case "CROSS_J_PULL_LIMIT": return "Too many open pulls";
    case "CROSS_J_PULL_CROSS_LIMIT": return `Too many open cross-j pulls: max ${MAX_ACCOUNT_CROSS_J_SWAP_OFFERS}`;
    case "CROSS_J_PULL_HASH_INVALID": return "Invalid pull hashladder commitment";
    case "CROSS_J_PULL_HASH_COLLISION":
      return `Pull hash material collides with live pull ${collidingPull(pullsOf(a), x)?.pullId}`;
    case "CROSS_J_PULL_TOKEN_INVALID": return "Invalid pull tokenId";
    case "CROSS_J_PULL_AMOUNT_ZERO": return "Pull amount must be non-zero";
    case "CROSS_J_PULL_AMOUNT_RANGE": return `Pull amount out of bounds: ${absBig(x.amount)}`;
    default: return reason;
  }
};
/** og getPullLockAdmissionError's text: pullAdmission's refusal rendered as og's message, or null when admitted. */
export const pullAdmissionText = (a: AccountBody, x: PullLockTx): string | null => {
  const r = pullAdmission(a, x);
  if (r.ok) return null;
  const e = r.error;
  switch (e._tag) {
    case "duplicate": return `Pull ${x.pullId} already exists`;
    case "insufficient_capacity": return `Insufficient pull capacity: need ${e.requested}, available ${e.available}`;
    case "cross_j": return pullRefusalText(a, x, e.reason);
    default: return e._tag;
  }
};
/** og handlePullLock: admission, the payer-side hold (uint256), and the PullCommitment at the frame's jHeight. */
const crossPullLock = (a: AccountBody, x: PullLockTx, ctx: FoldCtx): BodyStep => {
  const held = chain(pullAdmission(a, x), () => holdRoom(a, x.tokenId, absBig(x.amount), x.amount < 0n));
  return chain(held, () => map(noteErr(cloneCrossBinding(x.crossJurisdiction)), (crossJurisdiction) => {
    const row: PullRow = {
      pullId: x.pullId, tokenId: Number(x.tokenId), amount: x.amount, claimedRatio: 0, claimedAmount: 0n,
      fullHash: x.fullHash, partialRoot: x.partialRoot, crossJurisdiction,
      createdHeight: Number(ctx.jHeight), createdTimestamp: Number(ctx.nowMs),
    };
    return step({ ...a, pulls: mapSet(pullsOf(a), x.pullId, row) });
  }));
};
type PullCloseTx = TxOf<"cross_pull_close">;
type CloseProof = PullCloseTx["proof"];
/** What a close proof claims on this pull's leg: the cumulative amount of that leg. */
const claimedOnLeg = (binding: CrossPullBinding, proof: CloseProof): bigint =>
  binding.leg === "source" ? proof.cumulativeSourceAmount : proof.cumulativeTargetAmount;
/** The proof is this pull's: same order and route, this pull on its leg, claiming exactly the ratio's share. */
const provesPull = (pull: PullRow, binding: CrossPullBinding, proof: CloseProof): boolean => {
  const total = absBig(pull.amount);
  const share = proof.fillRatio >= MAX_FILL ? total : (total * BigInt(proof.fillRatio)) / BigInt(MAX_FILL);
  const pullOnLeg = binding.leg === "source" ? proof.sourcePullId : proof.targetPullId;
  return proof.orderId === binding.orderId && lowerText(proof.routeHash) === lowerText(binding.routeHash)
    && pullOnLeg === pull.pullId && claimedOnLeg(binding, proof) === share;
};
/**
 * og validateCrossPullCloseEvidence: a uint16 ratio and known close mode, a proof bound to this pull, the binary
 * behind its hash, and a hash-ladder reveal at exactly that ratio.
 */
const closeEvidence = (pull: PullRow, binding: CrossPullBinding, x: PullCloseTx): Result<number, BodyError> => {
  const { binary, proof } = x;
  switch (true) {
    case !Number.isSafeInteger(proof.fillRatio) || proof.fillRatio < 0 || proof.fillRatio > MAX_FILL:
      return crossReject("CROSS_J_CLOSE_RATIO_RANGE");
    case !CLOSE_MODES.includes(proof.closeMode): return crossReject("CROSS_J_CLOSE_MODE_INVALID");
    case !provesPull(pull, binding, proof): return crossReject("CROSS_J_CLOSE_PROOF_MISMATCH");
  }
  const commitment = { fullHash: pull.fullHash, partialRoot: pull.partialRoot };
  return chain(noteErr(crossCloseBinaryHash(binary)), (h) => {
    if (h.toLowerCase() !== String(proof.binaryHash).toLowerCase()) return crossReject("CROSS_J_CLOSE_BINARY_HASH");
    return chain(noteErr(verifyHashLadderBinary(commitment, binary)), (d) =>
      d.fillRatio === proof.fillRatio ? ok(proof.fillRatio) : crossReject("CROSS_J_CLOSE_RATIO_MISMATCH"));
  });
};
/** The source leg's close retires the cross-j offer that rested on it. */
const retiresOffer = (a: AccountBody, binding: CrossPullBinding): boolean =>
  binding.leg === "source" && a.offers.get(binding.orderId)?.crossJurisdiction !== undefined;
/**
 * og handleCrossPullClose: only the leg's hub closes. The whole hold is released and the claimed part moves to the
 * beneficiary. The source hub is the beneficiary and the target hub the payer.
 */
const crossPullClose = (a: AccountBody, x: PullCloseTx, ctx: FoldCtx): BodyStep => {
  const pull = a.pulls?.get(x.pullId);
  if (pull === undefined) return MISSING;
  const binding = pull.crossJurisdiction;
  if (!binding) return crossReject("CROSS_J_CLOSE_BINDING_MISSING");
  const beneficiaryIsLeft = pull.amount > 0n;
  const hubIsLeft = binding.leg === "source" ? beneficiaryIsLeft : !beneficiaryIsLeft;
  return chain(closeEvidence(pull, binding, x), (): BodyStep => {
    if (ctx.byLeft !== hubIsLeft) return crossReject("CROSS_J_CLOSE_NOT_HUB");
    const claimed = claimedOnLeg(binding, x.proof);
    const row = getDelta(a.account, String(pull.tokenId) as TokenId);
    const moved = claimed > 0n ? shift(row, beneficiaryIsLeft ? claimed : -claimed) : row;
    const released: AccountBody = { ...a, pulls: mapDelete(pullsOf(a), x.pullId) };
    return map(representable(released, moved), () => {
      const next = putState(released, setDelta(a.account, moved));
      return step(retiresOffer(next, binding) ? { ...next, offers: mapDelete(next.offers, binding.orderId) } : next);
    });
  });
};

// ---- the Account tx dispatcher: og account/tx/handlers/* ----
/** og direct-payment.ts buildPaymentForward: a trusted payer->gateway leg asks the gateway, and only it, to forward. */
const paymentForward = (x: TxOf<"payment">): readonly Of<Effect, "direct_payment_forward">[] => {
  const gateway = x.trustedGatewayEntityId;
  if (x.route === undefined || x.route.length <= 1 || gateway === undefined) return [];
  return [{
    _tag: "direct_payment_forward", tokenId: Number(x.tokenId), amount: x.amount, route: [...x.route],
    ...(x.description ? { description: x.description } : {}), trustedGatewayEntityId: gateway,
  }];
};
/**
 * og handlers/htlc/lock.ts:32-52,71-81,95-116 in order: identity, expiry, amount, 32-lock cap, capacity, int512
 * range, uint256 hold.
 */
const htlcLock = (a: AccountBody, x: TxOf<"htlc_lock">, ctx: FoldCtx): BodyStep => {
  switch (true) {
    case x.lockId !== x.hashlock: return err({ _tag: "lock_id" });
    case a.locks.has(x.lockId): return err({ _tag: "duplicate" });
    case ctx.nowMs >= x.timelock || x.revealBeforeHeight <= ctx.jHeight: return err({ _tag: "htlc_expired" });
    case !inPaymentRange(x.amount): return err({ _tag: "non_positive_payment" });
    case a.locks.size >= MAX_ACCOUNT_HTLC_LOCKS: return err({ _tag: "htlc_lock_capacity" });
  }
  // og lock.ts: an envelope must be an opaque encrypted layer; the Account commits only its hash.
  const envelopeHash = x.envelope === undefined ? undefined : htlcEnvelopeHash(x.envelope);
  if (envelopeHash === null) return err({ _tag: "htlc_envelope" });
  const lock: HtlcLock = {
    lockId: x.lockId, hashlock: x.hashlock, timelock: x.timelock, revealBeforeHeight: x.revealBeforeHeight,
    amount: x.amount, tokenId: x.tokenId, senderIsLeft: ctx.byLeft,
    createdHeight: floor0(ctx.accountHeight - 1n), createdTimestamp: ctx.nowMs, ...opt("envelopeHash", envelopeHash),
  };
  const fits = checks(
    ensureRoom(a, x.tokenId, x.amount, ctx.byLeft),
    representable(a, getDelta(a.account, x.tokenId), { senderIsLeft: ctx.byLeft, amount: x.amount }),
    holdRoom(a, x.tokenId, x.amount, ctx.byLeft),
  );
  return map(fits, () => step({ ...a, locks: mapSet(a.locks, x.lockId, lock) }));
};
/**
 * og handlers/htlc/resolve.ts: a secret pays only before expiry. An error refund is the beneficiary's to give any
 * time and the payer's to take after expiry; a timeout is a timeout only once expired.
 */
const htlcResolve = (a: AccountBody, x: TxOf<"htlc_resolve">, ctx: FoldCtx): BodyStep<EffectOf<"htlc_resolve">> => {
  const live = a.locks.get(x.lockId);
  if (live === undefined || !a.account.deltas.has(live.tokenId)) return MISSING;
  const expired = htlcExpired(live, ctx);
  const released: AccountBody = { ...a, locks: mapDelete(a.locks, x.lockId) };
  if (x.outcome === "error") {
    const beneficiary = ctx.byLeft !== live.senderIsLeft;
    if (!expired && (!beneficiary || x.reason === "timeout")) return err({ _tag: "before_deadline" });
    const refunded = {
      _tag: "htlc_error" as const, lockId: live.lockId, hashlock: live.hashlock, tokenId: Number(live.tokenId),
      amount: live.amount, ...opt("reason", x.reason),
    };
    return ok(step(released, [refunded]));
  }
  if (expired) return err({ _tag: "htlc_expired" });
  if (hashHtlcSecret(x.secret) !== live.hashlock) return err({ _tag: "preimage" });
  return chain(offdeltaChange(live.senderIsLeft, live.amount), (by) => {
    const moved = shift(getDelta(a.account, live.tokenId), by);
    const revealed = { _tag: "forward_secret" as const, hashlock: live.hashlock, secret: x.secret };
    return map(representable(released, moved), () => step(putState(released, setDelta(a.account, moved)), [revealed]));
  });
};
/** og lifecycle/cancel.ts: the maker only asks; the offer and its hold stay until the counterparty's swap_resolve. */
const swapCancelRequest = (
  a: AccountBody, x: TxOf<"swap_cancel_request">, ctx: FoldCtx,
): BodyStep<EffectOf<"swap_cancel_request">> => {
  const offer = a.offers.get(x.offerId);
  if (offer === undefined) return MISSING;
  if (ctx.byLeft !== offer.makerIsLeft) return err({ _tag: "not_maker" });
  return ok(step(a, [{ _tag: "swap_cancel_requested", offerId: x.offerId }]));
};
type Arms = { readonly [K in AccountTx["type"]]: (tx: WireTxOf<K>) => BodyStep<EffectOf<K>> };
const applyArm = (a: AccountBody, tx: WireAccountTx, ctx: FoldCtx): BodyStep<Effect> => {
  const lend = (x: AccountLendingTx): BodyStep => lending(a, x, ctx);
  return matchBy<"type", WireAccountTx, BodyStep<Effect>>("type", tx, {
    add_delta: (x) => {
      const added = (): AccountBody => putState(a, setDelta(a.account, zeroDelta(x.tokenId)));
      return ok(step(a.account.deltas.has(x.tokenId) ? a : added()));
    },
    set_credit_limit: (x) => {
      const limited = updateDelta(a.account, x.tokenId, (d) => setCreditLimit(d, x.limit, ctx.byLeft));
      return map(limited, (s) => step(putState(a, s)));
    },
    payment: (x) => chain(paymentRoute(a, x, ctx.byLeft), () =>
      map(spend(a, x.tokenId, x.amount, ctx.byLeft), (b) => step(b, paymentForward(x)))),
    htlc_lock: (x) => htlcLock(a, x, ctx),
    htlc_resolve: (x) => htlcResolve(a, x, ctx),
    swap_offer: (x) => swapOffer(a, x, ctx),
    swap_cancel_request: (x) => swapCancelRequest(a, x, ctx),
    swap_resolve: (x) => swapResolve(a, x, ctx),
    request_collateral: (x) => requestCollateral(a, x, ctx),
    rebalance_refund: (x) => rebalanceRefund(a, x, ctx),
    rebalance_policy: (x) => rebalancePolicy(a, x, ctx),
    lending_fund: lend, lending_borrow_request: lend, lending_repay: lend,
    lending_credit: lend, lending_close_request: lend, lending_close_payout: lend,
    cross_pull_lock: (x) => crossPullLock(a, x, ctx),
    cross_pull_close: (x) => crossPullClose(a, x, ctx),
    j_event_claim: (x) => claimJ(a, x, ctx),
    settle_transition: (x) => settleTransition(a, x, ctx),
  } satisfies Arms);
};
const one = (x: { readonly tokenId: TokenId }): readonly string[] => [x.tokenId];
const none = (): readonly string[] => [];
/** Every token id a tx names; each must be a valid uint16 id before any handler runs. */
const namedTokens = (tx: WireAccountTx): readonly string[] => matchBy("type", tx, {
  add_delta: one, set_credit_limit: one, payment: one, htlc_lock: one, htlc_resolve: none,
  swap_offer: (x) => [x.giveTokenId, x.wantTokenId],
  swap_cancel_request: none,
  swap_resolve: (x) => (x.feeTokenId === undefined ? [] : [x.feeTokenId]),
  request_collateral: (x) => (x.feeTokenId === undefined ? [x.tokenId] : [x.tokenId, x.feeTokenId]),
  rebalance_refund: (x) => [x.requestTokenId],
  rebalance_policy: one,
  lending_fund: one, lending_borrow_request: one, lending_repay: one, lending_credit: one,
  lending_close_request: none, lending_close_payout: one,
  cross_pull_lock: one, cross_pull_close: none,
  j_event_claim: (x) => x.events.flatMap((row) => row.tokens.map((tk) => tk.tokenId.toString())),
  settle_transition: none,
});
/** A tx commits only when its post-state still prepares and, above L0, og's wire encoding accepts it. */
const commits = (before: AccountBody, tx: WireAccountTx, next: AccountStep): BodyStep<Effect> => {
  const wireAccepted = (): Result<void, Uncommitted> =>
    mapErr(isL0Tx(tx) ? ok(undefined) : txRefusal(wireOf(tx)), uncommitted);
  return chain(mapErr(prepareStep(before, next.state), uncommitted), () => map(wireAccepted(), () => next));
};
export const applyAccountBody: Layer<AccountBody, WireAccountTx, FoldCtx, Effect, BodyError> = (a, tx, ctx) => {
  const frozen = settlementFreeze(a, tx);
  if (!frozen.ok) return frozen;
  const unfit = namedTokens(tx).find((n) => !tokenId(n).ok);
  if (unfit !== undefined) return err({ _tag: "token_id", tokenId: unfit });
  return chain(applyArm(a, tx, ctx), (next) =>
    next.state.account.deltas.size > MAX_ROWS ? err({ _tag: "too_many_rows" }) : commits(a, tx, next));
};

// ---- Account tx messages: the lines og account/tx/handlers/* log for an applied tx ----
const shortId = (offerId: string): string => `${offerId.slice(0, 8)}...`;
/** og swap/resolve messages: the fill, the taker fee, and what became of the offer. */
const swapResolveMessages = (before: AccountBody, after: AccountBody, tx: TxOf<"swap_resolve">): readonly string[] => {
  const offer = before.offers.get(tx.offerId);
  if (offer === undefined) return [];
  // The tx applied, so without explicit execution its ratio was 0 and nothing filled.
  const give = tx.executionGiveAmount ?? 0n, want = tx.executionWantAmount ?? 0n, fee = tx.feeAmount ?? 0n;
  const rest = after.offers.get(tx.offerId);
  const outcome = (): string => {
    if (rest !== undefined) return `partially filled, ${rest.giveAmount} remaining`;
    if (tx.fillRatio === MAX_FILL) return "fully filled";
    return tx.cancelRemainder || tx.fillRatio === 0 ? "cancelled" : "filled remainder dropped below lot size";
  };
  return [
    ...(give > 0n ? [`💱 Swap filled: ${give} token${offer.giveTokenId} for ${want} token${offer.wantTokenId}`] : []),
    ...(fee > 0n ? [`💸 Swap taker fee: ${fee} token${tx.feeTokenId ?? offer.wantTokenId}`] : []),
    `📊 Swap offer ${shortId(tx.offerId)} ${outcome()}`,
  ];
};
const collateralMessages = (before: AccountBody, tx: TxOf<"request_collateral">): readonly string[] => {
  const existing = before.requested.get(tx.tokenId) ?? 0n;
  if (existing > 0n) {
    return [`ℹ️ request_collateral skipped: pending request is immutable token=${tx.tokenId} amount=${existing}`];
  }
  const feeToken = tx.feeTokenId ?? tx.tokenId;
  const amount = requestNetAmount(tx);
  if (amount <= 0n) {
    const why = `prepaid fee consumes the full request (fee=${tx.feeAmount}, token=${feeToken})`;
    return [`ℹ️ Collateral request skipped: ${why}`];
  }
  const prepaid = tx.feeAmount > 0n ? `, prepaidFee=${tx.feeAmount}` : "";
  return [`🔄 Collateral requested: ${amount} token ${tx.tokenId}${prepaid} (hub will deposit R→C)`];
};
const policyMessages = (before: AccountBody, tx: TxOf<"rebalance_policy">, byLeft: boolean): readonly string[] => {
  const held = before.feePolicies.get(tx.tokenId);
  switch (policyStanding(at(held?.left, held?.right, byLeft), tx.policyVersion)) {
    case "stale": return [`rebalance_policy: stale v${tx.policyVersion} ignored`];
    case "current": return [`rebalance_policy: exact v${tx.policyVersion} retry`];
    case "newer": {
      const side = byLeft ? "left" : "right";
      return [`rebalance_policy: ${side} published v${tx.policyVersion} token=${Number(tx.tokenId)}`];
    }
  }
};
const pullCloseMessages = (before: AccountBody, tx: PullCloseTx): readonly string[] => {
  const pull = before.pulls?.get(tx.pullId);
  if (pull === undefined) return [];
  const binding = pull.crossJurisdiction;
  const claimed = claimedOnLeg(binding, tx.proof);
  const closed = `🪝 Cross-j pull closed: ${shortId(tx.pullId)} ratio ${tx.proof.fillRatio}/${MAX_FILL}`
    + ` claimed ${claimed} released ${absBig(pull.amount) - claimed}`;
  const retired = `🌉 Cross-j offer ${binding.orderId.slice(0, 8)} closed with pull`;
  return retiresOffer(before, binding) ? [closed, retired] : [closed];
};
/** og j-claim-transition.ts: stale at or below finality, final with the peer's claim, idempotent, else retained. */
const jClaimMessages = (before: AccountBody, tx: TxOf<"j_event_claim">, byLeft: boolean): readonly string[] => {
  const own = claimRowOf(tx, byLeft);
  if (!own.ok) return [];
  const { onLeft, jHeight } = own.value;
  const held = before.claimRows ?? [];
  const heldBy = (left: boolean): boolean => held.some((h) => h.onLeft === left && h.jHeight === jHeight);
  switch (true) {
    case jHeight <= before.finalizedJHeight: return ["ℹ️ j_event_claim stale"];
    case heldBy(!onLeft): return ["✅ J-event claim finalized bilaterally"];
    case heldBy(onLeft): return ["ℹ️ j_event_claim idempotent"];
    default: return ["📥 J-event claim authenticated and retained"];
  }
};
const settleMessages = (after: AccountBody, tx: SettleTransitionTx): readonly string[] => {
  switch (tx.kind) {
    case "upsert": return [`Settlement workspace v${after.settlement?.revision ?? tx.revision} committed`];
    case "hanko": return [`Settlement workspace v${tx.revision} Hanko attached`];
    case "submit": return [`Settlement workspace v${tx.revision} submitted`];
    default: return [`Settlement workspace v${tx.revision} cleared`];
  }
};
/**
 * og ApplyAccountTxResult `events`: the handler messages of one applied Account tx, from `self`'s side, as og
 * account/tx/handlers/* word them. `before` and `after` are the body around the successful application of `tx`
 * under `ctx` (og byLeft = the frame proposer's side).
 */
export const accountTxMessages = (
  before: AccountBody, tx: WireAccountTx, ctx: FoldCtx, after: AccountBody, self: string,
): readonly string[] => {
  const { left, right } = before.account.id;
  const selfIsLeft = lower(self) === lower(left);
  const ours = ctx.byLeft === selfIsLeft;
  const payment = (tokenId: string, amount: bigint, route: readonly string[], note?: string): readonly string[] => {
    const tail = note ? `(${note})` : "";
    const head = ours
      ? `💸 Sent ${amount} token ${tokenId} to Entity ${at(right, left, selfIsLeft).slice(-4)} ${tail}`
      : `💰 Received ${amount} token ${tokenId} from Entity ${at(left, right, ctx.byLeft).slice(-4)} ${tail}`;
    // og buildPaymentForward: only the receiving gateway of a multi-hop route forwards.
    const forward = `↪️ Forwarding payment to ${String(route.at(-1)).slice(-4)} via ${route.length - 1} more hops`;
    return !ours && route.length > 1 ? [head, forward] : [head];
  };
  const creditLimit = (tokenId: TokenId, amount: bigint): readonly string[] => [
    ...(before.account.deltas.has(tokenId) ? [] : [`📊 Created delta for token ${tokenId}`]),
    `💳 ${ctx.byLeft ? "Right" : "Left"} credit limit = ${amount} for token ${tokenId}`,
  ];
  switch (tx.type) {
    case "add_delta": return before.account.deltas.has(tx.tokenId) ? [] : [`➕ Added token ${tx.tokenId} to account`];
    case "set_credit_limit": return creditLimit(tx.tokenId, tx.limit);
    case "payment": return payment(tx.tokenId, tx.amount, tx.route ?? [], tx.description);
    case "lending_fund": return payment(tx.tokenId, tx.amount, [tx.hubEntityId], `xln:${tx.type}`);
    case "lending_repay": return payment(tx.tokenId, tx.amount, [tx.hubEntityId], `xln:${tx.type}`);
    case "lending_close_payout": return payment(tx.tokenId, tx.amount, [tx.lenderEntityId], `xln:${tx.type}`);
    case "lending_credit": return creditLimit(tx.tokenId, tx.creditLimit);
    case "lending_borrow_request": return [`Lending borrow request ${tx.requestId} committed`];
    case "lending_close_request": return [`Lending close request ${tx.positionId} committed`];
    case "htlc_lock": {
      const expiry = `expires block ${tx.revealBeforeHeight}, hash ${tx.hashlock.slice(0, 16)}...`;
      return [`🔒 HTLC locked: ${tx.amount} token ${tx.tokenId}, ${expiry}`];
    }
    case "htlc_resolve": {
      const lock = before.locks.get(tx.lockId);
      if (lock === undefined) return [];
      const what = `${lock.amount} token ${lock.tokenId}`;
      return tx.outcome === "secret"
        ? [`🔓 HTLC resolved (secret): ${what}`]
        : [`❌ HTLC resolved (error): ${what} returned — ${tx.reason || "unknown"}`];
    }
    case "swap_offer": {
      const offer = after.offers.get(tx.offerId);
      if (offer === undefined) return [];
      const terms = `give ${offer.giveAmount} token${tx.giveTokenId} for ${offer.wantAmount} token${tx.wantTokenId}`;
      return [`📊 Swap offer created: ${shortId(tx.offerId)} ${terms}`];
    }
    case "swap_cancel_request": return [`📨 Swap cancel requested: ${shortId(tx.offerId)}`];
    case "swap_resolve": return swapResolveMessages(before, after, tx);
    case "request_collateral": return collateralMessages(before, tx);
    case "rebalance_refund": {
      const fees = before.requestFees.get(tx.requestTokenId);
      if (fees === undefined) return [];
      const refunded = (fees.refund?.refundedAmount ?? 0n) + tx.amount;
      return [`Rebalance refund ${tx.requestId}: ${refunded}/${fees.feePaidUpfront}`];
    }
    case "rebalance_policy": return policyMessages(before, tx, ctx.byLeft);
    case "cross_pull_lock": return [`🪝 Pull locked: ${shortId(tx.pullId)} amount ${tx.amount} token${tx.tokenId}`];
    case "cross_pull_close": return pullCloseMessages(before, tx);
    case "j_event_claim": return jClaimMessages(before, tx, ctx.byLeft);
    case "settle_transition": return settleMessages(after, tx);
  }
};
/**
 * og proposal/transactions.ts + incoming replay: every tx's handler messages across a frame folded from `s`; [] if
 * the frame does not fold.
 */
export const frameTxMessages = (
  s: AccountBody, f: AccountFrame, byLeft: boolean, self: string, settlement?: SettlementCtx,
): readonly string[] => {
  const ctx = foldCtx(f, byLeft, settlement);
  type Replay = Readonly<{ body: AccountBody; messages: readonly string[] }>;
  const replay = (r: Replay, tx: WireAccountTx): Result<Replay, BodyError> =>
    map(applyAccountBody(r.body, tx, ctx), (next) => ({
      body: next.state, messages: [...r.messages, ...accountTxMessages(r.body, tx, ctx, next.state, self)],
    }));
  return unwrapOr(map(foldResult(f.txs, { body: s, messages: [] }, replay), (r) => r.messages), () => []);
};
// ---- og per-tx failure text: the rejection or thrown Error og's handler produces for a tx this body refuses ----
// The handlers above answer with typed refusals. og answers with text, and some of its handlers throw instead,
// which aborts the whole Entity input. This section walks og's checks in og's order to reproduce that text.
/**
 * og ApplyAccountTxResult rejection / thrown Error of one Account tx: og's own message text, and whether og's
 * handler throws (a thrown handler error aborts the whole Entity input; a rejection becomes
 * `Frame application failed: <message>` dispute evidence on replay).
 */
export type AccountTxFailure = { readonly thrown: boolean; readonly message: string };
const refusedTx = (message: string): AccountTxFailure => ({ thrown: false, message });
const threwTx = (message: string): AccountTxFailure => ({ thrown: true, message });
const refuse = (fails: boolean, message: string): AccountTxFailure | null => (fails ? refusedTx(message) : null);
const raise = (fails: boolean, message: string): AccountTxFailure | null => (fails ? threwTx(message) : null);
const refuseIssue = (issue: string | undefined): AccountTxFailure | null =>
  issue === undefined ? null : refusedTx(issue);
type FailureCheck = () => AccountTxFailure | null | undefined;
const pass: FailureCheck = () => null;
const sideName = (onLeft: boolean): string => (onLeft ? "left" : "right");
/** og's checks run in order and the first failure is the answer; later checks never run. */
const firstFailure = (...checks: readonly FailureCheck[]): AccountTxFailure | null =>
  checks.reduce<AccountTxFailure | null>((found, check) => found ?? check() ?? null, null);
const checked = (f: AccountTxFailure | null | undefined): AccountTxFailure | null => f ?? null;
const U256 = MAX_PAYMENT_AMOUNT.toString();
const ogTxType = (tx: WireAccountTx): string => (tx.type === "payment" ? "direct_payment" : tx.type);
const tokenKey = (tk: TokenId | number): TokenId => String(Number(tk)) as TokenId;
/**
 * og createDeltaDraft (delta-utils.ts): a bad token id or a new row past MAX_ACCOUNT_TOKEN_ROWS is an
 * AccountDeltaError.
 */
const deltaDraftError = (a: AccountBody, tk: TokenId | number): string | null => {
  const n = Number(tk);
  if (!Number.isSafeInteger(n) || n < 0 || n > 65_535) return `ACCOUNT_DELTA_TOKEN_INVALID:${String(n)}`;
  const rows = a.account.deltas.size + 1;
  const fits = a.account.deltas.has(tokenKey(n)) || rows <= MAX_ROWS;
  return fits ? null : `ACCOUNT_DELTA_ROW_LIMIT_EXCEEDED:insert:${rows}:${MAX_ROWS}`;
};
const draftThrow = (a: AccountBody, tk: TokenId | number): FailureCheck => () => {
  const e = deltaDraftError(a, tk);
  return e === null ? null : threwTx(e);
};
/** og deriveDelta(delta, isLeft).outCapacity over the committed row (holds and allowances included). */
const ogOutCapacity = (a: AccountBody, tk: TokenId | number, isLeft: boolean): bigint =>
  outCapacity(getDelta(a.account, tokenKey(tk)), isLeft, holds(a, tokenKey(tk), isLeft));
/** og's capacity refusal, worded by the caller around what the side has available. */
const capacityCheck = (
  a: AccountBody, tk: TokenId | number, isLeft: boolean, amount: bigint, text: (available: bigint) => string,
): FailureCheck => () => {
  const available = ogOutCapacity(a, tk, isLeft);
  return refuse(amount > available, text(available));
};
const offdeltaText = (a: AccountBody, d: Delta, added?: LockMove, removedLockId?: string): FailureCheck => () => {
  const out = offdeltaOutside(a, d, added, removedLockId);
  return out === null ? null : refusedTx(`Offdelta outside int512: ${out}`);
};
/** The offdelta range check of a row after a plain transfer from one side. */
const paidRowText = (a: AccountBody, tk: TokenId, payerIsLeft: boolean, amount: bigint): FailureCheck =>
  offdeltaText(a, shift(getDelta(a.account, tk), payerIsLeft ? -amount : amount));
const holdOverflowText = (
  a: AccountBody, tk: TokenId | number, isLeft: boolean, amount: bigint,
): FailureCheck => () => {
  const totals = sideTotals(a, tokenKey(tk));
  const held = at(totals.leftHold, totals.rightHold, isLeft);
  const text = `HOLD_ADD_OVERFLOW:${sideName(isLeft)} hold=${held} amount=${amount}`;
  return refuse(held + amount > MAX_PAYMENT_AMOUNT, text);
};

// ---- og failure text: HTLC envelopes, payments, credit limits ----
/** How og describes a malformed envelope: its kind, or its keys, version and ciphertext length. */
const envelopeShape = (v: unknown): string => {
  if (v === null) return "null";
  if (Array.isArray(v)) return "array";
  if (typeof v !== "object") return typeof v;
  const rec = v as Record<string, unknown>, ct = rec["ciphertext"];
  const version = `version=${typeof rec["version"]}:${String(rec["version"] ?? "").slice(0, 40)}`;
  const ciphertext = `ciphertext=${typeof ct}:${typeof ct === "string" ? ct.length : -1}`;
  return [`keys=${Object.keys(rec).sort().join(",")}`, version, ciphertext].join(":");
};
/** Exactly a version and a non-empty ciphertext no longer than the largest packed layer encodes to. */
const opaqueShape = (rec: Record<string, unknown>): boolean => {
  const keys = Object.keys(rec), ct = rec["ciphertext"];
  const exactKeys = keys.length === 2 && keys.includes("ciphertext") && keys.includes("version");
  const sized = typeof ct === "string" && ct.length > 0 && ct.length <= Math.ceil(MAX_HTLC_PACKED_BYTES / 3) * 4;
  return exactKeys && rec["version"] === HTLC_ENVELOPE_VERSION && sized;
};
/**
 * og assertOpaqueHtlcCiphertext (thrown from handleHtlcLock's encryptedHtlcLayer): shape, canonical base64, packed
 * size.
 */
const envelopeError = (v: unknown): string | null => {
  const record = v !== null && typeof v === "object" && !Array.isArray(v);
  if (!record || !opaqueShape(v as Record<string, unknown>)) {
    return `HTLC_OPAQUE_CIPHERTEXT_INVALID:${envelopeShape(v)}`;
  }
  const packed = decodeBase64((v as { ciphertext: string }).ciphertext);
  if (packed === null) return "HTLC_OPAQUE_CIPHERTEXT_BASE64_INVALID";
  return packed.length < 48 || packed.length > MAX_HTLC_PACKED_BYTES ? "HTLC_OPAQUE_CIPHERTEXT_SIZE_INVALID" : null;
};
/** og validatePaymentEnvelope / validatePaymentRoute wording of a paymentRoute refusal. */
const paymentRouteText = (reason: string, x: TxOf<"payment">, hops: number): string => {
  switch (reason) {
    case "non_positive_payment": return `Invalid payment amount: ${x.amount} (min 1, max ${U256})`;
    case "ROUTE_LENGTH": return `Route too long: ${hops} hops (max ${MAX_ROUTE_HOPS})`;
    case "DELIVERY_MODE": return "Payment delivery mode must be direct or trusted";
    case "DIRECT_WITH_GATEWAY": return "Direct payment forbids a trusted gateway";
    case "TRUSTED_WITHOUT_GATEWAY": return "Trusted payment requires one declared gateway";
    case "DIRECTION": return "FATAL: Payment direction must match the frame proposer";
    case "DIRECT_ROUTE": return "Direct payment route must contain only the bilateral recipient";
    case "GATEWAY_FINAL_LEG": return "Trusted gateway final leg must contain only the recipient";
    default: return "Trusted payment must be source → declared gateway → recipient";
  }
};
/** og handleDirectPayment: envelope, direction, route, the payer's capacity, then the int512 range. */
const paymentFailure = (a: AccountBody, x: TxOf<"payment">, byLeft: boolean): AccountTxFailure | null => {
  const { left, right } = a.account.id;
  const from = at(left, right, byLeft);
  const routed = paymentRoute(a, x, byLeft);
  if (!routed.ok) {
    const e = routed.error;
    const hops = (x.route ?? [at(right, left, byLeft)]).length;
    return refusedTx(paymentRouteText(e._tag === "payment_route" ? e.reason : e._tag, x, hops));
  }
  return firstFailure(
    draftThrow(a, x.tokenId),
    capacityCheck(a, x.tokenId, byLeft, x.amount,
      (available) => `Insufficient capacity for sender ${from.slice(-4)}: need ${x.amount}, available ${available}`),
    paidRowText(a, x.tokenId, byLeft, x.amount),
  );
};
/** og handleSetCreditLimit: sign, uint256 ceiling, then the (caught) delta-row errors. */
const creditLimitFailure = (a: AccountBody, tk: TokenId, limit: bigint): AccountTxFailure | null => firstFailure(
  () => refuse(limit < 0n, `Credit limit cannot be negative: ${limit}`),
  () => refuse(limit > MAX_CREDIT_LIMIT, `Credit limit exceeds maximum: ${limit} > ${U256}`),
  () => refuseIssue(deltaDraftError(a, tk) ?? undefined),
);

// ---- og failure text: swap offers ----
/** og swap-limits.ts: a maker's per-market cap; a market key that cannot be derived throws. */
const marketCapFailure = (a: AccountBody, x: OfferTx, makerIsLeft: boolean): AccountTxFailure | null => {
  const key = offerMarketKey(x);
  if (!key.ok) return threwTx(bodyErrorCode(key.error));
  const makers = [...a.offers.values()].filter((o) => o.makerIsLeft === makerIsLeft);
  const resting = makers.filter((o) => unwrapOr(offerMarketKey(o), () => "") === key.value).length;
  const cap = MAX_SWAP_OFFERS_PER_SIDE_PER_MARKET;
  const text = `Too many open swap offers for ${key.value} on ${sideName(makerIsLeft)} side: max ${cap}`;
  return refuse(resting >= cap, text);
};
/** og admission.ts: limits, shape, net authority, venue, time in force, the cross-j proposer, then the market cap. */
const offerAdmissionFailure = (
  a: AccountBody, x: OfferTx, ctx: FoldCtx, makerIsLeft: boolean,
): AccountTxFailure | null => {
  const route = x.crossJurisdiction;
  const offers = [...a.offers.values()];
  const sameJ = offers.filter((o) => o.crossJurisdiction === undefined).length;
  const decimals = `give=${String(x.giveTokenDecimals)} want=${String(x.wantTokenDecimals)}`;
  const prepared = route !== undefined && route.status === "resting" && route.sourcePull && route.targetPull;
  return firstFailure(
    () => refuse(x.offerId.includes(":"), `Invalid offerId: colons not allowed (got ${x.offerId})`),
    () => refuse(a.offers.has(x.offerId), `Offer ${x.offerId} already exists`),
    () => refuse(offers.length >= MAX_ACCOUNT_SWAP_OFFERS, `Too many open swap offers: max ${MAX_ACCOUNT_SWAP_OFFERS}`),
    () => refuse(route === undefined && sameJ >= MAX_ACCOUNT_SAME_J_SWAP_OFFERS,
      `Too many open same-j swap offers: max ${MAX_ACCOUNT_SAME_J_SWAP_OFFERS}`),
    () => refuse(route !== undefined && offers.length - sameJ >= MAX_ACCOUNT_CROSS_J_SWAP_OFFERS,
      `Too many open cross-j swap offers: max ${MAX_ACCOUNT_CROSS_J_SWAP_OFFERS}`),
    () => refuse(!decimalsOk(x.giveTokenDecimals) || !decimalsOk(x.wantTokenDecimals),
      `Invalid token decimals: ${decimals}`),
    () => refuse(!inPaymentRange(x.giveAmount), `Invalid giveAmount: ${x.giveAmount} (min 1, max ${U256})`),
    () => refuse(!inPaymentRange(x.wantAmount), `Invalid wantAmount: ${x.wantAmount} (min 1, max ${U256})`),
    () => refuse(x.maxFee >= x.wantAmount || x.minNetReceive <= 0n, "SWAP_NET_AUTH_INITIAL_TERMS_INVALID"),
    () => refuseIssue(netAuthError(x, NO_FILL, false)),
    () => refuse(route !== undefined && (x.maxFee !== 0n || x.minNetReceive !== x.wantAmount),
      "CROSS_J_SWAP_NET_AUTH_INVALID"),
    () => refuse(route === undefined && x.giveTokenId === x.wantTokenId,
      `Cannot swap same token: ${Number(x.giveTokenId)}`),
    () => refuse(route !== undefined && !prepared, "Cross-j swap must be prepared before entering the book"),
    () => refuse(!timeInForce(x).ok, `Invalid timeInForce: ${String(x.timeInForce)}`),
    () => refuse(route !== undefined && !crossJMaker(a, route, ctx).ok,
      "Cross-j swap proposer must be the maker or source hub"),
    () => marketCapFailure(a, x, makerIsLeft),
  );
};
const quantizedBoundsFailure = (give: bigint, want: bigint): AccountTxFailure | null => firstFailure(
  () => refuse(!inPaymentRange(give), `Quantized giveAmount out of bounds: ${give} (min 1, max ${U256})`),
  () => refuse(!inPaymentRange(want), `Quantized wantAmount out of bounds: ${want} (min 1, max ${U256})`),
);
/** og commit.ts: net authority for the quantized amounts, the give row, then (same-j only) capacity and hold. */
const offerCommitFailure = (
  a: AccountBody, x: OfferTx, give: bigint, want: bigint, makerIsLeft: boolean,
): AccountTxFailure | null => {
  const sameJ = x.crossJurisdiction === undefined;
  const short = (available: bigint): string => `Insufficient capacity: need ${give}, available ${available}`;
  return firstFailure(
    () => { const r = requantizeAuth(x, give, want); return r.ok ? null : refusedTx(bodyErrorCode(r.error)); },
    draftThrow(a, x.giveTokenId),
    sameJ ? capacityCheck(a, x.giveTokenId, makerIsLeft, give, short) : pass,
    sameJ ? holdOverflowText(a, x.giveTokenId, makerIsLeft, give) : pass,
  );
};
const lotText = (lot: bigint): string => `Order too small for lot size (${lot} base units)`;
/** og same-j quantization: lot, canonical price, a maker tick within one step, then the quantized amounts. */
const sameJOfferFailure = (a: AccountBody, x: OfferTx, makerIsLeft: boolean): AccountTxFailure | null => {
  const d = swapDims(x);
  const { base, quote } = toMarket(d, x.giveAmount, x.wantAmount);
  const lot = lotScale(d.bd);
  if (base < lot) return refusedTx(lotText(lot));
  const prepared = preparedPrice(d, base, quote);
  if (prepared === undefined) return refusedTx("Invalid price ratio or order too small after canonical quantization");
  const input = x.priceTicks;
  if (input !== undefined && input <= 0n) return refusedTx(`Invalid explicit priceTicks: ${input}`);
  const drift = input === undefined ? 0n : input > prepared ? input - prepared : prepared - input;
  if (drift > 1n) {
    const detail = `expected ${prepared}, got ${input} (drift ${drift} > step 1)`;
    return refusedTx(`Price mismatch after deterministic quantization: ${detail}`);
  }
  const quantizedBase = (base / lot) * lot;
  const { give, want } = toOffer(d, quantizedBase, quoteAt(d.bd, d.qd, quantizedBase, input ?? prepared));
  return quantizedBoundsFailure(give, want) ?? offerCommitFailure(a, x, give, want, makerIsLeft);
};
const PAIRED_PULL_TEXT: Readonly<Record<string, string>> = {
  CROSS_J_SWAP_SOURCE_PULL_MISSING: "Cross-j swap offer requires paired source pull lock",
  CROSS_J_SWAP_SOURCE_PULL_MISMATCH: "Cross-j swap offer source pull mismatch",
  CROSS_J_SWAP_SOURCE_BINDING_MISMATCH: "Cross-j source pull binding mismatch",
};
/** og cross-j quantization and binding: the route's exact lot-aligned amounts, then its paired source pull. */
const crossJOfferFailure = (
  a: AccountBody, x: OfferTx, route: CrossRoute, makerIsLeft: boolean,
): AccountTxFailure | null => {
  const m = crossMarket(route);
  if (!m.ok) return threwTx(m.error.reason);
  const d = dimsAt(m.value.sourceIsBase ? 1 : 0, x);
  const { base, quote } = toMarket(d, x.giveAmount, x.wantAmount);
  const lot = lotScale(d.bd);
  if (base < lot) return refusedTx(lotText(lot));
  if (base % lot !== 0n) return refusedTx(`Cross-j base amount must align to lot size (${lot} base units)`);
  if (priceTicksOf(d, base, quote) <= 0n) {
    return refusedTx("Invalid cross-j price ratio or order too small after canonical quantization");
  }
  const changed = (leg: CrossLegRole, routeAmount: CrossLeg["amount"], offerAmount: bigint): FailureCheck =>
    () => refuse(
    offerAmount !== BigInt(routeAmount),
    `Cross-j ${leg} amount changed by quantization: route=${routeAmount} offer=${offerAmount}`,
  );
  const quantized = firstFailure(
    () => quantizedBoundsFailure(x.giveAmount, x.wantAmount),
    changed("source", route.source.amount, x.giveAmount),
    changed("target", route.target.amount, x.wantAmount),
  );
  if (quantized !== null) return quantized;
  const canonical = canonicalCrossRoute(route);
  if (!canonical.ok) return threwTx(crossRouteErrorText(route, canonical.error.reason));
  const paired = pairedSourcePull(a, x, route, canonical.value);
  if (!paired.ok) return refusedTx(PAIRED_PULL_TEXT[bodyErrorCode(paired.error)] ?? bodyErrorCode(paired.error));
  return offerCommitFailure(a, x, x.giveAmount, x.wantAmount, makerIsLeft);
};
/** og handlers/swap/offer: admission, then the venue's quantization, binding, net authority, capacity and hold. */
const swapOfferFailure = (a: AccountBody, x: OfferTx, ctx: FoldCtx): AccountTxFailure | null => {
  const route = x.crossJurisdiction;
  const makerIsLeft = route === undefined
    ? ctx.byLeft
    : lowerText(route.makerEntityId) === lowerText(a.account.id.left);
  return offerAdmissionFailure(a, x, ctx, makerIsLeft)
    ?? (route === undefined ? sameJOfferFailure(a, x, makerIsLeft) : crossJOfferFailure(a, x, route, makerIsLeft));
};

// ---- og failure text: swap resolve ----
const EXECUTION_TEXT: Readonly<Record<string, string>> = {
  SWAP_EXECUTION_PARTIAL: "executionGiveAmount and executionWantAmount must both be provided",
  SWAP_EXECUTION_REQUIRED: "executionGiveAmount and executionWantAmount required for non-zero fills",
};
const exactRatioText = (x: ResolveTx, reason: string, qG: bigint, fG: bigint): string => {
  switch (reason) {
    case "SWAP_EXACT_RATIO_PARTIAL": return "fillNumerator and fillDenominator must both be provided";
    case "SWAP_EXACT_RATIO_RANGE": return `Exact fill ratio out of range: ${x.fillNumerator}/${x.fillDenominator}`;
    default: return `Exact fill ratio mismatch: ${x.fillNumerator}/${x.fillDenominator} != ${fG}/${qG}`;
  }
};
/** og resolve economics: fee, net authority, execution against the offer and its limit, bounds, rows, capacity. */
const fillEconomicsFailure = (a: AccountBody, x: ResolveTx, o: SwapOffer, e: Execution): AccountTxFailure | null => {
  const qG = o.quantizedGive, qW = o.quantizedWant, fG = e.give, fW = e.want;
  const fee = x.feeAmount ?? 0n, feeToken = x.feeTokenId ?? o.wantTokenId;
  const fills = e.provided && (fG > 0n || fW > 0n);
  const limitText = (): string =>
    `Execution violates maker limit price: offer=${x.offerId} makerIsLeft=${o.makerIsLeft}`
    + ` effectiveGive=${qG} effectiveWant=${qW} filledGive=${fG} filledWant=${fW} lhs=${fW * qG} rhs=${fG * qW}`;
  const counterparty = other(o.makerIsLeft);
  return firstFailure(
    () => refuse(fee < 0n, "Swap taker fee must be >= 0"),
    () => refuse(fee > 0n && fG <= 0n, "Swap taker fee requires a non-zero fill"),
    () => refuse(fee > 0n && feeToken !== o.wantTokenId,
      `Swap taker fee token mismatch: expected ${Number(o.wantTokenId)}, got ${Number(feeToken)}`),
    () => refuse(fee >= fW && fW > 0n, `Swap taker fee ${fee} exceeds or equals filled receive amount ${fW}`),
    () => refuseIssue(netAuthError(o, { give: fG, want: fW, fee }, x.cancelRemainder)),
    () => refuse(fills && (fG <= 0n || fW <= 0n), "Execution amounts must both be positive for a fill"),
    () => refuse(e.provided && x.fillRatio !== e.ratio,
      `fillRatio ${x.fillRatio} does not match canonical execution ratio ${e.ratio}`),
    () => refuse(fills && fG > qG, `Execution give amount ${fG} exceeds offer limit ${qG}`),
    () => (fills && fW * qG < fG * qW ? refusedTx(limitText()) : null),
    () => refuse(e.ratio > 0 && !inPaymentRange(fG), `Filled give amount out of bounds: ${fG} (min 1, max ${U256})`),
    () => refuse(e.ratio > 0 && !inPaymentRange(fW), `Filled want amount out of bounds: ${fW} (min 1, max ${U256})`),
    draftThrow(a, o.giveTokenId),
    draftThrow(a, o.wantTokenId),
    fW <= 0n ? pass : capacityCheck(a, o.wantTokenId, counterparty, fW,
      (has) => `Counterparty insufficient capacity on token ${Number(o.wantTokenId)}: needs ${fW}, has ${has}`),
  );
};
/** og remainder.ts: the requantized remainder must fit the give still held and keep a valid authority. */
const remainderFailure = (o: SwapOffer, f: Fill): AccountTxFailure | null => {
  const d = swapDims(o);
  const unfilledGive = o.quantizedGive - f.give;
  const next = requantizeRemaining(d, toMarket(d, unfilledGive, o.quantizedWant - f.want).base, o.priceTicks);
  if (next === undefined) return null;
  if (unfilledGive - next.give < 0n) {
    return refusedTx(`Swap remainder exceeds held give: remaining=${unfilledGive} required=${next.give}`);
  }
  const r = requantizeAuth(o, next.give, next.want);
  return r.ok ? null : refusedTx(bodyErrorCode(r.error));
};
/** og handlers/swap/resolve: the resting offer, execution, exact ratio, economics, the two rows, then the remainder. */
const swapResolveFailure = (a: AccountBody, x: ResolveTx, ctx: FoldCtx): AccountTxFailure | null => {
  const o = a.offers.get(x.offerId);
  if (o === undefined) return refusedTx(`Offer ${x.offerId} not found`);
  if (o.crossJurisdiction !== undefined) {
    return refusedTx("Cross-jurisdiction offers settle through requestCrossJurisdictionClear/cross_pull_close");
  }
  if (!quotesResting(x, o)) return refusedTx("Resting swap terms mismatch live offer");
  if (ctx.byLeft === o.makerIsLeft) return refusedTx("Only counterparty can resolve swap");
  if (!Number.isInteger(x.fillRatio) || x.fillRatio < 0 || x.fillRatio > MAX_FILL) {
    return refusedTx(`Invalid fillRatio: ${x.fillRatio}`);
  }
  const execution = executionOf(x, o.quantizedGive);
  if (!execution.ok) return refusedTx(EXECUTION_TEXT[bodyErrorCode(execution.error)] ?? bodyErrorCode(execution.error));
  const e = execution.value;
  const exact = exactRatio(x, o.quantizedGive, e.give);
  if (!exact.ok) return refusedTx(exactRatioText(x, bodyErrorCode(exact.error), o.quantizedGive, e.give));
  const economics = fillEconomicsFailure(a, x, o, e);
  if (economics !== null) return economics;
  const fill: Fill = { give: e.give, want: e.want, fee: x.feeAmount ?? 0n };
  const fromMaker = (n: bigint): bigint => (o.makerIsLeft ? -n : n);
  const giveRow = shift(getDelta(a.account, o.giveTokenId), fromMaker(fill.give));
  const wantRow = shift(getDelta(a.account, o.wantTokenId), -fromMaker(fill.want - fill.fee));
  const closes = x.cancelRemainder || x.fillRatio === 0 || e.ratio === MAX_FILL;
  const remainder: FailureCheck = () => (closes ? null : remainderFailure(o, fill));
  return firstFailure(offdeltaText(a, giveRow), offdeltaText(a, wantRow), remainder);
};

// ---- og failure text: lending, cross-j pull close ----
/** og handleLendingAccountTx: intent id, role and counterparty checks throw, naming what they saw. */
const lendingPartiesFailure = (a: AccountBody, byLeft: boolean, role: LendingRole): AccountTxFailure | null => {
  const title = role.actorRole.toUpperCase();
  const left = lower(a.account.id.left), right = lower(a.account.id.right);
  const proposer = at(left, right, byLeft), claimed = lower(role.actor);
  const intent = lower(role.id);
  const expected = claimed === left ? right : left;
  return firstFailure(
    () => raise(!LENDING_INTENT.test(intent) || !intent.startsWith(`${role.prefix}-`),
      `LENDING_INTENT_ID_INVALID:${role.id}`),
    () => raise(!LENDING_ENTITY.test(claimed), `LENDING_${title}_INVALID:${role.actor}`),
    () => raise(claimed !== proposer, `LENDING_${title}_NOT_PROPOSER: claimed=${claimed} proposer=${proposer}`),
    () => raise(lower(role.counterparty) !== expected,
      `LENDING_COUNTERPARTY_INVALID: expected=${expected} got=${lower(role.counterparty)}`),
  );
};
/** og lending steps: amount, terms and replay throw; the owned-balance check and the payment itself refuse. */
const lendingStepFailure = (
  a: AccountBody, x: AccountLendingTx, role: LendingRole, byLeft: boolean,
): AccountTxFailure | null => {
  const replay: FailureCheck = () => raise(a.lendingIntents.has(intentKey(x)), `LENDING_INTENT_REPLAY:${intentKey(x)}`);
  const amount = (v: bigint, label: string): FailureCheck => () => raise(v <= 0n, `${label}_AMOUNT_MUST_BE_POSITIVE`);
  const terms = (term: unknown, bps: unknown): readonly FailureCheck[] => [
    () => raise(!LENDING_TERMS.has(term), `LENDING_INVALID_TERM: ${String(term)}`),
    () => raise(!interestOk(bps), `LENDING_INVALID_INTEREST_BPS: ${String(bps)}`),
  ];
  // The lender, borrower or hub pays the party across as a direct payment.
  const pay = (tk: TokenId, v: bigint): FailureCheck => () => paymentFailure(a, {
    type: "payment", tokenId: tk, amount: v, route: [role.counterparty], fromEntityId: role.actor,
    toEntityId: role.counterparty, deliveryMode: "direct", description: `xln:${x.type}`,
  }, byLeft);
  switch (x.type) {
    case "lending_fund": return firstFailure(
      amount(x.amount, "LENDING_FUND"), ...terms(x.termId, x.interestBps), replay,
      () => refuse(!ownsFunds(a, x, byLeft), "LENDING_FUND_OWNED_BALANCE_INSUFFICIENT"),
      pay(x.tokenId, x.amount),
    );
    case "lending_borrow_request":
      return firstFailure(amount(x.amount, "LENDING_BORROW"), ...terms(x.termId, x.maxInterestBps), replay);
    case "lending_repay": return firstFailure(amount(x.amount, "LENDING_REPAY"), replay, pay(x.tokenId, x.amount));
    case "lending_credit": return firstFailure(
      () => raise(x.creditLimit < 0n, `LENDING_CREDIT_LIMIT_NEGATIVE:${x.creditLimit}`),
      () => creditLimitFailure(a, x.tokenId, x.creditLimit),
      replay,
    );
    case "lending_close_request": return checked(replay());
    case "lending_close_payout":
      return firstFailure(amount(x.amount, "LENDING_CLOSE_PAYOUT"), replay, pay(x.tokenId, x.amount));
  }
};
const lendingFailure = (a: AccountBody, x: AccountLendingTx, ctx: FoldCtx): AccountTxFailure | null => {
  const role = lendingRole(x);
  return lendingPartiesFailure(a, ctx.byLeft, role) ?? lendingStepFailure(a, x, role, ctx.byLeft);
};
/** og validateCrossPullCloseEvidence: each mismatch between the proof and this pull, named. */
const closeProofFailure = (pull: PullRow, binding: CrossPullBinding, proof: CloseProof): AccountTxFailure | null => {
  const mismatch = (fails: boolean, detail: string): AccountTxFailure | null =>
    refuse(fails, `Cross-j close proof mismatch: ${detail}`);
  const total = absBig(pull.amount);
  // Only a uint16 ratio reaches this share, so BigInt never sees a fraction.
  const share = (): bigint =>
    (proof.fillRatio >= MAX_FILL ? total : (total * BigInt(proof.fillRatio)) / BigInt(MAX_FILL));
  const pullOnLeg = binding.leg === "source" ? proof.sourcePullId : proof.targetPullId;
  const claimed = claimedOnLeg(binding, proof);
  return firstFailure(
    () => refuse(!Number.isSafeInteger(proof.fillRatio) || proof.fillRatio < 0 || proof.fillRatio > MAX_FILL,
      `Cross-j close proof ratio out of uint16 range: ${proof.fillRatio}`),
    () => refuse(!CLOSE_MODES.includes(proof.closeMode), `Cross-j close mode invalid: ${String(proof.closeMode)}`),
    () => mismatch(proof.orderId !== binding.orderId, `order ${proof.orderId} != ${binding.orderId}`),
    () => mismatch(lowerText(proof.routeHash) !== lowerText(binding.routeHash),
      `routeHash ${proof.routeHash} != ${binding.routeHash}`),
    () => mismatch(pullOnLeg !== pull.pullId, `${binding.leg} pull ${pullOnLeg} != ${pull.pullId}`),
    () => {
      const expected = share();
      return mismatch(claimed !== expected, `${binding.leg} amount ${claimed} != chain-proportional ${expected}`);
    },
  );
};
/** og binary check: the binary behind the proof's hash, then a hash-ladder reveal at the proof's ratio. */
const closeBinaryFailure = (pull: PullRow, x: PullCloseTx): AccountTxFailure | null => {
  const { proof, binary } = x;
  const h = crossCloseBinaryHash(binary);
  // og ethers.keccak256 throws on a non-hex binary before any comparison.
  if (!h.ok) return threwTx(h.error.reason);
  if (h.value.toLowerCase() !== String(proof.binaryHash).toLowerCase()) {
    return refusedTx("Cross-j close binary hash mismatch");
  }
  const decoded = verifyHashLadderBinary({ fullHash: pull.fullHash, partialRoot: pull.partialRoot }, binary);
  if (!decoded.ok) return refusedTx(`Invalid cross-j close binary: ${decoded.error.reason}`);
  const ratio = decoded.value.fillRatio;
  return refuse(ratio !== proof.fillRatio, `Cross-j close ratio mismatch: binary ${ratio} != proof ${proof.fillRatio}`);
};
/** og validateCrossPullCloseEvidence + handleCrossPullClose. */
const crossPullCloseFailure = (a: AccountBody, x: PullCloseTx, ctx: FoldCtx): AccountTxFailure | null => {
  const pull = a.pulls?.get(x.pullId);
  if (pull === undefined) return refusedTx(`Cross-j close pull missing: ${shortId(x.pullId)}`);
  const binding = pull.crossJurisdiction;
  if (!binding) return refusedTx("Cross-j close requires pull binding");
  const evidence = closeProofFailure(pull, binding, x.proof) ?? closeBinaryFailure(pull, x);
  if (evidence !== null) return evidence;
  const beneficiaryIsLeft = pull.amount > 0n;
  const hubIsLeft = binding.leg === "source" ? beneficiaryIsLeft : !beneficiaryIsLeft;
  if (ctx.byLeft !== hubIsLeft) return refusedTx(`Only the ${binding.leg} Hub can close cross-j pull`);
  const claimed = claimedOnLeg(binding, x.proof);
  const row = getDelta(a.account, tokenKey(pull.tokenId));
  const moved = claimed > 0n ? shift(row, beneficiaryIsLeft ? claimed : -claimed) : row;
  const released: AccountBody = { ...a, pulls: mapDelete(pullsOf(a), x.pullId) };
  return firstFailure(draftThrow(a, pull.tokenId), offdeltaText(released, moved));
};
// ---- og failure text: J-event claims ----
/** The evidence failed although every row builds an event, so og saw a duplicate rather than a malformed row. */
const duplicateClaimEvents = (x: TxOf<"j_event_claim">): boolean => {
  const rows = x.events.flatMap((row) => row.tokens.map((token) => ({ row, token })));
  return rows.length > 0 && traverse(rows, ({ row, token }) => settledEvent(row, token)).ok;
};
/** og names the side whose retained claim at this height carries different evidence: left if it does, else right. */
const claimConflictText = (a: AccountBody, x: TxOf<"j_event_claim">, byLeft: boolean): string => {
  const own = claimRowOf(x, byLeft);
  const differs = (h: ClaimRow): boolean =>
    own.ok && h.onLeft && h.jHeight === own.value.jHeight && !sameEvidence(h, { ...own.value, onLeft: true });
  const side = (a.claimRows ?? []).some(differs) ? "left" : "right";
  return `ACCOUNT_J_CLAIM_${side.toUpperCase()}_CONFLICT:${side}:${Number(x.jHeight)}`;
};
/** The first settled nonce that runs backwards, from the Account's jNonce through the events in order. */
const nonceRegressionText = (jNonce: number, events: readonly SettledEvent[]): string | undefined => {
  const previous = [jNonce, ...events.map((ev) => ev.data.nonce)];
  const steps = events.map((ev, i) => [previous[i] ?? jNonce, ev.data.nonce] as const);
  const back = steps.find(([previous, next]) => next < previous);
  return back === undefined ? undefined : `ACCOUNT_SETTLED_NONCE_REGRESSION:${back[0]}:${back[1]}`;
};
/** og settled-nonce finality against a signed workspace: which proof piece is missing or off, naming the peer. */
const claimSettlementText = (a: AccountBody, reason: string, self: string | undefined): string => {
  const w = a.settlement, signed = w?.nonceAtSign;
  const { left, right } = a.account.id;
  const peer = self === undefined ? "" : lower(self) === lower(left) ? right : left;
  switch (reason) {
    case "SETTLEMENT_SIGNED_NONCE_MISSING": return `SETTLEMENT_SIGNED_NONCE_MISSING:${String(signed)}`;
    case "POST_SETTLEMENT_PROOF_MISSING": case "POST_SETTLEMENT_PROOF_HANKO_MISSING":
    case "POST_SETTLEMENT_DISPUTE_HASH_MISSING": return `${reason}:${peer}`;
    case "POST_SETTLEMENT_PROOF_NONCE_MISMATCH":
      return `POST_SETTLEMENT_PROOF_NONCE_MISMATCH:${w?.postSettlementDisputeProof?.nonce}:${(signed ?? 0) + 1}`;
    default: return reason;
  }
};
/**
 * og j-claim-transition.ts + j-events/finality.ts: malformed evidence and finality violations throw; only a same-height
 * conflict is a rejection.
 */
const claimFailure = (
  a: AccountBody, x: TxOf<"j_event_claim">, ctx: FoldCtx, e: BodyError, self: string | undefined,
): AccountTxFailure => {
  const evidence = claimEvidence(x.events);
  if (!evidence.ok) {
    return threwTx(duplicateClaimEvents(x) ? "ACCOUNT_J_CLAIM_EVENT_DUPLICATE" : "ACCOUNT_J_CLAIM_EVENTS_INVALID");
  }
  if (!claimHeight(x.jHeight).ok) return threwTx(`ACCOUNT_J_CLAIM_HEIGHT_INVALID:${String(x.jHeight)}`);
  if (!claimBlock(x.jBlockHash).ok) {
    const hash = typeof x.jBlockHash === "string" ? x.jBlockHash.trim().toLowerCase() : "";
    return threwTx(`ACCOUNT_J_CLAIM_BLOCK_HASH_INVALID:${hash || "missing"}`);
  }
  const { events } = evidence.value;
  const left = a.account.id.left.toLowerCase(), right = a.account.id.right.toLowerCase();
  switch (e._tag) {
    case "claim_conflict": return refusedTx(claimConflictText(a, x, ctx.byLeft));
    case "settled_pair": {
      const bad = events.find((ev) => ev.data.leftEntity !== left || ev.data.rightEntity !== right);
      return threwTx(`ACCOUNT_SETTLED_PAIR_MISMATCH:${bad?.data.leftEntity}:${bad?.data.rightEntity}:${left}:${right}`);
    }
    case "settled_nonce": return threwTx(nonceRegressionText(a.jNonce, events) ?? bodyErrorCode(e));
    case "settlement": return threwTx(claimSettlementText(a, e.reason, self));
    default: return threwTx(bodyErrorCode(e));
  }
};

// ---- og failure text: settlement workspace ----
const SETTLEMENT_OP_TYPES: readonly unknown[] = ["forgive", "rawDiff", "r2c", "c2r", "r2r"];
const DIFF_FIELDS = ["leftDiff", "rightDiff", "collateralDiff", "ondeltaDiff"] as const;
/** og compileOps' first pass over one op: its token, a repeated forgiveness, then its type. */
const opCompileText = (
  ops: readonly SettlementOp[], context: string,
) => (op: SettlementOp, i: number): string | undefined => {
  if (!settlementToken(op.tokenId)) return `SETTLEMENT_TOKEN_INVALID:${context}=${i}:${String(op.tokenId)}`;
  const forgivenBefore = ops.slice(0, i).some((o) => o.type === "forgive" && o.tokenId === op.tokenId);
  const repeated = op.type === "forgive" && forgivenBefore;
  if (repeated) return `SETTLEMENT_DUPLICATE_FORGIVENESS_TOKEN:${op.tokenId}`;
  if (SETTLEMENT_OP_TYPES.includes(op.type)) return undefined;
  const unknown = op as { readonly type?: unknown; readonly tokenId?: unknown };
  const named = `type=${String(unknown.type ?? "unknown")} tokenId=${String(unknown.tokenId ?? "unknown")}`;
  return `SETTLEMENT_UNKNOWN_OP_TYPE: ${named}`;
};
/** og's per-token sums as its text walk builds them: raw diffs as given, each amount op as it compiles on its own. */
const textDiffs = (ops: readonly SettlementOp[], proposerIsLeft: boolean): readonly WorkspaceDiff[] => {
  const alone = (op: SettlementOp): DiffParts => op.type === "rawDiff"
    ? op
    : unwrapOr(map(compileOps([op], proposerIsLeft), (c) => c.diffs[0] ?? noDiff), () => noDiff);
  const sums = ops.filter((op) => op.type !== "forgive").reduce((m, op) => {
    const held = m.get(op.tokenId) ?? { tokenId: op.tokenId, ...noDiff };
    return mapSet(m, op.tokenId, addDiff(held, alone(op)));
  }, new Map<number, WorkspaceDiff>() as ReadonlyMap<number, WorkspaceDiff>);
  return [...sums.values()];
};
/** og's per-diff checks: each field within SignedAmount, then conservation. */
const diffRangeText = (d: WorkspaceDiff): string | undefined => {
  const wide = DIFF_FIELDS.find((f) => d[f] < -MAX_PAYMENT_AMOUNT || d[f] > MAX_PAYMENT_AMOUNT);
  if (wide !== undefined) return `SETTLEMENT_SIGNED_AMOUNT_RANGE:${wide}:token=${d.tokenId}`;
  const sum = d.leftDiff + d.rightDiff + d.collateralDiff;
  if (sum === 0n) return undefined;
  const terms = `leftDiff(${d.leftDiff}) + rightDiff(${d.rightDiff}) + collateralDiff(${d.collateralDiff})`;
  return `SETTLEMENT_INVARIANT_VIOLATION: ${terms} = ${sum} !== 0 for tokenId ${d.tokenId}`;
};
/** og compileOps (protocol/settlement/operations.ts) messages in og's order: per op, then per diff, then the caps. */
const compileOpsText = (ops: readonly SettlementOp[], proposerIsLeft: boolean, context: string): string | null => {
  const opText = ops.map(opCompileText(ops, context)).find((t) => t !== undefined);
  if (opText !== undefined) return opText;
  const compiled = compileOps(ops, proposerIsLeft);
  if (compiled.ok) return null;
  const diffs = textDiffs(ops, proposerIsLeft);
  const rangeText = diffs.map(diffRangeText).find((t) => t !== undefined);
  if (rangeText !== undefined) return rangeText;
  const forgive = ops.filter((o) => o.type === "forgive").length;
  if (diffs.length > MAX_SETTLEMENT_DIFFS) {
    return `SETTLEMENT_DIFF_LIMIT_EXCEEDED:${diffs.length}:${MAX_SETTLEMENT_DIFFS}`;
  }
  if (forgive > MAX_SETTLEMENT_DIFFS) return `SETTLEMENT_FORGIVENESS_LIMIT_EXCEEDED:${forgive}:${MAX_SETTLEMENT_DIFFS}`;
  return bodyErrorCode(compiled.error);
};
type ProjectedRow = Readonly<{ collateral: bigint; ondelta: bigint }>;
/** og projectSettlementDeltaOverrides: the new-row cap and the projected collateral/ondelta ranges. */
const projectionText = (a: AccountBody, diffs: readonly WorkspaceDiff[], forgive: readonly number[]): string | null => {
  const deltas = a.account.deltas;
  // A row enters the projection the first time a diff or forgiveness names it; each new one counts against the cap.
  const rowOf = (m: ReadonlyMap<number, ProjectedRow>, tk: number): Result<ProjectedRow, string> => {
    const held = m.get(tk);
    if (held !== undefined) return ok(held);
    const row = deltas.get(tokenKey(tk));
    const rows = deltas.size + [...m.keys()].filter((k) => !deltas.has(tokenKey(k))).length + 1;
    if (row === undefined && rows > MAX_ROWS) return err(`ACCOUNT_DELTA_ROW_LIMIT_EXCEEDED:insert:${rows}:${MAX_ROWS}`);
    return ok({ collateral: row?.collateral ?? 0n, ondelta: row?.ondelta ?? 0n });
  };
  const applyDiff = (m: ReadonlyMap<number, ProjectedRow>, d: WorkspaceDiff) => chain(rowOf(m, d.tokenId), (v) => {
    const next = { collateral: v.collateral + d.collateralDiff, ondelta: v.ondelta + d.ondeltaDiff };
    if (next.collateral < 0n || next.collateral > MAX_PAYMENT_AMOUNT) {
      return err(`SETTLEMENT_PROJECTED_COLLATERAL_RANGE:token=${d.tokenId}`);
    }
    if (next.ondelta < INT512_MIN || next.ondelta > INT512_MAX) {
      return err(`SETTLEMENT_PROJECTED_ONDELTA_RANGE:token=${d.tokenId}`);
    }
    return ok(mapSet(m, d.tokenId, next));
  });
  const touch = (m: ReadonlyMap<number, ProjectedRow>, tk: number) => map(rowOf(m, tk), (v) => mapSet(m, tk, v));
  const start: ReadonlyMap<number, ProjectedRow> = new Map();
  const projected = chain(foldResult(diffs, start, applyDiff), (m) => foldResult(forgive, m, touch));
  return projected.ok ? null : projected.error;
};
/** og assertCurrentWorkspace: version, presence, the requested hash's shape, then revision and hash equality. */
const currentWorkspaceText = (a: AccountBody, revision: number, hash: string): string | null => {
  if (!Number.isSafeInteger(revision) || revision < 1) {
    return `SETTLEMENT_WORKSPACE_VERSION_INVALID:${String(revision)}`;
  }
  const w = a.settlement;
  if (w === undefined) return "SETTLEMENT_WORKSPACE_MISSING";
  if (typeof hash !== "string" || !WORKSPACE_HASH.test(hash)) {
    return `SETTLEMENT_WORKSPACE_TARGET_HASH_INVALID:${String(hash)}`;
  }
  if (w.revision !== revision) return `SETTLEMENT_WORKSPACE_VERSION_MISMATCH:${w.revision}:${revision}`;
  const held = w.workspaceHash.toLowerCase(), asked = hash.toLowerCase();
  return held === asked ? null : `SETTLEMENT_WORKSPACE_TARGET_HASH_MISMATCH:${held}:${asked}`;
};
/** og's text for one upsert op's shape refusal, carrying the op's index. */
const workspaceOpText = (op: SettlementOp, i: number): string | undefined => {
  const issue = opShapeIssue(op);
  switch (issue) {
    case undefined: return undefined;
    case "SETTLEMENT_TOKEN_INVALID": return `${issue}:workspace-op=${i}:${String(op.tokenId)}`;
    case "SETTLEMENT_WORKSPACE_OP_INVALID":
      return `${issue}:index=${i}:type=${String((op as { readonly type?: unknown }).type)}`;
    default: return `${issue}:index=${i}`;
  }
};
/** og's wording of a workspace link refusal, with the revisions or hashes it compared. */
const workspaceLinkText = (cur: SettlementWorkspace | undefined, x: UpsertTx): string | undefined => {
  const link = workspaceLink(cur, x);
  if (link.ok) return undefined;
  const code = bodyErrorCode(link.error), prev = x.previousWorkspaceHash;
  switch (code) {
    case "SETTLEMENT_WORKSPACE_NON_CONTIGUOUS_VERSION": return `${code}:${cur?.revision}:${x.revision}`;
    case "SETTLEMENT_WORKSPACE_PREVIOUS_HASH_INVALID": return `${code}:${String(prev ?? "")}`;
    case "SETTLEMENT_WORKSPACE_PREVIOUS_HASH_MISMATCH":
      return `${code}:${cur?.workspaceHash.toLowerCase()}:${prev?.toLowerCase()}`;
    default: return code;
  }
};
/**
 * og planWorkspaceHoldRelease(previous) then planWorkspaceHoldAdd(next): each side's new hold is checked with the
 * previous workspace's holds released. A diff that funds collateral from the same side needs no capacity.
 */
const workspaceHoldText = (base: AccountBody, diff: WorkspaceDiff): string | undefined => {
  const l = diff.leftDiff < 0n ? -diff.leftDiff : 0n, r = diff.rightDiff < 0n ? -diff.rightDiff : 0n;
  if (l === 0n && r === 0n) return undefined;
  const tk = tokenKey(diff.tokenId);
  const funds = (sideDiff: bigint): boolean => sideDiff < 0n && diff.collateralDiff > 0n;
  const short = (side: string): string => `SETTLEMENT_HOLD_CAPACITY:${side}:token=${diff.tokenId}`;
  switch (true) {
    case !base.account.deltas.has(tk): return `SETTLEMENT_HOLD_DELTA_MISSING:add:token=${diff.tokenId}`;
    case !funds(diff.leftDiff) && l > ogOutCapacity(base, tk, true): return short("left");
    case !funds(diff.rightDiff) && r > ogOutCapacity(base, tk, false): return short("right");
    default: return (holdOverflowText(base, tk, true, l)() ?? holdOverflowText(base, tk, false, r)())?.message;
  }
};
/** og upsert: revision, op shapes, executor, compilation, the workspace link, then the holds it adds. */
const upsertText = (a: AccountBody, x: UpsertTx, ctx: FoldCtx, e: BodyError): string => {
  if (!Number.isSafeInteger(x.revision) || x.revision < 1) {
    return `SETTLEMENT_WORKSPACE_VERSION_INVALID:${String(x.revision)}`;
  }
  if (!Array.isArray(x.ops) || x.ops.length === 0) return "SETTLEMENT_WORKSPACE_OPS_EMPTY";
  const shape = x.ops.map(workspaceOpText).find((t) => t !== undefined);
  if (shape !== undefined) return shape;
  if (typeof x.executorIsLeft !== "boolean") return "SETTLEMENT_WORKSPACE_EXECUTOR_INVALID";
  const base: AccountBody = { ...a, settlement: undefined };
  const diffs = unwrapOr(map(compileOps(x.ops, ctx.byLeft), (c) => c.diffs), () => []);
  return compileOpsText(x.ops, ctx.byLeft, "op")
    ?? workspaceLinkText(a.settlement, x)
    ?? diffs.map((d) => workspaceHoldText(base, d)).find((t) => t !== undefined)
    ?? bodyErrorCode(e);
};
/** og hanko nonce: a positive nonce, below exhaustion, pinned once signed or else exactly the next proof nonce. */
const hankoNonceText = (
  a: AccountBody, ws: SettlementWorkspace, nonce: number, proofFloor: number, w: DisputeWitnesses | undefined,
): string | undefined => {
  if (!Number.isSafeInteger(nonce) || nonce < 1) return `SETTLEMENT_HANKO_NONCE_INVALID:${String(nonce)}`;
  const floor = Math.max(a.jNonce + 1, proofFloor);
  if (floor >= Number.MAX_SAFE_INTEGER) return `SETTLEMENT_NONCE_EXHAUSTED:${floor}`;
  if (ws.nonceAtSign !== undefined) {
    return ws.nonceAtSign === nonce ? undefined : `SETTLEMENT_HANKO_NONCE_MISMATCH:${ws.nonceAtSign}:${nonce}`;
  }
  if (nonce === floor) return undefined;
  const cursors = `j=${a.jNonce}:next=${w?.nextProofNonce ?? 0}:local=${w?.current?.proofNonce ?? 0}`
    + `:peer=${w?.counterparty?.proofNonce ?? 0}`;
  return `SETTLEMENT_HANKO_NONCE_MISMATCH:${nonce}:${floor}:${cursors}`;
};
/** og hanko hash: the settlement hash at this nonce, as supplied and as pinned by an earlier signature. */
const hankoHashText = (a: AccountBody, x: HankoTx, ws: SettlementWorkspace, c: Compiled): string | undefined => {
  const expected = settlementHashOf(a, c.diffs, c.forgive, x.settlementNonce);
  if (!expected.ok) return bodyErrorCode(expected.error);
  const hash = expected.value;
  if (typeof x.settlementHash !== "string" || !WORKSPACE_HASH.test(x.settlementHash)) {
    return `SETTLEMENT_HANKO_HASH_INVALID:${String(x.settlementHash)}`;
  }
  if (x.settlementHash.toLowerCase() !== hash.toLowerCase()) {
    return `SETTLEMENT_HANKO_HASH_MISMATCH:${x.settlementHash.toLowerCase()}:${hash}`;
  }
  const pinned = ws.settlementHash;
  return pinned === undefined || pinned.toLowerCase() === hash.toLowerCase()
    ? undefined
    : `SETTLEMENT_HANKO_PINNED_HASH_MISMATCH:${pinned}:${hash}`;
};
/** og post-settlement proof: the next nonce, the projected rows, then the body and dispute hashes it commits. */
const postProofText = (a: AccountBody, x: HankoTx, s: SettlementCtx, c: Compiled): string | undefined => {
  const proof = x.postProof, nonce = proof.nonce;
  if (!Number.isSafeInteger(nonce) || nonce < 1) return `POST_SETTLEMENT_PROOF_NONCE_INVALID:${String(nonce)}`;
  if (nonce !== x.settlementNonce + 1) return `POST_SETTLEMENT_PROOF_NONCE_MISMATCH:${nonce}:${x.settlementNonce + 1}`;
  const projected = projectionText(a, c.diffs, c.forgive);
  if (projected !== null) return projected;
  const bodyHash = projectedProofHash(a, c.diffs, c.forgive, s.deltaTransformer);
  if (!bodyHash.ok) return bodyErrorCode(bodyHash.error);
  if (proof.proofBodyHash.toLowerCase() !== bodyHash.value.toLowerCase()) {
    return `POST_SETTLEMENT_PROOF_BODY_HASH_MISMATCH:${proof.proofBodyHash}:${bodyHash.value}`;
  }
  const disputeHash = chain(mapErr(committedView(a), uncommitted), (view) => mapErr(
    accountDisputeHash(view, bodyHash.value, nonce, proof.proposerIsLeft),
    (e): BodyError => ({ _tag: "settlement", reason: e._tag }),
  ));
  if (!disputeHash.ok) return bodyErrorCode(disputeHash.error);
  return proof.disputeHash.toLowerCase() === disputeHash.value.toLowerCase()
    ? undefined
    : `POST_SETTLEMENT_DISPUTE_HASH_MISMATCH:${proof.disputeHash}:${disputeHash.value}`;
};
/** og hanko: context, the current unsubmitted workspace, nonce, compilation, hashes, then the post-settlement proof. */
const hankoText = (a: AccountBody, x: HankoTx, ctx: FoldCtx, e: BodyError, w: DisputeWitnesses | undefined): string => {
  const current = currentWorkspaceText(a, x.revision, x.workspaceHash), ws = a.settlement;
  if (ctx.settlement === undefined) return "SETTLEMENT_HANKO_CONTEXT_MISSING";
  if (current !== null || ws === undefined) return current ?? "SETTLEMENT_WORKSPACE_MISSING";
  if (ws.status === "submitted") return "SETTLEMENT_HANKO_SUBMITTED_FORBIDDEN";
  const nonce = hankoNonceText(a, ws, x.settlementNonce, ctx.settlement.proofNonceFloor, w);
  if (nonce !== undefined) return nonce;
  const compiled = compileOps(ws.ops, ws.lastModifiedByLeft);
  if (!compiled.ok) return compileOpsText(ws.ops, ws.lastModifiedByLeft, "op") ?? bodyErrorCode(compiled.error);
  return hankoHashText(a, x, ws, compiled.value)
    ?? postProofText(a, x, ctx.settlement, compiled.value)
    ?? bodyErrorCode(e);
};
/** og handleSettleTransition: every failure is caught into a rejection carrying the thrown message. */
const settleFailure = (
  a: AccountBody, x: TxOf<"settle_transition">, ctx: FoldCtx, e: BodyError, w?: DisputeWitnesses,
): AccountTxFailure => {
  switch (x.kind) {
    case "upsert": return refusedTx(upsertText(a, x, ctx, e));
    case "hanko": return refusedTx(hankoText(a, x, ctx, e, w));
    default: return refusedTx(currentWorkspaceText(a, x.revision, x.workspaceHash) ?? bodyErrorCode(e));
  }
};
/** A BodyError as a bare og-style code, for refusals whose og text carries no further detail. */
const bodyErrorCode = (e: BodyError): string => {
  switch (e._tag) {
    case "settlement": case "swap": case "rebalance": case "lending": case "payment_route": case "cross_j":
      return e.reason;
    case "uncommitted": return `ACCOUNT_STATE_UNCOMMITTABLE:${e.reason._tag}`;
    case "too_many_rows": return `ACCOUNT_DELTA_ROW_LIMIT_EXCEEDED:insert:${MAX_ROWS + 1}:${MAX_ROWS}`;
    default: return e._tag;
  }
};

// ---- og failure text: HTLCs, swap cancel, rebalance ----
const htlcLockFailure = (a: AccountBody, x: TxOf<"htlc_lock">, ctx: FoldCtx): AccountTxFailure | null => firstFailure(
  () => refuse(x.lockId !== x.hashlock, `Lock ID must equal hashlock (${x.lockId} != ${x.hashlock})`),
  () => refuse(a.locks.has(x.lockId), `Lock ${x.lockId} already exists`),
  () => refuse(ctx.nowMs >= x.timelock, `Timelock ${x.timelock} already expired (timestamp)`),
  () => refuse(x.revealBeforeHeight <= ctx.jHeight,
    `revealBeforeHeight ${x.revealBeforeHeight} already passed (current J height: ${ctx.jHeight})`),
  () => refuse(!inPaymentRange(x.amount), `Invalid amount: ${x.amount} (min 1, max ${U256})`),
  () => refuse(a.locks.size >= MAX_ACCOUNT_HTLC_LOCKS, `Too many active HTLC locks: max ${MAX_ACCOUNT_HTLC_LOCKS}`),
  draftThrow(a, x.tokenId),
  () => {
    const bad = x.envelope === undefined ? null : envelopeError(x.envelope);
    return bad === null ? null : threwTx(bad);
  },
  capacityCheck(a, x.tokenId, ctx.byLeft, x.amount, (n) => `Insufficient capacity: need ${x.amount}, available ${n}`),
  offdeltaText(a, getDelta(a.account, x.tokenId), { senderIsLeft: ctx.byLeft, amount: x.amount }),
  holdOverflowText(a, x.tokenId, ctx.byLeft, x.amount),
);
const htlcResolveFailure = (a: AccountBody, x: TxOf<"htlc_resolve">, ctx: FoldCtx): AccountTxFailure | null => {
  const lock = a.locks.get(x.lockId);
  if (lock === undefined) return refusedTx(`Lock ${x.lockId} not found`);
  if (!a.account.deltas.has(lock.tokenId)) return refusedTx(`Delta ${Number(lock.tokenId)} not found`);
  const expired = htlcExpired(lock, ctx);
  if (x.outcome === "error") {
    if (ctx.byLeft === lock.senderIsLeft && !expired) {
      return refusedTx("Only beneficiary can release an active HTLC; payer can cancel only after expiry");
    }
    return refuse(x.reason === "timeout" && !expired, "Lock not expired yet");
  }
  if (expired) {
    const when = `timestamp=${ctx.nowMs}/${lock.timelock} jHeight=${ctx.jHeight}/${lock.revealBeforeHeight}`;
    return refusedTx(`Lock expired: ${when}`);
  }
  const computed = hashHtlcSecret(x.secret);
  if (computed === null) {
    return refusedTx(`Invalid secret: HTLC secret must be 32-byte hex (got ${x.secret.length} chars)`);
  }
  if (computed !== lock.hashlock) {
    return refusedTx(`Hash mismatch: expected ${lock.hashlock.slice(0, 8)}..., got ${computed.slice(0, 8)}...`);
  }
  const paid = shift(getDelta(a.account, lock.tokenId), lock.senderIsLeft ? -lock.amount : lock.amount);
  return checked(offdeltaText(a, paid, undefined, lock.lockId)());
};
const swapCancelFailure = (a: AccountBody, x: TxOf<"swap_cancel_request">, ctx: FoldCtx): AccountTxFailure | null => {
  const offer = a.offers.get(x.offerId);
  if (offer === undefined) return refusedTx(`Offer ${x.offerId} not found`);
  return refuse(ctx.byLeft !== offer.makerIsLeft, "Only maker can cancel swap offer");
};
/** og accepts here without applying anything; the rewrite's refusal lies past og's handler. */
const acceptedByOg: AccountTxFailure = refusedTx("");
const collateralFailure = (a: AccountBody, x: TxOf<"request_collateral">, ctx: FoldCtx): AccountTxFailure | null => {
  const feeToken = x.feeTokenId ?? x.tokenId;
  return firstFailure(
    () => refuse(x.amount <= 0n, "request_collateral: amount must be > 0"),
    () => refuse(x.feeAmount < 0n, "request_collateral: feeAmount must be >= 0"),
    () => refuse(!Number.isFinite(x.policyVersion) || x.policyVersion < 1,
      `request_collateral: invalid policyVersion ${x.policyVersion}`),
    () => refuse(!a.account.deltas.has(x.tokenId), `request_collateral: no delta for token ${Number(x.tokenId)}`),
    () => ((a.requested.get(x.tokenId) ?? 0n) > 0n ? acceptedByOg : null),
    () => refuse(x.feeAmount <= 0n, "request_collateral: feeAmount must produce effectiveFee > 0"),
    () => refuse(!a.account.deltas.has(feeToken), `request_collateral: no delta for fee token ${Number(feeToken)}`),
    () => (requestNetAmount(x) <= 0n ? acceptedByOg : null),
    capacityCheck(a, feeToken, ctx.byLeft, x.feeAmount,
      (n) => `request_collateral: insufficient fee capacity in token ${Number(feeToken)} (${n} < ${x.feeAmount})`),
    paidRowText(a, feeToken, ctx.byLeft, x.feeAmount),
  );
};
const refundFailure = (a: AccountBody, x: TxOf<"rebalance_refund">, ctx: FoldCtx): AccountTxFailure | null => {
  if (!x.requestId || x.amount <= 0n) return refusedTx("rebalance_refund: requestId and positive amount required");
  const fees = a.requestFees.get(x.requestTokenId);
  const open = (a.requested.get(x.requestTokenId) ?? 0n) > 0n;
  if (fees === undefined || !open || fees.requestId !== x.requestId) {
    return refusedTx(`rebalance_refund: pending request not found (${x.requestId})`);
  }
  const outstanding = fees.feePaidUpfront - (fees.refund?.refundedAmount ?? 0n);
  const feeToken = tokenKey(fees.feeTokenId);
  return firstFailure(
    () => refuse(ctx.byLeft === fees.requestedByLeft, "rebalance_refund: requester cannot refund itself"),
    () => refuse(fees.refund !== undefined && fees.refund.reason !== x.reason,
      "rebalance_refund: reason conflicts with partial refund"),
    () => raise(outstanding <= 0n, `REBALANCE_REFUND_STATE_CORRUPT:${x.requestId}`),
    () => refuse(x.amount > outstanding, `rebalance_refund: amount ${x.amount} exceeds outstanding ${outstanding}`),
    () => refuse(!a.account.deltas.has(feeToken), `rebalance_refund: fee token ${fees.feeTokenId} missing`),
    capacityCheck(a, feeToken, ctx.byLeft, x.amount,
      (n) => `rebalance_refund: insufficient capacity (${n} < ${x.amount})`),
    paidRowText(a, feeToken, ctx.byLeft, x.amount),
  );
};
const policyTermsText = (issue: string, x: TxOf<"rebalance_policy">, ts: number): string => {
  const token = Number(x.tokenId);
  switch (issue) {
    case "REBALANCE_POLICY_TOKEN": return `rebalance_policy: invalid tokenId ${token}`;
    case "REBALANCE_POLICY_VERSION": return `rebalance_policy: invalid policyVersion ${x.policyVersion}`;
    case "REBALANCE_POLICY_FEE_TYPES": return `rebalance_policy: invalid fee types for token ${token}`;
    case "REBALANCE_POLICY_TIMESTAMP": return `rebalance_policy: invalid committed timestamp ${ts}`;
    default: return `rebalance_policy: invalid fee terms for token ${token}`;
  }
};
const policyFailure = (a: AccountBody, x: TxOf<"rebalance_policy">, ctx: FoldCtx): AccountTxFailure | null => {
  const ts = Number(ctx.nowMs), token = Number(x.tokenId);
  const issue = policyTermsIssue(x, ts);
  if (issue !== undefined) return refusedTx(policyTermsText(issue, x, ts));
  if (!a.account.deltas.has(x.tokenId)) return refusedTx(`rebalance_policy: no delta for token ${token}`);
  const held = a.feePolicies.get(x.tokenId);
  const current = at(held?.left, held?.right, ctx.byLeft);
  const equivocates = policyStanding(current, x.policyVersion) === "current" && !samePolicyTerms(current, x);
  const text = `REBALANCE_POLICY_EQUIVOCATION: side=${sideName(ctx.byLeft)} token=${token} version=${x.policyVersion}`;
  return refuse(equivocates, text);
};

// ---- og failure text: the whole tx ----
/** og's handler checks for `tx`, walked in og's order; null when og's handler would not refuse it. */
const ogHandlerFailure = (
  a: AccountBody, tx: WireAccountTx, ctx: FoldCtx, e: BodyError, self?: string, witnesses?: DisputeWitnesses,
): AccountTxFailure | null => {
  switch (tx.type) {
    case "add_delta": return refuseIssue(deltaDraftError(a, tx.tokenId) ?? undefined);
    case "set_credit_limit": return creditLimitFailure(a, tx.tokenId, tx.limit);
    case "payment": return paymentFailure(a, tx, ctx.byLeft);
    case "htlc_lock": return htlcLockFailure(a, tx, ctx);
    case "htlc_resolve": return htlcResolveFailure(a, tx, ctx);
    case "swap_offer": return swapOfferFailure(a, tx, ctx);
    case "swap_cancel_request": return swapCancelFailure(a, tx, ctx);
    case "swap_resolve": return swapResolveFailure(a, tx, ctx);
    case "request_collateral": return collateralFailure(a, tx, ctx);
    case "rebalance_refund": return refundFailure(a, tx, ctx);
    case "rebalance_policy": return policyFailure(a, tx, ctx);
    case "lending_fund": case "lending_borrow_request": case "lending_repay":
    case "lending_credit": case "lending_close_request": case "lending_close_payout": return lendingFailure(a, tx, ctx);
    case "cross_pull_lock": {
      const text = pullAdmissionText(a, tx);
      if (text !== null) return refusedTx(text);
      const payerIsLeft = tx.amount < 0n;
      return firstFailure(draftThrow(a, tx.tokenId), holdOverflowText(a, tx.tokenId, payerIsLeft, absBig(tx.amount)));
    }
    case "cross_pull_close": return crossPullCloseFailure(a, tx, ctx);
    case "j_event_claim": return claimFailure(a, tx, ctx, e, self);
    case "settle_transition": return settleFailure(a, tx, ctx, e, witnesses);
  }
};
/**
 * og's failure for `tx` refused by this body with `e` under `ctx` (og byLeft = the frame proposer's side): og's
 * handler checks walked in og's order, rendered with og's message text. `self` is the local Entity (og
 * proofHeader.fromEntity); `witnesses` are the dispute nonce cursors og names in an account-basis settlement nonce
 * mismatch.
 */
export const accountTxFailure = (
  a: AccountBody, tx: WireAccountTx, ctx: FoldCtx, e: BodyError, self?: string, witnesses?: DisputeWitnesses,
): AccountTxFailure => {
  if (e._tag === "settlement_frozen") return refusedTx(`SETTLEMENT_SIGNED_ACCOUNT_FROZEN:${ogTxType(tx)}`);
  const found = ogHandlerFailure(a, tx, ctx, e, self, witnesses);
  // og's early accepts render as "": the refusal lies past og's handler, in the rewrite's commit step.
  if (found !== null && found.message !== "") return found;
  const code = bodyErrorCode(e);
  return e._tag === "uncommitted" || e._tag === "too_many_rows" ? threwTx(code) : refusedTx(code);
};
export const accountSnapshot = (a: AccountBody): Required<Omit<AccountBody, "account">> & { readonly state: Hash } => ({
  state: hashAccountState(a.account), terms: a.terms, locks: a.locks, offers: a.offers, requested: a.requested,
  requestFees: a.requestFees, feePolicies: a.feePolicies, lendingIntents: a.lendingIntents, claimRows: a.claimRows,
  jNonce: a.jNonce, settlement: a.settlement, finalizedJHeight: a.finalizedJHeight, pulls: a.pulls,
  submittedAt: a.submittedAt,
});

// ---- committed view: og's CommittedAccountState of a body, its pending J-claim tries, and the commitment root ----
export type ViewError = CommitmentError | Tagged<"token_id", { tokenId: TokenId }> | ClaimError;
export type UncommittedReason = ViewError | FrameHashError | Tagged<"unsafe_number">;
export type Uncommitted = Tagged<"uncommitted", { reason: UncommittedReason }>;
export const uncommitted = (reason: UncommittedReason): Uncommitted => ({ _tag: "uncommitted", reason });
export const tokenNumber = (id: TokenId): Result<number, ViewError> =>
  tokenId(id).ok ? ok(Number(id)) : err({ _tag: "token_id", tokenId: id });
export const tokenOrder = (b: AccountBody): readonly TokenId[] =>
  [...b.account.deltas.keys()].toSorted((x, y) => Number(x) - Number(y));

// Each side's pending J-claims commit as og's crit-bit Patricia trie: leaves keyed by (account, side, jHeight),
// branches splitting on the first bit where two keys differ.
const EMPTY_J_CLAIMS: JClaimAccumulator = { version: 1, root: EMPTY_J_ROOT, count: 0n };
const claimTag = (name: string): string => keccak256Hex(utf8(`xln.account-j-claim.${name}.v1`));
const CLAIM_ACCOUNT = claimTag("account"), CLAIM_KEY = claimTag("key"), CLAIM_RECORD = claimTag("record");
const CLAIM_LEAF = claimTag("leaf"), CLAIM_BRANCH = claimTag("branch");
type ClaimLeaf = { readonly key: string; readonly record: string; readonly rec: JClaimRecord };
type ClaimNode = Tagged<"leaf", ClaimLeaf> | Tagged<"branch", { bit: number; left: ClaimNode; right: ClaimNode }>;
/** Bit `index` of a 32-byte hex key, the first byte's most significant bit first. */
const claimBit = (key: string, index: number): 0 | 1 => {
  const offset = 2 + (index >> 3) * 2;
  const byte = Number.parseInt(key.slice(offset, offset + 2), 16);
  return ((byte >> (7 - (index & 7))) & 1) === 0 ? 0 : 1;
};
const KEY_BITS = Array.from({ length: 256 }, (_, i) => i);
const claimAccountKey = (domain: Domain, left: string, right: string): string => keccak256Hex(abiEncode([
  A.b32(CLAIM_ACCOUNT), A.uint(BigInt(domain.chainId)), A.address(domain.depositoryAddress), A.b32(left), A.b32(right),
]));
const claimLeaf = (accountKey: string, row: ClaimRow): ClaimLeaf => {
  const side = row.onLeft ? 0n : 1n;
  const slot = [A.b32(accountKey), A.uint(side), A.uint(row.jHeight)];
  return {
    key: keccak256Hex(abiEncode([A.b32(CLAIM_KEY), ...slot])),
    record: keccak256Hex(abiEncode([A.b32(CLAIM_RECORD), ...slot, A.b32(row.jBlockHash), A.b32(row.eventsHash)])),
    rec: {
      version: 1, accountKey: accountKey.toLowerCase(), side: row.onLeft ? "left" : "right",
      jHeight: Number(row.jHeight), jBlockHash: row.jBlockHash, eventsHash: row.eventsHash,
    },
  };
};
const claimNodeHash = (node: ClaimNode): string => match(node, {
  leaf: ({ key, record }) => keccak256Hex(abiEncode([A.b32(CLAIM_LEAF), A.uint(1n), A.b32(key), A.b32(record)])),
  branch: ({ bit, left, right }) => keccak256Hex(abiEncode([
    A.b32(CLAIM_BRANCH), A.uint(1n), A.uint(BigInt(bit)), A.b32(claimNodeHash(left)), A.b32(claimNodeHash(right)),
  ])),
});
/** The leaf a key's bit path ends at. */
const claimTerminal = (node: ClaimNode, key: string): Of<ClaimNode, "leaf"> => match(node, {
  leaf: (leaf) => leaf,
  branch: (b) => claimTerminal(claimBit(key, b.bit) === 0 ? b.left : b.right, key),
});
/** A new branch at `diff`, the leaf on its key's side and the existing subtree on the other. */
const claimPlace = (diff: number, key: string, leaf: Of<ClaimNode, "leaf">, other: ClaimNode): ClaimNode => {
  const leftward = claimBit(key, diff) === 0;
  return { _tag: "branch", bit: diff, left: leftward ? leaf : other, right: leftward ? other : leaf };
};
/** Descend while branches split before `diff`, then place the leaf. */
const claimInsertAt = (
  node: ClaimNode, key: string, leaf: Of<ClaimNode, "leaf">, diff: number,
): ClaimNode => match(node, {
  leaf: () => claimPlace(diff, key, leaf, node),
  branch: (b) => {
    if (b.bit >= diff) return claimPlace(diff, key, leaf, node);
    return claimBit(key, b.bit) === 0
      ? { ...b, left: claimInsertAt(b.left, key, leaf, diff) }
      : { ...b, right: claimInsertAt(b.right, key, leaf, diff) };
  },
});
/** The same key with the same record is already there; the same key with another record is a conflict. */
const insertClaim = (node: ClaimNode, leaf: ClaimLeaf): Result<ClaimNode, ClaimError> => {
  const term = claimTerminal(node, leaf.key);
  if (term.key === leaf.key) return term.record === leaf.record ? ok(node) : err({ _tag: "claim_conflict" });
  const diff = KEY_BITS.findIndex((i) => claimBit(leaf.key, i) !== claimBit(term.key, i));
  if (diff < 0) return err({ _tag: "claim_conflict" });
  return ok(claimInsertAt(node, leaf.key, { _tag: "leaf", ...leaf }, diff));
};
/**
 * og's crit-bit Patricia trie is canonical in its key set, so any insertion order rebuilds the committed node
 * structure.
 */
const claimTree = (accountKey: string, rows: readonly ClaimRow[]): Result<ClaimNode | undefined, ClaimError> =>
  foldResult<ClaimNode | undefined, ClaimRow, ClaimError>(rows, undefined, (node, row) => {
    const leaf = claimLeaf(accountKey, row);
    return node === undefined ? ok({ _tag: "leaf", ...leaf }) : insertClaim(node, leaf);
  });
const claimAccumulator = (accountKey: string, rows: readonly ClaimRow[]): Result<JClaimAccumulator, ClaimError> =>
  map(claimTree(accountKey, rows), (node) =>
    node === undefined ? EMPTY_J_CLAIMS : { version: 1, root: claimNodeHash(node), count: BigInt(rows.length) });
/**
 * og j-claim-proof.ts createAccountJClaimProof: every node from the root down the key's bit path to the terminal leaf.
 */
const claimPath = (node: ClaimNode, key: string): readonly JClaimNode[] => match(node, {
  leaf: ({ key: k, rec }): readonly JClaimNode[] => [{ version: 1, type: "leaf", key: k, record: rec }],
  branch: ({ bit, left, right }): readonly JClaimNode[] => [
    { version: 1, type: "branch", bit, left: claimNodeHash(left), right: claimNodeHash(right) },
    ...claimPath(claimBit(key, bit) === 0 ? left : right, key),
  ],
});
const claimKeyOf = (b: Pick<AccountBody, "account" | "terms">): Result<string, ViewError> => {
  const { left, right } = b.account.id;
  if (!WORD.test(left) || !WORD.test(right)) return err({ _tag: "claim_entity" });
  return map(domainOf(b.terms.domain), (domain) => claimAccountKey(domain, left.toLowerCase(), right.toLowerCase()));
};
const pendingOn = (b: AccountBody, onLeft: boolean): Result<JClaimAccumulator, ViewError> => {
  const rows = (b.claimRows ?? []).filter((r) => r.onLeft === onLeft);
  if (rows.length === 0) return ok(EMPTY_J_CLAIMS);
  return chain(claimKeyOf(b), (accountKey) => claimAccumulator(accountKey, rows));
};

// Holds: what each side has committed but not yet paid, per token. Allowances are always zero here.
export type SideTotals = {
  readonly leftHold: bigint; readonly rightHold: bigint;
  readonly leftAllowance: bigint; readonly rightAllowance: bigint;
};
const NO_TOTALS: SideTotals = { leftHold: 0n, rightHold: 0n, leftAllowance: 0n, rightAllowance: 0n };
type Hold = Readonly<{ tokenId: TokenId; onLeft: boolean; amount: bigint }>;
/** An unsubmitted workspace holds each side's outgoing diff until it lands. */
const workspaceHolds = (w: SettlementWorkspace | undefined): readonly Hold[] => {
  if (w === undefined || w.status === "submitted") return [];
  return workspaceDiffs(w).flatMap((d): readonly Hold[] => {
    const tokenId = String(d.tokenId) as TokenId;
    return [
      ...(d.leftDiff < 0n ? [{ tokenId, onLeft: true, amount: -d.leftDiff }] : []),
      ...(d.rightDiff < 0n ? [{ tokenId, onLeft: false, amount: -d.rightDiff }] : []),
    ];
  });
};
/** Every hold on the Account: HTLC locks, same-j offers' give, pulls' payer side, and the workspace's outflows. */
const accountHolds = (b: AccountBody): readonly Hold[] => [
  ...[...b.locks.values()].map((l) => ({ tokenId: l.tokenId, onLeft: l.senderIsLeft, amount: l.amount })),
  // og commit.ts: a cross-j offer adds no hold (its source pull already owns the lock); a pull holds |amount| on
  // the payer side.
  ...[...b.offers.values()].filter((o) => o.crossJurisdiction === undefined)
    .map((o) => ({ tokenId: o.giveTokenId, onLeft: o.makerIsLeft, amount: o.giveAmount })),
  ...[...(b.pulls?.values() ?? [])]
    .map((p) => ({ tokenId: String(p.tokenId) as TokenId, onLeft: p.amount < 0n, amount: absBig(p.amount) })),
  ...workspaceHolds(b.settlement),
];
const addHold = (s: SideTotals, h: Hold): SideTotals =>
  h.onLeft ? { ...s, leftHold: s.leftHold + h.amount } : { ...s, rightHold: s.rightHold + h.amount };
const totalsOn = (b: AccountBody, counted: (id: TokenId) => boolean): ReadonlyMap<TokenId, SideTotals> =>
  accountHolds(b).filter((h) => counted(h.tokenId)).reduce(
    (m, h) => mapSet(m, h.tokenId, addHold(m.get(h.tokenId) ?? NO_TOTALS, h)),
    new Map() as ReadonlyMap<TokenId, SideTotals>,
  );
export const sideTotals = (b: AccountBody, tk: TokenId): SideTotals =>
  totalsOn(b, (id) => id === tk).get(tk) ?? NO_TOTALS;
const byToken = <V>(rows: Iterable<readonly [TokenId, V]>): ReadonlyMap<number, V> =>
  new Map([...rows].map(([id, v]) => [Number(id), v]));
const committedDeltas = (b: AccountBody): ReadonlyMap<number, CommittedDelta> => {
  const totals = totalsOn(b, () => true);
  return byToken([...b.account.deltas.values()].map((d): readonly [TokenId, CommittedDelta] => {
    const s = totals.get(d.tokenId) ?? NO_TOTALS;
    return [d.tokenId, {
      tokenId: Number(d.tokenId), collateral: d.collateral, ondelta: d.ondelta, offdelta: d.offdelta,
      leftCreditLimit: d.leftCreditLimit, rightCreditLimit: d.rightCreditLimit,
      leftAllowance: s.leftAllowance, rightAllowance: s.rightAllowance, leftHold: s.leftHold, rightHold: s.rightHold,
    }];
  }));
};
/** og's CommittedAccountState: every token key valid, the finalized height safe, both claim tries built. */
const project = (b: AccountBody): Result<CommittedAccountState, ViewError> => {
  const height = b.finalizedJHeight;
  if (height < 0n || height > BigInt(Number.MAX_SAFE_INTEGER)) return err({ _tag: "bad_j_claims" });
  const keys = [...b.account.deltas.keys(), ...b.requested.keys(), ...b.requestFees.keys(), ...b.feePolicies.keys()];
  const pending = (): Result<{ left: JClaimAccumulator; right: JClaimAccumulator }, ViewError> =>
    all({ left: pendingOn(b, true), right: pendingOn(b, false) });
  return chain(traverse(keys, tokenNumber), () => map(pending(), ({ left, right }) => {
    const { terms } = b;
    const swapOffers = [...b.offers].map(([id, o]) =>
      [id, { ...o, giveTokenId: Number(o.giveTokenId), wantTokenId: Number(o.wantTokenId) }] as const);
    return {
      domain: terms.domain, leftEntity: b.account.id.left, rightEntity: b.account.id.right,
      watchSeed: terms.watchSeed, disputeConfig: terms.disputeConfig,
      jNonce: b.jNonce, lastFinalizedJHeight: Number(height), leftPendingJClaims: left, rightPendingJClaims: right,
      deltas: committedDeltas(b), locks: new Map([...b.locks].map(([id, l]) => [id, ogLockRow(l)])),
      pulls: b.pulls ?? new Map(), swapOffers: new Map(swapOffers), subcontracts: new Map(),
      lendingIntents: b.lendingIntents, requestedRebalance: byToken(b.requested),
      requestedRebalanceFeeState: byToken(b.requestFees), rebalanceFeePolicies: byToken(b.feePolicies),
      settlementWorkspace: b.settlement,
    };
  }));
};
// Projection and preparation are pure but costly, so each body's view is computed once and the bodies whose
// commitment was already prepared are remembered; preparing a successor then only hashes what changed.
const views = new WeakMap<AccountBody, Result<CommittedAccountState, ViewError>>();
const preparedBodies = new WeakSet<AccountBody>();
export const committedView = (b: AccountBody): Result<CommittedAccountState, ViewError> => {
  const held = views.get(b);
  if (held !== undefined) return held;
  const view = project(b);
  views.set(b, view);
  return view;
};
export const prepareCommitment = (b: AccountBody): Result<PreparedCommitment, ViewError> =>
  chain(committedView(b), (v) => map(prepareState(v), (p) => {
    preparedBodies.add(b);
    return p;
  }));
export const prepareStep = (before: AccountBody, after: AccountBody): Result<void, ViewError> =>
  chain(committedView(after), (next) => {
    const was = preparedBodies.has(before) ? committedView(before) : undefined;
    const prepared = was?.ok === true ? prepareChanges(was.value, next) : map(prepareState(next), () => undefined);
    return map(prepared, () => {
      preparedBodies.add(after);
    });
  });
export type Committed = { readonly view: CommittedAccountState; readonly root: string };
export const committed = (b: AccountBody): Result<Committed, ViewError> =>
  chain(prepareCommitment(b), (p) => map(preparedRoot(p), (root) => ({ view: p.state, root })));
export const committedRoot = (b: AccountBody): Result<string, ViewError> => map(committed(b), (c) => c.root);


export type ProofError =
  | Tagged<"bad_watch_seed" | "bad_dispute_config" | "offdelta_beyond_money" | "final_delta_overflow">
  | Tagged<"final_delta_int256_min" | "too_many_tokens" | "bad_domain" | "bad_entity" | "bad_nonce" | "bad_hash">
  | Tagged<"transformer", { code: string }>;
const MAX_MONEY = 1n << 200n, MAX_PROOF_TOKENS = 128;
const inInt256 = (n: bigint): boolean => n >= INT256_MIN && n <= INT256_MAX;
// ---- og proof-builder.ts buildCanonicalProofBatches: the DeltaTransformer clauses of locks, swaps and pulls ----
export type ProofPayment = {
  readonly deltaIndex: number; readonly amount: bigint; readonly revealedUntilTimestamp: bigint; readonly hash: string;
};
export type ProofSwap = {
  readonly ownerIsLeft: boolean;
  readonly addDeltaIndex: number; readonly addAmount: bigint;
  readonly subDeltaIndex: number; readonly subAmount: bigint;
};
export type ProofPull = {
  readonly deltaIndex: number; readonly amount: bigint; readonly claimedRatio: number;
  readonly fullHash: string; readonly partialRoot: string; readonly targetRole: boolean;
};
export type ProofBatch = {
  readonly payments: readonly ProofPayment[];
  readonly swaps: readonly ProofSwap[];
  readonly pulls: readonly ProofPull[];
};
const paymentAbi = (p: ProofPayment): Abi =>
  t([A.uint(BigInt(p.deltaIndex)), signedAmountAbi(p.amount), A.uint(p.revealedUntilTimestamp), A.b32(p.hash)]);
const swapAbi = (s: ProofSwap): Abi => t([
  A.bool(s.ownerIsLeft),
  A.uint(BigInt(s.addDeltaIndex)), A.uint(s.addAmount),
  A.uint(BigInt(s.subDeltaIndex)), A.uint(s.subAmount),
]);
const pullAbi = (p: ProofPull): Abi => t([
  A.uint(BigInt(p.deltaIndex)), signedAmountAbi(p.amount), A.uint(BigInt(p.claimedRatio)),
  A.b32(p.fullHash), A.b32(p.partialRoot), A.bool(p.targetRole),
]);
const deltaBatchAbi = (b: ProofBatch): Abi =>
  t([arr(b.payments, paymentAbi), arr(b.swaps, swapAbi), arr(b.pulls, pullAbi)]);
export const encodeDeltaBatch = (b: ProofBatch): string => abiEncodeHex([deltaBatchAbi(b)]);
/**
 * og storageAtomBytes: 1 + msgpackr({kind:'atom', value}) of the batch hex.
 * Every clause is static, so the ABI is 7 head words plus 5/5/7 words per item.
 */
const MAX_PROOF_ATOM_BYTES = 10_000;
const batchAtomBytes = (b: ProofBatch): number => {
  const len = 2 + 2 * 32 * (7 + 5 * b.payments.length + 5 * b.swaps.length + 7 * b.pulls.length);
  const header = len < 32 ? 1 : len < 256 ? 2 : len < 65536 ? 3 : 5;
  return len + 21 + header;
};
const transferChange = (senderIsLeft: boolean, amount: bigint): bigint => (senderIsLeft ? -amount : amount);
/** One signed change a clause may make to one delta: positive moves toward left, negative toward right. */
type Movement = readonly [deltaIndex: number, change: bigint];
const batchMovements = (b: ProofBatch): readonly Movement[] => [
  ...b.payments.map((p): Movement => [p.deltaIndex, p.amount]),
  ...b.swaps.flatMap((s): readonly Movement[] => [
    [s.addDeltaIndex, transferChange(s.ownerIsLeft, s.addAmount)],
    [s.subDeltaIndex, transferChange(!s.ownerIsLeft, s.subAmount)],
  ]),
  ...b.pulls.map((p): Movement => [p.deltaIndex, p.amount]),
];
const deltaAllowance = (deltaIndex: number, moves: readonly Movement[]): JAllowance => ({
  deltaIndex: BigInt(deltaIndex),
  rightAllowance: moves.reduce((sum, [, change]) => (change < 0n ? sum - change : sum), 0n),
  leftAllowance: moves.reduce((sum, [, change]) => (change > 0n ? sum + change : sum), 0n),
});
/** og buildTransformerAllowances: per deltaIndex, positive movements authorize left and negative ones right. */
export const batchAllowances = (b: ProofBatch): readonly JAllowance[] => {
  const moving = batchMovements(b).filter(([, change]) => change !== 0n);
  return [...Map.groupBy(moving, ([deltaIndex]) => deltaIndex)]
    .toSorted(([x], [y]) => x - y)
    .map(([deltaIndex, moves]) => deltaAllowance(deltaIndex, moves));
};
const fitsUint256 = (a: JAllowance): boolean => a.leftAllowance <= UINT256_MAX && a.rightAllowance <= UINT256_MAX;
const batchFits = (b: ProofBatch): boolean =>
  batchAllowances(b).every(fitsUint256) && batchAtomBytes(b) < MAX_PROOF_ATOM_BYTES;
const transformerError = (code: string): ProofError => ({ _tag: "transformer", code });
const proofErr = (code: string): Result<never, ProofError> => err(transformerError(code));
type Chunking<X> = { readonly done: readonly ProofBatch[]; readonly open: readonly X[] };
/**
 * og chunkProofItems: greedy in canonical order; one item that alone does not fit refuses the proof
 * (its index counts this kind's batches only).
 */
const chunkProof = <X>(
  items: readonly X[], make: (xs: readonly X[]) => ProofBatch,
): Result<readonly ProofBatch[], ProofError> => {
  const closed = ({ done, open }: Chunking<X>): readonly ProofBatch[] =>
    (open.length > 0 ? [...done, make(open)] : done);
  const place = (c: Chunking<X>, item: X): Result<Chunking<X>, ProofError> => {
    const alone = make([item]);
    switch (true) {
      case batchFits(make([...c.open, item])): return ok({ ...c, open: [...c.open, item] });
      case batchFits(alone): return ok({ done: closed(c), open: [item] });
      default: return proofErr(
        `ACCOUNT_DISPUTE_PROOF_ATOM_BYTES_EXCEEDED:transformer=${closed(c).length}:`
        + `${batchAtomBytes(alone)}/${MAX_PROOF_ATOM_BYTES}`,
      );
    }
  };
  return map(foldResult<Chunking<X>, X, ProofError>(items, { done: [], open: [] }, place), closed);
};
type ProofLockRow = {
  readonly hashlock: string; readonly timelock: bigint; readonly amount: bigint;
  readonly tokenId: number; readonly senderIsLeft: boolean;
};
const byTextKey = <V>(m: CommittedMap): readonly (readonly [string, V])[] =>
  ([...m] as [string, V][]).toSorted(([x], [y]) => asc(String(x), String(y)));
/** Delta rows in token order: a row's position is its deltaIndex in every clause. */
const tokenRows = (s: CommittedAccountState): readonly CommittedDelta[] =>
  [...s.deltas.values()].toSorted((x, y) => x.tokenId - y.tokenId);
const proofPayment = (deltaIndexOf: DeltaIndexOf) => ([id, l]: readonly [string, ProofLockRow]) => {
  const seconds = (l.timelock - 1n) / 1000n;
  return seconds <= 0n || seconds > BigInt(Number.MAX_SAFE_INTEGER)
    ? proofErr(`HTLC_LOCK_INVALID_TIMELOCK:${id}`)
    : map(deltaIndexOf(l.tokenId, `PROOF_BODY_LOCK_TOKEN_MISSING:${id}:${l.tokenId}`), (deltaIndex): ProofPayment => ({
      deltaIndex, amount: transferChange(l.senderIsLeft, l.amount), revealedUntilTimestamp: seconds, hash: l.hashlock,
    }));
};
const proofSwap = (deltaIndexOf: DeltaIndexOf) => ([id, o]: readonly [string, SwapOffer]) => {
  const code = `PROOF_BODY_SWAP_TOKEN_MISSING:${id}:give=${o.giveTokenId}:want=${o.wantTokenId}`;
  const legs = all({ add: deltaIndexOf(Number(o.giveTokenId), code), sub: deltaIndexOf(Number(o.wantTokenId), code) });
  return map(legs, ({ add, sub }): ProofSwap => ({
    ownerIsLeft: o.makerIsLeft,
    addDeltaIndex: add, addAmount: o.giveAmount,
    subDeltaIndex: sub, subAmount: o.wantAmount,
  }));
};
const claimedRatioWord = (p: PullRow): number => Math.max(0, Math.min(0xffff, Math.floor(Number(p.claimedRatio ?? 0))));
const proofPull = (deltaIndexOf: DeltaIndexOf) => ([id, p]: readonly [string, PullRow]) =>
  map(deltaIndexOf(p.tokenId, `PROOF_BODY_PULL_TOKEN_MISSING:${id}:${p.tokenId}`), (deltaIndex): ProofPull => ({
    deltaIndex, amount: p.amount, claimedRatio: claimedRatioWord(p),
    fullHash: p.fullHash, partialRoot: p.partialRoot, targetRole: p.crossJurisdiction?.leg === "target",
  }));
type DeltaIndexOf = (tokenId: number, code: string) => Result<number, ProofError>;
const deltaIndexing = (s: CommittedAccountState): DeltaIndexOf => {
  const index = new Map(tokenRows(s).map((r, i) => [r.tokenId, i] as const));
  return (tokenId, code) => {
    const i = index.get(tokenId);
    return i === undefined ? proofErr(code) : ok(i);
  };
};
const EMPTY_BATCH: ProofBatch = { payments: [], swaps: [], pulls: [] };
/** og buildCanonicalProofBatches: payments (locks by lockId), same-J swaps (by offerId), pulls (by pullId). */
export const proofBatches = (s: CommittedAccountState): Result<readonly ProofBatch[], ProofError> => {
  const deltaIndexOf = deltaIndexing(s);
  const sameJSwaps = byTextKey<SwapOffer>(s.swapOffers).filter(([, o]) => !o.crossJurisdiction);
  const kinds = all({
    payments: chain(traverse(byTextKey<ProofLockRow>(s.locks), proofPayment(deltaIndexOf)),
      (items) => chunkProof(items, (payments) => ({ ...EMPTY_BATCH, payments }))),
    swaps: chain(traverse(sameJSwaps, proofSwap(deltaIndexOf)),
      (items) => chunkProof(items, (swaps) => ({ ...EMPTY_BATCH, swaps }))),
    pulls: chain(traverse(byTextKey<PullRow>(s.pulls), proofPull(deltaIndexOf)),
      (items) => chunkProof(items, (pulls) => ({ ...EMPTY_BATCH, pulls }))),
  });
  return map(kinds, ({ payments, swaps, pulls }) => [...payments, ...swaps, ...pulls]);
};
/**
 * og requireAccountDeltaTransformerAddress: the one durable jurisdiction whose chainId and first usable Depository
 * are the Account's domain; its stack must name all four contracts.
 * Names, active-J defaults and peer payloads are never proof authority.
 */
export type DeltaTransformerRef = Result<string, string>;
const STACK_CONTRACTS = [
  ["DEPOSITORY", "depository"], ["ENTITY_PROVIDER", "entityProvider"],
  ["ACCOUNT", "account"], ["DELTA_TRANSFORMER", "deltaTransformer"],
] as const;
const servesDomain = (r: JReplica, domain: Domain): boolean =>
  Number(r.chainId) === domain.chainId
  && usableAddress(r.contracts?.depository)?.toLowerCase() === domain.depositoryAddress.toLowerCase();
const stackTransformer = (r: JReplica): Result<string, string> => {
  const chainId = Number(r.chainId);
  if (!Number.isSafeInteger(chainId) || chainId <= 0) return err("JURISDICTION_DURABLE_STACK_CHAIN_ID_MISSING");
  const missing = STACK_CONTRACTS.find(([, key]) => usableAddress(r.contracts?.[key]) === null);
  return missing === undefined
    ? ok(String(r.contracts?.deltaTransformer).toLowerCase())
    : err(`JURISDICTION_DURABLE_STACK_${missing[0]}_MISSING`);
};
const jurisdictionMiss = (domain: Domain, found: number): string =>
  `ACCOUNT_PROOF_JURISDICTION_${found === 0 ? "NOT_FOUND" : "AMBIGUOUS"}`
  + `:${domain.chainId}:${domain.depositoryAddress.toLowerCase()}`;
export const deltaTransformerFor = (
  jReplicas: ReadonlyMap<string, JReplica> | undefined, domain: Domain,
): DeltaTransformerRef => {
  const serving = [...jReplicas?.values() ?? []].filter((r) => servesDomain(r, domain));
  return chain(traverse(serving, stackTransformer), (found) => {
    const [only] = found;
    return found.length === 1 && only !== undefined ? ok(only) : err(jurisdictionMiss(domain, found.length));
  });
};
const MAX_PROOF_TRANSFORMERS = 32, MAX_PROOF_BODY_BYTES = 176 * 1024;
const isUint32 = (n: number): boolean => Number.isInteger(n) && n >= 0 && n <= 0xffff_ffff;
const deltaRowIssue = (r: CommittedDelta): ProofError | undefined => {
  const final = r.ondelta + r.offdelta;
  switch (true) {
    case !inInt256(r.ondelta) || !inInt256(r.offdelta): return { _tag: "final_delta_overflow" };
    case r.offdelta > MAX_MONEY || r.offdelta < -MAX_MONEY: return { _tag: "offdelta_beyond_money" };
    case !inInt256(final): return { _tag: "final_delta_overflow" };
    case final === INT256_MIN: return { _tag: "final_delta_int256_min" };
    default: return undefined;
  }
};
/** Why the body's fixed part (seed, response windows, token rows) cannot be signed, if it cannot. */
const proofHeaderIssue = (s: CommittedAccountState, rows: readonly CommittedDelta[]): ProofError | undefined => {
  const { leftResponseSeconds: left, rightResponseSeconds: right } = s.disputeConfig;
  const rowIssue = rows.map(deltaRowIssue).find((e) => e !== undefined);
  switch (true) {
    case !WORD.test(s.watchSeed): return { _tag: "bad_watch_seed" };
    case !isUint32(left) || !isUint32(right): return { _tag: "bad_dispute_config" };
    case rowIssue !== undefined: return rowIssue;
    case rows.length > MAX_PROOF_TOKENS: return { _tag: "too_many_tokens" };
    default: return undefined;
  }
};
const signingTransformer = (
  s: CommittedAccountState, ref: DeltaTransformerRef | undefined,
): Result<string, ProofError> => {
  const resolved = ref ?? err(jurisdictionMiss(s.domain, 0));
  return chain(mapErr(resolved, transformerError), (address) =>
    (usableAddress(address) === null ? proofErr("MISSING_DELTA_TRANSFORMER_ADDRESS") : ok(address)));
};
const proofClauses = (
  s: CommittedAccountState, batches: readonly ProofBatch[], ref: DeltaTransformerRef | undefined,
): Result<readonly TransformerClause[], ProofError> =>
  (batches.length === 0 ? ok([]) : map(signingTransformer(s, ref), (transformerAddress) =>
    batches.map((b): TransformerClause => ({
      transformerAddress, encodedBatch: encodeDeltaBatch(b), allowances: batchAllowances(b),
    }))));
const proofBodyOf = (
  s: CommittedAccountState, rows: readonly CommittedDelta[], transformers: readonly TransformerClause[],
): ProofBody => ({
  watchSeed: s.watchSeed.toLowerCase(),
  leftResponseSeconds: BigInt(s.disputeConfig.leftResponseSeconds),
  rightResponseSeconds: BigInt(s.disputeConfig.rightResponseSeconds),
  offdeltas: rows.map((r) => r.offdelta),
  tokenIds: rows.map((r) => BigInt(r.tokenId)),
  transformers,
});
const boundedBody = (body: ProofBody): Result<ProofBody, ProofError> => {
  const count = body.transformers.length;
  if (count > MAX_PROOF_TRANSFORMERS) return proofErr(`J_DISPUTE_PROOFBODY_TRANSFORMER_LIMIT:account.signing:${count}`);
  const size = (encodeProofBodyBytes(body).length - 2) / 2;
  return size > MAX_PROOF_BODY_BYTES
    ? proofErr(`J_DISPUTE_PROOFBODY_BYTES_EXCEEDED:account.signing:${size}/${MAX_PROOF_BODY_BYTES}`)
    : ok(body);
};
/**
 * og buildAccountProofBody. The DeltaTransformer is read only when the Account has clauses to sign; og resolves it for
 * every body, but an Account without locks, swaps or pulls signs the same body either way, so only an unresolved stack
 * that would be read refuses.
 */
export const accountProofBody = (
  s: CommittedAccountState, deltaTransformer?: DeltaTransformerRef,
): Result<ProofBody, ProofError> => {
  const rows = tokenRows(s);
  const issue = proofHeaderIssue(s, rows);
  if (issue !== undefined) return err(issue);
  const transformers = chain(proofBatches(s), (batches) => proofClauses(s, batches, deltaTransformer));
  return chain(transformers, (clauses) => boundedBody(proofBodyOf(s, rows, clauses)));
};
export const proofBodyHash = (b: ProofBody): string => keccak256Hex(hexToBytes(encodeProofBodyBytes(b)));
const BYTES32 = /^0[xX][0-9a-fA-F]{64}$/;
const disputeDomain = (s: CommittedAccountState): Result<Domain, ProofError> => {
  const domain = domainOf(s.domain);
  return domain.ok && !/^0x0{40}$/.test(domain.value.depositoryAddress)
    ? ok(domain.value)
    : err({ _tag: "bad_domain" });
};
const disputeHashIssue = (s: CommittedAccountState, bodyHash: string, nonce: number): ProofError | undefined => {
  switch (true) {
    case !BYTES32.test(s.leftEntity) || !BYTES32.test(s.rightEntity): return { _tag: "bad_entity" };
    case !Number.isSafeInteger(nonce) || nonce < 0: return { _tag: "bad_nonce" };
    case !BYTES32.test(bodyHash): return { _tag: "bad_hash" };
    case !BYTES32.test(s.watchSeed): return { _tag: "bad_watch_seed" };
    default: return undefined;
  }
};
export const accountDisputeHash = (
  s: CommittedAccountState, bodyHash: string, nonce: number, proposerIsLeft: boolean,
): Result<string, ProofError> => chain(disputeDomain(s), (domain) => {
  const issue = disputeHashIssue(s, bodyHash, nonce);
  if (issue !== undefined) return err(issue);
  const accountKey = encodeAccountKey({ e1: s.leftEntity.toLowerCase(), e2: s.rightEntity.toLowerCase() });
  return ok(encodeDisputeProofHash({
    messageType: 1, chainId: domain.chainId, contractAddress: domain.depositoryAddress,
    accountKey: accountKey.lesserThenGreater, nonce: String(nonce), proposerIsLeft,
    proofbodyHash: bodyHash.toLowerCase(), watchSeed: s.watchSeed.toLowerCase(),
  }));
});
export type DisputeHanko = {
  readonly hanko: string; readonly hash: string; readonly proofBodyHash: string;
  readonly proofNonce: number; readonly proposerIsLeft: boolean;
};
export type DisputeDraft = Omit<DisputeHanko, "hanko">;
export type DisputeWitnesses = {
  readonly nextProofNonce: number;
  readonly current?: DisputeHanko | undefined;
  readonly counterparty?: DisputeHanko | undefined;
};
export const genesisWitnesses = (): DisputeWitnesses => ({ nextProofNonce: 1 });
/**
 * og operations.ts getMinimumSafeSettlementNonce, replica cursors: every locally known signed proof nonce is spent
 * (the body adds jNonce + 1).
 */
export const proofNonceFloor = (w: DisputeWitnesses): number =>
  Math.max(w.nextProofNonce, (w.current?.proofNonce ?? 0) + 1, (w.counterparty?.proofNonce ?? 0) + 1);
export const settlementOf = (w: DisputeWitnesses, verify: Verify): SettlementCtx =>
  ({ verify, proofNonceFloor: proofNonceFloor(w) });
export type DisputePlan =
  | Tagged<"sign", { draft: DisputeDraft }>
  | Tagged<"resend", { disputeHanko: DisputeHanko }>
  | Tagged<"none">;
export type DisputeReason =
  | "hanko_missing" | "shape" | "hash_mismatch" | "hanko_invalid" | "unexpected" | "required" | "draft_mismatch"
  | "nonce_finalized" | "nonce_regression" | "nonce_reuse" | "body_mismatch";
export type DisputeError =
  | Tagged<"dispute_hanko", { reason: DisputeReason }>
  | Tagged<"dispute_proof", { error: ProofError | ViewError }>;
const refuseDispute = (reason: DisputeReason): DisputeError => ({ _tag: "dispute_hanko", reason });
const asProof = <X>(r: Result<X, ProofError | ViewError>): Result<X, DisputeError> =>
  mapErr(r, (error): DisputeError => ({ _tag: "dispute_proof", error }));
/**
 * og securityContext.verifyHanko authority: `allowPreviousBoard` admits the immediately previous board within its grace
 * window (ACK and replay paths only, never a fresh proposal); `registeredBoardHash` pins the certified board.
 */
export type HankoAuthority = {
  readonly allowPreviousBoard: boolean; readonly registeredBoardHash?: string | undefined;
};
export type Verify = (digest: string, hanko: string, entity: EntityId, authority?: HankoAuthority) => boolean;
export type LocalProof = Tagged<"complete", { body: ProofBody; bodyHash: string; jNonce: number }>;
export type CompleteProof = Of<LocalProof, "complete">;
const proofOf = (view: CommittedAccountState, dt?: DeltaTransformerRef): Result<LocalProof, ProofError> =>
  map(accountProofBody(view, dt), (body): LocalProof =>
    ({ _tag: "complete", body, bodyHash: proofBodyHash(body), jNonce: view.jNonce }));
export const localProof = (view: CommittedAccountState, dt?: DeltaTransformerRef): Result<LocalProof, DisputeError> =>
  asProof(proofOf(view, dt));
export type StartRefusal =
  | Tagged<"no_witness" | "body_mismatch" | "nonce_stale" | "hash_mismatch" | "hanko_invalid" | "not_attempted">
  | Tagged<"proof", { error: ProofError | ViewError }>;
const asStartRefusal = <X>(r: Result<X, ProofError | ViewError>): Result<X, StartRefusal> =>
  mapErr(r, (error): StartRefusal => ({ _tag: "proof", error }));
const witnessedStart = (proof: CompleteProof, w: DisputeHanko, peer: EntityId): DisputeStart => ({
  counterentity: peer, nonce: BigInt(w.proofNonce), proposerIsLeft: w.proposerIsLeft, proofbodyHash: w.proofBodyHash,
  initialProofbody: proof.body, watchSeed: proof.body.watchSeed, sig: w.hanko,
  starterInitialArguments: "0x", starterCounterArguments: "0x", starterCounterProofCommitment: ZERO_WORD,
});
/** The counterparty's witness opens a dispute on our proof only if it signs this very body at a nonce past jNonce. */
export const disputeStart = (
  view: CommittedAccountState, proof: CompleteProof, w: DisputeHanko, peer: EntityId, verify: Verify,
): Result<DisputeStart, StartRefusal> => {
  if (!sameHex(proof.bodyHash, w.proofBodyHash)) return err({ _tag: "body_mismatch" });
  if (w.proofNonce <= 0 || w.proofNonce <= proof.jNonce) return err({ _tag: "nonce_stale" });
  const hash = asStartRefusal(accountDisputeHash(view, w.proofBodyHash, w.proofNonce, w.proposerIsLeft));
  return chain(hash, (h): Result<DisputeStart, StartRefusal> => {
    switch (true) {
      case !sameHex(h, w.hash): return err({ _tag: "hash_mismatch" });
      case !verify(h, w.hanko, peer): return err({ _tag: "hanko_invalid" });
      default: return ok(witnessedStart(proof, w, peer));
    }
  });
};
export const startOf = (
  body: AccountBody, witnesses: DisputeWitnesses, peer: EntityId, verify: Verify, dt?: DeltaTransformerRef,
): Result<DisputeStart, StartRefusal> => {
  const w = witnesses.counterparty;
  if (w === undefined) return err({ _tag: "no_witness" });
  return chain(asStartRefusal(committedView(body)), (view) =>
    chain(asStartRefusal(proofOf(view, dt)), (proof) => disputeStart(view, proof, w, peer, verify)));
};
/** A held witness is stale once it signs another body or a nonce the jurisdiction already finalized. */
const staleWitness = (held: DisputeHanko | undefined, proof: Pick<LocalProof, "bodyHash" | "jNonce">): boolean =>
  !sameHex(proof.bodyHash, held?.proofBodyHash) || (held?.proofNonce ?? 0) <= proof.jNonce;
const freshNonce = (w: DisputeWitnesses, jNonce: number): number => Math.max(w.nextProofNonce, jNonce + 1);
const draftPlan = (
  view: CommittedAccountState, bodyHash: string, proofNonce: number, proposerIsLeft: boolean,
): Result<DisputePlan, DisputeError> =>
  map(asProof(accountDisputeHash(view, bodyHash, proofNonce, proposerIsLeft)), (hash): DisputePlan =>
    ({ _tag: "sign", draft: { hash, proofBodyHash: bodyHash, proofNonce, proposerIsLeft } }));
export const proposalPlan = (
  view: CommittedAccountState, proof: LocalProof, witnesses: DisputeWitnesses, proposerIsLeft: boolean,
): Result<DisputePlan, DisputeError> => {
  const { current } = witnesses;
  if (staleWitness(current, proof)) {
    return draftPlan(view, proof.bodyHash, freshNonce(witnesses, proof.jNonce), proposerIsLeft);
  }
  return ok(current !== undefined && current.proposerIsLeft === proposerIsLeft
    ? { _tag: "resend", disputeHanko: current }
    : { _tag: "none" });
};
type AckedFrame = {
  readonly candidate: { readonly draft: { readonly state: AccountBody }; readonly frameProof: LocalProof };
  readonly dispute: DisputeWitnesses;
};
export const ackPlan = (held: AckedFrame, proposerIsLeft: boolean): Result<DisputePlan, DisputeError> => {
  const { candidate: { draft, frameProof }, dispute: witnesses } = held, { current } = witnesses;
  const changed = staleWitness(current, frameProof) || current?.proposerIsLeft !== proposerIsLeft;
  if (!changed && current !== undefined) return ok({ _tag: "resend", disputeHanko: current });
  return chain(asProof(committedView(draft.state)), (view) =>
    draftPlan(view, frameProof.bodyHash, freshNonce(witnesses, frameProof.jNonce), proposerIsLeft));
};
const sameDraft = (a: DisputeDraft, b: DisputeDraft): boolean =>
  a.hash === b.hash && a.proofBodyHash === b.proofBodyHash
  && a.proofNonce === b.proofNonce && a.proposerIsLeft === b.proposerIsLeft;
export type Settled = { readonly carried: DisputeHanko | undefined; readonly witnesses: DisputeWitnesses };
const requiredHanko = (given: DisputeHanko | undefined): Result<DisputeHanko, DisputeError> =>
  (given === undefined ? err(refuseDispute("required")) : ok(given));
/** Our own hanko on the frame must be exactly what the plan asked for: a fresh signature, the held one, or none. */
export const settleLocal = (
  plan: DisputePlan, given: DisputeHanko | undefined, witnesses: DisputeWitnesses, self: EntityId, verify: Verify,
): Result<Settled, DisputeError> => match(plan, {
  sign: ({ draft }) => chain(requiredHanko(given), (g): Result<Settled, DisputeError> => {
    const signed: DisputeHanko = { ...draft, hanko: g.hanko };
    switch (true) {
      case !sameDraft(g, draft): return err(refuseDispute("draft_mismatch"));
      case !verify(draft.hash, g.hanko, self): return err(refuseDispute("hanko_invalid"));
      default: return ok({
        carried: signed, witnesses: { ...witnesses, current: signed, nextProofNonce: draft.proofNonce + 1 },
      });
    }
  }),
  resend: ({ disputeHanko }) => chain(requiredHanko(given), (g): Result<Settled, DisputeError> =>
    (g.hanko === disputeHanko.hanko && sameDraft(g, disputeHanko)
      ? ok({ carried: disputeHanko, witnesses })
      : err(refuseDispute("draft_mismatch")))),
  none: (): Result<Settled, DisputeError> =>
    (given === undefined ? ok({ carried: undefined, witnesses }) : err(refuseDispute("unexpected"))),
});
/** A signed post-settlement proof whose settlement this frame finalizes, with both hankos present. */
type Promotion = {
  readonly proof: PostSettlementProof; readonly left: string; readonly right: string; readonly signed: number;
};
const promotionOf = (pre: AccountBody, finalized: readonly number[]): Promotion | undefined => {
  const ws = pre.settlement, proof = ws?.postSettlementDisputeProof, signed = ws?.nonceAtSign;
  if (ws === undefined || !signedWorkspace(ws) || proof === undefined || signed === undefined) return undefined;
  const { leftHanko: left, rightHanko: right } = proof;
  const decides = pre.jNonce < signed && finalized.find((n) => n >= signed) === signed;
  return decides && left !== undefined && right !== undefined ? { proof, left, right, signed } : undefined;
};
const samePostProof = (held: DisputeHanko, p: PostSettlementProof): boolean =>
  sameHex(held.proofBodyHash, p.proofBodyHash) && held.proposerIsLeft === p.proposerIsLeft
  && sameHex(held.hash, p.disputeHash);
/** One side's witness after promotion: a newer proof stays, an older one yields, an equal nonce must be this proof. */
const promotedWitness = (
  p: PostSettlementProof, held: DisputeHanko | undefined, hanko: string,
): Result<DisputeHanko | undefined, DisputeError> => {
  const heldNonce = held?.proofNonce ?? 0;
  switch (true) {
    case heldNonce > p.nonce: return ok(held);
    case heldNonce < p.nonce: return ok({
      hanko, hash: p.disputeHash, proofBodyHash: p.proofBodyHash, proofNonce: p.nonce, proposerIsLeft: p.proposerIsLeft,
    });
    case held !== undefined && samePostProof(held, p): return ok(held);
    default: return err(refuseDispute("nonce_reuse"));
  }
};
/**
 * og finality.ts activatePostSettlementProof, replica side: the frame whose J claim finalizes the signed settlement
 * nonce promotes both N+1 hankos into the dispute witnesses (an equal nonce must be the same proof) and moves the proof
 * cursor past everything spent. `finalized` lists each finalizing claim's reached nonce in frame order: the first to
 * reach the signed nonce decides, even when a later claim of the same frame finalizes past it.
 */
export const promoteSettled = (
  w: DisputeWitnesses, pre: AccountBody, post: AccountBody, localIsLeft: boolean,
  finalized: readonly number[] = post.jNonce !== pre.jNonce ? [post.jNonce] : [],
): Result<DisputeWitnesses, DisputeError> => {
  const promotion = promotionOf(pre, finalized);
  if (promotion === undefined) return ok(w);
  const { proof, left, right, signed } = promotion;
  const sides = all({
    current: promotedWitness(proof, w.current, localIsLeft ? left : right),
    counterparty: promotedWitness(proof, w.counterparty, localIsLeft ? right : left),
  });
  return map(sides, ({ current, counterparty }): DisputeWitnesses => {
    const promoted = {
      ...opt("current", current), ...opt("counterparty", counterparty), nextProofNonce: w.nextProofNonce,
    };
    return { ...promoted, nextProofNonce: Math.max(proofNonceFloor(promoted), proof.nonce + 1, signed + 1) };
  });
};
const disputeHankoShaped = (given: DisputeHanko): boolean =>
  WORD.test(given.hash) && WORD.test(given.proofBodyHash)
  && Number.isSafeInteger(given.proofNonce) && given.proofNonce >= 0 && typeof given.proposerIsLeft === "boolean";
export const validateCounterparty = (
  body: AccountBody, given: DisputeHanko, from: EntityId, verify: Verify,
  authority: HankoAuthority = { allowPreviousBoard: true },
): Result<DisputeHanko, DisputeError> => {
  if (given.hanko.length === 0) return err(refuseDispute("hanko_missing"));
  if (!disputeHankoShaped(given)) return err(refuseDispute("shape"));
  const expectedHash = chain(asProof(committedView(body)), (view) =>
    asProof(accountDisputeHash(view, given.proofBodyHash, given.proofNonce, given.proposerIsLeft)));
  return chain(expectedHash, (expected): Result<DisputeHanko, DisputeError> => {
    switch (true) {
      case !sameHex(given.hash, expected): return err(refuseDispute("hash_mismatch"));
      case !verify(expected, given.hanko, from, authority): return err(refuseDispute("hanko_invalid"));
      default: return ok({ ...given, hash: expected });
    }
  });
};
type ReceivedWitness = { readonly proofNonce: number; readonly proofBodyHash: string };
const receivedIssue = (
  received: ReceivedWitness, expectedBody: string,
  previousBody: string | undefined, previousNonce: number | undefined, jNonce: number,
): DisputeReason | undefined => {
  switch (true) {
    case received.proofNonce <= jNonce: return "nonce_finalized";
    case previousNonce !== undefined && received.proofNonce < previousNonce: return "nonce_regression";
    case received.proofNonce === previousNonce && previousBody !== undefined
      && !sameHex(received.proofBodyHash, previousBody): return "nonce_reuse";
    case !sameHex(received.proofBodyHash, expectedBody): return "body_mismatch";
    default: return undefined;
  }
};
/** Why the counterparty's witness is refused: it must be fresh for our body, and present once ours moved. */
export const disputeRequirement = (
  expectedBody: string | undefined, previousBody: string | undefined, previousNonce: number | undefined,
  jNonce: number, received: ReceivedWitness | undefined,
): DisputeReason | undefined => {
  if (expectedBody === undefined) return received === undefined ? undefined : "unexpected";
  if (received !== undefined) return receivedIssue(received, expectedBody, previousBody, previousNonce, jNonce);
  return !sameHex(expectedBody, previousBody) || (previousNonce ?? 0) <= jNonce ? "required" : undefined;
};
/**
 * og getDisputeHankoRequirementError's text for a requireDispute refusal (og's failureMessage for an unsafe frame);
 * undefined for any other refusal.
 */
export const disputeRequirementText = (
  e: DisputeError, proof: LocalProof, witnesses: DisputeWitnesses, received: DisputeHanko | undefined,
): string | undefined => {
  if (e._tag !== "dispute_hanko") return undefined;
  const n = received?.proofNonce, prev = witnesses.counterparty?.proofNonce;
  switch (e.reason) {
    case "unexpected": return "DISPUTE_HANKO_UNEXPECTED_WITHOUT_LOCAL_PROOF";
    case "nonce_finalized": return `DISPUTE_HANKO_NONCE_ALREADY_FINALIZED: received=${n} jNonce=${proof.jNonce}`;
    case "nonce_regression": return `DISPUTE_HANKO_NONCE_REGRESSION: received=${n} previous=${prev}`;
    case "nonce_reuse": return `DISPUTE_HANKO_NONCE_REUSE: nonce=${n}`;
    case "body_mismatch":
      return `DISPUTE_HANKO_PROOFBODY_MISMATCH: expected=${proof.bodyHash} received=${received?.proofBodyHash}`;
    case "required": return `DISPUTE_HANKO_REQUIRED: proofBodyHash=${proof.bodyHash} jNonce=${proof.jNonce}`;
    default: return undefined;
  }
};
export const requireDispute = (
  proof: LocalProof, witnesses: DisputeWitnesses, received: DisputeHanko | undefined,
): Result<void, DisputeError> => {
  const { counterparty } = witnesses;
  const reason = disputeRequirement(
    proof.bodyHash, counterparty?.proofBodyHash, counterparty?.proofNonce, proof.jNonce, received,
  );
  return reason === undefined ? ok(undefined) : err(refuseDispute(reason));
};
export const storeCounterparty = (witnesses: DisputeWitnesses, validated: DisputeHanko | undefined): DisputeWitnesses =>
  (validated === undefined ? witnesses : { ...witnesses, counterparty: validated });
const evenHex = (h: unknown): boolean => typeof h === "string" && hexBody(h).length > 0 && hexBody(h).length % 2 === 0;
export const disputeShapes = (carried: readonly (DisputeHanko | undefined)[]): Result<void, DisputeError> =>
  guard(carried.every((d) => d === undefined || evenHex(d.hanko)), refuseDispute("shape"));


export const ACCOUNT_NETWORK_ALLOWANCE_MS = 30_000n;
export const ACCOUNT_MEMPOOL_SIZE = 10_000;
export type FrameClock = { readonly timestamp: bigint; readonly jHeight: bigint };
export type FoldAt = FrameClock & { readonly height: bigint };
export type AccountFrame = FoldAt & {
  readonly prevFrameHash: string; readonly txs: readonly WireAccountTx[];
  readonly accountStateRoot: string; readonly stateHash: string;
};
const EMPTY_CLAIM_PROOF: JClaimProof = { version: 1, nodes: [] };
/**
 * `finalized`: the settled nonce each finalizing claim reaches, in frame order
 * (og collectSettledEvents: max of jNonce and the claim's nonces).
 */
export type StampedClaims = { readonly txs: readonly WireAccountTx[]; readonly finalized: readonly number[] };
type AccountKeyOf = () => Result<string, ClaimError>;
const claimProofOn = (
  accountKey: AccountKeyOf, rows: readonly ClaimRow[], record: ClaimRow,
): Result<JClaimProof, ClaimError> => {
  if (rows.length === 0) return ok(EMPTY_CLAIM_PROOF);
  return chain(accountKey(), (key) => map(claimTree(key, rows), (node): JClaimProof =>
    (node === undefined ? EMPTY_CLAIM_PROOF : { version: 1, nodes: claimPath(node, claimLeaf(key, record).key) })));
};
type ClaimWitnesses = { readonly leftProof: JClaimProof; readonly rightProof: JClaimProof };
type StampAcc = {
  readonly cursor: ClaimCursor; readonly jNonce: number;
  readonly txs: readonly WireAccountTx[]; readonly finalized: readonly number[];
};
/** Both side witnesses of one claim, against the pending tries as the frame's earlier claims left them. */
const claimWitnesses = (
  accountKey: AccountKeyOf, cursor: ClaimCursor, own: ClaimRow,
): Result<ClaimWitnesses, ClaimError> => {
  const held = cursor.claimRows ?? [];
  const side = (onLeft: boolean): Result<JClaimProof, ClaimError> =>
    claimProofOn(accountKey, held.filter((r) => r.onLeft === onLeft), { ...own, onLeft });
  return all({ leftProof: side(true), rightProof: side(false) });
};
const stampedClaim = (
  acc: StampAcc, tx: TxOf<"j_event_claim">, witnesses: ClaimWitnesses, step: ClaimStep, nonces: readonly number[],
): StampAcc => {
  const reached = step.finalizes ? Math.max(acc.jNonce, ...nonces) : acc.jNonce;
  return {
    cursor: step,
    jNonce: reached,
    txs: [...acc.txs, { ...tx, ...witnesses }],
    finalized: step.finalizes ? [...acc.finalized, reached] : acc.finalized,
  };
};
/** og proposal/transactions.ts prepareAccountJClaimTx, applied to every claim of the frame in order. */
const stampClaims = (
  txs: readonly WireAccountTx[], b: AccountBody, byLeft: boolean,
): Result<StampedClaims, ClaimError> => {
  const accountKey: AccountKeyOf = () => mapErr(claimKeyOf(b), (): ClaimError => ({ _tag: "claim_entity" }));
  const stampClaim = (acc: StampAcc, tx: TxOf<"j_event_claim">): Result<StampAcc, ClaimError> =>
    chain(claimRowOf(tx, byLeft), (own) =>
      chain(claimWitnesses(accountKey, acc.cursor, own), (witnesses) =>
        chain(claimStep(acc.cursor, own), (step) =>
          map(claimEvidence(tx.events), ({ events }) =>
            stampedClaim(acc, tx, witnesses, step, events.map((e) => e.data.nonce))))));
  const stamp = (acc: StampAcc, tx: WireAccountTx): Result<StampAcc, ClaimError> =>
    (tx.type === "j_event_claim" ? stampClaim(acc, tx) : ok({ ...acc, txs: [...acc.txs, tx] }));
  const start: StampAcc = { cursor: b, jNonce: b.jNonce, txs: [], finalized: [] };
  return map(foldResult(txs, start, stamp), ({ txs: stamped, finalized }) => ({ txs: stamped, finalized }));
};
const lowerHexDeep = (v: unknown): unknown => {
  if (typeof v === "string") return /^0x/i.test(v.trim()) ? v.trim().toLowerCase() : v;
  if (Array.isArray(v)) return v.map(lowerHexDeep);
  if (v !== null && typeof v === "object") {
    return Object.fromEntries(Object.entries(v).map(([k, x]) => [k, lowerHexDeep(x)]));
  }
  return v;
};
const sameClaimProof = (given: unknown, stamped: unknown): boolean =>
  given !== undefined && canon(lowerHexDeep(given)) === canon(stamped);
/** og verifyAccountJClaimProof: a root admits one valid path per key, so a witness verifies iff it is regenerated. */
const claimProofsMatch = (given: readonly WireAccountTx[], stamped: readonly WireAccountTx[]): boolean =>
  given.every((tx, i) => {
    const s = stamped[i];
    return tx.type !== "j_event_claim" || (s?.type === "j_event_claim"
      && sameClaimProof(tx.leftProof, s.leftProof) && sameClaimProof(tx.rightProof, s.rightProof));
  });
const claimWire = (tx: TxOf<"j_event_claim">): Result<WireTx, ClaimError> => {
  const { leftProof, rightProof } = tx;
  if (leftProof === undefined || rightProof === undefined) return err({ _tag: "claim_proof" });
  return map(claimFrame(tx), (data) => ({ type: tx.type, data: { ...data, leftProof, rightProof } }));
};
const paymentWire = (p: TxOf<"payment">, tokenId: number, payer: EntityId, payee: EntityId): WireTx => ({
  type: "direct_payment",
  data: {
    tokenId, amount: p.amount, route: p.route ?? [payee],
    ...opt("description", p.description),
    fromEntityId: payer, toEntityId: payee, deliveryMode: p.deliveryMode ?? "direct",
    ...opt("trustedGatewayEntityId", p.trustedGatewayEntityId),
  },
});
export const wireTx = (tx: WireAccountTx, id: AccountId, byLeft: boolean): Result<WireTx, Uncommitted> => {
  if (tx.type === "j_event_claim") return mapErr(claimWire(tx), uncommitted);
  if (!isL0Tx(tx)) return ok(ownWire(wireOf(tx)));
  const payer = at(id.left, id.right, byLeft), payee = at(id.left, id.right, other(byLeft));
  return map(mapErr(tokenNumber(tx.tokenId), uncommitted), (tk) => matchBy("type", tx, {
    add_delta: (): WireTx => ({ type: "add_delta", data: { tokenId: tk } }),
    set_credit_limit: (c): WireTx => ({ type: "set_credit_limit", data: { tokenId: tk, amount: c.limit } }),
    payment: (p): WireTx => paymentWire(p, tk, payer, payee),
  }));
};
const safe = (n: bigint): Result<number, Uncommitted> =>
  (n >= 0n && n <= BigInt(Number.MAX_SAFE_INTEGER) ? ok(Number(n)) : err(uncommitted({ _tag: "unsafe_number" })));
export const frameStateHash = (
  f: Omit<AccountFrame, "stateHash">, id: AccountId, byLeft: boolean,
): Result<string, Uncommitted> => {
  const wire = all({
    height: safe(f.height), timestamp: safe(f.timestamp), jHeight: safe(f.jHeight),
    accountTxs: traverse(f.txs, (tx) => wireTx(tx, id, byLeft)),
  });
  const links = { prevFrameHash: f.prevFrameHash, accountStateRoot: f.accountStateRoot };
  return chain(wire, (w) => mapErr(accountFrameHash({ ...w, ...links }), uncommitted));
};
const acceptFrame = (
  frame: AccountFrame, id: AccountId, onLeft: boolean,
): Result<void, Uncommitted | Tagged<"frame_hash_mismatch">> =>
  chain(frameStateHash(frame, id, onLeft), (recomputed) =>
    guard(recomputed === frame.stateHash, { _tag: "frame_hash_mismatch" }));
export const commit = (s: AccountBody): Result<Committed, Uncommitted> => mapErr(committed(s), uncommitted);
export const accountStateRoot = (s: AccountBody): Result<string, Uncommitted> => map(commit(s), (c) => c.root);
export const foldCtx = (at: FoldAt, byLeft: boolean, settlement?: SettlementCtx): FoldCtx =>
  ({ byLeft, nowMs: at.timestamp, jHeight: at.jHeight, accountHeight: at.height, ...opt("settlement", settlement) });
export type FrameFold = Step<AccountBody, Effect>;
export const foldAccountTxs = strictFold(applyAccountBody);
export const foldFrame = (
  s: AccountBody, f: AccountFrame, byLeft: boolean, settlement?: SettlementCtx,
): Result<FrameFold, BodyError> => foldAccountTxs(s, f.txs, foldCtx(f, byLeft, settlement));
export const proposalFold = lenientFold(applyAccountBody);
export type ProposalFold = Lenient<AccountBody, WireAccountTx, Effect, BodyError>;

export type Hanko = string;
export const GENESIS_LINK = "genesis";
export type HeadCertificate = { readonly parent: string; readonly left: Hanko; readonly right: Hanko };
/** `timestamp` is the last committed frame's clock (og `currentFrame.timestamp`, 0 at genesis). */
export type AccountHead =
  | Tagged<"genesis", { height: 0n; prevFrameHash: typeof GENESIS_LINK; timestamp: 0n }>
  | Tagged<"installed", {
    height: bigint; prevFrameHash: string; timestamp: bigint;
    certificate: HeadCertificate; crossTxs?: readonly WireAccountTx[] | undefined;
  }>;
/**
 * og currentFrame.accountTxs as og buildCrossJAckFrameCandidate reads them: the committed frame's cross-j pull locks
 * and closes (absent when none).
 */
const committedCrossTxs = (txs: readonly WireAccountTx[]): readonly WireAccountTx[] | undefined => {
  const kept = txs.filter((tx) => tx.type === "cross_pull_lock" || tx.type === "cross_pull_close");
  return kept.length === 0 ? undefined : kept;
};
export type InstalledHead = Of<AccountHead, "installed">;
export const genesisAccountHead = (): AccountHead =>
  ({ _tag: "genesis", height: 0n, prevFrameHash: GENESIS_LINK, timestamp: 0n });
export type AccountAck = {
  readonly height: bigint; readonly frameHash: string; readonly frameHanko: Hanko;
  readonly disputeHanko?: DisputeHanko | undefined;
};
export const certifiedBy = (c: HeadCertificate, party: Party): { readonly own: Hanko; readonly peer: Hanko } =>
  ({ own: at(c.left, c.right, party.left), peer: at(c.left, c.right, other(party.left)) });
/**
 * An unsafe peer frame: the refusal, the signed frame and its Hanko, and og's failureMessage text for the refusal when
 * the rewrite reproduces it.
 */
export type FrameEvidence = {
  readonly cause: AccountReplicaError; readonly frame: AccountFrame; readonly frameHanko: Hanko;
  readonly reason?: string | undefined;
};
/**
 * og account.shadow.rejectedFrameEvidence as the Entity root commits it (state-root.ts): the dispute reason, the
 * frame's stateHash and its Hanko.
 */
export type RejectedFrame = { readonly reason: string; readonly frameHash: string; readonly frameHanko: Hanko };
export type AccountInput =
  /** `selected`: og `selectedMempoolTxs`, a sub-multiset of the mempool proposed instead of the whole mempool. */
  | ({
    readonly kind: "propose"; readonly frameHanko?: Hanko | undefined;
    readonly disputeHanko?: DisputeHanko | undefined; readonly selected?: readonly WireAccountTx[] | undefined;
  } & FrameClock)
  | { readonly kind: "freeze"; readonly evidence?: FrameEvidence | undefined }
  | { readonly kind: "resume" }
  | ({ readonly kind: "ack" } & AccountAck & AccountEnvelope)
  | ({
    readonly kind: "ack_frame"; readonly ack: AccountAck | null; readonly frame: AccountFrame;
    readonly frameHanko: Hanko; readonly disputeHanko?: DisputeHanko | undefined;
  } & AccountEnvelope)
  /** og `kind: 'dispute'`: a standalone peer dispute-Hanko witness, sequenced by proof nonce, not frame height. */
  | ({ readonly kind: "dispute"; readonly disputeHanko: DisputeHanko } & AccountEnvelope)
  /**
   * og `kind: 'board_hanko_refresh'` (AccountBoardHankoRefresh): the peer re-Hankos our committed frame (and dispute
   * proof) under its newly activated board.
   */
  | ({
    readonly kind: "board_hanko_refresh";
    readonly boardActivationJHeight: number; readonly boardActivationLogIndex: number;
  } & AccountAck & AccountEnvelope)
  /** og AccountFinality: an authenticated Depository dispute event, routed by the Entity; unilateral, never ACKed. */
  | ({ readonly kind: "external_finality"; readonly finality: AccountFinality } & AccountEnvelope);
/** og types/account.ts AccountFinality['finality']. */
export type DisputeStartedFinality = {
  readonly kind: "dispute_started";
  readonly starterEntityId: string;
  readonly initialProofbodyHash: string; readonly initialNonce: number; readonly initialProposerIsLeft: boolean;
  readonly disputeTimeout: number; readonly disputeStartTimestamp: number;
  readonly leftResponseSeconds: number; readonly rightResponseSeconds: number;
  readonly jNonce: number;
  readonly starterInitialArguments: string; readonly starterCounterArguments: string;
  readonly starterCounterProofCommitment: string;
  readonly observedBlockNumber: number; readonly batchNonce?: number | undefined;
};
export type AccountFinality =
  | DisputeStartedFinality
  | {
    readonly kind: "dispute_finalized"; readonly finalizedJNonce: number; readonly finalizedTokenIds: readonly number[];
  };
/** og AccountReplica.activeDispute as applyAccountDisputeStarted writes it (field order is og's). */
export type ActiveDispute = {
  readonly startedByLeft: boolean;
  readonly initialProofbodyHash: string; readonly initialNonce: number; readonly initialProposerIsLeft: boolean;
  readonly disputeTimeout: number; readonly disputeStartTimestamp: number;
  readonly jNonce: number;
  readonly starterInitialArguments: string; readonly starterCounterArguments: string;
  readonly starterCounterProofCommitment: string;
  readonly observedOnChain: true; readonly observedBlockNumber: number;
  readonly batchNonce?: number | undefined; readonly finalizeQueued: boolean;
  /** og CounterDisputeRegistered: the counter-proof Solidity selected, which disputeFinalize must execute. */
  readonly selectedCounterNonce?: number | undefined;
  readonly selectedCounterProofbodyHash?: string | undefined;
  readonly selectedCounterProposerIsLeft?: boolean | undefined;
  /** og activeDispute.crossJurisdictionRecovery: the non-starter Target user's recovery, latched at DisputeStarted. */
  readonly crossJurisdictionRecovery?: CrossRecovery | undefined;
};
export type AccountMessage = Extract<AccountInput, { readonly kind: "ack" | "ack_frame" }>;
/** Every peer-originated input (og routes `dispute` through the same Entity accountInput lane). */
export type AccountPeerInput =
  Extract<AccountInput, { readonly kind: "ack" | "ack_frame" | "dispute" | "board_hanko_refresh" }>;
/**
 * `message`: og HandleAccountInputResult / proposal `events`, the Account's status lines the Entity certifies as frame
 * events (og addMessages).
 */
export type AccountOutput =
  | AccountMessage
  | { readonly kind: "effect"; readonly effect: Effect }
  | { readonly kind: "start_dispute"; readonly start: DisputeStart }
  | { readonly kind: "message"; readonly message: string };
const accountSay = (message: string): AccountOutput => ({ kind: "message", message });
export type AccountPhase = "open" | "proposed" | "received" | "preparing" | "disputed";
export type AccountEvent = AccountInput["kind"];
/** A frame under consideration: its hanko, its local proof and the fold it makes. */
export class Candidate {
  protected declare readonly established: true;
  /**
   * `floor`: the settlement proof-nonce floor the frame's txs folded under (og reads the pre-frame replica cursors).
   * `sent`: what og keeps as `pendingAccountInput` beside its own proposal: the bundled ACK and the dispute Hanko the
   * ack_frame carried.
   */
  constructor(
    readonly frame: AccountFrame,
    readonly frameHanko: Hanko,
    readonly frameProof: LocalProof,
    readonly draft: FrameFold,
    readonly floor: number,
    readonly sent?: SentProposal,
  ) {}
}
export type SentProposal = { readonly ack: AccountAck | null; readonly disputeHanko?: DisputeHanko | undefined };
/** og RebalancePolicy (types/finance/rebalance.ts): the Entity's private per-token automation policy on one Account. */
export type RebalancePolicy = {
  readonly r2cRequestSoftLimit: bigint; readonly hardLimit: bigint; readonly maxAcceptableFee: bigint;
};
type AccountEnv = {
  readonly state: AccountBody;
  readonly head: AccountHead;
  readonly mempool: readonly WireAccountTx[];
  readonly acknowledged?: AccountAck | undefined;
  readonly dispute: DisputeWitnesses;
  readonly boardRefresh?: BoardRefresh | undefined;
  readonly publicPinned?: true | undefined;
  /** og replica shadow.rebalance.policy, outside the Account state root, committed as the Entity leaf's policyRoot. */
  readonly rebalancePolicy?: ReadonlyMap<number, RebalancePolicy> | undefined;
  /** og boardHankoRefreshMigration: our own board-rotation refresh marker (committed in the Entity leaf). */
  readonly refreshMigration?: RefreshMigration | undefined;
  /** og shadow.rejectedFrameEvidence: the last unsafe peer frame the Entity disputed (committed; never cleared). */
  readonly rejectedFrame?: RejectedFrame | undefined;
};
type EnvMeta =
  Pick<AccountEnv, "boardRefresh" | "publicPinned" | "rebalancePolicy" | "refreshMigration" | "rejectedFrame">;
/** Entity-side Account fields every phase keeps (og counterpartyBoardHankoRefresh, publicPinned, rebalance policy). */
const envMeta = (r: EnvMeta): EnvMeta => ({
  ...opt("boardRefresh", r.boardRefresh),
  ...opt("publicPinned", r.publicPinned),
  ...opt("rebalancePolicy", r.rebalancePolicy),
  ...opt("refreshMigration", r.refreshMigration),
  ...opt("rejectedFrame", r.rejectedFrame),
});
/** og AccountBoardHankoRefreshMigration: the activation our refresh belongs to and its outcome. */
export type RefreshMigration = {
  readonly activationJHeight: number; readonly activationLogIndex: number;
  readonly reason:
    | "pending" | "issued" | "output-route-unavailable" | "account-identity-invalid"
    | "bilateral-frame-uncertified" | "certified-frame-invalid"
    | "bilateral-dispute-uncertified" | "certified-dispute-invalid";
  readonly issuedFrameHeight?: number | undefined; readonly issuedFrameHash?: string | undefined;
};
type Held = AccountEnv & { readonly candidate: Candidate };
type Frozen = Omit<AccountEnv, "mempool"> & { readonly evidence?: FrameEvidence | undefined };
export interface OpenAccount extends Tagged<"open", AccountEnv> {}
export interface ProposedAccount extends Tagged<"proposed", Held> {}
export interface ReceivedAccount extends Tagged<"received", Held & { disputeHanko: DisputeHanko | undefined }> {}
/**
 * og `dispute_preparing` keeps deferred J claims and dispute evidence queued (dispute/policy.ts); `disputed` keeps
 * nothing.
 * `prepare`: og disputePrepare, set when the Entity's prepareDispute froze the Account (committed in the Entity leaf).
 */
export interface PreparingAccount extends Tagged<"preparing", Frozen & {
  mempool: readonly WireAccountTx[]; unready: StartRefusal; prepare?: DisputePrepare | undefined;
}> {}
/** og AccountReplica.disputePrepare (dispute/index.ts markAccountDisputePreparing). */
export type DisputePrepare = {
  readonly startedAt: number; readonly readyAfter: number; readonly reason: string;
  /** og pendingOrderbookRemovalIds: cross-Entity book rows that must confirm removal before disputeStart (sorted). */
  readonly pendingOrderbookRemovalIds?: readonly string[] | undefined;
  /** og crossJurisdictionRecovery: retained across prepare → DisputeStarted so j-finality can latch it. */
  readonly crossJurisdictionRecovery?: CrossRecovery | undefined;
  readonly startIntent: {
    readonly description: string;
    readonly crossJurisdictionRouteId?: string | undefined;
    readonly starterInitialArguments?: string | undefined;
  };
};
/** og activeDispute as handleDisputeStart (dispute/start.ts queueDisputeStart) writes it before DisputeStarted. */
export type QueuedDispute = {
  readonly startedByLeft: boolean;
  readonly initialProofbodyHash: string; readonly initialNonce: number; readonly initialProposerIsLeft: boolean;
  readonly disputeTimeout: 0; readonly jNonce: number;
  readonly starterInitialArguments: string; readonly starterCounterArguments: string;
  readonly starterCounterProofCommitment: string;
  readonly observedOnChain: false; readonly finalizeQueued: false;
};
/**
 * og admits local txs into a disputed Account's mempool too (local-tx-admission.ts has no status gate); nothing ever
 * proposes them. `start`: our own dispute start, when we froze with a complete witness. `active`: og activeDispute, the
 * on-chain dispute observed through external finality.
 */
export interface DisputedAccount extends Tagged<"disputed", Frozen & {
  mempool: readonly WireAccountTx[];
  start?: DisputeStart | undefined; active?: ActiveDispute | undefined; queued?: QueuedDispute | undefined;
}> {}
export type FrozenAccount = PreparingAccount | DisputedAccount;
export type AccountReplica = OpenAccount | ProposedAccount | ReceivedAccount | FrozenAccount;
export const certifies = (
  verify: Verify, digest: string, hanko: Hanko, entity: EntityId, authority?: HankoAuthority,
): Result<void, Tagged<"invalid_hanko", { entity: EntityId }>> =>
  guard(verify(digest, hanko, entity, authority), { _tag: "invalid_hanko", entity });
/** og securityContext.counterpartyCertifiedBoard: the peer's certified board and the log position activating it. */
export type CertifiedBoard = {
  readonly boardHash: string; readonly activatedAtJHeight: number; readonly logIndex: number;
};
/** og AccountReplica.counterpartyBoardHankoRefresh: the last board activation the peer re-Hanko'd our frame under. */
export type BoardRefresh = {
  readonly activationJHeight: number; readonly activationLogIndex: number;
  readonly frameHeight: number; readonly frameHash: string;
};
/** What every Account transition may consult beyond the replica itself (never committed). */
type AccountAuthority = {
  readonly verify: Verify;
  /** og counterpartyCertifiedBoard. */
  readonly counterpartyBoard?: CertifiedBoard | undefined;
  /** og runPostFrameAutoRebalanceCheck after each commit (og owningEntityIsHub = !hubRebalanceConfig is false here). */
  readonly autoRebalance?: boolean | undefined;
  /** og requireAccountDeltaTransformerAddress over the Runtime's jReplicas for this Account's domain. */
  readonly deltaTransformer?: DeltaTransformerRef | undefined;
  readonly boardAuthority?: BoardAuthority | undefined;
};
/** `finalizedJHeight`: the owning Entity's finalized J height (og securityContext); defaults to the Account's own. */
export type DoorContext = AccountAuthority & {
  readonly self: EntityId; readonly now: bigint; readonly finalizedJHeight?: bigint | undefined;
};
export type AccountContext = AccountAuthority & { readonly party: Party };
/** og verifyHanko authority for a peer Hanko: its certified board, and whether the prior board's grace applies. */
const peerAuthority = (ctx: AccountContext, allowPreviousBoard: boolean): HankoAuthority =>
  ({ ...opt("registeredBoardHash", ctx.counterpartyBoard?.boardHash), allowPreviousBoard });
export type AckContext = AccountContext & { readonly delivery: Delivery };
export type ReceivedContext = AccountContext & { readonly from: EntityId };
export type InboundAccountContext = ReceivedContext & { readonly now: bigint; readonly finalizedJHeight: bigint };
export type CtxFor<I extends AccountInput> =
  I extends { kind: "ack_frame" } ? InboundAccountContext
  : I extends { kind: "ack" } ? AckContext
  : I extends { kind: "dispute" | "board_hanko_refresh" } ? ReceivedContext
  : AccountContext;
export type AccountInputFor<E extends AccountEvent> = Extract<AccountInput, { readonly kind: E }>;
export type AccountGrammar = {
  readonly table: typeof AccountTransition;
  readonly replica: AccountReplica;
  readonly input: AccountInput;
  readonly ctx: { readonly [E in AccountEvent]: CtxFor<AccountInputFor<E>> };
  readonly output: AccountOutput;
  readonly error: AccountReplicaError;
};
export type NextAccountPhase<S extends AccountPhase, E extends AccountEvent> = Next<AccountGrammar, S, E>;
export type AccountCases<E extends AccountEvent> = Cases<AccountGrammar, E>;
export type AccountApply<R extends AccountReplica = AccountReplica> = Apply<R, AccountOutput>;
export interface DisputeRequired extends Tagged<"dispute_required", FrameEvidence> {}
/**
 * A refusal after the bundled ACK already committed: `committed` is the Account-level post-state og keeps (its Entity
 * then evicts the whole input).
 */
export interface RejectedAfterAck extends Tagged<"rejected_after_ack", {
  cause: AccountReplicaError; committed: AccountApply;
}> {}
export type AccountReplicaError =
  | BodyError | DisputeError | EnvelopeError | DisputeRequired | RejectedAfterAck | DeadlineViolation["error"]
  | Tagged<"proposal_selection", { reason: "empty" | "too_large" | "not_in_mempool" }>
  | Tagged<"finality", { reason:
    | "initial_nonce" | "j_nonce" | "observed_block" | "timeout" | "clock_mismatch" | "finalized_nonce" | "token_id" }>
  | Tagged<"board_hanko_refresh", { reason: BoardRefreshRefusal }>
  | Tagged<"already_proposed" | "empty_mempool" | "not_proposed" | "height_mismatch" | "hash_mismatch">
  | Tagged<"frame_hash_mismatch" | "state_root_mismatch" | "ack_unmatched" | "not_preparing">
  | Tagged<"frame_structure", { field: "timestamp" | "jHeight" | "txs" | "accountStateRoot" | "future_timestamp" }>
  | Tagged<"stale_settlement_hanko", { cause: Of<BodyError, "settlement"> }>
  | Tagged<"invalid_hanko", { entity: EntityId }> | Tagged<"unknown_signer", { entity: EntityId }>
  | Tagged<"bad_account", { reason: "entity_id" | "same_entity" | TermsError["_tag"] }>
  | Tagged<"ack_conflict", { field: "frameHash" | "frameHanko" | "disputeHanko" | "height" }>
  | Tagged<"halt_runtime", { reason: "state_hash_after_verify" }>
  | Tagged<"proposal_halt", { txType: WireAccountTx["type"]; cause: BodyError; message: string }>
  | Tagged<"account_tx_thrown", { message: string }>
  | Tagged<"mempool_full", { limit: number }>
  | Tagged<"admission_policy", { reason: "policy_version" }>
  | Tagged<"frozen", { phase: FrozenAccount["_tag"] }>;
export const evidenceOf = (e: AccountReplicaError): FrameEvidence | null => {
  if (e._tag !== "dispute_required") return null;
  const { cause, frame, frameHanko, reason } = e;
  return { cause, frame, frameHanko, ...opt("reason", reason) };
};
export const replicaId = (r: AccountReplica): AccountId => r.state.account.id;
/**
 * og AccountReplica.pendingAccountInput: the exact ack_frame our own unanswered proposal went out as; only a proposed
 * Account holds one (og drops it on commit, rollback and freeze).
 */
export const pendingAccountInput = (
  r: AccountReplica, self: EntityId,
): Extract<AccountMessage, { readonly kind: "ack_frame" }> | undefined => {
  const party = partyOf(replicaId(r), self);
  if (r._tag !== "proposed" || r.candidate.sent === undefined || !party.ok) return undefined;
  const { frame, frameHanko, sent } = r.candidate;
  return {
    kind: "ack_frame", ...sentBy(r, party.value),
    ack: sent.ack, frame, frameHanko, ...opt("disputeHanko", sent.disputeHanko),
  };
};
export const sentBy = (r: AccountReplica, party: Party): AccountEnvelope => envelopeOf(r.state.terms, party);
export const ackOf = (m: Extract<AccountInput, { readonly kind: "ack" }>): AccountAck =>
  ({ height: m.height, frameHash: m.frameHash, frameHanko: m.frameHanko, ...opt("disputeHanko", m.disputeHanko) });
export const lifecycleKey = (tx: WireAccountTx): string | undefined =>
  (arm(AccountKinds, tx.type).repeatable ? undefined : canon(wireOf(tx)));
export const unqueued = (txs: readonly WireAccountTx[], queued: readonly WireAccountTx[]): readonly WireAccountTx[] =>
  firstBy(txs, lifecycleKey, queued.flatMap((tx) => lifecycleKey(tx) ?? []));
const frozenError = (phase: FrozenAccount["_tag"]): AccountReplicaError => ({ _tag: "frozen", phase });

/**
 * `deferred`: the mempool after proposing: unselected txs plus refused txs that stay queued for retry
 * (og proposal/transactions.ts `retry`); every other refused or included tx leaves it.
 */
export type Preview = {
  readonly frame: AccountFrame; readonly draft: FrameFold;
  readonly frameProof: LocalProof; readonly dispute: DisputePlan;
  readonly deferred: readonly WireAccountTx[]; readonly witnesses: DisputeWitnesses; readonly floor: number;
};
export type ProposalPlan =
  | Tagged<"frame", { preview: Preview }>
  | Tagged<"idle", { refused: AccountReplicaError; deferred: readonly WireAccountTx[] }>;
/**
 * og proposal/transactions.ts throwCriticalProposalFailure: a refused matcher/settlement/cross-j-owned tx halts with
 * og's text; others are dropped.
 */
const proposalHaltText = (tx: WireAccountTx, reason: string): string | null => {
  switch (tx.type) {
    case "settle_transition": return `SETTLEMENT_TRANSITION_PROPOSAL_FAILED:${tx.kind}:${reason}`;
    case "swap_resolve": return `SWAP_RESOLVE_PROPOSAL_FAILED: offer=${tx.offerId} error=${reason}`;
    case "cross_pull_lock":
      return `CROSS_J_PULL_LOCK_PROPOSAL_FAILED: pull=${tx.pullId} order=${String(tx.crossJurisdiction.orderId)}`
        + ` error=${reason}`;
    case "swap_offer": return tx.crossJurisdiction === undefined
      ? null
      : `CROSS_J_SWAP_OFFER_PROPOSAL_FAILED: offer=${tx.offerId} error=${reason}`;
    case "cross_pull_close": return `CROSS_J_PULL_CLOSE_PROPOSAL_FAILED: pull=${tx.pullId} error=${reason}`;
    default: return null;
  }
};
const DEFERRED_REFUSALS: readonly string[] = ["htlc_lock_capacity", "settlement_frozen"];
const deferredRefusal = (e: BodyError): boolean => DEFERRED_REFUSALS.includes(e._tag);
/**
 * A settle hanko refused only for an `account`-basis nonce against an unsigned workspace it targets exactly
 * (og isRefreshableStaleSettlementHanko / isRefreshableStaleIncomingSettlementHanko).
 */
const staleHankoNonce = (s: AccountBody, tx: WireAccountTx, e: BodyError): SettlementNonceMismatch | undefined => {
  const ws = s.settlement, n = e._tag === "settlement" ? e.nonce : undefined;
  if (tx.type !== "settle_transition" || tx.kind !== "hanko" || n?.basis !== "account") return undefined;
  if (ws === undefined || ws.nonceAtSign !== undefined) return undefined;
  const targetsWorkspace = tx.revision === ws.revision
    && typeof tx.workspaceHash === "string" && tx.workspaceHash.toLowerCase() === ws.workspaceHash.toLowerCase();
  return targetsWorkspace && n.supplied === tx.settlementNonce ? n : undefined;
};
/** og getMinimumSafeSettlementNonce (== getNextSettlementNonce) on the committed replica. */
const minimumSafeNonce = (s: AccountBody, w: DisputeWitnesses): number => Math.max(s.jNonce + 1, proofNonceFloor(w));
type Refusals = ProposalFold["refused"];
type RetryRule = (tx: WireAccountTx, e: BodyError) => boolean;
type FailureText = (index: number, e: BodyError) => AccountTxFailure;
const txAt = (txs: readonly WireAccountTx[], i: number): WireAccountTx => txs[i] ?? assertNever(i as never);
/** A refused tx stops the proposal when its handler threw, or when it is critical and not retried. */
const refusalHalt = (
  tx: WireAccountTx, error: BodyError, f: AccountTxFailure, retried: boolean,
): AccountReplicaError | undefined => {
  if (f.thrown) return { _tag: "account_tx_thrown", message: f.message };
  const halt = retried ? null : proposalHaltText(tx, f.message);
  return halt === null ? undefined : { _tag: "proposal_halt", txType: tx.type, cause: error, message: halt };
};
/**
 * `retry`: og proposalFailureDisposition's extra `retry` cases beyond the capacity/freeze refusals. `failure`: og's
 * text for the refused tx at an index; in window order the first thrown handler (og applyProposalTransaction rethrows)
 * or non-retried critical refusal halts the proposal. Otherwise the answer is the refused txs that stay queued.
 */
const proposalRefusals = (
  mempool: readonly WireAccountTx[], refused: Refusals,
  retry: RetryRule = () => false, failure: FailureText = (_, e) => refusedTx(e._tag),
): Result<readonly WireAccountTx[], AccountReplicaError> => {
  const retried = (index: number, error: BodyError): boolean =>
    deferredRefusal(error) || retry(txAt(mempool, index), error);
  const halted = refused.reduce<AccountReplicaError | undefined>((found, { index, error }) =>
    found ?? refusalHalt(txAt(mempool, index), error, failure(index, error), retried(index, error)), undefined);
  return halted === undefined
    ? ok(refused.flatMap(({ index, error }) => (retried(index, error) ? [txAt(mempool, index)] : [])))
    : err(halted);
};
/** og tx-multiset.ts removeCommittedTxsFromMempool: drop each removed tx once, by exact bytes, in mempool order. */
const withoutAccountTxs = (
  mempool: readonly WireAccountTx[], removed: readonly WireAccountTx[],
): readonly WireAccountTx[] => {
  const quota = Map.groupBy(removed, canon);
  const keyed = mempool.map((tx, i) => [canon(tx), i] as const);
  const dropped = new Set([...Map.groupBy(keyed, ([key]) => key)]
    .flatMap(([key, copies]) => copies.slice(0, quota.get(key)?.length ?? 0).map(([, i]) => i)));
  return mempool.filter((_, i) => !dropped.has(i));
};
/** og admission.ts selectProposalWindow: the whole mempool, or a non-empty, bounded sub-multiset of it. */
export const proposalWindow = (
  mempool: readonly WireAccountTx[], selected?: readonly WireAccountTx[],
): Result<readonly WireAccountTx[], AccountReplicaError> => {
  const source = selected ?? mempool;
  switch (true) {
    case source.length === 0: return err({ _tag: "proposal_selection", reason: "empty" });
    case source.length > ACCOUNT_MEMPOOL_SIZE: return err({ _tag: "proposal_selection", reason: "too_large" });
    case selected !== undefined && withoutAccountTxs(mempool, selected).length !== mempool.length - selected.length:
      return err({ _tag: "proposal_selection", reason: "not_in_mempool" });
    default: return ok(source);
  }
};
/** Who may sign what inside a proposal: without `verify`, settle hankos refuse as context-missing. */
type ProposalAuthority = {
  readonly verify?: Verify | undefined;
  readonly dt?: DeltaTransformerRef | undefined;
  readonly authority?: BoardAuthority | undefined;
};
export const planOpen = (
  r: OpenAccount, party: Party, entityClock: FrameClock, verify?: Verify, selected?: readonly WireAccountTx[],
  dt?: DeltaTransformerRef, authority?: BoardAuthority,
): Result<ProposalPlan, AccountReplicaError> => {
  if (r.mempool.length === 0) return err({ _tag: "empty_mempool" });
  return chain(proposalWindow(r.mempool, selected), (window) =>
    planWindow(r, window, party, entityClock, { verify, dt, authority }));
};
/** The lenient proposal body just before window[index] (refused txs skipped, as og's per-tx discard does). */
const lenientBefore = (s: AccountBody, window: readonly WireAccountTx[], index: number, ctx: FoldCtx): AccountBody =>
  proposalFold(s, window.slice(0, index), ctx).state;
/** og admission.ts: a lagging proposer never mints a frame behind the committed watermark. */
const proposalClock = (r: OpenAccount, entityClock: FrameClock): FrameClock => ({
  ...entityClock,
  timestamp: entityClock.timestamp > r.head.timestamp ? entityClock.timestamp : r.head.timestamp,
});
const proposalSettlement = (floor: number, a: ProposalAuthority): SettlementCtx | undefined =>
  (a.verify === undefined ? undefined : {
    verify: a.verify, proofNonceFloor: floor, ...opt("deltaTransformer", a.dt), ...opt("boardAuthority", a.authority),
  });
/** A stale settle hanko that the minimum safe nonce would refresh stays queued (og's refreshable-hanko retry). */
const refreshableHanko = (r: OpenAccount): RetryRule => {
  const required = minimumSafeNonce(r.state, r.dispute);
  return (tx, e) => {
    const n = staleHankoNonce(r.state, tx, e);
    return n !== undefined && n.required === required && n.supplied !== required;
  };
};
/** The frame a proposal's included txs make, with its proof and the dispute Hanko plan it needs. */
const framePlan = (
  r: OpenAccount, party: Party, dt: DeltaTransformerRef | undefined,
  at: FoldAt, floor: number, folded: ProposalFold, deferred: readonly WireAccountTx[],
): Result<ProposalPlan, AccountReplicaError> => {
  const built = all({ committed: commit(folded.state), stamped: stampClaims(folded.included, r.state, party.left) });
  return chain(built, ({ committed: { view, root }, stamped }) => {
    const unhashed = {
      height: at.height, timestamp: at.timestamp, jHeight: at.jHeight,
      prevFrameHash: r.head.prevFrameHash, txs: stamped.txs, accountStateRoot: root,
    };
    const sealed = all({
      stateHash: frameStateHash(unhashed, replicaId(r), party.left),
      frameProof: localProof(view, dt),
      witnesses: promoteSettled(r.dispute, r.state, folded.state, party.left, stamped.finalized),
    });
    return chain(sealed, ({ stateHash, frameProof, witnesses }) =>
      map(proposalPlan(view, frameProof, witnesses, party.left), (dispute): ProposalPlan => ({
        _tag: "frame",
        preview: {
          frame: { ...unhashed, stateHash }, draft: { state: folded.state, effects: folded.effects },
          frameProof, dispute, deferred, witnesses, floor,
        },
      })));
  });
};
const planWindow = (
  r: OpenAccount, window: readonly WireAccountTx[], party: Party, entityClock: FrameClock, a: ProposalAuthority,
): Result<ProposalPlan, AccountReplicaError> => {
  const at: FoldAt = { height: r.head.height + 1n, ...proposalClock(r, entityClock) };
  const floor = proofNonceFloor(r.dispute);
  const ctx = foldCtx(at, party.left, proposalSettlement(floor, a));
  const folded = proposalFold(r.state, window, ctx);
  const failure: FailureText = (index, e) =>
    accountTxFailure(lenientBefore(r.state, window, index, ctx), txAt(window, index), ctx, e, party.self, r.dispute);
  const deferred = map(proposalRefusals(window, folded.refused, refreshableHanko(r), failure), (retried) =>
    withoutAccountTxs(r.mempool, withoutAccountTxs(window, retried)));
  return chain(deferred, (kept) => {
    const [firstRefusal] = folded.refused;
    return folded.included.length === 0 && firstRefusal !== undefined
      ? ok({ _tag: "idle", refused: firstRefusal.error, deferred: kept })
      : framePlan(r, party, a.dt, at, floor, folded, kept);
  });
};
type PlanResult = Result<ProposalPlan, AccountReplicaError>;
export const planAccountProposal = (
  r: AccountReplica, self: EntityId, clock: FrameClock, verify?: Verify, selected?: readonly WireAccountTx[],
  dt?: DeltaTransformerRef, authority?: BoardAuthority,
): PlanResult => chain(partyOf(replicaId(r), self), (party) => match(r, {
  open: (o): PlanResult => planOpen(o, party, clock, verify, selected, dt, authority),
  proposed: (): PlanResult => err({ _tag: "already_proposed" }),
  received: (): PlanResult => err({ _tag: "already_proposed" }),
  preparing: (): PlanResult => err(frozenError("preparing")),
  disputed: (): PlanResult => err(frozenError("disputed")),
}));
const previewOf = (planned: PlanResult): Result<Preview, AccountReplicaError> => chain(planned, (p) => match(p, {
  frame: ({ preview }): Result<Preview, AccountReplicaError> => ok(preview),
  idle: ({ refused }): Result<Preview, AccountReplicaError> => err(refused),
}));
export const previewOpen = (
  r: OpenAccount, self: EntityId, clock: FrameClock, verify?: Verify,
): Result<Preview, AccountReplicaError> => previewOf(planAccountProposal(r, self, clock, verify));
export const previewAccountProposal = (
  r: AccountReplica, self: EntityId, clock: FrameClock, verify?: Verify,
): Result<Preview, AccountReplicaError> => previewOf(planAccountProposal(r, self, clock, verify));
export const previewAccountFrame = (
  r: AccountReplica, self: EntityId, clock: FrameClock, verify?: Verify,
): Result<AccountFrame, AccountReplicaError> => map(previewAccountProposal(r, self, clock, verify), (p) => p.frame);
export type AckPreview = { readonly height: bigint; readonly frameHash: string; readonly dispute: DisputePlan };
type AckPreviewResult = Result<AckPreview, AccountReplicaError>;
export const previewAck = (r: AccountReplica, self: EntityId): AckPreviewResult =>
  chain(partyOf(replicaId(r), self), (party) => match(r, {
    open: (): AckPreviewResult => err({ _tag: "not_proposed" }),
    proposed: (): AckPreviewResult => err({ _tag: "already_proposed" }),
    received: (h): AckPreviewResult => map(ackPlan(h, proposerIsLeft(h, party)), (dispute) =>
      ({ height: h.candidate.frame.height, frameHash: h.candidate.frame.stateHash, dispute })),
    preparing: (): AckPreviewResult => err(frozenError("preparing")),
    disputed: (): AckPreviewResult => err(frozenError("disputed")),
  }));

type Propose = AccountInputFor<"propose">;
type Ack = AccountInputFor<"ack">;
type AckFrame = AccountInputFor<"ack_frame">;
type Freeze = AccountInputFor<"freeze">;
type PeerDispute = AccountInputFor<"dispute">;
type Verb<R extends AccountReplica> = Result<AccountApply<R>, AccountReplicaError>;
/** What an Account reopens with; an absent ack or witness set keeps the replica's own. */
type Reopening = {
  readonly state: AccountBody; readonly head: AccountHead; readonly mempool: readonly WireAccountTx[];
  readonly acknowledged?: AccountAck | undefined; readonly dispute?: DisputeWitnesses | undefined;
};
const reopen = (r: AccountReplica, next: Reopening): OpenAccount => ({
  _tag: "open",
  state: next.state,
  head: next.head,
  mempool: next.mempool,
  acknowledged: next.acknowledged ?? r.acknowledged,
  dispute: next.dispute ?? r.dispute,
  ...envMeta(r),
});
const effectsOut = (effects: readonly Effect[]): readonly AccountOutput[] =>
  effects.map((effect) => ({ kind: "effect", effect }));
type Replayed = {
  readonly draft: FrameFold; readonly view: CommittedAccountState; readonly finalized: readonly number[];
};
/** A peer frame replayed on our committed body: its claim witnesses must be the regenerated ones and its root ours. */
const replay = (
  s: AccountBody, f: AccountFrame, byLeft: boolean, settlement: SettlementCtx,
): Result<Replayed, AccountReplicaError> => {
  const replayed = all({ draft: foldFrame(s, f, byLeft, settlement), stamped: stampClaims(f.txs, s, byLeft) });
  return chain(replayed, ({ draft, stamped }): Result<Replayed, AccountReplicaError> => {
    if (!claimProofsMatch(f.txs, stamped.txs)) return err({ _tag: "claim_proof" });
    return chain(commit(draft.state), ({ view, root }) => (root === f.accountStateRoot
      ? ok({ draft, view, finalized: stamped.finalized })
      : err({ _tag: "state_root_mismatch" })));
  });
};
/**
 * og inspectAccountJClaimProof's throw for a received witness `given` against the canonical one `want` over the same
 * root (both already well-shaped by the frame profile): the first node that differs breaks the hash link at its index;
 * a proper prefix misses the terminal leaf; nodes past the leaf (or any node under an empty root) trail.
 */
const claimProofText = (given: JClaimProof | undefined, want: JClaimProof | undefined): string | null => {
  if (given === undefined) return "ACCOUNT_J_CLAIM_PROOF_REQUIRED";
  if (want === undefined || sameClaimProof(given, want)) return null;
  const differs = given.nodes.findIndex((n, i) =>
    i >= want.nodes.length || canon(lowerHexDeep(n)) !== canon(want.nodes[i]));
  switch (true) {
    case want.nodes.length === 0: return "ACCOUNT_J_CLAIM_PROOF_TRAILING_NODES";
    case given.nodes.length < 1: return "ACCOUNT_J_CLAIM_PROOF_LENGTH_INVALID";
    case differs < 0: return "ACCOUNT_J_CLAIM_PROOF_TERMINAL_LEAF_MISSING";
    case differs >= want.nodes.length: return "ACCOUNT_J_CLAIM_PROOF_TRAILING_NODES";
    default: return `ACCOUNT_J_CLAIM_PROOF_LINK_INVALID:${differs}`;
  }
};
/**
 * og replayIncomingFrameOnClone's first failing tx, walked in frame order with og's text: a claim whose witnesses
 * differ from the accumulator's throws (og validateAccountJEventClaimAdmission verifies left, then right), else the
 * first refused or thrown handler.
 */
const frameFailure = (
  s: AccountBody, f: AccountFrame, byLeft: boolean, settlement: SettlementCtx, self: EntityId, w: DisputeWitnesses,
): AccountTxFailure | null => {
  const ctx = foldCtx(f, byLeft, settlement), stamped = stampClaims(f.txs, s, byLeft);
  const witnessText = (tx: WireAccountTx, i: number): string | null => {
    const want = stamped.ok ? stamped.value.txs[i] : undefined;
    return tx.type === "j_event_claim" && want?.type === "j_event_claim"
      ? claimProofText(tx.leftProof, want.leftProof) ?? claimProofText(tx.rightProof, want.rightProof)
      : null;
  };
  const replayTx = (body: AccountBody, tx: WireAccountTx, i: number): Result<AccountBody, AccountTxFailure> => {
    const witnessRefusal = witnessText(tx, i);
    if (witnessRefusal !== null) return err(threwTx(witnessRefusal));
    const applied = applyAccountBody(body, tx, ctx);
    return applied.ok ? ok(applied.value.state) : err(accountTxFailure(body, tx, ctx, applied.error, self, w));
  };
  const walked = foldResult(f.txs, s, replayTx);
  return walked.ok ? null : walked.error;
};
type FrameField = Of<AccountReplicaError, "frame_structure">["field"];
const malformedField = (f: AccountFrame): FrameField | null => {
  switch (true) {
    case f.timestamp < 0n: return "timestamp";
    case f.jHeight < 0n: return "jHeight";
    case f.txs.length > ACCOUNT_MEMPOOL_SIZE: return "txs";
    case !BYTES32.test(f.accountStateRoot): return "accountStateRoot";
    default: return null;
  }
};
const frameStructure = (f: AccountFrame): Result<void, AccountReplicaError> => {
  const field = malformedField(f);
  return field === null ? ok(undefined) : err({ _tag: "frame_structure", field });
};
export const receiverClock = (f: AccountFrame, now: bigint): Result<void, AccountReplicaError> =>
  guard(f.timestamp - now <= ACCOUNT_NETWORK_ALLOWANCE_MS, { _tag: "frame_structure", field: "future_timestamp" });
/** First refusal among checks run in order; a later check never runs after an earlier refusal. */
const lazyChecks = <E>(...gs: readonly (() => Result<unknown, E>)[]): Result<void, E> =>
  foldResult(gs, undefined as void, (_, g) => map(g(), () => undefined));
export const HTLC_ENFORCEMENT_RESERVE_MS = ACCOUNT_NETWORK_ALLOWANCE_MS;
export type DeadlineReason =
  | "lock_window" | "secret_window" | "secret_frame_expired" | "payer_cancel_early" | "timeout_not_expired";
export type DeadlineViolation = {
  readonly error: Tagged<"frame_deadline", { reason: DeadlineReason; lockId: string }>; readonly dispute: boolean;
};
type DeadlineLock = Pick<HtlcLock, "hashlock" | "timelock" | "revealBeforeHeight" | "senderIsLeft">;
type Clock = { readonly timestamp: bigint; readonly jHeight: bigint };
// og htlc-deadline.ts: the time bound is exclusive, the J-height bound inclusive.
const deadlinePassed = (l: DeadlineLock, c: Clock): boolean =>
  c.jHeight > l.revealBeforeHeight || c.timestamp >= l.timelock;
// og validHtlcSecret: the htlc_resolve arm's 32-byte preimage check.
const opensLock = (l: DeadlineLock, secret: string): boolean => hashHtlcSecret(secret) === l.hashlock;
type LockBook = ReadonlyMap<string, DeadlineLock>;
type DeadlineScan = Result<LockBook, DeadlineViolation>;
/** The peer frame under scan, who proposed it, and our local clock (now, finalized J height). */
type ScanSite = { readonly f: AccountFrame; readonly proposerIsLeft: boolean; readonly local: Clock };
type SecretResolve = Extract<TxOf<"htlc_resolve">, { outcome: "secret" }>;
type ErrorResolve = Extract<TxOf<"htlc_resolve">, { outcome: "error" }>;
const deadlineViolation = (reason: DeadlineReason, lockId: string, dispute = false): DeadlineScan =>
  err({ error: { _tag: "frame_deadline", reason, lockId }, dispute });
const lockWindowUnsafe = (tx: TxOf<"htlc_lock">, at: ScanSite): boolean =>
  tx.timelock <= at.local.timestamp + HTLC_ENFORCEMENT_RESERVE_MS || tx.revealBeforeHeight <= at.local.jHeight
  || at.f.timestamp >= tx.timelock || tx.revealBeforeHeight <= at.f.jHeight;
const scanLock = (locks: LockBook, tx: TxOf<"htlc_lock">, at: ScanSite): DeadlineScan => {
  const { hashlock, timelock, revealBeforeHeight } = tx;
  switch (true) {
    case locks.has(tx.lockId): return ok(locks);
    case lockWindowUnsafe(tx, at): return deadlineViolation("lock_window", tx.lockId);
    default: return ok(mapSet(locks, tx.lockId, {
      hashlock, timelock, revealBeforeHeight, senderIsLeft: at.proposerIsLeft,
    }));
  }
};
const scanSecret = (locks: LockBook, lock: DeadlineLock, tx: SecretResolve, at: ScanSite): DeadlineScan => {
  const reserved: Clock = { timestamp: at.local.timestamp + HTLC_ENFORCEMENT_RESERVE_MS, jHeight: at.local.jHeight };
  switch (true) {
    case !opensLock(lock, tx.secret): return ok(locks);
    case deadlinePassed(lock, reserved): return deadlineViolation("secret_window", tx.lockId, true);
    case deadlinePassed(lock, at.f): return deadlineViolation("secret_frame_expired", tx.lockId);
    default: return ok(mapDelete(locks, tx.lockId));
  }
};
// og `outcome: 'error'`: a payer cancel waits for local expiry; a payer cancel or `timeout` needs an expired frame.
const scanCancel = (locks: LockBook, lock: DeadlineLock, tx: ErrorResolve, at: ScanSite): DeadlineScan => {
  const payer = at.proposerIsLeft === lock.senderIsLeft, timeout = tx.reason === "timeout";
  const locallyExpired = deadlinePassed(lock, at.local);
  switch (true) {
    case payer && !locallyExpired: return deadlineViolation("payer_cancel_early", tx.lockId);
    case (payer || timeout) && !deadlinePassed(lock, at.f): return deadlineViolation("timeout_not_expired", tx.lockId);
    case locallyExpired || (!payer && !timeout): return ok(mapDelete(locks, tx.lockId));
    default: return ok(locks);
  }
};
const scanDeadlines = (at: ScanSite) => (locks: LockBook, tx: WireAccountTx): DeadlineScan => {
  if (tx.type === "htlc_lock") return scanLock(locks, tx, at);
  if (tx.type !== "htlc_resolve") return ok(locks);
  const lock = locks.get(tx.lockId);
  if (lock === undefined) return ok(locks);
  return tx.outcome === "secret" ? scanSecret(locks, lock, tx, at) : scanCancel(locks, lock, tx, at);
};
/** og deadline-policy.ts getIncomingAccountDeadlineViolation: a speculative HTLC scan against our local clock. */
export const incomingDeadline = (
  s: AccountBody, f: AccountFrame, proposerIsLeft: boolean,
  ctx: { readonly now: bigint; readonly finalizedJHeight: bigint },
): Result<void, DeadlineViolation> => {
  const at: ScanSite = { f, proposerIsLeft, local: { timestamp: ctx.now, jHeight: ctx.finalizedJHeight } };
  return map(foldResult(f.txs, s.locks as LockBook, scanDeadlines(at)), () => undefined);
};
type SignedPair = { readonly left: Hanko; readonly right: Hanko };
const signedBy = (party: Party, ours: Hanko, theirs: Hanko): SignedPair =>
  ({ left: at(ours, theirs, party.left), right: at(ours, theirs, other(party.left)) });
const installedHead = (frame: AccountFrame, signed: SignedPair): AccountHead => ({
  _tag: "installed",
  height: frame.height,
  prevFrameHash: frame.stateHash,
  timestamp: frame.timestamp,
  certificate: { parent: frame.prevFrameHash, ...signed },
  ...opt("crossTxs", committedCrossTxs(frame.txs)),
});
type AfterInstall = { readonly dispute: DisputeWitnesses; readonly acknowledged?: AccountAck | undefined };
/** The candidate becomes the committed head, certified by both Hankos; its effects are released. */
const install = (
  r: ProposedAccount | ReceivedAccount, signed: SignedPair, after: AfterInstall,
): Step<OpenAccount, Effect> => {
  const { frame, draft } = r.candidate;
  const head = installedHead(frame, signed);
  const { acknowledged, dispute } = after;
  return step(reopen(r, { state: draft.state, head, mempool: r.mempool, acknowledged, dispute }), draft.effects);
};
const residentAck = (r: OpenAccount): AccountAck | null =>
  (r.acknowledged !== undefined && r.acknowledged.height === r.head.height ? r.acknowledged : null);
type ProposeResult = Verb<OpenAccount | ProposedAccount>;
/** Our own frame goes out only with our frame Hanko on it and the dispute Hanko its plan asks for. */
const proposeFrame = (r: OpenAccount, input: Propose, ctx: AccountContext, preview: Preview): ProposeResult => {
  const { frame, draft, frameProof, dispute, deferred, witnesses: promoted, floor } = preview;
  const frameHanko = input.frameHanko;
  if (frameHanko === undefined) return err({ _tag: "invalid_hanko", entity: ctx.party.self });
  const sealed = checks(frameStructure(frame), certifies(ctx.verify, frame.stateHash, frameHanko, ctx.party.self));
  const settled = chain(sealed, () => settleLocal(dispute, input.disputeHanko, promoted, ctx.party.self, ctx.verify));
  return map(settled, ({ carried, witnesses }) => {
    const ack = residentAck(r), sent: SentProposal = { ack, ...opt("disputeHanko", carried) };
    const candidate = new Candidate(frame, frameHanko, frameProof, draft, floor, sent);
    const proposed: ProposedAccount = { ...r, _tag: "proposed", mempool: deferred, candidate, dispute: witnesses };
    // og proposal/finalize.ts: the proposal's only routed event
    return done<OpenAccount | ProposedAccount, AccountOutput>(proposed, [
      { kind: "ack_frame", ...sentBy(r, ctx.party), ack, frame, frameHanko, ...opt("disputeHanko", carried) },
      accountSay(`🚀 Proposed frame ${frame.height} with ${frame.txs.length} transactions`),
    ]);
  });
};
export const proposeOpen = (r: OpenAccount, input: Propose, ctx: AccountContext): ProposeResult => {
  const clock: FrameClock = { timestamp: input.timestamp, jHeight: input.jHeight };
  const planned = planOpen(r, ctx.party, clock, ctx.verify, input.selected, ctx.deltaTransformer, ctx.boardAuthority);
  return chain(planned, (plan) => match(plan, {
    idle: ({ refused, deferred }): ProposeResult =>
      (input.frameHanko === undefined && input.disputeHanko === undefined
        ? ok(done({ ...r, mempool: deferred }))
        : err(refused)),
    frame: ({ preview }): ProposeResult => proposeFrame(r, input, ctx, preview),
  }));
};
const receivedDispute = (
  r: AccountReplica, d: DisputeHanko | undefined, from: EntityId, verify: Verify,
): Result<DisputeHanko | undefined, AccountReplicaError> =>
  (d === undefined ? ok(undefined) : validateCounterparty(r.state, d, from, verify));
const sameWitness = (a: DisputeHanko, b: DisputeHanko | undefined): boolean =>
  b !== undefined && sameHex(a.hanko, b.hanko) && sameHex(a.hash, b.hash) && sameHex(a.proofBodyHash, b.proofBodyHash)
  && a.proofNonce === b.proofNonce && a.proposerIsLeft === b.proposerIsLeft;
const conflict = (field: "frameHash" | "frameHanko" | "disputeHanko" | "height"): AccountReplicaError =>
  ({ _tag: "ack_conflict", field });
type Checked = Result<void, AccountReplicaError>;
/** A peer re-ACKing our head's parent: it must name that parent and certify it. */
const predecessorAck = (
  r: AccountReplica, a: AccountAck, from: EntityId, verify: Verify, authority: HankoAuthority,
): Checked =>
  match(r.head, {
    genesis: (): Checked => err(conflict("frameHash")),
    installed: ({ certificate: { parent } }): Checked =>
      (sameHex(a.frameHash, parent)
        ? certifies(verify, parent, a.frameHanko, from, authority)
        : err(conflict("frameHash"))),
  });
/** A peer re-ACKing our head: the very Hanko and witness it already gave. */
const repeatedAck = (
  r: AccountReplica, a: AccountAck, validated: DisputeHanko | undefined, ctx: ReceivedContext,
): Checked =>
  match(r.head, {
    genesis: (): Checked => err(conflict("frameHash")),
    installed: ({ prevFrameHash: head, certificate }): Checked => {
      switch (true) {
        case !sameHex(a.frameHash, head): return err(conflict("frameHash"));
        case a.frameHanko.length === 0 || !sameHex(a.frameHanko, certifiedBy(certificate, ctx.party).peer):
          return err(conflict("frameHanko"));
        case validated !== undefined && !sameWitness(validated, r.dispute.counterparty):
          return err(conflict("disputeHanko"));
        default: return certifies(ctx.verify, head, a.frameHanko, ctx.from, peerAuthority(ctx, true));
      }
    },
  });
/** `notes`: og's status lines for an ACK that changes nothing (ack-commit.ts handleObsoleteAck). */
type HeadAck =
  | Tagged<"identity", { notes?: readonly string[] }>
  | Tagged<"advance", { target: bigint; validated: DisputeHanko | undefined }>;
const IDENTITY: HeadAck = { _tag: "identity" };
/** Where an incoming ACK lands against our head: an echo of what is committed, a stale no-op, or a step forward. */
const headAck = (
  r: AccountReplica, a: AccountAck, ctx: ReceivedContext, riding: bigint | undefined, target: bigint,
): Result<HeadAck, AccountReplicaError> =>
  chain(receivedDispute(r, a.disputeHanko, ctx.from, ctx.verify), (validated): Result<HeadAck, AccountReplicaError> => {
    const height = r.head.height;
    switch (true) {
      case riding === undefined && height > 1n && a.height === height - 1n:
        return map(predecessorAck(r, a, ctx.from, ctx.verify, peerAuthority(ctx, true)), () => IDENTITY);
      case target < height - 1n:
        return ok({ _tag: "identity", notes: [`ℹ️ Ignored stale ACK ${a.height} (current=${height})`] });
      case target === height && height > 0n: return map(repeatedAck(r, a, validated, ctx), () => IDENTITY);
      default: return ok({ _tag: "advance", target, validated });
    }
  });
/**
 * og runPostFrameAutoRebalanceCheck: after a commit a non-hub Entity queues its due request_collateral txs
 * (og appendAccountMempoolTxs) and says so.
 */
const postCommitRebalance = (
  r: OpenAccount, ctx: AccountContext, after: "frame commit" | "ACK commit",
): Apply<OpenAccount, AccountOutput> => {
  const txs = ctx.autoRebalance === true ? autoRebalance(r, ctx.party.self) : [];
  return txs.length === 0
    ? done(r)
    : done({ ...r, mempool: [...r.mempool, ...txs] }, [
      accountSay(`🔄 Auto-rebalance queued ${txs.length} tx(s) after ${after}`),
    ]);
};
/** The peer's ACK of our proposal commits it once the witness it owes, the frame hash and its Hanko all hold. */
const commitOwn = (
  r: ProposedAccount, a: AccountAck, validated: DisputeHanko | undefined, ctx: ReceivedContext,
): Verb<OpenAccount> => {
  const { frame, frameHanko, frameProof } = r.candidate;
  const accepted = lazyChecks<AccountReplicaError>(
    () => requireDispute(frameProof, r.dispute, validated),
    () => guard(sameHex(a.frameHash, frame.stateHash), { _tag: "hash_mismatch" }),
    () => certifies(ctx.verify, frame.stateHash, a.frameHanko, ctx.from, peerAuthority(ctx, true)),
  );
  return map(accepted, () => {
    const signed = signedBy(ctx.party, frameHanko, a.frameHanko);
    const installed = install(r, signed, { dispute: storeCounterparty(r.dispute, validated) });
    // og ack-commit.ts: `✅ Frame N confirmed and committed`, then queuePostAckWork
    const post = postCommitRebalance(installed.state, ctx, "ACK commit");
    return done(post.replica, [
      accountSay(`✅ Frame ${frame.height} confirmed and committed`),
      ...post.outputs,
      ...effectsOut(installed.effects),
    ]);
  });
};
type Gate<R extends AccountReplica, X extends object = {}> =
  | Tagged<"answered", { result: Verb<R> }>
  | Tagged<"continue", X>;
const answered = <R extends AccountReplica>(result: Verb<R>): Gate<R, never> => ({ _tag: "answered", result });
const proceedIf = <R extends AccountReplica>(check: Checked): Gate<R> =>
  (check.ok ? { _tag: "continue" } : answered(check));
/** Our own ACK of the held frame: below the head it is a no-op; otherwise it must be this frame, Hanko'd by us. */
const ownAck = <R extends ProposedAccount | ReceivedAccount>(r: R, input: Ack, ctx: AccountContext): Gate<R> => {
  const shaped = disputeShapes([input.disputeHanko]);
  if (!shaped.ok) return answered(shaped);
  const { frame } = r.candidate;
  switch (true) {
    case input.height <= r.head.height: return answered(ok(done(r)));
    case input.height !== frame.height: return answered(err({ _tag: "height_mismatch" }));
    case !sameHex(input.frameHash, frame.stateHash): return answered(err({ _tag: "hash_mismatch" }));
    default: return proceedIf(certifies(ctx.verify, frame.stateHash, input.frameHanko, ctx.party.self));
  }
};
/** og replays the received frame's handler messages under the settlement context it folded with. */
const receivedFrameSettlement = (r: ReceivedAccount, ctx: AccountContext): SettlementCtx => ({
  verify: ctx.verify,
  proofNonceFloor: r.candidate.floor,
  ...opt("registeredBoardHash", ctx.counterpartyBoard?.boardHash),
  ...opt("boardAuthority", ctx.boardAuthority),
});
type ReceivedAckResult = Verb<OpenAccount | ReceivedAccount>;
/** Our ACK of the peer's frame commits it, carrying the dispute Hanko our plan signs, and answers the peer. */
const acceptReceived = (r: ReceivedAccount, input: Ack, ctx: AccountContext): ReceivedAckResult => {
  const { frame, frameHanko } = r.candidate;
  const byLeft = proposerIsLeft(r, ctx.party);
  const settled = chain(ackPlan(r, byLeft), (plan) =>
    settleLocal(plan, input.disputeHanko, r.dispute, ctx.party.self, ctx.verify));
  return map(settled, ({ carried, witnesses }) => {
    const ackOut: AccountAck = {
      height: frame.height, frameHash: frame.stateHash, frameHanko: input.frameHanko, ...opt("disputeHanko", carried),
    };
    const signed = signedBy(ctx.party, input.frameHanko, frameHanko);
    const dispute = storeCounterparty(witnesses, r.disputeHanko);
    const installed = install(r, signed, { dispute, acknowledged: ackOut });
    // og consensus/index.ts commit: the frame's handler messages (replayed from the proposer's side),
    // `🤝 Accepted frame`, then the post-commit rebalance
    const said = frameTxMessages(r.state, frame, byLeft, ctx.party.self, receivedFrameSettlement(r, ctx));
    const post = postCommitRebalance(installed.state, ctx, "frame commit");
    return done<OpenAccount | ReceivedAccount, AccountOutput>(post.replica, [
      { kind: "ack", ...sentBy(r, ctx.party), ...ackOut },
      ...said.map(accountSay),
      accountSay(`🤝 Accepted frame ${frame.height} from Entity ${ctx.party.peer.slice(-4)}`),
      ...post.outputs,
      ...effectsOut(installed.effects),
    ]);
  });
};
const createAck = (r: ReceivedAccount, input: Ack, ctx: AccountContext): ReceivedAckResult =>
  match(ownAck(r, input, ctx), {
    answered: ({ result }): ReceivedAckResult => result,
    continue: (): ReceivedAckResult => acceptReceived(r, input, ctx),
  });
const receiveAck = (r: AccountReplica, input: Ack, ctx: ReceivedContext): Result<HeadAck, AccountReplicaError> => {
  if (ctx.from !== ctx.party.peer) return err({ _tag: "unknown_signer", entity: ctx.from });
  return chain(disputeShapes([input.disputeHanko]), () => headAck(r, input, ctx, undefined, input.height));
};
const noted = <R extends AccountReplica>(r: R, notes: readonly string[] | undefined): Apply<R, AccountOutput> =>
  done<R, AccountOutput>(r, (notes ?? []).map(accountSay));
/** A replica holding no frame of ours can only take an ACK that changes nothing. */
const unmatched = <R extends AccountReplica>(r: R, head: Result<HeadAck, AccountReplicaError>): Verb<R> =>
  chain(head, (h) => match(h, {
    identity: ({ notes }): Verb<R> => ok(noted(r, notes)),
    advance: (): Verb<R> => err({ _tag: "ack_unmatched" }),
  }));
const withFrameHanko = (r: ProposedAccount, frameHanko: Hanko): ProposedAccount => {
  const { frame, frameProof, draft, floor, sent } = r.candidate;
  return { ...r, candidate: new Candidate(frame, frameHanko, frameProof, draft, floor, sent) };
};
type ProposedAckResult = Verb<OpenAccount | ProposedAccount>;
export const ackProposed = (r: ProposedAccount, input: Ack, ctx: AckContext): ProposedAckResult => match(ctx.delivery, {
  local: () => match(ownAck(r, input, ctx), {
    answered: ({ result }): ProposedAckResult => result,
    continue: (): ProposedAckResult => (input.disputeHanko === undefined
      ? ok(done(withFrameHanko(r, input.frameHanko)))
      : err(refuseDispute("unexpected"))),
  }),
  received: ({ from }) => {
    const received = { ...ctx, from };
    return chain(receiveAck(r, input, received), (h) => match(h, {
      identity: ({ notes }): ProposedAckResult => ok(noted(r, notes)),
      advance: ({ target, validated }): ProposedAckResult => (target === r.candidate.frame.height
        ? commitOwn(r, input, validated, received)
        : err({ _tag: "ack_unmatched" })),
    }));
  },
});
export const ackReceived = (r: ReceivedAccount, input: Ack, ctx: AckContext): ReceivedAckResult => match(ctx.delivery, {
  local: () => createAck(r, input, ctx),
  received: ({ from }) => unmatched(r, receiveAck(r, input, { ...ctx, from })),
});
export const ackOpen = (r: OpenAccount, input: Ack, ctx: AckContext): Verb<OpenAccount> => match(ctx.delivery, {
  local: (): Verb<OpenAccount> => chain(disputeShapes([input.disputeHanko]), () =>
    (input.height <= r.head.height ? ok(done(r)) : err({ _tag: "not_proposed" }))),
  received: ({ from }) => unmatched(r, receiveAck(r, input, { ...ctx, from })),
});
const restore = (r: ProposedAccount): OpenAccount =>
  reopen(r, { state: r.state, head: r.head, mempool: [...unqueued(r.candidate.frame.txs, r.mempool), ...r.mempool] });
/** An ACK riding a duplicate of our head must be the peer's ACK of the head's parent. */
const priorAck = (r: AccountReplica, ack: AccountAck | null, height: bigint, ctx: InboundAccountContext): Checked => {
  if (ack === null) return ok(undefined);
  if (height <= 1n || ack.height !== height - 1n) return err(conflict("height"));
  return chain(receivedDispute(r, ack.disputeHanko, ctx.from, ctx.verify), () =>
    predecessorAck(r, ack, ctx.from, ctx.verify, peerAuthority(ctx, true)));
};
/** og replay.ts reusableCertifiedAckHanko: only a witness above the finalized jNonce is reusable. */
const reusableWitness = (w: DisputeWitnesses, jNonce: number): DisputeHanko | undefined =>
  (w.current !== undefined && w.current.proofNonce > jNonce ? w.current : undefined);
const ackAgain = <R extends AccountReplica>(
  r: R, ack: AccountAck, ctx: AccountContext, note: string,
): Apply<R, AccountOutput> =>
  done<R, AccountOutput>(r, [{ kind: "ack", ...sentBy(r, ctx.party), ...ack }, accountSay(note)]);
/** og incoming/replay.ts: a retained ACK is re-sent, else rebuilt from the certified head. */
const ackHeadAgain = <R extends AccountReplica>(r: R, head: InstalledHead, ctx: AccountContext): Verb<R> => {
  const sent = r.acknowledged, height = head.height;
  if (sent !== undefined && sent.height === height) {
    return ok(ackAgain(r, sent, ctx, `↩️ Re-sent ACK for duplicate committed frame ${height}`));
  }
  return map(asProof(committedView(r.state)), ({ jNonce }) => {
    const rebuilt: AccountAck = {
      height, frameHash: head.prevFrameHash, frameHanko: certifiedBy(head.certificate, ctx.party).own,
      ...opt("disputeHanko", reusableWitness(r.dispute, jNonce)),
    };
    const note = `↩️ Rebuilt ACK for duplicate committed frame ${height}`;
    return ackAgain({ ...r, acknowledged: rebuilt }, rebuilt, ctx, note);
  });
};
/** A peer resending our committed head (its ACK was lost): verify it is that very frame, then ACK it again. */
const duplicateOfHead = <R extends AccountReplica>(
  r: R, head: InstalledHead, input: AckFrame, ctx: InboundAccountContext,
): Verb<R> => {
  const same = lazyChecks<AccountReplicaError>(
    () => acceptFrame(input.frame, replicaId(r), other(ctx.party.left)),
    () => guard(sameHex(input.frameHanko, certifiedBy(head.certificate, ctx.party).peer), conflict("frameHanko")),
    () => priorAck(r, input.ack, head.height, ctx),
  );
  return chain(same, () => ackHeadAgain(r, head, ctx));
};
/** og incoming/preflight.ts handleStaleIncomingFrame: a frame below the head is a no-op the Entity records. */
const staleFrame = <R extends AccountReplica>(r: R, frame: AccountFrame): Apply<R, AccountOutput> =>
  done<R, AccountOutput>(r, [accountSay(`ℹ️ Ignored stale frame ${frame.height} (current=${r.head.height})`)]);
const replayGate = <R extends AccountReplica>(r: R, input: AckFrame, ctx: InboundAccountContext): Gate<R> => {
  const { frame } = input;
  const stale = (): Gate<R> => (input.ack === null && frame.height < r.head.height
    ? answered(ok(staleFrame(r, frame)))
    : { _tag: "continue" });
  return match(r.head, {
    genesis: stale,
    installed: (head): Gate<R> => (frame.height === head.height && sameHex(frame.stateHash, head.prevFrameHash)
      ? answered(duplicateOfHead(r, head, input, ctx))
      : stale()),
  });
};
/** A peer frame that breaks an HTLC deadline is refused; one whose secret came too late is dispute evidence. */
const deadlineRefusal = (input: AckFrame) => (v: DeadlineViolation): AccountReplicaError =>
  (v.dispute
    ? { _tag: "dispute_required", cause: v.error, frame: input.frame, frameHanko: input.frameHanko }
    : v.error);
/** og incoming/preflight.ts order: structure, chain, tx profile, then (only then) the Hanko, then HTLC deadlines. */
const receipt = <R extends AccountReplica>(
  r: R, input: AckFrame, ctx: InboundAccountContext,
): Gate<R, { validated: DisputeHanko | undefined }> => {
  const { frame } = input, validated = receivedDispute(r, input.disputeHanko, ctx.from, ctx.verify);
  if (!validated.ok) return answered(validated);
  if (frame.height < r.head.height) return answered(ok(staleFrame(r, frame)));
  const byLeft = other(ctx.party.left);
  const gates = lazyChecks<AccountReplicaError>(
    () => frameStructure(frame),
    () => receiverClock(frame, ctx.now),
    () => guard(frame.prevFrameHash === r.head.prevFrameHash, { _tag: "hash_mismatch" }),
    () => guard(frame.height === r.head.height + 1n, { _tag: "height_mismatch" }),
    () => traverse(frame.txs, (tx) => wireTx(tx, replicaId(r), byLeft)),
    () => certifies(ctx.verify, frame.stateHash, input.frameHanko, ctx.from, peerAuthority(ctx, false)),
    () => mapErr(incomingDeadline(r.state, frame, byLeft, ctx), deadlineRefusal(input)),
  );
  return gates.ok ? { _tag: "continue", validated: validated.value } : answered(gates);
};
const ROOT_MISMATCH_TEXT = "Bilateral account state root mismatch";
type Evidence = (cause: AccountReplicaError, reason?: string) => AccountReplicaError;
const disputeEvidence = (input: AckFrame): Evidence => (cause, reason) => ({
  _tag: "dispute_required", cause, frame: input.frame, frameHanko: input.frameHanko,
  ...opt("reason", reason ?? (cause._tag === "state_root_mismatch" ? ROOT_MISMATCH_TEXT : undefined)),
});
/**
 * og consensus/index.ts classifyIncomingValidationFailure: a stale account-basis hanko for the one unsigned workspace
 * is a plain refusal, not dispute evidence.
 */
const staleIncomingHankos = (
  cur: OpenAccount, frame: AccountFrame, cause: Of<BodyError, "settlement">,
): readonly WireAccountTx[] => {
  const required = minimumSafeNonce(cur.state, cur.dispute);
  return frame.txs.filter((tx) => {
    const n = staleHankoNonce(cur.state, tx, cause);
    return n !== undefined && n.supplied < n.required && n.required === required;
  });
};
/**
 * og replayIncomingFrameOnClone: a refused tx is dispute evidence `Frame application failed: <og text>`; a thrown
 * handler aborts the input.
 */
const replayRefusal = (
  cur: OpenAccount, frame: AccountFrame, ctx: AccountContext, settlement: SettlementCtx, evidence: Evidence,
) => (cause: AccountReplicaError): AccountReplicaError => {
  if (cause._tag === "settlement" && staleIncomingHankos(cur, frame, cause).length === 1) {
    return { _tag: "stale_settlement_hanko", cause };
  }
  if (cause._tag === "state_root_mismatch") return evidence(cause);
  const failure = frameFailure(cur.state, frame, other(ctx.party.left), settlement, ctx.party.self, cur.dispute);
  if (failure === null) return evidence(cause);
  return failure.thrown
    ? { _tag: "account_tx_thrown", message: failure.message }
    : evidence(cause, `Frame application failed: ${failure.message}`);
};
const peerFrameSettlement = (cur: OpenAccount, ctx: AccountContext): SettlementCtx => ({
  verify: ctx.verify,
  proofNonceFloor: proofNonceFloor(cur.dispute),
  ...opt("deltaTransformer", ctx.deltaTransformer),
  ...opt("registeredBoardHash", ctx.counterpartyBoard?.boardHash),
  ...opt("boardAuthority", ctx.boardAuthority),
});
/** The peer's frame, replayed on our body and owed our witness, is held for our ACK; any divergence is evidence. */
const admitPeerFrame = (
  cur: OpenAccount, input: AckFrame, ctx: AccountContext, validated: DisputeHanko | undefined,
): Verb<ReceivedAccount> => {
  const { frame } = input, onLeft = other(ctx.party.left);
  const evidence = disputeEvidence(input), settlement = peerFrameSettlement(cur, ctx);
  const replayed = chain(acceptFrame(frame, replicaId(cur), onLeft), () =>
    mapErr(replay(cur.state, frame, onLeft, settlement), replayRefusal(cur, frame, ctx, settlement, evidence)));
  return chain(replayed, ({ draft, view, finalized }) => {
    const proven = all({
      frameProof: localProof(view, ctx.deltaTransformer),
      witnesses: mapErr(promoteSettled(cur.dispute, cur.state, draft.state, ctx.party.left, finalized), evidence),
    });
    return chain(proven, ({ frameProof, witnesses }) => {
      const owed = mapErr(requireDispute(frameProof, witnesses, validated), (e) =>
        evidence(e, disputeRequirementText(e, frameProof, witnesses, validated)));
      const candidate = new Candidate(frame, input.frameHanko, frameProof, draft, settlement.proofNonceFloor);
      return map(owed, () => done<ReceivedAccount, AccountOutput>({
        ...cur, _tag: "received", candidate, disputeHanko: validated, dispute: witnesses,
      }));
    });
  });
};
type OnOpen = Verb<OpenAccount | ReceivedAccount>;
const proposalOnOpen = (r: OpenAccount, input: AckFrame, ctx: InboundAccountContext): OnOpen =>
  match(receipt(r, input, ctx), {
    answered: ({ result }): OnOpen => result,
    continue: ({ validated }): OnOpen => admitPeerFrame(r, input, ctx, validated),
  });
type OnProposed = Verb<OpenAccount | ProposedAccount | ReceivedAccount>;
/** og incoming/collision.ts: LEFT keeps its frame, ignoring RIGHT's; RIGHT rolls its frame back and accepts LEFT's. */
const collision = (
  r: ProposedAccount, input: AckFrame, ctx: InboundAccountContext, validated: DisputeHanko | undefined,
): OnProposed => {
  const height = input.frame.height, own = r.candidate.frame;
  if (ctx.party.left) {
    const waiting = `⚠️ LEFT has ${r.mempool.length} pending txs while waiting for RIGHT's ACK`;
    const pending = r.mempool.length > 0 ? [accountSay(waiting)] : [];
    return ok(done<OpenAccount | ProposedAccount | ReceivedAccount, AccountOutput>(r, [
      accountSay(`📤 LEFT-WINS: Ignored RIGHT's frame ${height} (waiting for their ACK)`), ...pending,
    ]));
  }
  const restored = unqueued(own.txs, r.mempool).length;
  const said = [
    accountSay(`🔄 ROLLBACK: Discarded our frame ${own.height}, restored ${restored}/${own.txs.length} txs to mempool`),
    accountSay(`📥 Accepted LEFT's frame ${height} (we are RIGHT, deterministic tiebreaker)`),
  ];
  return map(admitPeerFrame(restore(r), input, ctx, validated), (a) =>
    ({ replica: a.replica, outputs: [...said, ...a.outputs] }));
};
const proposalOnProposed = (r: ProposedAccount, input: AckFrame, ctx: InboundAccountContext): OnProposed =>
  match(receipt(r, input, ctx), {
    answered: ({ result }): OnProposed => result,
    continue: ({ validated }): OnProposed => collision(r, input, ctx, validated),
  });
const proposalOnReceived = (r: ReceivedAccount, input: AckFrame, ctx: InboundAccountContext): Verb<ReceivedAccount> =>
  match(receipt(r, input, ctx), {
    answered: ({ result }): Verb<ReceivedAccount> => result,
    continue: (): Verb<ReceivedAccount> =>
      (sameHex(input.frame.stateHash, r.candidate.frame.stateHash) ? ok(done(r)) : err({ _tag: "already_proposed" })),
  });
/** A held frame re-verified from scratch (after a restart): its hash, its proposer's Hanko, and its replay. */
export const restoreCandidate = (
  held: ProposedAccount | ReceivedAccount, party: Party, verify: Verify, dt?: DeltaTransformerRef,
): Result<Candidate, AccountReplicaError> => {
  const { frame, frameHanko, floor, sent } = held.candidate, byLeft = proposerIsLeft(held, party);
  const proposer = at(party.self, party.peer, byLeft === party.left);
  const settlement: SettlementCtx = { verify, proofNonceFloor: floor, ...opt("deltaTransformer", dt) };
  const verified = lazyChecks<AccountReplicaError>(
    () => acceptFrame(frame, replicaId(held), byLeft),
    () => certifies(verify, frame.stateHash, frameHanko, proposer, { allowPreviousBoard: true }),
  );
  return chain(verified, () => chain(replay(held.state, frame, byLeft, settlement), ({ draft, view }) =>
    map(localProof(view, dt), (frameProof) => new Candidate(frame, frameHanko, frameProof, draft, floor, sent))));
};
export const dropFrozen = <R extends FrozenAccount>(r: R): Verb<R> => ok(done(r));
// og freezeAccountForDispute: J claims survive while preparation can still return to active; matcher evidence survives
// preparation only.
const isDeferredClaim = (tx: WireAccountTx): boolean => tx.type === "j_event_claim";
const isDisputeEvidence = (tx: WireAccountTx): boolean => tx.type === "swap_resolve";
const survivesPreparation = (tx: WireAccountTx): boolean => isDeferredClaim(tx) || isDisputeEvidence(tx);
const retainedThroughFreeze = (r: AccountReplica, keep: (tx: WireAccountTx) => boolean): readonly WireAccountTx[] => {
  const queued = r.mempool.filter(keep), pending = r._tag === "proposed" ? r.candidate.frame.txs.filter(keep) : [];
  return [...unqueued(pending, queued), ...queued];
};
/** What a frozen Account keeps of the live one: the committed state, head, witnesses and Entity-side fields. */
const frozenCore = (r: AccountReplica, evidence: FrameEvidence | undefined) => {
  const { state, head, dispute, acknowledged } = r;
  return { state, head, dispute, acknowledged, evidence, ...envMeta(r) };
};
/** A complete peer witness starts the dispute at once; without one the Account prepares, keeping what it needs. */
const freeze = (
  r: AccountReplica, evidence: FrameEvidence | undefined, ctx: AccountContext,
): Verb<PreparingAccount | DisputedAccount> => {
  const frozen = frozenCore(r, evidence);
  const start = startOf(r.state, r.dispute, ctx.party.peer, ctx.verify, ctx.deltaTransformer);
  if (!start.ok) {
    const mempool = retainedThroughFreeze(r, survivesPreparation);
    return ok(done<PreparingAccount, AccountOutput>({ _tag: "preparing", ...frozen, mempool, unready: start.error }));
  }
  return ok(done<DisputedAccount, AccountOutput>(
    { _tag: "disputed", ...frozen, mempool: [], start: start.value },
    [{ kind: "start_dispute", start: start.value }],
  ));
};
/** og returnPreparedAccountToActive: only a preparing Account reopens, keeping its deferred J claims. */
export const resumePreparing = (r: PreparingAccount): Verb<OpenAccount> =>
  ok(done(reopen(r, { state: r.state, head: r.head, mempool: retainedThroughFreeze(r, isDeferredClaim) })));
export const disputeLive = (
  r: OpenAccount | ProposedAccount | ReceivedAccount, input: Freeze, ctx: AccountContext,
): Verb<PreparingAccount | DisputedAccount> => freeze(r, input.evidence, ctx);
export const disputePreparing = (
  r: PreparingAccount, _input: Freeze, ctx: AccountContext,
): Verb<PreparingAccount | DisputedAccount> => freeze(r, r.evidence, ctx);
export const disputeDisputed = (r: DisputedAccount): Verb<DisputedAccount> => ok(done(r));
/**
 * og markAccountDisputePreparing (replaceDisputeLifecycle → dispute_preparing): freeze keeping deferred J claims and
 * dispute evidence, record disputePrepare.
 */
export const prepareFrozen = (
  r: OpenAccount | ProposedAccount | ReceivedAccount, prepare: DisputePrepare, unready: StartRefusal,
): PreparingAccount =>
  ({
    _tag: "preparing", ...frozenCore(r, undefined),
    mempool: retainedThroughFreeze(r, survivesPreparation), unready, prepare,
  });
/**
 * og queueDisputeStart's replaceDisputeLifecycle → disputed: disputePrepare is dropped, the mempool is emptied,
 * activeDispute is the unobserved start.
 */
export const startPrepared = (r: PreparingAccount, start: DisputeStart, queued: QueuedDispute): DisputedAccount =>
  ({ _tag: "disputed", ...frozenCore(r, r.evidence), mempool: [], start, queued });
/**
 * og index.ts handleStandaloneDispute: shape, validate against the committed state, then the requirement ladder against
 * our own current draft (og `currentDisputeProofBodyHash`), then store.
 */
export const peerWitness = <R extends LiveAccount>(r: R, input: PeerDispute, ctx: ReceivedContext): Verb<R> => {
  if (ctx.from !== ctx.party.peer) return err({ _tag: "unknown_signer", entity: ctx.from });
  const { current, counterparty } = r.dispute;
  const validated = chain(disputeShapes([input.disputeHanko]), () =>
    validateCounterparty(r.state, input.disputeHanko, ctx.from, ctx.verify));
  return chain(validated, (witness) => chain(asProof(committedView(r.state)), ({ jNonce }): Verb<R> => {
    const reason = disputeRequirement(
      current?.proofBodyHash, counterparty?.proofBodyHash, counterparty?.proofNonce, jNonce, witness,
    );
    return reason !== undefined
      ? err(refuseDispute(reason))
      : ok(done<R, AccountOutput>({ ...r, dispute: storeCounterparty(r.dispute, witness) }));
  }));
};
export type BoardRefreshRefusal =
  | "party_mismatch" | "activation_height" | "activation_log_index" | "certified_board_missing" | "activation_mismatch"
  | "activation_order" | "height_mismatch" | "frame_hash_mismatch" | "frame_hanko_missing" | "frame_hanko_invalid"
  | "dispute_mismatch";
const refuseRefresh = (reason: BoardRefreshRefusal): Result<never, AccountReplicaError> =>
  err({ _tag: "board_hanko_refresh", reason });
type FramePoint = { readonly height: bigint; readonly hash: string };
/**
 * og currentHeight/currentFrame: the committed head, or a received frame (og commits it on receipt; the rewrite holds
 * it until the Entity answers).
 */
const currentFrameOf = (r: AccountReplica): FramePoint => (r._tag === "received"
  ? { height: r.candidate.frame.height, hash: r.candidate.frame.stateHash }
  : match(r.head, {
    genesis: (): FramePoint => ({ height: 0n, hash: "" }),
    installed: (h): FramePoint => ({ height: h.height, hash: h.prevFrameHash }),
  }));
type BoardActivation = { readonly jHeight: number; readonly logIndex: number };
/** The activation must be the peer's certified board's own. */
const activationRefusal = (a: BoardActivation, board: CertifiedBoard | undefined): BoardRefreshRefusal | undefined => {
  switch (true) {
    case !Number.isSafeInteger(a.jHeight) || a.jHeight < 1: return "activation_height";
    case !Number.isSafeInteger(a.logIndex) || a.logIndex < 0: return "activation_log_index";
    case board === undefined: return "certified_board_missing";
    case a.jHeight !== board?.activatedAtJHeight || a.logIndex !== board?.logIndex: return "activation_mismatch";
    default: return undefined;
  }
};
/** An activation no newer than the last refresh is refused, unless it retries that very refresh. */
const staleActivation = (prev: BoardRefresh | undefined, a: BoardActivation, at: FramePoint): boolean => {
  if (prev === undefined) return false;
  const sameActivation = a.jHeight === prev.activationJHeight && a.logIndex === prev.activationLogIndex;
  const notNewer = a.jHeight < prev.activationJHeight
    || (a.jHeight === prev.activationJHeight && a.logIndex <= prev.activationLogIndex);
  const exactRetry = sameActivation && at.height === BigInt(prev.frameHeight) && at.hash === prev.frameHash;
  return notNewer && !exactRetry;
};
/** The refresh must name our current frame exactly, and carry a frame Hanko. */
const refreshFrameRefusal = (
  at: FramePoint, current: FramePoint, frameHanko: Hanko,
): BoardRefreshRefusal | undefined => {
  switch (true) {
    case at.height < 1n || at.height > BigInt(Number.MAX_SAFE_INTEGER) || at.height !== current.height:
      return "height_mismatch";
    case !/^0x[0-9a-f]{64}$/.test(at.hash) || at.hash !== current.hash: return "frame_hash_mismatch";
    case frameHanko.length === 0: return "frame_hanko_missing";
    default: return undefined;
  }
};
const sameWitnessTerms = (d: DisputeHanko, held: DisputeHanko): boolean =>
  sameHex(d.hash, held.hash) && sameHex(d.proofBodyHash, held.proofBodyHash)
  && d.proofNonce === held.proofNonce && d.proposerIsLeft === held.proposerIsLeft;
/** A re-Hanko'd dispute witness must be the one we hold, re-signed under the current board: only its Hanko changes. */
const refreshedWitness = (
  r: AccountReplica, d: DisputeHanko | undefined, ctx: ReceivedContext, fresh: HankoAuthority,
): Result<DisputeHanko | undefined, AccountReplicaError> => {
  const held = r.dispute.counterparty;
  if (d === undefined) return ok(undefined);
  if (held === undefined || !sameWitnessTerms(d, held)) return refuseRefresh("dispute_mismatch");
  return map(validateCounterparty(r.state, d, ctx.from, ctx.verify, fresh), (v) => ({ ...held, hanko: v.hanko }));
};
/** The peer's side of the head certificate (or the held frame) carries its new Hanko; money and nonces never change. */
const rehankoed = <R extends AccountReplica>(
  r: R, frameHanko: Hanko, party: Party, witness: DisputeHanko | undefined, record: BoardRefresh,
): R => {
  const peerSide = (c: HeadCertificate): HeadCertificate =>
    (party.left ? { ...c, right: frameHanko } : { ...c, left: frameHanko });
  const head: AccountHead = r._tag !== "received" && r.head._tag === "installed"
    ? { ...r.head, certificate: peerSide(r.head.certificate) }
    : r.head;
  const dispute = witness === undefined ? r.dispute : { ...r.dispute, counterparty: witness };
  const rehanko = (c: Candidate): Candidate => new Candidate(c.frame, frameHanko, c.frameProof, c.draft, c.floor);
  const held = r._tag === "received" ? { candidate: rehanko(r.candidate) } : {};
  return { ...r, head, dispute, boardRefresh: record, ...held } as R;
};
/**
 * og incoming/board-hanko-refresh.ts handleBoardHankoRefresh: party, activation against the certified board, activation
 * order (an exact retry passes), the current frame, then the frame Hanko and optional dispute Hanko under the current
 * board only (allowPreviousBoard false). Installs the Hankos and the refresh record.
 */
export const refreshBoardHanko = <R extends AccountReplica>(
  r: R, input: AccountInputFor<"board_hanko_refresh">, ctx: ReceivedContext,
): Verb<R> => {
  const activation: BoardActivation = { jHeight: input.boardActivationJHeight, logIndex: input.boardActivationLogIndex };
  const named: FramePoint = { height: input.height, hash: input.frameHash.toLowerCase() };
  const head = currentFrameOf(r), current: FramePoint = { ...head, hash: head.hash.toLowerCase() };
  const fresh = peerAuthority(ctx, false);
  const refusal = firstDefined<BoardRefreshRefusal>(
    () => (ctx.from !== ctx.party.peer ? "party_mismatch" : undefined),
    () => activationRefusal(activation, ctx.counterpartyBoard),
    () => (staleActivation(r.boardRefresh, activation, named) ? "activation_order" : undefined),
    () => refreshFrameRefusal(named, current, input.frameHanko),
    () => (ctx.verify(current.hash, input.frameHanko, ctx.from, fresh) ? undefined : "frame_hanko_invalid"),
  );
  if (refusal !== undefined) return refuseRefresh(refusal);
  const record: BoardRefresh = {
    activationJHeight: activation.jHeight, activationLogIndex: activation.logIndex,
    frameHeight: Number(named.height), frameHash: current.hash,
  };
  return map(refreshedWitness(r, input.disputeHanko, ctx, fresh), (witness) => done<R, AccountOutput>(
    rehankoed(r, input.frameHanko, ctx.party, witness, record),
    [accountSay(`🔐 Refreshed Account frame ${named.height} Hankos under the current board`)],
  ));
};
const checkAckFrame = (input: AckFrame, ctx: InboundAccountContext): Checked =>
  (ctx.from !== ctx.party.peer
    ? err({ _tag: "unknown_signer", entity: ctx.from })
    : disputeShapes([input.disputeHanko, input.ack?.disputeHanko]));
/** A bundled ACK on a replica holding no frame of ours can only be a no-op; its notes are still said. */
const carriedWithoutFrame = (
  r: OpenAccount | ReceivedAccount, input: AckFrame, ctx: InboundAccountContext,
): Result<readonly AccountOutput[], AccountReplicaError> =>
  (input.ack === null
    ? ok([])
    : map(headAck(r, input.ack, ctx, input.frame.height, input.ack.height), (h) =>
      (h._tag === "identity" ? (h.notes ?? []).map(accountSay) : [])));
const saidFirst = <R extends AccountReplica>(said: readonly AccountOutput[], v: Verb<R>): Verb<R> =>
  (said.length === 0 ? v : map(v, (a) => ({ replica: a.replica, outputs: [...said, ...a.outputs] })));
const pastGates = <R extends AccountReplica, S extends AccountReplica>(
  r: R, input: AckFrame, ctx: InboundAccountContext, rest: () => Verb<S>,
): Verb<R | S> => chain(checkAckFrame(input, ctx), () => match(replayGate(r, input, ctx), {
  answered: ({ result }): Verb<R | S> => result,
  continue: (): Verb<R | S> => rest(),
}));
type AnyLive = Verb<OpenAccount | ProposedAccount | ReceivedAccount>;
/** og index.ts: the ACK phase commits before the proposal phase, so a refused successor frame keeps the ACK. */
const thenProposal = (
  acked: Verb<OpenAccount | ProposedAccount>, input: AckFrame, ctx: InboundAccountContext,
): AnyLive =>
  chain(acked, (a) => {
    const next = match(a.replica, {
      open: (o): AnyLive => proposalOnOpen(o, input, ctx),
      proposed: (p): AnyLive => proposalOnProposed(p, input, ctx),
    });
    if (!next.ok && a.replica._tag === "open") {
      return err({ _tag: "rejected_after_ack", cause: next.error, committed: a });
    }
    return map(next, (p) => ({ replica: p.replica, outputs: [...a.outputs, ...p.outputs] }));
  });
/** The ACK bundled with a peer frame: it commits our held frame, or (riding the frame right after it) is a no-op. */
const carriedAck = (
  r: ProposedAccount, a: AccountAck, head: HeadAck, input: AckFrame, ctx: InboundAccountContext,
): ProposeResult => match(head, {
  identity: ({ notes }): ProposeResult => ok(noted(r, notes)),
  advance: ({ target, validated }): ProposeResult => {
    const held = r.candidate.frame.height;
    switch (true) {
      case target === held: return commitOwn(r, a, validated, ctx);
      case input.frame.height === held && a.height === held - 1n: return ok(done(r));
      default: return err({ _tag: "ack_unmatched" });
    }
  },
});
export const ackFrameOpen = (r: OpenAccount, input: AckFrame, ctx: InboundAccountContext): OnOpen =>
  pastGates(r, input, ctx, (): OnOpen =>
    chain(carriedWithoutFrame(r, input, ctx), (said) => saidFirst(said, proposalOnOpen(r, input, ctx))));
export const ackFrameProposed = (r: ProposedAccount, input: AckFrame, ctx: InboundAccountContext): AnyLive =>
  pastGates(r, input, ctx, (): AnyLive => {
    const a = input.ack;
    if (a === null) return thenProposal(ok(done(r)), input, ctx);
    const held = r.candidate.frame.height, target = input.frame.height === held + 1n ? held : a.height;
    return chain(headAck(r, a, ctx, input.frame.height, target), (h) =>
      thenProposal(carriedAck(r, a, h, input, ctx), input, ctx));
  });
export const ackFrameReceived = (
  r: ReceivedAccount, input: AckFrame, ctx: InboundAccountContext,
): Verb<ReceivedAccount> =>
  pastGates(r, input, ctx, (): Verb<ReceivedAccount> =>
    chain(carriedWithoutFrame(r, input, ctx), (said) => saidFirst(said, proposalOnReceived(r, input, ctx))));
const accountVerb = grammar<AccountGrammar>(AccountTransition);
export const propose = accountVerb("propose", {
  open: proposeOpen,
  proposed: { _tag: "already_proposed" },
  received: { _tag: "already_proposed" },
  preparing: frozenError("preparing"),
  disputed: frozenError("disputed"),
});
export const ack = accountVerb("ack", {
  open: ackOpen, proposed: ackProposed, received: ackReceived, preparing: dropFrozen, disputed: dropFrozen,
});
export const ackFrame = accountVerb("ack_frame", {
  open: ackFrameOpen,
  proposed: ackFrameProposed,
  received: ackFrameReceived,
  preparing: dropFrozen,
  disputed: dropFrozen,
});
export const freezeAccount = accountVerb("freeze", {
  open: disputeLive,
  proposed: disputeLive,
  received: disputeLive,
  preparing: disputePreparing,
  disputed: disputeDisputed,
});
export const dispute = accountVerb("dispute", {
  open: peerWitness, proposed: peerWitness, received: peerWitness, preparing: dropFrozen, disputed: dropFrozen,
});
export const boardHankoRefresh = accountVerb("board_hanko_refresh", {
  open: refreshBoardHanko,
  proposed: refreshBoardHanko,
  received: refreshBoardHanko,
  preparing: refreshBoardHanko,
  disputed: refreshBoardHanko,
});
type FinalityReason = Of<AccountReplicaError, "finality">["reason"];
const finalityErr = (reason: FinalityReason): Result<never, AccountReplicaError> => err({ _tag: "finality", reason });
const safeNonce = (n: number): boolean => Number.isSafeInteger(n) && n >= 0;
/** The on-chain dispute's nonces must be sane and its clock the Account's own response windows. */
const startedRefusal = (r: AccountReplica, f: DisputeStartedFinality): FinalityReason | undefined => {
  const windows = r.state.terms.disputeConfig;
  switch (true) {
    case !safeNonce(f.initialNonce): return "initial_nonce";
    case !safeNonce(f.jNonce): return "j_nonce";
    case !safeNonce(f.observedBlockNumber): return "observed_block";
    case !Number.isSafeInteger(f.disputeTimeout) || !Number.isSafeInteger(f.disputeStartTimestamp)
      || f.disputeStartTimestamp <= 0 || f.disputeTimeout < f.disputeStartTimestamp: return "timeout";
    case f.leftResponseSeconds !== windows.leftResponseSeconds
      || f.rightResponseSeconds !== windows.rightResponseSeconds
      || f.disputeTimeout !== f.disputeStartTimestamp + f.leftResponseSeconds + f.rightResponseSeconds:
      return "clock_mismatch";
    default: return undefined;
  }
};
const activeDisputeOf = (r: AccountReplica, f: DisputeStartedFinality): ActiveDispute => ({
  startedByLeft: f.starterEntityId.toLowerCase() === replicaId(r).left.toLowerCase(),
  initialProofbodyHash: f.initialProofbodyHash,
  initialNonce: f.initialNonce,
  initialProposerIsLeft: f.initialProposerIsLeft,
  disputeTimeout: f.disputeTimeout,
  disputeStartTimestamp: f.disputeStartTimestamp,
  jNonce: f.jNonce,
  starterInitialArguments: f.starterInitialArguments,
  starterCounterArguments: f.starterCounterArguments,
  starterCounterProofCommitment: f.starterCounterProofCommitment,
  observedOnChain: true,
  observedBlockNumber: f.observedBlockNumber,
  ...opt("batchNonce", f.batchNonce),
  ...opt("crossJurisdictionRecovery", r._tag === "preparing" ? r.prepare?.crossJurisdictionRecovery : undefined),
  finalizeQueued: false,
});
const frozenEvidenceOf = (r: AccountReplica): FrameEvidence | undefined =>
  (r._tag === "preparing" || r._tag === "disputed" ? r.evidence : undefined);
const ownStartOf = (r: AccountReplica): DisputeStart | undefined => (r._tag === "disputed" ? r.start : undefined);
/**
 * og j-finality.ts applyAccountDisputeStarted: validate nonces and the dispute clock, freeze keeping evidence, record
 * activeDispute, raise jNonce.
 */
const disputeStarted = (r: AccountReplica, f: DisputeStartedFinality): Verb<DisputedAccount> => {
  const refusal = startedRefusal(r, f);
  if (refusal !== undefined) return finalityErr(refusal);
  // og sets status 'disputed' before freezeAccountForDispute(account, true): deferred claims are dropped, dispute
  // evidence (queued or in our pending frame) is kept.
  const { head, dispute: witnesses, acknowledged } = r;
  return ok(done<DisputedAccount, AccountOutput>({
    _tag: "disputed",
    state: { ...r.state, jNonce: Math.max(r.state.jNonce, f.jNonce) },
    head, dispute: witnesses, acknowledged, evidence: frozenEvidenceOf(r),
    mempool: retainedThroughFreeze(r, isDisputeEvidence),
    ...opt("start", ownStartOf(r)),
    active: activeDisputeOf(r, f),
    ...envMeta(r),
  }));
};
type FinalizedDispute = Extract<AccountFinality, { kind: "dispute_finalized" }>;
/** Every delta's off-chain part is retired; a finalized token's on-chain collateral and ondelta settle to zero too. */
const finalizedDeltas = (deltas: ReadonlyMap<TokenId, Delta>, finalized: ReadonlySet<string>): Map<TokenId, Delta> =>
  new Map([...deltas].map(([tk, d]): readonly [TokenId, Delta] =>
    [tk, { ...d, ...(finalized.has(String(tk)) ? { collateral: 0n, ondelta: 0n } : {}), offdelta: 0n }]));
const finalizedBody = (s: AccountBody, f: FinalizedDispute): AccountBody => {
  const deltas = finalizedDeltas(s.account.deltas, new Set(f.finalizedTokenIds.map((t) => String(t))));
  return {
    ...s,
    settlement: undefined,
    jNonce: f.finalizedJNonce,
    account: { ...s.account, deltas },
    locks: new Map(),
    offers: new Map(),
    ...(s.pulls !== undefined && s.pulls.size > 0 ? { pulls: new Map() } : {}),
  };
};
/**
 * og j-finality.ts applyAccountDisputeFinality: the winning proof's nonce becomes jNonce; off-chain economics, holds
 * and encumbrances are retired; the peer witness tuple is dropped.
 */
const disputeFinalized = (r: AccountReplica, f: FinalizedDispute): Verb<DisputedAccount> => {
  if (!safeNonce(f.finalizedJNonce)) return finalityErr("finalized_nonce");
  if (!f.finalizedTokenIds.every((t) => Number.isSafeInteger(t) && t >= 0)) return finalityErr("token_id");
  const { current, nextProofNonce } = r.dispute;
  const witnesses: DisputeWitnesses = {
    ...opt("current", current), nextProofNonce: Math.max(nextProofNonce, f.finalizedJNonce + 1),
  };
  return ok(done<DisputedAccount, AccountOutput>({
    _tag: "disputed", state: finalizedBody(r.state, f), head: r.head, dispute: witnesses,
    acknowledged: r.acknowledged, evidence: frozenEvidenceOf(r), mempool: [],
    ...opt("start", ownStartOf(r)), ...envMeta(r),
  }));
};
export const applyFinality = (r: AccountReplica, input: AccountInputFor<"external_finality">): Verb<DisputedAccount> =>
  (input.finality.kind === "dispute_started" ? disputeStarted(r, input.finality) : disputeFinalized(r, input.finality));
export const externalFinality = accountVerb("external_finality", {
  open: applyFinality,
  proposed: applyFinality,
  received: applyFinality,
  preparing: applyFinality,
  disputed: applyFinality,
});
export const resume = accountVerb("resume", {
  open: { _tag: "not_preparing" },
  proposed: { _tag: "not_preparing" },
  received: { _tag: "not_preparing" },
  preparing: resumePreparing,
  disputed: frozenError("disputed"),
});


const ENTITY_WORD = /^0x[0-9a-f]{64}$/;
export const genesisReplica = (id: AccountId, terms: AccountTerms): Result<OpenAccount, AccountReplicaError> => {
  if (!ENTITY_WORD.test(id.left) || !ENTITY_WORD.test(id.right)) {
    return err({ _tag: "bad_account", reason: "entity_id" });
  }
  if (id.left === id.right) return err({ _tag: "bad_account", reason: "same_entity" });
  const normalized = mapErr(accountTerms(terms), (e): AccountReplicaError => ({ _tag: "bad_account", reason: e._tag }));
  return map(normalized, (t): OpenAccount => ({
    _tag: "open", state: genesisAccountBody(genesisAccount(id), t),
    head: genesisAccountHead(), mempool: [], dispute: genesisWitnesses(),
  }));
};
export type LiveAccount = OpenAccount | ProposedAccount | ReceivedAccount;
type Queued<R extends AccountReplica = AccountReplica> = {
  readonly replica: R; readonly queued: readonly WireAccountTx[];
};
type ClaimVerdict = "admit" | "duplicate" | "conflict";
type JClaim = TxOf<"j_event_claim">;
/** A record we hold at the claim's height: another record is a conflict; the same one on our own side a duplicate. */
const heldVerdict = (s: AccountBody, own: ClaimRow, onLeft: boolean | undefined): ClaimVerdict | undefined =>
  [true, false].reduce<ClaimVerdict | undefined>((found, side) => {
    if (found !== undefined) return found;
    const held = (s.claimRows ?? []).find((h) => h.onLeft === side && h.jHeight === own.jHeight);
    if (held === undefined) return undefined;
    if (!sameEvidence(held, own)) return "conflict";
    return side === onLeft ? "duplicate" : undefined;
  }, undefined);
/** The first queued claim at the same height decides: the same evidence is a duplicate, anything else a conflict. */
const queuedVerdict = (queued: readonly WireAccountTx[], tx: JClaim, own: ClaimRow): ClaimVerdict | undefined => {
  const rival = queued.find((q): q is JClaim => q.type === "j_event_claim" && q.jHeight === tx.jHeight);
  if (rival === undefined) return undefined;
  const row = claimRowOf(rival, true);
  return row.ok && sameEvidence(row.value, own) ? "duplicate" : "conflict";
};
/**
 * og planAccountJClaimLocalAdmission: a claim at or below the finalized height, or already held on our own side or
 * queued with the same evidence, is a duplicate; a different record at that height (either side, or queued) is a row
 * conflict. `onLeft` undefined: the caller did not name its side, so no held record counts as our own.
 */
const claimAdmission = (
  s: AccountBody, queued: readonly WireAccountTx[], tx: JClaim, onLeft: boolean | undefined,
): Result<ClaimVerdict, AccountReplicaError> =>
  map(mapErr(claimRowOf(tx, onLeft ?? true), (e): AccountReplicaError => e), (own) =>
    (own.jHeight <= s.finalizedJHeight
      ? "duplicate"
      : heldVerdict(s, own, onLeft) ?? queuedVerdict(queued, tx, own) ?? "admit"));
/**
 * og assertAccountTxsAdmissible: a rebalance_policy policyVersion outside 0..=MAX_SAFE_INTEGER refuses the whole batch
 * before any mempool write.
 */
const admissible = (txs: readonly WireAccountTx[]): Checked => guard(
  txs.every((tx) =>
    tx.type !== "rebalance_policy" || (Number.isSafeInteger(tx.policyVersion) && tx.policyVersion >= 0)),
  { _tag: "admission_policy", reason: "policy_version" },
);
/**
 * og local-tx-admission.ts: dedupe against mempool + own pending frame; the limit counts both (mempool.ts). A received
 * frame is committed in og, so it pends nothing. og has no status gate: a frozen Account queues too.
 * A claim's lifecycle key is its exact bytes, so a later copy of a refused claim is refused too: deduping first and
 * then judging the surviving claims in order admits exactly what og admits.
 */
const queueOn = <R extends AccountReplica>(
  r: R, pending: readonly WireAccountTx[], txs: readonly WireAccountTx[], onLeft: boolean | undefined,
): Result<Queued<R>, AccountReplicaError> => chain(admissible(txs), () => {
  const held = [...r.mempool, ...pending];
  const fresh = firstBy(txs, lifecycleKey, held.flatMap((tx) => lifecycleKey(tx) ?? []));
  const claims = fresh.filter((tx): tx is JClaim => tx.type === "j_event_claim");
  const judged = foldResult(claims, [] as readonly JClaim[], (admitted, tx) =>
    map(claimAdmission(r.state, [...held, ...admitted], tx, onLeft), (v) => (v === "admit" ? [...admitted, tx] : admitted)));
  return chain(judged, (admitted): Result<Queued<R>, AccountReplicaError> => {
    const kept = new Set<WireAccountTx>(admitted);
    const queued = fresh.filter((tx) => tx.type !== "j_event_claim" || kept.has(tx));
    return held.length + queued.length > ACCOUNT_MEMPOOL_SIZE
      ? err({ _tag: "mempool_full", limit: ACCOUNT_MEMPOOL_SIZE })
      : ok({ replica: { ...r, mempool: [...r.mempool, ...queued] }, queued });
  });
});
const pendingOf = (r: AccountReplica): readonly WireAccountTx[] => (r._tag === "proposed" ? r.candidate.frame.txs : []);
const enqueue = (
  r: AccountReplica, txs: readonly WireAccountTx[], onLeft?: boolean,
): Result<Queued, AccountReplicaError> =>
  queueOn(r, pendingOf(r), txs, onLeft);
const isLive = (r: AccountReplica): r is LiveAccount =>
  r._tag === "open" || r._tag === "proposed" || r._tag === "received";
/**
 * og applyAccountEnqueue: the Account-level lane admits in every status. It never runs a tx body: validation happens
 * when the tx is proposed (og proposal/transactions.ts). `self` names our side for the j-claim duplicate rule.
 */
export const admit = (
  r: AccountReplica, txs: readonly WireAccountTx[], self?: EntityId,
): Result<AccountReplica, AccountReplicaError> => {
  const onLeft = self === undefined ? ok(undefined) : map(partyOf(replicaId(r), self), (p) => p.left);
  return chain(onLeft, (side) => map(enqueue(r, txs, side), (q) => q.replica));
};
/**
 * Entity-owned admission (og tx-effects.ts applyLocalAccountEffects → applyAccountEnqueue). og
 * shouldSuppressReturnedAccountTx: a frozen Account silently takes no new work. `clock`/`verify` are kept for callers;
 * og admission reads neither.
 */
export const admitAt = (
  r: AccountReplica, txs: readonly WireAccountTx[], self: EntityId, _clock?: FrameClock, _verify?: Verify,
): Result<AccountReplica, AccountReplicaError> =>
  (isLive(r) ? admit(r, txs, self) : map(partyOf(replicaId(r), self), () => r));
const accountContext = (r: AccountReplica, ctx: DoorContext): Result<AccountContext, AccountReplicaError> =>
  map(partyOf(replicaId(r), ctx.self), (party) => ({
    verify: ctx.verify,
    party,
    ...opt("counterpartyBoard", ctx.counterpartyBoard),
    ...opt("autoRebalance", ctx.autoRebalance),
    ...opt("deltaTransformer", ctx.deltaTransformer),
    ...opt("boardAuthority", ctx.boardAuthority),
  }));
type Applied = Result<AccountApply, AccountReplicaError>;
export const applyAccountInput = (r: AccountReplica, input: AccountInput, ctx: DoorContext): Applied =>
  chain(accountContext(r, ctx), (c) => {
    const sealed = <I extends AccountEnvelope>(i: I, then: (sender: EntityId) => Applied): Applied =>
      chain(checkEnvelope(replicaId(r), r.state.terms, i), then);
    const delivery = (sender: EntityId): Delivery =>
      (sender === c.party.self ? { _tag: "local" } : { _tag: "received", from: sender });
    return matchBy("kind", input, {
      propose: (i) => propose(r, i, c),
      freeze: (i) => freezeAccount(r, i, c),
      resume: (i) => resume(r, i, c),
      dispute: (i) => sealed(i, (from) => dispute(r, i, { ...c, from })),
      board_hanko_refresh: (i) => sealed(i, (from) => boardHankoRefresh(r, i, { ...c, from })),
      external_finality: (i) => sealed(i, () => externalFinality(r, i, c)),
      ack: (i) => sealed(i, (sender) => ack(r, i, { ...c, delivery: delivery(sender) })),
      ack_frame: (i) => sealed(i, (from) => ackFrame(r, i, {
        ...c, now: ctx.now, finalizedJHeight: ctx.finalizedJHeight ?? r.state.finalizedJHeight, from,
      })),
    });
  });
export const applyDelivered = (
  r: AccountReplica, input: AccountInput, delivery: Delivery, ctx: DoorContext,
): Applied => {
  const local = (): Checked => localOnly(delivery);
  const byPeer = (m: AccountEnvelope): Checked => deliveredBy(m, ctx.self, delivery);
  const routed = matchBy<"kind", AccountInput, Checked>("kind", input, {
    propose: local, freeze: local, resume: local, external_finality: local,
    dispute: byPeer, board_hanko_refresh: byPeer, ack: byPeer, ack_frame: byPeer,
  });
  return chain(routed, () => applyAccountInput(r, input, ctx));
};
/** A refusal that is dispute evidence freezes the Account (after the ACK the input committed first, if any). */
export const disputeUnsafe = (r: AccountReplica, applied: Applied, ctx: DoorContext): Applied => {
  if (applied.ok) return applied;
  const after = applied.error._tag === "rejected_after_ack" ? applied.error : undefined;
  const evidence = evidenceOf(after?.cause ?? applied.error);
  if (evidence === null) return applied;
  const frozen = applyAccountInput(after?.committed.replica ?? r, { kind: "freeze", evidence }, ctx);
  return map(frozen, (d) => ({ replica: d.replica, outputs: [...(after?.committed.outputs ?? []), ...d.outputs] }));
};
export const restoreAccount = (
  r: AccountReplica, self: EntityId, verify: Verify, dt?: DeltaTransformerRef,
): Result<AccountReplica, AccountReplicaError> => chain(partyOf(replicaId(r), self), (party) => {
  type Restored = Result<AccountReplica, AccountReplicaError>;
  const restored = <R extends ProposedAccount | ReceivedAccount>(held: R): Restored =>
    map(restoreCandidate(held, party, verify, dt), (candidate) => ({ ...held, candidate }));
  const kept = (x: AccountReplica): Restored => ok(x);
  return match(r, { open: kept, proposed: restored, received: restored, preparing: kept, disputed: kept });
});

export type Head = { readonly height: bigint; readonly prevFrameHash: EntityFrameHash };
export const genesisHead = (): Head => ({ height: 0n, prevFrameHash: ZERO_HASH as EntityFrameHash });
export type MemberVerify = (h: Hash, sig: Signature, addr: Address) => boolean;
/** The replica's own signer key (og `signAccountFrame(env, signerId, hash)`), one signature per manifest entry. */
export type MemberSign = (h: Hash, addr: Address) => Result<Signature, unknown>;
type Members = ReadonlyMap<Address, { readonly shares: bigint }>;
type TeachingQuorum = { readonly threshold: bigint; readonly members: Members };
type BoardQuorum = { readonly board: Board; readonly entityId: string };
export type Authority = Tagged<"teaching", TeachingQuorum> | Tagged<"board", BoardQuorum>;
/**
 * `proposer` is og's CEO leader: `validators[0]` in stored (positional) order (og leader/index.ts
 * getEntityLeaderOrder).
 */
export type Quorum = Authority & { readonly proposer: Address };
/** og `config.jurisdiction` beyond the account Domain; committed inside the root's config section when present. */
export type JurisdictionConfig = {
  /**
   * og JurisdictionConfig.name: the J replica an Entity's J outputs go to, also read by og's cross-j binding message;
   * never committed (og projectConsensusConfigCommitment).
   */
  readonly name?: string | undefined;
  readonly entityProviderAddress: string;
  readonly registrationBlock?: number | undefined;
  readonly entityProviderDeploymentBlock?: number | undefined;
  readonly blockTimeMs?: number | undefined;
  /** og JurisdictionConfig.rebalancePolicyUsd: whole-USD numbers (og commits them as numbers). */
  readonly rebalancePolicyUsd?:
    { readonly r2cRequestSoftLimit: number; readonly hardLimit: number; readonly maxFee: number } | undefined;
};
/**
 * og EntityState root fields the rewrite carries without interpreting (nonces, reserves, profile, crontabState, ...),
 * by og field name; collection fields in committed radix form.
 */
export type EntityCommitted = { readonly [field: string]: Binary };
/** og EntityLeaderState: absent until the first frame; every proposal state carries it (og buildProposalState). */
export type LeaderState = {
  readonly activeValidatorId: string;
  readonly view: number;
  readonly changedAtHeight: number;
};
export type EntityState = {
  readonly id: EntityId;
  readonly quorum: Quorum;
  readonly jurisdiction: Domain;
  readonly accounts: ReadonlyMap<EntityId, AccountState>;
  readonly height: bigint;
  readonly timestamp: bigint;
  readonly jurisdictionConfig?: JurisdictionConfig | undefined;
  readonly committed: EntityCommitted;
  readonly leaderState?: LeaderState | undefined;
  /**
   * og EntityState.paybook: absent until the first HTLC entry; the root then commits it instead of `committed.paybook`.
   */
  readonly paybook?: Paybook | undefined;
  /**
   * og infrastructure.certifiedBoardNodes: the content-addressed registry nodes under `committed.certifiedBoardState`;
   * never in the root.
   */
  readonly boardNodes?: BoardNodes | undefined;
  /**
   * og crossJurisdictionSwaps / crossJurisdictionAuthorizations: absent until the first cross-j setup; the root then
   * commits each as a text-keyed collection.
   */
  readonly crossJurisdictionSwaps?: ReadonlyMap<string, CrossRoute> | undefined;
  readonly crossJurisdictionAuthorizations?: ReadonlyMap<string, CrossRoute> | undefined;
  /**
   * og crossJurisdictionBookAdmissions: the book owner's admitted cross-j routes, keyed `${source user}:${orderId}`;
   * committed as a text-keyed collection once it exists.
   */
  readonly crossJurisdictionBookAdmissions?: BookAdmissions | undefined;
  /**
   * og EntityState.orderbookExt: present only on a hub that ran initOrderbookExt; the root commits it through
   * orderbookSection.
   */
  readonly orderbookExt?: OrderbookExt | undefined;
};
/** og orderbook/types.ts SpreadDistribution / HubProfile / SwapPairDimensions. */
export type SpreadDistribution = {
  readonly makerBps: number;
  readonly takerBps: number;
  readonly hubBps: number;
  readonly makerReferrerBps: number;
  readonly takerReferrerBps: number;
};
export type HubProfile = {
  readonly entityId: string;
  readonly name: string;
  readonly spreadDistribution: SpreadDistribution;
  readonly referenceTokenId: number;
  readonly usdQuoteAuthorityEntityId: string;
  readonly minTradeSize: bigint;
  readonly supportedPairs: readonly string[];
};
export type PairDimensions = { readonly baseTokenDecimals: number; readonly quoteTokenDecimals: number };
/**
 * og OrderbookExtState without `orderPairs`, which og derives from `books` and never commits. `referrals` is never
 * written by og.
 */
export type OrderbookExt = {
  readonly books: ReadonlyMap<string, Book>;
  readonly pairDimensions: ReadonlyMap<string, PairDimensions>;
  readonly referrals: ReadonlyMap<string, Binary>;
  readonly hubProfile: HubProfile;
};
/**
 * og state-root.ts projectOrderbookConsensusState: each book by its commitment hash, then pairDimensions, hubProfile
 * and referrals.
 */
export const orderbookSection = (x: OrderbookExt): Binary => ({
  books: new Map([...x.books].map(([pairId, book]) => [pairId, bookCommitmentHash(book)])), pairDimensions: new Map(x.pairDimensions), hubProfile: x.hubProfile, referrals: new Map(x.referrals),
}) as unknown as Binary;
/** og EntityLeaderTimeoutVoteBody (leader/index.ts buildEntityLeaderVoteBody). */
export type LeaderVoteBody = {
  readonly entityId: string;
  readonly targetHeight: number;
  readonly previousFrameHash: string;
  readonly fromView: number;
  readonly toView: number;
  readonly previousLeaderId: string;
  readonly nextLeaderId: string;
};
/** og vote.preparedFrame: the voter's exact locked frame with every precommit bundle it holds (og collectedSigs). */
export type PreparedFrame = { readonly frame: EntityFrame; readonly signatures: Precommits };
export type LeaderVote = LeaderVoteBody & {
  readonly voterId: string;
  readonly signature: string;
  readonly preparedFrame?: PreparedFrame | undefined;
};
/** og EntityLeaderCertificate: compact `votes` unless some vote carried prepared evidence (`preparedVotes`). */
export type LeaderCertificate = LeaderVoteBody & {
  readonly votes: ReadonlyMap<string, string>;
  readonly preparedVotes?: ReadonlyMap<string, LeaderVote> | undefined;
  readonly preparedFrameHash?: string | undefined;
};
/** og EntityFrame.leader: not in the frame hash; bound through the committed leaderState in stateRoot/authorityRoot. */
export type FrameLeader = {
  readonly proposerSignerId: string;
  readonly view: number;
  readonly certificate?: LeaderCertificate | undefined;
  readonly relayCertificate?: LeaderCertificate | undefined;
};
/**
 * og `core/types/entity-tx.ts` wire shape `{type, data}`. Account frames are proposed by the Entity frame itself (og
 * proposePendingAccountFrames).
 */
export type EntityTx =
  | {
      readonly type: "openAccount";
      readonly data: {
        readonly targetEntityId: EntityId;
        readonly disputeConfig: DisputeConfig;
        readonly accountDomain: Domain;
        readonly watchSeed: string;
        readonly creditAmount?: bigint | undefined;
        readonly tokenId?: TokenId | undefined;
        readonly pinPublic?: boolean | undefined;
        readonly rebalancePolicy?: RebalancePolicy | undefined;
      };
    }
  | { readonly type: "accountInput"; readonly data: AccountPeerInput }
  | {
      readonly type: "extendCredit";
      readonly data: { readonly counterpartyEntityId: EntityId; readonly tokenId: TokenId; readonly amount: bigint };
    }
  | {
      readonly type: "directPayment";
      readonly data: {
        readonly targetEntityId: EntityId;
        readonly tokenId: TokenId;
        readonly amount: bigint;
        readonly route: readonly EntityId[];
        readonly description?: string | undefined;
        readonly deliveryMode: "direct" | "trusted";
        readonly trustedGatewayEntityId?: EntityId | undefined;
      };
    }
  /** og chat / chatMessage: frame-local messages, never part of the committed state. */
  | { readonly type: "chat"; readonly data: { readonly from: string; readonly message: string } }
  | {
      readonly type: "chatMessage";
      readonly data: {
        readonly message: string;
        readonly timestamp: number;
        readonly metadata?: Readonly<Record<string, unknown>> | undefined;
      };
    }
  | {
      readonly type: "requestCollateral";
      readonly data: {
        readonly counterpartyEntityId: EntityId;
        readonly tokenId: TokenId;
        readonly amount: bigint;
        readonly feeTokenId?: TokenId | undefined;
        readonly feeAmount: bigint;
        readonly policyVersion: number;
      };
    }
  | { readonly type: "profile-update"; readonly data: { readonly profile: ProfileUpdate } }
  /**
   * og setHubConfig (lifecycle/admin.ts): the hub's committed rebalance fee policy; raw token-unit overrides are
   * refused.
   */
  | { readonly type: "setHubConfig"; readonly data: HubConfigInput }
  /**
   * og prepareDispute / disputeStart (entity/tx/handlers/dispute): freeze the Account for a unilateral dispute, then
   * queue its DisputeStart into jBatchState.
   */
  | {
      readonly type: "prepareDispute";
      readonly data: {
        readonly counterpartyEntityId: EntityId;
        readonly description?: string | undefined;
        readonly minCooldownMs?: number | undefined;
        readonly crossJurisdictionRouteId?: string | undefined;
        readonly starterInitialArguments?: string | undefined;
      };
    }
  | {
      readonly type: "disputeStart";
      readonly data: {
        readonly counterpartyEntityId: EntityId;
        readonly crossJurisdictionRouteId?: string | undefined;
        readonly starterInitialArguments?: string | undefined;
        readonly starterCounterArguments?: string | undefined;
        readonly description?: string | undefined;
      };
    }
  /** og disputeFinalize (dispute/finalize.ts): queue the DisputeFinalization row for an on-chain-observed dispute. */
  | {
      readonly type: "disputeFinalize";
      readonly data: {
        readonly counterpartyEntityId: EntityId;
        readonly useOnchainRegistry?: boolean | undefined;
        readonly description?: string | undefined;
      };
    }
  /**
   * og scheduledWake (scheduler/wake): the proposer's consensus-visible crontab marker; validators replay the same due
   * work from EntityState.
   */
  | {
      readonly type: "scheduledWake";
      readonly data: {
        readonly version: 1;
        readonly proposerSignerId: string;
        readonly dueAt: number;
        readonly jobs: readonly ScheduledWakeJob[];
      };
    }
  /** og processHtlcTimeouts (htlc/direct.ts): one `htlc_resolve` timeout Account tx per expired lock. */
  | {
      readonly type: "processHtlcTimeouts";
      readonly data: {
        readonly expiredLocks?: readonly { readonly accountId: string; readonly lockId: string }[] | undefined;
      };
    }
  /**
   * og setRebalancePolicy: this Entity's private per-token automation policy on one Account (the leaf's policyRoot).
   */
  | {
      readonly type: "setRebalancePolicy";
      readonly data: {
        readonly counterpartyEntityId: EntityId;
        readonly tokenId: TokenId;
        readonly r2cRequestSoftLimit: bigint;
        readonly hardLimit: bigint;
        readonly maxAcceptableFee: bigint;
      };
    }
  /**
   * og entityCommand: one board member's signed individual command (og command/command-codec.ts SignedEntityCommandV1).
   */
  | { readonly type: "entityCommand"; readonly data: EntityCommand }
  /**
   * og governance (system/basic.ts): a board proposal and a vote on it; both reach the reducer only inside an
   * entityCommand.
   */
  | { readonly type: "propose"; readonly data: { readonly action: ProposalAction; readonly proposer: string } }
  | {
      readonly type: "vote";
      readonly data: {
        readonly proposalId: string;
        readonly voter: string;
        readonly choice: "yes" | "no";
        readonly comment?: string | undefined;
      };
    }
  | { readonly type: "htlcPayment"; readonly data: HtlcPaymentData }
  /**
   * og entityProviderTransfer / entityProviderReleaseControlShares / entityProviderCancelAction
   * (entity/tx/handlers/entity-provider-action.ts): collective EntityProvider actions.
   */
  | {
      readonly type: "entityProviderTransfer";
      readonly data: { readonly to: string; readonly tokenId: bigint; readonly amount: bigint };
    }
  | {
      readonly type: "entityProviderReleaseControlShares";
      readonly data: {
        readonly recipientAddress: string;
        readonly controlAmount: bigint;
        readonly dividendAmount: bigint;
        readonly purpose: string;
      };
    }
  | { readonly type: "entityProviderCancelAction"; readonly data: { readonly actionHash: string } }
  /**
   * og entityProviderProposeControlBoard / entityProviderActivateBoard (entity/tx/handlers/control-board-proposal.ts).
   */
  | {
      readonly type: "entityProviderProposeControlBoard";
      readonly data: {
        readonly targetEntityId: string;
        readonly newBoardHash: string;
        readonly actionNonce: bigint;
        readonly supporterVotes?: readonly { readonly entityId: string; readonly hankoSignature: string }[] | undefined;
      };
    }
  | { readonly type: "entityProviderActivateBoard"; readonly data: { readonly targetEntityId: string } }
  /**
   * og cross-j setup (handlers/cross-j/setup.ts): user authorization / hub raw intent, the default proposer's prepared
   * route, the paired hub registration.
   */
  | { readonly type: "prepareCrossJurisdictionSwap"; readonly data: { readonly route: CrossRoute } }
  | {
      readonly type: "materializeCrossJurisdictionSwap";
      readonly data: { readonly proposerSignerId: string; readonly route: CrossRoute };
    }
  | { readonly type: "registerCrossJurisdictionSwap"; readonly data: { readonly route: CrossRoute } }
  /**
   * og cross-j book lifecycle (handlers/cross-j/{book-order,book-removal-ack,fill,clear}.ts): admission on the book
   * owner, remote removal and its ACK, hub-internal fill progress, the clear request.
   */
  | {
      readonly type: "admitCrossJurisdictionBookOrder";
      readonly data: { readonly route: CrossRoute; readonly reason?: string | undefined };
    }
  | {
      readonly type: "removeCrossJurisdictionBookOrder";
      readonly data: {
        readonly orderId: string;
        readonly sourceEntityId: string;
        readonly sourceAccountId?: string | undefined;
        readonly route?: CrossRoute | undefined;
        readonly reason?: string | undefined;
      };
    }
  | {
      readonly type: "crossJurisdictionBookOrderRemoved";
      readonly data: {
        readonly orderId: string;
        readonly sourceEntityId: string;
        readonly sourceAccountId: string;
        readonly route: CrossRoute;
        readonly removedAt: number;
        readonly reason?: string | undefined;
      };
    }
  | { readonly type: "crossJurisdictionFillNotice"; readonly data: CrossProgress }
  | {
      readonly type: "requestCrossJurisdictionClear";
      readonly data: {
        readonly orderId: string;
        readonly cancelRemainder?: boolean | undefined;
        readonly route?: CrossRoute | undefined;
      };
    }
  /**
   * og cross-j clear lifecycle (handlers/cross-j/{clear,sweep}.ts, payments/pull.ts): the proposer's public ladder
   * reveal, a paired pull close, the book-TTL sweep.
   */
  | { readonly type: "materializeCrossJurisdictionClear"; readonly data: CrossClearReveal }
  | { readonly type: "crossPullClose"; readonly data: CrossPullCloseData }
  /**
   * og cross-j recovery (handlers/cross-j/{salvage,force-sibling-dispute}.ts, htlc/direct.ts): the Target reveal port,
   * the sibling dispute clock, a verified HTLC resolve.
   */
  | {
      readonly type: "crossJurisdictionSalvage";
      readonly data: {
        readonly routeId: string;
        readonly binary: string;
        readonly fillRatio: number;
        readonly sourceEntityId: string;
        readonly sourceCounterpartyEntityId: string;
        readonly observedAt?: number | undefined;
      };
    }
  | {
      readonly type: "crossJurisdictionForceSiblingDispute";
      readonly data: {
        readonly routeId: string;
        readonly observedCounterpartyEntityId: string;
        readonly observedAt?: number | undefined;
      };
    }
  | {
      readonly type: "resolveHtlcLock";
      readonly data: {
        readonly counterpartyEntityId: string;
        readonly lockId: string;
        readonly secret: string;
        readonly crossJurisdictionRouteId?: string | undefined;
        readonly description?: string | undefined;
      };
    }
  | { readonly type: "orderbookSweepCrossJurisdiction"; readonly data: { readonly reason?: string | undefined } }
  /**
   * og certified Entity->Entity command lane: a committed source frame's cross-j commands, wrapped by its emitter
   * (consensus/output/publication.ts).
   */
  | { readonly type: "runtimeOutput"; readonly data: RuntimeOutputData }
  /**
   * og proposeAccountsNow (handlers/account/propose-accounts-now.ts): the active leader asks its Entity to re-send the
   * retained proposals it still owes these peers.
   */
  | {
      readonly type: "proposeAccountsNow";
      readonly data: {
        readonly version: number;
        readonly proposerSignerId: string;
        readonly counterparties: readonly string[];
      };
    }
  /** og initOrderbookExt (system/basic.ts): make this Entity a matcher with an empty book set. */
  | {
      readonly type: "initOrderbookExt";
      readonly data: {
        readonly name: string;
        readonly spreadDistribution: SpreadDistribution;
        readonly referenceTokenId: number;
        readonly usdQuoteAuthorityEntityId: string;
        readonly minTradeSize: bigint;
        readonly supportedPairs: readonly string[];
      };
    }
  | SwapRequestEntityTx
  | LendingEntityTx
  | SettleEntityTx
  | JBatchEntityTx;
/**
 * og types/entity-tx.ts J-batch txs (entity/tx/handlers/j-batch): queue reserve ops into the committed jBatchState,
 * seal / resend / abort / clear it, the direct admin mint.
 */
export type JBatchEntityTx =
  | {
      readonly type: "r2r";
      readonly data: { readonly toEntityId: string; readonly tokenId: number; readonly amount: bigint };
    }
  | {
      readonly type: "r2e";
      readonly data: { readonly receivingEntity: string; readonly tokenId: number; readonly amount: bigint };
    }
  | {
      readonly type: "r2c";
      readonly data: {
        readonly counterpartyId: string;
        readonly receivingEntityId?: string | undefined;
        readonly tokenId: number;
        readonly amount: bigint;
        readonly rebalanceQuoteId?: number | undefined;
        readonly rebalanceFeeTokenId?: number | undefined;
        readonly rebalanceFeeAmount?: bigint | undefined;
      };
    }
  | {
      readonly type: "e2r";
      readonly data: {
        readonly contractAddress: string;
        readonly tokenType?: number | undefined;
        readonly externalTokenId?: bigint | undefined;
        readonly internalTokenId?: number | undefined;
        readonly amount: bigint;
      };
    }
  | {
      readonly type: "j_broadcast";
      readonly data: { readonly hankoSignature?: string | undefined; readonly feeOverrides?: FeeOverrides | undefined };
    }
  | { readonly type: "j_rebroadcast"; readonly data: { readonly gasBumpBps?: number | undefined } }
  | {
      readonly type: "j_abort_sent_batch";
      readonly data: { readonly reason?: string | undefined; readonly requeueToCurrent?: boolean | undefined };
    }
  | { readonly type: "j_clear_batch"; readonly data: { readonly reason?: string | undefined } }
  | { readonly type: "mintReserves"; readonly data: { readonly tokenId: number; readonly amount: bigint } }
  // og j_event: the active proposer's signed, canonical J range (JurisdictionEventData) the Entity certifies
  | { readonly type: "j_event"; readonly data: { readonly [field: string]: Binary } }
  // og boardHandover: the new board an on-chain BoardActivated chain in the same frame's j_event authorizes
  | { readonly type: "boardHandover"; readonly data: { readonly board: HandoverConfig } };
/** og types/entity-tx.ts settlement workspace operations (payments/settle.ts). */
export type SettleEntityTx =
  | {
      readonly type: "settle_propose";
      readonly data: {
        readonly counterpartyEntityId: EntityId;
        readonly ops: readonly SettlementOp[];
        readonly executorIsLeft?: boolean | undefined;
        readonly memo?: string | undefined;
        readonly continuation?: SettlementContinuationPlan | undefined;
      };
    }
  | {
      readonly type: "settle_update";
      readonly data: {
        readonly counterpartyEntityId: EntityId;
        readonly ops: readonly SettlementOp[];
        readonly executorIsLeft?: boolean | undefined;
        readonly memo?: string | undefined;
      };
    }
  | {
      readonly type: "settle_approve";
      readonly data: { readonly counterpartyEntityId: EntityId; readonly workspaceHash: string };
    }
  | {
      readonly type: "settle_execute";
      readonly data: { readonly counterpartyEntityId: EntityId; readonly disableC2RShortcut?: boolean | undefined };
    }
  | {
      readonly type: "settle_reject";
      readonly data: { readonly counterpartyEntityId: EntityId; readonly reason?: string | undefined };
    };
export type RuntimeOutputData = {
  readonly protocol: "cross-j";
  readonly sourceEntityId: string;
  readonly sourceSignerId: string;
  readonly targetEntityId: string;
  readonly entityTxs: readonly EntityTx[];
};
/**
 * og types/entity-tx.ts placeSwapOffer / proposeCancelSwap (payments/swap-requests.ts): one swap Account tx on the hub
 * Account.
 */
export type SwapRequestEntityTx =
  | {
      readonly type: "placeSwapOffer";
      readonly data: {
        readonly counterpartyEntityId: EntityId;
        readonly offerId: string;
        readonly giveTokenId: TokenId;
        readonly giveTokenDecimals: number;
        readonly giveAmount: bigint;
        readonly wantTokenId: TokenId;
        readonly wantTokenDecimals: number;
        readonly wantAmount: bigint;
        readonly maxFee: bigint;
        readonly minNetReceive: bigint;
        readonly priceTicks?: bigint | undefined;
        readonly timeInForce?: 0 | 1 | 2 | undefined;
      };
    }
  | {
      readonly type: "proposeCancelSwap";
      readonly data: { readonly counterpartyEntityId: EntityId; readonly offerId: string };
    };
/** og setHubConfig data (types/entity-tx.ts). */
export type HubConfigInput = {
  readonly hubName?: string | undefined;
  readonly matchingStrategy?: "amount" | "time" | "fee" | undefined;
  readonly policyVersion?: number | undefined;
  readonly routingFeePPM?: number | undefined;
  readonly baseFee?: bigint | undefined;
  readonly swapTakerFeeBps?: number | undefined;
  readonly disputeAutoFinalizeMode?: "auto" | "ignore" | undefined;
  readonly minCollateralThreshold?: bigint | undefined;
  readonly c2rWithdrawSoftLimit?: bigint | undefined;
  readonly rebalanceBaseFee?: bigint | undefined;
  readonly rebalanceLiquidityFeeBps?: bigint | undefined;
  readonly rebalanceGasFee?: bigint | undefined;
  readonly rebalanceTimeoutMs?: number | undefined;
};
/** og SignedEntityCommandV1: `signature` is og's `0x` compact signature (recovery 0/1) over hashEntityCommand. */
export type EntityCommand = {
  readonly version: 1;
  readonly entityId: string;
  readonly stackKey: string;
  readonly boardHash: string;
  readonly boardEpoch: number;
  readonly authorSignerId: string;
  readonly authorSigner: string;
  readonly nonce: bigint;
  readonly txsHash: string;
  readonly txs: readonly EntityTx[];
  readonly signature: string;
};
/** og ProposalAction (entity/types.ts). */
export type ProposalAction =
  | { readonly type: "collective_message"; readonly data: { readonly message: string } }
  | {
      readonly type: "entity_transaction";
      readonly data: { readonly version: 1; readonly actionHash: string; readonly txs: readonly EntityTx[] };
    };
/** og EntityFrameEvent (frame-events.ts): certified in the frame hash (`events`), never durable state. */
export type FrameEvent =
  | { readonly type: "status"; readonly message: string }
  | { readonly type: "text"; readonly validatorId: string; readonly message: string };
/**
 * og types/entity-tx.ts lendingOffer/Borrow/Repay/ClosePosition: each queues one Account lending tx on the hub Account.
 */
export type LendingEntityTx =
  | {
      readonly type: "lendingOffer";
      readonly data: {
        readonly positionId: string;
        readonly hubEntityId: string;
        readonly tokenId: TokenId;
        readonly amount: bigint;
        readonly termId: string;
        readonly interestBps: number;
      };
    }
  | {
      readonly type: "lendingBorrow";
      readonly data: {
        readonly requestId: string;
        readonly hubEntityId: string;
        readonly tokenId: TokenId;
        readonly amount: bigint;
        readonly termId: string;
        readonly maxInterestBps?: number | undefined;
      };
    }
  | {
      readonly type: "lendingRepay";
      readonly data: {
        readonly hubEntityId: string;
        readonly loanId: string;
        readonly tokenId: TokenId;
        readonly amount: bigint;
      };
    }
  | {
      readonly type: "lendingClosePosition";
      readonly data: { readonly hubEntityId: string; readonly positionId: string };
    };
/** og ProfileUpdateTx & { entityId }. */
export type ProfileUpdate = {
  readonly entityId: string;
  readonly name?: string | undefined;
  readonly entityKind?: string | null | undefined;
  readonly sectors?: readonly string[] | undefined;
  readonly avatar?: string | undefined;
  readonly bio?: string | undefined;
  readonly website?: string | undefined;
};
export type HashToSign = {
  readonly hash: string;
  readonly type:
    "entityFrame" | "accountFrame" | "dispute" | "profile" | "settlement" | "entityProviderAction" | "jBatch";
  readonly context: string;
};
export type EntityFrame = Head & {
  readonly timestamp: bigint;
  readonly txs: readonly EntityTx[];
  readonly events: readonly Binary[];
  readonly stateRoot: string;
  readonly authorityRoot: string;
  readonly entityContext: EntityInfraContext;
  readonly hashesToSign: readonly HashToSign[];
  readonly leader: FrameLeader;
  /**
   * og frame.jPrefixCertificate: the round certificate this frame's J prefix (and any j_event) is judged by; in the
   * frame hash.
   */
  readonly jPrefixCertificate?: JPrefixCertificate | undefined;
};
/** og `hashPrecommits`/`collectedSigs`: lowercase signer id to one signature per `hashesToSign` entry. */
export type Precommits = ReadonlyMap<string, readonly Signature[]>;
export type OutputTx = Extract<EntityTx, { readonly type: "accountInput" }>;
/** An Account message for a peer entity's leader, or an og consensus input for one validator replica of this entity. */
export type EntityOutput = (
  | { readonly to: EntityId; readonly tx: OutputTx }
  | { readonly to: EntityId; readonly signerId: Address; readonly input: EntityInput }
) & {
  /** og markCommittedAtomicCrossJAckOutputs stamps the two ACKs of a committed atomic cross-j proposal pair. */
  readonly atomicCrossJurisdictionPair?: AtomicCrossPair | undefined;
};
/** og EntityInput lanes: `entityTxs`, `proposedFrame` (+`collectedSigs`), `hashPrecommitFrame` + `hashPrecommits`. */
export type EntityInput =
  | { readonly kind: "txs"; readonly timestamp: bigint; readonly txs: readonly EntityTx[] }
  /**
   * `hankos`: og's commit notice carries the committed frame's own Hanko (og installCommittedState `frame.hankos =
   * [hankos[0]]`); receivers rebuild it.
   */
  | {
      readonly kind: "proposal";
      readonly frame: EntityFrame;
      readonly signatures: Precommits;
      readonly hankos?: readonly Hanko[] | undefined;
    }
  | {
      readonly kind: "precommit";
      readonly height: bigint;
      readonly frameHash: EntityFrameHash;
      readonly signatures: Precommits;
    }
  /**
   * og `leaderTimeoutVote` lane. `local` is og's process-local marker: the scheduler's unsigned intent for this
   * replica's own signer, never set on an output.
   */
  | {
      readonly kind: "leaderTimeoutVote";
      readonly timestamp: bigint;
      readonly vote: LeaderVote;
      readonly local?: boolean | undefined;
    }
  /**
   * og `jPrefixAttestations` lane: validators' signed J-prefix heads (one per input after merge); the Runtime stamps
   * its frame clock.
   */
  | {
      readonly kind: "jPrefixAttestations";
      readonly attestations: ReadonlyMap<string, JPrefixAttestation>;
      readonly timestamp?: bigint | undefined;
    };
export type EntityPhase = "open" | "proposed" | "locked";
export type EntityEvent = EntityInput["kind"];
export type Folded = { readonly state: EntityState; readonly accountReplicas: ReadonlyMap<EntityId, AccountReplica> };
/**
 * og EntityCandidateEffect `runtimeEvent` (AccountOpening, Htlc*, request_collateral_committed, ...): inert values the
 * Runtime publishes after the frame commits, never in a hash.
 */
export type EntityRuntimeEvent = { readonly eventName: string; readonly data: { readonly [k: string]: unknown } };
/** What folding txs over an Entity produced so far, besides the state and its Accounts. */
export type Draft = Folded & {
  readonly outputs: readonly EntityOutput[];
  /** og frame events the folded txs emitted (og addMessage / addTextMessage), in order. */
  readonly events?: readonly FrameEvent[] | undefined;
  readonly touched?: readonly EntityId[] | undefined;
  /** og EntityTxReducerResult hashesToSign (settlement Hankos, EntityProvider actions), in order. */
  readonly hashes?: readonly HashToSign[] | undefined;
  /** og EntityTxReducerResult jOutputs of the folded txs, in order. */
  readonly jOutputs?: readonly JInput[] | undefined;
  readonly swaps?: SwapEvents | undefined;
  /** og candidateEffects runtime events the folded txs emitted, in order. */
  readonly runtimeEvents?: readonly EntityRuntimeEvent[] | undefined;
};
/** og replica `leaderVotes` (one collection key at a time) and `pendingLeaderCertificate`. */
type LeaderLane = {
  readonly leaderVotes?: ReadonlyMap<string, LeaderVote> | undefined;
  readonly pendingLeaderCertificate?: LeaderCertificate | undefined;
};
/**
 * og replica `jPrefixRound`: this validator's J-prefix round for the next Entity height (validator-private, persisted
 * in replica meta).
 */
type JPrefixLane = { readonly jPrefixRound?: JPrefixRound | undefined };
/**
 * og replica `certifiedFrameHead`: the committed frame's certified link as og projectCertifiedEntityFrameLinkIdentity
 * (absent before the first commit).
 */
type CertifiedLane = { readonly certifiedFrameHead?: Binary | undefined };
type EntityEnv = Folded &
  LeaderLane &
  JPrefixLane &
  CertifiedLane & { readonly signerId: Address; readonly head: Head; readonly mempool: readonly EntityTx[] };
type EntityCandidate = { readonly frame: EntityFrame; readonly signatures: Precommits; readonly draft: Draft };
export interface OpenEntity extends Tagged<"open", EntityEnv> {}
/** og `proposal`: this replica proposed `frame` and collects precommits. */
export interface ProposedEntity extends Tagged<"proposed", EntityEnv & EntityCandidate> {}
/** og `lockedFrame`: this validator replayed and signed `frame`. */
export interface LockedEntity extends Tagged<"locked", EntityEnv & EntityCandidate> {}
export type EntityReplica = OpenEntity | ProposedEntity | LockedEntity;
export type EntityContext = {
  readonly verify: Verify;
  readonly verifyMember: MemberVerify;
  readonly sign: MemberSign;
  readonly self: EntityId;
  readonly signerId: Address;
  readonly from?: EntityId | undefined;
  readonly htlc?: HtlcProposerInfra | undefined;
  /** og env.runtimeSeed: the default proposer derives cross-j hash-ladder seeds from it (never committed). */
  readonly runtimeSeed?: string | undefined;
  /**
   * og EntityRuntimeContext.activeJurisdiction: the Runtime's active J name (never committed; the Htlc* event
   * jurisdictionId fallback).
   */
  readonly activeJurisdiction?: string | undefined;
  /**
   * og replica.jHistory: this validator's local J history (Runtime replica-local, never committed), pruned to the
   * committed finality.
   */
  readonly jHistory?: ValidatorJHistory | undefined;
  /**
   * og env.state.eReplicas for a cross-j opening cohort (selectCrossJOpeningAccountProposalTxs); absent outside a
   * Runtime, where no sibling gating runs.
   */
  readonly siblings?: SiblingReplicas | undefined;
  /**
   * og resolveSettlementBoardAuthority over env.state.eReplicas (the source Entity's local replicas); absent outside a
   * Runtime.
   */
  readonly boardAuthority?: BoardAuthority | undefined;
  /**
   * og applyEntityInput options for a `txs` input: `defer` admits without proposing (the Runtime flushes each touched
   * replica once per frame), `cross-j` is a trusted local cross-j command that must commit alone, `account-work`
   * proposes only queued Account work.
   */
  readonly lane?: "defer" | "cross-j" | "account-work" | undefined;
  /** og requiredEntityTx: the one accountInput of an atomic cross-j leg, which the proposal must select. */
  readonly required?: EntityTx | undefined;
  /**
   * og env.jReplicas: the durable jurisdiction stacks Account proofs resolve their DeltaTransformer from (never
   * committed).
   */
  readonly jReplicas?: ReadonlyMap<string, JReplica> | undefined;
};
export type EntityFrameHashError =
  | BinaryError
  | Tagged<"frame_clock", { readonly value: bigint }>
  | Tagged<"frame_root", { readonly value: string }>
  | Tagged<"frame_too_large">
  | Tagged<"frame_tx", { readonly code: string }>;
export type EntityError =
  | AccountReplicaError
  | EntityRootError
  | EntityFrameHashError
  | Tagged<
      | "account_exists"
      | "no_such_account"
      | "create_ack_required"
      | "account_envelope"
      | "swap_request_account_missing",
      { target: EntityId }
    >
  | Tagged<
      | "self_account"
      | "wrong_entity"
      | "bad_quorum"
      | "bad_jurisdiction"
      | "from_not_converted"
      | "not_l0"
      | "mempool_full"
      | "sign_failed"
      | "payment_route"
      | "secondary_hash_duplicate"
    >
  | Tagged<"frame_timestamp_invalid" | "frame_timestamp_regression", { timestamp: bigint }>
  | Tagged<"lending_entity", { reason: string }>
  /** og EntityCommandRejectionError (a MalformedEntityFrameInputError): the outermost frame tx is evicted. */
  | Tagged<"entity_command", { reason: string }>
  /** og plain Error from an entity-tx reducer: not a reject disposition, so the whole input is refused. */
  | Tagged<"entity_invariant", { reason: string }>
  | Tagged<
      | "proposal_digest"
      | "proposal_parent"
      | "proposal_leader"
      | "proposal_hash"
      | "proposal_manifest"
      | "proposal_signature"
      | "proposal_conflict"
      | "proposal_wait"
      | "local_manifest_mismatch"
      | "local_precommit_conflict"
    >
  | Tagged<
      | "precommit_frame_mismatch"
      | "precommit_not_active"
      | "precommit_signer_equivocation"
      | "commit_conflict"
      | "commit_wait"
    >
  | Tagged<
      | "leader_vote_invalid"
      | "leader_vote_equivocation"
      | "leader_prepared_rejected"
      | "proposal_superseded"
      | "hanko_build"
    >
  | Tagged<"profile_update", { reason: "entity" | "entity_kind" | "sectors_invalid" | "sectors_noncanonical" }>
  | Tagged<"htlc_payment", { code: string }>
  /**
   * og MalformedEntityFrameInputError from a cross-j setup handler: a reject disposition, the outermost tx is evicted.
   */
  | Tagged<"cross_j_entity", { reason: string }>
  | Tagged<
      "unknown_member" | "duplicate_member" | "not_proposer" | "invalid_signature" | "wrong_replica",
      { address: string }
    >
  /** og J-prefix outcomes: an input reject/defer reason code, or a thrown failure's code, message and disposition. */
  | Tagged<"j_prefix", { code: string; message: string; disposition: "reject" | "retry" | "halt" | "defer" }>;
export type EntityGrammar = {
  readonly table: typeof EntityTransition;
  readonly replica: EntityReplica;
  readonly input: EntityInput;
  readonly ctx: { readonly [E in EntityEvent]: EntityContext };
  readonly output: EntityOutput;
  readonly error: EntityError;
};
export type NextEntityPhase<S extends EntityPhase, E extends EntityEvent> = Next<EntityGrammar, S, E>;
/**
 * og HankoWitnessEntry: the quorum Hanko of one secondary `hashesToSign` entry, kept validator-locally for crash-safe
 * external writes.
 */
export type HankoWitness = {
  readonly hanko: Hanko;
  readonly type: "accountFrame" | "dispute" | "profile" | "settlement" | "jBatch" | "entityProviderAction";
  readonly entityHeight: number;
  readonly createdAt: number;
};
/**
 * og attachCommitProofsAndOutputs effects of one committed Entity frame: the witnesses of its secondary hashes (Runtime
 * stamps `createdAt`), and the frame's jOutputs with their quorum Hankos attached, present only on the emitter replica
 * (og `if (isEmitter) jOutbox.push(...)`).
 */
export type CommitEffects = {
  readonly height: number;
  readonly witnesses: readonly (readonly [string, Omit<HankoWitness, "createdAt">])[];
  readonly jOutputs: readonly JInput[];
  readonly runtimeEvents: readonly EntityRuntimeEvent[];
};
type EntityApply<R extends EntityReplica = EntityReplica> = Apply<R, EntityOutput> & {
  readonly committed?: CommitEffects | undefined;
};
export const encodeEntityTx = (tx: EntityTx): string => `${tx.type}|${canon(tx.data)}`;
const quorumSection = (q: Quorum) => match(q, {
  teaching: (t) => ({ threshold: t.threshold, members: t.members }),
  board: (b) => ({ board: encodeBoardHash({ board: b.board }), entityId: b.entityId }),
});
export const encodeEntityState = (s: EntityState): string => canon({
  id: s.id,
  quorum: quorumSection(s.quorum),
  jurisdiction: s.jurisdiction,
  accounts: new Map([...s.accounts].map(([peer, a]) => [peer, hashAccountState(a)])),
  height: s.height,
  timestamp: s.timestamp,
  jurisdictionConfig: s.jurisdictionConfig,
  committed: s.committed,
  leaderState: s.leaderState,
  paybook: s.paybook,
  boardNodes: s.boardNodes,
  crossJurisdictionSwaps: s.crossJurisdictionSwaps,
  crossJurisdictionAuthorizations: s.crossJurisdictionAuthorizations,
  crossJurisdictionBookAdmissions: s.crossJurisdictionBookAdmissions,
  orderbookExt: s.orderbookExt === undefined ? undefined : orderbookSection(s.orderbookExt),
} satisfies Record<keyof EntityState, unknown>);
export const hashEntityState = (s: EntityState): EntityStateHash => keccakUtf8(encodeEntityState(s)) as EntityStateHash;
const HEX_EXT = 0x48;
/** msgpackr registers one class for the process: 0x-hex text of at least 16 bytes packs as its raw bytes. */
class HexPack {
  readonly bytes: Uint8Array;
  constructor(value: string) { this.bytes = hexToBytes(value); }
}
addExtension({
  Class: HexPack, type: HEX_EXT, pack: (value: HexPack) => value.bytes, unpack: (bytes: Uint8Array) => bytesToHex(bytes),
});
const binaryPack = new Packr({ mapsAsObjects: false, moreTypes: true });
export type Binary =
  | null | boolean | number | bigint | string
  | readonly Binary[]
  | ReadonlyMap<Binary, Binary>
  | { readonly [key: string]: Binary };
export type BinaryError = Tagged<"binary">;
/** Byte-wise order, a proper prefix first. */
const compareBytes = (left: Uint8Array, right: Uint8Array): number => {
  const differs = left.findIndex((b, i) => i < right.length && b !== right[i]);
  return differs < 0 ? left.length - right.length : (left[differs] ?? 0) - (right[differs] ?? 0);
};
const packedHex = (value: string): HexPack | string =>
  value.length >= 34 && value.startsWith("0x") && value.length % 2 === 0 && /^0x[0-9a-f]+$/.test(value) ? new HexPack(value) : value;
type Walked = Binary | HexPack;
type PackedEntry = { readonly key: Walked; readonly value: Walked; readonly bytes: Uint8Array };
// og binary-codec.ts canonicalize: no non-finite, unsafe-integer or negative-zero numbers.
const canonicalNumber = (n: number): boolean =>
  Number.isFinite(n) && !(Number.isInteger(n) && !Number.isSafeInteger(n)) && !Object.is(n, -0);
/** og canonicalize: entries ordered by the packed bytes of the key, then of the value. */
const walkMap = (m: ReadonlyMap<Binary, Binary>): Result<Binary, BinaryError> => {
  const entries = traverse(m, ([key, item]) => chain(walkBinary(key), (k) =>
    map(walkBinary(item), (v): PackedEntry => ({ key: k, value: v, bytes: packBinary(k) }))));
  const byPacked = (a: PackedEntry, b: PackedEntry): number =>
    compareBytes(a.bytes, b.bytes) || compareBytes(packBinary(a.value), packBinary(b.value));
  return map(entries, (es) => new Map(es.toSorted(byPacked).map((e) => [e.key, e.value])) as unknown as Binary);
};
/** A plain record packs with its keys sorted, as a prototype-less object. */
const walkRecord = (value: { readonly [key: string]: Binary }): Result<Binary, BinaryError> => {
  const fields = traverse(Object.keys(value).toSorted(), (key) =>
    map(walkBinary(value[key] as Binary), (walked) => [key, walked] as const));
  return map(fields, (entries) => Object.assign(Object.create(null), Object.fromEntries(entries)) as Binary);
};
const walkBinary = (value: Binary): Result<Walked, BinaryError> => {
  if (value === null || typeof value === "boolean" || typeof value === "bigint") return ok(value);
  if (typeof value === "number") return canonicalNumber(value) ? ok(value) : err({ _tag: "binary" });
  if (typeof value === "string") return ok(packedHex(value));
  // og packs raw bytes (a BrowserVM state root) as msgpackr's typed-array extension, unlike hex text.
  if ((value as unknown) instanceof Uint8Array) return ok(value);
  if (Array.isArray(value)) return map(traverse(value, walkBinary), (items) => items as Binary);
  if (value instanceof Map) return walkMap(value as ReadonlyMap<Binary, Binary>);
  if (typeof value !== "object") return err({ _tag: "binary" });
  return walkRecord(value as { readonly [key: string]: Binary });
};
const packBinary = (value: unknown): Uint8Array => binaryPack.pack(value) as Uint8Array;
const encodeBinary = (value: Binary): Result<Uint8Array, BinaryError> =>
  map(walkBinary(value), (walked) => concat([Uint8Array.of(0x03), packBinary(walked)]));
const encodeConsensus = (value: Binary): Result<Uint8Array, BinaryError> => map(walkBinary(value), packBinary);
const integrity = (bytes: Uint8Array): string => bytesToHex(sha256(bytes));
export type EntityRootJurisdiction = {
  readonly chainId?: number | undefined;
  readonly depositoryAddress: string;
} & JurisdictionConfig;
export type EntityRootConfig = {
  readonly mode: "proposer-based" | "gossip-based";
  readonly threshold: bigint;
  readonly validators: readonly string[];
  readonly shares: Readonly<Record<string, bigint>>;
  readonly jurisdiction?: EntityRootJurisdiction | undefined;
};
export type EntityRootAccount = {
  readonly fromEntity: string;
  readonly toEntity: string;
  readonly state: CommittedAccountState;
  readonly status: "active" | "dispute_preparing" | "disputed";
  readonly currentHeight: number;
  readonly nextProofNonce: number;
  /** og commits `currentFrame.stateHash` only when the replica has a current frame. */
  readonly currentFrameHash?: string | undefined;
  readonly pendingWithdrawals: string;
  readonly policyRoot: string;
  readonly submittedAtByTokenRoot: string;
  /**
   * og ACCOUNT_ENTITY_COMMITTED_FIELDS that are committed only when present (state-root.ts:184); the three counterparty
   * Hankos are committed as their integrity digest.
   */
  readonly committed?: Partial<Readonly<Record<EntityLeafOptional, unknown>>> | undefined;
  /** og counterpartySettlementHankos(settlementWorkspace, localIsLeft), when present. */
  readonly counterpartySettlementHankos?: unknown;
  readonly activeQuote?: unknown;
  readonly pendingRequest?: unknown;
  readonly rejectedFrameEvidence?:
    { readonly reason: unknown; readonly frameHash: unknown; readonly frameHanko: unknown } | undefined;
};
export const ENTITY_LEAF_OPTIONAL = [
  "publicPinned", "boardHankoRefreshMigration", "counterpartyBoardHankoRefresh",
  "counterpartyFrameHanko", "counterpartyDisputeProofHanko", "counterpartySettlementHanko",
  "currentDisputeProofNonce", "currentDisputeProofProposerIsLeft", "currentDisputeProofBodyHash", "currentDisputeHash",
  "counterpartyDisputeProofNonce", "counterpartyDisputeProofProposerIsLeft", "counterpartyDisputeProofBodyHash",
  "counterpartyDisputeHash", "disputePrepare", "activeDispute",
] as const;
export type EntityLeafOptional = (typeof ENTITY_LEAF_OPTIONAL)[number];
const LEAF_HANKO_FIELDS: ReadonlySet<string> =
  new Set(["counterpartyFrameHanko", "counterpartyDisputeProofHanko", "counterpartySettlementHanko"]);
type EntityPaybook = {
  readonly entries: { readonly radix: 16; readonly leafCount: number; readonly root: string };
  readonly feesEarned: bigint;
};
const EMPTY_ENTITY_PAYBOOK = {
  entries: { radix: 16 as const, leafCount: 0, root: ZERO_WORD }, feesEarned: 0n,
} satisfies EntityPaybook;
export type EntityRootError =
  BinaryError | CommitmentError | Tagged<"bad_config" | "account_key" | "account_owner" | "duplicate_account">;
const signerId = (raw: string): string => raw.trim().toLowerCase();
/** Validator shares keyed by normalized signer id; an empty or repeated id is a bad config. */
const memberShares = (shares: Readonly<Record<string, bigint>>): Result<Record<string, bigint>, Tagged<"bad_config">> =>
  foldResult(Object.entries(shares), {} as Record<string, bigint>, (acc, [raw, share]) => {
    const id = signerId(raw);
    return id.length === 0 || Object.hasOwn(acc, id) ? err({ _tag: "bad_config" }) : ok({ ...acc, [id]: share });
  });
const rootJurisdiction = (j: EntityRootJurisdiction): Result<Binary, Tagged<"bad_config">> => {
  const depositoryAddress = j.depositoryAddress.trim().toLowerCase();
  const entityProviderAddress = j.entityProviderAddress.trim().toLowerCase();
  if (depositoryAddress === "" || entityProviderAddress === "") return err({ _tag: "bad_config" });
  const policy = j.rebalancePolicyUsd;
  return ok({
    ...opt("chainId", j.chainId), depositoryAddress, entityProviderAddress,
    ...opt("registrationBlock", j.registrationBlock),
    ...opt("entityProviderDeploymentBlock", j.entityProviderDeploymentBlock),
    ...opt("blockTimeMs", j.blockTimeMs),
    ...(policy === undefined ? {} : {
      rebalancePolicyUsd: { r2cRequestSoftLimit: policy.r2cRequestSoftLimit, hardLimit: policy.hardLimit, maxFee: policy.maxFee },
    }),
  });
};
const consensusConfig = (config: EntityRootConfig): Result<Binary, Tagged<"bad_config">> => chain(memberShares(config.shares), (shares) => {
  const validators = config.validators.map(signerId), j = config.jurisdiction;
  if (validators.some((id) => id.length === 0)) return err({ _tag: "bad_config" });
  const base = { mode: config.mode, threshold: config.threshold, validators, shares };
  return j === undefined ? ok(base) : map(rootJurisdiction(j), (jurisdiction) => ({ ...base, jurisdiction }));
});
const accountKey = (id: string): Result<Uint8Array, Tagged<"account_key">> => {
  const norm = signerId(id);
  const bytes = parseHex(norm);
  return bytes !== null && norm.startsWith("0x") && bytes.length === 32 ? ok(bytes) : err({ _tag: "account_key" });
};
const ACCOUNT_LEAF = utf8("xln.entity.account-leaf.v3");
/** og ACCOUNT_ENTITY_COMMITTED_FIELDS present on the leaf; the counterparty Hankos commit as their integrity digest. */
const leafOptional = (account: EntityRootAccount): Record<string, unknown> =>
  Object.fromEntries(ENTITY_LEAF_OPTIONAL.flatMap((field) => {
    const value = account.committed?.[field];
    if (value === undefined) return [];
    return [[field, LEAF_HANKO_FIELDS.has(field) && typeof value === "string" ? integrity(utf8(value)) : value]];
  }));
const leafBody = (account: EntityRootAccount, accountStateRoot: string) => ({
  ...leafOptional(account),
  status: account.status,
  currentHeight: account.currentHeight,
  proofHeader: { fromEntity: account.fromEntity, toEntity: account.toEntity, nextProofNonce: account.nextProofNonce },
  accountStateRoot,
  ...opt("currentFrameHash", account.currentFrameHash),
  ...opt("counterpartySettlementHankos", account.counterpartySettlementHankos),
  pendingWithdrawals: account.pendingWithdrawals,
  shadow: {
    rebalance: {
      policyRoot: account.policyRoot, submittedAtByTokenRoot: account.submittedAtByTokenRoot,
      ...opt("activeQuote", account.activeQuote), ...opt("pendingRequest", account.pendingRequest),
    },
    ...opt("rejectedFrameEvidence", account.rejectedFrameEvidence),
  },
});
const accountLeaf = (account: EntityRootAccount): Result<readonly [Uint8Array, Uint8Array], EntityRootError> => {
  const owner = signerId(account.fromEntity);
  if (owner !== signerId(account.state.leftEntity) && owner !== signerId(account.state.rightEntity)) {
    return err({ _tag: "account_owner" });
  }
  const parts = all({ key: accountKey(account.toEntity), stateRoot: accountStateCommitment(account.state) });
  return chain(parts, ({ key, stateRoot }) => map(encodeCanonicalValue(leafBody(account, stateRoot)), (encoded) =>
    [key, sha256(concat([ACCOUNT_LEAF, encoded]))] as const));
};
/**
 * og entityCollectionCommitment (entity/state/persistent-collection-map.ts): radix-16 over the raw keys (u16-prefixed
 * text, or a paybook hashlock's 32 bytes), each leaf the integrity digest of the value's consensus bytes.
 */
export type EntityCollectionCommitment = { readonly radix: 16; readonly leafCount: number; readonly root: string };
type CollectionKeys = "text" | "paybookHashlock";
const collectionKeyBytes = (key: string, keyCodec: CollectionKeys): Uint8Array =>
  (keyCodec === "paybookHashlock" ? hexToBytes(key) : concat([u16(utf8(key).length), utf8(key)]));
const collectionLeaf = (keyCodec: CollectionKeys) => ([key, value]: readonly [string, Binary]): Result<Leaf, EntityRootError> => {
  switch (true) {
    case keyCodec === "paybookHashlock" ? !/^0x[0-9a-f]{64}$/.test(key) : utf8(key).length > 0xffff: return err({ _tag: "bad_key" });
    case holdsCollection(value): return err({ _tag: "nested_collection" });
    default: return chain(encodeConsensus(value), (bytes): Result<Leaf, EntityRootError> => {
      const k = collectionKeyBytes(key, keyCodec);
      return bytes.length > MAX_LEAF_BYTES ? err({ _tag: "leaf_too_large" }) : ok({ nibbles: nibblesOf(k), key: k, digest: sha256(bytes) });
    });
  }
};
export const entityCollectionCommitment = (
  entries: ReadonlyMap<string, Binary>, keyCodec: CollectionKeys = "text",
): Result<EntityCollectionCommitment, EntityRootError> =>
  map(traverse(entries, collectionLeaf(keyCodec)), (leaves) =>
    ({ radix: 16 as const, leafCount: leaves.length, root: sealRadix(leaves) }));
const sectionDigest = (value: Binary): Result<string, BinaryError> => map(encodeConsensus(value), integrity);
export const ENTITY_STATE_ROOT_FIELDS = [
  "entityId", "height", "timestamp", "nonces", "entityCommandNonces", "proposals", "config", "leaderState", "reserves",
  "accounts", "externalWallet", "deferredAccountProposals", "settlementContinuations", "lastFinalizedJHeight",
  "jHistoryFinality", "certifiedBoardState", "crontabState", "jBatchState", "entityProviderActionState",
  "entityEncryptionPublicKey", "profile", "paybook", "outDebtsByToken", "inDebtsByToken", "orderbookExt",
  "swapTradingPairs", "crossJurisdictionSwaps", "crossJurisdictionAuthorizations", "crossJurisdictionBookAdmissions",
  "hubRebalanceConfig", "lending",
] as const;
const DERIVED_ROOT_FIELDS: ReadonlySet<string> =
  new Set(["entityId", "height", "timestamp", "config", "accounts", "leaderState"]);
export type EntityRootInput = {
  readonly config: EntityRootConfig;
  readonly accounts: readonly EntityRootAccount[];
  readonly entityId?: string | undefined;
  readonly height?: number | undefined;
  readonly timestamp?: number | undefined;
  readonly committed?: EntityCommitted | undefined;
  readonly leaderState?: LeaderState | undefined;
};
type Section = readonly [string, Binary];
const whenPresent = (field: string, value: Binary | undefined): readonly Section[] =>
  (value === undefined ? [] : [[field, value]]);
/** Every root section: the derived ones, then each committed og field the rewrite carries (a paybook defaults empty). */
const rootSections = (input: EntityRootInput, config: Binary, leaves: readonly (readonly [Uint8Array, Uint8Array])[]): readonly Section[] => {
  const root = sealRadix(leaves.map(([key, digest]) => ({ nibbles: nibblesOf(key), key, digest })));
  const committed = Object.entries(input.committed ?? {}).filter(([field]) =>
    (ENTITY_STATE_ROOT_FIELDS as readonly string[]).includes(field) && !DERIVED_ROOT_FIELDS.has(field));
  const accounts = { domain: "xln.entity.accounts.radix-merkle:binary", radix: 16, hashAlgorithm: "integrity", leafCount: leaves.length, root };
  return [
    ["accounts", accounts],
    ["config", config],
    ...(committed.some(([field]) => field === "paybook") ? [] : [["paybook", EMPTY_ENTITY_PAYBOOK] as const]),
    ...whenPresent("entityId", input.entityId),
    ...whenPresent("height", input.height),
    ...whenPresent("timestamp", input.timestamp),
    ...whenPresent("leaderState", input.leaderState === undefined ? undefined : { ...input.leaderState }),
    ...committed,
  ];
};
/** The root: the keccak of the consensus bytes of every section's integrity digest, sections in name order. */
const sealSections = (sections: readonly Section[]): Result<string, EntityRootError> => {
  const digests = traverse(sections.toSorted(([left], [right]) => asc(left, right)), ([field, value]) =>
    map(sectionDigest(value), (digest) => ({ field, digest })));
  return chain(digests, (sealed) =>
    map(encodeConsensus({ domain: "xln.entity.consensus-state.sections:binary", sections: sealed }), (bytes) =>
      bytesToHex(keccak_256(bytes))));
};
export const entityStateRoot = (input: EntityRootInput): Result<string, EntityRootError> => {
  const parts = all({ config: consensusConfig(input.config), leaves: traverse(input.accounts, accountLeaf) });
  return chain(parts, ({ config, leaves }): Result<string, EntityRootError> =>
    (new Set(leaves.map(([key]) => bytesToHex(key))).size !== leaves.length
      ? err({ _tag: "duplicate_account" })
      : sealSections(rootSections(input, config, leaves))));
};
const ACCOUNT_INPUT_DOMAIN = "xln:account-input-commitment:v1";
const accountInputCommitment = (value: Binary): Result<{ readonly domain: string; readonly inputDigest: string }, BinaryError> =>
  map(encodeConsensus(value), (body) =>
    ({ domain: ACCOUNT_INPUT_DOMAIN, inputDigest: integrity(concat([utf8(ACCOUNT_INPUT_DOMAIN), body])) }));
const looseInt = (v: unknown): number => {
  const n = Number(v ?? 0);
  return Number.isFinite(n) ? Math.floor(n) : 0;
};
const looseLower = (v: unknown): string => String(v ?? "").toLowerCase();
const jEventForHash = (e: WireJEvent) => ({
  blockNumber: e.blockNumber ?? null,
  blockHash: e.blockHash?.toLowerCase() ?? null,
  transactionHash: e.transactionHash?.toLowerCase() ?? null,
  logIndex: e.logIndex ?? null,
  eventIndex: e.eventIndex ?? null,
  type: e.type,
  data: e.data,
});
const jBlockForHash = (raw: unknown): Result<Binary, EntityFrameHashError> => {
  const block = looseRecord(raw);
  const events = canonicalJEvents(Array.isArray(block["events"]) ? block["events"] : []);
  if (!events.ok) return err({ _tag: "frame_tx", code: (events.error as { code?: string }).code ?? events.error._tag });
  return ok({
    blockNumber: looseInt(block["blockNumber"]),
    blockHash: looseLower(block["blockHash"]),
    eventsHash: looseLower(block["eventsHash"]),
    events: events.value.map(jEventForHash) as unknown as Binary,
    disputeFinalizationEvidenceHash: looseLower(block["disputeFinalizationEvidenceHash"]),
  });
};
/**
 * og canonicalJEventDataForFrameHash: a J range commits as its canonical projection (version tag, lowercased text,
 * integer heights, canonical events).
 */
const jEventDataForHash = (value: Binary): Result<Binary, EntityFrameHashError> => {
  const plain = value !== null && typeof value === "object" && !(value instanceof Uint8Array) && !(value instanceof Map);
  const data = plain ? looseRecord(value) : {};
  const rawBlocks = data["blocks"];
  if (!Array.isArray(rawBlocks) || data["rangeHash"] === undefined) {
    return err({ _tag: "frame_tx", code: "ENTITY_FRAME_J_EVENT_RANGE_REQUIRED" });
  }
  return chain(traverse(rawBlocks as readonly unknown[], jBlockForHash), (blocks) => mapErr(binaryBody({
    version: "xln:j-event-range-frame:v1",
    from: looseLower(data["from"]),
    jurisdictionRef: String(data["jurisdictionRef"] ?? "").trim().toLowerCase(),
    baseHeight: looseInt(data["baseHeight"]),
    scannedThroughHeight: looseInt(data["scannedThroughHeight"]),
    tipBlockHash: looseLower(data["tipBlockHash"]),
    eventHistoryRoot: looseLower(data["eventHistoryRoot"]),
    rangeHash: looseLower(data["rangeHash"]),
    blocks,
    signature: looseLower(data["signature"]),
    observedAt: looseInt(data["observedAt"]),
  }), (): EntityFrameHashError => ({ _tag: "binary" })));
};
type HashedTx = { readonly type: string; readonly data: unknown };
const entityTxForHash = (tx: EntityFrameTx): Result<HashedTx, EntityFrameHashError> => {
  switch (tx.type) {
    case "accountInput": return map(accountInputCommitment(tx.data), (data) => ({ type: tx.type, data }));
    case "j_event": return map(jEventDataForHash(tx.data), (data) => ({ type: tx.type, data }));
    default: return ok(tx);
  }
};
/** Each part prefixed by its u32 big-endian length. */
const u32Framed = (parts: readonly Uint8Array[]): Uint8Array => concat(parts.flatMap((p) => [u32(p.byteLength), p]));
const entityTxDigest = (txs: readonly EntityFrameTx[]): Result<{ readonly digest: string; readonly bytes: number }, EntityFrameHashError> => {
  const encoded = traverse(txs, (tx) => chain(entityTxForHash(tx), (hashed) => encodeBinary(hashed as Binary)));
  return map(encoded, (parts) => {
    const body = u32Framed(parts);
    return { digest: integrity(concat([utf8("xln:entity-frame-txs:binary"), body])), bytes: body.byteLength };
  });
};
export type EntityInfraContext = {
  readonly version: number;
  readonly proposerReplicaId: string;
  readonly entityId: string;
  readonly proposerSignerId: string;
  readonly parentFrameHash: string;
  readonly height: number;
  readonly gossipProfiles: readonly Binary[];
  readonly peerAssertions: readonly Binary[];
  readonly htlc: {
    readonly version: number;
    readonly entries: readonly Binary[];
    readonly originated: readonly Binary[];
  };
};
export type EntityFrameTx = { readonly type: string; readonly data: Binary };
export type EntityFrameHashInput = {
  readonly prevFrameHash: string;
  readonly height: number;
  readonly timestamp: number;
  readonly txs: readonly EntityFrameTx[];
  readonly events: readonly Binary[];
  readonly entityId: string;
  readonly stateRoot: string;
  readonly authorityRoot: string;
  readonly entityContext: EntityInfraContext;
  readonly jPrefixCertificate?: Binary | null;
};
/** og LIMITS.MAX_FRAME_SIZE_BYTES: the event budget and the whole frame (header + context + length-prefixed txs). */
const MAX_FRAME_SIZE_BYTES = 100_000_000;
const frameHeader = (input: EntityFrameHashInput, txsDigest: string, context: Uint8Array): Binary => ({
  domain: "xln:entity-frame:binary-context-digest",
  prevFrameHash: input.prevFrameHash,
  height: input.height,
  timestamp: input.timestamp,
  txCount: input.txs.length,
  txsDigest,
  events: input.events,
  entityId: input.entityId,
  stateRoot: input.stateRoot.toLowerCase(),
  authorityRoot: input.authorityRoot.toLowerCase(),
  entityContextDigest: integrity(context),
  jPrefixCertificate: input.jPrefixCertificate ?? null,
});
/**
 * og createEntityFrameHashFromStateRoot (frame.ts:393): event budget, canonical lowercase roots, then the total wire
 * budget.
 */
export const entityFrameHash = (input: EntityFrameHashInput): Result<string, EntityFrameHashError> => {
  const events = input.events.length === 0 ? ok(new Uint8Array()) : encodeBinary(input.events);
  if (!events.ok) return events;
  if (events.value.byteLength > MAX_FRAME_SIZE_BYTES) return err({ _tag: "frame_too_large" });
  const badRoot = [input.stateRoot, input.authorityRoot].find((root) => !/^0x[0-9a-f]{64}$/.test(root));
  if (badRoot !== undefined) return err({ _tag: "frame_root", value: badRoot });
  const parts = all({ txs: entityTxDigest(input.txs), context: encodeBinary(input.entityContext) });
  return chain(parts, ({ txs, context }) => chain(encodeBinary(frameHeader(input, txs.digest, context)), (header) =>
    (header.byteLength + context.byteLength + txs.bytes > MAX_FRAME_SIZE_BYTES
      ? err({ _tag: "frame_too_large" })
      : ok(bytesToHex(keccak_256(header))))));
};

/** og getPrevFrameHash: height 0 links to the literal "genesis". */
const frameWord = (value: string): string => {
  switch (true) {
    case value === "genesis": return value;
    case value.startsWith("0x"): return value.toLowerCase();
    default: return `0x${value.toLowerCase()}`;
  }
};
const frameNumber = (value: bigint): Result<number, EntityFrameHashError> =>
  value >= 0n && value <= BigInt(Number.MAX_SAFE_INTEGER) ? ok(Number(value)) : err({ _tag: "frame_clock", value });
const isScalar = (value: unknown): value is null | boolean | bigint | number | string =>
  value === null || ["boolean", "bigint", "number", "string"].includes(typeof value);
/** A plain record (no class instance, typed array, Map or Set). */
const isPlainRecord = (value: unknown): value is Record<string, unknown> => {
  const opaque = typeof value !== "object" || value === null || ArrayBuffer.isView(value);
  if (opaque || value instanceof Map || value instanceof Set) return false;
  const proto = Object.getPrototypeOf(value);
  return proto === Object.prototype || proto === null;
};
/** og wire data as the binary codec takes it: scalars, arrays and plain records, undefined fields dropped. */
const binaryBody = (value: unknown): Result<Binary, BinaryError> => {
  if (isScalar(value)) return ok(value);
  if (Array.isArray(value)) return traverse(value, binaryBody);
  if (!isPlainRecord(value)) return err({ _tag: "binary" });
  const fields = traverse(Object.keys(value).filter((key) => value[key] !== undefined), (key) =>
    map(binaryBody(value[key]), (walked) => [key, walked] as const));
  return map(fields, (entries) => Object.fromEntries(entries) as Binary);
};
/** og wire: token ids are numbers inside entity tx data. */
const wireData = (tx: EntityTx): unknown => {
  switch (tx.type) {
    case "entityCommand": return { ...tx.data, txs: tx.data.txs.map(wireEntityTx) };
    case "propose": return { ...tx.data, action: wireAction(tx.data.action) };
    case "placeSwapOffer":
      return { ...tx.data, giveTokenId: Number(tx.data.giveTokenId), wantTokenId: Number(tx.data.wantTokenId) };
    case "requestCollateral": return {
      ...tx.data, tokenId: Number(tx.data.tokenId),
      ...(tx.data.feeTokenId === undefined ? {} : { feeTokenId: Number(tx.data.feeTokenId) }),
    };
    case "accountInput": return tx.data;
    default:
      return "tokenId" in tx.data && tx.data.tokenId !== undefined
        ? { ...tx.data, tokenId: Number(tx.data.tokenId) }
        : tx.data;
  }
};
/** og EntityTx wire object `{type, data}` (numeric token ids, nested command / proposal txs in wire form). */
export const wireEntityTx = (tx: EntityTx): { readonly type: string; readonly data: unknown } =>
  ({ type: tx.type, data: wireData(tx) });
const wireAction = (a: ProposalAction): unknown =>
  (a.type === "entity_transaction" ? { type: a.type, data: { ...a.data, txs: a.data.txs.map(wireEntityTx) } } : a);
/**
 * Inverse of wireData for the collective txs a stored proposal carries: token ids back to the rewrite's TokenId
 * strings.
 */
const unwireEntityTx = (w: { readonly type: string; readonly data: unknown }): EntityTx => {
  const data = w.data as Record<string, unknown>;
  const textId = (k: string) => (typeof data[k] === "number" ? { [k]: String(data[k]) } : {});
  return { type: w.type, data: { ...data, ...textId("tokenId"), ...textId("feeTokenId") } } as EntityTx;
};
const entityFrameTx = (tx: EntityTx): Result<EntityFrameTx, BinaryError> =>
  map(binaryBody(wireData(tx)), (data) => ({ type: tx.type, data }));
export const hashEntityFrame = (f: EntityFrame): Result<EntityFrameHash, EntityFrameHashError> => {
  const parts = all({
    height: frameNumber(f.height),
    timestamp: frameNumber(f.timestamp),
    txs: traverse(f.txs, entityFrameTx),
  });
  return chain(parts, ({ height, timestamp, txs }) => map(entityFrameHash({
    prevFrameHash: frameWord(f.prevFrameHash), height, timestamp, txs,
    events: f.events, entityId: f.entityContext.entityId,
    stateRoot: f.stateRoot, authorityRoot: f.authorityRoot, entityContext: f.entityContext,
    ...(f.jPrefixCertificate === undefined ? {} : { jPrefixCertificate: jpBinary(f.jPrefixCertificate) }),
  }), (digest) => digest as EntityFrameHash));
};
/** A J-prefix value as og's binary codec sees it (Maps kept, undefined fields absent). */
const jpBinary = (v: unknown): Binary => {
  if (v instanceof Map) return new Map([...v].map(([k, x]) => [k as Binary, jpBinary(x)]));
  if (Array.isArray(v)) return v.map(jpBinary);
  if (v === null || typeof v !== "object") return v as Binary;
  return Object.fromEntries(Object.entries(v).filter(([, x]) => x !== undefined).map(([k, x]) => [k, jpBinary(x)]));
};
/** og getEntityLeaderState without a view change: the CEO `validators[0]`. */
export const allowedProposer = (q: Quorum): Address => q.proposer;
const boardMembers = (board: Board): Members => new Map(board.entityIds.map((id, i) =>
  [checksum(`0x${id.slice(-40)}`) as Address, { shares: BigInt(board.votingPowers[i] ?? 0) }]));
const membersOf = (a: Authority): Members =>
  match(a, { teaching: (x) => x.members, board: (b) => boardMembers(b.board) });
const thresholdOf = (a: Authority): bigint =>
  match(a, { teaching: (x) => x.threshold, board: (b) => BigInt(b.board.votingThreshold) });
/** og normalizes every signer id with trim + lowercase (replica-validation.ts, certificates.ts). */
const memberId = (a: Authority, id: string): Address | undefined =>
  [...membersOf(a).keys()].find((m) => signerId(m) === signerId(id));
const sharesOf = (a: Authority, id: string): bigint => {
  const m = memberId(a, id);
  return m === undefined ? 0n : membersOf(a).get(m)?.shares ?? 0n;
};
/** og isSingleSignerBoard: exactly one validator whose share alone reaches the threshold. */
export const isSingleSigner = (a: Authority): boolean => {
  const [only, ...rest] = membersOf(a).keys();
  return only !== undefined && rest.length === 0 && sharesOf(a, only) >= thresholdOf(a);
};
/** A board authority names word ids, one safe voting power per member, and no two members on one address. */
const boardShaped = (a: Authority): boolean => match(a, {
  teaching: () => true,
  board: ({ board, entityId }) =>
    WORD.test(entityId) && Number.isSafeInteger(board.votingThreshold)
    && board.entityIds.length === board.votingPowers.length
    && board.entityIds.every((id) => WORD.test(id) && isAddressId(id))
    && board.votingPowers.every(Number.isSafeInteger)
    && boardMembers(board).size === board.entityIds.length,
});
const UINT16 = 0xffffn;
const inUint16 = (n: bigint): boolean => n >= 1n && n <= UINT16;
/**
 * og validateConsensusConfig: unique (case-insensitive) validators, uint16 threshold and shares, 1 <= threshold <=
 * total power.
 */
const admitQuorum = (a: Authority): Result<Quorum, EntityError> => {
  if (!boardShaped(a)) return err({ _tag: "bad_quorum" });
  const members = membersOf(a), [proposer] = members.keys();
  const ids = [...members.keys()].map(signerId), shares = [...members.values()].map((m) => m.shares);
  const threshold = thresholdOf(a), power = shares.reduce((sum, n) => sum + n, 0n);
  const valid = proposer !== undefined && ids.every((id) => id.length > 0) && new Set(ids).size === ids.length
    && inUint16(threshold) && shares.every(inUint16) && threshold <= power;
  return valid ? ok({ ...a, proposer }) : err({ _tag: "bad_quorum" });
};
const openEntity = (
  signer: Address, state: EntityState, head: Head, mempool: readonly EntityTx[],
  accountReplicas: ReadonlyMap<EntityId, AccountReplica>,
): OpenEntity => ({ _tag: "open", signerId: signer, state, head, mempool, accountReplicas });
type EntitySeed = {
  readonly id: EntityId;
  readonly jurisdiction: Domain;
  readonly signerId?: Address | undefined;
  readonly timestamp?: bigint | undefined;
  readonly jurisdictionConfig?: JurisdictionConfig | undefined;
  readonly committed?: EntityCommitted | undefined;
} & (TeachingQuorum | { readonly board: Board & { readonly entityId: string } });
const authorityOf = (p: EntitySeed): Authority => {
  if (!("board" in p)) return { _tag: "teaching", threshold: p.threshold, members: p.members };
  const { entityId, ...board } = p.board;
  return { _tag: "board", board, entityId };
};
/** One validator replica (og `eReplicas` key `entityId:signerId`); `signerId` defaults to the proposer. */
export const createEntity = (p: EntitySeed): Result<OpenEntity, EntityError> => {
  const parts = all({
    quorum: admitQuorum(authorityOf(p)),
    jurisdiction: mapErr(domainOf(p.jurisdiction), (): EntityError => ({ _tag: "bad_jurisdiction" })),
  });
  return chain(parts, ({ quorum, jurisdiction }): Result<OpenEntity, EntityError> => {
    const signer = p.signerId === undefined ? quorum.proposer : memberId(quorum, p.signerId);
    if (signer === undefined) return err({ _tag: "unknown_member", address: p.signerId ?? "" });
    const state: EntityState = {
      id: p.id, quorum, jurisdiction, accounts: new Map(), height: 0n, timestamp: p.timestamp ?? 0n,
      ...opt("jurisdictionConfig", p.jurisdictionConfig), committed: p.committed ?? {},
    };
    return ok(openEntity(signer, state, genesisHead(), [], new Map()));
  });
};
const memberSigned = (q: Quorum, h: string, sig: Signature, addr: Address, ctx: EntityContext): boolean => match(q, {
  teaching: () => ctx.verifyMember(h as Hash, sig, addr),
  board: () => sameHex(recoverRawSigner(h, sig) ?? undefined, addr),
});
/** og calculateQuorumPower over normalized signer ids. */
const quorumPower = (q: Quorum, signers: ReadonlyMap<string, unknown>): bigint =>
  [...signers.keys()].reduce((power, id) => power + sharesOf(q, id), 0n);
/** og normalizePrecommitBundles: trim+lowercase keys, validators only, no duplicate after normalization. */
const normalizeBundles = (q: Quorum, bundles: Precommits): Result<Precommits, EntityError> =>
  foldResult(bundles, new Map() as Precommits, (seen, [raw, sigs]): Result<Precommits, EntityError> => {
    const id = signerId(raw);
    switch (true) {
      case memberId(q, id) === undefined: return err({ _tag: "unknown_member", address: raw });
      case seen.has(id): return err({ _tag: "duplicate_member", address: raw });
      default: return ok(mapSet(seen, id, sigs));
    }
  });
/** og verifyHashPrecommitSignatures: one valid signature per manifest entry, in order. */
const bundleValid = (
  q: Quorum, hashes: readonly HashToSign[], id: string, sigs: readonly Signature[], ctx: EntityContext,
): boolean => {
  const addr = memberId(q, id);
  return addr !== undefined && hashes.length > 0 && sigs.length === hashes.length && hashes.every((h, i) => {
    const sig = sigs[i];
    return sig !== undefined && memberSigned(q, h.hash, sig, addr, ctx);
  });
};
/**
 * og signProposalManifest / signAndLockProposal: the board authority of the frame's state is asserted before any
 * manifest signature.
 */
const signManifest = (
  hashes: readonly HashToSign[], signer: Address, ctx: EntityContext, state: EntityState,
): Result<readonly Signature[], EntityError> => chain(assertBoardAuthority(state), () =>
  traverse(hashes, (h) => mapErr(ctx.sign(h.hash as Hash, signer), (): EntityError => ({ _tag: "sign_failed" }))));
const sameSigs = (a: readonly Signature[], b: readonly Signature[]): boolean =>
  a.length === b.length && a.every((sig, i) => sig === b[i]);

// ---- leader failover: og leader/{index,certificates,timeout-input}.ts ----
const LEADER_TIMEOUT_BASE_MS = 10_000, LEADER_TIMEOUT_MAX_MS = 60_000;
/** og getEntityLeaderOrder: the CEO `validators[0]`, then successors by shares descending, then position. */
export const leaderOrder = (q: Quorum): readonly string[] => {
  const validators = [...membersOf(q).keys()].map(signerId);
  const [ceo, ...successors] = validators;
  const heavierFirst = (l: string, r: string): number =>
    asc(sharesOf(q, r), sharesOf(q, l)) || validators.indexOf(l) - validators.indexOf(r) || asc(l, r);
  return ceo === undefined ? [] : [ceo, ...successors.toSorted(heavierFirst)];
};
/** og getEntityLeaderState: the committed leader, defaulting to the CEO at view 0. */
export const leaderStateOf = (state: EntityState): LeaderState => {
  const committed = state.leaderState;
  return {
    activeValidatorId: signerId(committed?.activeValidatorId ?? leaderOrder(state.quorum)[0] ?? ""),
    view: committed?.view ?? 0,
    changedAtHeight: committed?.changedAtHeight ?? 0,
  };
};
/** og getNextEntityFailoverLeader: rotate through the successors, never back to the CEO. */
export const nextFailoverLeader = (state: EntityState): string => {
  const [ceo = "", ...successors] = leaderOrder(state.quorum);
  if (successors.length === 0) return ceo;
  const active = successors.indexOf(leaderStateOf(state).activeValidatorId);
  return successors[(active + 1) % successors.length] ?? "";
};
/** og getEntityLeaderTimeoutMs. */
export const leaderTimeoutMs = (nextView: number): number =>
  Math.min(LEADER_TIMEOUT_MAX_MS, LEADER_TIMEOUT_BASE_MS * Math.max(1, Math.floor(nextView)));
const parentOf = (head: Head): string => (head.height === 0n ? GENESIS_PARENT : frameWord(head.prevFrameHash));
/** og buildEntityLeaderVoteBody over the committed state. */
export const leaderVoteBody = (state: EntityState, head: Head): LeaderVoteBody => {
  const leader = leaderStateOf(state);
  return {
    entityId: signerId(state.id),
    targetHeight: Number(head.height) + 1,
    previousFrameHash: parentOf(head),
    fromView: leader.view,
    toView: leader.view + 1,
    previousLeaderId: leader.activeValidatorId,
    nextLeaderId: nextFailoverLeader(state),
  };
};
const voteFields = (v: LeaderVoteBody): LeaderVoteBody => ({
  entityId: signerId(v.entityId),
  targetHeight: v.targetHeight,
  previousFrameHash: v.previousFrameHash,
  fromView: v.fromView,
  toView: v.toView,
  previousLeaderId: signerId(v.previousLeaderId),
  nextLeaderId: signerId(v.nextLeaderId),
});
/** og signatures are `0x`-prefixed lowercase hex; the rewrite's Signature is bare hex. Both parse. */
const ogSig = (s: string): string => (s.startsWith("0x") ? s.toLowerCase() : `0x${s}`);
const sigOf = (s: string): Signature | undefined =>
  unwrapOr(signature(s.startsWith("0x") ? s.slice(2).toLowerCase() : s), () => undefined);
const precommitsBinary = (sigs: Precommits): Binary => new Map([...sigs].map(([k, v]) => [k, v.map(ogSig)]));
const preparedVotesBinary = (votes: ReadonlyMap<string, LeaderVote>): Result<Binary, EntityError> =>
  map(traverse([...votes], ([k, v]) => map(voteBinary(v), (b) => [k, b] as const)), (rows) => new Map(rows));
const certificateBinary = (c: LeaderCertificate): Result<Binary, EntityError> =>
  map(whenDefined(c.preparedVotes, preparedVotesBinary), (preparedVotes): Binary => ({
    ...voteFields(c),
    votes: new Map([...c.votes].map(([k, s]) => [k, ogSig(s)])),
    ...opt("preparedVotes", preparedVotes),
    ...opt("preparedFrameHash", c.preparedFrameHash),
  }));
/** `evidence` is og buildPreparedFrameEvidence: the relay certificate stays out of prepared evidence. */
const leaderBinary = (l: FrameLeader, evidence: boolean): Result<Binary, EntityError> => {
  const parts = all({
    certificate: whenDefined(l.certificate, certificateBinary),
    relayCertificate: whenDefined(evidence ? undefined : l.relayCertificate, certificateBinary),
  });
  return map(parts, ({ certificate, relayCertificate }): Binary => ({
    proposerSignerId: l.proposerSignerId,
    view: l.view,
    ...opt("certificate", certificate),
    ...opt("relayCertificate", relayCertificate),
  }));
};
/**
 * og EntityFrame wire shape (height, parentFrameHash, roots, txs, hash, leader, manifest, collectedSigs); `evidence`
 * is og buildPreparedFrameEvidence (no relay certificate, no hankos).
 */
const frameBinary = (
  f: EntityFrame, signatures: Precommits | undefined, evidence: boolean,
): Result<Binary, EntityError> => {
  const parts = all({
    hash: hashEntityFrame(f),
    txs: traverse(f.txs, entityFrameTx),
    height: frameNumber(f.height),
    timestamp: frameNumber(f.timestamp),
    leader: leaderBinary(f.leader, evidence),
  });
  return map(parts, ({ hash, txs, height, timestamp, leader }): Binary => ({
    height,
    parentFrameHash: f.prevFrameHash,
    stateRoot: f.stateRoot,
    authorityRoot: f.authorityRoot,
    timestamp,
    entityContext: f.entityContext as unknown as Binary,
    txs: txs as unknown as Binary,
    events: f.events,
    hash,
    leader,
    hashesToSign: f.hashesToSign as unknown as Binary,
    ...(signatures === undefined ? {} : { collectedSigs: precommitsBinary(signatures) }),
    ...(f.jPrefixCertificate === undefined ? {} : { jPrefixCertificate: jpBinary(f.jPrefixCertificate) }),
  }));
};
const preparedBinary = (pf: PreparedFrame | undefined): Result<Binary | undefined, EntityError> =>
  whenDefined(pf, (p) => frameBinary(p.frame, p.signatures, true));
const voteBinary = (v: LeaderVote): Result<Binary, EntityError> =>
  map(preparedBinary(v.preparedFrame), (preparedFrame): Binary => ({
    ...voteFields(v),
    voterId: signerId(v.voterId),
    signature: ogSig(v.signature),
    ...opt("preparedFrame", preparedFrame),
  }));
const bytesKeccak = (value: Binary): Result<string, EntityError> =>
  map(encodeConsensus(value), (bytes) => bytesToHex(keccak_256(bytes)));
const LEADER_TIMEOUT_DOMAIN = "xln.entity.leader-timeout.v1";
type TimeoutVote = LeaderVoteBody & { readonly preparedFrame?: PreparedFrame | undefined };
/** og hashEntityLeaderVoteBody: domain, normalized body fields, prepared evidence or null. */
export const hashLeaderVote = (v: TimeoutVote): Result<string, EntityError> =>
  chain(preparedBinary(v.preparedFrame), (preparedFrame) =>
    bytesKeccak({ domain: LEADER_TIMEOUT_DOMAIN, ...voteFields(v), preparedFrame: preparedFrame ?? null }));
/** The view change a vote is about, without its evidence: every vote for one change shares it. */
const collectionKey = (v: LeaderVoteBody): Result<string, EntityError> => hashLeaderVote(voteFields(v));
const voteMatchesState = (state: EntityState, head: Head, v: LeaderVoteBody): boolean =>
  canon(voteFields(v)) === canon(leaderVoteBody(state, head));
/**
 * og buildEntityLeaderCertificate: votes sorted by normalized signer; per-voter votes only when some carried
 * prepared evidence.
 */
export const buildLeaderCertificate = (
  body: LeaderVoteBody, votes: ReadonlyMap<string, LeaderVote>,
): LeaderCertificate => {
  const rows = [...votes].map(([k, v]) => [signerId(k), v] as const).toSorted(([a], [b]) => asc(a, b));
  const evidenced = rows.some(([, v]) => v.preparedFrame !== undefined);
  const normalized = (v: LeaderVote): LeaderVote => ({
    ...voteFields(v),
    voterId: signerId(v.voterId),
    signature: v.signature,
    ...opt("preparedFrame", v.preparedFrame),
  });
  return {
    ...voteFields(body),
    votes: new Map(rows.map(([k, v]) => [k, v.signature])),
    ...(evidenced ? { preparedVotes: new Map(rows.map(([k, v]) => [k, normalized(v)])) } : {}),
  };
};
const LEADER_VOTE_INVALID: EntityError = { _tag: "leader_vote_invalid" };
/** Rows re-keyed by normalized signer; two keys naming one signer, or a row `admit` refuses, void the whole map. */
const bySigner = <V>(
  rows: ReadonlyMap<string, V>, admit: (id: string, v: V) => boolean,
): Result<ReadonlyMap<string, V>, EntityError> =>
  foldResult(rows, new Map() as ReadonlyMap<string, V>, (seen, [raw, v]) => {
    const id = signerId(raw);
    return seen.has(id) || !admit(id, v) ? err(LEADER_VOTE_INVALID) : ok(mapSet(seen, id, v));
  });
/** og getCertificateSignedVotes: compact signatures, or prepared votes matching them one for one. */
const certificateVotes = (c: LeaderCertificate): Result<ReadonlyMap<string, LeaderVote>, EntityError> =>
  chain(bySigner(c.votes, () => true), (signatures): Result<ReadonlyMap<string, LeaderVote>, EntityError> => {
    if (c.preparedVotes === undefined) {
      const bare = (id: string, signature: string): LeaderVote => ({ ...voteFields(c), voterId: id, signature });
      return ok(new Map([...signatures].map(([id, signature]) => [id, bare(id, signature)])));
    }
    const matching = (id: string, vote: LeaderVote): boolean =>
      signerId(vote.voterId) === id && signatures.get(id) === vote.signature;
    return chain(bySigner(c.preparedVotes, matching), (prepared) =>
      (prepared.size === signatures.size ? ok(prepared) : err(LEADER_VOTE_INVALID)));
  });
type LeaderView = { readonly state: EntityState; readonly head: Head };
/** A timeout vote counts when a member signed exactly the view change the certificate is keyed by. */
const timeoutVoteValid = (q: Quorum, key: string, id: string, vote: LeaderVote, ctx: EntityContext): boolean => {
  const addr = memberId(q, id), sig = sigOf(vote.signature), h = hashLeaderVote(vote), k = collectionKey(vote);
  return addr !== undefined && sig !== undefined && h.ok && k.ok
    && signerId(vote.voterId) === id && k.value === key
    && memberSigned(q, h.value, sig, addr, ctx);
};
const certifiedQuorum = (q: Quorum, c: LeaderCertificate, ctx: EntityContext): boolean => {
  const signed = certificateVotes(c), key = collectionKey(c);
  if (!signed.ok || !key.ok) return false;
  const valid = [...signed.value].every(([id, vote]) => timeoutVoteValid(q, key.value, id, vote, ctx));
  return valid && quorumPower(q, signed.value) >= thresholdOf(q);
};
/**
 * og verifyEntityLeaderCertificate: no certificate means the committed leader at the committed view; otherwise a
 * quorum of signed timeout votes for this exact view change.
 */
const verifyLeaderCertificate = (at: LeaderView, leader: FrameLeader, ctx: EntityContext): boolean => {
  const committed = leaderStateOf(at.state), proposed = signerId(leader.proposerSignerId), c = leader.certificate;
  if (c === undefined) return proposed === committed.activeValidatorId && leader.view === committed.view;
  return voteMatchesState(at.state, at.head, c) && proposed === signerId(c.nextLeaderId) && leader.view === c.toView
    && certifiedQuorum(at.state.quorum, c, ctx);
};
const PREPARED_REJECTED: EntityError = { _tag: "leader_prepared_rejected" };
/** og hasVerifiedPreparedQuorum: a canonical manifest, every bundle valid (else refused), at least the threshold. */
const preparedQuorum = (
  q: Quorum, frame: EntityFrame, sigs: Precommits, ctx: EntityContext,
): Result<boolean, EntityError> => chain(hashEntityFrame(frame), (hash) => {
  const [head] = frame.hashesToSign;
  if (head === undefined || head.type !== "entityFrame" || head.hash !== hash) return err(PREPARED_REJECTED);
  return chain(normalizeBundles(q, sigs), (bundles) => {
    const valid = [...bundles].every(([id, s]) => bundleValid(q, frame.hashesToSign, id, s, ctx));
    return valid ? ok(quorumPower(q, bundles) >= thresholdOf(q)) : err(PREPARED_REJECTED);
  });
});
/** og validatePreparedFrameEvidence: the next frame on our head, proposed by a certified leader, never relayed. */
const preparedEvidence = (
  at: LeaderView, c: LeaderCertificate, pf: PreparedFrame, ctx: EntityContext,
): Result<Precommits, EntityError> => {
  const f = pf.frame;
  const admissible = f.height === BigInt(c.targetHeight) && f.prevFrameHash === parentOf(at.head)
    && f.leader.relayCertificate === undefined && verifyLeaderCertificate(at, f.leader, ctx);
  if (!admissible) return err(PREPARED_REJECTED);
  const q = at.state.quorum;
  return chain(preparedQuorum(q, f, pf.signatures, ctx), () => normalizeBundles(q, pf.signatures));
};
type PreparedGroup = { readonly frame: EntityFrame; readonly body: string; readonly signatures: Precommits };
type PreparedGroups = ReadonlyMap<string, PreparedGroup>;
const evidenceBody = (f: EntityFrame): Result<string, EntityError> =>
  chain(frameBinary(f, undefined, true), (body) => map(encodeConsensus(body), bytesToHex));
/** Evidence joins its frame hash's group: the same encoded body, and no signer changing its bundle. */
const joinGroup = (
  groups: PreparedGroups, hash: string, fresh: PreparedGroup,
): Result<PreparedGroups, EntityError> => {
  const group = groups.get(hash) ?? { ...fresh, signatures: new Map() };
  if (group.body !== fresh.body) return err(PREPARED_REJECTED);
  const merged = foldResult(fresh.signatures, group.signatures, (held, [id, s]) => {
    const prior = held.get(id);
    return prior !== undefined && !sameSigs(prior, s) ? err(PREPARED_REJECTED) : ok(mapSet(held, id, s));
  });
  return map(merged, (signatures) => mapSet(groups, hash, { ...group, signatures }));
};
const groupEvidence = (at: LeaderView, c: LeaderCertificate, ctx: EntityContext) =>
  (groups: PreparedGroups, pf: PreparedFrame): Result<PreparedGroups, EntityError> =>
    chain(preparedEvidence(at, c, pf, ctx), (signatures) =>
      chain(all({ hash: hashEntityFrame(pf.frame), body: evidenceBody(pf.frame) }), ({ hash, body }) =>
        joinGroup(groups, hash, { frame: pf.frame, body, signatures })));
/** Among the quorum groups, the one at the highest view; a tie at that view is refused. */
const highestPrepared = (q: Quorum, groups: PreparedGroups): Result<PreparedFrame | null, EntityError> => {
  const prepared = [...groups.values()].filter((g) => quorumPower(q, g.signatures) >= thresholdOf(q));
  if (prepared.length === 0) return ok(null);
  const view = Math.max(...prepared.map((g) => g.frame.leader.view));
  const [only, ...rivals] = prepared.filter((g) => g.frame.leader.view === view);
  if (only === undefined || rivals.length > 0) return err(PREPARED_REJECTED);
  return ok({ frame: only.frame, signatures: new Map([...only.signatures].toSorted(([a], [b]) => asc(a, b))) });
};
/**
 * og selectPreparedFrameFromCertificate: group valid evidence by frame hash, keep quorum groups, the highest view
 * must be unique.
 */
const selectPrepared = (
  at: LeaderView, c: LeaderCertificate, ctx: EntityContext,
): Result<PreparedFrame | null, EntityError> =>
  chain(certificateVotes(c), (votes) => {
    const evidence = [...votes.values()].flatMap((v) => v.preparedFrame ?? []);
    const grouped = foldResult(evidence, new Map() as PreparedGroups, groupEvidence(at, c, ctx));
    return chain(grouped, (groups) => highestPrepared(at.state.quorum, groups));
  });
/** og verifyEntityRelayCertificate: a relayed frame is exactly the prepared frame its relay certificate selects. */
const verifyRelayCertificate = (at: LeaderView, frame: EntityFrame, frameHash: string, ctx: EntityContext): boolean => {
  const relay = frame.leader.relayCertificate;
  if (relay === undefined) return true;
  const relayLeader = { proposerSignerId: relay.nextLeaderId, view: relay.toView, certificate: relay };
  if (!verifyLeaderCertificate(at, relayLeader, ctx)) return false;
  const selected = selectPrepared(at, relay, ctx);
  return selected.ok && selected.value !== null && relay.preparedFrameHash === frameHash
    && unwrapOr(hashEntityFrame(selected.value.frame), () => "") === frameHash;
};
/** The leader a certificate installs at `height`. */
const certifiedLeader = (c: LeaderCertificate, height: number): LeaderState =>
  ({ activeValidatorId: signerId(c.nextLeaderId), view: c.toView, changedAtHeight: height });
/** og expectedCommittedLeaderState: a certified view change installs the next leader at this height. */
const committedLeaderFor = (state: EntityState, frame: EntityFrame): LeaderState => {
  const c = frame.leader.certificate;
  return c === undefined ? leaderStateOf(state) : certifiedLeader(c, Number(frame.height));
};
/** og getReplicaProposalLeader: a pending certificate for the next height names the leader, else the committed one. */
export const proposalLeader = (r: EntityEnv): LeaderState => {
  const c = r.pendingLeaderCertificate;
  const forNextHeight = c !== undefined && c.targetHeight === Number(r.head.height) + 1;
  return forNextHeight ? certifiedLeader(c, c.targetHeight) : leaderStateOf(r.state);
};
const isProposalLeader = (r: EntityEnv): boolean => proposalLeader(r).activeValidatorId === signerId(r.signerId);
export const isActiveLeader = (r: EntityReplica): boolean =>
  leaderStateOf(r.state).activeValidatorId === signerId(r.signerId);
/**
 * og hasEntityLeaderWork (without J-prefix rounds): queued txs, a frame in flight, a pending certificate or queued
 * Account work.
 */
export const hasLeaderWork = (r: EntityReplica, jHistory?: ValidatorJHistory): boolean =>
  r.mempool.length > 0
  || r._tag !== "open"
  || r.pendingLeaderCertificate !== undefined
  || jPrefixLeaderWork(r, jHistory)
  || hasProposableAccount(r);
/** A follower with leader work owes a timeout vote; the leader, or an idle Entity, owes nothing. */
const awaitsLeader = (r: EntityReplica, jHistory?: ValidatorJHistory): boolean =>
  !isActiveLeader(r) && hasLeaderWork(r, jHistory);
/**
 * og nextReplicaDeadline (non-leader branch): when this validator's timeout vote falls due, given its last consensus
 * progress.
 */
export const leaderTimeoutDue = (
  r: EntityReplica, lastProgressAt: bigint, jHistory?: ValidatorJHistory,
): bigint | undefined => {
  if (!awaitsLeader(r, jHistory)) return undefined;
  const body = leaderVoteBody(r.state, r.head), mine = r.leaderVotes?.get(signerId(r.signerId));
  const alreadyVoted = mine !== undefined && canon(voteFields(mine)) === canon(voteFields(body));
  return alreadyVoted ? undefined : lastProgressAt + BigInt(leaderTimeoutMs(body.toView));
};
/**
 * og createDueScheduledWakeInputs (non-leader branch): the local unsigned vote carrying this replica's lock as
 * prepared evidence.
 */
export const localTimeoutVote = (
  r: EntityReplica, timestamp: bigint, jHistory?: ValidatorJHistory,
): EntityInput | undefined => {
  if (!awaitsLeader(r, jHistory)) return undefined;
  const lock = r._tag === "locked" ? { frame: r.frame, signatures: r.signatures } : undefined;
  const vote = {
    ...leaderVoteBody(r.state, r.head),
    voterId: signerId(r.signerId),
    signature: "",
    ...opt("preparedFrame", lock),
  };
  return { kind: "leaderTimeoutVote", timestamp, local: true, vote };
};

type FoldContext = {
  readonly verify: Verify;
  readonly timestamp: bigint;
  readonly htlc?: HtlcFrameInfra | undefined;
  /** og EntityRuntimeContext.activeJurisdiction (the Runtime's first imported J), the Htlc* jurisdictionId fallback. */
  readonly activeJurisdiction?: string | undefined;
  readonly boardHandover?: HandoverConfig | undefined;
  readonly siblings?: SiblingReplicas | undefined;
  readonly boardAuthority?: BoardAuthority | undefined;
  readonly jReplicas?: ReadonlyMap<string, JReplica> | undefined;
  readonly runtimeSeed?: string | undefined;
};
/**
 * og env.state.eReplicas as a cross-j opening reads it: the live sibling replica of this Runtime by (Entity, signer),
 * both normalized.
 */
export type SiblingReplicas = (entityId: string, signerId: string) => EntityReplica | undefined;
/** og requireAccountDeltaTransformerAddress(env, account.state) for one of this Entity's Accounts. */
const accountDt = (
  ctx: { readonly jReplicas?: ReadonlyMap<string, JReplica> | undefined }, child: AccountReplica,
): DeltaTransformerRef => deltaTransformerFor(ctx.jReplicas, child.state.terms.domain);
type Replicas = ReadonlyMap<EntityId, AccountReplica>;
/** Who the tx is about: og routes accountInput by its envelope, the rest by an explicit counterparty. */
const peerOf = (tx: EntityTx, self: EntityId): EntityId => {
  const own = () => self;
  const counterparty = (x: { readonly data: { readonly counterpartyEntityId: EntityId } }) =>
    x.data.counterpartyEntityId;
  const hub = (x: { readonly data: { readonly hubEntityId: string } }) => lower(x.data.hubEntityId) as EntityId;
  return matchBy("type", tx, {
    openAccount: (x) => x.data.targetEntityId,
    accountInput: (x) => (namesEntity(x.data.fromEntityId, self) ? x.data.toEntityId : x.data.fromEntityId),
    extendCredit: counterparty,
    directPayment: (x) => x.data.route[1] ?? x.data.targetEntityId,
    htlcPayment: (x) => lower(x.data.route[1] ?? x.data.targetEntityId) as EntityId,
    requestCollateral: counterparty,
    placeSwapOffer: counterparty,
    proposeCancelSwap: counterparty,
    setRebalancePolicy: counterparty,
    prepareDispute: counterparty,
    disputeStart: counterparty,
    disputeFinalize: counterparty,
    settle_propose: counterparty,
    settle_update: counterparty,
    settle_approve: counterparty,
    settle_execute: counterparty,
    settle_reject: counterparty,
    lendingOffer: hub,
    lendingBorrow: hub,
    lendingRepay: hub,
    lendingClosePosition: hub,
    setHubConfig: own,
    scheduledWake: own,
    processHtlcTimeouts: own,
    chat: own,
    chatMessage: own,
    "profile-update": own,
    entityCommand: own,
    propose: own,
    vote: own,
    entityProviderTransfer: own,
    entityProviderReleaseControlShares: own,
    entityProviderCancelAction: own,
    entityProviderProposeControlBoard: own,
    entityProviderActivateBoard: own,
    prepareCrossJurisdictionSwap: own,
    materializeCrossJurisdictionSwap: own,
    registerCrossJurisdictionSwap: own,
    admitCrossJurisdictionBookOrder: own,
    removeCrossJurisdictionBookOrder: own,
    crossJurisdictionBookOrderRemoved: own,
    crossJurisdictionFillNotice: own,
    requestCrossJurisdictionClear: own,
    materializeCrossJurisdictionClear: own,
    crossPullClose: own,
    crossJurisdictionSalvage: own,
    crossJurisdictionForceSiblingDispute: own,
    resolveHtlcLock: own,
    orderbookSweepCrossJurisdiction: own,
    runtimeOutput: own,
    proposeAccountsNow: own,
    initOrderbookExt: own,
    r2r: own,
    r2e: own,
    r2c: own,
    e2r: own,
    j_broadcast: own,
    j_rebroadcast: own,
    j_abort_sent_batch: own,
    j_clear_batch: own,
    mintReserves: own,
    j_event: own,
    boardHandover: own,
  });
};
/** A peer's Account message names its sender in its envelope; everything else is this entity's own command. */
const originOf = (tx: EntityTx, self: EntityId): Delivery =>
  (tx.type === "accountInput" && !namesEntity(tx.data.fromEntityId, self)
    ? { _tag: "received", from: tx.data.fromEntityId }
    : { _tag: "local" });
const putChild = (state: EntityState, replicas: Replicas, peer: EntityId, child: AccountReplica): Folded => ({
  state: { ...state, accounts: mapSet(state.accounts, peer, child.state.account) },
  accountReplicas: mapSet(replicas, peer, child),
});
const withChild = (
  replicas: Replicas, target: EntityId, f: (child: AccountReplica) => Result<Draft, EntityError>,
): Result<Draft, EntityError> => {
  const child = replicas.get(target);
  return child === undefined ? err({ _tag: "no_such_account", target }) : f(child);
};
/**
 * Account outputs the Entity consumes: the gateway forward, HTLC failures and preimages (htlcFollowups, from the
 * committed frames), og's collateral-request runtime event, and the swap cancel outcomes og hands to the hub book (an
 * Entity without an order book ignores them).
 */
const ENTITY_CONSUMED_EFFECTS: ReadonlySet<Effect["_tag"]> = new Set([
  "direct_payment_forward",
  "htlc_error",
  "forward_secret",
  "request_collateral_committed",
  "account_settled_finalized_bilateral",
  "swap_cancel_requested",
  "swap_cancelled",
  "swap_offer_upsert",
]);
/**
 * og committed-input.ts applySameJurisdictionSwapOutput: the committed frames' swap outputs, keyed by this Account,
 * for the frame's book phase.
 */
const swapEventsOf = (accountId: EntityId, effects: readonly Effect[]): SwapEvents | undefined => {
  const created = effects.flatMap((e) => (e._tag === "swap_offer_upsert" ? [swapOfferEvent(accountId, e)] : []));
  const cancelled = effects.flatMap((e) => (e._tag === "swap_cancelled" ? [{ offerId: e.offerId, accountId }] : []));
  const cancelRequests = effects.flatMap((e) =>
    (e._tag === "swap_cancel_requested" ? [{ offerId: e.offerId, accountId }] : []));
  const quiet = created.length + cancelled.length + cancelRequests.length === 0;
  return quiet ? undefined : { created, cancelled, cancelRequests };
};
/**
 * og Account candidateEffects `runtimeEvent`, from this Entity's side of the Account: applyCollateralRequest
 * (tx/mutation.ts) and handleJEventClaim (j-events/claim.ts).
 */
export const accountRuntimeEvents = (entityId: string, accountId: string, e: Effect): readonly EntityRuntimeEvent[] => {
  switch (e._tag) {
    case "request_collateral_committed": return [{
      eventName: e._tag,
      data: {
        entityId,
        accountId,
        tokenId: e.tokenId,
        requestedAmount: e.requestedAmount.toString(),
        prepaidFee: e.prepaidFee.toString(),
        requestedAt: e.requestedAt,
      },
    }];
    case "account_settled_finalized_bilateral": return [{
      eventName: e._tag,
      data: {
        entityId,
        accountId,
        tokenId: e.tokenId,
        jHeight: e.jHeight,
        collateral: e.collateral.toString(),
        ondelta: e.ondelta.toString(),
      },
    }];
    default: return [];
  }
};
/** What of an Account output goes to the peer: its acks; consumed effects and local notes stay with the Entity. */
const wireMessages = (o: AccountOutput): Result<readonly AccountMessage[], EntityError> => matchBy("kind", o, {
  effect: (e) => (ENTITY_CONSUMED_EFFECTS.has(e.effect._tag) ? ok([]) : err({ _tag: "not_l0" })),
  ack: (m) => ok([m]),
  ack_frame: (m) => ok([m]),
  start_dispute: () => ok([]),
  message: () => ok([]),
});
/**
 * An applied Account input before its committed-frame followups: the Entity draft and the Account's consumed effects,
 * in commit order.
 */
type Routed = { readonly draft: Draft; readonly effects: readonly Effect[] };
const routedRaw = (
  state: EntityState, replicas: Replicas, target: EntityId, applied: Result<AccountApply, AccountReplicaError>,
): Result<Routed, EntityError> => chain(applied, (a) => map(traverse(a.outputs, wireMessages), (messages): Routed => {
  const effects = a.outputs.flatMap((o) => (o.kind === "effect" ? [o.effect] : []));
  const runtimeEvents = effects.flatMap((e) => accountRuntimeEvents(state.id, target, e));
  // og committed-input.ts / application.ts addMessages: the Account's status lines become certified Entity frame events
  const said = a.outputs.flatMap((o) => (o.kind === "message" ? [status(o.message)] : []));
  const outputs = messages.flat().map((data): EntityOutput => ({ to: target, tx: { type: "accountInput", data } }));
  const draft: Draft = {
    ...putChild(state, replicas, target, a.replica),
    outputs,
    ...(runtimeEvents.length === 0 ? {} : { runtimeEvents }),
    ...(said.length === 0 ? {} : { events: said }),
  };
  return { draft, effects };
}));
type DirectForward = Of<Effect, "direct_payment_forward">;
const routed = (
  state: EntityState, replicas: Replicas, target: EntityId, applied: Result<AccountApply, AccountReplicaError>,
): Result<Draft, EntityError> => chain(routedRaw(state, replicas, target, applied), ({ draft, effects }) => {
  const forwards = effects.flatMap((e) =>
    (e._tag === "direct_payment_forward" && sameHex(e.route[0], state.id) ? [e] : []));
  const swaps = swapEventsOf(target, effects);
  const forwarded = foldResult<Draft, DirectForward, EntityError>(forwards, draft, forwardPayment);
  return map(forwarded, (d) => (swaps === undefined ? d : { ...d, swaps }));
});
/**
 * og applyDirectPaymentForwardFollowups: the gateway's next leg on its Account with route[1] (a missing Account
 * refuses the input).
 */
const forwardLeg = (d: Folded, f: DirectForward): Result<AccountTxTarget, EntityError> => {
  const next = f.route[1] as EntityId | undefined;
  if (next === undefined || !d.accountReplicas.has(next)) {
    return err({ _tag: "no_such_account", target: (next ?? "") as EntityId });
  }
  return ok({
    accountId: next,
    tx: {
      type: "payment",
      tokenId: String(f.tokenId) as TokenId,
      amount: f.amount,
      route: f.route.slice(1),
      description: f.description || "Forwarded payment",
      fromEntityId: d.state.id,
      toEntityId: next,
      deliveryMode: "trusted",
      trustedGatewayEntityId: f.trustedGatewayEntityId,
    },
  });
};
const forwardPayment = (d: Draft, f: DirectForward): Result<Draft, EntityError> =>
  chain(forwardLeg(d, f), ({ accountId, tx }) => withChild(d.accountReplicas, accountId as EntityId, (child) =>
    map(admitAt(child, [tx], d.state.id, L0_CLOCK), (admitted) =>
      ({ ...putChild(d.state, d.accountReplicas, accountId as EntityId, admitted), outputs: d.outputs }))));
const L0_CLOCK = { timestamp: 0n, jHeight: 0n } as const;
type InitOrderbook = Extract<EntityTx, { readonly type: "initOrderbookExt" }>["data"];
const LOWER_WORD = /^0x[0-9a-f]{64}$/;
/**
 * og handleInitOrderbookExtEntityTx: an existing extension and a spread that does not sum to 10000 bps are silent
 * no-ops; a USD quote authority that is not a lowercase 32-byte id halts (ORDERBOOK_USD_QUOTE_AUTHORITY_INVALID).
 */
const initOrderbookExt = (state: EntityState, d: InitOrderbook): Result<OrderbookExt | undefined, EntityError> => {
  const s = d.spreadDistribution;
  const spreadBps = s.makerBps + s.takerBps + s.hubBps + s.makerReferrerBps + s.takerReferrerBps;
  const usdQuoteAuthorityEntityId = String(d.usdQuoteAuthorityEntityId || "").toLowerCase();
  switch (true) {
    case state.orderbookExt !== undefined: return ok(state.orderbookExt);
    case spreadBps !== 10_000: return ok(undefined);
    case !LOWER_WORD.test(usdQuoteAuthorityEntityId):
      return invariant(`ORDERBOOK_USD_QUOTE_AUTHORITY_INVALID:${usdQuoteAuthorityEntityId}`);
    default: return ok({
      books: new Map(),
      pairDimensions: new Map(),
      referrals: new Map(),
      hubProfile: {
        entityId: state.id,
        name: d.name,
        spreadDistribution: s,
        referenceTokenId: d.referenceTokenId,
        usdQuoteAuthorityEntityId,
        minTradeSize: d.minTradeSize,
        supportedPairs: [...d.supportedPairs],
      },
    });
  }
};
export const MAX_PROPOSE_ACCOUNTS_NOW_COUNTERPARTIES = 1_000;
type ProposeAccountsNow = Extract<EntityTx, { readonly type: "proposeAccountsNow" }>["data"];
const counterpartyIssue = (cp: unknown, prev: unknown): Result<void, EntityError> => {
  switch (true) {
    case typeof cp !== "string" || cp.length === 0 || cp.length > 256 || cp !== cp.toLowerCase():
      return invariant(`PROPOSE_ACCOUNTS_NOW_COUNTERPARTY_INVALID:${String(cp)}`);
    case typeof prev === "string" && asc(prev, cp as string) >= 0:
      return invariant(`PROPOSE_ACCOUNTS_NOW_ORDER_INVALID:${prev}:${cp}`);
    default: return ok(undefined);
  }
};
/**
 * og assertProposeAccountsNowMatchesState: the marker's signer is the active leader, version 1, 1..1000 lowercase
 * ids of at most 256 chars, strictly ascending. Plain Errors: the whole input is refused.
 */
const proposeAccountsNowOk = (state: EntityState, d: ProposeAccountsNow): Result<void, EntityError> => {
  const cps: unknown = d.counterparties;
  const leader = leaderStateOf(state).activeValidatorId.toLowerCase();
  if (typeof d.proposerSignerId !== "string" || leader !== d.proposerSignerId.toLowerCase()) {
    return invariant("PROPOSE_ACCOUNTS_NOW_PROPOSER_MISMATCH");
  }
  if (!Array.isArray(cps)) return invariant("PROPOSE_ACCOUNTS_NOW_INVALID_PAYLOAD:not-array");
  if (d.version !== 1 || cps.length === 0 || cps.length > MAX_PROPOSE_ACCOUNTS_NOW_COUNTERPARTIES) {
    return invariant(`PROPOSE_ACCOUNTS_NOW_INVALID_PAYLOAD:${cps.length}`);
  }
  return foldResult(cps, undefined as void, (_, cp: unknown, i) => counterpartyIssue(cp, cps[i - 1]));
};
/**
 * Peer Account txs an Entity takes into a received frame beyond L0: the HTLC and swap flows, the collateral request,
 * og lending (the hub's committed lending followup consumes them, committed-lending-followup.ts) and the rebalance
 * policy a hub proposes on an inbound Account right after genesis (og queueInitialHubPolicies).
 */
const ENTITY_PEER_TX_TYPES: ReadonlySet<WireAccountTx["type"]> = new Set([
  "htlc_lock",
  "htlc_resolve",
  "request_collateral",
  "swap_offer",
  "swap_cancel_request",
  "swap_resolve",
  "cross_pull_lock",
  "cross_pull_close",
  "lending_fund",
  "lending_borrow_request",
  "lending_repay",
  "lending_credit",
  "lending_close_request",
  "lending_close_payout",
  "rebalance_policy",
]);
const entityAcceptsPeerTx = (tx: WireAccountTx): boolean => isL0Tx(tx) || ENTITY_PEER_TX_TYPES.has(tx.type);
/** og DEFAULT_ACCOUNT_TOKEN_IDS (account/config/defaults.ts). */
const DEFAULT_ACCOUNT_TOKEN_IDS = ["1", "3", "2"] as const;
/**
 * og TOKEN_REGISTRY decimals (account/utils.ts over DEFAULT_TOKENS + TRON_ONLY_DEFAULT_TOKENS): USDC, WETH, USDT, TRX,
 * SUN.
 */
const TOKEN_DECIMALS: ReadonlyMap<number, number> = new Map([[1, 6], [2, 18], [3, 6], [4, 6], [5, 18]]);
/** og getTokenInfo(tokenId).decimals: an unknown token is a plain Error (TOKEN_METADATA_UNAVAILABLE). */
const tokenDecimals = (tokenId: number): Result<bigint, EntityError> => {
  const decimals = TOKEN_DECIMALS.get(tokenId);
  return decimals === undefined ? invariant(`TOKEN_METADATA_UNAVAILABLE:${String(tokenId)}`) : ok(BigInt(decimals));
};
/**
 * og resolveJurisdictionRebalanceDefaults: 500 / 10_000 / 15 whole tokens, or the jurisdiction's whole-USD policy
 * scaled to the token's decimals.
 */
export const rebalanceDefaults = (
  j: JurisdictionConfig | undefined, tokenId: number,
): Result<RebalancePolicy, EntityError> => {
  const usd = j?.rebalancePolicyUsd;
  if (usd === undefined) {
    return map(tokenDecimals(tokenId), (d) => ({
      r2cRequestSoftLimit: 500n * 10n ** d,
      hardLimit: 10_000n * 10n ** d,
      maxAcceptableFee: 15n * 10n ** d,
    }));
  }
  // og scaleWholePolicyAmount: the value is checked before the token's decimals are read
  const scale = (v: number): Result<bigint, EntityError> => (!Number.isFinite(v) || v < 0
    ? invariant(`REBALANCE_POLICY_USD_INVALID:token=${tokenId}:value=${String(v)}`)
    : map(tokenDecimals(tokenId), (d) => BigInt(Math.floor(v)) * 10n ** d));
  const scaled = all({ soft: scale(usd.r2cRequestSoftLimit), hard: scale(usd.hardLimit), fee: scale(usd.maxFee) });
  return chain(scaled, ({ soft, hard, fee }): Result<RebalancePolicy, EntityError> => (soft <= 0n || hard < soft
    ? invariant(`REBALANCE_POLICY_USD_INVALID:token=${tokenId}`)
    : ok({ r2cRequestSoftLimit: soft, hardLimit: hard, maxAcceptableFee: fee })));
};
/** og getDefaultRebalanceBaseFeeForToken: 0.1 of the token's whole unit. */
export const defaultRebalanceBaseFee = (tokenId: number): Result<bigint, EntityError> =>
  chain(tokenDecimals(tokenId), (d) =>
    (d === 0n ? invariant("TOKEN_AMOUNT_PRECISION_UNREPRESENTABLE:0.1:0") : ok(10n ** (d - 1n))));
/** og HubRebalanceConfig as committed in EntityState.hubRebalanceConfig. */
type HubConfig = {
  readonly hubName?: string;
  readonly matchingStrategy: string;
  readonly policyVersion: number;
  readonly routingFeePPM: number;
  readonly baseFee: bigint;
  readonly swapTakerFeeBps: number;
  readonly disputeAutoFinalizeMode: string;
  readonly minCollateralThreshold: bigint;
  readonly rebalanceLiquidityFeeBps: bigint;
  readonly rebalanceTimeoutMs: number;
};
const hubConfigOf = (state: EntityState): HubConfig | undefined =>
  state.committed["hubRebalanceConfig"] as HubConfig | undefined;
/** og buildHubRebalancePolicyTx: the hub's per-token fee terms at the token-default base fee and zero gas fee. */
const hubPolicyTx = (config: HubConfig, tokenId: TokenId): Result<AccountTx, EntityError> =>
  map(defaultRebalanceBaseFee(Number(tokenId)), (baseFee): AccountTx => ({
    type: "rebalance_policy",
    tokenId,
    policyVersion: config.policyVersion,
    baseFee,
    liquidityFeeBps: config.rebalanceLiquidityFeeBps,
    gasFee: 0n,
  }));
/** Per-token amounts a tokenless hub config may not set. */
const HUB_RAW_OVERRIDES = ["rebalanceBaseFee", "c2rWithdrawSoftLimit", "rebalanceGasFee"] as const;
/**
 * og policyVersion rules: a requested version is a positive integer, never below the previous one, and equal to it
 * only when the fee terms did not change; otherwise the version starts at 1 and bumps when the terms change.
 */
const nextPolicyVersion = (
  requested: number | undefined, prev: number, changed: boolean,
): Result<number, EntityError> => {
  if (requested === undefined) return ok(Math.max(1, changed ? prev + 1 : prev));
  switch (true) {
    case !Number.isSafeInteger(requested) || requested <= 0:
      return invariant(`HUB_REBALANCE_POLICY_VERSION_INVALID:${String(requested)}`);
    case requested < prev: return invariant(`HUB_REBALANCE_POLICY_VERSION_STALE:${requested}<${prev}`);
    case requested === prev && changed: return invariant(`HUB_REBALANCE_POLICY_EQUIVOCATION:version=${requested}`);
    default: return ok(requested);
  }
};
/**
 * og buildHubConfig: raw overrides refused, liquidity fee bounded, policyVersion stale / equivocation /
 * auto-increment rules.
 */
export const buildHubConfig = (
  previous: HubConfig | undefined, data: HubConfigInput,
): Result<HubConfig, EntityError> => {
  const forbidden = HUB_RAW_OVERRIDES.filter((field) => data[field] !== undefined);
  const liquidityFeeBps = data.rebalanceLiquidityFeeBps ?? 1n;
  if (forbidden.length > 0) return invariant(`HUB_REBALANCE_TOKENLESS_RAW_OVERRIDE_FORBIDDEN:${forbidden.join(",")}`);
  if (liquidityFeeBps < 0n || liquidityFeeBps > 10_000n) {
    return invariant(`HUB_REBALANCE_LIQUIDITY_FEE_BPS_INVALID:${liquidityFeeBps}`);
  }
  const changed = previous === undefined || previous.rebalanceLiquidityFeeBps !== liquidityFeeBps;
  const hubName = typeof data.hubName === "string" && data.hubName.trim() ? data.hubName.trim() : previous?.hubName;
  const takerFeeBps = Math.floor(Number(data.swapTakerFeeBps ?? 0) || 0);
  const strategy = data.matchingStrategy;
  return map(nextPolicyVersion(data.policyVersion, previous?.policyVersion ?? 0, changed), (policyVersion) => ({
    ...(hubName ? { hubName } : {}),
    matchingStrategy: strategy === "time" || strategy === "fee" ? strategy : "amount",
    policyVersion,
    routingFeePPM: data.routingFeePPM ?? 1,
    baseFee: data.baseFee ?? 0n,
    swapTakerFeeBps: Math.max(0, Math.min(10_000, takerFeeBps)),
    disputeAutoFinalizeMode: data.disputeAutoFinalizeMode ?? "auto",
    minCollateralThreshold: data.minCollateralThreshold ?? 0n,
    rebalanceLiquidityFeeBps: liquidityFeeBps,
    rebalanceTimeoutMs: data.rebalanceTimeoutMs ?? 10 * 60 * 1000,
  }));
};
/**
 * og checkAutoRebalance (account/tx/handlers/rebalance/request-collateral.ts): per policy token in ascending order,
 * request collateral for the peer credit we use once it crosses the soft limit, priced by the peer's published fee
 * terms, unless a request, a queued request, a pending frame or a settlement is in flight, the fee exceeds our
 * ceiling, or the fee would eat the whole request.
 */
export const autoRebalance = (child: AccountReplica, self: EntityId): readonly AccountTx[] => {
  const body = child.state, isLeft = sameHex(body.account.id.left, self);
  const mempool = "mempool" in child ? child.mempool : [];
  const policies = [...(child.rebalancePolicy ?? new Map<number, RebalancePolicy>())].toSorted(([a], [b]) => a - b);
  const requestFor = ([token, policy]: readonly [number, RebalancePolicy]): readonly AccountTx[] => {
    const tokenId = String(token) as TokenId, fees = body.feePolicies.get(tokenId);
    const fee = isLeft ? fees?.right : fees?.left, delta = body.account.deltas.get(tokenId);
    const inFlight = (body.requested.get(tokenId) ?? 0n) > 0n || child._tag === "proposed"
      || body.settlement !== undefined
      || mempool.some((t) => t.type === "request_collateral" && Number(t.tokenId) === token);
    const pinnedPolicy = policy.r2cRequestSoftLimit === policy.hardLimit;
    if (fee === undefined || delta === undefined || pinnedPolicy || inFlight) return [];
    const total = delta.ondelta + delta.offdelta;
    const usedPeerCredit = isLeft ? floor0(total - floor0(delta.collateral)) : floor0(-total);
    const feeAmount = fee.baseFee + fee.gasFee + (usedPeerCredit * fee.liquidityFeeBps) / 10000n;
    const worthIt = usedPeerCredit >= policy.r2cRequestSoftLimit
      && feeAmount <= policy.maxAcceptableFee && usedPeerCredit > feeAmount;
    return worthIt ? [{
      type: "request_collateral",
      tokenId,
      amount: usedPeerCredit,
      feeTokenId: tokenId,
      feeAmount,
      policyVersion: fee.policyVersion,
    }] : [];
  };
  return policies.flatMap(requestFor);
};
/** og processingTrigger / direct-payment wake: an empty input to `validators[0]`. */
const wake = (state: EntityState, timestamp: bigint): EntityOutput =>
  ({ to: state.id, signerId: state.quorum.proposer, input: { kind: "txs", timestamp, txs: [] } });
/** og queueLocalJBatchBroadcast's output: a self j_broadcast for the given signer. */
const selfJBroadcast = (state: EntityState, signerId: string, timestamp: bigint): EntityOutput =>
  ({ to: state.id, signerId: signerId as Address, input: { kind: "txs", timestamp, txs: [{ type: "j_broadcast", data: {} }] } });
const sameDomain = (a: Domain, b: Domain): boolean =>
  a.chainId === b.chainId && sameHex(a.depositoryAddress, b.depositoryAddress);
type OpenAccountData = Extract<EntityTx, { type: "openAccount" }>["data"];
/** og MAX_PROFILE_ADVERTISED_ACCOUNTS. */
const MAX_PINNED_ACCOUNTS = 100;
/** og seedOpenAccountPolicies: a requested policy has a positive soft limit, a hard limit not below it, a fee >= 0. */
const policySane = (p: RebalancePolicy): boolean =>
  p.r2cRequestSoftLimit > 0n && p.hardLimit >= p.r2cRequestSoftLimit && p.maxAcceptableFee >= 0n;
/**
 * og seedOpenAccountPolicies: the requested policy (validated) for tokenId, the jurisdiction defaults for the rest,
 * then the hub's fee terms; add_delta per token and the optional credit line are queued with them.
 */
const seedAccount = (
  state: EntityState, replicas: Replicas, data: OpenAccountData, opened: AccountReplica, now: bigint,
): Result<Draft, EntityError> => {
  const credit = data.tokenId ?? "1", requested = data.rebalancePolicy, hub = hubConfigOf(state);
  const tokens = [...new Set([credit, ...DEFAULT_ACCOUNT_TOKEN_IDS])].filter((t) => Number(t) > 0) as TokenId[];
  if (requested !== undefined && !policySane(requested)) {
    return invariant(`REBALANCE_POLICY_INVALID:token=${Number(credit)}`);
  }
  const policyOf = (t: TokenId): Result<RebalancePolicy, EntityError> =>
    (requested !== undefined && t === credit ? ok(requested) : rebalanceDefaults(state.jurisdictionConfig, Number(t)));
  const parts = all({
    policies: traverse(tokens, (t) => map(policyOf(t), (p) => [Number(t), { ...p }] as const)),
    hubTxs: hub === undefined ? ok([]) : traverse(tokens, (t) => hubPolicyTx(hub, t)),
  });
  return chain(parts, ({ policies, hubTxs }) => {
    const creditLine: readonly AccountTx[] = data.creditAmount !== undefined && data.creditAmount > 0n
      ? [{ type: "set_credit_limit", tokenId: credit as TokenId, limit: data.creditAmount }]
      : [];
    const seeded = [...tokens.map((t): AccountTx => ({ type: "add_delta", tokenId: t })), ...hubTxs, ...creditLine];
    // og resolveOpenAccountPublicPin: the opener pins unless `pinPublic: false` or the pin cap is reached
    const pinnedCount = [...replicas.values()].filter((c) => c.publicPinned === true).length;
    const pinned = data.pinPublic !== false && pinnedCount < MAX_PINNED_ACCOUNTS;
    const child: AccountReplica = {
      ...opened,
      rebalancePolicy: new Map(policies),
      ...(pinned ? { publicPinned: true } : {}),
    };
    // og admission never folds; the rewrite's admission fold needs a real clock only for rebalance_policy's timestamp
    const clock = hubTxs.length > 0 ? { ...L0_CLOCK, timestamp: now } : L0_CLOCK;
    return map(admitAt(child, seeded, state.id, clock), (admitted) =>
      ({ ...putChild(state, replicas, data.targetEntityId, admitted), outputs: [] }));
  });
};
/**
 * og handleOpenAccountEntityTx: no output (the peer learns from the first Account frame); seeds add_delta for tokenId
 * + defaults and an optional credit line.
 */
const openChild = (
  state: EntityState, replicas: Replicas, tx: Extract<EntityTx, { type: "openAccount" }>, now: bigint,
): Result<Draft, EntityError> => {
  const { targetEntityId: target, accountDomain, watchSeed, disputeConfig } = tx.data;
  const id = accountId(state.id, target);
  if (!id.ok) return err({ _tag: "self_account" });
  const opening = genesisReplica(id.value, { domain: accountDomain, watchSeed, disputeConfig });
  return chain(opening, (opened): Result<Draft, EntityError> => {
    switch (true) {
      case !sameDomain(opened.state.terms.domain, state.jurisdiction): return err({ _tag: "domain_mismatch" });
      case replicas.has(target): return err({ _tag: "account_exists", target });
      default: return seedAccount(state, replicas, tx.data, opened, now);
    }
  });
};
/** og createInboundAccountState: an unknown peer's first proposal (height 1) opens the Account from its envelope. */
const inboundChild = (
  state: EntityState, replicas: Replicas, from: EntityId, m: Extract<AccountMessage, { kind: "ack_frame" }>,
): Result<Folded, EntityError> => {
  const { watchSeed, domain, disputeConfig } = m;
  if (m.frame.height !== 1n || watchSeed === undefined) return err({ _tag: "no_such_account", target: from });
  if (!sameDomain(domain, state.jurisdiction)) return err({ _tag: "domain_mismatch" });
  // og createInboundAccountState: shadow.rebalance.policy holds the jurisdiction defaults for DEFAULT_ACCOUNT_TOKEN_IDS
  const parts = all({
    id: mapErr(accountId(state.id, from), (): EntityError => ({ _tag: "self_account" })),
    policies: traverse(DEFAULT_ACCOUNT_TOKEN_IDS, (t) =>
      map(rebalanceDefaults(state.jurisdictionConfig, Number(t)), (p) => [Number(t), p] as const)),
  });
  return chain(parts, ({ id, policies }) => map(genesisReplica(id, { domain, watchSeed, disputeConfig }), (opened) =>
    putChild(state, replicas, from, { ...opened, rebalancePolicy: new Map(policies) })));
};
const UINT256_MAX = (1n << 256n) - 1n;
const lendingEntityErr = (reason: string): Result<never, EntityError> => err({ _tag: "lending_entity", reason });
const lendingIntent = (value: string, prefix: "lend" | "borrow" | "loan"): Result<string, EntityError> => {
  const id = lower(value);
  const valid = LENDING_INTENT.test(id) && id.startsWith(`${prefix}-`);
  return valid ? ok(id) : lendingEntityErr("LENDING_INTENT_ID_INVALID");
};
const lendingAmount = (amount: bigint, context: string): Result<bigint, EntityError> =>
  (amount <= 0n ? lendingEntityErr(`${context}_AMOUNT_MUST_BE_POSITIVE`) : ok(amount));
const lendingTerm = (v: string): Result<string, EntityError> =>
  (LENDING_TERMS.has(v) ? ok(v) : lendingEntityErr("LENDING_INVALID_TERM"));
const lendingBps = (v: number): Result<number, EntityError> =>
  (interestOk(v) ? ok(Math.floor(Number(v))) : lendingEntityErr("LENDING_INVALID_INTEREST_BPS"));
/**
 * og payments/lending.ts: validate the hub Account, intent id, amount, term and interest, then queue one Account
 * lending tx on the hub Account with this entity (lowercased) as the actor, plus the processing wake to
 * `validators[0]`. Every refusal is a plain Error in og.
 */
const entityLending = (
  state: EntityState, replicas: Replicas, tx: LendingEntityTx,
  queue: (hub: EntityId, accountTx: AccountTx) => Result<Draft, EntityError>,
): Result<Draft, EntityError> => {
  const hub = lower(tx.data.hubEntityId), self = lower(state.id);
  const key = [...replicas.keys()].find((k) => lower(k) === hub);
  const child = key === undefined ? undefined : replicas.get(key);
  if (hub === "" || key === undefined || child === undefined) return lendingEntityErr("LENDING_HUB_ACCOUNT_MISSING");
  switch (tx.type) {
    case "lendingOffer": {
      const x = tx.data;
      const checked = all({
        positionId: lendingIntent(x.positionId, "lend"),
        amount: lendingAmount(x.amount, "LENDING_FUND"),
        termId: lendingTerm(x.termId),
        interestBps: lendingBps(x.interestBps),
      });
      return chain(checked, ({ positionId, amount, termId, interestBps }) => (child.state.account.deltas.has(x.tokenId)
        ? queue(key, {
          type: "lending_fund",
          positionId,
          hubEntityId: hub,
          lenderEntityId: self,
          tokenId: x.tokenId,
          amount,
          termId,
          interestBps,
        })
        : lendingEntityErr("LENDING_TOKEN_NOT_ENABLED")));
    }
    case "lendingBorrow": {
      const x = tx.data;
      const checked = all({
        requestId: lendingIntent(x.requestId, "borrow"),
        amount: lendingAmount(x.amount, "LENDING_BORROW"),
        termId: lendingTerm(x.termId),
        maxInterestBps: lendingBps(x.maxInterestBps ?? 10_000),
      });
      return chain(checked, ({ requestId, amount, termId, maxInterestBps }) => queue(key, {
        type: "lending_borrow_request",
        requestId,
        hubEntityId: hub,
        borrowerEntityId: self,
        tokenId: x.tokenId,
        amount,
        termId,
        maxInterestBps,
      }));
    }
    case "lendingRepay": {
      const x = tx.data;
      const checked = all({
        loanId: lendingIntent(x.loanId, "loan"),
        amount: lendingAmount(x.amount, "LENDING_REPAY"),
      });
      return chain(checked, ({ loanId, amount }) => queue(key, {
        type: "lending_repay", loanId, hubEntityId: hub, borrowerEntityId: self, tokenId: x.tokenId, amount,
      }));
    }
    case "lendingClosePosition": return chain(lendingIntent(tx.data.positionId, "lend"), (positionId) =>
      queue(key, { type: "lending_close_request", positionId, hubEntityId: hub, lenderEntityId: self }));
  }
};
// ---- og entity/tx/handlers/dispute/{index,start,start-admission,start-evidence,start-hanko}.ts: prepareDispute, disputeStart ----
type JBatchRows = { readonly [field: string]: readonly Binary[] };
/**
 * og EntityState.jBatchState as committed (the J-layer's JBatchState shape); only the dispute-start rows are read and
 * written here.
 */
type CommittedJBatch = {
  readonly batch: JBatchRows;
  readonly sentBatch?: { readonly batch: JBatchRows; readonly entityNonce: number } | undefined;
  readonly recoveryBatches?: readonly JBatchRows[] | undefined;
};
const committedJBatch = (state: EntityState): CommittedJBatch | undefined =>
  state.committed["jBatchState"] as CommittedJBatch | undefined;
/** The Entity with its committed jBatchState replaced; every jBatch shape is stored as the same binary slot. */
const withCommittedJBatch = (state: EntityState, jb: object): EntityState =>
  ({ ...state, committed: { ...state.committed, jBatchState: jb as unknown as Binary } });
/**
 * og hasQueuedDisputeStart / hasQueuedDisputeFinalize: an operation for this counterparty in the draft, the sent
 * batch or a recovery batch.
 */
const queuedDisputeOp = (
  jb: CommittedJBatch, peer: string, kind: "disputeStarts" | "disputeFinalizations",
): boolean => {
  const target = lower(peer);
  const names = (rows: readonly Binary[] | undefined): boolean =>
    (rows ?? []).some((r) => lower((r as { readonly counterentity?: string }).counterentity) === target);
  const batches = [jb.batch, jb.sentBatch?.batch, ...(jb.recoveryBatches ?? [])];
  return batches.some((b) => names(b?.[kind]));
};
const queuedDisputeStart = (jb: CommittedJBatch, peer: EntityId): boolean => queuedDisputeOp(jb, peer, "disputeStarts");
/** og canonicalizeProofBodyStruct: u32 response seconds as numbers, offdeltas as Int512 {high, low} words. */
export const ogProofBody = (b: ProofBody): Binary => ({
  watchSeed: b.watchSeed,
  leftResponseSeconds: Number(b.leftResponseSeconds),
  rightResponseSeconds: Number(b.rightResponseSeconds),
  offdeltas: b.offdeltas.map((v) => ({ high: v >> 256n, low: v & UINT256_MAX })),
  tokenIds: [...b.tokenIds],
  transformers: b.transformers.map((c) => ({
    transformerAddress: c.transformerAddress,
    encodedBatch: c.encodedBatch,
    allowances: c.allowances.map((a) => ({
      deltaIndex: a.deltaIndex,
      rightAllowance: a.rightAllowance,
      leftAllowance: a.leftAllowance,
    })),
  })),
});
/** og collectDisputeEvidenceReadinessIssues: the cooldown, then the cross-Entity book removals still unconfirmed. */
const disputeIssues = (child: AccountReplica, now: number): readonly string[] => {
  const prepare = child._tag === "preparing" ? child.prepare : undefined;
  const readyAfter = prepare?.readyAfter ?? 0, removals = prepare?.pendingOrderbookRemovalIds?.length ?? 0;
  return [
    ...(readyAfter > now ? [`cooldown:${readyAfter - now}ms`] : []),
    ...(removals > 0 ? [`orderbookRemovals:${removals}`] : []),
  ];
};
type StartIntent = DisputePrepare["startIntent"];
/** ethers' strict reader: `base` anchors a dynamic region, `off` is the read position inside it. */
type AbiReader = { readonly base: number; readonly off: number };
/** Where a read left the reader, and the running byte budget ethers charges against its inflation bound. */
type AbiRead = { readonly reader: AbiReader; readonly consumed: number };
/** A padded span read at byte `at`. */
type AbiSpan = AbiRead & { readonly at: number };
/** An index word; `huge` is above MAX_SAFE_INTEGER, the numeric fault ethers stores rather than throws. */
type AbiWord = AbiRead & { readonly value: number | "huge" };
/**
 * ethers v6 Reader primitives over one root buffer: padded reads within the region, and the root's 1024x read budget.
 * Every `undefined` read is an overrun, which ethers throws.
 */
const ethersReads = (buf: Uint8Array) => {
  const limit = 1024 * buf.length;
  const room = (r: AbiReader): number => Math.max(0, buf.length - r.base);
  const region = (r: AbiReader, offset: number): AbiReader => ({ base: r.base + r.off + offset, off: 0 });
  const skip = (r: AbiReader, length: number, consumed: number): AbiSpan | undefined => {
    const aligned = Math.ceil(length / 32) * 32;
    const fits = r.off + aligned <= room(r) && consumed + length <= limit;
    return fits
      ? { at: r.base + r.off, reader: { base: r.base, off: r.off + aligned }, consumed: consumed + length }
      : undefined;
  };
  const index = (r: AbiReader, consumed: number): AbiWord | undefined => {
    const span = skip(r, 32, consumed);
    if (span === undefined) return undefined;
    const word = wordAt(buf, span.at);
    return { ...span, value: word > MAX_SAFE_WORD ? "huge" : Number(word) };
  };
  /** The region the root's head offset points at, and its first word (a `bytes[]` count, a tuple's first offset). */
  const headArray = (): { readonly array: AbiReader; readonly first: AbiWord | undefined } | undefined => {
    const origin: AbiReader = { base: 0, off: 0 }, head = index(origin, 0);
    if (head === undefined || head.value === "huge") return undefined;
    const array = region(origin, head.value);
    return { array, first: index(array, head.consumed) };
  };
  return { room, region, skip, index, headArray };
};
/**
 * Whether ethers v6 `AbiCoder.defaultAbiCoder().decode(["bytes[]"], bytes)` returns rather than throws: strict
 * (padded) reads, the array-count bound, the 1024x inflation ratio, and ethers' swallowing of a non-overrun fault (an
 * index above MAX_SAFE_INTEGER) inside a dynamic member.
 */
const decodesAsBytesArray = (buf: Uint8Array): boolean => {
  const { room, region, skip, index, headArray } = ethersReads(buf);
  const top = headArray(), count = top?.first;
  if (top === undefined || count === undefined) return false;
  const array = top.array;
  if (count.value === "huge") return true;
  if (count.value * 32 > room(array)) return false;
  const members = region(count.reader, 0);
  /** A settled verdict, or where the offset table reads next. */
  type Scan = AbiRead | boolean;
  const member = (scan: Scan): Scan => {
    if (typeof scan === "boolean") return scan;
    const offset = index(scan.reader, scan.consumed);
    if (offset === undefined) return false;
    if (offset.value === "huge") return true;
    const length = index(region(members, offset.value), offset.consumed);
    if (length === undefined) return false;
    const body = length.value === "huge" ? length : skip(length.reader, length.value, length.consumed);
    return body === undefined ? false : { reader: offset.reader, consumed: body.consumed };
  };
  return Array.from({ length: count.value }).reduce<Scan>(member, count) !== false;
};
const EVEN_HEX = /^0x(?:[0-9a-fA-F]{2})*$/;
const MAX_DISPUTE_ARGUMENT_BYTES = 64 * 1024;
/**
 * og sanitizeOptionalDisputeArgument (its warnings are debug logs only): malformed, oversized or non-`bytes[]`
 * evidence becomes empty evidence.
 */
export const sanitizeDisputeArgument = (value: unknown): string => {
  if (typeof value !== "string" || !EVEN_HEX.test(value)) return "0x";
  const oversized = (value.length - 2) / 2 > MAX_DISPUTE_ARGUMENT_BYTES;
  return value === "0x" || (!oversized && decodesAsBytesArray(hexToBytes(value))) ? value : "0x";
};
// ---- og protocol/dispute/arguments.ts, entity/dispute-arguments.ts: positional DeltaTransformer arguments and the frozen Account's known secrets ----
/**
 * og DisputeArgumentPlan: runtime ids of the positional evidence (payments by lockId, same-j swaps by offerId, pulls
 * by pullId).
 */
export type DisputeArgumentPlan = {
  readonly paymentHashlocks: readonly string[];
  readonly leftSwapOfferIds: readonly string[];
  readonly rightSwapOfferIds: readonly string[];
  readonly leftPullIds: readonly string[];
  readonly rightPullIds: readonly string[];
};
/**
 * og buildCurrentDisputeArgumentPlan: a left-made offer is the right side's to fill; cross-j offers carry no fill
 * ratio.
 */
export const disputeArgumentPlan = (s: CommittedAccountState): DisputeArgumentPlan => {
  const swaps = byTextKey<SwapOffer>(s.swapOffers).filter(([, o]) => !o.crossJurisdiction);
  const pulls = byTextKey<PullRow>(s.pulls);
  const ids = <X>(rows: readonly (readonly [string, X])[], keep: (x: X) => boolean): readonly string[] =>
    rows.filter(([, x]) => keep(x)).map(([id]) => id);
  return {
    paymentHashlocks: byTextKey<ProofLockRow>(s.locks).map(([, l]) => String(l.hashlock)),
    leftSwapOfferIds: ids(swaps, (o) => !o.makerIsLeft),
    rightSwapOfferIds: ids(swaps, (o) => o.makerIsLeft),
    leftPullIds: ids(pulls, (p) => p.amount >= 0n),
    rightPullIds: ids(pulls, (p) => p.amount < 0n),
  };
};
export type DisputeArgumentSide = "left" | "right" | "none";
const UINT16_MAX = 0xffff;
const clampArgumentRatio = (v: number): number => {
  switch (true) {
    case !Number.isFinite(v) || v <= 0: return 0;
    case v >= UINT16_MAX: return UINT16_MAX;
    default: return Math.floor(v);
  }
};
const deltaTransformerArgs = (ratios: readonly number[], secrets: readonly string[]): string =>
  abiEncodeHex([t([arr(ratios, (r) => A.uint(BigInt(clampArgumentRatio(r)))), arr(secrets, A.b32)])]);
const fillRatioOk = (ratio: number): boolean => Number.isSafeInteger(ratio) && ratio > 0 && ratio <= UINT16_MAX;
/** The first valid swap_resolve fill ratio per planned offer, in evidence order. */
const plannedFillRatios = (
  evidence: readonly WireAccountTx[], planned: ReadonlySet<string>,
): ReadonlyMap<string, number> => {
  const resolves = evidence.flatMap((tx) =>
    (tx.type === "swap_resolve" && planned.has(tx.offerId) && fillRatioOk(tx.fillRatio) ? [tx] : []));
  return new Map(firstBy(resolves, (tx) => tx.offerId).map((tx) => [tx.offerId, tx.fillRatio]));
};
/**
 * Each clause's fill ratios for one side: a payment clause carries them all, a swap-only clause its own positional
 * slice.
 */
const clauseRatios = (
  clauses: readonly ProofBatch[], ratios: readonly number[], mine: (w: ProofSwap) => boolean,
): readonly (readonly number[])[] =>
  mapAccum(clauses, 0, (from, b) => {
    const n = b.swaps.filter(mine).length;
    return [from + n, b.payments.length > 0 ? ratios : ratios.slice(from, from + n)] as const;
  })[1];
/**
 * og buildDisputeArgumentsFromState: fill ratios from the first valid swap_resolve per planned offer (the Account's
 * in-flight frame, then its mempool), secrets on one side, one `(uint16[] fillRatios, bytes32[] secrets)` per
 * payment/swap clause (a swap clause gets its own positional slice), wrapped as `bytes[]` only when the side has
 * evidence, then og sanitizeOptionalDisputeArgument.
 */
export const disputeArguments = (
  s: CommittedAccountState, evidence: readonly WireAccountTx[], side: DisputeArgumentSide, secrets: readonly string[],
): Result<{ readonly left: string; readonly right: string }, ProofError> => chain(proofBatches(s), (batches) => {
  const plan = disputeArgumentPlan(s);
  const fills = plannedFillRatios(evidence, new Set([...plan.leftSwapOfferIds, ...plan.rightSwapOfferIds]));
  const clauses = batches.filter((b) => b.payments.length > 0 || b.swaps.length > 0);
  const sideArguments = (
    offerIds: readonly string[], own: DisputeArgumentSide, mine: (w: ProofSwap) => boolean,
  ): Result<string, ProofError> => {
    const ratios = offerIds.map((id) => fills.get(id) ?? 0), known = side === own ? secrets : [];
    if (!ratios.some((r) => r > 0) && known.length === 0) return ok("0x");
    const args = clauseRatios(clauses, ratios, mine).map((rs) => deltaTransformerArgs(rs, known));
    return args.length < 1 || args.length > MAX_PROOF_TRANSFORMERS
      ? proofErr(`DISPUTE_ARGUMENT_CANONICAL_CLAUSE_COUNT_INVALID:${args.length}`)
      : ok(abiEncodeHex([arr(args, A.bytes)]));
  };
  const sides = all({
    left: sideArguments(plan.leftSwapOfferIds, "left", (w) => !w.ownerIsLeft),
    right: sideArguments(plan.rightSwapOfferIds, "right", (w) => w.ownerIsLeft),
  });
  return map(sides, ({ left, right }) =>
    ({ left: sanitizeDisputeArgument(left), right: sanitizeDisputeArgument(right) }));
});
/**
 * og collectKnownDisputeSecretsForState: the paybook preimages of exactly the frozen Account's hashlocks (in
 * transformer order), for routes whose inbound or outbound hop is this counterparty, each opening its hashlock, each
 * once.
 */
export const knownDisputeSecrets = (
  s: CommittedAccountState, paybook: Paybook | undefined, counterparty: string,
): readonly string[] => {
  if (paybook === undefined) return [];
  const secretFor = (raw: string): readonly string[] => {
    const hashlock = raw.toLowerCase(), route = paybook.entries.get(hashlock), secret = route?.secret;
    const opens = route !== undefined && secret !== undefined && route.hashlock.toLowerCase() === hashlock
      && WORD.test(secret)
      && (route.inboundEntity === counterparty || route.outboundEntity === counterparty)
      && keccak256Hex(abiEncode([A.b32(secret)])).toLowerCase() === hashlock;
    return opens ? [secret] : [];
  };
  return [...new Set(disputeArgumentPlan(s).paymentHashlocks.flatMap(secretFor))];
};
/** ethers `decode(["bytes[]"])` read as og's decodeStringArray does: any fault (swallowed or thrown) is no array. */
const ethersBytesArray = (buf: Uint8Array): readonly Uint8Array[] | undefined => {
  const { room, region, skip, index, headArray } = ethersReads(buf);
  const top = headArray(), count = top?.first;
  if (top === undefined || count === undefined || count.value === "huge" || count.value * 32 > room(top.array)) {
    return undefined;
  }
  const members = region(count.reader, 0);
  type Collected = (AbiRead & { readonly items: readonly Uint8Array[] }) | undefined;
  const member = (acc: Collected): Collected => {
    if (acc === undefined) return undefined;
    const offset = index(acc.reader, acc.consumed);
    if (offset === undefined || offset.value === "huge") return undefined;
    const length = index(region(members, offset.value), offset.consumed);
    if (length === undefined || length.value === "huge") return undefined;
    const body = skip(length.reader, length.value, length.consumed);
    if (body === undefined) return undefined;
    const item = buf.subarray(body.at, body.at + length.value);
    return { reader: offset.reader, consumed: body.consumed, items: [...acc.items, item] };
  };
  return Array.from({ length: count.value }).reduce<Collected>(member, { ...count, items: [] })?.items;
};
/**
 * ethers `decode(["tuple(uint16[] fillRatios, bytes32[] secrets)"])[0].secrets`: a non-overrun fault in fillRatios
 * is a stored error that secrets never touch.
 */
const ethersArgSecrets = (buf: Uint8Array): readonly string[] | undefined => {
  const { room, region, skip, index, headArray } = ethersReads(buf);
  const top = headArray();
  if (top === undefined) return undefined;
  const tuple = top.array;
  type Words = AbiRead & { readonly words: readonly string[] | "huge" };
  /**
   * A static-element array member (uint16[] and bytes32[] both pad to 32-byte words); `huge` is a stored count fault.
   */
  const words = (at: AbiWord | undefined): Words | undefined => {
    if (at === undefined || at.value === "huge") return undefined;
    const array = region(tuple, at.value), count = index(array, at.consumed);
    if (count === undefined) return undefined;
    if (count.value === "huge") return { ...count, words: "huge" };
    const n = count.value;
    const span = n * 32 > room(array) ? undefined : skip(region(count.reader, 0), 32 * n, count.consumed);
    if (span === undefined) return undefined;
    const word = (i: number): string => bytesToHex(buf.subarray(span.at + 32 * i, span.at + 32 * i + 32));
    return { ...span, words: Array.from({ length: n }, (_, i) => word(i)) };
  };
  const ratiosAt = top.first, ratios = words(ratiosAt);
  if (ratiosAt === undefined || ratios === undefined) return undefined;
  const secrets = words(index(ratiosAt.reader, ratios.consumed));
  return secrets === undefined || secrets.words === "huge" ? undefined : secrets.words;
};
/**
 * og decodeDisputeStarterInitialSecrets: the first (DeltaTransformer) clause's secrets of the starter's arguments,
 * lowercased, once each; malformed evidence is none.
 */
export const starterSecrets = (raw: unknown): readonly string[] => {
  const value = String(raw || "0x");
  const buf = value !== "0x" && /^0x(?:[0-9a-fA-F]{2})*$/i.test(value) ? parseHex(value) : null;
  const first = buf === null ? undefined : ethersBytesArray(buf)?.[0];
  if (first === undefined || first.length === 0) return [];
  return [...new Set((ethersArgSecrets(first) ?? []).map((x) => x.toLowerCase()))];
};
/** A status line appended to the draft's frame events (og addMessage). */
const withStatus = (d: Draft, message: string): Draft => ({ ...d, events: [...(d.events ?? []), status(message)] });
const START_EVIDENCE_INVALID: EntityError = { _tag: "entity_invariant", reason: "DISPUTE_START_EVIDENCE_INVALID" };
/**
 * og loadStartProof: the revealed body (built under the Account's DeltaTransformer) must hash to the stored
 * proofBodyHash.
 */
const revealedStartProof = (child: AccountReplica, dt: DeltaTransformerRef): Result<LocalProof, EntityError> =>
  chain(mapErr(committedView(child.state), () => START_EVIDENCE_INVALID), (view) =>
    mapErr(localProof(view, dt), (e): EntityError => (e._tag === "dispute_proof" && e.error._tag === "transformer"
      ? { _tag: "entity_invariant", reason: e.error.code }
      : START_EVIDENCE_INVALID)));
/**
 * og assertCrossJurisdictionDisputeProofHasPulls (after loadStartProof, before the nonce): a route-bound start must
 * carry a canonical Pull.
 */
const routeCarriesPull = (
  routeId: string | undefined, body: Pick<ProofBody, "transformers">, dt: DeltaTransformerRef,
): Result<void, EntityError> => {
  if (!routeId) return ok(undefined);
  if (!dt.ok) return halt(dt.error);
  return chain(proofBodyHasPulls(body, dt.value), (has) =>
    (has ? ok(undefined) : halt(`DISPUTE_START_CROSS_J_PROOF_PULLS_MISSING:${routeId}`)));
};
/** A start that passed og's admission: the seeded draft, its batch, the preparing Account and how it is judged. */
type StartCase = {
  readonly admitted: Draft;
  readonly jb: CommittedJBatch;
  readonly child: PreparingAccount;
  readonly peer: EntityId;
  readonly intent: StartIntent;
  readonly dt: DeltaTransformerRef;
};
/** og start admission: the Account's status and readiness; og's status line when the start cannot proceed. */
const startGate = (d: Draft, jb: CommittedJBatch, peer: EntityId, now: number): PreparingAccount | string => {
  const child = d.accountReplicas.get(peer), tag = peer.slice(-4);
  if (child === undefined) return `❌ No account with ${tag} - cannot start dispute`;
  if (child._tag === "disputed") return `❌ Account with ${tag} is disputed - reopen required`;
  if (child._tag !== "preparing") return `❌ Account with ${tag} must enter dispute preparation before disputeStart`;
  const issues = disputeIssues(child, now);
  if (issues.length > 0) return `⏳ disputeStart blocked until evidence is stable for ${tag}: ${issues.join("; ")}`;
  return queuedDisputeStart(jb, peer) ? `ℹ️ disputeStart already queued for ${tag} (awaiting batch lifecycle)` : child;
};
/**
 * og start evidence refusals: a missing witness is og's status line; a body, nonce, hash or Hanko problem is judged
 * after og loadStartProof reveals the body (and, for the nonce and Hanko, the route's Pull check).
 */
const startRefusal = (c: StartCase, refusal: StartRefusal): Result<Draft, EntityError> => {
  const w = c.child.dispute.counterparty, jNonce = c.child.state.jNonce, signed = w?.proofNonce ?? 0;
  const afterReveal = (f: (p: LocalProof) => Result<Draft, EntityError>): Result<Draft, EntityError> =>
    chain(revealedStartProof(c.child, c.dt), f);
  const withPulls = (p: LocalProof, then: () => Result<Draft, EntityError>): Result<Draft, EntityError> =>
    chain(routeCarriesPull(c.intent.crossJurisdictionRouteId, p.body, c.dt), then);
  switch (refusal._tag) {
    case "no_witness": return ok(withStatus(c.admitted, "❌ Missing counterparty dispute hanko - cannot start dispute"));
    case "body_mismatch": return afterReveal((p) =>
      halt(`DISPUTE_START_PROOFBODY_HASH_MISMATCH:${c.peer}:${w?.proofBodyHash ?? ""}:${p.bodyHash}`));
    case "nonce_stale": return afterReveal((p) => withPulls(p, () =>
      ok(withStatus(c.admitted, `❌ Stale dispute proof nonce ${signed} (on-chain=${jNonce}) - reopen required`))));
    case "hanko_invalid": return afterReveal((p) => withPulls(p, () => ok(withStatus(c.admitted,
      "❌ Counterparty dispute proof invalid for current account snapshot; "
        + `nonce=${signed} onChain=${jNonce} source=counterpartyHanko`))));
    case "hash_mismatch":
      return afterReveal((p) => withPulls(p, () => invariant(`DISPUTE_STORED_HASH_MISMATCH:${c.peer}`)));
    case "proof":
      return refusal.error._tag === "transformer" ? halt(refusal.error.code) : err(START_EVIDENCE_INVALID);
    default: return err(START_EVIDENCE_INVALID);
  }
};
/** og queueDisputeStart limits: a Pull-bearing start rides an empty batch; starts and total ops stay bounded. */
const startBatchFits = (jb: CommittedJBatch, hasPulls: boolean): Result<void, EntityError> => {
  const starts = (jb.batch["disputeStarts"] ?? []).length;
  const total = BATCH_FIELDS.reduce((n, f) => n + (jb.batch[f] ?? []).length, 0);
  const { maxDisputeStarts, maxTotalOps } = J_BATCH_LIMITS;
  switch (true) {
    case hasPulls && total !== 0: return halt(`DISPUTE_START_PULL_BATCH_NOT_EMPTY:${total}`);
    case starts >= maxDisputeStarts:
      return invariant(`J_BATCH_LIMIT_EXCEEDED: disputeStarts ${starts + 1}/${maxDisputeStarts}`);
    case total + 1 > maxTotalOps:
      return invariant(`J_BATCH_LIMIT_EXCEEDED: disputeStart would exceed total ops ${total + 1}/${maxTotalOps}`);
    default: return ok(undefined);
  }
};
/** The DisputeStart row in the draft batch, and the Account disputed with og's unobserved activeDispute. */
const startQueued = (c: StartCase, s: DisputeStart, starterInitialArguments: string): Draft => {
  const { admitted, jb, child, peer } = c, nonce = Number(s.nonce);
  const row: Binary = {
    counterentity: peer,
    nonce,
    proposerIsLeft: s.proposerIsLeft,
    proofbodyHash: s.proofbodyHash,
    initialProofbody: ogProofBody(s.initialProofbody),
    watchSeed: String(s.initialProofbody.watchSeed),
    sig: s.sig,
    starterInitialArguments,
    starterCounterArguments: "0x",
    starterCounterProofCommitment: ZERO_WORD,
  };
  const queued: QueuedDispute = {
    startedByLeft: sameHex(admitted.state.id, child.state.account.id.left),
    initialProofbodyHash: s.proofbodyHash,
    initialNonce: nonce,
    initialProposerIsLeft: s.proposerIsLeft,
    disputeTimeout: 0,
    jNonce: child.state.jNonce,
    starterInitialArguments,
    starterCounterArguments: "0x",
    starterCounterProofCommitment: ZERO_WORD,
    observedOnChain: false,
    finalizeQueued: false,
  };
  const starts = [...(jb.batch["disputeStarts"] ?? []), row];
  const jBatchState = { ...jb, batch: { ...jb.batch, disputeStarts: starts } } as unknown as Binary;
  const state = { ...admitted.state, committed: { ...admitted.state.committed, jBatchState } };
  return { ...admitted, ...putChild(state, admitted.accountReplicas, peer, startPrepared(child, s, queued)) };
};
/**
 * og queueDisputeStart after the evidence holds: the route's Pull check, the Account's DeltaTransformer (required for
 * every start), the batch limits and the starter's arguments; a Pull-bearing start also registers its Source claims.
 */
const queueStart = (c: StartCase, s: DisputeStart, ctx: FoldContext): Result<Draft, EntityError> => {
  const { admitted, child, peer, intent, dt } = c;
  const transformer = chain(routeCarriesPull(intent.crossJurisdictionRouteId, s.initialProofbody, dt), () =>
    (dt.ok ? ok(dt.value) : halt(dt.error)));
  return chain(transformer, (address) => chain(proofBodyHasPulls(s.initialProofbody, address), (hasPulls) => {
    const starterIsLeft = sameHex(child.state.account.id.left, admitted.state.id);
    const checked = all({
      fits: startBatchFits(c.jb, hasPulls),
      built: builtDisputeArguments(child, peer, admitted.state.paybook, starterIsLeft ? "left" : "right"),
    });
    return chain(checked, ({ built }) => {
      // og buildStarterArguments: a non-empty override replaces the starter side's built arguments, then is sanitized
      const own = starterIsLeft ? built.left : built.right, override = intent.starterInitialArguments;
      const disputed = startQueued(c, s, sanitizeDisputeArgument(override && override !== "0x" ? override : own));
      const note = intent.description ? `(${intent.description})` : "";
      const message = `⚔️ Dispute started vs ${peer.slice(-4)} ${note} - account frozen, use jBroadcast to commit`;
      if (!hasPulls) return ok(withStatus(disputed, message));
      const cj = cjOf(disputed, ctx.timestamp, ctx.runtimeSeed);
      const registered = pullDisputeRegistrations(cj, peer, s.initialProofbody, address);
      return map(registered, (cj) => withStatus(cjInto(disputed, cj, ctx.timestamp), message));
    });
  }));
};
/**
 * og handleDisputeStart: admission (jBatchState seeded, account status, readiness, an already queued start), evidence
 * (og loadStartProof / resolveStartNonce / verifyStartHanko through the rewrite's startOf), then the DisputeStart row
 * in the draft batch and the Account disputed with og's unobserved activeDispute. A missing or unusable witness is og's
 * status message; og's throws are fatal.
 */
type StartRequest = StartIntent & { readonly starterCounterArguments?: string | undefined };
const startDispute = (d: Draft, peer: EntityId, intent: StartRequest, ctx: FoldContext): Result<Draft, EntityError> => {
  if (intent.starterCounterArguments !== undefined) {
    return invariant("DISPUTE_INCREMENTED_ARGUMENT_OVERRIDE_UNSUPPORTED");
  }
  const routeIssue = crossDisputeRouteIssue(d.state, peer, intent.crossJurisdictionRouteId);
  if (routeIssue !== undefined) return halt(routeIssue);
  const { jb, admitted } = seededBatch(d, "disputeStart");
  const gate = startGate(admitted, jb, peer, Number(ctx.timestamp));
  if (typeof gate === "string") return ok(withStatus(admitted, gate));
  const c: StartCase = { admitted, jb, child: gate, peer, intent, dt: accountDt(ctx, gate) };
  const start = startOf(gate.state, gate.dispute, peer, ctx.verify, c.dt);
  return start.ok ? queueStart(c, start.value, ctx) : startRefusal(c, start.error);
};
/**
 * og validateCrossJurisdictionDisputeRoute: a route-bound start names a stored route whose source or target leg
 * (exactly one) is this Account, that is not terminal, and that carries both pull commitments.
 */
const crossDisputeRouteIssue = (state: EntityState, peer: string, routeId: string | undefined): string | undefined => {
  if (!routeId) return undefined;
  const route = state.crossJurisdictionSwaps?.get(routeId);
  if (route === undefined || route.orderId !== routeId) return `DISPUTE_START_CROSS_J_ROUTE_MISSING:${routeId}`;
  const self = lc(state.id), cp = lc(peer);
  const thisAccount = (a: string, b: string): boolean =>
    (lc(a) === self && lc(b) === cp) || (lc(b) === self && lc(a) === cp);
  const onSource = thisAccount(route.source.entityId, route.source.counterpartyEntityId);
  const onTarget = thisAccount(route.target.entityId, route.target.counterpartyEntityId);
  switch (true) {
    case onSource === onTarget: return `DISPUTE_START_CROSS_J_ROUTE_ROLE_MISMATCH:${routeId}`;
    case isCrossTerminal(route.status): return `DISPUTE_START_CROSS_J_ROUTE_INACTIVE:${routeId}:${route.status}`;
    case !route.sourcePull || !route.targetPull: return `DISPUTE_START_CROSS_J_PULLS_MISSING:${routeId}`;
    default: return undefined;
  }
};
/** The ethers release og pins (package.json): its decode errors end with `version=`, which og's halt text carries. */
const ETHERS_VERSION = "6.17.0";
/**
 * An ethers ABI decode failure: BUFFER_OVERRUN always propagates; any other error inside a dynamic slot is deferred
 * into that slot (og Result).
 */
type EthersFail = { readonly overrun: boolean; readonly message: string };
const ethersFail = (
  overrun: boolean, short: string, details: readonly string[], code: string,
): Result<never, EthersFail> => {
  const detail = [...details, `code=${code}`, `version=${ETHERS_VERSION}`].join(", ");
  return err({ overrun, message: `${short} (${detail})` });
};
/** ethers Reader.readBytes(32) on a sub-reader's own buffer: `data out-of-bounds` past its end. */
const ethersWordText = (data: Uint8Array, at: number): Result<bigint, EthersFail> => {
  if (at + 32 <= data.length) return ok(BigInt(bytesToHex(data.subarray(at, at + 32))));
  const details = [`buffer=${bytesToHex(data)}`, `length=${data.length}`, `offset=${at + 32}`];
  return ethersFail(true, "data out-of-bounds", details, "BUFFER_OVERRUN");
};
/** ethers Reader.readIndex: getNumber refuses a word above MAX_SAFE_INTEGER with an INVALID_ARGUMENT `overflow`. */
const ethersIndexText = (data: Uint8Array, at: number): Result<number, EthersFail> =>
  chain(ethersWordText(data, at), (w) => (w > MAX_SAFE_WORD
    ? ethersFail(false, "overflow", [`argument="value"`, `value=${w}`], "INVALID_ARGUMENT")
    : ok(Number(w))));
/**
 * ethers ArrayCoder.decode of a dynamic array of static `size`-word tuples: its count, the `insufficient data length`
 * guard, then each word in order.
 */
const ethersArrayText = (arr: Uint8Array, size: number): Result<number, EthersFail> =>
  chain(ethersIndexText(arr, 0), (count) => {
    if (count * 32 > arr.length) {
      const details = [`buffer=${bytesToHex(arr)}`, `offset=${count * 32}`, `length=${arr.length}`];
      return ethersFail(true, "insufficient data length", details, "BUFFER_OVERRUN");
    }
    const words = Array.from({ length: count * size }, (_, j) => 32 + j * 32);
    return map(traverse(words, (at) => ethersWordText(arr, at)), () => count);
  });
/** ethers' rendering of a value it refuses as BytesLike. */
const ethersValueText = (value: unknown): string => {
  switch (true) {
    case value == null: return "null";
    case typeof value === "string": return JSON.stringify(value);
    default: return String(value);
  }
};
/** The og DeltaBatch arrays in slot order (payments, swaps, pulls), each as its static tuple's word count. */
const DELTA_BATCH_SLOTS = [[0, 5], [1, 5], [2, 7]] as const;
/**
 * og proofBodyHasPulls' `ABI_CODER.decode([DELTA_BATCH_PARAM], encodedBatch)[0].pull.length` (ethers 6.17.0), as the
 * thrown message or the Pull count: the root offset is read outside any slot (both failures throw); the batch's own
 * offsets are too, so an overflow there is deferred into result index 0; an array's count overflow is deferred into
 * its slot, and throws only when the slot read is `pull`. Reads stay far below ethers' inflation bound (each array
 * lies within the buffer), so that guard never fires.
 */
export const ethersBatchPulls = (encoded: unknown): Result<number, string> => {
  if (typeof encoded !== "string" || encoded.length % 2 !== 0 || !/^0x[0-9a-f]*$/i.test(encoded)) {
    const value = ethersValueText(encoded);
    const detail = `argument="value", value=${value}, code=INVALID_ARGUMENT, version=${ETHERS_VERSION}`;
    return err(`invalid BytesLike value (${detail})`);
  }
  const data = hexToBytes(`0x${encoded.slice(2)}`);
  const deferred = (name: string): Result<never, string> =>
    err(`deferred error during ABI decoding triggered accessing ${name}`);
  const root = ethersIndexText(data, 0);
  if (!root.ok) return err(root.error.message);
  const batch = data.subarray(root.value);
  const slot = ([k, size]: readonly [number, number]): Result<Result<number, EthersFail>, string> => {
    const off = ethersIndexText(batch, k * 32);
    if (!off.ok) return off.error.overrun ? err(off.error.message) : deferred("index 0");
    const arr = ethersArrayText(batch.subarray(off.value), size);
    return !arr.ok && arr.error.overrun ? err(arr.error.message) : ok(arr);
  };
  return chain(traverse(DELTA_BATCH_SLOTS, slot), ([, , pull]) =>
    (pull !== undefined && pull.ok ? ok(pull.value) : deferred(`property "pull"`)));
};
/**
 * og proofBodyHasPulls: does a canonical DeltaTransformer clause of the signed body carry a Pull (malformed canonical
 * bytes halt, as Solidity's abi.decode reverts), halting with the clause index and og's ethers decode message.
 */
export const proofBodyHasPulls = (
  body: Pick<ProofBody, "transformers">, transformer: string,
): Result<boolean, EntityError> => {
  if (!isAddressText(transformer)) return halt(`DISPUTE_DELTA_TRANSFORMER_ADDRESS_INVALID:${transformer}`);
  const canonical = transformer.toLowerCase();
  const clauses = body.transformers.flatMap((t, i) =>
    (String(t.transformerAddress).toLowerCase() === canonical ? [[i, t.encodedBatch] as const] : []));
  const pullsIn = (i: number, batch: unknown): Result<boolean, EntityError> => {
    const pulls = ethersBatchPulls(batch);
    return pulls.ok ? ok(pulls.value > 0) : halt(`DISPUTE_CANONICAL_DELTA_BATCH_INVALID:${i}:${pulls.error}`);
  };
  // the first Pull-bearing clause answers; clauses after it are never decoded
  return foldResult(clauses, false, (found, [i, batch]) => (found ? ok(true) : pullsIn(i, batch)));
};
/**
 * One Source hub claim the start bundles: an impossible window halts, a deferred claim needs a sent batch to wait
 * behind.
 */
const bundleSourceClaim = (claimed: CjHost) => (next: Cj, c: SourceClaim): Result<Cj, EntityError> => {
  switch (c.result) {
    case "source-window-expired": return halt(`DISPUTE_START_SOURCE_CLAIM_WINDOW_IMPOSSIBLE:${c.routeId}`);
    case "already-queued": return ok(next);
    case "queued": return ok(cjSay(next, `🌉 Cross-j claim ${c.routeId}: source reveal bundled with dispute start`));
    case "deferred-batch-pending": return claimed.jb?.sentBatch === undefined
      ? halt(`DISPUTE_START_SOURCE_CLAIM_NOT_ATOMIC:${c.routeId}`)
      : ok(cjSay(next, `⏳ Cross-j claim ${c.routeId}: start/reveal held behind immutable jBatch`));
  }
};
/** og latches autoBroadcastDraft; without a sent batch it also broadcasts through `validators[0]`. */
const latchBroadcast = (signerId: string | undefined) => (next: Cj): Result<Cj, EntityError> => {
  const jb = next.host.jb ?? (initJBatch() as unknown as CjJBatch);
  const latched = cjHost(next, { ...next.host, jb: { ...jb, autoBroadcastDraft: true } });
  if (jb.sentBatch !== undefined) return ok(latched);
  return signerId
    ? ok(cjBroadcast(latched, signerId))
    : halt("DISPUTE_START_CROSS_J_BROADCAST_SIGNER_MISSING");
};
/**
 * og queuePullDisputeRegistrations: flush this Account's stashed reveals, bundle every Source hub claim the signed
 * body freezes into the start batch, latch autoBroadcastDraft and (without a sent batch) broadcast.
 */
const pullDisputeRegistrations = (
  cj: Cj, peer: string, body: Pick<ProofBody, "transformers">, transformer: string,
): Result<Cj, EntityError> =>
  chain(flushDeferredReveals(cj.host, peer), ({ host }) =>
    chain(sourceHubClaims(host, peer, body, transformer), ({ host: claimed, claims }) =>
      chain(foldResult(claims, cjHost(cj, claimed), bundleSourceClaim(claimed)),
        latchBroadcast(cj.host.validators[0]))));
/**
 * og buildDisputeArgumentsForCurrentState over the frozen Account (its retained swap_resolve evidence and the
 * Entity's paybook secrets).
 */
const builtDisputeArguments = (
  child: AccountReplica, peer: string, paybook: Paybook | undefined, side: DisputeArgumentSide,
): Result<{ readonly left: string; readonly right: string }, EntityError> =>
  chain(mapErr(committedView(child.state), () => START_EVIDENCE_INVALID), (view) => {
    const args = disputeArguments(view, child.mempool, side, knownDisputeSecrets(view, paybook, peer));
    return mapErr(args, (e): EntityError => ({
      _tag: "entity_invariant",
      reason: e._tag === "transformer" ? e.code : `DISPUTE_START_EVIDENCE_INVALID:${e._tag}`,
    }));
  });
type BookRemoval = {
  readonly books: OrderbookExt["books"] | undefined;
  readonly removed: number;
  readonly remote: readonly string[];
  readonly outputs: readonly EntityOutput[];
};
/** og removeBookOrderById on this Entity's own book; an order no book holds is already gone. */
const removeLocalOrder = (acc: BookRemoval, orderId: string): Result<BookRemoval, EntityError> => {
  const books = acc.books;
  if (books === undefined) return ok(acc);
  const pairs = pairsHolding(books, orderId);
  if (pairs.length > 1) return halt(`ORDERBOOK_DUPLICATE_BOOK_ORDER: order=${orderId} matches=${pairs.length}`);
  const [pairId] = pairs, book = pairId === undefined ? undefined : books.get(pairId);
  const order = book?.orders.get(orderId);
  if (pairId === undefined || book === undefined || order === undefined) return ok(acc);
  const r = applyBookCommand(book, { kind: 1, ownerId: order.ownerId, orderId });
  if (!r.ok) return halt(r.error.code);
  return ok({ ...acc, books: mapSet(books, pairId, r.value.state), removed: acc.removed + 1 });
};
/** og removeCrossJurisdictionBookOrder to the Entity that owns the cross-j order's book. */
const remoteRemoval = (
  d: Draft, peer: string, offerId: string, route: CrossRoute, owner: string, timestamp: bigint,
): Result<EntityOutput, EntityError> => {
  const signerId = crossRouteSigner(route, owner);
  if (!signerId) {
    const at = `order=${offerId} owner=${owner} source=${route.source.entityId}`;
    return halt(`DISPUTE_CROSS_J_BOOK_OWNER_SIGNER_MISSING: ${at}`);
  }
  const data = {
    orderId: offerId,
    sourceEntityId: route.source.entityId,
    sourceAccountId: peer,
    route,
    reason: "account_dispute_prepare",
  };
  const tx = { type: "removeCrossJurisdictionBookOrder", data } as EntityTx;
  return ok(cjOutput(d.state, { kind: "cross", entityId: owner, signerId, txs: [tx] }, timestamp));
};
/**
 * og removeDisputedAccountOrdersFromBook / removeOrderbookRowForDispute: every resting order of the frozen Account (in
 * og's radix key order) leaves this Entity's book (og removeBookOrderById: a same-j order under `${peer}:${offerId}`,
 * a cross-j order this Entity owns the book of under `${source}:${offerId}`); a cross-j order whose book another
 * Entity owns becomes og's removeCrossJurisdictionBookOrder output to that owner.
 */
type RestingOffer = { readonly crossJurisdiction?: CrossRoute | undefined };
const disputeBookRemoval = (
  d: Draft, peer: string, offers: ReadonlyMap<string, RestingOffer>, timestamp: bigint,
): Result<{ readonly draft: Draft; readonly remote: readonly string[] }, EntityError> => {
  const self = lower(d.state.id);
  type Resting = readonly [string, RestingOffer];
  const removeOffer = (acc: BookRemoval, [offerId, offer]: Resting): Result<BookRemoval, EntityError> => {
    const route = offer.crossJurisdiction;
    if (route === undefined) return removeLocalOrder(acc, swapKeyOf(peer, offerId));
    const owner = lower(route.bookOwnerEntityId || route.source.counterpartyEntityId || route.hubEntityId || "");
    if (owner === self) return removeLocalOrder(acc, swapKeyOf(lower(route.source.entityId), offerId));
    return map(remoteRemoval(d, peer, offerId, route, owner, timestamp), (output) =>
      ({ ...acc, remote: [...acc.remote, offerId], outputs: [...acc.outputs, output] }));
  };
  const start: BookRemoval = { books: d.state.orderbookExt?.books, removed: 0, remote: [], outputs: [] };
  const swept = foldResult([...offers].toSorted(([a], [b]) => radixOrder(a, b)), start, removeOffer);
  return map(swept, ({ books, removed, remote, outputs }) => {
    const withOutputs: Draft = { ...d, outputs: [...d.outputs, ...outputs] };
    if (removed === 0 && remote.length === 0) return { draft: withOutputs, remote };
    const ext = d.state.orderbookExt;
    const state = removed === 0 || ext === undefined || books === undefined
      ? d.state
      : { ...d.state, orderbookExt: { ...ext, books } };
    const message = `⚔️ Dispute removed ${removed} local orderbook row(s), `
      + `queued ${remote.length} remote row removal(s)`;
    return { draft: withStatus({ ...withOutputs, state }, message), remote };
  });
};
/** The start intent a preparing Account retained, else og's default. */
const retainedIntent = (child: PreparingAccount): StartIntent =>
  child.prepare?.startIntent ?? { description: "prepare-dispute" };
type PrepareDisputeData = Extract<EntityTx, { type: "prepareDispute" }>["data"];
/**
 * og planCrossJurisdictionTargetRecovery, removeDisputedAccountOrdersFromBook (local rows, remote removal requests),
 * then the Account frozen into dispute_preparing, starting at once when its evidence is already stable.
 */
const freezeForDispute = (
  d: Draft, child: LiveAccount, x: PrepareDisputeData, ctx: FoldContext, supplied: Readonly<Record<string, string>>,
): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId, tag = peer.slice(-4), now = Number(ctx.timestamp);
  const description = x.description ?? "prepare-dispute";
  const cj = cjOf(d, ctx.timestamp, ctx.runtimeSeed), account = cj.host.accounts.get(lc(peer));
  if (account === undefined) return invariant(`PREPARE_DISPUTE_ACCOUNT_MISSING:${peer}`);
  const cleared = chain(planTargetRecovery(cj.host, account, peer, supplied), (plan) =>
    map(disputeBookRemoval(d, peer, child.state.offers, ctx.timestamp), (removal) => ({ plan, ...removal })));
  return chain(cleared, ({ plan, draft, remote }) => {
    const recovery = plan?.recovery, routeId = x.crossJurisdictionRouteId ?? plan?.representativeRouteId;
    const startIntent: StartIntent = {
      description,
      ...opt("crossJurisdictionRouteId", routeId),
      ...(recovery === undefined ? opt("starterInitialArguments", x.starterInitialArguments) : {}),
    };
    const prepare: DisputePrepare = {
      startedAt: now,
      readyAfter: now + Math.max(0, Math.floor(x.minCooldownMs ?? 0)),
      reason: description || "prepare-dispute",
      ...(remote.length > 0 ? { pendingOrderbookRemovalIds: remote.toSorted() } : {}),
      ...opt("crossJurisdictionRecovery", recovery),
      startIntent,
    };
    const frozen = prepareFrozen(child, prepare, { _tag: "not_attempted" }), issues = disputeIssues(frozen, now);
    const readiness = issues.length > 0
      ? `waiting for stable evidence: ${issues.join("; ")}`
      : "evidence currently stable, queue disputeStart when ready";
    const prepared = withStatus({ ...draft, ...putChild(draft.state, draft.accountReplicas, peer, frozen) },
      `⏳ Dispute prepared vs ${tag}; ${readiness}`);
    const reopened = (): Draft => {
      const active = reopen(frozen, { state: frozen.state, head: frozen.head, mempool: frozen.mempool });
      return withStatus({ ...prepared, ...putChild(prepared.state, prepared.accountReplicas, peer, active) },
        "🌉 Cross-j source finality required no target dispute");
    };
    switch (true) {
      case issues.length > 0: return ok(prepared);
      // og finishDisputePreparation: a recovery whose every source result is `0x` needs no target dispute
      case recovery !== undefined && recovery.requiredPullIds.every((id) => recovery.resultsByPullId[id] === "0x"):
        return ok(reopened());
      default: return startDispute(prepared, peer, startIntent, ctx);
    }
  });
};
/**
 * og handlePrepareDispute: status messages for a missing / already disputed / still preparing Account; otherwise
 * freeze into dispute_preparing and start once ready.
 */
const prepareDispute = (
  d: Draft, x: PrepareDisputeData, ctx: FoldContext, supplied: Readonly<Record<string, string>> = {},
): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId, tag = peer.slice(-4), child = d.accountReplicas.get(peer);
  if (child === undefined) return ok(withStatus(d, `❌ No account with ${tag} - cannot prepare dispute`));
  switch (child._tag) {
    case "disputed": return ok(withStatus(d, `ℹ️ Dispute already active/queued for ${tag}`));
    case "preparing": {
      const issues = disputeIssues(child, Number(ctx.timestamp));
      return issues.length === 0
        ? startDispute(d, peer, retainedIntent(child), ctx)
        : ok(withStatus(d, `⏳ Dispute preparation still pending for ${tag}: ${issues.join("; ")}`));
    }
    default: return freezeForDispute(d, child, x, ctx, supplied);
  }
};
/**
 * og draftPreparedDisputeStartIfReady: a preparing Account with no readiness issue starts with its retained start
 * intent.
 */
const draftPreparedStart = (d: Draft, peer: EntityId, ctx: FoldContext): Result<Draft, EntityError> => {
  const child = d.accountReplicas.get(peer);
  const ready = child?._tag === "preparing" && disputeIssues(child, Number(ctx.timestamp)).length === 0;
  return ready ? startDispute(d, peer, retainedIntent(child), ctx) : ok(d);
};
type BookRemovedData = Extract<EntityTx, { type: "crossJurisdictionBookOrderRemoved" }>["data"];
/**
 * og handleCrossJurisdictionBookOrderRemovedEntityTx's dispute branch: after the route checks, a preparing Account
 * waiting on this removal confirms it (og confirmDisputeBookRemoval) and starts once ready. undefined: not a pending
 * dispute removal, the book lifecycle handles the ACK.
 */
const disputeRemovalAck = (
  d: Draft, data: BookRemovedData, ctx: FoldContext,
): Result<Draft, EntityError> | undefined => {
  const route = canonicalCrossRoute(data.route);
  if (!route.ok) return undefined;
  const id = route.value.orderId, current = d.state.crossJurisdictionSwaps?.get(id);
  const ours = entityRef(d.state.id) === entityRef(route.value.source.counterpartyEntityId)
    && current !== undefined && entityRef(current.routeHash || "") === entityRef(route.value.routeHash || "");
  const accountId = data.sourceAccountId as EntityId, child = d.accountReplicas.get(accountId);
  const prepare = child?._tag === "preparing" ? child.prepare : undefined;
  const pending = prepare?.pendingOrderbookRemovalIds;
  if (!ours || child?._tag !== "preparing" || prepare === undefined || pending === undefined || !pending.includes(id)) {
    return undefined;
  }
  const rest = pending.filter((o) => o !== id), { pendingOrderbookRemovalIds: _drop, ...settled } = prepare;
  const confirmed: PreparingAccount = {
    ...child,
    prepare: rest.length > 0 ? { ...settled, pendingOrderbookRemovalIds: rest } : settled,
  };
  const next = withStatus({ ...d, ...putChild(d.state, d.accountReplicas, accountId, confirmed) },
    `🌉 Cross-j dispute book removal confirmed ${id}`);
  return draftPreparedStart(next, accountId, ctx);
};
// ---- og entity/tx/handlers/dispute/{finalize,finalize-admission,finalize-proof}.ts: disputeFinalize ----
/**
 * og AccountReplica.activeDispute: the observed on-chain dispute, or our queued start before DisputeStarted is
 * observed.
 */
const activeOf = (c: AccountReplica): ActiveDispute | QueuedDispute | undefined =>
  (c._tag === "disputed" ? c.active ?? c.queued : undefined);
/** og replaceDisputeLifecycle with `{ ...activeDispute, finalizeQueued }`. */
const latchFinalize = (
  f: Folded,
  peer: string,
  child: DisputedAccount,
  active: ActiveDispute,
  finalizeQueued: boolean,
): Folded =>
  putChild(f.state, f.accountReplicas, peer as EntityId, { ...child, active: { ...active, finalizeQueued } });
/** og encodedHexBytes + assertDisputeArgumentsWithinContractLimits for one argument blob. */
const disputeArgumentsIssue = (value: string, context: string): string | undefined => {
  const bytes = (value.length - 2) / 2;
  switch (true) {
    case !EVEN_HEX.test(value): return `J_DISPUTE_HEX_INVALID:${context}[0]`;
    case bytes > MAX_DISPUTE_ARGUMENT_BYTES:
      return `J_DISPUTE_ARGUMENT_BYTES_EXCEEDED:${context}:${bytes}/${MAX_DISPUTE_ARGUMENT_BYTES}`;
    default: return undefined;
  }
};
type FinalizeIntent = Extract<EntityTx, { type: "disputeFinalize" }>["data"];
/**
 * og selectFinalProof's counter-proof: the one Solidity selected, else a newer counterparty proof the non-starter
 * holds a Hanko for; none means the initial unilateral proof. A selected body not yet local is og's wait message.
 */
const finalCounter = (
  active: ActiveDispute,
  w: DisputeHanko | undefined,
  callerIsStarter: boolean,
): Result<FinalCounter | undefined, string> => {
  const {
    selectedCounterNonce: nonce,
    selectedCounterProposerIsLeft: left,
    selectedCounterProofbodyHash: hash,
  } = active;
  if (nonce !== undefined) {
    return typeof left !== "boolean" || !hash
      ? err(`⏳ Selected counter-proof N${nonce} body is not locally available yet`)
      : ok({ nonce, proposerIsLeft: left, hash, hanko: "0x" });
  }
  if (callerIsStarter || w === undefined) return ok(undefined);
  const signed = w.hanko !== "" && w.hanko !== "0x" && w.proofBodyHash !== "";
  const newer =
    w.proofNonce > active.initialNonce ||
    (w.proofNonce === active.initialNonce && w.proposerIsLeft && !active.initialProposerIsLeft);
  return ok(
    signed && newer
      ? { nonce: w.proofNonce, proposerIsLeft: w.proposerIsLeft, hash: w.proofBodyHash, hanko: w.hanko }
      : undefined,
  );
};
/** The frozen Account's proof, which the final proof body must be. */
const frozenFinalProof = (
  child: DisputedAccount, entityId: string, peer: EntityId, ctx: FoldContext,
): Result<{ readonly view: CommittedAccountState; readonly proof: LocalProof }, EntityError> => {
  const invalid: EntityError = {
    _tag: "entity_invariant",
    reason: `DISPUTE_FINALIZE_PROOFBODY_INVALID: entity=${entityId} counterparty=${peer} source=current`,
  };
  return chain(mapErr(committedView(child.state), () => invalid), (view) =>
    map(mapErr(localProof(view, accountDt(ctx, child)), () => invalid), (proof) => ({ view, proof })));
};
/**
 * og handleDisputeFinalize: admission (jBatchState seeded, an observed and not yet queued dispute, an already queued
 * finalization latches finalizeQueued), og selectFinalProof (a selected or newer counterparty proof, else the initial
 * unilateral one, against the frozen proof body), the cross-j Target recovery readiness, verifyCounterProofIdentity,
 * buildFinalProofPayload (starter arguments by the counter-proof commitment), the registry secrets
 * (useOnchainRegistry), og's Account.sol timing gate (Pulls wait for T), the hash-ladder reveal deferral (flush,
 * j_broadcast, deadline hook), then the revealSecret rows, the DisputeFinalization row and the Account's
 * finalizeQueued latch. og's throws are fatal.
 */
const finalizeDispute = (d: Draft, x: FinalizeIntent, ctx: FoldContext): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId;
  const { jb, admitted } = seededBatch(d, "disputeFinalize");
  const gate = finalizeGate(admitted, jb, peer);
  if (gate._tag === "answered") return ok(gate.draft);
  const { child, active } = gate;
  return chain(frozenFinalProof(child, d.state.id, peer, ctx), ({ view, proof }) => {
    const selfLeft = sameHex(child.state.account.id.left, d.state.id);
    const callerIsStarter = selfLeft === active.startedByLeft;
    const selected = finalCounter(active, child.dispute.counterparty, callerIsStarter);
    if (!selected.ok) return ok(withStatus(admitted, selected.error));
    const counter = selected.value;
    const finalNonce = counter?.nonce ?? active.initialNonce;
    const proposerIsLeft = counter?.proposerIsLeft ?? active.initialProposerIsLeft;
    const finalHash = counter?.hash ?? active.initialProofbodyHash;
    if (finalNonce <= 0) return ok(withStatus(admitted, `❌ Invalid dispute finalNonce=${finalNonce} — must be > 0`));
    if (!sameHex(proof.bodyHash, finalHash)) {
      return invariant(`DISPUTE_FROZEN_ACCOUNT_STATE_MISMATCH:finalize:${peer}:${finalHash}:${proof.bodyHash}`);
    }
    const sel: FinalSelection = {
      view,
      proof,
      counter,
      finalNonce,
      proposerIsLeft,
      finalHash,
      callerIsStarter,
      selfLeft,
    };
    // og selectedCrossJurisdictionRecoveryIsReady: missing source results before T wait (hook + message)
    return chain(recoveryReadiness(admitted, peer, child, active, ctx), ({ draft, waiting }) =>
      waiting ? ok(draft) : finalizeReady(draft, x, ctx, sel),
    );
  });
};
/** og seeds jBatchState before admission; an op queued while a sent batch is pending joins the current draft batch. */
const seededBatch = (d: Draft, op: string): { readonly jb: CommittedJBatch; readonly admitted: Draft } => {
  const jb = committedJBatch(d.state) ?? (initJBatch() as unknown as CommittedJBatch);
  const seeded: Draft = {
    ...d,
    state: { ...d.state, committed: { ...d.state.committed, jBatchState: jb as unknown as Binary } },
  };
  const admitted =
    jb.sentBatch === undefined
      ? seeded
      : withStatus(
          seeded,
          `ℹ️ ${op} queued to current batch while sentBatch nonce=${jb.sentBatch.entityNonce} is still pending`,
        );
  return { jb, admitted };
};
type FinalizeGate =
  | Tagged<"answered", { draft: Draft }>
  | Tagged<"admitted", { child: DisputedAccount; active: ActiveDispute }>;
/** og finalize admission: an observed dispute not yet queued; one already in the batch lifecycle only latches. */
const finalizeGate = (admitted: Draft, jb: CommittedJBatch, peer: EntityId): FinalizeGate => {
  const tag = peer.slice(-4);
  const child = admitted.accountReplicas.get(peer);
  const disputed = child?._tag === "disputed" ? child : undefined;
  const active = disputed?.active ?? disputed?.queued;
  const answer = (message: string, d: Draft = admitted): FinalizeGate => ({
    _tag: "answered",
    draft: withStatus(d, message),
  });
  if (child === undefined) return answer(`❌ No account with ${tag} - cannot finalize dispute`);
  if (disputed === undefined || active === undefined)
    return answer(`❌ No active dispute with ${tag} - must call disputeStart first`);
  if (active.observedOnChain !== true) {
    return answer(`⏳ disputeFinalize blocked until DisputeStarted is observed on-chain for ${tag}`);
  }
  if (active.finalizeQueued) return answer(`ℹ️ disputeFinalize already queued for ${tag} (awaiting batch lifecycle)`);
  if (queuedDisputeOp(jb, peer, "disputeFinalizations")) {
    const latched: Draft = { ...admitted, ...latchFinalize(admitted, peer, disputed, active, true) };
    return answer(`ℹ️ disputeFinalize already present in batch lifecycle for ${tag}`, latched);
  }
  return { _tag: "admitted", child: disputed, active };
};
type FinalCounter = {
  readonly nonce: number;
  readonly proposerIsLeft: boolean;
  readonly hash: string;
  readonly hanko: string;
};
/** The proof finalize submits, and who submits it. */
type FinalSelection = {
  readonly view: CommittedAccountState;
  readonly proof: CompleteProof;
  readonly counter: FinalCounter | undefined;
  readonly finalNonce: number;
  readonly proposerIsLeft: boolean;
  readonly finalHash: string;
  readonly callerIsStarter: boolean;
  readonly selfLeft: boolean;
};
/**
 * og selectedCrossJurisdictionRecoveryIsReady on the Draft: `waiting` when missing source results keep finalize back
 * before T.
 */
type Readiness = { readonly draft: Draft; readonly waiting: boolean };
const recoveryReadiness = (
  d: Draft,
  peer: EntityId,
  child: DisputedAccount,
  active: ActiveDispute,
  ctx: FoldContext,
): Result<Readiness, EntityError> => {
  const current = active.crossJurisdictionRecovery;
  if (current === undefined) return ok({ draft: d, waiting: false });
  const cj = cjOf(d, ctx.timestamp, ctx.runtimeSeed);
  const account = cj.host.accounts.get(lc(peer));
  if (account === undefined) return invariant(`DISPUTE_FINALIZE_ACTIVE_ACCOUNT_MISSING:${peer}`);
  return chain(refreshTargetRecovery(cj.host, account, peer, current), (plan): Result<Readiness, EntityError> => {
    const { crossJurisdictionRecovery: _old, ...rest } = active;
    const nextActive: ActiveDispute = plan === null ? rest : { ...rest, crossJurisdictionRecovery: plan.recovery };
    const updated: Draft = { ...d, ...putChild(d.state, d.accountReplicas, peer, { ...child, active: nextActive }) };
    const missing =
      plan === null
        ? []
        : plan.recovery.requiredPullIds.filter((id) => !Object.hasOwn(plan.recovery.resultsByPullId, id));
    const timeoutSec = Number(active.disputeTimeout || 0);
    const nowSec = Math.floor(Number(ctx.timestamp) / 1000);
    if (missing.length === 0 || (timeoutSec > 0 && nowSec >= timeoutSec)) return ok({ draft: updated, waiting: false });
    const message =
      `⏳ disputeFinalize waiting for ${missing.length} cross-j source result(s) required by the selected proof ` +
      `(nowSec=${nowSec}, timeoutSec=${timeoutSec})`;
    return map(scheduleDisputeDeadline(updated.state, peer, Number(ctx.timestamp) + 1000), (state) => ({
      draft: withStatus({ ...updated, state }, message),
      waiting: true,
    }));
  });
};
/** og isUsableContractAddress: a well-formed, non-zero address. */
const usableContract = (a: string): boolean => /^0x[0-9a-fA-F]{40}$/.test(a) && !/^0x0{40}$/.test(a);
/**
 * og verifyCounterProofIdentity: the peer's stored dispute hash must be the counter-proof's hash under the Entity's
 * own depository domain.
 */
const counterIdentity = (
  state: EntityState,
  w: DisputeHanko | undefined,
  sel: FinalSelection,
  peer: EntityId,
): Result<void, EntityError> => {
  if (sel.counter === undefined || w === undefined) return ok(undefined);
  if (state.jurisdictionConfig === undefined) return invariant("DISPUTE_COUNTER_FINALIZE_DEPOSITORY_MISSING");
  const expected = accountDisputeHash(
    { ...sel.view, domain: state.jurisdiction },
    sel.finalHash,
    sel.finalNonce,
    sel.proposerIsLeft,
  );
  if (!expected.ok) return invariant("DISPUTE_COUNTER_FINALIZE_DEPOSITORY_MISSING");
  return sameHex(w.hash, expected.value)
    ? ok(undefined)
    : invariant(`DISPUTE_COUNTER_FINALIZE_HASH_MISMATCH:${peer}:${w.hash}:${expected.value}`);
};
type FinalArguments = { readonly starterArguments: string; readonly otherArguments: string };
/**
 * og buildFinalProofPayload: the starter's side as precommitted (its counter arguments only for the committed
 * counter-proof); the non-starter submits its own side's arguments (with its known secrets).
 */
const finalArguments = (
  d: Draft,
  child: DisputedAccount,
  active: ActiveDispute,
  sel: FinalSelection,
  peer: EntityId,
): Result<FinalArguments, EntityError> => {
  const commitment = (): string =>
    keccak256Hex(
      abiEncode([A.uint(BigInt(sel.finalNonce)), A.bool(sel.proposerIsLeft), A.b32(sel.finalHash.toLowerCase())]),
    );
  const starterArguments = (): string => {
    switch (true) {
      case sel.counter === undefined:
        return active.starterInitialArguments;
      case sameHex(commitment(), active.starterCounterProofCommitment):
        return active.starterCounterArguments;
      default:
        return "0x";
    }
  };
  const built = sel.callerIsStarter
    ? ok({ left: "0x", right: "0x" })
    : builtDisputeArguments(child, peer, d.state.paybook, sel.selfLeft ? "left" : "right");
  return chain(built, (b) => {
    const nonStarterSide = active.startedByLeft ? b.right : b.left;
    const args = { starterArguments: starterArguments(), otherArguments: sel.callerIsStarter ? "0x" : nonStarterSide };
    const issue =
      disputeArgumentsIssue(args.starterArguments, "disputeFinalize.starterArguments") ??
      disputeArgumentsIssue(args.otherArguments, "disputeFinalize.otherArguments");
    return issue === undefined ? ok(args) : invariant(issue);
  });
};
type Registry = { readonly secrets: readonly string[]; readonly transformer: string };
/**
 * og collectRegistryPublication: useOnchainRegistry publishes every known secret under the Account's DeltaTransformer.
 */
const registrySecrets = (
  useRegistry: boolean | undefined,
  view: CommittedAccountState,
  paybook: Paybook | undefined,
  peer: string,
  dt: DeltaTransformerRef,
): Result<Registry, EntityError> => {
  const secrets = useRegistry ? knownDisputeSecrets(view, paybook, peer) : [];
  if (secrets.length === 0) return ok({ secrets, transformer: "" });
  if (!dt.ok) return halt(dt.error);
  return usableContract(dt.value)
    ? ok({ secrets, transformer: dt.value })
    : halt("DISPUTE_FINALIZE_MISSING_DELTA_TRANSFORMER_ADDRESS");
};
/** When finalize may submit: `notBefore` T (Account.sol waits), null for at once; otherwise og's status line. */
type SubmitTiming = Tagged<"submit", { notBefore: number | null }> | Tagged<"wait", { message: string }>;
/**
 * og resolveFinalizeSubmitNotBefore (Account.sol): a Pull counter-proof must be locked on-chain; Pulls, the starter
 * and a selected counter-proof wait for T; the non-starter may accept the starter's pull-free state at once.
 */
const submitTiming = (
  hasPulls: boolean,
  sel: FinalSelection,
  selected: boolean,
  timeoutSec: number,
  nowSec: number,
): SubmitTiming => {
  const submit = (notBefore: number | null): SubmitTiming => ({ _tag: "submit", notBefore });
  const wait = (message: string): SubmitTiming => ({ _tag: "wait", message });
  const clock = `nowSec=${nowSec}, timeoutSec=${timeoutSec}`;
  switch (true) {
    case hasPulls && sel.counter !== undefined && !selected:
      return wait("⏳ Pull counter-proof must be locked on-chain before finalization");
    case timeoutSec > 0 && nowSec >= timeoutSec:
      return submit(hasPulls || sel.callerIsStarter || selected ? timeoutSec : null);
    case selected:
      return wait(`❌ selected counter-proof cannot finalize before timeout: ${clock}`);
    case !hasPulls && !sel.callerIsStarter:
      return submit(null);
    default:
      return wait(`❌ disputeFinalize too early: ${clock}${hasPulls ? ", pulls=yes" : ", starter-unilateral"}`);
  }
};
/**
 * og deferFinalizeForPendingReveals: hash-ladder reveals never share a batch with a finalization; they broadcast
 * first and the dispute deadline hook comes back for it.
 */
const deferFinalize = (
  flushed: Draft,
  host: CjHost,
  pending: number,
  peer: EntityId,
  timeoutSec: number,
  ctx: FoldContext,
): Result<Draft, EntityError> => {
  const signer = rootConfig(flushed.state).validators[0];
  const nowMs = Number(ctx.timestamp);
  const timeoutMs = timeoutSec > 0 ? timeoutSec * 1000 : nowMs + 1000;
  const broadcast = signer
    ? cjInto(flushed, { host, messages: [], outputs: [{ kind: "j_broadcast", signerId: signer }] }, ctx.timestamp)
    : flushed;
  return map(scheduleDisputeDeadline(broadcast.state, peer, Math.min(nowMs + 1000, timeoutMs)), (state) =>
    withStatus(
      { ...broadcast, state },
      `⏳ disputeFinalize deferred: ${pending} hashLadderReveal(s) must broadcast first`,
    ),
  );
};
const batchOps = (jb: CommittedJBatch): number => BATCH_FIELDS.reduce((n, f) => n + (jb.batch[f] ?? []).length, 0);
const finalizationFits = (jb: CommittedJBatch): Result<void, EntityError> => {
  const rows = (jb.batch["disputeFinalizations"] ?? []).length, total = batchOps(jb);
  const { maxDisputeFinalizations, maxTotalOps } = J_BATCH_LIMITS;
  switch (true) {
    case rows >= maxDisputeFinalizations:
      return invariant(`J_BATCH_LIMIT_EXCEEDED: disputeFinalizations ${rows + 1}/${maxDisputeFinalizations}`);
    case total + 1 > maxTotalOps:
      return invariant(`J_BATCH_LIMIT_EXCEEDED: disputeFinalize would exceed total ops ${total + 1}/${maxTotalOps}`);
    default: return ok(undefined);
  }
};
/** og batchAddRevealSecret: an exact duplicate is skipped; the batch's total ops and reveal count stay bounded. */
const addRevealSecret = (jb: CommittedJBatch, transformer: string) =>
  (reveals: readonly Binary[], secret: string): Result<readonly Binary[], EntityError> => {
    const row = (r: Binary) => r as { readonly transformer?: unknown; readonly secret?: unknown };
    if (reveals.some((r) => row(r).transformer === transformer && row(r).secret === secret)) return ok(reveals);
    const ops = batchOps({ ...jb, batch: { ...jb.batch, revealSecrets: reveals } }) + 1;
    const { maxTotalOps, maxSecretReveals } = J_BATCH_LIMITS;
    switch (true) {
      case ops > maxTotalOps:
        return invariant(`J_BATCH_LIMIT_EXCEEDED: revealSecret would exceed total ops ${ops}/${maxTotalOps}`);
      case reveals.length + 1 > maxSecretReveals:
        return invariant(`J_BATCH_LIMIT_EXCEEDED: revealSecrets ${reveals.length + 1}/${maxSecretReveals}`);
      default: return ok([...reveals, { transformer, secret } as unknown as Binary]);
    }
  };
/** og DisputeFinalization row. */
const finalizationRow = (
  peer: EntityId, active: ActiveDispute, sel: FinalSelection, args: FinalArguments, notBefore: number | null,
): Binary => ({
  counterentity: peer,
  initialNonce: active.initialNonce,
  finalNonce: sel.finalNonce,
  proposerIsLeft: sel.proposerIsLeft,
  initialProofbodyHash: active.initialProofbodyHash,
  finalProofbody: ogProofBody(sel.proof.body),
  starterArguments: args.starterArguments,
  otherArguments: args.otherArguments,
  sig: sel.counter?.hanko ?? "0x",
  startedByLeft: active.startedByLeft,
  cooperative: false,
  ...(notBefore === null ? {} : { submitNotBeforeTimestamp: notBefore }),
});
/**
 * The revealSecret rows and the DisputeFinalization row in the draft batch, then the Account's finalizeQueued latch.
 */
const queueFinalization = (
  flushed: Draft,
  jb: CommittedJBatch,
  peer: EntityId,
  registry: Registry,
  row: Binary,
  x: FinalizeIntent,
): Result<Draft, EntityError> => {
  const held = jb.batch["revealSecrets"] ?? [];
  const reveals = chain(finalizationFits(jb), () =>
    foldResult(registry.secrets, held, addRevealSecret(jb, registry.transformer)),
  );
  return map(reveals, (revealSecrets) => {
    const accumulating =
      revealSecrets.length > held.length && (jb as { readonly status?: string }).status === "empty"
        ? { status: "accumulating" }
        : {};
    const disputeFinalizations = [...(jb.batch["disputeFinalizations"] ?? []), row];
    const jBatchState = {
      ...jb,
      ...accumulating,
      batch: { ...jb.batch, revealSecrets, disputeFinalizations },
    } as unknown as Binary;
    const latest = flushed.accountReplicas.get(peer) as DisputedAccount;
    const latestActive = activeOf(latest) as ActiveDispute;
    const state = { ...flushed.state, committed: { ...flushed.state.committed, jBatchState } };
    const queued = latchFinalize({ state, accountReplicas: flushed.accountReplicas }, peer, latest, latestActive, true);
    const note = x.description ? `(${x.description})` : "";
    return withStatus(
      { ...flushed, ...queued },
      `⚖️ Dispute finalized vs ${peer.slice(-4)} ${note} - use jBroadcast to commit`,
    );
  });
};
/**
 * og handleDisputeFinalize after the recovery readiness: identity, payload, registry, timing, reveal deferral, queue,
 * latch.
 */
const finalizeReady = (
  d: Draft,
  x: FinalizeIntent,
  ctx: FoldContext,
  sel: FinalSelection,
): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId;
  const child = d.accountReplicas.get(peer) as DisputedAccount;
  const active = activeOf(child) as ActiveDispute;
  const dt = accountDt(ctx, child);
  const prepared = all({
    identity: counterIdentity(d.state, child.dispute.counterparty, sel, peer),
    args: finalArguments(d, child, active, sel, peer),
    registry: registrySecrets(x.useOnchainRegistry, sel.view, d.state.paybook, peer, dt),
    transformer: dt.ok ? ok(dt.value) : halt(dt.error),
  });
  return chain(prepared, ({ args, registry, transformer }) =>
    chain(proofBodyHasPulls(sel.proof.body, transformer), (hasPulls) => {
      const timeoutSec = Number(active.disputeTimeout || 0);
      const nowSec = Math.floor(Number(ctx.timestamp) / 1000);
      const timing = submitTiming(hasPulls, sel, active.selectedCounterNonce !== undefined, timeoutSec, nowSec);
      if (timing._tag === "wait") return ok(withStatus(d, timing.message));
      const row = finalizationRow(peer, active, sel, args, timing.notBefore);
      return chain(flushDeferredReveals(cjOf(d, ctx.timestamp, ctx.runtimeSeed).host, undefined), ({ host }) => {
        const flushed = cjInto(d, { host, messages: [], outputs: [] }, ctx.timestamp);
        const jb = committedJBatch(flushed.state) ?? (initJBatch() as unknown as CommittedJBatch);
        const pending = (jb.batch["hashLadderRegistrations"] ?? []).length + countDeferredReveals(host);
        return pending > 0
          ? deferFinalize(flushed, host, pending, peer, timeoutSec, ctx)
          : queueFinalization(flushed, jb, peer, registry, row, x);
      });
    }),
  );
};
// ---- og entity/tx/j-events-htlc/index.ts: the hash-ladder reveal queue, Source hub claims, reveal ports, Target recovery, sibling dispute fanout ----
type CjRows = { readonly [field: string]: readonly Binary[] };
/** The committed og jBatchState fields the reveal queue reads and writes. */
export type CjJBatch = {
  readonly batch: CjRows;
  readonly sentBatch?: { readonly batch: CjRows; readonly entityNonce: number } | undefined;
  readonly recoveryBatches?: readonly CjRows[] | undefined;
  readonly status?: string | undefined;
  readonly autoBroadcastDraft?: boolean | undefined;
};
/**
 * One Account as og's helpers read it: its sides, its bilateral response windows, and its activeDispute (queued or
 * observed).
 */
export type CjAccount = {
  readonly left: string;
  readonly right: string;
  readonly leftResponseSeconds: number;
  readonly rightResponseSeconds: number;
  readonly active?:
    { readonly observedOnChain: boolean; readonly disputeStartTimestamp?: number | undefined } | undefined;
  /** og buildCurrentDisputeArgumentPlan's pull ids (left side: amount >= 0). */
  readonly leftPullIds: readonly string[];
  readonly rightPullIds: readonly string[];
};
/**
 * The og EntityState fields j-events-htlc reads (Accounts keyed by lowercase counterparty) and writes (the route
 * collection and jBatchState).
 */
export type CjHost = {
  readonly id: string;
  readonly timestamp: number;
  readonly validators: readonly string[];
  readonly runtimeSeed?: string | undefined;
  readonly swaps?: ReadonlyMap<string, CrossRoute> | undefined;
  readonly jb?: CjJBatch | undefined;
  readonly accounts: ReadonlyMap<string, CjAccount>;
};
/**
 * og outputs of these helpers: a self `j_broadcast` for validators[0], or a cross-j Entity output (wrapped as a
 * runtimeOutput at commit).
 */
export type CjOut =
  | { readonly kind: "j_broadcast"; readonly signerId: string }
  | { readonly kind: "cross"; readonly entityId: string; readonly signerId: string; readonly txs: readonly EntityTx[] };
export type Cj = { readonly host: CjHost; readonly messages: readonly string[]; readonly outputs: readonly CjOut[] };
export type CjStep<T> = Result<{ readonly cj: Cj; readonly value: T }, EntityError>;
export type LadderRevealResult = "queued" | "already-queued" | "deferred-batch-pending" | "source-window-expired";
export type LadderDecoded = {
  readonly fillRatio: number;
  readonly fullSecret?: string | undefined;
  readonly reveals?: Reveals | undefined;
};
const lc = (v: unknown): string => String(v || "").toLowerCase();
const cjSay = (cj: Cj, ...messages: readonly string[]): Cj => ({ ...cj, messages: [...cj.messages, ...messages] });
const cjHost = (cj: Cj, host: CjHost): Cj => ({ ...cj, host });
const cjBroadcast = (cj: Cj, signerId: string): Cj => ({ ...cj, outputs: [...cj.outputs, { kind: "j_broadcast", signerId }] });
/**
 * og PersistentEntityCollectionMap iteration: keys by their u16-length-prefixed UTF-8 bytes (length first, then bytes).
 */
const radixOrder = (a: string, b: string): number => {
  const x = utf8(a), y = utf8(b);
  return x.length - y.length || compareBytes(x, y);
};
const routeKeys = (h: CjHost): readonly string[] => [...(h.swaps?.keys() ?? [])].toSorted(radixOrder);
const routeEntries = (h: CjHost): readonly (readonly [string, CrossRoute])[] =>
  routeKeys(h).flatMap((key) => {
    const route = h.swaps?.get(key);
    return route === undefined ? [] : [[key, route] as const];
  });
const routesOf = (h: CjHost): readonly CrossRoute[] => routeEntries(h).map(([, route]) => route);
const cjPutRoute = (h: CjHost, key: string, route: CrossRoute): CjHost =>
  ({ ...h, swaps: mapSet(h.swaps ?? new Map<string, CrossRoute>(), key, route) });
const LADDER_ZERO = ZERO_WORD;
const LADDER_ZERO_REVEALS: Reveals = [ZERO_WORD, ZERO_WORD, ZERO_WORD, ZERO_WORD];
type LadderPull = { readonly fullHash: string; readonly partialRoot: string };
/** og ladderHashForPull: keccak256(solidityPacked(bytes32 fullHash, bytes32 partialRoot)), lowercase. */
export const pullLadderHash = (p: LadderPull): string =>
  keccak256Hex(concat([hexToBytes(p.fullHash), hexToBytes(p.partialRoot)])).toLowerCase();
/**
 * og hashLadderRegistrationKey: keccak256(solidityPacked(bytes32 counterpartyEntity, fullHash, partialRoot)),
 * lowercase.
 */
const ladderRegistrationKey = (r: LadderPull & { readonly counterpartyEntity: string }): string =>
  keccak256Hex(
    concat([hexToBytes(r.counterpartyEntity), hexToBytes(r.fullHash), hexToBytes(r.partialRoot)]),
  ).toLowerCase();
const roleName = (targetRole: boolean): string => (targetRole ? "target" : "source");
/** og counterpartyForRouteLeg: the other party on the role's leg, which must include us. */
const routeLegPeer = (h: CjHost, route: CrossRoute, targetRole: boolean): Result<string, EntityError> => {
  const self = lc(h.id), leg = targetRole ? route.target : route.source;
  const entity = lc(leg.entityId), counterparty = lc(leg.counterpartyEntityId);
  switch (self) {
    case entity: return ok(counterparty);
    case counterparty: return ok(entity);
    default: return invariant(`J_HASH_LADDER_ACCOUNT_PARTY_MISMATCH:${route.orderId}:${self}`);
  }
};
/**
 * og sourceRevealWindowStatus: a local unobserved start is open; an observed one is open through its start second plus
 * our own window.
 */
const sourceRevealWindow = (h: CjHost, counterparty: string): Result<"open" | "expired", EntityError> => {
  const cp = counterparty.toLowerCase();
  const account = h.accounts.get(cp);
  const active = account?.active;
  if (account === undefined || active === undefined)
    return invariant(`J_HASH_LADDER_SOURCE_ACTIVE_DISPUTE_MISSING:${cp}`);
  if (!active.observedOnChain) return ok("open");
  const startSec = Number(active.disputeStartTimestamp);
  if (!Number.isSafeInteger(startSec) || startSec < 0) {
    return invariant(`J_HASH_LADDER_SOURCE_DISPUTE_START_INVALID:${String(active.disputeStartTimestamp)}`);
  }
  const window = lc(account.left) === lc(h.id) ? account.leftResponseSeconds : account.rightResponseSeconds;
  const nowSec = Math.floor(Number(h.timestamp) / 1000);
  if (nowSec < startSec) return invariant(`J_HASH_LADDER_SOURCE_WINDOW_NOT_OPEN:${nowSec}:${startSec}`);
  return ok(nowSec > startSec + window ? "expired" : "open");
};
type PendingReveal = { readonly fillRatio: number; readonly fullSecret: string; readonly reveals: Reveals };
const pendingReveal = (d: LadderDecoded): PendingReveal =>
  ({ fillRatio: d.fillRatio, fullSecret: d.fullSecret ?? LADDER_ZERO, reveals: d.reveals ?? LADDER_ZERO_REVEALS });
/** Routes, by key, whose Pull for the role is on this ladder and whose leg for the role is our Account with `cp`. */
const ladderRoutes = (
  h: CjHost, cp: string, ladder: string, targetRole: boolean,
): Result<readonly (readonly [string, CrossRoute])[], EntityError> => {
  const onLadder = routeEntries(h).filter(([, route]) => {
    const pull = targetRole ? route.targetPull : route.sourcePull;
    return pull !== undefined && pullLadderHash(pull) === ladder;
  });
  return foldResult(onLadder, [] as readonly (readonly [string, CrossRoute])[], (found, row) =>
    map(routeLegPeer(h, row[1], targetRole), (peer) => (peer === cp ? [...found, row] : found)));
};
/**
 * og stashPendingRegistryReveal: Target always replaces its unsent reveal, Source keeps the first; every matching
 * route's updatedAt moves.
 */
const stashReveal = (
  h: CjHost, counterparty: string, pull: LadderPull, decoded: LadderDecoded, targetRole: boolean,
): Result<CjHost, EntityError> => {
  const ladder = pullLadderHash(pull), pending = pendingReveal(decoded);
  const stashed = (route: CrossRoute): CrossRoute => {
    const updatedAt = Number(h.timestamp || route.updatedAt || 0);
    switch (true) {
      case targetRole: return { ...route, pendingTargetRegistryReveal: pending, updatedAt };
      case route.pendingSourceRegistryReveal !== undefined: return { ...route, updatedAt };
      default: return { ...route, pendingSourceRegistryReveal: pending, updatedAt };
    }
  };
  return chain(ladderRoutes(h, String(counterparty).toLowerCase(), ladder, targetRole), (routes) => (routes.length === 0
    ? invariant(`J_HASH_LADDER_ROUTE_SLOT_MISSING:${h.id}:${ladder}:${roleName(targetRole)}`)
    : ok(routes.reduce((host, [key, route]) => cjPutRoute(host, key, stashed(route)), h))));
};
/**
 * og targetRevealHasActiveDispute: the Target witness is publishable once this Entity's Account with the target hub
 * has an activeDispute.
 */
const targetRevealLive = (h: CjHost, route: CrossRoute): boolean =>
  h.accounts.get(lc(route.target.entityId))?.active !== undefined;
/**
 * og countDeferredHashLadderReveals: witnesses not yet in an immutable sent batch (a Target one only once its dispute
 * defines S).
 */
export const countDeferredReveals = (h: CjHost): number => routesOf(h).reduce((n, r) => {
  const source = r.pendingSourceRegistryReveal ? 1 : 0;
  const target = r.pendingTargetRegistryReveal && targetRevealLive(h, r) ? 1 : 0;
  return n + source + target;
}, 0);
const cjRows = (rows: CjRows | undefined, field: string): readonly Binary[] => rows?.[field] ?? [];
const cjOps = (rows: CjRows): number => BATCH_FIELDS.reduce((n, f) => n + cjRows(rows, f).length, 0);
type LadderRow = LadderPull & {
  readonly counterpartyEntity: string;
  readonly targetRole: boolean;
  readonly witness: PendingReveal;
};
const ladderRows = (rows: CjRows | undefined): readonly LadderRow[] =>
  cjRows(rows, "hashLadderRegistrations") as unknown as readonly LadderRow[];
type RegistryRatios = {
  readonly jb: CjJBatch;
  readonly confirmed: readonly number[];
  readonly queued: readonly number[];
};
/**
 * og collectExistingRegistryRatios: confirmed and queued ratios for one (counterparty, ladder, role); seeds
 * jBatchState.
 */
const existingRegistryRatios = (
  h: CjHost,
  cp: string,
  ladder: string,
  targetRole: boolean,
): Result<RegistryRatios, EntityError> => {
  const jb = h.jb ?? (initJBatch() as unknown as CjJBatch);
  return map(ladderRoutes(h, cp, ladder, targetRole), (routes) => {
    const confirmed = routes.flatMap(
      ([, r]) => (targetRole ? r.targetRegistryFillRatio : r.sourceRegistryFillRatio) ?? [],
    );
    const stashed = routes
      .flatMap(([, r]) => (targetRole ? r.pendingTargetRegistryReveal : r.pendingSourceRegistryReveal) ?? [])
      .map((p) => p.fillRatio);
    const sameSlot = (r: LadderRow): boolean =>
      r.targetRole === targetRole && lc(r.counterpartyEntity) === cp && pullLadderHash(r) === ladder;
    const batched = [jb.batch, jb.sentBatch?.batch, ...(jb.recoveryBatches ?? [])].flatMap((rows) =>
      ladderRows(rows)
        .filter(sameSlot)
        .map((r) => r.witness.fillRatio),
    );
    return { jb, confirmed, queued: [...stashed, ...batched] };
  });
};
const sameLadderSlot = (a: LadderRow, b: LadderRow): boolean =>
  a.targetRole === b.targetRole && ladderRegistrationKey(a) === ladderRegistrationKey(b);
/** og hasHashLadderRegistrationRoom: replacing an existing slot is free; a new slot needs array and total-op room. */
const ladderRoom = (rows: CjRows, r: LadderRow): boolean =>
  ladderRows(rows).some((e) => sameLadderSlot(e, r))
  || (ladderRows(rows).length < J_BATCH_LIMITS.maxHashLadderRegistrations && cjOps(rows) < J_BATCH_LIMITS.maxTotalOps);
/**
 * og batchAddHashLadderRegistration + upsertHashLadderRegistration: an exact retry is a no-op, a Source change or a
 * Target regression conflicts.
 */
const upsertLadder = (jb: CjJBatch, r: LadderRow): Result<CjJBatch, EntityError> => {
  const key = ladderRegistrationKey(r);
  const rows = ladderRows(jb.batch);
  const at = rows.findIndex((e) => lc(e.counterpartyEntity) === lc(r.counterpartyEntity) && sameLadderSlot(e, r));
  const held = at >= 0 ? rows[at] : undefined;
  const ratio = r.witness.fillRatio;
  const row: LadderRow = {
    counterpartyEntity: r.counterpartyEntity.toLowerCase(),
    targetRole: r.targetRole,
    fullHash: r.fullHash,
    partialRoot: r.partialRoot,
    witness: {
      fillRatio: ratio,
      fullSecret: r.witness.fullSecret,
      reveals: [...r.witness.reveals] as unknown as Reveals,
    },
  };
  const { maxTotalOps, maxHashLadderRegistrations } = J_BATCH_LIMITS;
  const slotted = (): Result<readonly LadderRow[], EntityError> => {
    const total = cjOps(jb.batch) + 1;
    switch (true) {
      case held !== undefined && (!r.targetRole || ratio < held.witness.fillRatio):
        return invariant(
          `J_HASH_LADDER_REGISTRATION_CONFLICT:${key}:${roleName(r.targetRole)}:${held?.witness.fillRatio}:${ratio}`,
        );
      case held !== undefined:
        return ok(rows.map((e, i) => (i === at ? row : e)));
      case total > maxTotalOps:
        return invariant(
          `J_BATCH_LIMIT_EXCEEDED: hashLadderRegistration would exceed total ops ${total}/${maxTotalOps}`,
        );
      case rows.length + 1 > maxHashLadderRegistrations:
        return invariant(
          `J_BATCH_LIMIT_EXCEEDED: hashLadderRegistrations ${rows.length + 1}/${maxHashLadderRegistrations}`,
        );
      default:
        return ok([...rows, row]);
    }
  };
  if (held !== undefined && held.witness.fillRatio === ratio) return ok(jb);
  return map(slotted(), (next): CjJBatch => ({
    ...jb,
    batch: { ...jb.batch, hashLadderRegistrations: next as unknown as readonly Binary[] },
    ...(jb.status === "empty" ? { status: "accumulating" } : {}),
  }));
};
type LadderQueued = { readonly host: CjHost; readonly result: LadderRevealResult };
/** A sent batch in flight: the draft broadcasts automatically once it lands. */
const latchDraft = (jb: CjJBatch): CjJBatch => (jb.sentBatch !== undefined ? { ...jb, autoBroadcastDraft: true } : jb);
/** The witness enters the draft batch, or, with no room, waits stashed on its routes. */
const registerReveal = (
  seeded: CjHost,
  jb: CjJBatch,
  cp: string,
  pull: LadderPull,
  decoded: LadderDecoded,
  targetRole: boolean,
): Result<LadderQueued, EntityError> => {
  const row: LadderRow = {
    counterpartyEntity: cp,
    targetRole,
    fullHash: pull.fullHash,
    partialRoot: pull.partialRoot,
    witness: pendingReveal(decoded),
  };
  if (ladderRoom(jb.batch, row)) {
    return map(upsertLadder(jb, row), (next): LadderQueued => ({
      host: { ...seeded, jb: latchDraft(next) },
      result: "queued",
    }));
  }
  const held = cjOps(jb.batch) > 0 ? latchDraft(jb) : jb;
  return map(stashReveal(seeded, cp, pull, decoded, targetRole), (stashed): LadderQueued => ({
    host: { ...stashed, jb: held },
    result: "deferred-batch-pending",
  }));
};
/**
 * og queueHashLadderRevealRegistration: one signed-role registry write into the mutable draft. A retry of a queued or
 * confirmed ratio is a no-op, a Source change or Target regression conflicts, an expired Source window refuses, a full
 * draft stashes the witness on its routes.
 */
export const queueLadderReveal = (
  h: CjHost,
  counterparty: string,
  pull: LadderPull,
  decoded: LadderDecoded,
  targetRole: boolean,
): Result<LadderQueued, EntityError> => {
  const ratio = decoded.fillRatio;
  const ladder = pullLadderHash(pull);
  const cp = String(counterparty).toLowerCase();
  if (!fillRatioOk(ratio)) return invariant(`J_HASH_LADDER_FILL_RATIO_INVALID:${String(ratio)}`);
  if (!LOWER_WORD.test(cp)) return invariant(`J_HASH_LADDER_COUNTERPARTY_INVALID:${counterparty}`);
  return chain(existingRegistryRatios(h, cp, ladder, targetRole), ({ jb, confirmed, queued }) => {
    const seeded: CjHost = { ...h, jb };
    const existing = [...confirmed, ...queued];
    const current = existing.length > 0 ? Math.max(...existing) : undefined;
    const window = (): Result<"open" | "expired", EntityError> =>
      targetRole ? ok("open") : sourceRevealWindow(seeded, cp);
    switch (true) {
      case queued.includes(ratio) || (!targetRole && confirmed.includes(ratio)):
        return ok({ host: seeded, result: "already-queued" });
      case current !== undefined && (!targetRole || ratio < current):
        return invariant(`J_HASH_LADDER_REGISTRATION_CONFLICT:${ladder}:${roleName(targetRole)}:${current}:${ratio}`);
      default:
        return chain(window(), (w) =>
          w === "open"
            ? registerReveal(seeded, jb, cp, pull, decoded, targetRole)
            : ok({ host: seeded, result: "source-window-expired" }),
        );
    }
  });
};
type Flushed = { readonly host: CjHost; readonly flushed: number };
/** One stashed witness back into the queue, cleared from its route first; `undefined` when the scope excludes it. */
const requeueWitness = (
  acc: Flushed,
  key: string,
  route: CrossRoute,
  targetRole: boolean,
  scope: string | undefined,
): Result<Flushed | undefined, EntityError> => {
  const pull = targetRole ? route.targetPull : route.sourcePull;
  const witness = targetRole ? route.pendingTargetRegistryReveal : route.pendingSourceRegistryReveal;
  if (pull === undefined || witness === undefined) return ok(acc);
  return chain(routeLegPeer(acc.host, route, targetRole), (peer) => {
    if (scope && peer !== scope) return ok(undefined);
    const { pendingSourceRegistryReveal: _s, pendingTargetRegistryReveal: _t, ...rest } = route;
    const cleared = (
      targetRole
        ? { ...rest, ...opt("pendingSourceRegistryReveal", _s) }
        : { ...rest, ...opt("pendingTargetRegistryReveal", _t) }
    ) as CrossRoute;
    return map(queueLadderReveal(cjPutRoute(acc.host, key, cleared), peer, pull, witness, targetRole), (q) => ({
      host: q.host,
      flushed: acc.flushed + (q.result === "queued" ? 1 : 0),
    }));
  });
};
/**
 * og flushDeferredHashLadderReveals: once no batch is in flight, every stashed witness (a Target one only under its
 * dispute) re-enters the queue, Source first; a route whose Source witness is out of scope is skipped whole.
 */
export const flushDeferredReveals = (h: CjHost, scoped?: string): Result<Flushed, EntityError> => {
  if (h.jb?.sentBatch !== undefined) return ok({ host: h, flushed: 0 });
  const scope = scoped?.toLowerCase();
  const flushRoute = (acc: Flushed, key: string): Result<Flushed, EntityError> => {
    const found = acc.host.swaps?.get(key);
    if (found === undefined) return ok(acc);
    const tgt = found.pendingTargetRegistryReveal;
    return chain(requeueWitness(acc, key, found, false, scope), (mid) => {
      if (mid === undefined) return ok(acc);
      const route = mid.host.swaps?.get(key) ?? found;
      const live = tgt !== undefined && targetRevealLive(mid.host, route);
      if (!live) return ok(mid);
      const target: CrossRoute = { ...route, pendingTargetRegistryReveal: tgt };
      return map(requeueWitness(mid, key, target, true, scope), (done) => done ?? mid);
    });
  };
  return foldResult(routeKeys(h), { host: h, flushed: 0 }, flushRoute);
};
export type SourceClaim = { readonly routeId: string; readonly fillRatio: number; readonly result: LadderRevealResult };
/** An observed dispute's beneficiary window: its start second plus our own response window. */
const beneficiaryWindowClosed = (h: CjHost, account: CjAccount): boolean => {
  const active = account.active;
  if (!active?.observedOnChain) return false;
  const window = lc(account.left) === lc(h.id) ? account.leftResponseSeconds : account.rightResponseSeconds;
  return Math.floor(Number(h.timestamp) / 1000) > Number(active.disputeStartTimestamp) + window;
};
type HubClaimStep = { readonly host: CjHost; readonly claim?: SourceClaim | undefined };
/**
 * og queueSourceHubClaimRegistrationForRoute: the Source hub's committed ratio, revealed from its private ladder seed,
 * unless the beneficiary window closed.
 */
const sourceHubClaimForRoute = (h: CjHost, routeId: string, counterparty: string): Result<HubClaimStep, EntityError> => {
  const route = h.swaps?.get(routeId);
  const cp = counterparty.toLowerCase();
  if (route === undefined) return invariant(`CROSS_J_SOURCE_CLAIM_ROUTE_MISSING:${routeId}`);
  const pull = route.sourcePull;
  if (isCrossTerminal(route.status) || pull === undefined) return ok({ host: h });
  if (lc(route.source.counterpartyEntityId) !== lc(h.id) || lc(route.source.entityId) !== cp) return ok({ host: h });
  return chain(fatalCross(crossProofRatio(route)), (fillRatio): Result<HubClaimStep, EntityError> => {
    if (fillRatio <= 0) return ok({ host: h });
    const account = h.accounts.get(cp);
    if (account === undefined) return invariant(`CROSS_J_SOURCE_CLAIM_ACCOUNT_MISSING:${routeId}:${counterparty}`);
    if (beneficiaryWindowClosed(h, account)) {
      return ok({ host: h, claim: { routeId, fillRatio, result: "source-window-expired" } });
    }
    const decoded = chain(fatalCross(crossPrivateSeed(h.runtimeSeed, route)), (seed) =>
      chain(fatalCross(crossPullReveal(fillRatio, seed)), (reveal) =>
        fatalCross(decodeHashLadderBinary(reveal.binary)),
      ),
    );
    return chain(decoded, (d) =>
      map(queueLadderReveal(h, counterparty, pull, d, false), (q) => ({
        host: q.host,
        claim: { routeId, fillRatio, result: q.result },
      })),
    );
  });
};
type SourceClaims = { readonly host: CjHost; readonly claims: readonly SourceClaim[] };
/**
 * og queueSourceHubClaimRegistrationsForAccount: every live route whose Source pull this hub holds against
 * `counterparty` and the signed body carries.
 */
export const sourceHubClaims = (
  h: CjHost,
  counterparty: string,
  signedBody: Pick<ProofBody, "transformers">,
  transformer: string,
): Result<SourceClaims, EntityError> => {
  const self = lc(h.id);
  const cp = counterparty.toLowerCase();
  const held = (r: CrossRoute): boolean =>
    !isCrossTerminal(r.status) &&
    r.sourcePull !== undefined &&
    r.source.counterpartyEntityId.toLowerCase() === self &&
    r.source.entityId.toLowerCase() === cp;
  const ids = routesOf(h)
    .filter(held)
    .map((r) => r.orderId)
    .toSorted(asc);
  const claimOn = (acc: SourceClaims, id: string): Result<SourceClaims, EntityError> => {
    const pull = acc.host.swaps?.get(id)?.sourcePull;
    if (pull === undefined) return ok(acc);
    return chain(fatalCross(findSignedProofBodyPull(signedBody, pull, false, transformer)), (signed) =>
      signed === undefined
        ? ok(acc)
        : map(sourceHubClaimForRoute(acc.host, id, counterparty), (c) => ({
            host: c.host,
            claims: c.claim === undefined ? acc.claims : [...acc.claims, c.claim],
          })),
    );
  };
  return foldResult(ids, { host: h, claims: [] } as SourceClaims, claimOn);
};
/** One cross-j Entity tx for the (entity, signer) lane that carries it. */
type CjLane = { readonly entityId: string; readonly signerId: string; readonly tx: EntityTx };
/**
 * og buildCrossJurisdictionEntityOutput batches: one output per lane in stable text order, its txs in arrival order.
 */
const cjBatches = (lanes: readonly CjLane[]): readonly CjOut[] =>
  [...Map.groupBy(lanes, (l) => `${l.entityId}\0${l.signerId}`)]
    .toSorted(([a], [b]) => asc(a, b))
    .map(([key, rows]): CjOut => {
      const [entityId = "", signerId = ""] = key.split("\0");
      return {
        kind: "cross",
        entityId: entityId.trim().toLowerCase(),
        signerId: signerId.trim().toLowerCase(),
        txs: rows.map((r) => r.tx),
      };
    });
/** og HashLadderRevealRegistered event data. */
export type LadderRevealEvent = {
  readonly entity: string;
  readonly counterpartyEntity: string;
  readonly ladderHash: string;
  readonly fillRatio: number;
  readonly fullSecret: string;
  readonly reveals: readonly string[];
  readonly targetRole: boolean;
  readonly revealedAt?: number | undefined;
};
/**
 * The hash-ladder binary a registered reveal publishes: the full secret at a full fill, else the ratio and its partial
 * reveals.
 */
const revealBinary = (e: LadderRevealEvent): string => {
  const partial = `0x${e.fillRatio.toString(16).padStart(4, "0")}${e.reveals.map((r) => r.slice(2)).join("")}`;
  return (e.fillRatio >= MAX_FILL ? e.fullSecret : partial).toLowerCase();
};
/** The target user's salvage of one ported reveal; the Target Pull's verified ratio must be the event's. */
const portedSalvage = (
  route: CrossRoute,
  targetPull: CrossPullLeg,
  e: LadderRevealEvent,
  binary: string,
  blockNumber: number,
): Result<CjLane, EntityError> => {
  const verified = fatalCross(
    verifyHashLadderBinary({ fullHash: targetPull.fullHash, partialRoot: targetPull.partialRoot }, binary),
  );
  return chain(verified, ({ fillRatio }) => {
    const entityId = lc(route.target.counterpartyEntityId);
    const signerId = lc(route.targetSignerId);
    if (fillRatio !== e.fillRatio) {
      return invariant(
        `CROSS_J_REVEAL_PORT_RATIO_MISMATCH:${route.orderId}:event=${e.fillRatio}:verified=${fillRatio}`,
      );
    }
    if (!entityId || !signerId) return invariant(`CROSS_J_REVEAL_PORT_LANE_MISSING:${route.orderId}`);
    const data = {
      routeId: route.orderId,
      binary,
      fillRatio: e.fillRatio,
      sourceEntityId: route.source.entityId,
      sourceCounterpartyEntityId: route.source.counterpartyEntityId,
      observedAt: blockNumber,
    };
    return ok({ entityId, signerId, tx: { type: "crossJurisdictionSalvage", data } as EntityTx });
  });
};
/**
 * og queueCrossJurisdictionRevealPorts: a Source registry reveal by our Source hub against us ports, per live route,
 * to the target user as a crossJurisdictionSalvage; the verified ratio must equal the event's.
 */
export const revealPorts = (cj: Cj, e: LadderRevealEvent, blockNumber: number): CjStep<number> => {
  const self = lc(cj.host.id), ladder = lc(e.ladderHash);
  if (!self || !ladder || e.fillRatio <= 0 || e.targetRole) return ok({ cj, value: 0 });
  const binary = revealBinary(e);
  const ported = routesOf(cj.host).flatMap((route) => {
    const { sourcePull, targetPull } = route;
    const ours = lc(route.source?.entityId) === self && lc(route.source?.counterpartyEntityId) === lc(e.entity)
      && lc(route.source.entityId) === lc(e.counterpartyEntity);
    const live = sourcePull !== undefined && targetPull !== undefined && !isCrossTerminal(route.status)
      && pullLadderHash(sourcePull) === ladder;
    return ours && live && targetPull !== undefined ? [{ route, targetPull }] : [];
  });
  return map(traverse(ported, (p) => portedSalvage(p.route, p.targetPull, e, binary, blockNumber)), (lanes) => {
    const outputs = cjBatches(lanes), next: Cj = { ...cj, outputs: [...cj.outputs, ...outputs] };
    const message = `🌉 Cross-j reveal observed: porting ratio ${e.fillRatio} to ${outputs.length} target lane(s)`;
    return { cj: outputs.length > 0 ? cjSay(next, message) : next, value: outputs.length };
  });
};
/** og CrossJurisdictionDisputeRecovery. */
export type CrossRecovery = {
  readonly requiredPullIds: readonly string[];
  readonly resultsByPullId: Readonly<Record<string, string>>;
};
export type TargetRecoveryPlan = { readonly recovery: CrossRecovery; readonly representativeRouteId: string };
type TargetBound = CrossRoute & { readonly targetPull: CrossPullLeg };
type TargetRecoveryRoutes = { readonly routes: readonly TargetBound[]; readonly frozen: readonly string[] };
/**
 * og selectTargetRecoveryRoutes: live routes where we are the target user against this target hub, restricted to our
 * own frozen Pull ids.
 */
const targetRecoveryRoutes = (
  h: CjHost,
  account: CjAccount,
  counterparty: string,
): Result<TargetRecoveryRoutes, EntityError> => {
  const self = lc(h.id);
  const cp = lc(counterparty);
  const recovering = (r: CrossRoute): r is TargetBound =>
    lc(r.target.counterpartyEntityId) === self &&
    lc(r.target.entityId) === cp &&
    r.targetPull !== undefined &&
    !isCrossTerminal(r.status);
  const candidates = routesOf(h)
    .filter(recovering)
    .toSorted((a, b) => asc(String(a.orderId || ""), String(b.orderId || "")));
  if (candidates.length === 0) return ok({ routes: [], frozen: [] });
  const ourPulls = (): readonly string[] | undefined => {
    switch (self) {
      case lc(account.left):
        return account.leftPullIds;
      case lc(account.right):
        return account.rightPullIds;
      default:
        return undefined;
    }
  };
  const side = ourPulls();
  if (side === undefined) return invariant(`CROSS_J_TARGET_ACCOUNT_ROLE_MISMATCH:${h.id}`);
  const frozen = [...new Set(side)];
  const held = new Set(frozen);
  return ok({ routes: candidates.filter((r) => held.has(r.targetPull.pullId)), frozen });
};
/**
 * og planCrossJurisdictionTargetRecovery: the frozen Target pulls whose Source result this dispute must wait for, with
 * the supplied results.
 */
export const planTargetRecovery = (
  h: CjHost,
  account: CjAccount,
  counterparty: string,
  supplied: Readonly<Record<string, string>>,
): Result<TargetRecoveryPlan | null, EntityError> =>
  chain(targetRecoveryRoutes(h, account, counterparty), ({ routes, frozen }) => {
    const [representative] = routes;
    if (representative === undefined) return ok(null);
    const required = new Set(routes.map((r) => r.targetPull.pullId));
    const bound = foldResult(
      Object.entries(supplied),
      {} as Readonly<Record<string, string>>,
      (results, [pullId, result]) =>
        required.has(pullId)
          ? ok({ ...results, [pullId]: String(result || "0").toLowerCase() })
          : invariant(`CROSS_J_TARGET_RECOVERY_RESULT_UNBOUND:${pullId}`),
    );
    return map(bound, (resultsByPullId) => ({
      representativeRouteId: representative.orderId,
      recovery: { requiredPullIds: frozen.filter((id) => required.has(id)), resultsByPullId },
    }));
  });
/** og refreshCrossJurisdictionTargetRecovery: re-plan, keeping only results still required. */
export const refreshTargetRecovery = (
  h: CjHost, account: CjAccount, counterparty: string, current: CrossRecovery,
): Result<TargetRecoveryPlan | null, EntityError> =>
  chain(targetRecoveryRoutes(h, account, counterparty), ({ routes }) => {
    const required = new Set(routes.map((r) => r.targetPull.pullId));
    const kept = Object.entries(current.resultsByPullId).filter(([id]) => required.has(id));
    return planTargetRecovery(h, account, counterparty, Object.fromEntries(kept));
  });
/** A cross-j route's four parties: the users at its ends and the hubs they route through. */
const routeParties = (route: CrossRoute) => ({
  sourceUser: lc(route.source?.entityId),
  sourceHub: lc(route.source?.counterpartyEntityId),
  targetHub: lc(route.target?.entityId),
  targetUser: lc(route.target?.counterpartyEntityId),
});
type Sibling = { readonly entityId: string; readonly signerId: string };
/** og siblingDisputeTargetForRoute: users fan out user to user, hubs hub to hub. */
const siblingOf = (route: CrossRoute, self: string): Sibling | null => {
  const p = routeParties(route);
  const pick = (signer: unknown, entityId: string): Sibling | null => {
    const signerId = lc(signer);
    return signerId && entityId ? { entityId, signerId } : null;
  };
  switch (self) {
    case p.sourceUser: return pick(route.targetSignerId, p.targetUser);
    case p.targetUser: return pick(route.sourceSignerId, p.sourceUser);
    case p.sourceHub: return pick(route.targetHubSignerId, p.targetHub);
    case p.targetHub: return pick(route.sourceHubSignerId, p.sourceHub);
    default: return null;
  }
};
/** og routeTouchesDisputedAccount: one of the route's two Accounts is ours with `cp`. */
const routeTouches = (route: CrossRoute, self: string, cp: string): boolean => {
  const p = routeParties(route);
  const account = (a: string, b: string): boolean => (self === a && cp === b) || (self === b && cp === a);
  return account(p.sourceUser, p.sourceHub) || account(p.targetUser, p.targetHub);
};
type Fanout = { readonly host: CjHost; readonly lanes: readonly CjLane[] };
/**
 * og queueCrossJurisdictionSiblingDisputeFanout: every live route leg on the disputed Account asks its sibling to
 * start its own clock (crossJurisdictionForceSiblingDispute); a raw pull-less intent is cancelled instead; any other
 * pull-less or signer-less route halts.
 */
export const siblingFanout = (cj: Cj, counterparty: string, observedAt?: number): CjStep<number> => {
  const self = lc(cj.host.id);
  const cp = lc(counterparty);
  if (!self || !cp) return ok({ cj, value: 0 });
  const fanRoute = (acc: Fanout, [key, route]: readonly [string, CrossRoute]): Result<Fanout, EntityError> => {
    if (isCrossTerminal(route.status) || !routeTouches(route, self, cp)) return ok(acc);
    if (route.sourcePull === undefined || route.targetPull === undefined) {
      const rawIntent = route.status === "intent" && route.sourcePull === undefined && route.targetPull === undefined;
      if (!rawIntent) return invariant(`CROSS_J_SIBLING_DISPUTE_PULLS_MISSING:${route.orderId}`);
      const cancelled = transitionCrossStatus(route, "cancelled", Number(acc.host.timestamp || 0));
      return cancelled.ok
        ? ok({ ...acc, host: cjPutRoute(acc.host, key, cancelled.value) })
        : invariant(`CROSS_J_ROUTE_TRANSITION_INVALID: route=${route.orderId} ${route.status || "intent"}->cancelled`);
    }
    const sibling = siblingOf(route, self);
    if (sibling === null) return invariant(`CROSS_J_SIBLING_DISPUTE_SIGNER_MISSING:${route.orderId}:self=${self}`);
    const data = { routeId: route.orderId, observedCounterpartyEntityId: cp, ...opt("observedAt", observedAt) };
    return ok({
      ...acc,
      lanes: [...acc.lanes, { ...sibling, tx: { type: "crossJurisdictionForceSiblingDispute", data } as EntityTx }],
    });
  };
  return map(foldResult(routeEntries(cj.host), { host: cj.host, lanes: [] } as Fanout, fanRoute), ({ host, lanes }) => {
    const outputs = cjBatches(lanes);
    const next: Cj = { ...cj, host, outputs: [...cj.outputs, ...outputs] };
    const message = `⚔️ Cross-j dispute observed vs ${cp.slice(-4)}: fanning out to ${outputs.length} sibling lane(s)`;
    return { cj: outputs.length > 0 ? cjSay(next, message) : next, value: outputs.length };
  });
};
/**
 * og queueLocalJBatchBroadcast: one self j_broadcast for validators[0] when unsent work exists and none is already in
 * this step's outputs.
 */
const localJBroadcast = (cj: Cj): Result<{ readonly cj: Cj; readonly value: boolean }, EntityError> => {
  const jb = cj.host.jb, signerId = cj.host.validators[0];
  const unsent = jb !== undefined && jb.sentBatch === undefined
    && ((jb.recoveryBatches ?? []).some((b) => cjOps(b) > 0) || cjOps(jb.batch) > 0);
  if (!unsent || cj.outputs.some((o) => o.kind === "j_broadcast")) return ok({ cj, value: false });
  if (!signerId) return invariant("J_BATCH_AUTO_BROADCAST_SIGNER_MISSING");
  return ok({ cj: cjBroadcast(cj, signerId), value: true });
};
/** One Account in og's helper view: sides, response windows, activeDispute, and the frozen Pull ids of each side. */
const cjAccountOf = (child: AccountReplica): CjAccount => {
  const active = activeOf(child);
  const view = committedView(child.state);
  const pulls = view.ok ? disputeArgumentPlan(view.value) : { leftPullIds: [], rightPullIds: [] };
  const started = active !== undefined && "disputeStartTimestamp" in active ? active.disputeStartTimestamp : undefined;
  return {
    left: child.state.account.id.left,
    right: child.state.account.id.right,
    leftResponseSeconds: child.state.terms.disputeConfig.leftResponseSeconds,
    rightResponseSeconds: child.state.terms.disputeConfig.rightResponseSeconds,
    ...(active === undefined
      ? {}
      : { active: { observedOnChain: active.observedOnChain, ...opt("disputeStartTimestamp", started) } }),
    leftPullIds: pulls.leftPullIds,
    rightPullIds: pulls.rightPullIds,
  };
};
/** The Entity's og helper view: routes, committed jBatchState, and every Account by lowercase counterparty. */
const cjHostOf = (
  state: EntityState,
  replicas: Replicas,
  timestamp: bigint | number,
  runtimeSeed?: string,
): CjHost => ({
  id: state.id,
  timestamp: Number(timestamp),
  validators: rootConfig(state).validators,
  ...opt("runtimeSeed", runtimeSeed),
  ...opt("swaps", state.crossJurisdictionSwaps),
  ...opt("jb", state.committed["jBatchState"] as unknown as CjJBatch | undefined),
  accounts: new Map([...replicas].map(([peer, child]): [string, CjAccount] => [lc(peer), cjAccountOf(child)])),
});
/** A Cj over the Draft's Entity, fresh messages and outputs. */
const cjOf = (d: Draft, timestamp: bigint | number, runtimeSeed?: string): Cj =>
  ({ host: cjHostOf(d.state, d.accountReplicas, timestamp, runtimeSeed), messages: [], outputs: [] });
/**
 * Write a Cj back into the Draft: routes, jBatchState, messages (og addMessage), then outputs (self j_broadcast,
 * cross-j runtimeOutput).
 */
const cjInto = (d: Draft, cj: Cj, timestamp: bigint): Draft => {
  const h = cj.host;
  const committed =
    h.jb === undefined ? d.state.committed : { ...d.state.committed, jBatchState: h.jb as unknown as Binary };
  const state: EntityState = { ...d.state, ...opt("crossJurisdictionSwaps", h.swaps), committed };
  return {
    ...d,
    state,
    outputs: [...d.outputs, ...cj.outputs.map((o) => cjOutput(state, o, timestamp))],
    events: [...(d.events ?? []), ...cj.messages.map(status)],
  };
};
/**
 * One og helper output as an Entity output: a self j_broadcast, or a cross-j Entity output wrapped as a runtimeOutput
 * command.
 */
const cjOutput = (state: EntityState, o: CjOut, timestamp: bigint): EntityOutput => {
  if (o.kind === "j_broadcast") return selfJBroadcast(state, o.signerId, timestamp);
  const data = {
    protocol: "cross-j" as const,
    sourceEntityId: lower(state.id),
    sourceSignerId: "",
    targetEntityId: o.entityId,
    entityTxs: o.txs,
  };
  return {
    to: o.entityId as EntityId,
    signerId: o.signerId as Address,
    input: { kind: "txs", timestamp, txs: [{ type: "runtimeOutput", data }] },
  };
};
/**
 * og `if (state.crontabState) scheduleHook(..., dispute-deadline:<peer>)`: replace the Account's dispute deadline hook
 * when the Entity has a crontab.
 */
const scheduleDisputeDeadline = (
  state: EntityState,
  peer: string,
  triggerAt: number,
): Result<EntityState, EntityError> => {
  if (state.committed["crontabState"] === undefined) return ok(state);
  const hook: ScheduledHook = {
    id: `dispute-deadline:${peer.toLowerCase()}`,
    triggerAt,
    type: "dispute_deadline",
    data: { accountId: peer },
  };
  return map(crontabOf(state), (c) => withCrontab(state, scheduleHook(c, hook)));
};
// ---- og entity/tx/handlers/cross-j/{salvage,force-sibling-dispute}.ts, htlc/direct.ts handleResolveHtlcLockEntityTx, paybook/lifecycle.ts ----
type SalvageData = Extract<EntityTx, { type: "crossJurisdictionSalvage" }>["data"];
/** A reveal port checked against its route: a note to log, nothing to do, or a pull binary the Target chain accepts. */
type PortCheck =
  | { readonly kind: "note"; readonly message: string }
  | { readonly kind: "settled" }
  | {
      readonly kind: "verified";
      readonly route: CrossRoute;
      readonly pull: LadderPull;
      readonly decoded: LadderDecoded;
    };
type VerifiedPort = Extract<PortCheck, { kind: "verified" }>;
/**
 * og salvage admission: a claimed fill, a route this Entity is Target user of, and a binary that opens its Target pull.
 */
const checkPort = (state: EntityState, x: SalvageData): Result<PortCheck, EntityError> => {
  const { routeId, binary } = x;
  const claimed = Math.floor(Number(x.fillRatio) || 0);
  const note = (message: string): Result<PortCheck, EntityError> => ok({ kind: "note", message });
  if (!binary || claimed <= 0) return note(`🌉 Cross-j reveal port ignored for ${routeId}: invalid result`);
  const route = state.crossJurisdictionSwaps?.get(routeId);
  if (route === undefined || lc(route.target?.counterpartyEntityId) !== lc(state.id))
    return note(`🌉 Cross-j reveal port ${routeId} skipped: route not owned here`);
  const pull = route.targetPull;
  if (pull === undefined) return halt(`CROSS_J_REVEAL_PORT_TARGET_PULL_MISSING:${routeId}:${state.id}`);
  if (isCrossTerminal(route.status)) return ok({ kind: "settled" });
  if (binary === "0x") return note(`🌉 Cross-j reveal port ignored for ${routeId}: empty result`);
  const verified = verifyHashLadderBinary({ fullHash: pull.fullHash, partialRoot: pull.partialRoot }, binary);
  if (!verified.ok) return note(`❌ Cross-j reveal port ${routeId} invalid pull binary: ${verified.error.reason}`);
  const ratio = verified.value.fillRatio;
  if (ratio <= 0) return note(`🌉 Cross-j reveal port ignored for ${routeId}: zero fill`);
  if (ratio !== claimed)
    return note(`❌ Cross-j reveal port ${routeId} fill mismatch: claimed ${claimed}, verified ${ratio}`);
  return ok({ kind: "verified", route, pull, decoded: verified.value });
};
/**
 * Register a verified reveal on the Target chain: stashed until the Target Account's dispute clock runs, else queued,
 * with a broadcast when no batch is in flight to carry it.
 */
const portReveal = (cj: Cj, routeId: string, port: VerifiedPort): Result<Cj, EntityError> => {
  const { route, pull, decoded } = port;
  const counterparty = route.target.entityId;
  const signerId = cj.host.validators[0];
  if (cj.host.accounts.get(lc(counterparty))?.active === undefined)
    return map(stashReveal(cj.host, counterparty, pull, decoded, true), (host) =>
      cjSay(cjHost(cj, host), `⏳ Cross-j reveal port ${routeId}: waiting for the target dispute clock`),
    );
  return chain(queueLadderReveal(cj.host, counterparty, pull, decoded, true), ({ host, result }) => {
    const queued = cjHost(cj, host);
    const jb = host.jb;
    const sent = jb?.sentBatch !== undefined;
    const broadcast = (): Result<Cj, EntityError> =>
      signerId ? ok(cjBroadcast(queued, signerId)) : halt(`CROSS_J_REVEAL_PORT_SIGNER_MISSING:${routeId}`);
    switch (result) {
      case "queued":
        return sent
          ? ok(cjSay(queued, `⏳ Cross-j reveal port ${routeId}: queued behind the pending jBatch`))
          : map(broadcast(), (c) =>
              cjSay(c, `🌉 Cross-j reveal port ${routeId}: registering ratio ${decoded.fillRatio} on the target chain`),
            );
      case "deferred-batch-pending":
        return map(!sent && jb !== undefined && cjOps(jb.batch) > 0 ? broadcast() : ok(queued), (c) =>
          cjSay(c, `⏳ Cross-j reveal port ${routeId}: deferred until the pending jBatch is acknowledged`),
        );
      default:
        return ok(queued);
    }
  });
};
/**
 * og handleCrossJurisdictionSalvageEntityTx: the Target user re-registers the Source chain's verified hash-ladder
 * reveal on its own chain.
 */
const crossSalvage = (d: Draft, x: SalvageData, ctx: FoldContext): Result<Draft, EntityError> =>
  chain(checkPort(d.state, x), (port) => {
    switch (port.kind) {
      case "note":
        return ok(withStatus(d, port.message));
      case "settled":
        return ok(d);
      case "verified":
        return map(portReveal(cjOf(d, ctx.timestamp, ctx.runtimeSeed), x.routeId, port), (cj) =>
          cjInto(d, cj, ctx.timestamp),
        );
    }
  });
type ForceSiblingData = Extract<EntityTx, { type: "crossJurisdictionForceSiblingDispute" }>["data"];
/** og localDisputeCounterparty: the other end of this participant's own leg, the Account it disputes. */
const legPeerOf = (route: CrossRoute, self: string): string | null => {
  const p = routeParties(route);
  switch (self) {
    case p.sourceUser: return p.sourceHub;
    case p.sourceHub: return p.sourceUser;
    case p.targetUser: return p.targetHub;
    case p.targetHub: return p.targetUser;
    default: return null;
  }
};
/** og observedIsOtherLegParticipant: the observed peer sits on the leg this Entity is not on. */
const observedOtherLeg = (route: CrossRoute, self: string, observed: string): boolean => {
  const p = routeParties(route), source = [p.sourceUser, p.sourceHub], target = [p.targetHub, p.targetUser];
  const onSource = source.includes(self), onTarget = target.includes(self);
  const otherLeg = onSource && !onTarget ? target : onTarget && !onSource ? source : [];
  return observed !== "" && observed !== self && otherLeg.includes(observed);
};
/**
 * og handleCrossJurisdictionForceSiblingDisputeEntityTx: fail loud on a broken route mirror, then prepare the sibling
 * Account's dispute.
 */
const forceSiblingDispute = (d: Draft, x: ForceSiblingData, ctx: FoldContext): Result<Draft, EntityError> => {
  const { routeId } = x;
  const self = lc(d.state.id);
  const observed = lc(x.observedCounterpartyEntityId);
  const route = d.state.crossJurisdictionSwaps?.get(routeId);
  if (route === undefined) return halt(`CROSS_J_SIBLING_DISPUTE_ROUTE_MISSING:${routeId}`);
  if (!route.sourcePull || !route.targetPull) return halt(`CROSS_J_SIBLING_DISPUTE_PULLS_MISSING:${routeId}`);
  const local = legPeerOf(route, self);
  if (!local) return halt(`CROSS_J_SIBLING_DISPUTE_NOT_PARTICIPANT:${routeId}:self=${self}`);
  if (!observedOtherLeg(route, self, observed))
    return halt(
      `CROSS_J_SIBLING_DISPUTE_OBSERVED_LEG_INVALID:${routeId}:observed=${observed}:self=${self}:local=${local}`,
    );
  const dispute = {
    counterpartyEntityId: local as EntityId,
    description: `sibling-dispute:${routeId}`,
    crossJurisdictionRouteId: routeId,
  };
  return prepareDispute(d, dispute, ctx);
};
/** The lock fields a paid preimage is checked against. */
type PaidLock = Pick<HtlcLock, "lockId" | "hashlock" | "tokenId" | "amount" | "senderIsLeft">;
/**
 * og persistVerifiedPaymentSecret: the canonical payment id, the entry's secret / token / amount / endpoint agree,
 * then the secret and our side's endpoint.
 */
export const persistVerifiedPaymentSecret = (
  paybook: Paybook,
  selfIsLeft: boolean,
  counterparty: string,
  lock: PaidLock,
  secret: string,
  timestamp: number,
): Result<Paybook, EntityError> => {
  const { hashlock, amount } = lock;
  const tokenId = Number(lock.tokenId);
  const localSent = lock.senderIsLeft === selfIsLeft;
  const entry: PaybookEntry = paybook.entries.get(hashlock) ?? {
    hashlock,
    tokenId,
    amount,
    createdTimestamp: timestamp,
  };
  const endpoint = localSent ? entry.outboundEntity : entry.inboundEntity;
  const clashes = (held: string | undefined, next: string): boolean =>
    !!held && held.toLowerCase() !== next.toLowerCase();
  switch (true) {
    case lock.lockId.toLowerCase() !== hashlock.toLowerCase():
      return invariant(`PAYBOOK_LOCK_ID_MUST_EQUAL_HASHLOCK:${lock.lockId}:${hashlock}`);
    case clashes(entry.secret, secret):
      return invariant(`PAYBOOK_SECRET_CONFLICT:${hashlock}`);
    case entry.tokenId !== undefined && entry.tokenId !== tokenId:
      return invariant(`PAYBOOK_TOKEN_CONFLICT:${hashlock}`);
    case entry.amount !== undefined && entry.amount !== amount:
      return invariant(`PAYBOOK_AMOUNT_CONFLICT:${hashlock}`);
    case clashes(endpoint, counterparty):
      return invariant(`PAYBOOK_ENTITY_CONFLICT:${hashlock}`);
    default: {
      const paid: PaybookEntry = localSent
        ? { ...entry, secret, outboundEntity: counterparty }
        : { ...entry, secret, inboundEntity: counterparty };
      return ok({ ...paybook, entries: mapSet(paybook.entries, hashlock, paid) });
    }
  }
};
type EvidenceSecret = { readonly hashlock: string; readonly secret: string };
/**
 * og deadline-policy.ts evidenceSecrets: the `secret_window` violation's preimage, from the first frame resolve that
 * opens the lock (committed, else opened in the frame).
 */
const unsafeEvidenceSecrets = (s: AccountBody, e: FrameEvidence): readonly EvidenceSecret[] => {
  const cause = e.cause;
  if (cause._tag !== "frame_deadline" || cause.reason !== "secret_window") return [];
  const opened = e.frame.txs.flatMap((t) => (t.type === "htlc_lock" && t.lockId === cause.lockId ? [t.hashlock] : []));
  const hashlock = s.locks.get(cause.lockId)?.hashlock ?? opened[0];
  if (hashlock === undefined) return [];
  const opens = e.frame.txs.flatMap((t) =>
    t.type === "htlc_resolve" &&
    t.lockId === cause.lockId &&
    t.outcome === "secret" &&
    hashHtlcSecret(t.secret) === hashlock
      ? [{ hashlock, secret: t.secret }]
      : [],
  );
  return opens.slice(0, 1);
};
/**
 * og AccountInputDisputeRequired.reason: the deadline scan's text for a secret-window violation, else og's replay
 * failureMessage where the rewrite reproduces it (the state root mismatch, the dispute Hanko requirement, og's per-tx
 * `Frame application failed: <handler text>` from accountTxFailure). Only a cause with no og replay text (none is
 * known) falls back to its tag.
 */
const unsafeReason = (e: FrameEvidence, timestamp: bigint): string => {
  const cause = e.cause;
  if (cause._tag !== "frame_deadline" || cause.reason !== "secret_window")
    return e.reason ?? `ACCOUNT_FRAME_DISPUTE_REQUIRED:${cause._tag}`;
  const window = `lock=${cause.lockId} reserve=${HTLC_ENFORCEMENT_RESERVE_MS}ms localTimestamp=${timestamp}`;
  return `HTLC_SECRET_ENFORCEMENT_WINDOW_TOO_SHORT: ${window}`;
};
/** og armPaymentSecretAckTimeout: the upstream resolve now waits for its ACK. */
const armSecretAck = (entry: PaybookEntry, timestamp: number): PaybookEntry => ({
  ...entry,
  secretAckPending: true,
  secretAckStartedAt: timestamp,
  secretAckDeadlineAt: timestamp + HTLC_SECRET_ACK_TIMEOUT_MS,
});
/** Evidence secrets written into the paybook, and the resolves they owe upstream. */
type SecretFallout = { readonly paybook: Paybook; readonly resolves: readonly AccountTxTarget[] };
/** What persisting one evidence secret needs to know about the disputed Account. */
type SecretContext = {
  readonly child: AccountReplica;
  readonly selfIsLeft: boolean;
  readonly peer: EntityId;
  readonly timestamp: number;
};
/**
 * og persistVerifiedPaymentSecret for one evidence secret; a lock we sent on an inbound route also resolves upstream,
 * its ACK deadline armed.
 */
const persistEvidenceSecret =
  (c: SecretContext) =>
  (acc: SecretFallout, { hashlock, secret }: EvidenceSecret): Result<SecretFallout, EntityError> => {
    const lock = [...c.child.state.locks.values()].find((l) => l.hashlock.toLowerCase() === hashlock.toLowerCase());
    if (lock === undefined) return invariant(`HTLC_DISPUTE_EVIDENCE_LOCK_MISSING:${hashlock}`);
    const persisted = persistVerifiedPaymentSecret(acc.paybook, c.selfIsLeft, c.peer, lock, secret, c.timestamp);
    return map(persisted, (paybook): SecretFallout => {
      const route = paybook.entries.get(lock.hashlock);
      if (route === undefined || lock.senderIsLeft !== c.selfIsLeft || !hasInbound(route)) return { ...acc, paybook };
      const upstream: AccountTxTarget = {
        accountId: route.inboundEntity as EntityId,
        tx: { type: "htlc_resolve", lockId: hashlock, outcome: "secret", secret },
      };
      const armed = mapSet(paybook.entries, lock.hashlock, armSecretAck(route, c.timestamp));
      return { paybook: { ...paybook, entries: armed }, resolves: [...acc.resolves, upstream] };
    });
  };
/**
 * og handleUnsafeAccountFrame's shadow.rejectedFrameEvidence: every phase keeps the rejected frame; a frozen replica
 * also keeps the evidence.
 */
const withRejectedFrame = (d: Draft, peer: EntityId, evidence: FrameEvidence, rejectedFrame: RejectedFrame): Draft => {
  const frozen = d.accountReplicas.get(peer);
  const recorded = (child: AccountReplica): AccountReplica => {
    switch (child._tag) {
      case "preparing":
      case "disputed":
        return { ...child, evidence, rejectedFrame };
      default:
        return { ...child, rejectedFrame };
    }
  };
  return frozen === undefined ? d : { ...d, ...putChild(d.state, d.accountReplicas, peer, recorded(frozen)) };
};
const disputeStartCount = (state: EntityState): number => committedJBatch(state)?.batch["disputeStarts"]?.length ?? 0;
/**
 * og effects.accountTxs: each returned resolve is admitted after the Entity tx, so one on the just-frozen Account is
 * suppressed.
 */
const queueResolves = (d: Draft, resolves: readonly AccountTxTarget[]): Draft => {
  const queued = resolves.reduce(queueReturned, d);
  return resolves.length === 0
    ? queued
    : { ...queued, touched: [...(queued.touched ?? []), ...resolves.map((t) => t.accountId as EntityId)] };
};
/**
 * og dispute-input.ts handleUnsafeAccountFrame (input-phases finishDisputedAccountInput): an Account input og answers
 * with disposition 'dispute'. A just-created inbound Account is dropped with og's message. Otherwise the frame evidence
 * is kept, each evidence secret is persisted and resolved upstream where owed; then og handlePrepareDispute, the
 * autoBroadcastDraft latch and a self j_broadcast when a start was queued and no batch is in flight. `undefined` when
 * the refusal is not a dispute.
 */
export const unsafeAccountFrame = (
  held: Folded,
  at: Folded,
  peer: EntityId,
  error: AccountReplicaError,
  created: boolean,
  ctx: FoldContext,
): Result<Draft, EntityError> | undefined => {
  const after = error._tag === "rejected_after_ack" ? error : undefined;
  const evidence = evidenceOf(after?.cause ?? error);
  if (evidence === null) return undefined;
  if (created)
    return ok(withStatus({ ...held, outputs: [] }, `⚠️ Rejected uncommitted account genesis from ${peer.slice(-8)}`));
  const live = at.accountReplicas.get(peer);
  if (live === undefined) return err({ _tag: "no_such_account", target: peer });
  const child = after?.committed.replica ?? live;
  const selfIsLeft = sameHex(child.state.account.id.left, at.state.id);
  const secrets = unsafeEvidenceSecrets(child.state, evidence);
  const context: SecretContext = { child, selfIsLeft, peer, timestamp: Number(ctx.timestamp) };
  const fallout = foldResult<SecretFallout, EvidenceSecret, EntityError>(
    secrets,
    { paybook: at.state.paybook ?? EMPTY_PAYBOOK, resolves: [] },
    persistEvidenceSecret(context),
  );
  return chain(fallout, ({ paybook, resolves }) => {
    const state = secrets.length === 0 ? at.state : { ...at.state, paybook };
    const base: Draft = { ...putChild(state, at.accountReplicas, peer, child), outputs: [] };
    const startsBefore = disputeStartCount(base.state);
    const reason = unsafeReason(evidence, ctx.timestamp);
    const rejectedFrame: RejectedFrame = {
      reason,
      frameHash: evidence.frame.stateHash,
      frameHanko: evidence.frameHanko,
    };
    return map(prepareDispute(base, { counterpartyEntityId: peer, description: reason }, ctx), (prepared) => {
      const jb = committedJBatch(prepared.state);
      const started = jb !== undefined && disputeStartCount(prepared.state) > startsBefore;
      const kept = withRejectedFrame(prepared, peer, evidence, rejectedFrame);
      const latched: Draft = started
        ? { ...kept, state: withCommittedJBatch(kept.state, { ...jb, autoBroadcastDraft: true }) }
        : kept;
      const said = withStatus(
        latched,
        started
          ? "⚠️ Unsafe account frame rejected; dispute start queued"
          : "⚠️ Unsafe account frame rejected; dispute preparation awaits Hanko",
      );
      const signerId = rootConfig(said.state).validators[0];
      const broadcast =
        started && jb.sentBatch === undefined && signerId !== undefined
          ? [selfJBroadcast(said.state, signerId, ctx.timestamp)]
          : [];
      return queueResolves({ ...said, outputs: [...said.outputs, ...broadcast] }, resolves);
    });
  });
};
type ResolveHtlcData = Extract<EntityTx, { type: "resolveHtlcLock" }>["data"];
/**
 * og handleResolveHtlcLockEntityTx: a verified preimage for one of the Account's locks; persist it, queue the secret
 * resolve, wake the proposer.
 */
const resolveHtlcLockTx = (d: Draft, x: ResolveHtlcData, ctx: FoldContext): Result<Draft, EntityError> => {
  const { counterpartyEntityId, lockId, secret } = x;
  const peer = lc(counterpartyEntityId) as EntityId;
  const child = d.accountReplicas.get(peer);
  if (child === undefined) return invariant(`HTLC_RESOLVE_ACCOUNT_MISSING:${counterpartyEntityId}`);
  if (!/^0x[0-9a-fA-F]{64}$/.test(lockId)) return invariant(`HTLC_RESOLVE_LOCK_ID_INVALID:${lockId}`);
  const expected = hashHtlcSecret(secret);
  if (expected === null) return invariant("HTLC_RESOLVE_SECRET_INVALID");
  const lock = child.state.locks.get(lockId);
  if (lock === undefined) return invariant(`HTLC_RESOLVE_LOCK_MISSING:${peer}:${lockId}`);
  if (lock.hashlock !== expected) return invariant(`HTLC_RESOLVE_HASHLOCK_MISMATCH:${lockId}`);
  const selfIsLeft = sameHex(child.state.account.id.left, d.state.id);
  const persisted = persistVerifiedPaymentSecret(
    d.state.paybook ?? EMPTY_PAYBOOK,
    selfIsLeft,
    peer,
    lock,
    secret,
    Number(ctx.timestamp),
  );
  return map(persisted, (paybook) => {
    const resolve: AccountTxTarget = {
      accountId: peer,
      tx: { type: "htlc_resolve", lockId, outcome: "secret", secret },
    };
    const queued = withStatus(
      queueReturned({ ...d, state: { ...d.state, paybook } }, resolve),
      `🔓 HTLC resolve queued for ${peer}`,
    );
    const woken = rootConfig(d.state).validators[0] ? [wake(d.state, ctx.timestamp)] : [];
    return { ...queued, outputs: [...queued.outputs, ...woken], touched: [...(queued.touched ?? []), peer] };
  });
};
// ---- og entity/scheduler/{index,types,hook-state,derived-deadlines,due-hooks,dispute-deadline-hook}.ts, scheduler/wake, runtime/mempool/scheduled-wake.ts ----
/** A one-shot deadline: an id, when it fires, and what it is about. */
type Timed<Type extends string, Data> = {
  readonly id: string;
  readonly triggerAt: number;
  readonly type: Type;
  readonly data: Data;
};
/**
 * og ScheduledHook (scheduler/types.ts): a deterministic one-shot hook, replaced by id, fired at the Entity's logical
 * time (the frame timestamp).
 */
export type ScheduledHook =
  | Timed<"dispute_deadline", { readonly accountId: string }>
  | Timed<"settlement_window" | "watchdog", { readonly [k: string]: never }>
  | Timed<"hub_rebalance_kick", { readonly reason: string; readonly counterpartyId: string }>
  | Timed<"board_hanko_refresh", {
    readonly activationJHeight: number;
    readonly activationLogIndex: number;
    readonly afterCounterpartyId: string;
  }>
  | Timed<"counterparty_board_hanko_refresh_deadline", {
    readonly accountId: string;
    readonly activationJHeight: number;
    readonly activationLogIndex: number;
  }>
  | Timed<"cross_j_orderbook_sweep", { readonly reason: string }>;
/** og CrontabTaskState: the one periodic task, hubRebalance. */
export type CrontabTask = {
  readonly method: "hubRebalance";
  readonly intervalMs: number;
  readonly lastRun: number;
  readonly enabled: boolean;
  readonly params: { readonly [k: string]: string | number | boolean };
};
/** og CrontabState: committed as `crontabState`, its hooks as an Entity collection commitment. */
export type Crontab = {
  readonly tasks: ReadonlyMap<"hubRebalance", CrontabTask>;
  readonly hooks: ReadonlyMap<string, ScheduledHook>;
};
export const HUB_REBALANCE_INTERVAL_MS = 1000;
/** og initCrontab. */
export const initCrontab = (): Crontab => {
  const hubRebalance: CrontabTask =
    { method: "hubRebalance", intervalMs: HUB_REBALANCE_INTERVAL_MS, lastRun: 0, enabled: true, params: {} };
  return { tasks: new Map([["hubRebalance", hubRebalance]]), hooks: new Map() };
};
/** og scheduleHook: replace or create by id. */
export const scheduleHook = (c: Crontab, hook: ScheduledHook): Crontab => ({
  ...c,
  hooks: mapSet(c.hooks, hook.id, hook),
});
/** og cancelHook. */
export const cancelHook = (c: Crontab, id: string): Crontab =>
  c.hooks.has(id) ? { ...c, hooks: mapDelete(c.hooks, id) } : c;
/** og crontabTaskDueAt. */
export const crontabTaskDueAt = (t: Pick<CrontabTask, "lastRun" | "intervalMs">): number => t.lastRun + t.intervalMs;
const emptyCollection = (v: unknown): boolean =>
  typeof v === "object" && v !== null && !(v instanceof Map) && (v as { readonly leafCount?: unknown }).leafCount === 0;
/** The hooks as held: a map, or an empty collection commitment; a non-empty commitment cannot be read back. */
const heldHooks = (hooks: unknown): ReadonlyMap<string, ScheduledHook> | undefined => {
  switch (true) {
    case hooks instanceof Map: return hooks as ReadonlyMap<string, ScheduledHook>;
    case emptyCollection(hooks): return new Map();
    default: return undefined;
  }
};
/** The Entity's crontab (og `state.crontabState ??= initCrontab()` before the frame's txs). */
export const crontabOf = (state: EntityState): Result<Crontab, EntityError> => {
  const raw = state.committed["crontabState"] as { readonly tasks?: unknown; readonly hooks?: unknown } | undefined;
  if (raw === undefined) return ok(initCrontab());
  const hooks = heldHooks(raw.hooks);
  if (hooks === undefined || !(raw.tasks instanceof Map)) return invariant("SCHEDULED_WAKE_CRONTAB_MISSING");
  return ok({ tasks: raw.tasks as ReadonlyMap<"hubRebalance", CrontabTask>, hooks });
};
export const withCrontab = (state: EntityState, c: Crontab): EntityState => ({
  ...state,
  committed: { ...state.committed, crontabState: { tasks: c.tasks, hooks: c.hooks } as unknown as Binary },
});
/**
 * og projectEntityConsensusState `crontabState`: the tasks as held, the hooks as their Entity collection commitment.
 */
const crontabSection = (committed: EntityCommitted): Result<EntityCommitted, EntityRootError> => {
  const raw = committed["crontabState"] as { readonly tasks?: Binary; readonly hooks?: unknown } | undefined;
  if (raw === undefined || !(raw.hooks instanceof Map)) return ok(committed);
  return map(entityCollectionCommitment(raw.hooks as ReadonlyMap<string, Binary>), (hooks) =>
    ({ ...committed, crontabState: { tasks: raw.tasks ?? new Map(), hooks } as unknown as Binary }));
};
/** og DerivedDeadline: per-payment deadlines read from Account locks and paybook entries, never stored as hooks. */
export type DerivedDeadline =
  | Timed<"htlc_timeout", { readonly accountId: string; readonly lockId: string }>
  | Timed<"htlc_secret_ack_timeout", { readonly hashlock: string; readonly counterpartyEntityId: string }>
  | Timed<"lending_overdue", { readonly loanId: string }>;
/** og LendingState (types/finance/lending.ts): the hub's committed `lending` book of pools and loans. */
export type LendingPool = {
  readonly positionId: string;
  readonly hubEntityId: string;
  readonly lenderEntityId: string;
  readonly tokenId: number;
  readonly principalAmount: bigint;
  readonly availableAmount: bigint;
  readonly borrowedAmount: bigint;
  readonly interestBps: number;
  readonly termId: string;
  readonly termMs: number;
  readonly createdAt: number;
  readonly updatedAt: number;
  readonly status: "open" | "closing" | "closed";
};
export type LendingLoan = {
  readonly requestId: string;
  readonly loanId: string;
  readonly hubEntityId: string;
  readonly borrowerEntityId: string;
  readonly lenderEntityId: string;
  readonly positionId: string;
  readonly tokenId: number;
  readonly principalAmount: bigint;
  readonly interestAmount: bigint;
  readonly repaymentAmount: bigint;
  readonly repaidAmount: bigint;
  readonly interestBps: number;
  readonly termId: string;
  readonly termMs: number;
  readonly openedAt: number;
  readonly dueAt: number;
  readonly updatedAt: number;
  readonly status: "opening" | "active" | "closing" | "repaid" | "defaulted";
};
export type LendingBook = {
  readonly pools: ReadonlyMap<string, LendingPool>;
  readonly loans: ReadonlyMap<string, LendingLoan>;
};
const lendingBook = (state: EntityState): LendingBook | undefined => {
  const raw = state.committed["lending"] as Partial<LendingBook> | undefined;
  return raw?.pools instanceof Map && raw.loans instanceof Map ? (raw as LendingBook) : undefined;
};
export type DueHook = ScheduledHook | DerivedDeadline;
type Deadline = { readonly triggerAt: number; readonly id: string };
/** og compareDeadlines: triggerAt, then the id text. */
export const compareDeadlines = (a: Deadline, b: Deadline): number => a.triggerAt - b.triggerAt || asc(a.id, b.id);
/** og canProcessAccountTxForDisputeStatus: only an active Account has an Account-tx consumer. */
const activeAccount = (c: AccountReplica): boolean =>
  c._tag === "open" || c._tag === "proposed" || c._tag === "received";
/** og isSecretAckPendingPayment. */
const secretAckPending = (e: PaybookEntry): boolean => {
  const started = e.secretAckStartedAt, deadline = e.secretAckDeadlineAt;
  const owed = (e.inboundEntity ?? "") !== "" && (e.secret ?? "") !== "" && e.secretAckPending === true;
  return owed && Number.isSafeInteger(started) && Number.isSafeInteger(deadline) && (deadline ?? 0) >= (started ?? 0);
};
/** og HTLC lock timeouts: each positive safe-integer timelock of an active Account. */
const lockTimeouts = (replicas: Replicas): readonly DerivedDeadline[] =>
  [...replicas]
    .filter(([, c]) => activeAccount(c))
    .flatMap(([accountId, c]) =>
      [...c.state.locks.values()].flatMap((l): DerivedDeadline[] => {
        const t = Number(l.timelock);
        if (!Number.isSafeInteger(t) || t <= 0) return [];
        return [
          { id: `htlc-timeout:${l.lockId}`, triggerAt: t, type: "htlc_timeout", data: { accountId, lockId: l.lockId } },
        ];
      }),
    );
/** og secret-ack deadlines: each paybook entry whose upstream secret resolve waits for its ACK. */
const secretAckDeadlines = (state: EntityState): readonly DerivedDeadline[] =>
  [...(state.paybook?.entries.values() ?? [])].filter(secretAckPending).map((e): DerivedDeadline => ({
    id: `htlc-secret-ack:${e.hashlock}`,
    triggerAt: e.secretAckDeadlineAt ?? 0,
    type: "htlc_secret_ack_timeout",
    data: { hashlock: e.hashlock, counterpartyEntityId: e.inboundEntity ?? "" },
  }));
/** og lending overdue deadlines: each active loan's due time. */
const loanDeadlines = (state: EntityState): readonly DerivedDeadline[] =>
  [...(lendingBook(state)?.loans.values() ?? [])].filter((l) => l.status === "active").map((l): DerivedDeadline =>
    ({ id: `lending-overdue:${l.loanId}`, triggerAt: l.dueAt, type: "lending_overdue", data: { loanId: l.loanId } }));
/**
 * og collectDerivedDeadlines, optionally only those due by `now`: HTLC lock timeouts, secret-ack deadlines and active
 * loans' due times.
 */
export const derivedDeadlines = (state: EntityState, replicas: Replicas, now?: number): readonly DerivedDeadline[] =>
  [...lockTimeouts(replicas), ...secretAckDeadlines(state), ...loanDeadlines(state)]
    .filter((d) => now === undefined || d.triggerAt <= now)
    .toSorted(compareDeadlines);
/**
 * og ScheduledWakeJob: advisory diagnostics; execution recomputes the full due set from EntityState at the frame
 * timestamp.
 */
export type ScheduledWakeJob = { readonly kind: "hook" | "task"; readonly id: string; readonly dueAt: number };
export const MAX_SCHEDULED_WAKE_JOBS = 1_000;
const compareJobs = (a: ScheduledWakeJob, b: ScheduledWakeJob): number =>
  a.dueAt - b.dueAt || asc(a.kind, b.kind) || asc(a.id, b.id);
/**
 * og crontabTaskHasPendingWork: periodic hub work needs a hub config and either a sent batch or an Account with
 * rebalance work (og getRebalanceAccountIds).
 */
export const crontabTaskHasPendingWork = (state: EntityState, replicas: Replicas): boolean => {
  if (state.committed["hubRebalanceConfig"] === undefined) return false;
  if (committedJBatch(state)?.sentBatch !== undefined) return true;
  const ids = rebalanceAccountIds(state, replicas);
  return !ids.ok || ids.value.length > 0;
};
/** The enabled periodic tasks, while there is periodic work for them at all. */
const pendingTasks = (state: EntityState, replicas: Replicas, crontab: Crontab): readonly CrontabTask[] => {
  const enabled = [...crontab.tasks.values()].filter((t) => t.enabled);
  return enabled.length > 0 && crontabTaskHasPendingWork(state, replicas) ? enabled : [];
};
const hookJob = (d: Deadline): ScheduledWakeJob => ({ kind: "hook", id: d.id, dueAt: d.triggerAt });
const taskJob = (t: CrontabTask): ScheduledWakeJob => ({ kind: "task", id: t.method, dueAt: crontabTaskDueAt(t) });
/**
 * og collectDueScheduledWakeJobs: derived deadlines and stored hooks due by `now`, and, when periodic work is asked
 * for, the pending periodic tasks due by then.
 */
export const dueWakeJobs = (
  state: EntityState, replicas: Replicas, crontab: Crontab, now: number, includePeriodic: boolean,
): readonly ScheduledWakeJob[] => {
  const periodic = includePeriodic ? pendingTasks(state, replicas, crontab) : [];
  return [
    ...derivedDeadlines(state, replicas, now).map(hookJob),
    ...[...crontab.hooks.values()].filter((h) => h.triggerAt <= now).map(hookJob),
    ...periodic.map(taskJob).filter((j) => j.dueAt <= now),
  ].toSorted(compareJobs);
};
/**
 * og nextReplicaDeadline (active-leader branch; the other branch is leaderTimeoutDue): the earliest derived deadline,
 * hook or pending periodic task.
 */
export const nextWakeAt = (r: EntityReplica): number | undefined => {
  const c = crontabOf(r.state);
  if (!isActiveLeader(r) || !c.ok) return undefined;
  const times = [
    ...derivedDeadlines(r.state, r.accountReplicas).map((d) => d.triggerAt),
    ...[...c.value.hooks.values()].map((h) => h.triggerAt),
    ...pendingTasks(r.state, r.accountReplicas, c.value).map(crontabTaskDueAt),
  ];
  return times.length === 0 ? undefined : times.reduce((a, b) => Math.min(a, b));
};
/**
 * og createDueScheduledWakeInputs (active-leader branch): the advisory due jobs as the local input's one scheduledWake
 * tx (none while a wake is queued).
 */
export const localScheduledWake = (r: EntityReplica, now: bigint): EntityInput | undefined => {
  const c = crontabOf(r.state);
  if (!isActiveLeader(r) || !c.ok || r.mempool.some((tx) => tx.type === "scheduledWake")) return undefined;
  const jobs = dueWakeJobs(r.state, r.accountReplicas, c.value, Number(now), true).slice(0, MAX_SCHEDULED_WAKE_JOBS);
  const first = jobs[0];
  if (first === undefined) return undefined;
  const wake: WakeData = { version: 1, proposerSignerId: signerId(r.signerId), dueAt: first.dueAt, jobs };
  return { kind: "txs", timestamp: now, txs: [{ type: "scheduledWake", data: wake }] };
};
type WakeData = Extract<EntityTx, { type: "scheduledWake" }>["data"];
/** og's job shape: a known kind, a bounded non-empty id, a safe due time no later than the frame. */
const validJob = (now: number) => (j: ScheduledWakeJob): boolean =>
  (j.kind === "hook" || j.kind === "task") && typeof j.id === "string" && j.id.length > 0 && j.id.length <= 256
  && Number.isSafeInteger(j.dueAt) && j.dueAt >= 0 && j.dueAt <= now;
/**
 * og assertScheduledWakeMatchesState: the frame leader's wake with a canonical, unique, structurally valid job list
 * whose first job is `dueAt`, nothing due after the frame time.
 */
const checkWake = (state: EntityState, w: WakeData, now: number): Result<void, EntityError> => {
  const leader = signerId(leaderStateOf(state).activeValidatorId);
  const dueOk = Number.isSafeInteger(w.dueAt) && w.dueAt >= 0 && w.dueAt <= now;
  const sized = Array.isArray(w.jobs) && w.jobs.length > 0 && w.jobs.length <= MAX_SCHEDULED_WAKE_JOBS;
  if (leader === "" || leader !== signerId(String(w.proposerSignerId)))
    return invariant("SCHEDULED_WAKE_PROPOSER_MISMATCH");
  if (w.version !== 1 || !dueOk || !sized) return invariant("SCHEDULED_WAKE_INVALID_PAYLOAD");
  const sorted = w.jobs.toSorted(compareJobs);
  const unique = new Set(sorted.map((j) => stableJson(j))).size === sorted.length;
  const canonical = sorted[0]?.dueAt === w.dueAt && stableJson(sorted) === stableJson(w.jobs) && unique;
  return canonical && sorted.every(validJob(now))
    ? ok(undefined)
    : invariant(`SCHEDULED_WAKE_INVALID_PAYLOAD: jobs=${stableJson(w.jobs)}`);
};
/**
 * og assertScheduledWakeFrameOrder: a wake is the unique first tx of its frame (a plain Error: the whole input is
 * refused).
 */
const wakeOrderIssue = (txs: readonly EntityTx[]): EntityError | undefined => {
  const at = txs.flatMap((tx, i) => (tx.type === "scheduledWake" ? [i] : []));
  if (at.length === 0 || (at.length === 1 && at[0] === 0)) return undefined;
  return { _tag: "entity_invariant", reason: `SCHEDULED_WAKE_FRAME_ORDER_INVALID: indexes=${at.join(",")}` };
};
/**
 * og prioritizeScheduledWakeTransactions: the (identical) wake runs first, before the txs that could replace its hooks.
 */
export const prioritizeWake = (txs: readonly EntityTx[]): Result<readonly EntityTx[], EntityError> => {
  const wakes = txs.filter((tx) => tx.type === "scheduledWake"), first = wakes[0];
  if (first === undefined) return ok(txs);
  const wire = stableJson(wireEntityTx(first));
  if (wakes.some((w) => stableJson(wireEntityTx(w)) !== wire)) return invariant("SCHEDULED_WAKE_CONFLICTING_INPUTS");
  return ok([first, ...txs.filter((tx) => tx.type !== "scheduledWake")]);
};
/**
 * A wake output's tx: an Entity tx, or og's j_broadcast / j_abort_sent_batch continuation, which the rewrite's Entity
 * does not carry.
 */
export type WakeTx =
  | EntityTx
  | { readonly type: "j_broadcast"; readonly data: { readonly [k: string]: never } }
  | {
      readonly type: "j_abort_sent_batch";
      readonly data: { readonly reason: string; readonly requeueToCurrent: boolean };
    };
/** og processDueHooks outputs: each is an EntityInput to this Entity's validators[0]. */
export type WakeOutput = { readonly signerId: string; readonly txs: readonly WakeTx[] };
/**
 * og DueHookPlan plus the state the due hooks rewrite (re-armed hooks, finalizeQueued latches, paybook entries, the
 * kicked task). `sent`: og's outputs to other Entities (the board Hanko refreshes); `hashes`: og context.hashesToSign.
 */
type HookRun = Folded & {
  readonly crontab: Crontab;
  readonly outputs: readonly WakeOutput[];
  readonly timeouts: readonly { readonly accountId: string; readonly lockId: string }[];
  readonly prepare: ReadonlyMap<string, string>;
  readonly finalize: readonly string[];
  readonly broadcast: boolean;
  readonly sent: readonly EntityOutput[];
  readonly hashes: readonly HashToSign[];
  readonly accountTxs: readonly AccountTxTarget[];
};
// ---- og entity/tx/state-effects/board-rotation-hanko-refresh.ts, scheduler/board-hanko-refresh-hook.ts, tx/j-events-board.ts (BoardActivated),
// ---- entity/account/account-counterparty-route.ts: our board rotation re-Hankos every certified Account frame for the peer ----
export const BOARD_HANKO_REFRESH_HOOK_ID = "board-hanko-refresh";
export const BOARD_HANKO_REFRESH_RETRY_MS = 60_000;
export const MAX_BOARD_HANKO_REFRESHES_PER_FRAME = 32;
export const COUNTERPARTY_BOARD_HANKO_REFRESH_DEADLINE_MS = 24 * 60 * 60 * 1_000;
/** A board activation's place on its J chain: the block and the log within it. */
type Activation = { readonly jHeight: number; readonly logIndex: number };
const isWord = (v: string): boolean => /^0x[0-9a-f]{64}$/.test(v.toLowerCase());
/** og RefreshMigration for an activation: why its refresh stands where it does, and the frame it was issued for. */
const refreshMarker = (
  a: Activation, reason: RefreshMigration["reason"], issued?: { readonly height: number; readonly frameHash: string },
): RefreshMigration => ({
  activationJHeight: a.jHeight,
  activationLogIndex: a.logIndex,
  reason,
  ...(issued === undefined ? {} : { issuedFrameHeight: issued.height, issuedFrameHash: issued.frameHash }),
});
/** The board Hanko refresh hook for an activation, resuming after `afterCounterpartyId`. */
const refreshHookFor = (a: Activation, triggerAt: number, afterCounterpartyId: string): ScheduledHook => ({
  id: BOARD_HANKO_REFRESH_HOOK_ID,
  triggerAt,
  type: "board_hanko_refresh",
  data: { activationJHeight: a.jHeight, activationLogIndex: a.logIndex, afterCounterpartyId },
});
/** og accountNeedsBoardHankoRefreshForActivation: this activation's marker, not yet issued for the current frame. */
export const needsBoardRefresh = (c: AccountReplica, a: Activation): boolean => {
  const m = c.refreshMigration, cur = currentFrameOf(c);
  if (m === undefined || m.activationJHeight !== a.jHeight || m.activationLogIndex !== a.logIndex) return false;
  const issuedForHead = m.reason === "issued" && m.issuedFrameHeight === Number(cur.height)
    && (m.issuedFrameHash ?? "").toLowerCase() === cur.hash.toLowerCase();
  return !issuedForHead;
};
const withMarker = (c: AccountReplica, marker: RefreshMigration | undefined): AccountReplica => {
  const { refreshMigration: _m, ...rest } = c;
  return (marker === undefined ? rest : { ...rest, refreshMigration: marker }) as AccountReplica;
};
/** The frame Hankos: ours (og currentFrameHanko) and the peer's (og counterpartyFrameHanko) on the committed head. */
const headHankos = (c: AccountReplica, self: EntityId): { readonly own: Hanko; readonly peer: Hanko } | undefined => {
  const party = partyOf(replicaId(c), self);
  return c.head._tag !== "installed" || !party.ok ? undefined : certifiedBy(c.head.certificate, party.value);
};
/** A registry fault met while authorizing a Hanko's boards: it aborts the check instead of refusing the Hanko. */
type RegistryFault = { readonly _tag: "registry_fault"; readonly fault: EntityError };
/**
 * og resolveObserverCertifiedAccountCounterpartyProposer: verify the peer's frame Hanko on our committed frame against
 * the observer's certified board records (every claim that is not self-hashed) and route to the target claim's first
 * member. A Hanko refusal is og's retryable HankoValidationError (no route); a malformed frame hash or a registry fault
 * is a plain Error.
 */
export const counterpartyProposer = (
  state: EntityState,
  c: AccountReplica,
  peer: EntityId,
): Result<string | null, EntityError> => {
  const hankos = headHankos(c, state.id);
  if (hankos === undefined || !hankos.peer) return ok(null);
  const frameHash = lower(currentFrameOf(c).hash);
  if (!isWord(frameHash)) return invariant(`ACCOUNT_COUNTERPARTY_ROUTE_FRAME_HASH_INVALID:${frameHash || "missing"}`);
  const certified = (entityId: string, boardHash: string): Result<boolean, RegistryFault> => {
    const record = observerBoardRecord(state, entityId);
    if (!record.ok) return err({ _tag: "registry_fault", fault: record.error });
    return ok(record.value !== null && lower(record.value.boardHash) === lower(boardHash));
  };
  const verdict = checkAccountHanko(hankos.peer, frameHash, peer, certified);
  if (!verdict.ok) return verdict.error._tag === "registry_fault" ? err(verdict.error.fault) : ok(null);
  const first = lower(verdict.value.firstMember);
  return ok(/^0x0{24}[0-9a-f]{40}$/.test(first) ? `0x${first.slice(-40)}` : null);
};
type DisputeProof = Omit<DisputeHanko, "hanko">;
/** og exactBilateralDisputeHanko: no dispute evidence, one certified tuple on both sides, or why it is neither. */
type BilateralDispute =
  | { readonly kind: "none" }
  | { readonly kind: "exact"; readonly proof: DisputeProof }
  | { readonly kind: "refused"; readonly reason: "bilateral-dispute-uncertified" | "certified-dispute-invalid" };
const sameProof = (a: DisputeHanko, b: DisputeHanko): boolean =>
  lower(a.hash) === lower(b.hash) && lower(a.proofBodyHash) === lower(b.proofBodyHash)
  && a.proofNonce === b.proofNonce && a.proposerIsLeft === b.proposerIsLeft;
const bilateralDispute = (w: DisputeWitnesses): BilateralDispute => {
  const own = w.current;
  const theirs = w.counterparty;
  if (own === undefined && theirs === undefined) return { kind: "none" };
  if (
    own === undefined ||
    theirs === undefined ||
    !own.hanko ||
    !theirs.hanko ||
    !own.hash ||
    !own.proofBodyHash ||
    !sameProof(own, theirs) ||
    typeof own.proposerIsLeft !== "boolean"
  )
    return { kind: "refused", reason: "bilateral-dispute-uncertified" };
  if (!isWord(own.hash) || !isWord(own.proofBodyHash) || !Number.isSafeInteger(own.proofNonce) || own.proofNonce < 0)
    return { kind: "refused", reason: "certified-dispute-invalid" };
  const { hash, proofBodyHash, proofNonce, proposerIsLeft } = own;
  return {
    kind: "exact",
    proof: { hash: lower(hash), proofBodyHash: lower(proofBodyHash), proofNonce, proposerIsLeft },
  };
};
type RefreshDraft = {
  readonly output?: EntityOutput | undefined;
  readonly hashes: readonly HashToSign[];
  readonly marker: RefreshMigration | undefined;
};
/** The refresh AccountInput for a certified head (and its exact dispute proof), with the Hankos it asks us to sign. */
const issuedRefresh = (
  state: EntityState,
  peer: EntityId,
  c: AccountReplica,
  a: Activation,
  frameHash: string,
  proof: DisputeProof | undefined,
): RefreshDraft => {
  const height = currentFrameOf(c).height;
  const { domain, disputeConfig } = c.state.terms;
  const context = `board-hanko-refresh:${a.jHeight}:${a.logIndex}:${peer}`;
  const data: AccountPeerInput = {
    kind: "board_hanko_refresh",
    fromEntityId: state.id,
    toEntityId: peer,
    domain,
    disputeConfig,
    height,
    frameHash,
    frameHanko: pendingHanko(frameHash),
    boardActivationJHeight: a.jHeight,
    boardActivationLogIndex: a.logIndex,
    ...opt("disputeHanko", proof === undefined ? undefined : { ...proof, hanko: pendingHanko(proof.hash) }),
  };
  const frameSign: HashToSign = { hash: frameHash, type: "accountFrame", context: `${context}:frame` };
  const disputeSign =
    proof === undefined ? [] : [{ hash: proof.hash, type: "dispute", context: `${context}:dispute` } as const];
  return {
    output: { to: peer, tx: { type: "accountInput", data } },
    hashes: [frameSign, ...disputeSign],
    marker: refreshMarker(a, "issued", { height: Number(height), frameHash }),
  };
};
/**
 * og buildAccountHankoRefreshDraft: a certified frame (and exact bilateral dispute proof) becomes the refresh
 * AccountInput and its Hankos, else a marker reason.
 */
const refreshDraft = (
  state: EntityState,
  peer: EntityId,
  c: AccountReplica,
  a: Activation,
): Result<RefreshDraft, EntityError> => {
  const cur = currentFrameOf(c);
  const frameHash = cur.hash.toLowerCase();
  const hankos = headHankos(c, state.id);
  const markedOnly = (reason: RefreshMigration["reason"]): RefreshDraft => ({
    hashes: [],
    marker: refreshMarker(a, reason),
  });
  if (cur.height < 1n) return ok({ hashes: [], marker: undefined });
  if (hankos === undefined || !hankos.own || !hankos.peer) return ok(markedOnly("bilateral-frame-uncertified"));
  if (!isWord(frameHash)) return ok(markedOnly("certified-frame-invalid"));
  const dispute = bilateralDispute(c.dispute);
  if (dispute.kind === "refused") return ok(markedOnly(dispute.reason));
  const proof = dispute.kind === "exact" ? dispute.proof : undefined;
  return map(counterpartyProposer(state, c, peer), (route) =>
    route === null ? markedOnly("output-route-unavailable") : issuedRefresh(state, peer, c, a, frameHash, proof),
  );
};
const activationOf = (d: { readonly activationJHeight: number; readonly activationLogIndex: number }): Activation =>
  ({ jHeight: d.activationJHeight, logIndex: d.activationLogIndex });
/**
 * og processBoardHankoRefreshHook: at most MAX_BOARD_HANKO_REFRESHES_PER_FRAME Accounts still needing this
 * activation's refresh, after the cursor in id order, each re-marked; the hook re-arms now for the next batch, or in
 * BOARD_HANKO_REFRESH_RETRY_MS while a route is unavailable.
 */
const boardRefreshHook = (
  run: HookRun,
  hook: Extract<ScheduledHook, { type: "board_hanko_refresh" }>,
  now: number,
): Result<HookRun, EntityError> => {
  const a = activationOf(hook.data);
  const after = lower(hook.data.afterCounterpartyId);
  const pending = [...run.accountReplicas]
    .map(([peer, c]) => ({ id: lower(peer), peer, c }))
    .filter(({ id, c }) => id > after && needsBoardRefresh(c, a))
    .toSorted((x, y) => asc(x.id, y.id));
  const batch = pending.slice(0, MAX_BOARD_HANKO_REFRESHES_PER_FRAME);
  const drafted = traverse(batch, ({ peer, c }) =>
    map(refreshDraft(run.state, peer, c, a), (draft) => ({ peer, c, draft })),
  );
  return map(drafted, (drafts): HookRun => {
    const hasMore = pending.length > batch.length;
    const retry = drafts.some(({ draft }) => draft.marker?.reason === "output-route-unavailable");
    const cursor = batch.at(-1)?.id ?? after;
    const crontab =
      hasMore || retry
        ? scheduleHook(
            run.crontab,
            refreshHookFor(a, hasMore ? now : now + BOARD_HANKO_REFRESH_RETRY_MS, hasMore ? cursor : ""),
          )
        : run.crontab;
    const remarked = drafts.map(({ peer, c, draft }) => [peer, withMarker(c, draft.marker)] as const);
    return {
      ...run,
      accountReplicas: new Map([...run.accountReplicas, ...remarked]),
      crontab,
      sent: [...run.sent, ...drafts.flatMap(({ draft }) => (draft.output === undefined ? [] : [draft.output]))],
      hashes: [...run.hashes, ...drafts.flatMap(({ draft }) => draft.hashes)],
    };
  });
};
/**
 * og boardHankoRefreshEvidence without our own Hankos, which og attaches only after the frame certifies (never inside
 * one frame).
 */
const refreshEvidence = (c: AccountReplica, self: EntityId): string => {
  const cur = currentFrameOf(c);
  const w = c.dispute;
  const both = (k: keyof DisputeProof) => [w.current?.[k] ?? null, w.counterparty?.[k] ?? null];
  const peerHankos = [headHankos(c, self)?.peer ?? null, w.counterparty?.hanko ?? null];
  return stableJson([
    String(cur.height),
    cur.hash,
    ...peerHankos,
    ...both("hash"),
    ...both("proofBodyHash"),
    ...both("proofNonce"),
    ...both("proposerIsLeft"),
  ]);
};
/** og processDueHook's hub_rebalance_kick: the periodic rebalance runs at the next wake. */
const kickRebalance = (run: HookRun): HookRun => {
  const task = run.crontab.tasks.get("hubRebalance");
  if (task === undefined) return run;
  return {
    ...run,
    crontab: { ...run.crontab, tasks: mapSet(run.crontab.tasks, "hubRebalance", { ...task, lastRun: 0 }) },
  };
};
/** Whether activation `x` is `y` or later on the J chain. */
const atOrAfter = (x: Activation, y: Activation): boolean =>
  x.jHeight > y.jHeight || (x.jHeight === y.jHeight && x.logIndex >= y.logIndex);
/** The later of two activations on the J chain; a tie keeps the first. */
const laterActivation = (x: Activation, y: Activation): Activation => (atOrAfter(x, y) ? x : y);
/**
 * og scheduleChangedAccountBoardHankoRefreshes (end of the Entity frame): an Account whose certified frame evidence
 * changed and still needs its marker's refresh re-arms the refresh hook now, for the latest such activation. A
 * marker-only change never re-arms.
 */
export const rearmBoardRefreshes = <D extends Folded>(before: Replicas, d: D, now: number): Result<D, EntityError> => {
  const self = d.state.id;
  const changed = (peer: EntityId, c: AccountReplica): boolean => {
    const prev = before.get(peer);
    return prev === undefined || refreshEvidence(prev, self) !== refreshEvidence(c, self);
  };
  const due = [...d.accountReplicas].flatMap(([peer, c]): Activation[] => {
    const m = c.refreshMigration;
    const a = m === undefined ? undefined : activationOf(m);
    return a !== undefined && changed(peer, c) && needsBoardRefresh(c, a) ? [a] : [];
  });
  const latest = due.reduce<Activation | undefined>(
    (best, a) => (best === undefined ? a : laterActivation(best, a)),
    undefined,
  );
  if (latest === undefined) return ok(d);
  return map(crontabOf(d.state), (c) => ({
    ...d,
    state: withCrontab(d.state, scheduleHook(c, refreshHookFor(latest, now, ""))),
  }));
};
type ActivationStep = BoardJEventStep & { readonly accountReplicas: Replicas };
/** A peer's activation: a certified Account with it arms the 24h counterparty refresh deadline hook. */
const peerActivation = (
  step: BoardJEventStep,
  replicas: Replicas,
  crontab: Crontab,
  activated: string,
  a: Activation,
  now: number,
): ActivationStep => {
  const child = replicas.get(activated as EntityId);
  if (child === undefined || currentFrameOf(child).height < 1n) return { ...step, accountReplicas: replicas };
  const hook: ScheduledHook = {
    id: `counterparty-board-hanko-refresh:${activated}:${a.jHeight}:${a.logIndex}`,
    triggerAt: now + COUNTERPARTY_BOARD_HANKO_REFRESH_DEADLINE_MS,
    type: "counterparty_board_hanko_refresh_deadline",
    data: { accountId: activated, activationJHeight: a.jHeight, activationLogIndex: a.logIndex },
  };
  const awaiting = status(`⏳ Awaiting current-board Account Hanko refresh from ${activated.slice(-4)} within 24h`);
  return {
    state: withCrontab(step.state, scheduleHook(crontab, hook)),
    events: [...step.events, awaiting],
    accountReplicas: replicas,
  };
};
/**
 * Our own activation: every certified Account is marked `pending` (a genesis Account's marker cleared) and the refresh
 * hook arms now, or is cancelled with nothing to refresh.
 */
const ownActivation = (
  step: BoardJEventStep,
  replicas: Replicas,
  crontab: Crontab,
  a: Activation,
  now: number,
): ActivationStep => {
  const remarked = [...replicas].flatMap(([peer, c]): (readonly [EntityId, AccountReplica])[] => {
    if (currentFrameOf(c).height >= 1n) return [[peer, withMarker(c, refreshMarker(a, "pending"))]];
    return c.refreshMigration === undefined ? [] : [[peer, withMarker(c, undefined)]];
  });
  const next =
    remarked.length === 0
      ? cancelHook(crontab, BOARD_HANKO_REFRESH_HOOK_ID)
      : scheduleHook(crontab, refreshHookFor(a, now, ""));
  const state = next === crontab ? step.state : withCrontab(step.state, next);
  return { ...step, state, accountReplicas: remarked.length === 0 ? replicas : new Map([...replicas, ...remarked]) };
};
/**
 * og applyCertifiedBoardJEvent's BoardActivated tail (markBoardRotationHankoRefreshesPending): our own activation
 * re-marks our certified Accounts; a peer's activation arms its 24h counterparty deadline hook.
 */
const boardActivation = (
  step: BoardJEventStep,
  replicas: Replicas,
  event: Extract<JEvent, { type: "BoardActivated" }>,
  blockNumber: number,
  now: number,
): Result<ActivationStep, EntityError> => {
  const height = event.meta?.blockNumber ?? blockNumber;
  const jHeight = Number(height);
  const logIndex = Number(event.meta?.logIndex);
  if (!Number.isSafeInteger(jHeight) || jHeight < 1)
    return invariant(`BOARD_HANKO_REFRESH_ACTIVATION_HEIGHT_INVALID:${String(height)}`);
  if (!Number.isSafeInteger(logIndex) || logIndex < 0)
    return invariant(`BOARD_HANKO_REFRESH_ACTIVATION_LOG_INDEX_INVALID:${String(event.meta?.logIndex)}`);
  const a: Activation = { jHeight, logIndex };
  const activated = lower(event.entityId);
  return map(crontabOf(step.state), (crontab) =>
    activated === lower(step.state.id)
      ? ownActivation(step, replicas, crontab, a, now)
      : peerActivation(step, replicas, crontab, activated, a, now),
  );
};
const retryDeadline = (
  run: HookRun,
  hook: Extract<ScheduledHook, { type: "dispute_deadline" }>,
  ms: number,
  now: number,
): HookRun => ({
  ...run,
  crontab: scheduleHook(run.crontab, {
    id: hook.id,
    triggerAt: now + ms,
    type: "dispute_deadline",
    data: { accountId: hook.data.accountId },
  }),
});
const latchRun = (
  run: HookRun,
  peer: string,
  child: DisputedAccount,
  active: ActiveDispute,
  queued: boolean,
): HookRun => ({ ...run, ...latchFinalize(run, peer, child, active, queued) });
/** Where an Account's dispute finalization already sits in the J batch, and whether a batch is in flight. */
type FinalizeQueue = {
  readonly draft: boolean;
  readonly sent: boolean;
  readonly recovery: boolean;
  readonly inFlight: boolean;
};
const finalizeQueue = (jb: CommittedJBatch | undefined, accountId: string): FinalizeQueue => {
  const target = lower(accountId);
  const has = (rows: readonly Binary[] | undefined): boolean =>
    (rows ?? []).some((r) => lower((r as { readonly counterentity?: string }).counterentity) === target);
  return {
    draft: has(jb?.batch["disputeFinalizations"]),
    sent: has(jb?.sentBatch?.batch["disputeFinalizations"]),
    recovery: (jb?.recoveryBatches ?? []).some((b) => has(b["disputeFinalizations"])),
    inFlight: jb?.sentBatch !== undefined,
  };
};
const autoFinalizeMode = (state: EntityState): string | undefined =>
  (state.committed["hubRebalanceConfig"] as { readonly disputeAutoFinalizeMode?: string } | undefined)
    ?.disputeAutoFinalizeMode;
/**
 * og processDisputeDeadlineHook: wait for the observed start and its timeout, defer behind a sent batch, latch an
 * already drafted finalization, else finalize (one per batch).
 */
const disputeDeadline = (
  run: HookRun,
  hook: Extract<ScheduledHook, { type: "dispute_deadline" }>,
  now: number,
): HookRun => {
  const accountId = hook.data.accountId;
  const child = run.accountReplicas.get(accountId as EntityId);
  const active = child === undefined ? undefined : activeOf(child);
  if (child === undefined || active === undefined || autoFinalizeMode(run.state) === "ignore") return run;
  const timeoutSec = Number(active.disputeTimeout || 0);
  const nowSec = Math.floor(now / 1000);
  if (active.observedOnChain !== true) return retryDeadline(run, hook, 5000, now);
  if (!timeoutSec || nowSec < timeoutSec) return retryDeadline(run, hook, 1000, now);
  const queue = finalizeQueue(committedJBatch(run.state), accountId);
  const disputed = child as DisputedAccount;
  if (queue.sent || queue.inFlight) {
    const latched = latchRun(run, accountId, disputed, active, queue.sent || active.finalizeQueued);
    return retryDeadline(latched, hook, 1000, now);
  }
  if (queue.draft || queue.recovery) return { ...latchRun(run, accountId, disputed, active, true), broadcast: true };
  const reset = active.finalizeQueued ? latchRun(run, accountId, disputed, active, false) : run;
  return reset.finalize.length > 0
    ? retryDeadline(reset, hook, 1, now)
    : { ...reset, finalize: [...reset.finalize, accountId] };
};
const withPaybook = (run: HookRun, paybook: Paybook): HookRun => ({ ...run, state: { ...run.state, paybook } });
/**
 * og processSecretAckTimeout: an unacknowledged revealed secret whose lock still stands prepares a dispute; a full J
 * batch re-arms the deadline.
 */
const secretAckTimeout = (
  run: HookRun,
  hook: Extract<DerivedDeadline, { type: "htlc_secret_ack_timeout" }>,
  now: number,
): Result<HookRun, EntityError> => {
  const { hashlock, counterpartyEntityId: cp } = hook.data;
  const paybook = run.state.paybook ?? EMPTY_PAYBOOK;
  const route = paybook.entries.get(hashlock);
  if (route === undefined) return ok(run);
  if (!secretAckPending(route) || now < (route.secretAckDeadlineAt ?? 0))
    return route.secretAckPending ? invariant(`HTLC_SECRET_ACK_ROUTE_INVALID:${hashlock}`) : ok(run);
  const child = run.accountReplicas.get(cp as EntityId);
  if (child === undefined) return ok(run);
  if (!child.state.locks.has(hashlock))
    return ok(withPaybook(run, { ...paybook, entries: mapDelete(paybook.entries, hashlock) }));
  if (activeOf(child) !== undefined) return ok(run);
  const queued = (committedJBatch(run.state)?.batch["disputeStarts"] ?? []).length;
  if (queued + run.prepare.size >= J_BATCH_LIMITS.maxDisputeStarts && !run.prepare.has(cp)) {
    const rearmed: PaybookEntry = { ...route, secretAckDeadlineAt: now + HTLC_SECRET_ACK_TIMEOUT_MS };
    return ok(withPaybook(run, { ...paybook, entries: mapSet(paybook.entries, hashlock, rearmed) }));
  }
  return ok({ ...run, prepare: mapSet(run.prepare, cp, "auto-prepare-dispute-after-secret-ack-timeout") });
};
/**
 * og extensions/lending.ts projectedHubCreditLimit: the credit we grant the borrower (our peerCreditLimit) once our
 * pending frame, the mempool and the Account txs this wake already returned land.
 */
const projectedHubCredit = (
  c: AccountReplica,
  self: EntityId,
  borrower: string,
  queued: readonly AccountTxTarget[],
  tk: TokenId,
): bigint => {
  const d = c.state.account.deltas.get(tk);
  const party = partyOf(replicaId(c), self);
  const selfIsLeft = party.ok && party.value.left;
  const limitSet = (tx: AccountTx): bigint | undefined => {
    switch (tx.type) {
      case "set_credit_limit":
        return tx.tokenId === tk ? tx.limit : undefined;
      case "lending_credit":
        return tx.tokenId === tk ? tx.creditLimit : undefined;
      default:
        return undefined;
    }
  };
  const pending = c._tag === "proposed" ? c.candidate.frame.txs : [];
  const returned = queued.filter((q) => lower(q.accountId) === borrower).map((q) => q.tx);
  const committed = d === undefined ? 0n : selfIsLeft ? d.rightCreditLimit : d.leftCreditLimit;
  return [...pending, ...c.mempool, ...returned].reduce((limit, tx) => limitSet(tx) ?? limit, committed);
};
/**
 * og settleOverdueLendingLoan (tx/handlers/account/committed-lending-close.ts): an active loan past due defaults, its
 * principal returns to the pool and the borrower's credit line is called in by a `lending_credit` revoke; a loan
 * without its pool or Account is dropped.
 */
const lendingOverdue = (run: HookRun, loanId: string, now: number): HookRun => {
  const book = lendingBook(run.state);
  const loan = book?.loans.get(loanId);
  if (book === undefined || loan === undefined || loan.status !== "active" || loan.dueAt > now) return run;
  const pool = book.pools.get(loan.positionId);
  const borrower = lower(loan.borrowerEntityId);
  const child = run.accountReplicas.get(borrower as EntityId);
  const principal = loan.principalAmount;
  if (pool === undefined || child === undefined || pool.borrowedAmount < principal) return run;
  const tk = String(loan.tokenId) as TokenId;
  const current = projectedHubCredit(child, run.state.id, borrower, run.accountTxs, tk);
  const returnedPool: LendingPool = {
    ...pool,
    borrowedAmount: pool.borrowedAmount - principal,
    availableAmount: pool.availableAmount + principal,
    updatedAt: now,
  };
  const lending: LendingBook = {
    ...book,
    loans: mapSet(book.loans, loanId, { ...loan, status: "defaulted", updatedAt: now }),
    pools: mapSet(book.pools, loan.positionId, returnedPool),
  };
  const revoke: AccountTxTarget = {
    accountId: loan.borrowerEntityId,
    tx: {
      type: "lending_credit",
      action: "revoke",
      loanId,
      hubEntityId: lower(run.state.id),
      borrowerEntityId: loan.borrowerEntityId,
      tokenId: tk,
      creditLimit: current > principal ? current - principal : 0n,
    },
  };
  const state = { ...run.state, committed: { ...run.state.committed, lending: lending as unknown as Binary } };
  return { ...run, state, accountTxs: [...run.accountTxs, revoke] };
};
// ---- og tx/handlers/account/committed-lending-followup.ts + committed-lending-close.ts + extensions/lending.ts: the hub's lending book producer ----
/** og LENDING_TERM_MS. */
const LENDING_TERM_MS: { readonly [term: string]: number } = { "1h": 3_600_000, "1d": 86_400_000, "1m": 2_592_000_000 };
/** og computeLendingInterest: a positive rate on a positive principal never rounds to zero. */
export const lendingInterest = (principal: bigint, bps: number): bigint => {
  if (principal <= 0n || bps <= 0) return 0n;
  const raw = (principal * BigInt(bps)) / 10_000n;
  return raw === 0n ? 1n : raw;
};
/** The facts a loan id commits to. */
type LoanRequest = {
  readonly hubEntityId: string;
  readonly borrowerEntityId: string;
  readonly tokenId: number;
  readonly amount: bigint;
  readonly termId: string;
  readonly openedAt: number;
  readonly requestId?: string | undefined;
};
/** og buildLendingLoanId: `loan-` and 16 hex chars of keccak256 over the `|`-joined request facts. */
export const lendingLoanId = (i: LoanRequest): string => {
  const facts = [
    "loan",
    i.hubEntityId.toLowerCase(),
    i.borrowerEntityId.toLowerCase(),
    i.tokenId,
    i.amount.toString(),
    i.termId,
    i.openedAt,
    i.requestId ?? "",
  ];
  return `loan-${keccak256Hex(utf8(facts.map(String).join("|"))).slice(2, 18)}`;
};
/**
 * og selectBestLendingPool: the cheapest open pool of the token and term that covers the amount, then the oldest, then
 * the position id.
 */
const bestLendingPool = (
  book: LendingBook,
  tokenId: number,
  amount: bigint,
  termId: string,
  maxInterestBps: number,
): LendingPool | undefined =>
  [...book.pools.values()]
    .filter(
      (p) =>
        p.status === "open" &&
        p.tokenId === tokenId &&
        p.termId === termId &&
        p.availableAmount >= amount &&
        p.interestBps <= maxInterestBps,
    )
    .toSorted(
      (a, b) => a.interestBps - b.interestBps || a.createdAt - b.createdAt || asc(a.positionId, b.positionId),
    )[0];
const EMPTY_LENDING_BOOK: LendingBook = { pools: new Map(), loans: new Map() };
const withPool = (book: LendingBook, p: LendingPool): LendingBook => ({
  ...book,
  pools: mapSet(book.pools, p.positionId, p),
});
const withLoan = (book: LendingBook, l: LendingLoan): LendingBook => ({
  ...book,
  loans: mapSet(book.loans, l.loanId, l),
});
const withLendingBook = (state: EntityState, book: LendingBook): EntityState =>
  ({ ...state, committed: { ...state.committed, lending: book as unknown as Binary } });
/** og's `lending_credit` revoke: the borrower's credit line shrinks by the loan's principal, never below zero. */
const creditRevoke = (loan: LendingLoan, hub: string, tokenId: TokenId, current: bigint): AccountTxTarget => ({
  accountId: loan.borrowerEntityId,
  tx: {
    type: "lending_credit",
    action: "revoke",
    loanId: loan.loanId,
    hubEntityId: hub,
    borrowerEntityId: loan.borrowerEntityId,
    tokenId,
    creditLimit: current > loan.principalAmount ? current - loan.principalAmount : 0n,
  },
});
/** One committed Account frame and its proposer, as og applyCommittedAccountFrameFollowups sees it. */
export type LendingFrame = { readonly frame: Pick<AccountFrame, "timestamp" | "txs">; readonly proposer: string };
export type LendingFollowup = { readonly state: EntityState; readonly accountTxs: readonly AccountTxTarget[] };
type LendingRun = { readonly book: LendingBook | undefined; readonly accountTxs: readonly AccountTxTarget[] };
/** What one committed lending tx sees: the hub, the peer's Account, the book so far, the tx's proposer and the time. */
type LendingCtx = {
  readonly hub: string;
  readonly cp: string;
  readonly child: AccountReplica;
  readonly book: LendingBook;
  readonly proposer: string;
  readonly now: number;
  /** og projectedHubCreditLimit for the peer, after the Account txs returned so far. */
  readonly credit: (tk: TokenId) => bigint;
};
/** A lending tx's effect: the next book and, at most, one Account tx back to the peer. */
type LendingMove = { readonly book: LendingBook; readonly out?: AccountTxTarget | undefined };
/** og lending_fund: the lender opens a pool with its whole principal available. */
const lendingFund = (x: LendingCtx, tx: TxOf<"lending_fund">): Result<LendingMove, EntityError> => {
  if (x.proposer !== lowerText(tx.lenderEntityId) || x.proposer !== x.cp)
    return invariant(`LENDING_FUND_PROPOSER_MISMATCH:${tx.positionId}`);
  if (x.book.pools.has(tx.positionId)) return invariant(`LENDING_POSITION_ALREADY_EXISTS:${tx.positionId}`);
  const pool: LendingPool = {
    positionId: tx.positionId,
    hubEntityId: x.hub,
    lenderEntityId: x.proposer,
    tokenId: Number(tx.tokenId),
    principalAmount: tx.amount,
    availableAmount: tx.amount,
    borrowedAmount: 0n,
    interestBps: tx.interestBps,
    termId: tx.termId,
    termMs: LENDING_TERM_MS[tx.termId] as number,
    createdAt: x.now,
    updatedAt: x.now,
    status: "open",
  };
  return ok({ book: withPool(x.book, pool) });
};
/**
 * og lending_borrow_request: the cheapest covering pool lends, the loan opens and the borrower's credit line grows by
 * the amount.
 */
const lendingBorrow = (x: LendingCtx, tx: TxOf<"lending_borrow_request">): Result<LendingMove, EntityError> => {
  if (x.proposer !== lowerText(tx.borrowerEntityId) || x.proposer !== x.cp)
    return invariant(`LENDING_BORROW_PROPOSER_MISMATCH:${tx.requestId}`);
  const tk = Number(tx.tokenId);
  const p = bestLendingPool(x.book, tk, tx.amount, tx.termId, tx.maxInterestBps);
  if (p === undefined) return invariant(`LENDING_LIQUIDITY_UNAVAILABLE:${tx.requestId}`);
  const request = {
    hubEntityId: x.hub,
    borrowerEntityId: x.proposer,
    tokenId: tk,
    amount: tx.amount,
    termId: tx.termId,
    openedAt: x.now,
    requestId: tx.requestId,
  };
  const loanId = lendingLoanId(request);
  if (x.book.loans.has(loanId)) return invariant(`LENDING_LOAN_ALREADY_EXISTS:${loanId}`);
  const interestAmount = lendingInterest(tx.amount, p.interestBps);
  const drawn: LendingPool = {
    ...p,
    availableAmount: p.availableAmount - tx.amount,
    borrowedAmount: p.borrowedAmount + tx.amount,
    updatedAt: x.now,
  };
  const loan: LendingLoan = {
    requestId: tx.requestId,
    loanId,
    hubEntityId: x.hub,
    borrowerEntityId: x.proposer,
    lenderEntityId: p.lenderEntityId,
    positionId: p.positionId,
    tokenId: tk,
    principalAmount: tx.amount,
    interestAmount,
    repaymentAmount: tx.amount + interestAmount,
    repaidAmount: 0n,
    interestBps: p.interestBps,
    termId: p.termId,
    termMs: p.termMs,
    openedAt: x.now,
    dueAt: x.now + p.termMs,
    updatedAt: x.now,
    status: "opening",
  };
  const grant: AccountTxTarget = {
    accountId: x.proposer,
    tx: {
      type: "lending_credit",
      action: "grant",
      loanId,
      hubEntityId: x.hub,
      borrowerEntityId: x.proposer,
      tokenId: tx.tokenId,
      creditLimit: x.credit(tx.tokenId) + tx.amount,
    },
  };
  return ok({ book: withLoan(withPool(x.book, drawn), loan), out: grant });
};
/** og lending_credit grant: the granted credit line activates an opening loan. */
const lendingGrant = (x: LendingCtx, loan: LendingLoan): Result<LendingMove, EntityError> =>
  (loan.status === "opening"
    ? ok({ book: withLoan(x.book, { ...loan, status: "active", updatedAt: x.now }) })
    : invariant(`LENDING_GRANT_STATUS_INVALID:${loan.loanId}:${loan.status}`));
/** og lending_credit revoke: the revoked line repays a closing loan and returns principal plus interest to its pool. */
const lendingRevoke = (x: LendingCtx, loan: LendingLoan): Result<LendingMove, EntityError> => {
  // og: the overdue settlement already released the pool at the derived deadline; this revoke only lands the
  // credit-line reduction
  if (loan.status === "defaulted") return ok({ book: withLoan(x.book, { ...loan, updatedAt: x.now }) });
  if (loan.status !== "closing") return invariant(`LENDING_REVOKE_STATUS_INVALID:${loan.loanId}:${loan.status}`);
  const p = x.book.pools.get(loan.positionId);
  if (p === undefined) return invariant(`LENDING_POOL_MISSING_FOR_LOAN:${loan.loanId}`);
  if (p.borrowedAmount < loan.principalAmount) return invariant(`LENDING_POOL_BORROWED_UNDERFLOW:${p.positionId}`);
  const repaid: LendingLoan = { ...loan, repaidAmount: loan.repaymentAmount, status: "repaid", updatedAt: x.now };
  const released: LendingPool = {
    ...p,
    borrowedAmount: p.borrowedAmount - loan.principalAmount,
    availableAmount: p.availableAmount + loan.repaymentAmount,
    updatedAt: x.now,
  };
  return ok({ book: withPool(withLoan(x.book, repaid), released) });
};
/** og lending_credit: only the hub moves a loan's credit line, and only for a loan it holds. */
const lendingCredit = (x: LendingCtx, tx: TxOf<"lending_credit">): Result<LendingMove, EntityError> => {
  if (x.proposer !== x.hub) return invariant(`LENDING_CREDIT_PROPOSER_MISMATCH:${tx.loanId}`);
  const loan = x.book.loans.get(tx.loanId);
  if (loan === undefined) return invariant(`LENDING_CREDIT_LOAN_MISSING:${tx.loanId}`);
  return tx.action === "grant" ? lendingGrant(x, loan) : lendingRevoke(x, loan);
};
/**
 * og lending_repay: the borrower pays the exact outstanding amount; the loan starts closing and its credit line is
 * revoked.
 */
const lendingRepay = (x: LendingCtx, tx: TxOf<"lending_repay">): Result<LendingMove, EntityError> => {
  if (x.proposer !== lowerText(tx.borrowerEntityId) || x.proposer !== x.cp)
    return invariant(`LENDING_REPAY_PROPOSER_MISMATCH:${tx.loanId}`);
  const loan = x.book.loans.get(tx.loanId);
  if (loan === undefined || loan.status !== "active") return invariant(`LENDING_REPAY_LOAN_NOT_ACTIVE:${tx.loanId}`);
  const exact =
    loan.borrowerEntityId === x.proposer &&
    loan.tokenId === Number(tx.tokenId) &&
    tx.amount === loan.repaymentAmount - loan.repaidAmount;
  if (!exact) return invariant(`LENDING_REPAYMENT_MISMATCH:${tx.loanId}`);
  const tk = String(loan.tokenId) as TokenId;
  return ok({
    book: withLoan(x.book, { ...loan, status: "closing", updatedAt: x.now }),
    out: creditRevoke(loan, x.hub, tk, x.credit(tk)),
  });
};
/** og getAccountOutCapacity: the hub's outCapacity on the token, 0 without the delta. */
const hubOutCapacity = (child: AccountReplica, hub: string, tk: TokenId): bigint => {
  const d = child.state.account.deltas.get(tk), hubIsLeft = lowerText(child.state.account.id.left) === hub;
  return d === undefined ? 0n : outCapacity(d, hubIsLeft, holds(child.state, tk, hubIsLeft));
};
/**
 * og lending_close_request: the lender closes an idle open pool; its available funds pay out through the Account, which
 * must carry them.
 */
const lendingCloseRequest = (
  x: LendingCtx,
  tx: TxOf<"lending_close_request">,
): Result<LendingMove, EntityError> => {
  if (x.proposer !== lowerText(tx.lenderEntityId) || x.proposer !== x.cp)
    return invariant(`LENDING_CLOSE_PROPOSER_MISMATCH:${tx.positionId}`);
  const p = x.book.pools.get(tx.positionId);
  if (p === undefined || p.status !== "open" || p.lenderEntityId !== x.proposer)
    return invariant(`LENDING_CLOSE_POSITION_NOT_OPEN:${tx.positionId}`);
  if (p.borrowedAmount !== 0n) return invariant(`LENDING_CLOSE_ACTIVE_LOANS:${p.positionId}`);
  if (p.availableAmount === 0n) return ok({ book: withPool(x.book, { ...p, status: "closed", updatedAt: x.now }) });
  const tk = String(p.tokenId) as TokenId;
  const payout = hubOutCapacity(x.child, x.hub, tk);
  if (payout < p.availableAmount)
    return invariant(`LENDING_CLOSE_PAYOUT_CAPACITY: available=${payout} required=${p.availableAmount}`);
  const pay: AccountTxTarget = {
    accountId: x.proposer,
    tx: {
      type: "lending_close_payout",
      positionId: p.positionId,
      hubEntityId: x.hub,
      lenderEntityId: x.proposer,
      tokenId: tk,
      amount: p.availableAmount,
    },
  };
  return ok({ book: withPool(x.book, { ...p, status: "closing", updatedAt: x.now }), out: pay });
};
/** og lending_close_payout: the hub's payout of the whole available amount closes the pool. */
const lendingClosePayout = (x: LendingCtx, tx: TxOf<"lending_close_payout">): Result<LendingMove, EntityError> => {
  if (x.proposer !== x.hub) return invariant(`LENDING_PAYOUT_PROPOSER_MISMATCH:${tx.positionId}`);
  const p = x.book.pools.get(tx.positionId);
  if (p === undefined || p.status !== "closing")
    return invariant(`LENDING_PAYOUT_POSITION_NOT_CLOSING:${tx.positionId}`);
  const exact =
    p.lenderEntityId === lowerText(tx.lenderEntityId) &&
    p.tokenId === Number(tx.tokenId) &&
    p.availableAmount === tx.amount;
  if (!exact) return invariant(`LENDING_PAYOUT_MISMATCH:${tx.positionId}`);
  return ok({ book: withPool(x.book, { ...p, availableAmount: 0n, status: "closed", updatedAt: x.now }) });
};
/** The lending tx's move on the book; `undefined` for a tx the lending book does not follow. */
const lendingMove = (x: LendingCtx, tx: AccountTx): Result<LendingMove, EntityError> | undefined => {
  switch (tx.type) {
    case "lending_fund": return lendingFund(x, tx);
    case "lending_borrow_request": return lendingBorrow(x, tx);
    case "lending_credit": return lendingCredit(x, tx);
    case "lending_repay": return lendingRepay(x, tx);
    case "lending_close_request": return lendingCloseRequest(x, tx);
    case "lending_close_payout": return lendingClosePayout(x, tx);
    default: return undefined;
  }
};
const isHubEntity = (state: EntityState): boolean =>
  (state.committed["profile"] as { readonly isHub?: unknown } | undefined)?.isHub === true;
/**
 * og applyCommittedLendingFollowup for every tx of the committed frames of one Account input: on a hub
 * (profile.isHub), a lending tx naming this hub moves the committed `lending` book (fund opens a pool, borrow opens a
 * loan and grants the credit line, the grant activates it, repay closes it and revokes the line, the revoke repays the
 * pool, close request / payout close the pool). The returned Account txs are admitted afterwards (og
 * applyLocalAccountEffects). Every og refusal is a plain Error, so it refuses the whole input. `timestamp` is the
 * Entity frame time (og state.timestamp).
 */
export const lendingFollowups = (
  state: EntityState,
  replicas: Replicas,
  peerRaw: string,
  frames: readonly LendingFrame[],
  timestamp: bigint,
  queued: readonly AccountTxTarget[] = [],
): Result<LendingFollowup, EntityError> => {
  if (!isHubEntity(state)) return ok({ state, accountTxs: queued });
  const hub = lowerText(state.id);
  const cp = lowerText(peerRaw);
  const child = replicas.get(cp as EntityId);
  const before = lendingBook(state);
  const steps = frames.flatMap(({ frame, proposer }) => {
    const now = Math.max(Math.floor(Number(frame.timestamp)), Math.floor(Number(timestamp)));
    return frame.txs.map((tx) => ({ tx, proposer: lowerText(proposer), now }));
  });
  const follow = (run: LendingRun, { tx, proposer, now }: (typeof steps)[number]): Result<LendingRun, EntityError> => {
    if (!("hubEntityId" in tx) || lowerText(tx.hubEntityId) !== hub) return ok(run);
    if (child === undefined) return invariant(`LENDING_ACCOUNT_MISSING:${peerRaw}`);
    // og ensureLendingState: the first lending tx naming this hub creates the (committed) book
    const book = run.book ?? EMPTY_LENDING_BOOK;
    const credit = (tk: TokenId): bigint => projectedHubCredit(child, state.id, cp, run.accountTxs, tk);
    const move = lendingMove({ hub, cp, child, book, proposer, now, credit }, tx);
    if (move === undefined) return ok(run);
    return map(move, ({ book: next, out }) => ({
      book: next,
      accountTxs: out === undefined ? run.accountTxs : [...run.accountTxs, out],
    }));
  };
  return map(foldResult(steps, { book: before, accountTxs: queued }, follow), (run): LendingFollowup => ({
    state: run.book === before || run.book === undefined ? state : withLendingBook(state, run.book),
    accountTxs: run.accountTxs,
  }));
};
type RefreshDeadline = Extract<ScheduledHook, { type: "counterparty_board_hanko_refresh_deadline" }>;
/**
 * og counterparty board Hanko refresh deadline: without the peer's refresh for this activation (or a later one), a
 * certified Account not already in dispute prepares one.
 */
const refreshDeadlineExpired = (run: HookRun, hook: RefreshDeadline): HookRun => {
  const h = hook.data, child = run.accountReplicas.get(h.accountId as EntityId);
  if (child === undefined || currentFrameOf(child).height < 1n) return run;
  const b = child.boardRefresh, refreshed = b !== undefined && atOrAfter(activationOf(b), activationOf(h));
  const disputing = activeOf(child) !== undefined || (child._tag === "preparing" && child.prepare !== undefined);
  if (refreshed || disputing) return run;
  return { ...run, prepare: mapSet(run.prepare, h.accountId, "counterparty-board-hanko-refresh-deadline-expired") };
};
/** og processDueHook. */
const dueHook = (run: HookRun, hook: DueHook, now: number, first: string): Result<HookRun, EntityError> => {
  switch (hook.type) {
    case "htlc_timeout": {
      const stands = run.accountReplicas.get(hook.data.accountId as EntityId)?.state.locks.has(hook.data.lockId);
      return ok(stands ? { ...run, timeouts: [...run.timeouts, hook.data] } : run);
    }
    case "dispute_deadline":
      return ok(disputeDeadline(run, hook, now));
    case "htlc_secret_ack_timeout":
      return secretAckTimeout(run, hook, now);
    case "settlement_window":
    case "watchdog":
      return ok(run);
    case "hub_rebalance_kick":
      return ok(kickRebalance(run));
    case "board_hanko_refresh":
      return boardRefreshHook(run, hook, now);
    case "lending_overdue":
      return ok(lendingOverdue(run, hook.data.loanId, now));
    case "counterparty_board_hanko_refresh_deadline":
      return ok(refreshDeadlineExpired(run, hook));
    case "cross_j_orderbook_sweep": {
      const sweep: WakeTx = {
        type: "orderbookSweepCrossJurisdiction",
        data: { reason: String(hook.data.reason || "cross-j-orderbook-sweep") },
      };
      return ok({ ...run, outputs: [...run.outputs, { signerId: first, txs: [sweep] }] });
    }
  }
};
/**
 * og appendBatchedHookOutputs: the HTLC timeouts, the dispute preparations, then the finalization with its j_broadcast
 * (or a lone j_broadcast for a drafted one).
 */
const batchedHookOutputs = (run: HookRun, first: string, manualBroadcast: boolean): readonly WakeOutput[] => {
  const input = (txs: readonly WakeTx[]): readonly WakeOutput[] => (txs.length === 0 ? [] : [{ signerId: first, txs }]);
  const broadcast: WakeTx = { type: "j_broadcast", data: {} };
  const timeouts: readonly WakeTx[] =
    run.timeouts.length === 0 ? [] : [{ type: "processHtlcTimeouts", data: { expiredLocks: run.timeouts } }];
  const prepares = [...run.prepare].map(([cp, description]): WakeTx => ({
    type: "prepareDispute",
    data: { counterpartyEntityId: cp as EntityId, description },
  }));
  const finalizes = run.finalize.map((cp): WakeTx => ({
    type: "disputeFinalize",
    data: {
      counterpartyEntityId: cp as EntityId,
      description: "auto-finalize-after-timeout",
      useOnchainRegistry: true,
    },
  }));
  const closing =
    finalizes.length > 0 ? [...finalizes, broadcast] : run.broadcast && !manualBroadcast ? [broadcast] : [];
  return [...run.outputs, ...input(timeouts), ...input(prepares), ...input(closing)];
};
// ---- og entity/scheduler/rebalance.ts hubRebalanceHandler, entity/account/account-work-flags.ts ACCOUNT_WORK_REBALANCE ----
export const HUB_PENDING_BROADCAST_STALE_MS = 120_000;
const HUB_MAX_R2C_PER_TICK = 10;
const HUB_MAX_C2R_PER_TICK = 10;
/** An Account token seen from one side: its collateral, the peer credit it uses and its own holds. */
type ViewerDelta = { readonly outCollateral: bigint; readonly outPeerCredit: bigint; readonly outTotalHold: bigint };
/** og deriveDelta from the viewer's side: outCollateral, outPeerCredit and outTotalHold (the viewer's own hold). */
const viewerDelta = (a: AccountBody, d: Delta, viewerIsLeft: boolean): ViewerDelta => {
  const total = d.ondelta + d.offdelta;
  const collateral = floor0(d.collateral);
  const t = sideTotals(a, d.tokenId);
  if (!viewerIsLeft)
    return {
      outCollateral: total > 0n ? floor0(collateral - total) : collateral,
      outPeerCredit: floor0(-total),
      outTotalHold: t.rightHold,
    };
  const outCollateral = total <= 0n ? 0n : total > collateral ? collateral : total;
  return { outCollateral, outPeerCredit: floor0(total - collateral), outTotalHold: t.leftHold };
};
/** og's Account token maps iterate in radix key order: ascending tokenId. */
const byTokenAsc = <V,>(m: ReadonlyMap<TokenId, V>): readonly (readonly [TokenId, V])[] =>
  [...m].toSorted(([a], [b]) => Number(a) - Number(b));
/** og `account.pendingFrame || hasPendingSettlementTransition(account)`. */
const accountBusy = (c: AccountReplica): boolean => c._tag === "proposed" || settlePending(c);
/**
 * og's executable C→R workspace: ready, pure C→R, last touched and executed by the owner, and holding the peer's
 * settlement Hanko.
 */
const readyC2RWorkspace = (w: SettlementWorkspace, ownerIsLeft: boolean): boolean => {
  const ownersMove =
    w.status === "ready_to_submit" && w.lastModifiedByLeft === ownerIsLeft && w.executorIsLeft === ownerIsLeft;
  const pureC2R = w.ops.length > 0 && w.ops.every((op) => op.type === "c2r");
  return ownersMove && pureC2R && Boolean(ownerIsLeft ? w.rightHanko : w.leftHanko);
};
/** Collateral the owner may withdraw from one token: its free collateral over the token's default soft limit. */
const withdrawable = (body: AccountBody, ownerIsLeft: boolean, tk: TokenId, d: Delta): Result<boolean, EntityError> => {
  if ((body.requested.get(tk) ?? 0n) > 0n) return ok(false);
  const v = viewerDelta(body, d, ownerIsLeft);
  const available = v.outCollateral - v.outTotalHold;
  return available <= 0n
    ? ok(false)
    : map(rebalanceDefaults(undefined, Number(tk)), (p) => available > p.r2cRequestSoftLimit);
};
/**
 * og hasRebalanceWork: a positive request, an executable C→R workspace, or withdrawable collateral over the token's
 * default soft limit.
 */
const hasRebalanceWork = (self: EntityId, c: AccountReplica): Result<boolean, EntityError> => {
  const body = c.state, ownerIsLeft = sameHex(body.account.id.left, self);
  if ([...body.requested.values()].some((n) => n > 0n)) return ok(true);
  if (accountBusy(c)) return ok(false);
  if (body.settlement !== undefined) return ok(readyC2RWorkspace(body.settlement, ownerIsLeft));
  return someResult(byTokenAsc(body.account.deltas), ([tk, d]) => withdrawable(body, ownerIsLeft, tk, d));
};
/**
 * og getRebalanceAccountIds (the ACCOUNT_WORK_REBALANCE index): the Accounts with rebalance work, in Account-map key
 * order.
 */
export const rebalanceAccountIds = (
  state: EntityState,
  replicas: Replicas,
): Result<readonly EntityId[], EntityError> => {
  const ordered = [...replicas].toSorted(([a], [b]) => asc(a, b));
  const flagged = traverse(ordered, ([peer, c]) =>
    map(hasRebalanceWork(state.id, c), (w): EntityId[] => (w ? [peer] : [])),
  );
  return map(flagged, (xs) => xs.flat());
};
/** og EntityState.reserves as committed (tokenId to amount). */
const committedReserves = (state: EntityState): ReadonlyMap<number, bigint> => {
  const r = state.committed["reserves"];
  return r instanceof Map ? (r as ReadonlyMap<number, bigint>) : new Map();
};
type OgSentBatch = NonNullable<CommittedJBatch["sentBatch"]> & { readonly lastSubmittedAt?: number };
type OgJBatchState = CommittedJBatch & {
  readonly status?: string;
  readonly lastBroadcast?: number;
  readonly sentBatch?: OgSentBatch | undefined;
};
type OgR2CRow = {
  readonly tokenId: number;
  readonly receivingEntity: string;
  readonly pairs: readonly { readonly entity: string; readonly amount: bigint }[];
};
/**
 * og batchAddReserveToCollateral's aggregation: top up the counterparty's pair, add a pair to the (receivingEntity,
 * tokenId) entry, or open a new entry, each within the contract limits.
 */
const withR2CPair = (
  batch: JBatchRows,
  rows: readonly OgR2CRow[],
  entity: string,
  counterparty: string,
  tokenId: number,
  amount: bigint,
): Result<readonly OgR2CRow[], EntityError> => {
  const L = J_BATCH_LIMITS;
  const total = rows.reduce((n, op) => n + op.pairs.length, 0);
  const room = (name: string, cur: number, max: number): Result<void, EntityError> =>
    cur + 1 > max ? invariant(`J_BATCH_LIMIT_EXCEEDED: ${name} ${cur + 1}/${max}`) : ok(undefined);
  const at = rows.findIndex((op) => op.receivingEntity === entity && op.tokenId === tokenId);
  const entry = rows[at];
  const withEntry = (op: OgR2CRow): readonly OgR2CRow[] => rows.map((o, i) => (i === at ? op : o));
  if (entry === undefined) {
    const ops = ogBatchOps(batch) + 1;
    if (ops > L.maxTotalOps)
      return invariant(`J_BATCH_LIMIT_EXCEEDED: reserveToCollateral would exceed total ops ${ops}/${L.maxTotalOps}`);
    const opened: OgR2CRow = { tokenId, receivingEntity: entity, pairs: [{ entity: counterparty, amount }] };
    return map(room("reserveToCollateral.pairs total", total, L.maxReserveToCollateralPairsTotal), () => [
      ...rows,
      opened,
    ]);
  }
  if (entry.pairs.some((p) => p.entity === counterparty)) {
    const pairs = entry.pairs.map((p) => (p.entity === counterparty ? { ...p, amount: p.amount + amount } : p));
    return ok(withEntry({ ...entry, pairs }));
  }
  const fits = checks(
    room("reserveToCollateral.pairs total", total, L.maxReserveToCollateralPairsTotal),
    room("reserveToCollateral.pairs", entry.pairs.length, L.maxReserveToCollateralPairs),
  );
  return map(fits, () => withEntry({ ...entry, pairs: [...entry.pairs, { entity: counterparty, amount }] }));
};
/**
 * og batchAddReserveToCollateral on the committed (og-shaped) jBatchState: aggregate into the (receivingEntity,
 * tokenId) entry within the contract limits.
 */
export const addCommittedR2C = (
  jb: OgJBatchState, entity: string, counterparty: string, tokenId: number, amount: bigint,
): Result<OgJBatchState, EntityError> => {
  if (amount <= 0n) return invariant("R2C_AMOUNT_MUST_BE_POSITIVE");
  if (!Number.isSafeInteger(tokenId) || tokenId <= 0) return invariant("R2C_TOKEN_ID_INVALID");
  if (!entity || !counterparty || entity === counterparty) return invariant("R2C_ACCOUNT_PARTIES_INVALID");
  const rows = (jb.batch["reserveToCollateral"] ?? []) as unknown as readonly OgR2CRow[];
  return map(withR2CPair(jb.batch, rows, entity, counterparty, tokenId, amount), (r2c) => ({
    ...jb,
    batch: { ...jb.batch, reserveToCollateral: r2c as unknown as readonly Binary[] },
    ...(jb.status === "empty" ? { status: "accumulating" } : {}),
  }));
};
/** A peer's funded-or-fundable R→C request: which token, how much, and what ranks it. */
type R2CTarget = {
  readonly counterpartyId: EntityId;
  readonly tokenId: number;
  readonly amount: bigint;
  readonly requestedAt: number;
  readonly feePaidUpfront: bigint;
};
const cmpDesc = (a: bigint, b: bigint): number => (a === b ? 0 : a > b ? -1 : 1);
/**
 * og validateR2CRequestPolicy + evaluateR2CRequest: a prepaid, unsubmitted, current-policy request, capped at the
 * peer's uncollateralized credit.
 */
const r2cTarget = (
  config: HubConfig,
  c: AccountReplica,
  peer: EntityId,
  tk: TokenId,
  requested: bigint,
): Result<R2CTarget | null, EntityError> => {
  if (requested <= 0n) return ok(null);
  const body = c.state;
  const fee = body.requestFees.get(tk);
  const tokenId = Number(tk);
  if (fee === undefined) return invariant(`REBALANCE_REQUEST_FEE_STATE_MISSING:${peer}:${tokenId}`);
  if (fee.refund !== undefined || (body.submittedAt?.get(tokenId) ?? 0) > 0) return ok(null);
  const policyVersion = Number.isFinite(config.policyVersion) && config.policyVersion > 0 ? config.policyVersion : 1;
  if ((fee.policyVersion || 0) !== policyVersion) return ok(null);
  return chain(defaultRebalanceBaseFee(tokenId), (base): Result<R2CTarget | null, EntityError> => {
    if (fee.feePaidUpfront < base + (requested * config.rebalanceLiquidityFeeBps) / 10_000n) return ok(null);
    const d = body.account.deltas.get(tk);
    if (d === undefined) return ok(null);
    const uncollateralized = viewerDelta(body, d, sameHex(body.account.id.left, peer)).outPeerCredit;
    if (uncollateralized <= 0n) return ok(null);
    const amount = requested > uncollateralized ? uncollateralized : requested;
    return ok({
      counterpartyId: peer,
      tokenId,
      amount,
      requestedAt: fee.requestedAt || 0,
      feePaidUpfront: fee.feePaidUpfront,
    });
  });
};
type MatchingStrategy = "amount" | "fee" | "time";
const strategyOf = (config: HubConfig): MatchingStrategy =>
  (config.matchingStrategy === "time" || config.matchingStrategy === "fee" ? config.matchingStrategy : "amount");
/** og's R→C ranking per matching strategy: the largest amount, the highest prepaid fee, or the oldest request first. */
const r2cRank = (strategy: MatchingStrategy) => (a: R2CTarget, b: R2CTarget): number => {
  switch (strategy) {
    case "amount": return cmpDesc(a.amount, b.amount) || a.requestedAt - b.requestedAt;
    case "fee": return cmpDesc(a.feePaidUpfront, b.feePaidUpfront) || cmpDesc(a.amount, b.amount);
    case "time": return a.requestedAt - b.requestedAt || cmpDesc(a.amount, b.amount);
  }
};
type R2CFunding = { readonly reserves: ReadonlyMap<number, bigint>; readonly funded: readonly R2CTarget[] };
/**
 * og collectR2CTargets: the ranked targets funded, at most HUB_MAX_R2C_PER_TICK of them, from the effective reserves;
 * a target its token's reserves cannot fund at all is skipped.
 */
const fundTargets = (ranked: readonly R2CTarget[], reserves: ReadonlyMap<number, bigint>): readonly R2CTarget[] => {
  const fund = (f: R2CFunding, t: R2CTarget): R2CFunding => {
    const have = f.reserves.get(t.tokenId) ?? 0n, amount = t.amount > have ? have : t.amount;
    if (f.funded.length >= HUB_MAX_R2C_PER_TICK || amount <= 0n) return f;
    return { reserves: mapSet(f.reserves, t.tokenId, have - amount), funded: [...f.funded, { ...t, amount }] };
  };
  return ranked.reduce(fund, { reserves, funded: [] }).funded;
};
/** og's hashed submitted markers: each funded Account records its R→C request as submitted now. */
const markSubmitted = (replicas: Replicas, targets: readonly R2CTarget[], now: number): Replicas =>
  targets.reduce((m, t) => {
    const c = m.get(t.counterpartyId) as AccountReplica;
    return mapSet(m, t.counterpartyId, {
      ...c,
      state: setRebalanceSubmittedAt(c.state, t.tokenId, now),
    } as AccountReplica);
  }, replicas);
type C2RPlan = { readonly ops: readonly SettlementOp[]; readonly total: bigint; readonly hubIsLeft: boolean };
/** One Account's C→R work: whether its ready workspace executes, and the withdrawal it would propose. */
type C2RWork = { readonly peer: EntityId; readonly executable: boolean; readonly plan?: C2RPlan | undefined };
/**
 * og collectC2RAccountWork: skip a busy Account, execute a ready workspace, else withdraw every token's free collateral
 * over the default soft limit.
 */
const c2rWork = (
  self: EntityId,
  peer: EntityId,
  c: AccountReplica,
  canTouch: boolean,
): Result<C2RWork, EntityError> => {
  const hubIsLeft = sameHex(c.state.account.id.left, self);
  const w = c.state.settlement;
  if (accountBusy(c)) return ok({ peer, executable: false });
  if (w !== undefined) return ok({ peer, executable: canTouch && readyC2RWorkspace(w, hubIsLeft) });
  const tokens = byTokenAsc(c.state.account.deltas).filter(([tk]) => (c.state.requested.get(tk) ?? 0n) <= 0n);
  const withdrawal = ([tk, delta]: readonly [TokenId, Delta]): Result<readonly SettlementOp[], EntityError> =>
    map(rebalanceDefaults(undefined, Number(tk)), (p) => {
      const v = viewerDelta(c.state, delta, hubIsLeft);
      const free = floor0(v.outCollateral - v.outTotalHold);
      return free <= p.r2cRequestSoftLimit ? [] : [{ type: "c2r", tokenId: Number(tk), amount: free }];
    });
  return map(traverse(tokens, withdrawal), (per) => {
    const ops = per.flat();
    const total = ops.reduce((n, op) => n + (op.type === "c2r" ? op.amount : 0n), 0n);
    return { peer, executable: false, ...(ops.length > 0 && total > 0n ? { plan: { ops, total, hubIsLeft } } : {}) };
  });
};
/**
 * og's rebalance continuation: the largest C→R proposals, the ready C→R executions, then a broadcast when batch work
 * was queued.
 */
const rebalanceTxs = (work: readonly C2RWork[], queuedR2C: boolean, mayBroadcast: boolean): readonly WakeTx[] => {
  const plans = work
    .flatMap((x) => (x.plan === undefined ? [] : [{ peer: x.peer, ...x.plan }]))
    .toSorted((a, b) => cmpDesc(a.total, b.total))
    .slice(0, HUB_MAX_C2R_PER_TICK);
  const executable = work.filter((x) => x.executable).map((x) => x.peer).slice(0, HUB_MAX_C2R_PER_TICK);
  const broadcast = (queuedR2C || executable.length > 0) && mayBroadcast;
  return [
    ...plans.map((p): WakeTx => ({
      type: "settle_propose",
      data: { counterpartyEntityId: p.peer, ops: p.ops, executorIsLeft: p.hubIsLeft, memo: "auto-c2r-rebalance" },
    })),
    ...executable.map((peer): WakeTx => ({ type: "settle_execute", data: { counterpartyEntityId: peer } })),
    ...(broadcast ? [{ type: "j_broadcast", data: {} } satisfies WakeTx] : []),
  ];
};
/** og rejects the tokenless raw overrides older hub configs carried. */
const rawOverrides = (config: HubConfig): readonly string[] => {
  const raw = config as unknown as { readonly [k: string]: unknown };
  return ["rebalanceBaseFee", "c2rWithdrawSoftLimit", "rebalanceGasFee"].filter((k) => raw[k] !== undefined);
};
export type RebalanceRun = Folded & { readonly outputs: readonly WakeOutput[] };
/**
 * og hubRebalanceHandler: a pending sent batch blocks R→C (a stale one only queues og's persisted j_abort_sent_batch);
 * the funded R→C requests join the draft batch with their submitted markers; over-collateralized Accounts propose C→R,
 * ready C→R workspaces execute; batch work broadcasts. All to the committed leader. `runtimeNow` is og's
 * env.state.timestamp, the clock `sentBatch.lastSubmittedAt` is written with. og's REB_STEP debug candidate effects
 * are log-only diagnostics (no state, output or frame effect).
 */
export const hubRebalance = (
  d: Folded,
  now: number,
  runtimeNow: number,
  manualBroadcast: boolean,
): Result<RebalanceRun, EntityError> => {
  const config = hubConfigOf(d.state);
  if (config === undefined) return ok({ ...d, outputs: [] });
  const forbidden = rawOverrides(config);
  if (forbidden.length > 0) return invariant(`HUB_REBALANCE_TOKENLESS_RAW_OVERRIDE_FORBIDDEN:${forbidden.join(",")}`);
  const self = d.state.id;
  const leader = leaderStateOf(d.state).activeValidatorId;
  const jb0 = (committedJBatch(d.state) ?? initJBatch()) as unknown as OgJBatchState;
  const sent = jb0.sentBatch;
  const seeded: Folded = { ...d, state: withCommittedJBatch(d.state, jb0) };
  if (
    sent !== undefined &&
    runtimeNow - (sent.lastSubmittedAt || jb0.lastBroadcast || 0) > HUB_PENDING_BROADCAST_STALE_MS
  ) {
    const abort: WakeTx = {
      type: "j_abort_sent_batch",
      data: { reason: "stale-hub-rebalance-latch", requeueToCurrent: true },
    };
    return ok({ ...seeded, outputs: [{ signerId: leader, txs: [abort] }] });
  }
  const canTouch = sent === undefined;
  return chain(rebalanceAccountIds(seeded.state, seeded.accountReplicas), (ids) => {
    const accounts = ids.map((peer) => [peer, seeded.accountReplicas.get(peer) as AccountReplica] as const);
    const requests = traverse(accounts, ([peer, c]) =>
      traverse(byTokenAsc(c.state.requested), ([tk, n]) => r2cTarget(config, c, peer, tk, n)),
    );
    return chain(requests, (perAccount) => {
      const ranked = perAccount
        .flat()
        .flatMap((t) => (t === null ? [] : [t]))
        .toSorted(r2cRank(strategyOf(config)));
      const targets = canTouch ? fundTargets(ranked, committedReserves(seeded.state)) : [];
      // og queueR2CTargets: the whole draft or nothing (a contract limit rejects the frame), then the hashed submitted
      // markers
      const drafted = foldResult(targets, jb0, (jb, t) =>
        addCommittedR2C(jb, self, t.counterpartyId, t.tokenId, t.amount),
      );
      return chain(drafted, (jb) => {
        const state = withCommittedJBatch(seeded.state, jb);
        const accountReplicas = markSubmitted(seeded.accountReplicas, targets, now);
        return map(
          traverse(accounts, ([peer, c]) => c2rWork(self, peer, c, canTouch)),
          (work): RebalanceRun => {
            const txs = rebalanceTxs(
              work,
              targets.length > 0,
              canTouch && jb.sentBatch === undefined && !manualBroadcast,
            );
            return { state, accountReplicas, outputs: txs.length > 0 ? [{ signerId: leader, txs }] : [] };
          },
        );
      });
    });
  });
};
/**
 * `outputs`: og outputs to this Entity (its collective continuations); `sent`: og outputs to other Entities; `hashes`:
 * og context.hashesToSign.
 */
export type CrontabRun = Folded & {
  readonly outputs: readonly WakeOutput[];
  readonly sent: readonly EntityOutput[];
  readonly hashes: readonly HashToSign[];
  readonly accountTxs: readonly AccountTxTarget[];
};
/**
 * og executeCrontab at the Entity's timestamp `now`: every due hook (the derived per-payment deadlines and the stored
 * hooks, which are removed as they fire) in (triggerAt, id) order, then the due periodic hubRebalance task (og
 * hubRebalanceHandler, a no-op without a hub config). `runtimeNow`: og's env.state.timestamp for the stale sent-batch
 * check (defaults to `now`).
 */
export const executeCrontab = (
  state: EntityState,
  replicas: Replicas,
  now: number,
  manualBroadcast = false,
  runtimeNow = now,
): Result<CrontabRun, EntityError> =>
  chain(crontabOf(state), (crontab) => {
    const stored = [...crontab.hooks.values()].filter((h) => h.triggerAt <= now);
    const due: readonly DueHook[] = [...derivedDeadlines(state, replicas, now), ...stored].toSorted(compareDeadlines);
    const first = signerId([...membersOf(state.quorum).keys()][0] ?? "");
    const start: HookRun = {
      state,
      accountReplicas: replicas,
      crontab: stored.reduce((c, h) => cancelHook(c, h.id), crontab),
      outputs: [],
      timeouts: [],
      prepare: new Map(),
      finalize: [],
      broadcast: false,
      sent: [],
      hashes: [],
      accountTxs: [],
    };
    const hooked = foldResult<HookRun, DueHook, EntityError>(due, start, (run, hook) => dueHook(run, hook, now, first));
    return chain(hooked, (run): Result<CrontabRun, EntityError> => {
      const outputs = due.length === 0 ? [] : batchedHookOutputs(run, first, manualBroadcast);
      const task = run.crontab.tasks.get("hubRebalance");
      const { sent, hashes, accountTxs } = run;
      const settled: CrontabRun = {
        state: withCrontab(run.state, run.crontab),
        accountReplicas: run.accountReplicas,
        outputs,
        sent,
        hashes,
        accountTxs,
      };
      if (task === undefined || !task.enabled || now - task.lastRun < task.intervalMs) return ok(settled);
      return map(hubRebalance(run, now, runtimeNow, manualBroadcast), (reb): CrontabRun => {
        const ran: Crontab = {
          ...run.crontab,
          tasks: mapSet(run.crontab.tasks, "hubRebalance", { ...task, lastRun: now }),
        };
        return {
          ...settled,
          state: withCrontab(reb.state, ran),
          accountReplicas: reb.accountReplicas,
          outputs: [...outputs, ...reb.outputs],
        };
      });
    });
  });
/** og returns the crontab's outputs to other Entities and its hashesToSign beside the approved self txs. */
const withCrontabEffects = (d: Draft, run: CrontabRun): Draft => {
  const hashes = [...run.hashes, ...(d.hashes ?? [])];
  return { ...d, outputs: [...run.sent, ...d.outputs], ...(hashes.length === 0 ? {} : { hashes }) };
};
/**
 * og handleScheduledWakeEntityTx: validate the wake against the frame state, run the crontab at the frame timestamp,
 * then apply its self-directed collective txs in the same frame (og approvedEntityTxs), j_broadcast and
 * j_abort_sent_batch included. orderbookSweepCrossJurisdiction is the cross-j owner's.
 */
const foldWake = (state: EntityState, replicas: Replicas, w: WakeData, ctx: FoldContext): Result<Draft, EntityError> =>
  chain(checkWake(state, w, Number(ctx.timestamp)), () =>
    chain(executeCrontab(state, replicas, Number(ctx.timestamp)), (run) => {
      const approved = run.outputs.flatMap((o) => o.txs) as readonly EntityTx[];
      // og applyLocalAccountEffects: the wake's returned Account txs (a lending_overdue revoke) are admitted before its
      // approved self txs
      const queued = run.accountTxs.reduce(queueReturned, {
        state: run.state,
        accountReplicas: run.accountReplicas,
        outputs: [],
      } as Draft);
      const applied =
        approved.length === 0
          ? ok(queued)
          : foldNested(queued.state, queued.accountReplicas, approved, ctx, "collective");
      return map(applied, (d) => withCrontabEffects(d, run));
    }),
  );
// ---- og entity/tx/handlers/j-batch/{r2r,r2e,e2r,r2c,j-broadcast,j-rebroadcast,j-abort-sent-batch,j-clear-batch,mint-reserves}.ts: the Entity J-batch txs on the committed (og-shaped) jBatchState ----
type OgRow = { readonly [field: string]: unknown };
type OgBatchRows = { readonly [field: string]: readonly Binary[] };
const bigOf = (v: unknown): bigint => (typeof v === "bigint" ? v : BigInt(v as number | string));
const ogStr = (r: OgRow, k: string): string => String(r[k]);
const ogBig = (r: OgRow, k: string): bigint => bigOf(r[k]);
const ogFlag = (r: OgRow, k: string): boolean => r[k] === true;
const ogRows = (r: OgRow, k: string): readonly OgRow[] => r[k] as readonly OgRow[];
/** og Int512 offdelta: a {high, low} pair of 256-bit words, or a plain number. */
const ogInt512 = (v: unknown): bigint =>
  typeof v === "object" && v !== null ? ogBig(v as OgRow, "high") * (1n << 256n) + ogBig(v as OgRow, "low") : bigOf(v);
const ogAllowance = (a: OgRow): ProofBody["transformers"][number]["allowances"][number] => ({
  deltaIndex: ogBig(a, "deltaIndex"),
  rightAllowance: ogBig(a, "rightAllowance"),
  leftAllowance: ogBig(a, "leftAllowance"),
});
const ogTransformer = (c: OgRow): ProofBody["transformers"][number] => ({
  transformerAddress: ogStr(c, "transformerAddress"),
  encodedBatch: ogStr(c, "encodedBatch"),
  allowances: ogRows(c, "allowances").map(ogAllowance),
});
/**
 * og ProofBodyStruct (Int512 offdeltas as {high, low}, numeric response seconds) or a rewrite ProofBody, as the ABI
 * reads it.
 */
const proofBodyOfOg = (p: OgRow): ProofBody => ({
  watchSeed: ogStr(p, "watchSeed"),
  leftResponseSeconds: ogBig(p, "leftResponseSeconds"),
  rightResponseSeconds: ogBig(p, "rightResponseSeconds"),
  offdeltas: (p["offdeltas"] as readonly unknown[]).map(ogInt512),
  tokenIds: (p["tokenIds"] as readonly unknown[]).map(bigOf),
  transformers: ogRows(p, "transformers").map(ogTransformer),
});
type BatchEntry<K extends keyof Batch> = Batch[K][number];
const ogTransfer = (r: OgRow): ReserveTransfer => ({
  receivingEntity: ogStr(r, "receivingEntity"),
  tokenId: ogBig(r, "tokenId"),
  amount: ogBig(r, "amount"),
});
const ogR2C = (r: OgRow): BatchEntry<"reserveToCollateral"> => ({
  tokenId: ogBig(r, "tokenId"),
  receivingEntity: ogStr(r, "receivingEntity"),
  pairs: ogRows(r, "pairs").map((p) => ({ entity: ogStr(p, "entity"), amount: ogBig(p, "amount") })),
});
const ogC2R = (r: OgRow): BatchEntry<"collateralToReserve"> => ({
  counterparty: ogStr(r, "counterparty"),
  tokenId: ogBig(r, "tokenId"),
  amount: ogBig(r, "amount"),
  nonce: ogBig(r, "nonce"),
  sig: ogStr(r, "sig"),
});
const ogDiff = (d: OgRow): BatchEntry<"settlements">["diffs"][number] => ({
  tokenId: ogBig(d, "tokenId"),
  leftDiff: ogBig(d, "leftDiff"),
  rightDiff: ogBig(d, "rightDiff"),
  collateralDiff: ogBig(d, "collateralDiff"),
  ondeltaDiff: ogBig(d, "ondeltaDiff"),
});
const ogSettlement = (r: OgRow): BatchEntry<"settlements"> => ({
  leftEntity: ogStr(r, "leftEntity"),
  rightEntity: ogStr(r, "rightEntity"),
  diffs: ogRows(r, "diffs").map(ogDiff),
  forgiveDebtsInTokenIds: (r["forgiveDebtsInTokenIds"] as readonly unknown[]).map(bigOf),
  sig: ogStr(r, "sig"),
  nonce: ogBig(r, "nonce"),
});
const ogDisputeStart = (r: OgRow): BatchEntry<"disputeStarts"> => ({
  counterentity: ogStr(r, "counterentity"),
  nonce: ogBig(r, "nonce"),
  proposerIsLeft: ogFlag(r, "proposerIsLeft"),
  proofbodyHash: ogStr(r, "proofbodyHash"),
  initialProofbody: proofBodyOfOg(r["initialProofbody"] as OgRow),
  watchSeed: ogStr(r, "watchSeed"),
  sig: ogStr(r, "sig"),
  starterInitialArguments: ogStr(r, "starterInitialArguments"),
  starterCounterArguments: ogStr(r, "starterCounterArguments"),
  starterCounterProofCommitment: ogStr(r, "starterCounterProofCommitment"),
});
const ogCounterDispute = (r: OgRow): BatchEntry<"counterDisputes"> => ({
  counterentity: ogStr(r, "counterentity"),
  initialNonce: ogBig(r, "initialNonce"),
  initialProofbodyHash: ogStr(r, "initialProofbodyHash"),
  counterNonce: ogBig(r, "counterNonce"),
  proposerIsLeft: ogFlag(r, "proposerIsLeft"),
  counterProofbody: proofBodyOfOg(r["counterProofbody"] as OgRow),
  sig: ogStr(r, "sig"),
});
const ogFinalization = (r: OgRow): BatchEntry<"disputeFinalizations"> => ({
  counterentity: ogStr(r, "counterentity"),
  initialNonce: ogBig(r, "initialNonce"),
  finalNonce: ogBig(r, "finalNonce"),
  proposerIsLeft: ogFlag(r, "proposerIsLeft"),
  initialProofbodyHash: ogStr(r, "initialProofbodyHash"),
  finalProofbody: proofBodyOfOg(r["finalProofbody"] as OgRow),
  starterArguments: ogStr(r, "starterArguments"),
  otherArguments: ogStr(r, "otherArguments"),
  sig: ogStr(r, "sig"),
  startedByLeft: ogFlag(r, "startedByLeft"),
  cooperative: ogFlag(r, "cooperative"),
});
const ogE2R = (r: OgRow): BatchEntry<"externalTokenToReserve"> => ({
  entity: ogStr(r, "entity"),
  contractAddress: ogStr(r, "contractAddress"),
  externalTokenId: ogBig(r, "externalTokenId"),
  tokenType: ogBig(r, "tokenType"),
  internalTokenId: ogBig(r, "internalTokenId"),
  amount: ogBig(r, "amount"),
});
const ogReveal = (r: OgRow): BatchEntry<"revealSecrets"> => ({
  transformer: ogStr(r, "transformer"),
  secret: ogStr(r, "secret"),
});
const ogLadder = (r: OgRow): BatchEntry<"hashLadderRegistrations"> => {
  const w = r["witness"] as OgRow;
  const [a, b, c, d] = (w["reveals"] as readonly unknown[]).map(String);
  return {
    counterpartyEntity: ogStr(r, "counterpartyEntity"),
    targetRole: ogFlag(r, "targetRole"),
    fullHash: ogStr(r, "fullHash"),
    partialRoot: ogStr(r, "partialRoot"),
    witness: {
      fillRatio: ogBig(w, "fillRatio"),
      fullSecret: ogStr(w, "fullSecret"),
      reveals: [a ?? "", b ?? "", c ?? "", d ?? ""] as const,
    },
  };
};
/**
 * The ABI view of a committed og JBatch (og encodeJBatch's input): numbers widen to uint256, og Int512 offdeltas join.
 * Idempotent on a rewrite Batch.
 */
export const batchOfOg = (b: OgBatchRows | Batch): Batch => {
  const rows = (f: keyof Batch): readonly OgRow[] => ((b as unknown as OgBatchRows)[f] ?? []) as readonly OgRow[];
  return {
    reserveToReserve: rows("reserveToReserve").map(ogTransfer),
    reserveToCollateral: rows("reserveToCollateral").map(ogR2C),
    collateralToReserve: rows("collateralToReserve").map(ogC2R),
    settlements: rows("settlements").map(ogSettlement),
    disputeStarts: rows("disputeStarts").map(ogDisputeStart),
    counterDisputes: rows("counterDisputes").map(ogCounterDispute),
    disputeFinalizations: rows("disputeFinalizations").map(ogFinalization),
    externalTokenToReserve: rows("externalTokenToReserve").map(ogE2R),
    reserveToExternalToken: rows("reserveToExternalToken").map(ogTransfer),
    revealSecrets: rows("revealSecrets").map(ogReveal),
    hashLadderRegistrations: rows("hashLadderRegistrations").map(ogLadder),
  };
};
/** og J_BATCH_CONTRACT_LIMITS.maxEncodedBatchBytes (encodeJBatch). */
const MAX_ENCODED_BATCH_BYTES = 256 * 1024;
/** og encodeJBatch: the ABI bytes of the batch, refused past the contract's encoded-size limit. */
export const encodeJBatch = (b: OgBatchRows | Batch): Result<string, JBatchError> => {
  const encoded = encodeBatch(batchOfOg(b));
  const size = (encoded.length - 2) / 2;
  return size > MAX_ENCODED_BATCH_BYTES
    ? batchErr(`J_BATCH_ENCODED_BYTES_EXCEEDED:${size}/${MAX_ENCODED_BATCH_BYTES}`)
    : ok(encoded);
};
/**
 * The committed jBatchState as the shared J-batch functions read it (every field they touch is shape-agnostic over og's
 * numeric rows).
 */
const entityJBatch = (state: EntityState): JBatchState | undefined =>
  state.committed["jBatchState"] as unknown as JBatchState | undefined;
/** The draft the Entity's J-batch txs extend: the committed one, or og's fresh batch. */
const draftJBatch = (state: EntityState): JBatchState => entityJBatch(state) ?? initJBatch();
const withEntityJBatch = (state: EntityState, jb: object | undefined): EntityState =>
  (jb === undefined ? state : withCommittedJBatch(state, jb));
/** og EntityState outDebtsByToken / inDebtsByToken as the reserve simulation reads them. */
const committedDebts = (state: EntityState): DebtLedger => {
  const book = (v: Binary | undefined): DebtBook => (v instanceof Map ? (v as unknown as DebtBook) : new Map());
  return { out: book(state.committed["outDebtsByToken"]), in: book(state.committed["inDebtsByToken"]) };
};
/** og getReserveCandidateIssue over the Entity's committed reserves, outgoing debts and draft batch. */
const entityReserveIssue = (state: EntityState, replicas: Replicas, c: ReserveCandidate): ReserveIssue | undefined => {
  const jb = entityJBatch(state);
  const view = {
    entityId: state.id,
    reserves: committedReserves(state),
    debts: committedDebts(state),
    accounts: new Set(replicas.keys()),
    jBatch: jb === undefined ? undefined : { ...jb, batch: batchOfOg(jb.batch) },
  };
  return reserveCandidateIssue(view, c);
};
/**
 * og batchAddReserveToReserve / batchAddReserveToExternal / batchAddExternalTokenToReserve: one appended og row within
 * the 50-op contract limit.
 */
const appendOgRow = (jb: JBatchState, field: AppendedField, row: OgRow): Result<JBatchState, EntityError> => {
  const next = batchOpCount(jb.batch) + 1;
  if (next > J_BATCH_LIMITS.maxTotalOps)
    return invariant(`J_BATCH_LIMIT_EXCEEDED: ${field} would exceed total ops ${next}/${J_BATCH_LIMITS.maxTotalOps}`);
  return ok({
    ...jb,
    batch: { ...jb.batch, [field]: [...jb.batch[field], row] },
    status: jb.status === "empty" ? "accumulating" : jb.status,
  });
};
const jSay = (d: Draft, ...messages: readonly string[]): Draft => ({
  ...d,
  events: [...(d.events ?? []), ...messages.map(status)],
});
const jQueued = (d: Draft, jb: JBatchState, message: string): Draft =>
  jSay({ ...d, state: withEntityJBatch(d.state, jb) }, message);
/**
 * og getJurisdictionConfigName: the rewrite's Entity names its J replica in its own config (og re-reads the stack from
 * that replica).
 */
const jurisdictionNameOf = (state: EntityState): string => (state.jurisdictionConfig?.name ?? "").trim();
type EntityJTx<T extends string> = Extract<EntityTx, { readonly type: T }>["data"];
/** og handleR2R / handleR2E: debt-aware reserve admission (a plain Error), then one appended row. */
const queueReserveOut = (
  d: Draft,
  type: "reserveToReserve" | "reserveToExternalToken",
  receivingEntity: string,
  tokenId: number,
  amount: bigint,
  message: string,
): Result<Draft, EntityError> => {
  const issue = entityReserveIssue(d.state, d.accountReplicas, { type, receivingEntity, tokenId, amount });
  if (issue !== undefined)
    return invariant(
      `❌ Insufficient spendable reserve: have ${issue.availableAfterDebt}, need ${amount} token ${tokenId}`,
    );
  return map(appendOgRow(draftJBatch(d.state), type, { receivingEntity, tokenId, amount }), (jb) =>
    jQueued(d, jb, message),
  );
};
const entityR2R = (d: Draft, x: EntityJTx<"r2r">): Result<Draft, EntityError> => {
  const queued = `📦 Queued R→R: ${x.amount} token ${x.tokenId} to ${x.toEntityId.slice(-4)} (use jBroadcast to commit)`;
  return queueReserveOut(d, "reserveToReserve", x.toEntityId, x.tokenId, x.amount, queued);
};
const entityR2E = (d: Draft, x: EntityJTx<"r2e">): Result<Draft, EntityError> => {
  const to = x.receivingEntity.slice(-8);
  const queued = `📦 Queued R→E: ${x.amount} token ${x.tokenId} to ${to} (use jBroadcast to commit)`;
  return queueReserveOut(d, "reserveToExternalToken", x.receivingEntity, x.tokenId, x.amount, queued);
};
/**
 * og handleE2R: a checksum-valid non-zero token contract (ethers.isAddress) and a positive amount, credited to this
 * Entity when the batch executes.
 */
const entityE2R = (d: Draft, x: EntityJTx<"e2r">): Result<Draft, EntityError> => {
  if (usableAddress(x.contractAddress) === null)
    return invariant(`❌ Invalid external token contract: ${x.contractAddress}`);
  if (x.amount <= 0n) return invariant("❌ External → Reserve amount must be positive");
  const row: OgRow = {
    entity: d.state.id,
    contractAddress: x.contractAddress,
    externalTokenId: typeof x.externalTokenId === "bigint" ? x.externalTokenId : 0n,
    tokenType: typeof x.tokenType === "number" ? x.tokenType : 0,
    internalTokenId: typeof x.internalTokenId === "number" ? x.internalTokenId : 0,
    amount: x.amount,
  };
  const queued = `📦 Queued E→R: ${x.amount} via ${x.contractAddress.slice(0, 10)}... (use j_broadcast to commit)`;
  return map(appendOgRow(draftJBatch(d.state), "externalTokenToReserve", row), (jb) => jQueued(d, jb, queued));
};
/**
 * og handleR2C: admission, debt-aware reserve and local-account refusals are status messages; a deposit carrying
 * `rebalanceQuoteId` needs the Account's accepted `shadow.rebalance.activeQuote`, which og consensus never writes
 * (r2c.ts collectRebalanceFee), so it is refused the same way.
 */
const entityR2C = (d: Draft, x: EntityJTx<"r2c">): Result<Draft, EntityError> => {
  const self = d.state.id.trim().toLowerCase();
  const receiving = String(x.receivingEntityId || d.state.id || "")
    .trim()
    .toLowerCase();
  const local = receiving === self;
  const cp = x.counterpartyId;
  const refuse = (m: string): Result<Draft, EntityError> => ok(jSay(d, m));
  if (x.amount <= 0n || !Number.isSafeInteger(x.tokenId) || x.tokenId <= 0)
    return refuse("❌ Collateral deposit requires a positive amount and registered tokenId");
  if (!receiving || receiving === cp.toLowerCase())
    return refuse("❌ Collateral deposit requires two distinct non-empty entities");
  const candidate: ReserveCandidate = {
    type: "reserveToCollateral",
    receivingEntity: receiving,
    counterparty: cp,
    tokenId: x.tokenId,
    amount: x.amount,
  };
  const issue = entityReserveIssue(d.state, d.accountReplicas, candidate);
  if (issue !== undefined) {
    const shortfall = `have ${issue.availableAfterDebt}, need ${x.amount} token ${x.tokenId}`;
    return refuse(`❌ Insufficient spendable reserve for collateral deposit: ${shortfall}`);
  }
  if (local && !d.accountReplicas.has(cp as EntityId))
    return refuse(`❌ Cannot deposit collateral: no account with ${cp.slice(-4)}`);
  if (x.rebalanceQuoteId !== undefined)
    return refuse(
      local
        ? `❌ Rebalance fee: no active quote for ${cp.slice(-4)}`
        : "❌ Rebalance fee unsupported for remote reserve → account deposits",
    );
  const drafted = addCommittedR2C(draftJBatch(d.state) as unknown as OgJBatchState, receiving, cp, x.tokenId, x.amount);
  const pair = `${receiving.slice(-4)}↔${cp.slice(-4)}`;
  const queued = `📦 Queued R→C: ${x.amount} token ${x.tokenId} to ${pair} (use j_broadcast to commit)`;
  return map(drafted, (jb) => jQueued(d, jb as unknown as JBatchState, queued));
};
/** One JTx to the Entity's J replica, with the Hanko the quorum signs for it. */
const jOutput = (d: Draft, name: string, jTx: unknown, hash?: JBatchHashToSign): Draft => ({
  ...d,
  jOutputs: [...(d.jOutputs ?? []), { jurisdictionName: name, jTxs: [jTx as Binary] }],
  ...(hash === undefined ? {} : { hashes: [...(d.hashes ?? []), hash] }),
});
/** The Entity's submission context for the shared J-batch sealing: its id, chain, depository, leader and clock. */
const jSubmission = (state: EntityState, signerId: string, timestamp: bigint): BroadcastContext => ({
  entityId: state.id,
  chainId: state.jurisdiction.chainId,
  depository: state.jurisdiction.depositoryAddress,
  signerId,
  timestamp: Number(timestamp),
});
/**
 * og flushDeferredHashLadderReveals: every stashed hash-ladder witness moves into the draft (a no-op while a batch is
 * in flight).
 */
const flushBeforeBroadcast = (d: Draft, timestamp: bigint): Result<Draft, EntityError> =>
  map(flushDeferredReveals(cjOf(d, timestamp).host), ({ host, flushed }) => {
    if (flushed === 0) return d;
    return cjInto(
      d,
      { host, messages: [`🌉 Flushed ${flushed} deferred hash-ladder reveal(s) into jBatch`], outputs: [] },
      timestamp,
    );
  });
/** The seal step of og handleJBroadcast, on the flushed draft. */
const sealJBroadcast = (d: Draft, x: EntityJTx<"j_broadcast">, timestamp: bigint): Result<Draft, EntityError> => {
  const jb = entityJBatch(d.state) as JBatchState;
  const refuse = (m: string): Result<Draft, EntityError> => ok(jSay(d, m));
  if (jb.sentBatch !== undefined)
    return invariant(
      `❌ Cannot broadcast: sentBatch pending nonce=${jb.sentBatch.entityNonce} attempts=${jb.sentBatch.submitAttempts}`,
    );
  if (!hasJBatchWork(jb)) return refuse("ℹ️ j_broadcast skipped: jBatch is empty");
  const name = jurisdictionNameOf(d.state);
  if (name === "") return refuse("❌ No jurisdiction configured for this entity");
  if (d.state.jurisdiction.chainId === 0) return refuse("❌ Missing chainId");
  const signer = leaderStateOf(d.state).activeValidatorId;
  if (signer === "") return refuse("❌ No signerId available");
  if (usableAddress(d.state.jurisdiction.depositoryAddress) === null) return invariant("INVALID_DEPOSITORY_ADDRESS");
  if (usableAddress(d.state.jurisdictionConfig?.entityProviderAddress) === null)
    return invariant("INVALID_ENTITY_PROVIDER_ADDRESS");
  const sealed = jBroadcast(jb, { ...jSubmission(d.state, signer, timestamp), feeOverrides: x.feeOverrides });
  if (!sealed.ok) return invariant(sealed.error.reason);
  const { jBatch, jTx, hashToSign } = sealed.value;
  if (jTx === undefined) return invariant("J_BROADCAST_SEALED_WITHOUT_JTX");
  const head = jb.recoveryBatches?.[0];
  const next = head !== undefined && batchOpCount(head) > 0 ? head : jb.batch;
  const priority = takeBroadcastBatch(next).disputePriority
    ? ["⚖️ Dispute operations broadcast before ordinary queued operations"]
    : [];
  const sent = `📤 Batch (${jTx.data.batchSize} ops) → hashesToSign [nonce=${jTx.data.entityNonce}]`;
  return ok(
    jOutput(jSay({ ...d, state: withEntityJBatch(d.state, jBatch) }, sent, ...priority), name, jTx, hashToSign),
  );
};
/**
 * og handleJBroadcast: a missing jBatchState or an in-flight sentBatch is a plain Error; an empty draft, a missing
 * jurisdiction, chain id or leader is a status message; unusable contract addresses, a contract-limit or encoded-size
 * overflow are plain Errors. The sealed batch goes out as og's `batch` JTx with the quorum's jBatch hash to sign.
 * Before the sentBatch check, og flushDeferredHashLadderReveals moves every stashed hash-ladder witness into the draft.
 */
const entityJBroadcast = (d: Draft, x: EntityJTx<"j_broadcast">, timestamp: bigint): Result<Draft, EntityError> => {
  if (entityJBatch(d.state) === undefined) return invariant("❌ No jBatchState found for j_broadcast");
  return chain(flushBeforeBroadcast(d, timestamp), (flushed) => sealJBroadcast(flushed, x, timestamp));
};
/**
 * og handleJRebroadcast: resend the stored sentBatch at its own nonce (the jurisdiction name is checked before the
 * chain); refusals as in the shared jRebroadcast.
 */
const entityJRebroadcast = (d: Draft, x: EntityJTx<"j_rebroadcast">, timestamp: bigint): Result<Draft, EntityError> => {
  const jb = entityJBatch(d.state);
  const sent = jb?.sentBatch;
  const name = jurisdictionNameOf(d.state);
  const signer = leaderStateOf(d.state).activeValidatorId;
  if (sent !== undefined && sent.terminalFailure === undefined && signer !== "" && name === "")
    return ok(jSay(d, "❌ No jurisdiction configured for j_rebroadcast"));
  const r = jRebroadcast(jb, { ...jSubmission(d.state, signer, timestamp), gasBumpBps: x.gasBumpBps });
  if (!r.ok) return invariant(r.error.reason);
  const { jBatch, note, jTx, hashToSign } = r.value;
  const next = jSay(
    jb === undefined ? d : { ...d, state: withEntityJBatch(d.state, jBatch) },
    ...(note === undefined ? [] : [note]),
  );
  return ok(jTx === undefined ? next : jOutput(next, name, jTx, hashToSign));
};
/**
 * og releaseR2CSubmittedLatches / clear's submitted-marker reset and releaseFinalizeLatches on the Entity's Accounts.
 */
const releaseEntityLatches = (d: Draft, release: JLatchRelease): Draft => {
  const unmarked = release.submitted.reduce((replicas, { accountId, tokenId }) => {
    const c = replicas.get(accountId as EntityId);
    if (c === undefined) return replicas;
    return mapSet(
      replicas,
      accountId as EntityId,
      { ...c, state: setRebalanceSubmittedAt(c.state, tokenId, undefined) } as AccountReplica,
    );
  }, d.accountReplicas);
  const unlatched = release.finalizers.reduce<Folded>(
    (f, peer) => {
      const c = f.accountReplicas.get(peer as EntityId);
      return c !== undefined && c._tag === "disputed" && c.active !== undefined
        ? latchFinalize(f, peer, c, c.active, false)
        : f;
    },
    { state: d.state, accountReplicas: unmarked },
  );
  return { ...d, ...unlatched };
};
/** An Account's jNonce by its peer id as given, else lowercased; 0 without the Account. */
const accountJNonce = (d: Draft, peer: string): number =>
  (d.accountReplicas.get(peer as EntityId) ?? d.accountReplicas.get(lower(peer) as EntityId))?.state.jNonce ?? 0;
/**
 * og handleJAbortSentBatch: requeue (default) drops finalizations and stale C2Rs by the Account's jNonce and parks the
 * rest first; drop releases the R2C markers.
 */
const entityJAbort = (d: Draft, x: EntityJTx<"j_abort_sent_batch">): Draft => {
  const jb = entityJBatch(d.state), r = jAbortSentBatch(jb, x, (peer) => accountJNonce(d, peer));
  const aborted = jb?.sentBatch === undefined ? d : { ...d, state: withEntityJBatch(d.state, r.jBatch) };
  return jSay(releaseEntityLatches(aborted, r.release), r.note);
};
/**
 * og handleJClearBatch: drop every batch, reset every Account's submitted markers, release the dropped finalize
 * latches.
 */
const entityJClear = (d: Draft, x: EntityJTx<"j_clear_batch">): Draft => {
  const jb = entityJBatch(d.state);
  const submitted = new Map(
    [...d.accountReplicas].map(
      ([peer, c]) => [peer as string, [...(c.state.submittedAt ?? new Map<number, number>()).keys()]] as const,
    ),
  );
  const r = jClearBatch(jb, x, jb === undefined ? new Map() : submitted);
  const cleared = jb === undefined ? d : { ...d, state: withEntityJBatch(d.state, r.jBatch) };
  return jSay(releaseEntityLatches(cleared, r.release), r.note);
};
/** og handleMintReserves: the direct admin `mint` JTx to the Entity's J replica (outside the batch). */
const entityMint = (d: Draft, x: EntityJTx<"mintReserves">, timestamp: bigint): Draft => {
  const name = jurisdictionNameOf(d.state);
  if (name === "") return jSay(d, "❌ Jurisdiction unavailable for mint: entity jurisdiction is not configured");
  const m = mintReservesTx(d.state.id, x.tokenId, x.amount, Number(timestamp));
  return jOutput(jSay(d, m.note), name, m.jTx);
};
// ---- og entity/tx/j-events.ts J7 Entity-side dispute effects and tx/dispute-finalize-guards.ts: J batch retirement, nonce sync, the dispute-deadline hook ----
type BatchRows = { readonly [field: string]: readonly Binary[] };
/** The row fields the dispute retirement scrubs read. */
type ScrubbedRow = {
  readonly counterentity?: unknown;
  readonly initialProofbodyHash?: unknown;
  readonly proofbodyHash?: unknown;
  readonly targetRole?: unknown;
  readonly counterpartyEntity?: unknown;
};
const rowId = (v: unknown): string => String(v || "").trim().toLowerCase();
const rowHash = (v: unknown): string => String(v || "").toLowerCase();
/** A batch with some rows scrubbed away, and how many went. */
type Scrubbed = { readonly batch: BatchRows; readonly removed: number };
type Scrub = (b: BatchRows) => Scrubbed;
const scrubRows =
  (field: string, keep: (row: ScrubbedRow) => boolean): Scrub =>
  (b) => {
    const before = b[field] ?? [];
    const after = before.filter((r) => keep(r as ScrubbedRow));
    return {
      batch: after.length === before.length ? b : { ...b, [field]: after },
      removed: before.length - after.length,
    };
  };
/** og scrubDisputeStartsForCounterparty. */
const scrubStarts = (peer: string): Scrub =>
  scrubRows("disputeStarts", (r) => peer === "" || rowId(r.counterentity) !== peer);
/** og scrubCounterDisputesForActiveStart: a counter-proof against another start of the Account's dispute. */
const scrubCountersForStart = (peer: string, initialHash: string): Scrub =>
  scrubRows(
    "counterDisputes",
    (r) => rowId(r.counterentity) !== peer || String(r.initialProofbodyHash).toLowerCase() === initialHash,
  );
/** og scrubDisputeFinalizationsForCounterparty. */
const scrubFinalizations = (peer: string): Scrub =>
  scrubRows("disputeFinalizations", (r) => peer === "" || rowId(r.counterentity) !== peer);
/** og scrubCounterDisputesForCounterparty. */
const scrubCounters = (peer: string): Scrub => scrubRows("counterDisputes", (r) => rowId(r.counterentity) !== peer);
/** og scrubSourceHashLadderRegistrationsForCounterparty. */
const scrubSourceLadders = (peer: string): Scrub =>
  scrubRows(
    "hashLadderRegistrations",
    (r) => peer === "" || r.targetRole !== false || rowId(r.counterpartyEntity) !== peer,
  );
const scrubAll = (b: BatchRows, scrubs: readonly Scrub[]): Scrubbed =>
  scrubs.reduce<Scrubbed>((acc, scrub) => {
    const r = scrub(acc.batch);
    return { batch: r.batch, removed: acc.removed + r.removed };
  }, { batch: b, removed: 0 });
const rowsEmpty = (b: BatchRows): boolean => BATCH_FIELDS.every((f) => (b[f] ?? []).length === 0);
type RetiringJBatch = {
  readonly batch: BatchRows;
  readonly sentBatch?: { readonly batch: BatchRows; readonly entityNonce: number } | undefined;
  readonly recoveryBatches?: readonly BatchRows[] | undefined;
  readonly entityNonce?: number | undefined;
  readonly status?: string | undefined;
};
/** Work left for a broadcast: a non-empty recovery batch or draft. */
const retiringWork = (batch: BatchRows, recovery: readonly BatchRows[] | undefined): boolean =>
  (recovery ?? []).some((b) => !rowsEmpty(b)) || !rowsEmpty(batch);
/**
 * og retireSentBatchInvalidatedBy*: a sent batch that lost operations goes to the front of the recovery queue (unless
 * emptied).
 */
const requeueSent = (scrubbed: RetiringJBatch, sentLeft: BatchRows): RetiringJBatch => {
  const { sentBatch: _s, ...unsent } = scrubbed;
  const queue = rowsEmpty(sentLeft) ? scrubbed.recoveryBatches : [sentLeft, ...(scrubbed.recoveryBatches ?? [])];
  const status = retiringWork(unsent.batch, queue) ? "accumulating" : "empty";
  return { ...unsent, ...(queue === undefined ? {} : { recoveryBatches: queue }), status };
};
type Retirement = { readonly jb: RetiringJBatch; readonly removed: number; readonly broadcast: boolean };
/**
 * og's shared retirement shape (retireStartedDisputeBatchOps, retireFinalizedDisputeState): scrub the draft and every
 * recovery batch (dropping the emptied ones), then, unless the sent batch will still be acknowledged, move a sent
 * batch that lost operations to the front of the recovery queue (og retireSentBatchInvalidatedBy*). `broadcast`: og
 * queueLocalJBatchBroadcast after a sent or recovery removal.
 */
const retireJBatch = (jb: RetiringJBatch, scrubs: readonly Scrub[], keepSent: boolean): Retirement => {
  const draft = scrubAll(jb.batch, scrubs), recovered = (jb.recoveryBatches ?? []).map((b) => scrubAll(b, scrubs));
  const removedRecovery = recovered.reduce((n, r) => n + r.removed, 0);
  const left = recovered.map((r) => r.batch).filter((b) => !rowsEmpty(b));
  const { recoveryBatches: _r, ...rest } = jb;
  const keptRecovery = jb.recoveryBatches === undefined || left.length === 0 ? {} : { recoveryBatches: left };
  const scrubbed: RetiringJBatch = { ...rest, batch: draft.batch, ...keptRecovery };
  const sent = keepSent || jb.sentBatch === undefined ? undefined : scrubAll(jb.sentBatch.batch, scrubs);
  const removedSent = sent?.removed ?? 0;
  const retired = sent === undefined || removedSent === 0 ? scrubbed : requeueSent(scrubbed, sent.batch);
  const broadcast = (removedSent > 0 || removedRecovery > 0) && retired.sentBatch === undefined
    && retiringWork(retired.batch, retired.recoveryBatches);
  return { jb: retired, removed: draft.removed + removedRecovery + removedSent, broadcast };
};
type NonceSync = { readonly jb: RetiringJBatch | undefined; readonly message?: string | undefined };
/** og syncJBatchEntityNonceFromEvent: our own event's batch nonce raises the J batch nonce. */
const syncBatchNonce = (
  jb: RetiringJBatch | undefined,
  sender: string,
  self: string,
  batchNonce: number | undefined,
): NonceSync => {
  if (rowId(sender) !== rowId(self) || batchNonce === undefined || batchNonce <= 0 || jb === undefined) return { jb };
  const current = jb.entityNonce || 0;
  if (batchNonce <= current) return { jb };
  return {
    jb: { ...jb, entityNonce: batchNonce },
    message: `↻ Synced J batch nonce from event (${current} → ${batchNonce})`,
  };
};
export type DisputeJEffects = {
  readonly state: EntityState;
  readonly events: readonly FrameEvent[];
  readonly broadcast: boolean;
};
const disputePeerOf = (self: string, sender: string, counterentity: string): string =>
  (rowId(sender) === rowId(self) ? rowId(counterentity) : rowId(sender));
/** The messages that apply, in order: a `false` or absent one is left out. */
const messagesOf = (...xs: readonly (string | false | undefined)[]): readonly string[] =>
  xs.filter((x): x is string => typeof x === "string");
/** A DisputeStarted as the Entity reads it. */
type StartedDisputeEvent = {
  readonly sender: string;
  readonly counterentity: string;
  readonly proofbodyHash: string;
  readonly disputeTimeout: number;
  readonly starterInitialArguments?: string | undefined;
  readonly batchNonce?: number | undefined;
};
/**
 * og initializeStartedDispute + applyStartedDisputeFollowups, the Entity-side part (the Account's external finality is
 * the Account machine's): nonce sync, retirement of every stale start and non-binding counter-proof (keeping our own
 * sent start until its HankoBatchProcessed), the `⚔️ DISPUTE` message and the dispute-deadline hook (1 ms for the
 * starter, 5 s for the other side). The Entity's full og handler, with the starter's secrets (applyKnownHtlcSecret),
 * the Pull counter-proof lock, the cross-j recovery plan and Source hub claims, is disputeStartedJEvent.
 */
export const disputeStartedEffects = (
  state: EntityState,
  e: StartedDisputeEvent,
  timestamp: number,
): Result<DisputeJEffects, EntityError> => {
  const self = rowId(state.id);
  const peer = disputePeerOf(self, e.sender, e.counterentity);
  const weAreStarter = rowId(e.sender) === self;
  const retired = startedRetirement(committedJBatch(state) as RetiringJBatch | undefined, self, e);
  const triggerAt = Math.max(0, Number.isFinite(timestamp) ? timestamp : 0) + (weAreStarter ? 1 : 5000);
  const deadline: ScheduledHook = {
    id: `dispute-deadline:${peer}`,
    triggerAt,
    type: "dispute_deadline",
    data: { accountId: peer },
  };
  const messages = messagesOf(
    retired.synced,
    retired.removed > 0 && `🧹 Removed ${retired.removed} stale dispute-start op(s) for ${peer.slice(-4)}`,
    `⚔️ DISPUTE ${weAreStarter ? "STARTED" : "vs us"} with ${peer.slice(-4)}, timeout: unix ${e.disputeTimeout}`,
  );
  return map(crontabOf(state), (crontab) => ({
    state: withCrontab(withEntityJBatch(state, retired.jb), scheduleHook(crontab, deadline)),
    events: messages.map(status),
    broadcast: retired.broadcast,
  }));
};
/** A DisputeFinalized as the Entity reads it, with the Account finality's result. */
type FinalizedDisputeEvent = {
  readonly sender: string;
  readonly counterentity: string;
  readonly initialProofbodyHash: string;
  readonly batchNonce?: number | undefined;
  readonly initialNonce: number;
  readonly hadActiveDispute: boolean;
  readonly settlementInvalidated: boolean;
};
/**
 * og applyResolvedDisputeFinality + retireFinalizedDisputeState, the Entity-side part: nonce sync, the `✅ DISPUTE
 * FINALIZED` message and the dispute-deadline hook's cancellation when the Account had an active dispute
 * (`hadActiveDispute`, `initialNonce`: the Account finality's result), og's stale settlement-intent message
 * (`settlementInvalidated`), then retirement of every finalization, counter-proof and Source hash-ladder registration
 * for the Account (keeping a sent batch that will acknowledge exactly this finalization).
 */
export const disputeFinalizedEffects = (
  state: EntityState,
  e: FinalizedDisputeEvent,
): Result<DisputeJEffects, EntityError> => {
  const self = rowId(state.id);
  const peer = disputePeerOf(self, e.sender, e.counterentity);
  const retired = finalizedRetirement(committedJBatch(state) as RetiringJBatch | undefined, self, e);
  const cp = peer.slice(-4);
  const messages = messagesOf(
    retired.synced,
    e.settlementInvalidated && `🧹 Invalidated stale settlement intent after dispute finality with ${cp}`,
    e.hadActiveDispute && `✅ DISPUTE FINALIZED with ${cp} (nonce ${e.initialNonce})`,
    retired.removed > 0 && `🧹 Removed ${retired.removed} stale dispute-finalize op(s) for ${cp}`,
  );
  return map(crontabOf(state), (crontab) => {
    const next = withEntityJBatch(state, retired.jb);
    return {
      state: e.hadActiveDispute ? withCrontab(next, cancelHook(crontab, `dispute-deadline:${peer}`)) : next,
      events: messages.map(status),
      broadcast: retired.broadcast,
    };
  });
};
/**
 * og J7's J-batch part, shared by the Entity (its committed jBatchState) and the Host (its JBatchState): the nonce
 * sync, then the retirement.
 */
type RetiredJBatch = {
  readonly jb: RetiringJBatch | undefined;
  readonly synced?: string | undefined;
  readonly removed: number;
  readonly broadcast: boolean;
};
const retiredWith = (synced: NonceSync, scrubs: readonly Scrub[], keepSent: boolean): RetiredJBatch => {
  if (synced.jb === undefined) return { jb: undefined, ...opt("synced", synced.message), removed: 0, broadcast: false };
  const r = retireJBatch(synced.jb, scrubs, keepSent);
  return { jb: r.jb, ...opt("synced", synced.message), removed: r.removed, broadcast: r.broadcast };
};
/** Whether our sent batch still carries the start of this very dispute (og keeps it until its HankoBatchProcessed). */
const sentStartOf = (jb: RetiringJBatch | undefined, peer: string, initialHash: string): boolean =>
  (jb?.sentBatch?.batch["disputeStarts"] ?? []).some((s) => {
    const row = s as ScrubbedRow;
    return rowId(row.counterentity) === peer && rowHash(row.proofbodyHash) === initialHash;
  });
/**
 * og initializeStartedDispute: every stale start and non-binding counter-proof for the Account goes, keeping our own
 * sent start until its HankoBatchProcessed.
 */
const startedRetirement = (
  jb: RetiringJBatch | undefined,
  self: string,
  e: Pick<StartedDisputeEvent, "sender" | "counterentity" | "proofbodyHash" | "batchNonce">,
): RetiredJBatch => {
  const sender = rowId(e.sender);
  const peer = disputePeerOf(self, e.sender, e.counterentity);
  const initialHash = String(e.proofbodyHash).toLowerCase();
  const synced = syncBatchNonce(jb, sender, self, e.batchNonce);
  const ownSent = sender === self && sentStartOf(synced.jb, peer, initialHash);
  return retiredWith(synced, [scrubStarts(peer), scrubCountersForStart(peer, initialHash)], ownSent);
};
/** og sentBatchOwnsDisputeFinalityAck: our sent batch, at the event's nonce, finalizes exactly this dispute. */
const sentAcksFinality = (
  jb: RetiringJBatch | undefined,
  batchNonce: number | undefined,
  peer: string,
  initialHash: string,
): boolean => {
  const sent = jb?.sentBatch;
  if (sent === undefined || batchNonce === undefined || !Number.isSafeInteger(batchNonce) || batchNonce <= 0)
    return false;
  return (
    sent.entityNonce === batchNonce &&
    (sent.batch["disputeFinalizations"] ?? []).some((f) => {
      const row = f as ScrubbedRow;
      return rowId(row.counterentity) === peer && rowHash(row.initialProofbodyHash) === initialHash;
    })
  );
};
/**
 * og retireFinalizedDisputeState: every finalization, counter-proof and Source hash-ladder registration for the Account
 * goes, keeping a sent batch that acknowledges exactly this finalization.
 */
const finalizedRetirement = (
  jb: RetiringJBatch | undefined,
  self: string,
  e: {
    readonly sender: string;
    readonly counterentity: string;
    readonly initialProofbodyHash?: string | undefined;
    readonly batchNonce?: number | undefined;
  },
): RetiredJBatch => {
  const sender = rowId(e.sender);
  const peer = disputePeerOf(self, e.sender, e.counterentity);
  const initialHash = rowHash(e.initialProofbodyHash);
  const synced = syncBatchNonce(jb, sender, self, e.batchNonce);
  const ownAck = sender === self && sentAcksFinality(synced.jb, e.batchNonce, peer, initialHash);
  return retiredWith(synced, [scrubFinalizations(peer), scrubCounters(peer), scrubSourceLadders(peer)], ownAck);
};
/**
 * og J7 on the Host's jBatchState for a DisputeStarted / DisputeFinalized whose Account is the Host's one Account: the
 * nonce sync and J batch retirement, and og queueLocalJBatchBroadcast as a `j_broadcast_request`. The Host carries no
 * crontab and no Entity messages (those are disputeStartedEffects / disputeFinalizedEffects on the Entity). og
 * applyKnownHtlcSecret on the starter's secrets can only queue htlc_resolves for this Host's one Account, which the
 * event just froze, so og's applyLocalAccountEffects suppresses them: nothing to do.
 */
export const hostDisputeJBatch = (
  j: JState,
  self: string,
  e: Extract<JEvent, { readonly type: "DisputeStarted" | "DisputeFinalized" }>,
): Result<{ readonly j: JState; readonly effects: readonly HostEffect[] }, JObserveError> => {
  const jb = j.jBatch as unknown as RetiringJBatch | undefined;
  const r =
    e.type === "DisputeStarted" ? startedRetirement(jb, rowId(self), e) : finalizedRetirement(jb, rowId(self), e);
  const effects: readonly HostEffect[] = r.broadcast ? [{ _tag: "j_broadcast_request", entityId: self }] : [];
  return ok({ j: r.jb === undefined ? j : { ...j, jBatch: r.jb as unknown as JBatchState }, effects });
};
// ---- Account Hankos through the Entity manifest: og accountInput response + proposePendingAccountFrames, hanko-witness.ts, hanko/signing.ts ----
/**
 * og signs Account frames, ACKs and dispute proofs as secondary `hashesToSign` of the Entity frame and attaches the
 * quorum Hanko only after the frame certifies. While the frame folds, this Entity's own Hanko on digest `d` is the
 * placeholder `pendingHanko(d)`; `installFrame` replaces it with og's quorum Hanko. Own Hankos never enter the Entity
 * state root (only the peer's do), so the placeholder never reaches a hash.
 */
const pendingHanko = (digest: string): Hanko => `0xfe${hexBody(digest).toLowerCase()}`;
const pendingVerify = (verify: Verify, self: EntityId): Verify => (d, h, e) =>
  (h === pendingHanko(d) && sameHex(e, self)) || verify(d, h, e);
/** The dispute Hanko an ACK or proposal carries: a pending one to sign, the one already signed, or none. */
const pendingDispute = (plan: DisputePlan): DisputeHanko | undefined =>
  match(plan, {
    sign: ({ draft }): DisputeHanko | undefined => ({ ...draft, hanko: pendingHanko(draft.hash) }),
    resend: ({ disputeHanko }) => disputeHanko,
    none: () => undefined,
  });
/**
 * og accountHasProposableMempool (without the settlement-freeze and HTLC-cap refinements): an active Account with no
 * frame in flight and queued txs.
 */
const proposableChild = (c: AccountReplica | undefined): c is OpenAccount =>
  c !== undefined && c._tag === "open" && c.mempool.length > 0;
const hasProposableAccount = (r: Folded): boolean => [...r.accountReplicas.values()].some(proposableChild);
/**
 * The Entity draft after an Account answer: the answer's state, both drafts' outputs and events, and the cross-j swaps
 * kept.
 */
const afterAnswer = (d: Draft, acked: Draft): Draft => ({
  ...acked,
  outputs: [...d.outputs, ...acked.outputs],
  events: [...(d.events ?? []), ...(acked.events ?? [])],
  runtimeEvents: [...(d.runtimeEvents ?? []), ...(acked.runtimeEvents ?? [])],
  ...opt("swaps", d.swaps),
});
/**
 * og commits a received Account frame and answers it in the same Entity frame (the forced ACK response), signed through
 * the manifest.
 */
const answerFrame = (d: Draft, peer: EntityId, ctx: FoldContext): Result<Routed, EntityError> => {
  const child = d.accountReplicas.get(peer), self = d.state.id;
  if (child === undefined || child._tag !== "received") return ok({ draft: d, effects: [] });
  return chain(partyOf(replicaId(child), self), (party) => chain(previewAck(child, self), (p) => {
    const ack: AccountInput = {
      kind: "ack",
      ...sentBy(child, party),
      height: p.height,
      frameHash: p.frameHash,
      frameHanko: pendingHanko(p.frameHash),
      ...opt("disputeHanko", pendingDispute(p.dispute)),
    };
    const env = {
      verify: pendingVerify(ctx.verify, self),
      self,
      now: ctx.timestamp,
      autoRebalance: hubConfigOf(d.state) === undefined,
      deltaTransformer: accountDt(ctx, child),
    };
    const answered = routedRaw(d.state, d.accountReplicas, peer, applyAccountInput(child, ack, env));
    return map(answered, ({ draft: acked, effects }) => ({ draft: afterAnswer(d, acked), effects }));
  }));
};
const entityJHeight = (state: EntityState): bigint => {
  const h = state.committed["lastFinalizedJHeight"];
  return typeof h === "number" && Number.isSafeInteger(h) && h >= 0 ? BigInt(h) : 0n;
};
// ---- og entity/transition/cross-j-proposer-materialization.ts: one exact opening cohort per sibling pair ----
const openingText = (v: unknown): string => String(v ?? "").trim().toLowerCase();
const byText = (a: string, b: string): number => a.localeCompare(b);
/**
 * og CROSS_J_OPENING_COHORT_MAX_ORDERS: one order per atomic opening cohort (both proofs stay under their byte limit).
 */
const CROSS_J_OPENING_COHORT_MAX_ORDERS = 1;
type OpeningLeg = { readonly orderId: string; readonly route: CrossRoute };
/** og crossJOpeningLegs: each cross_pull_lock carrying its binding and route, one per order, sorted by order id. */
const crossOpeningLegs = (txs: readonly AccountTx[]): Result<readonly OpeningLeg[], EntityError> => {
  const bound = txs.flatMap((tx) =>
    tx.type === "cross_pull_lock" && tx.crossJurisdiction && tx.crossJurisdictionRoute
      ? [{ orderId: openingText(tx.crossJurisdiction.orderId), route: tx.crossJurisdictionRoute }]
      : [],
  );
  const legs = traverse(bound, (leg) => (leg.orderId ? ok(leg) : invariant("CROSS_J_OPENING_ORDER_ID_REQUIRED")));
  return map(legs, (xs) =>
    [...new Map(xs.map((leg) => [leg.orderId, leg] as const)).values()].toSorted((a, b) =>
      byText(a.orderId, b.orderId),
    ),
  );
};
type SiblingAccount = { readonly entityId: string; readonly signerId: string; readonly accountId: string };
/** og pairedCrossJSiblingAccount: the other jurisdiction's Account of this leg, by this Entity's role in the route. */
const pairedSibling = (local: string, route: CrossRoute): Result<SiblingAccount, EntityError> => {
  const t = openingText;
  const sibling = (entityId: unknown, signerId: unknown, accountId: unknown): Result<SiblingAccount, EntityError> =>
    ok({ entityId: t(entityId), signerId: t(signerId), accountId: t(accountId) });
  switch (t(local)) {
    case t(route.source.entityId):
      return sibling(route.target.counterpartyEntityId, route.targetSignerId, route.target.entityId);
    case t(route.source.counterpartyEntityId):
      return sibling(route.target.entityId, route.targetHubSignerId, route.target.counterpartyEntityId);
    case t(route.target.entityId):
      return sibling(route.source.counterpartyEntityId, route.sourceHubSignerId, route.source.entityId);
    case t(route.target.counterpartyEntityId):
      return sibling(route.source.entityId, route.sourceSignerId, route.source.counterpartyEntityId);
    default:
      return invariant(`CROSS_J_OPENING_LOCAL_ROLE_INVALID:${route.orderId}:${t(local)}`);
  }
};
/** og crossJOpeningOrderId: a cross pull lock or a cross swap offer names its order. */
const openingOrderOf = (tx: AccountTx): Result<string | undefined, EntityError> => {
  const binding = tx.type === "cross_pull_lock" || tx.type === "swap_offer" ? tx.crossJurisdiction : undefined;
  if (!binding) return ok(undefined);
  const orderId = openingText(binding.orderId);
  return orderId ? ok(orderId) : invariant("CROSS_J_OPENING_ORDER_ID_REQUIRED");
};
const selectOpeningTxs = (
  txs: readonly AccountTx[],
  orderIds: ReadonlySet<string>,
): Result<readonly AccountTx[], EntityError> =>
  map(
    traverse(txs, (tx) => map(openingOrderOf(tx), (o) => (o !== undefined && orderIds.has(o) ? [tx] : []))),
    (xs) => xs.flat(),
  );
/** The local legs bound for one sibling Account, keyed by its identity. */
type SiblingGroup = { readonly key: string; readonly sibling: SiblingAccount; readonly orderIds: ReadonlySet<string> };
type BoundLeg = { readonly key: string; readonly sibling: SiblingAccount; readonly orderId: string };
/** Our opening legs grouped by the sibling Account each pairs with, in key order. */
const siblingGroups = (self: string, legs: readonly OpeningLeg[]): Result<readonly SiblingGroup[], EntityError> => {
  const bind = (leg: OpeningLeg): Result<BoundLeg, EntityError> =>
    chain(pairedSibling(self, leg.route), (sibling) => {
      if (!sibling.entityId || !sibling.signerId || !sibling.accountId)
        return invariant(`CROSS_J_OPENING_SIBLING_BINDING_REQUIRED:${leg.orderId}`);
      return ok({ key: `${sibling.entityId}:${sibling.signerId}:${sibling.accountId}`, sibling, orderId: leg.orderId });
    });
  const group = ([key, members]: readonly [string, readonly BoundLeg[]]): SiblingGroup[] => {
    const [first] = members;
    return first === undefined
      ? []
      : [{ key, sibling: first.sibling, orderIds: new Set(members.map((m) => m.orderId)) }];
  };
  return map(traverse(legs, bind), (bound) =>
    [...Map.groupBy(bound, (b) => b.key)].flatMap(group).toSorted((a, b) => byText(a.key, b.key)),
  );
};
/** The sibling's legs that pair back with our Account: its reciprocal order ids. */
const reciprocalOrders = (
  siblingId: string,
  legs: readonly OpeningLeg[],
  self: string,
  counterparty: string,
): Result<ReadonlySet<string>, EntityError> => {
  const back = (leg: OpeningLeg): Result<readonly string[], EntityError> =>
    map(pairedSibling(siblingId, leg.route), (b) =>
      b.entityId === openingText(self) && b.accountId === counterparty ? [leg.orderId] : [],
    );
  return map(traverse(legs, back), (ids) => new Set(ids.flat()));
};
/** A frozen sibling cohort: our side proposes exactly its orders, or waits. */
const frozenCohort = (
  mempool: readonly AccountTx[], reciprocal: ReadonlySet<string>, common: readonly string[],
): Result<readonly AccountTx[] | undefined, EntityError> => {
  if (reciprocal.size !== common.length) return ok(undefined);
  return chain(selectOpeningTxs(mempool, new Set(common)), (selected) => (selected.length > ACCOUNT_MEMPOOL_SIZE
    ? invariant(`CROSS_J_OPENING_RECIPROCAL_COHORT_TOO_LARGE:${selected.length}`)
    : ok(selected)));
};
type CohortFit = { readonly chosen: readonly string[]; readonly done: boolean };
/** og fitOpeningCohort: the first common order both Accounts can carry (one order per cohort). */
const fittedCohort = (
  mempool: readonly AccountTx[],
  siblingMempool: readonly AccountTx[],
  common: readonly string[],
): Result<readonly AccountTx[] | undefined, EntityError> => {
  const fit = (f: CohortFit, orderId: string): Result<CohortFit, EntityError> => {
    if (f.done || f.chosen.length >= CROSS_J_OPENING_COHORT_MAX_ORDERS) return ok({ ...f, done: true });
    const one = new Set([orderId]);
    return chain(selectOpeningTxs(mempool, one), (mine) =>
      map(selectOpeningTxs(siblingMempool, one), (theirs): CohortFit => {
        if (mine.length === 0 || theirs.length === 0) return f;
        if (mine.length > ACCOUNT_MEMPOOL_SIZE || theirs.length > ACCOUNT_MEMPOOL_SIZE) return { ...f, done: true };
        return { ...f, chosen: [...f.chosen, orderId] };
      }),
    );
  };
  return chain(foldResult(common, { chosen: [], done: false }, fit), ({ chosen }) =>
    chosen.length === 0 ? ok(undefined) : selectOpeningTxs(mempool, new Set(chosen)),
  );
};
/** One sibling group's cohort; `undefined` when this sibling offers none. */
const siblingCohort = (
  state: EntityState, counterparty: string, mempool: readonly AccountTx[], siblings: SiblingReplicas, g: SiblingGroup,
): Result<readonly AccountTx[] | undefined, EntityError> => {
  const replica = siblings(g.sibling.entityId, g.sibling.signerId);
  if (replica === undefined) return invariant(`CROSS_J_OPENING_SIBLING_REPLICA_MISSING:${g.key}`);
  const account = [...replica.accountReplicas].find(([id]) => openingText(id) === g.sibling.accountId)?.[1];
  if (account === undefined) return invariant(`CROSS_J_OPENING_SIBLING_ACCOUNT_MISSING:${g.key}`);
  // og: only a pending OPENING freezes the cohort; an unrelated pending frame never hides queued opening legs
  const pendingTxs = account._tag === "proposed" ? account.candidate.frame.txs : [];
  const siblingMempool = "mempool" in account ? account.mempool : [];
  return chain(crossOpeningLegs(pendingTxs), (pendingOpening) => {
    const frozen = pendingOpening.length > 0;
    return chain(crossOpeningLegs(frozen ? pendingTxs : siblingMempool), (siblingLegs) =>
      chain(reciprocalOrders(replica.state.id, siblingLegs, state.id, counterparty), (reciprocal) => {
        const common = [...g.orderIds].filter((o) => reciprocal.has(o)).toSorted(byText);
        if (reciprocal.size === 0 || common.length === 0) return ok(undefined);
        return frozen ? frozenCohort(mempool, reciprocal, common) : fittedCohort(mempool, siblingMempool, common);
      }));
  });
};
/**
 * og selectCrossJOpeningAccountProposalTxs: `undefined` for an ordinary proposal, `null` while the reciprocal leg is
 * not available, else exactly the cohort both siblings select (a frozen sibling cohort, or the first common order that
 * fits both frames).
 */
export const crossOpeningSelection = (
  state: EntityState,
  peer: EntityId,
  mempool: readonly AccountTx[],
  siblings: SiblingReplicas,
): Result<readonly AccountTx[] | null | undefined, EntityError> =>
  chain(crossOpeningLegs(mempool), (localLegs): Result<readonly AccountTx[] | null | undefined, EntityError> => {
    if (localLegs.length === 0) return ok(undefined);
    const counterparty = openingText(peer);
    return chain(siblingGroups(state.id, localLegs), (groups) => {
      const first = (
        found: readonly AccountTx[] | undefined,
        g: SiblingGroup,
      ): Result<readonly AccountTx[] | undefined, EntityError> =>
        found !== undefined ? ok(found) : siblingCohort(state, counterparty, mempool, siblings, g);
      return map(foldResult(groups, undefined as readonly AccountTx[] | undefined, first), (found) => found ?? null);
    });
  });
/**
 * og entityTxContainsCrossJSetup / entityTxContainsAccountTransition over a tx and its nested command / runtimeOutput
 * txs.
 */
const nestedFrameTxs = (tx: EntityTx): readonly EntityTx[] => {
  if (tx.type === "entityCommand") return tx.data.txs;
  return tx.type === "runtimeOutput" && tx.data.protocol === "cross-j" ? tx.data.entityTxs : [tx];
};
const CROSS_SETUP_TXS: ReadonlySet<string> =
  new Set(["materializeCrossJurisdictionSwap", "materializeCrossJurisdictionClear", "registerCrossJurisdictionSwap"]);
const crossSetupTx = (tx: EntityTx): boolean => nestedFrameTxs(tx).some((n) => CROSS_SETUP_TXS.has(n.type));
const accountTransitionTx = (tx: EntityTx): boolean =>
  nestedFrameTxs(tx).some((n) => n.type === "accountInput" || n.type === "crossJurisdictionFillNotice");
type Proposing = { readonly draft: Draft; readonly frames: number };
/** The propose input for a planned frame (with its pending Hankos) or an idle proposal, at the Entity clock. */
const proposalInput = (
  plan: ProposalPlan,
  clock: FrameClock,
  selected: readonly AccountTx[] | undefined,
): AccountInput =>
  match(plan, {
    frame: ({ preview }): AccountInput => ({
      kind: "propose",
      frameHanko: pendingHanko(preview.frame.stateHash),
      ...opt("disputeHanko", pendingDispute(preview.dispute)),
      ...opt("selected", selected),
      ...clock,
    }),
    idle: (): AccountInput => ({ kind: "propose", ...opt("selected", selected), ...clock }),
  });
/** The Entity draft after one Account proposal: the proposal's replicas and state, both drafts' outputs and events. */
const afterProposal = (draft: Draft, next: Draft): Draft => ({
  ...draft,
  ...next,
  outputs: [...draft.outputs, ...next.outputs],
  events: [...(draft.events ?? []), ...(next.events ?? [])],
  runtimeEvents: [...(draft.runtimeEvents ?? []), ...(next.runtimeEvents ?? [])],
});
/**
 * One Account's proposal in og proposePendingAccountFrames: a refused proposal is skipped unless the Account machine
 * threw.
 */
const proposeOne =
  (ctx: FoldContext, clock: FrameClock) =>
  (acc: Proposing, peer: EntityId): Result<Proposing, EntityError> => {
    const { draft } = acc;
    const self = draft.state.id;
    const child = draft.accountReplicas.get(peer);
    if (!proposableChild(child)) return ok(acc);
    const cohort =
      ctx.siblings === undefined
        ? ok(undefined)
        : crossOpeningSelection(draft.state, peer, child.mempool, ctx.siblings);
    return chain(cohort, (selected): Result<Proposing, EntityError> => {
      if (selected === null) return ok(acc);
      const dt = accountDt(ctx, child);
      const party = partyOf(replicaId(child), self);
      const plan = planAccountProposal(child, self, clock, ctx.verify, selected, dt, ctx.boardAuthority);
      if (!plan.ok) return accountThrew(plan.error) ? err(plan.error) : ok(acc);
      if (!party.ok) return ok(acc);
      const input = proposalInput(plan.value, clock, selected) as Propose;
      const proposed = propose(child, input, {
        verify: pendingVerify(ctx.verify, self),
        party: party.value,
        deltaTransformer: dt,
      });
      const next = routed(draft.state, draft.accountReplicas, peer, proposed);
      if (!next.ok) return accountThrew(next.error) ? err(next.error) : ok(acc);
      return ok({
        draft: afterProposal(draft, next.value),
        frames: acc.frames + (plan.value._tag === "frame" ? 1 : 0),
      });
    });
  };
/**
 * og sends one final Account input per Account: an ACK already riding on that Account's new frame is not sent again.
 */
const withoutCarriedAcks = (outputs: readonly EntityOutput[]): readonly EntityOutput[] => {
  const carried = new Set(
    outputs.flatMap((o) =>
      "tx" in o && o.tx.data.kind === "ack_frame" && o.tx.data.ack !== null ? [`${o.to}|${canon(o.tx.data.ack)}`] : [],
    ),
  );
  return outputs.filter(
    (o) => !("tx" in o && o.tx.data.kind === "ack" && carried.has(`${o.to}|${canon(ackOf(o.tx.data))}`)),
  );
};
/**
 * og proposePendingAccountFrames: every proposable Account in worklist order proposes one frame at the Entity clock; a
 * refused proposal is not a frame. A cross-j opening proposes exactly its sibling cohort (og
 * selectCrossJOpeningAccountProposalTxs) and waits while the reciprocal is missing.
 */
const proposeAccounts = (d: Draft, order: readonly EntityId[], ctx: FoldContext): Result<Proposing, EntityError> => {
  const clock: FrameClock = { timestamp: ctx.timestamp, jHeight: entityJHeight(d.state) };
  return map(foldResult(order, { draft: d, frames: 0 }, proposeOne(ctx, clock)), ({ draft, frames }) =>
    ({ draft: { ...draft, outputs: withoutCarriedAcks(draft.outputs) }, frames }));
};
type Validator = { readonly key: string; readonly share: bigint };
/**
 * One validator's signature over the digest, packed: a known validator, once, canonical (0/1 recovery, low s) and
 * recovering to it.
 */
const packSignature =
  (validators: readonly Validator[], digest: string) =>
  (
    packed: ReadonlyMap<string, RawSig>,
    [raw, sig]: readonly [string, Signature],
  ): Result<ReadonlyMap<string, RawSig>, EntityError> => {
    const bad: EntityError = { _tag: "hanko_build" };
    const key = signerId(raw);
    const bytes = parseHex(sig);
    if (!validators.some((x) => x.key === key) || packed.has(key) || bytes === null || bytes.length !== 65)
      return err(bad);
    const recovery = bytes[64] ?? 0;
    const r = bytes.subarray(0, 32);
    const s = bytes.subarray(32, 64);
    const canonical = (recovery === 0 || recovery === 1) && !isZeroWord(s) && wordAt(s, 0) <= HALF_ORDER;
    if (!canonical || !sameHex(recoverRawSigner(digest, sig) ?? undefined, key)) return err(bad);
    return ok(mapSet(packed, key, { r, s, v: 27 + recovery }));
  };
type HankoDelays = {
  readonly boardChangeDelay: number;
  readonly controlChangeDelay: number;
  readonly dividendChangeDelay: number;
};
const NO_DELAYS: HankoDelays = { boardChangeDelay: 0, controlChangeDelay: 0, dividendChangeDelay: 0 };
/**
 * A board quorum's Hanko: signers after the idle members' placeholders, both sorted by address, one claim over the
 * whole board.
 */
const boardQuorumHanko = (
  q: Authority,
  entityWord: string,
  validators: readonly Validator[],
  packed: ReadonlyMap<string, RawSig>,
): Hanko => {
  const delays = match<Authority, HankoDelays>(q, {
    teaching: () => NO_DELAYS,
    board: ({ board }) => ({
      boardChangeDelay: board.boardChangeDelay,
      controlChangeDelay: board.controlChangeDelay,
      dividendChangeDelay: board.dividendChangeDelay,
    }),
  });
  const byKey = (a: Validator, b: Validator): number => asc(a.key, b.key);
  const signing = validators.filter((v) => packed.has(v.key)).toSorted(byKey);
  const idle = validators.filter((v) => !packed.has(v.key)).toSorted(byKey);
  const entityIndexes = validators.map((v) => {
    const i = signing.indexOf(v);
    return i < 0 ? idle.indexOf(v) : idle.length + i;
  });
  return encodeHankoEnvelope({
    placeholders: idle.map((v) => `0x${hexBody(v.key).padStart(64, "0")}`),
    packedSignatures: packSignatures(signing.map((v) => packed.get(v.key) as RawSig)),
    memberSignatures: [],
    claims: [
      {
        entityId: entityWord,
        entityIndexes,
        weights: validators.map((v) => Number(v.share)),
        threshold: Number(thresholdOf(q)),
        ...delays,
      },
    ],
  });
};
/**
 * og buildQuorumHanko (single signer: encodeSingleSignerEntityHankos): canonical 0/1-recovery signatures by the named
 * validators, signers then placeholders sorted by address.
 */
export const quorumHanko = (
  state: EntityState,
  digest: string,
  sigs: ReadonlyMap<string, Signature>,
): Result<Hanko, EntityError> => {
  const q = state.quorum;
  const validators = [...membersOf(q)].map(([addr, m]): Validator => ({ key: signerId(addr), share: m.shares }));
  return chain(
    foldResult(sigs, new Map() as ReadonlyMap<string, RawSig>, packSignature(validators, digest)),
    (packed) => {
      const power = validators.reduce((n, v) => n + (packed.has(v.key) ? v.share : 0n), 0n);
      if (power < thresholdOf(q)) return err({ _tag: "hanko_build" });
      return chain(assertBoardAuthority(state), () => {
        const entityWord = `0x${BigInt(state.id).toString(16).padStart(64, "0")}`;
        if (!isSingleSigner(q)) return ok(boardQuorumHanko(q, entityWord, validators, packed));
        const claim = { entityId: entityWord, entityIndexes: [0], weights: [1], threshold: 1, ...NO_DELAYS };
        return ok(
          encodeHankoEnvelope({
            placeholders: [],
            packedSignatures: packSignatures([...packed.values()]),
            memberSignatures: [],
            claims: [claim],
          }),
        );
      });
    },
  );
};
/** The quorum Hanko of every secondary `hashesToSign` entry, by hash (og proof.hankos). */
const secondaryHankos = (
  d: Draft,
  frame: EntityFrame,
  signatures: Precommits,
): Result<ReadonlyMap<string, Hanko>, EntityError> => {
  const signed = (h: HashToSign, i: number): Result<readonly [string, Hanko], EntityError> => {
    const sigs = new Map(
      [...signatures].flatMap(([id, bundle]) => {
        const s = bundle[i];
        return s === undefined ? [] : [[id, s] as const];
      }),
    );
    return map(quorumHanko(d.state, h.hash, sigs), (hanko) => [h.hash, hanko] as const);
  };
  return map(
    traverse(frame.hashesToSign.slice(1), (h, i) => signed(h, i + 1)),
    (pairs) => new Map(pairs),
  );
};
/** A pending-Hanko substitution: the quorum Hanko for a placeholder, any other Hanko as it is. */
type HankoFill = (h: Hanko) => Hanko;
const fillMaybe = (fill: HankoFill, h: string | undefined): string | undefined => (h === undefined ? undefined : fill(h));
const fillDispute = (fill: HankoFill, x: DisputeHanko | undefined): DisputeHanko | undefined =>
  (x === undefined ? undefined : { ...x, hanko: fill(x.hanko) });
const fillAck = (fill: HankoFill, a: AccountAck): AccountAck =>
  ({ ...a, frameHanko: fill(a.frameHanko), ...opt("disputeHanko", fillDispute(fill, a.disputeHanko)) });
const fillSent = (fill: HankoFill, x: SentProposal | undefined): SentProposal | undefined =>
  x === undefined
    ? undefined
    : { ack: x.ack === null ? null : fillAck(fill, x.ack), ...opt("disputeHanko", fillDispute(fill, x.disputeHanko)) };
/** og attaches the settlement / post-proof quorum Hankos to our settle_transition hanko intent. */
const fillTx = (fill: HankoFill, tx: WireAccountTx): WireAccountTx => {
  if (tx.type !== "settle_transition" || tx.kind !== "hanko") return tx;
  const postProof = { ...tx.postProof, ...opt("hanko", fillMaybe(fill, tx.postProof.hanko)) };
  return { ...tx, ...opt("settlementHanko", fillMaybe(fill, tx.settlementHanko)), postProof };
};
/** ... and to the settlement workspace it signs. */
const fillBody = (fill: HankoFill, b: AccountBody): AccountBody => {
  const w = b.settlement;
  const p = w?.postSettlementDisputeProof;
  if (w === undefined) return b;
  const proof =
    p === undefined
      ? {}
      : {
          postSettlementDisputeProof: {
            ...p,
            leftHanko: fillMaybe(fill, p.leftHanko),
            rightHanko: fillMaybe(fill, p.rightHanko),
          },
        };
  return {
    ...b,
    settlement: { ...w, leftHanko: fillMaybe(fill, w.leftHanko), rightHanko: fillMaybe(fill, w.rightHanko), ...proof },
  };
};
const fillFrame = (fill: HankoFill, frame: AccountFrame): AccountFrame => ({
  ...frame,
  txs: frame.txs.map((tx) => fillTx(fill, tx)),
});
const fillReplica = (fill: HankoFill, c: AccountReplica): AccountReplica => {
  const head: AccountHead =
    c.head._tag !== "installed"
      ? c.head
      : {
          ...c.head,
          certificate: {
            ...c.head.certificate,
            left: fill(c.head.certificate.left),
            right: fill(c.head.certificate.right),
          },
        };
  const dispute: DisputeWitnesses = { ...c.dispute, ...opt("current", fillDispute(fill, c.dispute.current)) };
  const base = {
    ...c,
    head,
    dispute,
    state: fillBody(fill, c.state),
    mempool: c.mempool.map((tx) => fillTx(fill, tx)),
    ...(c.acknowledged === undefined ? {} : { acknowledged: fillAck(fill, c.acknowledged) }),
  };
  if (!("candidate" in c)) return base as AccountReplica;
  const k = c.candidate;
  const draft = { ...k.draft, state: fillBody(fill, k.draft.state) };
  return {
    ...base,
    candidate: new Candidate(
      fillFrame(fill, k.frame),
      fill(k.frameHanko),
      k.frameProof,
      draft,
      k.floor,
      fillSent(fill, k.sent),
    ),
  } as AccountReplica;
};
const fillMessage = (fill: HankoFill, m: AccountPeerInput): AccountPeerInput =>
  matchBy("kind", m, {
    ack: (a): AccountPeerInput => ({
      ...a,
      frameHanko: fill(a.frameHanko),
      ...opt("disputeHanko", fillDispute(fill, a.disputeHanko)),
    }),
    ack_frame: (a): AccountPeerInput => ({
      ...a,
      frame: fillFrame(fill, a.frame),
      ack: a.ack === null ? null : fillAck(fill, a.ack),
      frameHanko: fill(a.frameHanko),
      ...opt("disputeHanko", fillDispute(fill, a.disputeHanko)),
    }),
    dispute: (a): AccountPeerInput => ({
      ...a,
      disputeHanko: { ...a.disputeHanko, hanko: fill(a.disputeHanko.hanko) },
    }),
    board_hanko_refresh: (a): AccountPeerInput => ({
      ...a,
      frameHanko: fill(a.frameHanko),
      ...opt("disputeHanko", fillDispute(fill, a.disputeHanko)),
    }),
  });
/**
 * og attachHankoWitnessToOutputs/State: each secondary manifest entry's quorum Hanko replaces its placeholder in the
 * Account replicas and outputs.
 */
const fillHankos = (d: Draft, byHash: ReadonlyMap<string, Hanko>): Result<Draft, EntityError> => {
  const table = new Map([...byHash].map(([hash, hanko]) => [pendingHanko(hash), hanko] as const));
  if (table.size === 0) return ok(d);
  const fill: HankoFill = (h) => table.get(h) ?? h;
  const outputs = d.outputs.map((o) =>
    "tx" in o ? { to: o.to, tx: { type: "accountInput" as const, data: fillMessage(fill, o.tx.data) } } : o,
  );
  return ok({
    ...d,
    accountReplicas: new Map([...d.accountReplicas].map(([k, c]) => [k, fillReplica(fill, c)])),
    outputs,
  });
};
const PROFILE_ENTITY_KINDS: ReadonlySet<string> = new Set([
  "company",
  "foundation",
  "government",
  "nonprofit",
  "person",
  "protocol",
]);
const PROFILE_ENTITY_SECTORS: ReadonlySet<string> = new Set([
  "commerce",
  "education",
  "energy",
  "finance",
  "healthcare",
  "infrastructure",
  "media",
  "professional-services",
  "public-sector",
  "real-estate",
  "technology",
]);
/** og's entityKind update: absent keeps the committed kind, `null` clears it. */
const updatedKind = (next: string | null | undefined, prev: unknown): string | undefined => {
  if (next === null) return undefined;
  if (next !== undefined) return next;
  return typeof prev === "string" ? prev : undefined;
};
/**
 * og system/basic.ts handleProfileUpdateEntityTx: the committed profile with og's defaults, kind and canonical sector
 * rules; `isHub` is never taken from the update.
 */
const profileUpdate = (state: EntityState, p: ProfileUpdate): Result<Binary, EntityError> => {
  const bad = (reason: Of<EntityError, "profile_update">["reason"]): Result<never, EntityError> =>
    err({ _tag: "profile_update", reason });
  if (p.entityId !== state.id) return bad("entity");
  const prev = (state.committed["profile"] ?? {}) as { readonly [k: string]: unknown };
  const text = (k: string): string => {
    const v = prev[k];
    return typeof v === "string" ? v : "";
  };
  const entityKind = updatedKind(p.entityKind, prev["entityKind"]);
  if (entityKind !== undefined && !PROFILE_ENTITY_KINDS.has(entityKind)) return bad("entity_kind");
  const sectors = p.sectors ?? (Array.isArray(prev["sectors"]) ? (prev["sectors"] as readonly string[]) : []);
  if (!Array.isArray(sectors) || sectors.length > 4 || sectors.some((s) => !PROFILE_ENTITY_SECTORS.has(s)))
    return bad("sectors_invalid");
  const canonical = sectors.toSorted(asc);
  if (new Set(sectors).size !== sectors.length || canonical.some((s, i) => s !== sectors[i]))
    return bad("sectors_noncanonical");
  const rawName = p.name ?? prev["name"];
  const name =
    typeof rawName === "string" && rawName.trim().length > 0 ? rawName.trim() : `Entity ${state.id.slice(-4)}`;
  return ok({
    name,
    ...(prev["isHub"] === undefined ? {} : { isHub: prev["isHub"] as Binary }),
    ...(entityKind ? { entityKind } : {}),
    ...(canonical.length > 0 ? { sectors: canonical } : {}),
    avatar: typeof p.avatar === "string" ? p.avatar : text("avatar"),
    bio: typeof p.bio === "string" ? p.bio : text("bio"),
    website: typeof p.website === "string" ? p.website : text("website"),
  });
};
// ---- og jurisdiction/machine/board-registry/index.ts: the certified-board registry (one Patricia trie of board records per J stack) ----
export type CertifiedBoardSource = "FoundationBootstrapped" | "EntityRegistered" | "BoardActivated";
/** og CertifiedBoardRegistryState: the only board authority Entity consensus commits (EntityState.certifiedBoardState). */
export type CertifiedBoardRegistryState = { readonly stackKey: string; readonly boardRegistryRoot: string; readonly finalizedJHeight: number; readonly finalizedJBlockHash: string; readonly eventHistoryRoot: string };
export type CertifiedBoardRecord = {
  readonly stackKey: string; readonly entityId: string; readonly boardHash: string; readonly boardEpoch: number; readonly previousBoardHash: string; readonly previousBoardValidUntil: number;
  readonly activatedAtJHeight: number; readonly logIndex: number; readonly blockHash: string; readonly transactionHash: string; readonly source: CertifiedBoardSource;
};
export type CertifiedBoardNode = { readonly version: 1; readonly type: "leaf"; readonly key: string; readonly record: CertifiedBoardRecord } | { readonly version: 1; readonly type: "branch"; readonly bit: number; readonly left: string; readonly right: string };
/** og infrastructure.certifiedBoardNodes: immutable content-addressed nodes, never part of the Entity root. */
export type BoardNodes = ReadonlyMap<string, CertifiedBoardNode>;
export type CertifiedBoardProof = { readonly version: 1; readonly stackKey: string; readonly entityId: string; readonly nodes: readonly CertifiedBoardNode[] };
/** og config.jurisdiction fields the registry reads. */
export type BoardStack = { readonly chainId: unknown; readonly depositoryAddress: unknown; readonly entityProviderAddress: unknown; readonly entityProviderDeploymentBlock?: number | undefined };
/** og throws `CODE:detail`; `code` is the og message. */
export type BoardRegistryError = Tagged<"board_registry", { readonly code: string }>;
const boardErr = (code: string): Result<never, BoardRegistryError> => err({ _tag: "board_registry", code });
const boardDomain = (label: string): string => keccak256Hex(utf8(label));
const BOARD_KEY_DOMAIN = boardDomain("xln.certified-board.key.v1"), BOARD_RECORD_DOMAIN = boardDomain("xln.certified-board.record.v1");
const BOARD_LEAF_DOMAIN = boardDomain("xln.certified-board.leaf.v1"), BOARD_BRANCH_DOMAIN = boardDomain("xln.certified-board.branch.v1");
export const EMPTY_CERTIFIED_BOARD_ROOT = boardDomain("xln.certified-board.empty.v1");
export const FOUNDATION_ENTITY_ID = `0x${"00".repeat(31)}01`;
const BOARD_SOURCE_CODE: Readonly<Record<CertifiedBoardSource, bigint>> = { FoundationBootstrapped: 1n, EntityRegistered: 2n, BoardActivated: 3n };
/** og ethers.getAddress: optional 0x, 40 hex, a mixed-case address must carry its EIP-55 checksum. */
const ethAddress = (v: unknown): string | null => {
  const s = String(v ?? ""), a = /^[0-9a-fA-F]{40}$/.test(s) ? `0x${s}` : s;
  return /^0x[0-9a-fA-F]{40}$/.test(a) && checksumValid(a) ? a.toLowerCase() : null;
};
const boardWord = (v: unknown, label: string): Result<string, BoardRegistryError> => {
  const s = String(v ?? "").trim().toLowerCase();
  return /^0x[0-9a-f]{64}$/.test(s) ? ok(s) : boardErr(`CERTIFIED_BOARD_${label}_INVALID:${s || "missing"}`);
};
const boardJHeight = (v: unknown): Result<number, BoardRegistryError> => { const h = Number(v); return Number.isSafeInteger(h) && h >= 1 ? ok(h) : boardErr(`CERTIFIED_BOARD_J_HEIGHT_INVALID:${String(v)}`); };
const boardSeconds = (v: unknown, label: string): Result<number, BoardRegistryError> => {
  let n: bigint;
  try { n = BigInt(String(v)); } catch { return boardErr(`CERTIFIED_BOARD_${label}_INVALID:${String(v)}`); }
  return n < 0n || n > BigInt(Number.MAX_SAFE_INTEGER) ? boardErr(`CERTIFIED_BOARD_${label}_INVALID:${String(v)}`) : ok(Number(n));
};
/** og getCertifiedBoardStackKey with its chain / contract address validation. */
export const boardStackKey = (j: BoardStack): Result<string, BoardRegistryError> => {
  const chainId = Number(j.chainId);
  if (!Number.isSafeInteger(chainId) || chainId <= 0) return boardErr(`CERTIFIED_BOARD_STACK_CHAIN_INVALID:${String(j.chainId)}`);
  const depository = ethAddress(j.depositoryAddress);
  if (depository === null) return boardErr(`CERTIFIED_BOARD_STACK_DEPOSITORY_INVALID:${String(j.depositoryAddress ?? "")}`);
  const provider = ethAddress(j.entityProviderAddress);
  if (provider === null) return boardErr(`CERTIFIED_BOARD_STACK_ENTITY_PROVIDER_INVALID:${String(j.entityProviderAddress ?? "")}`);
  return ok(certifiedBoardStackKey({ chainId, depositoryAddress: depository, entityProviderAddress: provider }));
};
/** og getCertifiedBoardEntityKey. */
export const boardEntityKey = (stackKey: string, entityId: string): Result<string, BoardRegistryError> =>
  chain(boardWord(stackKey, "STACK_KEY"), (s) => map(boardWord(entityId, "ENTITY_ID"), (e) => keccak256Hex(abiEncode([A.b32(BOARD_KEY_DOMAIN), A.b32(s), A.b32(e)]))));
export const emptyBoardRegistry = (j: BoardStack): Result<CertifiedBoardRegistryState, BoardRegistryError> =>
  map(boardStackKey(j), (stackKey) => ({ stackKey, boardRegistryRoot: EMPTY_CERTIFIED_BOARD_ROOT, finalizedJHeight: 0, finalizedJBlockHash: ZERO_WORD, eventHistoryRoot: ZERO_WORD }));
const sameBoardRecord = (a: CertifiedBoardRecord, b: CertifiedBoardRecord): boolean =>
  a.stackKey === b.stackKey && a.entityId === b.entityId && a.boardHash === b.boardHash && a.boardEpoch === b.boardEpoch && a.previousBoardHash === b.previousBoardHash && a.previousBoardValidUntil === b.previousBoardValidUntil
  && a.activatedAtJHeight === b.activatedAtJHeight && a.logIndex === b.logIndex && a.blockHash === b.blockHash && a.transactionHash === b.transactionHash && a.source === b.source;
/** og hashCertifiedBoardRecord. */
export const hashBoardRecord = (r: CertifiedBoardRecord): Result<string, BoardRegistryError> =>
  chain(all({ stack: boardWord(r.stackKey, "STACK_KEY"), entity: boardWord(r.entityId, "ENTITY_ID"), board: boardWord(r.boardHash, "HASH"), prev: boardWord(r.previousBoardHash, "PREVIOUS_HASH"),
    until: boardSeconds(r.previousBoardValidUntil, "PREVIOUS_VALID_UNTIL"), height: boardJHeight(r.activatedAtJHeight), block: boardWord(r.blockHash, "BLOCK_HASH"), tx: boardWord(r.transactionHash, "TRANSACTION_HASH") }), (w) =>
    ok(keccak256Hex(abiEncode([A.b32(BOARD_RECORD_DOMAIN), A.b32(w.stack), A.b32(w.entity), A.b32(w.board), A.uint(BigInt(r.boardEpoch)), A.b32(w.prev), A.uint(BigInt(w.until)), A.uint(BigInt(w.height)),
      A.uint(BigInt(r.logIndex)), A.b32(w.block), A.b32(w.tx), A.uint(BOARD_SOURCE_CODE[r.source])]))));
/** og hashCertifiedBoardNode. */
export const hashBoardNode = (n: CertifiedBoardNode): Result<string, BoardRegistryError> => {
  if (n.version !== 1) return boardErr(`CERTIFIED_BOARD_NODE_VERSION_INVALID:${String(n.version)}`);
  if (n.type === "leaf") return chain(boardWord(n.key, "NODE_KEY"), (key) => chain(boardEntityKey(n.record.stackKey, n.record.entityId), (want) => key !== want ? boardErr(`CERTIFIED_BOARD_NODE_KEY_MISMATCH:${key}:${want}`)
    : map(hashBoardRecord(n.record), (rh) => keccak256Hex(abiEncode([A.b32(BOARD_LEAF_DOMAIN), A.uint(1n), A.b32(key), A.b32(rh)])))));
  if (!Number.isInteger(n.bit) || n.bit < 0 || n.bit > 255) return boardErr(`CERTIFIED_BOARD_BRANCH_BIT_INVALID:${String(n.bit)}`);
  return chain(boardWord(n.left, "BRANCH_LEFT"), (left) => chain(boardWord(n.right, "BRANCH_RIGHT"), (right) => left === right ? boardErr(`CERTIFIED_BOARD_BRANCH_UNARY:${left}`)
    : ok(keccak256Hex(abiEncode([A.b32(BOARD_BRANCH_DOMAIN), A.uint(1n), A.uint(BigInt(n.bit)), A.b32(left), A.b32(right)])))));
};
const readBoardNode = (nodes: BoardNodes, hash: string): Result<CertifiedBoardNode, BoardRegistryError> => chain(boardWord(hash, "NODE_HASH"), (h) => {
  const node = nodes.get(h);
  return node === undefined ? boardErr(`CERTIFIED_BOARD_NODE_MISSING:${h}`) : chain(hashBoardNode(node), (actual) => (actual !== h ? boardErr(`CERTIFIED_BOARD_NODE_CORRUPT:${h}:${actual}`) : ok(node)));
});
const keyBit = (key: string, bit: number): 0 | 1 => ((Number.parseInt(key.slice(2 + (bit >> 3) * 2, 4 + (bit >> 3) * 2), 16) >> (7 - (bit % 8))) & 1) as 0 | 1;
type BoardBranch = Extract<CertifiedBoardNode, { readonly type: "branch" }>;
type BoardLeaf = Extract<CertifiedBoardNode, { readonly type: "leaf" }>;
type BoardPath = readonly { readonly hash: string; readonly node: BoardBranch; readonly direction: 0 | 1 }[];
const walkToLeaf = (nodes: BoardNodes, root: string, key: string): Result<{ readonly path: BoardPath; readonly hash: string; readonly leaf: BoardLeaf }, BoardRegistryError> => chain(boardWord(root, "ROOT"), (start) => {
  const path: { hash: string; node: BoardBranch; direction: 0 | 1 }[] = [], seen = new Set<string>();
  let hash = start, previousBit = -1;
  for (;;) {
    if (seen.has(hash)) return boardErr(`CERTIFIED_BOARD_NODE_CYCLE:${hash}`);
    if (seen.size > 256) return boardErr("CERTIFIED_BOARD_PROOF_OVERSIZED");
    seen.add(hash);
    const read = readBoardNode(nodes, hash);
    if (!read.ok) return read;
    const node = read.value;
    if (node.type === "leaf") return ok({ path, hash, leaf: node });
    if (node.bit <= previousBit) return boardErr(`CERTIFIED_BOARD_BRANCH_ORDER_INVALID:${previousBit}:${node.bit}`);
    previousBit = node.bit;
    const direction = keyBit(key, node.bit);
    path.push({ hash, node, direction });
    hash = direction === 0 ? node.left : node.right;
  }
});
/** og lookupCertifiedBoardRecord: a divergent terminal leaf is an authenticated absence. */
export const lookupBoardRecord = (nodes: BoardNodes, root: string, stackKey: string, entityId: string): Result<CertifiedBoardRecord | null, BoardRegistryError> => chain(boardWord(root, "ROOT"), (r) =>
  r === EMPTY_CERTIFIED_BOARD_ROOT ? ok(null) : chain(boardEntityKey(stackKey, entityId), (key) => map(walkToLeaf(nodes, r, key), ({ leaf }) => (leaf.key === key ? leaf.record : null))));
type BoardPut = { readonly root: string; readonly newNodes: BoardNodes };
const putBoardNode = (fresh: Map<string, CertifiedBoardNode>, n: CertifiedBoardNode): Result<string, BoardRegistryError> => map(hashBoardNode(n), (h) => { fresh.set(h, n); return h; });
const rebuildAncestors = (fresh: Map<string, CertifiedBoardNode>, path: BoardPath, child: string): Result<string, BoardRegistryError> =>
  foldResult([...path].reverse(), child, (hash, e) => putBoardNode(fresh, { ...e.node, left: e.direction === 0 ? hash : e.node.left, right: e.direction === 1 ? hash : e.node.right }));
/** og putCertifiedBoardRecord: insert or replace one leaf, returning only the new nodes. */
export const putBoardRecord = (nodes: BoardNodes, root: string, record: CertifiedBoardRecord): Result<BoardPut, BoardRegistryError> => chain(boardEntityKey(record.stackKey, record.entityId), (key) => {
  const fresh = new Map<string, CertifiedBoardNode>();
  return chain(putBoardNode(fresh, { version: 1, type: "leaf", key, record }), (leafHash): Result<BoardPut, BoardRegistryError> => {
    if (root === EMPTY_CERTIFIED_BOARD_ROOT) return ok({ root: leafHash, newNodes: fresh });
    return chain(walkToLeaf(nodes, root, key), (walked): Result<BoardPut, BoardRegistryError> => {
      if (walked.leaf.key === key) return sameBoardRecord(walked.leaf.record, record) ? ok({ root, newNodes: new Map() }) : map(rebuildAncestors(fresh, walked.path, leafHash), (r) => ({ root: r, newNodes: fresh }));
      let bit = -1;
      for (let b = 0; b < 256; b += 1) if (keyBit(key, b) !== keyBit(walked.leaf.key, b)) { bit = b; break; }
      if (bit < 0) return boardErr(`CERTIFIED_BOARD_KEY_COLLISION:${key}`);
      const at = walked.path.findIndex((e) => e.node.bit >= bit), cut = at < 0 ? walked.path.length : at;
      const subtree = cut < walked.path.length ? (walked.path[cut]?.hash ?? walked.hash) : walked.hash, mine = keyBit(key, bit);
      return chain(putBoardNode(fresh, { version: 1, type: "branch", bit, left: mine === 0 ? leafHash : subtree, right: mine === 1 ? leafHash : subtree }), (branch) =>
        map(rebuildAncestors(fresh, walked.path.slice(0, cut), branch), (r) => ({ root: r, newNodes: fresh })));
    });
  });
});
type BoardEvent = Extract<JEvent, { readonly type: CertifiedBoardSource }>;
export const isBoardEvent = (e: JEvent): e is BoardEvent => e.type === "FoundationBootstrapped" || e.type === "EntityRegistered" || e.type === "BoardActivated";
type RecordFields = { readonly entityId: unknown; readonly boardHash: unknown; readonly boardEpoch?: number | undefined; readonly previousBoardHash?: unknown; readonly previousBoardValidUntil?: unknown; readonly source: CertifiedBoardSource };
/** og makeRecord, field validation in og's literal order. */
const makeBoardRecord = (stackKey: string, jHeight: number, blockHash: string, transactionHash: string, logIndex: number, f: RecordFields): Result<CertifiedBoardRecord, BoardRegistryError> =>
  chain(all({ stackKey: boardWord(stackKey, "STACK_KEY"), entityId: boardWord(f.entityId, "ENTITY_ID"), boardHash: boardWord(f.boardHash, "HASH") }), (w) => {
    const epoch = Number(f.boardEpoch ?? 0);
    if (!Number.isSafeInteger(epoch) || epoch < 0) return boardErr(`CERTIFIED_BOARD_EPOCH_INVALID:${String(f.boardEpoch)}`);
    return chain(boardWord(f.previousBoardHash ?? ZERO_WORD, "PREVIOUS_HASH"), (previousBoardHash) => chain(boardSeconds(f.previousBoardValidUntil ?? 0, "PREVIOUS_VALID_UNTIL"), (previousBoardValidUntil) =>
      chain(boardJHeight(jHeight), (activatedAtJHeight): Result<CertifiedBoardRecord, BoardRegistryError> => {
        if (!Number.isSafeInteger(logIndex) || logIndex < 0 || logIndex > 0xffff_ffff) return boardErr(`CERTIFIED_BOARD_LOG_INDEX_INVALID:${String(logIndex)}`);
        return chain(boardWord(blockHash, "BLOCK_HASH"), (b) => map(boardWord(transactionHash, "TRANSACTION_HASH"), (t): CertifiedBoardRecord =>
          ({ ...w, boardEpoch: epoch, previousBoardHash, previousBoardValidUntil, activatedAtJHeight, logIndex, blockHash: b, transactionHash: t, source: f.source })));
      })));
  });
export type BoardRegistryStep = { readonly state: CertifiedBoardRegistryState; readonly newNodes: BoardNodes };
/** og applyCertifiedBoardRegistryEvent: FoundationBootstrapped, EntityRegistered and BoardActivated advance the registry; any other event leaves it. */
export const applyBoardRegistryEvent = (current: CertifiedBoardRegistryState | undefined, nodes: BoardNodes, j: BoardStack, e: JEvent): Result<BoardRegistryStep, BoardRegistryError> =>
  chain(boardStackKey(j), (stackKey) => chain(current === undefined ? emptyBoardRegistry(j) : ok(current), (state): Result<BoardRegistryStep, BoardRegistryError> => {
    if (state.stackKey !== stackKey) return boardErr(`CERTIFIED_BOARD_STACK_MISMATCH:${state.stackKey}:${stackKey}`);
    const unchanged: BoardRegistryStep = { state, newNodes: new Map() };
    if (!isBoardEvent(e)) return ok(unchanged);
    const meta = e.meta ?? {};
    return chain(boardJHeight(meta.blockNumber), (jHeight) => chain(boardWord(meta.blockHash, "BLOCK_HASH"), (blockHash) => chain(boardWord(meta.transactionHash, "TRANSACTION_HASH"), (transactionHash) => {
      const logIndex = Number(meta.logIndex), make = (f: RecordFields) => makeBoardRecord(stackKey, jHeight, blockHash, transactionHash, logIndex, f);
      const recordOf = (): Result<CertifiedBoardRecord, BoardRegistryError> => {
        if (e.type === "FoundationBootstrapped") {
          const deployment = j.entityProviderDeploymentBlock;
          return deployment !== undefined && Number(deployment) !== jHeight ? boardErr(`CERTIFIED_BOARD_BOOTSTRAP_HEIGHT_MISMATCH:expected=${String(deployment)}:actual=${jHeight}`)
            : make({ entityId: FOUNDATION_ENTITY_ID, boardHash: e.boardHash, source: "FoundationBootstrapped" });
        }
        return chain(lookupBoardRecord(nodes, state.boardRegistryRoot, stackKey, FOUNDATION_ENTITY_ID), (foundation) => foundation === null ? boardErr(`CERTIFIED_BOARD_STACK_NOT_BOOTSTRAPPED:${stackKey}`)
          : chain(boardWord(e.entityId, "ENTITY_ID"), (entityId): Result<CertifiedBoardRecord, BoardRegistryError> => {
            if (e.type === "EntityRegistered") {
              return e.entityNumber <= 0n || e.entityNumber > UINT256_MAX || BigInt(entityId) !== e.entityNumber ? boardErr(`CERTIFIED_BOARD_ENTITY_NUMBER_MISMATCH:${entityId}:${e.entityNumber.toString()}`)
                : make({ entityId, boardHash: e.boardHash, source: "EntityRegistered" });
            }
            return chain(lookupBoardRecord(nodes, state.boardRegistryRoot, stackKey, entityId), (previous) => previous === null ? boardErr(`CERTIFIED_BOARD_ACTIVATION_BEFORE_REGISTRATION:${entityId}`)
              : make({ entityId, boardHash: e.newBoardHash, boardEpoch: previous.activatedAtJHeight === jHeight && previous.logIndex === logIndex ? previous.boardEpoch : previous.boardEpoch + 1,
                previousBoardHash: e.previousBoardHash, previousBoardValidUntil: e.previousBoardValidUntil, source: "BoardActivated" }));
          }));
      };
      return chain(recordOf(), (record) => chain(lookupBoardRecord(nodes, state.boardRegistryRoot, stackKey, record.entityId), (existing): Result<BoardRegistryStep, BoardRegistryError> => {
        if (record.source === "BoardActivated") {
          if (existing === null) return boardErr(`CERTIFIED_BOARD_ACTIVATION_BEFORE_REGISTRATION:${record.entityId}`);
          const order = existing.activatedAtJHeight === record.activatedAtJHeight ? existing.logIndex - record.logIndex : existing.activatedAtJHeight - record.activatedAtJHeight;
          if (order > 0) return boardErr(`CERTIFIED_BOARD_ACTIVATION_STALE:${record.entityId}:${record.activatedAtJHeight}`);
          if (order === 0) return sameBoardRecord(existing, record) ? ok(unchanged) : boardErr(`CERTIFIED_BOARD_ACTIVE_CONFLICT:${record.entityId}:${record.activatedAtJHeight}`);
          if (record.previousBoardHash !== existing.boardHash) return boardErr(`CERTIFIED_BOARD_PREVIOUS_HASH_MISMATCH:${record.entityId}:expected=${existing.boardHash}:received=${record.previousBoardHash}`);
          if (record.previousBoardValidUntil <= 0) return boardErr(`CERTIFIED_BOARD_PREVIOUS_EXPIRY_INVALID:${record.entityId}`);
        } else if (existing !== null) return sameBoardRecord(existing, record) ? ok(unchanged) : boardErr(`CERTIFIED_BOARD_REGISTRATION_CONFLICT:${record.entityId}`);
        return map(putBoardRecord(nodes, state.boardRegistryRoot, record), (put) => ({ state: { ...state, boardRegistryRoot: put.root }, newNodes: put.newNodes }));
      }));
    })));
  }));
/** og advanceCertifiedBoardFinality. */
export const advanceBoardFinality = (current: CertifiedBoardRegistryState | undefined, j: BoardStack, finalizedJHeight: number, finalizedJBlockHash: string, eventHistoryRoot: string): Result<CertifiedBoardRegistryState, BoardRegistryError> =>
  chain(current === undefined ? emptyBoardRegistry(j) : ok(current), (state) => chain(boardStackKey(j), (stackKey): Result<CertifiedBoardRegistryState, BoardRegistryError> => {
    if (state.stackKey !== stackKey) return boardErr(`CERTIFIED_BOARD_STACK_MISMATCH:${state.stackKey}:${stackKey}`);
    if (!Number.isSafeInteger(finalizedJHeight) || finalizedJHeight < state.finalizedJHeight) return boardErr(`CERTIFIED_BOARD_FINALITY_REWIND:${state.finalizedJHeight}:${String(finalizedJHeight)}`);
    return chain(boardWord(finalizedJBlockHash, "FINALIZED_BLOCK_HASH"), (block) => map(boardWord(eventHistoryRoot, "EVENT_HISTORY_ROOT"), (history) => ({ ...state, finalizedJHeight, finalizedJBlockHash: block, eventHistoryRoot: history })));
  }));
/** og createCertifiedBoardProof: root-to-leaf path (a divergent leaf proves absence). */
export const boardProof = (nodes: BoardNodes, state: CertifiedBoardRegistryState, entityId: string): Result<CertifiedBoardProof, BoardRegistryError> => chain(boardWord(entityId, "ENTITY_ID"), (id) =>
  state.boardRegistryRoot === EMPTY_CERTIFIED_BOARD_ROOT ? ok({ version: 1 as const, stackKey: state.stackKey, entityId: id, nodes: [] })
    : chain(boardEntityKey(state.stackKey, entityId), (key) => map(walkToLeaf(nodes, state.boardRegistryRoot, key), (w) => ({ version: 1 as const, stackKey: state.stackKey, entityId: id, nodes: [...w.path.map((e) => e.node), w.leaf] }))));
/** og verifyCertifiedBoardProof. */
export const verifyBoardProof = (root: string, proof: CertifiedBoardProof): Result<CertifiedBoardRecord | null, BoardRegistryError> => {
  if (proof.version !== 1) return boardErr(`CERTIFIED_BOARD_PROOF_VERSION_INVALID:${String(proof.version)}`);
  return chain(boardWord(root, "ROOT"), (r) => chain(boardEntityKey(proof.stackKey, proof.entityId), (key): Result<CertifiedBoardRecord | null, BoardRegistryError> => {
    if (r === EMPTY_CERTIFIED_BOARD_ROOT) return proof.nodes.length !== 0 ? boardErr("CERTIFIED_BOARD_PROOF_TRAILING_NODES") : ok(null);
    if (proof.nodes.length < 1 || proof.nodes.length > 257) return boardErr("CERTIFIED_BOARD_PROOF_LENGTH_INVALID");
    let expected = r, previousBit = -1;
    for (const [i, node] of proof.nodes.entries()) {
      const h = hashBoardNode(node);
      if (!h.ok) return h;
      if (h.value !== expected) return boardErr(`CERTIFIED_BOARD_PROOF_LINK_INVALID:${i}`);
      if (node.type === "leaf") return i !== proof.nodes.length - 1 ? boardErr("CERTIFIED_BOARD_PROOF_TRAILING_NODES") : ok(node.key === key ? node.record : null);
      if (node.bit <= previousBit) return boardErr(`CERTIFIED_BOARD_BRANCH_ORDER_INVALID:${previousBit}:${node.bit}`);
      previousBit = node.bit;
      expected = keyBit(key, node.bit) === 0 ? node.left : node.right;
    }
    return boardErr("CERTIFIED_BOARD_PROOF_TERMINAL_LEAF_MISSING");
  }));
};
/** og collectReachableCertifiedBoardNodes: every node under the given roots (a missing or corrupt node refuses). */
export const reachableBoardNodes = (nodes: BoardNodes, roots: Iterable<string>): Result<BoardNodes, BoardRegistryError> => chain(traverse([...new Set(roots)], (r) => boardWord(r, "ROOT")), (rs) => {
  const reachable = new Map<string, CertifiedBoardNode>(), pending = [...new Set(rs)].filter((r) => r !== EMPTY_CERTIFIED_BOARD_ROOT);
  while (pending.length > 0) {
    const hash = pending.pop() as string;
    if (reachable.has(hash)) continue;
    const read = readBoardNode(nodes, hash);
    if (!read.ok) return read;
    reachable.set(hash, read.value);
    if (read.value.type === "branch") pending.push(read.value.left, read.value.right);
  }
  return ok(reachable);
});
/** The Entity's J stack as og config.jurisdiction; absent without a JurisdictionConfig (og: no jurisdiction). */
const entityBoardStack = (state: EntityState): BoardStack | undefined => (state.jurisdictionConfig === undefined ? undefined
  : { chainId: state.jurisdiction.chainId, depositoryAddress: state.jurisdiction.depositoryAddress, entityProviderAddress: state.jurisdictionConfig.entityProviderAddress, ...opt("entityProviderDeploymentBlock", state.jurisdictionConfig.entityProviderDeploymentBlock) });
/** og EntityState.certifiedBoardState (committed). */
export const entityBoardRegistry = (state: EntityState): CertifiedBoardRegistryState | undefined => state.committed["certifiedBoardState"] as CertifiedBoardRegistryState | undefined;
const registryInvariant = (r: Result<unknown, BoardRegistryError>): EntityError => ({ _tag: "entity_invariant", reason: r.ok ? "" : r.error.code });
const fromRegistry = <T>(r: Result<T, BoardRegistryError>): Result<T, EntityError> => (r.ok ? r : err(registryInvariant(r)));
/** og resolveObserverCertifiedBoardRecord: the record the observing Entity's own certified registry holds for `entityId`. */
export const observerBoardRecord = (state: EntityState, entityId: string): Result<CertifiedBoardRecord | null, EntityError> => {
  const j = entityBoardStack(state), registry = entityBoardRegistry(state);
  if (j === undefined || registry === undefined) return ok(null);
  return fromRegistry(chain(boardStackKey(j), (stackKey) => registry.stackKey !== stackKey ? boardErr(`CERTIFIED_BOARD_STACK_MISMATCH:${registry.stackKey}:${stackKey}`)
    : lookupBoardRecord(state.boardNodes ?? new Map(), registry.boardRegistryRoot, stackKey, entityId)));
};
/** og resolveSigningCertifiedBoardHash with one candidate state: the stack, its committed registry and a record for the Entity are all required. */
export const signingBoardHash = (state: EntityState, entityId: string): Result<string, EntityError> => fromRegistry(chain(boardWord(entityId, "ENTITY_ID"), (id) => {
  const j = entityBoardStack(state), registry = entityBoardRegistry(state);
  if (j === undefined) return boardErr(`CERTIFIED_BOARD_SIGNING_STACK_MISSING:${id}`);
  return chain(boardStackKey(j), (stackKey) => registry === undefined ? boardErr(`CERTIFIED_BOARD_SIGNING_ROOT_MISSING:${id}:${stackKey}`)
    : chain(lookupBoardRecord(state.boardNodes ?? new Map(), registry.boardRegistryRoot, stackKey, id), (record) => record === null ? boardErr(`CERTIFIED_BOARD_SIGNING_MEMBERSHIP_MISSING:${id}:${stackKey}`) : ok(record.boardHash)));
}));
/**
 * og resolveSigningCertifiedBoardHash over several candidate states (board-registry/index.ts): each must sit on the requested stack and hold a
 * record for the Entity, and all must agree on (stack, activation, epoch, board).
 */
const signingBoardOver = (states: readonly EntityState[], entityId: string, requested: string | undefined): Result<string, string> => {
  const id = boardWord(entityId, "ENTITY_ID");
  if (!id.ok) return err(id.error.code);
  const bindings: string[] = [];
  for (const state of states) {
    const j = entityBoardStack(state), registry = entityBoardRegistry(state);
    if (j === undefined) return err(`CERTIFIED_BOARD_SIGNING_STACK_MISSING:${id.value}`);
    const stackKey = boardStackKey(j);
    if (!stackKey.ok) return err(stackKey.error.code);
    if (requested !== undefined && requested !== stackKey.value) return err(`CERTIFIED_BOARD_SIGNING_STACK_MISMATCH:${id.value}`);
    if (registry === undefined) return err(`CERTIFIED_BOARD_SIGNING_ROOT_MISSING:${id.value}:${stackKey.value}`);
    const record = lookupBoardRecord(state.boardNodes ?? new Map(), registry.boardRegistryRoot, stackKey.value, id.value);
    if (!record.ok) return err(record.error.code);
    if (record.value === null) return err(`CERTIFIED_BOARD_SIGNING_MEMBERSHIP_MISSING:${id.value}:${stackKey.value}`);
    bindings.push(`${stackKey.value}:${record.value.activatedAtJHeight}:${record.value.logIndex}:${record.value.boardEpoch}:${record.value.boardHash}`);
  }
  const unique = [...new Set(bindings)];
  return unique.length !== 1 ? err(`CERTIFIED_BOARD_SIGNING_REPLICA_DIVERGENCE:${id.value}:${unique.sort().join(",")}`) : ok(unique[0]!.slice(unique[0]!.lastIndexOf(":") + 1));
};
/**
 * og resolveSettlementBoardAuthority's fallback over the source Entity's local replica states: none, or a lazy board (config board == id),
 * pins nothing; otherwise the certified board, which must be the configured one.
 */
export const settlementBoardAuthority = (states: readonly EntityState[], sourceEntityId: string): Result<string | undefined, string> => {
  const source = lower(sourceEntityId);
  if (states.length === 0) return ok(undefined);
  const configured = new Set(states.map((st) => lower(quorumBoardHash(st.quorum))));
  if (configured.size !== 1) return err(`SETTLEMENT_HANKO_LOCAL_BOARD_DIVERGENCE:${sourceEntityId}`);
  const board = [...configured][0]!;
  if (board === source) return ok(undefined);
  const first = entityBoardStack(states[0]!), requested = first === undefined ? undefined : boardStackKey(first);
  if (requested !== undefined && !requested.ok) return err(requested.error.code);
  return chain(signingBoardOver(states, sourceEntityId, requested?.value), (certified) => lower(certified) !== board
    ? err(`SETTLEMENT_HANKO_BOARD_AUTHORITY_MISMATCH:${sourceEntityId}:${certified}:${board}`) : ok(certified));
};
export type BoardJEventStep = { readonly state: EntityState; readonly events: readonly FrameEvent[] };
/**
 * og applyCertifiedBoardJEvent (entity/tx/j-events-board.ts) for one finalized board event: the committed registry advances, its new nodes join
 * the Entity's node store and the frame records og's status message; a BoardActivated then takes og's board Hanko refresh effects (boardActivation)
 * on the Entity's Accounts at the frame time `now`.
 */
export const applyBoardJEvent = (state: EntityState, event: JEvent, blockNumber: number, replicas: Replicas = new Map(), now: bigint = state.timestamp): Result<BoardJEventStep & { readonly accountReplicas: Replicas }, EntityError> =>
  chain(boardRegistryStep(state, event, blockNumber), (step) => (event.type === "BoardActivated" ? boardActivation(step, replicas, event, blockNumber, Number(now)) : ok({ ...step, accountReplicas: replicas })));
const boardRegistryStep = (state: EntityState, event: JEvent, blockNumber: number): Result<BoardJEventStep, EntityError> => {
  if (!isBoardEvent(event)) return invariant(`J_EVENT_BOARD_ROUTE_MISMATCH:${event.type}`);
  const j = entityBoardStack(state);
  if (j === undefined) return invariant("CERTIFIED_BOARD_ENTITY_JURISDICTION_MISSING");
  const nodes = state.boardNodes ?? new Map<string, CertifiedBoardNode>();
  return chain(fromRegistry(applyBoardRegistryEvent(entityBoardRegistry(state), nodes, j, event)), (applied): Result<BoardJEventStep, EntityError> => {
    const next: EntityState = { ...state, committed: { ...state.committed, certifiedBoardState: { ...applied.state } }, boardNodes: applied.newNodes.size === 0 ? nodes : new Map([...nodes, ...applied.newNodes]) };
    const events: readonly FrameEvent[] = [status(`🔐 BOARD AUTHORITY: ${event.type} | Block ${blockNumber}`)];
    const pending = epActionState(next).pending;
    if (event.type !== "BoardActivated" || lower(event.entityId) !== lower(state.id) || pending === undefined) return ok({ state: next, events });
    // og: a pending EntityProvider action signed under an older board epoch expires at the activation
    return chain(observerBoardRecord(next, next.id), (record): Result<BoardJEventStep, EntityError> => {
      if (record === null) return invariant(`ENTITY_PROVIDER_ACTION_CERTIFIED_BOARD_MISSING:${next.id}`);
      const epoch = BigInt(record.boardEpoch);
      if (pending.boardEpoch > epoch) return invariant(`ENTITY_PROVIDER_ACTION_PENDING_BOARD_EPOCH_AHEAD:${pending.boardEpoch}:${epoch}`);
      if (pending.boardEpoch === epoch) return ok({ state: next, events });
      const { pending: _expired, ...kept } = epActionState(next);
      return ok({ state: putEpActionState(next, kept), events: [...events, status("🛑 Pending EntityProvider action expired at board activation")] });
    });
  });
};

// ---- og entity/entity-provider-action.ts, entity/tx/handlers/entity-provider-action.ts, entity/tx/j-events-entity-provider-action.ts,
// ---- hanko/onchain-domain.ts (ENTITY_TRANSFER / RELEASE_CONTROL_SHARES / CANCEL_ENTITY_PROVIDER_ACTION payloads)
export type EntityProviderActionPayload =
  | { readonly kind: "entityTransferTokens"; readonly transfer: { readonly to: string; readonly tokenId: bigint; readonly amount: bigint } }
  | { readonly kind: "releaseControlShares"; readonly release: { readonly recipientAddress: string; readonly controlAmount: bigint; readonly dividendAmount: bigint; readonly purpose: string } }
  | { readonly kind: "cancelPendingAction"; readonly cancel: { readonly cancelledActionHash: string; readonly cancelledActionKind: 0 | 1 } };
/** og EntityProviderActionIntent: the board-signed action the leader submits to EntityProvider; `actionHash` is its Hanko payload hash. */
export type EntityProviderActionIntent = {
  readonly version: 1; readonly entityId: string; readonly entityNumber: bigint; readonly chainId: bigint; readonly entityProviderAddress: string; readonly boardEpoch: bigint;
  readonly actionNonce: bigint; readonly generation: number; readonly createdAt: number; readonly payload: EntityProviderActionPayload; readonly actionHash: string;
};
/** og EntityState.entityProviderActionState (committed): one action lane per Entity, `pending` until its J receipt or a board activation. */
export type EntityProviderActionState = { readonly version: 1; readonly confirmedNonce: bigint; readonly generation: number; readonly pending?: EntityProviderActionIntent | undefined };
const EP_ACTION_EMPTY: EntityProviderActionState = { version: 1, confirmedNonce: 0n, generation: 0 };
const epActionState = (state: EntityState): EntityProviderActionState => (state.committed["entityProviderActionState"] as EntityProviderActionState | undefined) ?? EP_ACTION_EMPTY;
const putEpActionState = (state: EntityState, s: EntityProviderActionState): EntityState => ({ ...state, committed: { ...state.committed, entityProviderActionState: s as unknown as Binary } });
const packedLabel = (label: string): Packed => ({ _tag: "bytes", value: bytesToHex(utf8(label)) });
const u256 = (value: bigint): Packed => ({ _tag: "uint256", value });
/** og recomputeEntityProviderActionHash: keccak(abi.encodePacked(label, chainId, entityProvider, entityNumber, boardEpoch, ...action, nonce)). */
export const entityProviderActionHash = (intent: Omit<EntityProviderActionIntent, "actionHash">): string => {
  const head = [u256(intent.chainId), { _tag: "address", value: intent.entityProviderAddress } as Packed, u256(intent.entityNumber), u256(intent.boardEpoch)];
  const p = intent.payload;
  const parts: readonly Packed[] = p.kind === "entityTransferTokens"
    ? [packedLabel("ENTITY_TRANSFER"), ...head, { _tag: "address", value: p.transfer.to }, u256(p.transfer.tokenId), u256(p.transfer.amount), u256(intent.actionNonce)]
    : p.kind === "releaseControlShares"
      ? [packedLabel("RELEASE_CONTROL_SHARES"), ...head, { _tag: "address", value: p.release.recipientAddress }, u256(p.release.controlAmount), u256(p.release.dividendAmount), { _tag: "bytes32", value: keccak256Hex(utf8(p.release.purpose)) }, u256(intent.actionNonce)]
      : [packedLabel("CANCEL_ENTITY_PROVIDER_ACTION"), ...head, u256(intent.actionNonce), { _tag: "bytes32", value: p.cancel.cancelledActionHash }, { _tag: "bytes", value: `0x${p.cancel.cancelledActionKind.toString(16).padStart(2, "0")}` }];
  return keccak256Hex(encodePacked(parts)).toLowerCase();
};
const EP_MAX_UINT = (1n << 256n) - 1n, EP_ZERO_ADDRESS = `0x${"00".repeat(20)}`;
const epFail = (reason: string): Result<never, EntityError> => invariant(reason);
const epAddress = (value: unknown, label: string): Result<string, EntityError> => {
  const raw = String(value ?? "").trim(), a = ethAddress(raw);
  return a === null || a === EP_ZERO_ADDRESS ? epFail(`ENTITY_PROVIDER_ACTION_${label}_INVALID:${raw || "missing"}`) : ok(a);
};
const epUint = (value: unknown, label: string, allowZero = true): Result<bigint, EntityError> =>
  typeof value !== "bigint" || value < 0n || (!allowZero && value === 0n) ? epFail(`ENTITY_PROVIDER_ACTION_${label}_INVALID:${String(value)}`) : ok(value);
type EpDomain = { readonly name: string; readonly chainId: bigint; readonly entityProviderAddress: string; readonly depositoryAddress: string };
/** og resolveActionDomain; the rewrite's Entity carries the J replica's name and stack in its own config (og re-reads them from the named J replica). */
const epDomain = (state: EntityState): Result<EpDomain, EntityError> => {
  const j = state.jurisdictionConfig, name = (j?.name ?? "").trim();
  if (j === undefined || name === "") return epFail("ENTITY_PROVIDER_ACTION_JURISDICTION_MISSING");
  const chainId = BigInt(state.jurisdiction.chainId);
  if (chainId <= 0n) return epFail(`ENTITY_PROVIDER_ACTION_CHAIN_ID_INVALID:${chainId}`);
  const provider = ethAddress(j.entityProviderAddress), depository = ethAddress(state.jurisdiction.depositoryAddress);
  if (provider === null || provider === EP_ZERO_ADDRESS) return epFail("INVALID_ENTITY_PROVIDER_ADDRESS");
  if (depository === null || depository === EP_ZERO_ADDRESS) return epFail("INVALID_DEPOSITORY_ADDRESS");
  return ok({ name, chainId, entityProviderAddress: provider, depositoryAddress: depository });
};
const epCurrent = (state: EntityState): Result<EntityProviderActionState, EntityError> => {
  const c = epActionState(state);
  return c.version !== 1 || typeof c.confirmedNonce !== "bigint" || c.confirmedNonce < 0n || c.confirmedNonce > EP_MAX_UINT || !Number.isSafeInteger(c.generation) || c.generation < 0
    ? epFail("ENTITY_PROVIDER_ACTION_STATE_INVALID") : ok(c);
};
const epEntityNumber = (entityId: string): Result<bigint, EntityError> => {
  if (!/^(?:0x[0-9a-f]+|[0-9]+)$/i.test(entityId.trim())) return epFail(`ENTITY_PROVIDER_ACTION_ENTITY_ID_INVALID:${entityId}`);
  const n = BigInt(entityId.trim());
  return n <= 0n || n > EP_MAX_UINT ? epFail(`ENTITY_PROVIDER_ACTION_ENTITY_NUMBER_INVALID:${entityId}`) : ok(n);
};
const epBoardEpoch = (state: EntityState): Result<bigint, EntityError> =>
  chain(observerBoardRecord(state, state.id), (record) => (record === null ? epFail(`ENTITY_PROVIDER_ACTION_BOARD_AUTHORITY_MISSING:${state.id}`) : ok(BigInt(record.boardEpoch))));
type EpActionTx = Extract<EntityTx, { readonly type: "entityProviderTransfer" | "entityProviderReleaseControlShares" }>;
const epPayload = (tx: EpActionTx): Result<EntityProviderActionPayload, EntityError> => {
  if (tx.type === "entityProviderTransfer") {
    return chain(epAddress(tx.data.to, "RECIPIENT"), (to) => chain(epUint(tx.data.tokenId, "TOKEN_ID"), (tokenId) => map(epUint(tx.data.amount, "AMOUNT", false), (amount): EntityProviderActionPayload => ({ kind: "entityTransferTokens", transfer: { to, tokenId, amount } }))));
  }
  const d = tx.data;
  return chain(epUint(d.controlAmount, "CONTROL_AMOUNT"), (controlAmount) => chain(epUint(d.dividendAmount, "DIVIDEND_AMOUNT"), (dividendAmount): Result<EntityProviderActionPayload, EntityError> => {
    if (controlAmount === 0n && dividendAmount === 0n) return epFail("ENTITY_PROVIDER_ACTION_RELEASE_AMOUNT_EMPTY");
    if (typeof d.purpose !== "string") return epFail("ENTITY_PROVIDER_ACTION_PURPOSE_INVALID:not-string");
    const size = utf8(d.purpose).byteLength;
    if (size > 1_024) return epFail(`ENTITY_PROVIDER_ACTION_PURPOSE_OVERSIZED:${size}:1024`);
    return map(epAddress(d.recipientAddress, "RECIPIENT"), (recipientAddress) => ({ kind: "releaseControlShares", release: { recipientAddress, controlAmount, dividendAmount, purpose: d.purpose } }));
  }));
};
/** The frame effects of one EntityProvider action tx: the pending intent, its J submission and its board-signed hash. */
const epIssue = (state: EntityState, replicas: Replicas, domain: EpDomain, current: EntityProviderActionState, intent: EntityProviderActionIntent, signer: string, message: string, context: string, timestamp: bigint): Draft => {
  const jTx: Binary = { type: intent.payload.kind === "entityTransferTokens" ? "entityProviderTransfer" : intent.payload.kind === "releaseControlShares" ? "entityProviderReleaseControlShares" : "entityProviderCancelAction", entityId: state.id, data: { intent, signerId: signer }, timestamp: Number(timestamp) } as unknown as Binary;
  return {
    state: putEpActionState(state, { version: 1, confirmedNonce: current.confirmedNonce, generation: intent.generation, pending: intent }), accountReplicas: replicas, outputs: [], events: [status(message)],
    jOutputs: [{ jurisdictionName: domain.name, jTxs: [jTx] }], hashes: [{ hash: intent.actionHash, type: "entityProviderAction", context }],
  };
};
/** og handleEntityProviderTransfer / handleEntityProviderReleaseControlShares (handleAction). */
const entityProviderAction = (state: EntityState, replicas: Replicas, tx: EpActionTx, timestamp: bigint): Result<Draft, EntityError> =>
  chain(epDomain(state), (domain) => chain(epCurrent(state), (current): Result<Draft, EntityError> => {
    if (current.pending !== undefined) return epFail(`ENTITY_PROVIDER_ACTION_PENDING:${current.pending.actionNonce}:${current.pending.actionHash}`);
    if (current.confirmedNonce === EP_MAX_UINT) return epFail("ENTITY_PROVIDER_ACTION_NONCE_EXHAUSTED");
    if (current.generation >= Number.MAX_SAFE_INTEGER) return epFail("ENTITY_PROVIDER_ACTION_GENERATION_EXHAUSTED");
    const signer = leaderStateOf(state).activeValidatorId;
    if (signer === "") return epFail("ENTITY_PROVIDER_ACTION_SUBMITTER_MISSING");
    return chain(epEntityNumber(state.id), (entityNumber) => chain(epBoardEpoch(state), (boardEpoch) => map(epPayload(tx), (payload) => {
      const unsigned = { version: 1 as const, entityId: lower(state.id), entityNumber, chainId: domain.chainId, entityProviderAddress: domain.entityProviderAddress, boardEpoch, actionNonce: current.confirmedNonce + 1n, generation: current.generation + 1, createdAt: Number(timestamp), payload };
      const intent: EntityProviderActionIntent = { ...unsigned, actionHash: entityProviderActionHash(unsigned) };
      return epIssue(state, replicas, domain, current, intent, signer, `📤 EntityProvider ${payload.kind} → hashesToSign [nonce=${intent.actionNonce}]`,
        `entityProviderAction:${state.id.slice(-4)}:${payload.kind}:nonce:${intent.actionNonce}`, timestamp);
    })));
  }));
/** og assertEntityProviderActionIntent over a stored intent: its envelope must still be this Entity's stack and epoch, and its hash must recompute. */
const epIntentValid = (intent: EntityProviderActionIntent, domain: EpDomain, entityId: string, boardEpoch: bigint): Result<void, EntityError> => {
  const envelope = intent.version === 1 && intent.entityNumber > 0n && lower(intent.entityId) === `0x${intent.entityNumber.toString(16).padStart(64, "0")}` && lower(intent.entityId) === lower(entityId)
    && intent.chainId === domain.chainId && lower(intent.entityProviderAddress) === domain.entityProviderAddress && intent.boardEpoch === boardEpoch && intent.actionNonce > 0n
    && Number.isSafeInteger(intent.generation) && intent.generation > 0 && Number.isSafeInteger(intent.createdAt) && intent.createdAt >= 0 && WORD32.test(lower(intent.actionHash));
  if (!envelope) return epFail("ENTITY_PROVIDER_ACTION_INTENT_INVALID");
  const recomputed = entityProviderActionHash(intent);
  return recomputed === lower(intent.actionHash) ? ok(undefined) : epFail(`ENTITY_PROVIDER_ACTION_HASH_MISMATCH:${intent.actionHash}:${recomputed}`);
};
/** og handleEntityProviderCancelAction: a cancel intent at the same nonce, naming the pending executable action. */
const entityProviderCancel = (state: EntityState, replicas: Replicas, tx: Extract<EntityTx, { readonly type: "entityProviderCancelAction" }>, timestamp: bigint): Result<Draft, EntityError> =>
  chain(epDomain(state), (domain) => chain(epCurrent(state), (current) => chain(epBoardEpoch(state), (boardEpoch): Result<Draft, EntityError> => {
    const pending = current.pending;
    if (pending === undefined) return epFail("ENTITY_PROVIDER_ACTION_CANCEL_PENDING_MISSING");
    return chain(epIntentValid(pending, domain, state.id, boardEpoch), (): Result<Draft, EntityError> => {
      if (pending.payload.kind === "cancelPendingAction") return epFail(`ENTITY_PROVIDER_ACTION_CANCEL_ALREADY_PENDING:${pending.actionHash}`);
      const requested = String(tx.data.actionHash ?? "").trim().toLowerCase();
      if (requested !== lower(pending.actionHash)) return epFail(`ENTITY_PROVIDER_ACTION_CANCEL_TARGET_MISMATCH:${requested || "missing"}:${pending.actionHash}`);
      if (pending.actionNonce !== current.confirmedNonce + 1n) return epFail(`ENTITY_PROVIDER_ACTION_PENDING_NONCE_CORRUPT:${pending.actionNonce}:${current.confirmedNonce + 1n}`);
      if (current.generation >= Number.MAX_SAFE_INTEGER) return epFail("ENTITY_PROVIDER_ACTION_GENERATION_EXHAUSTED");
      const signer = leaderStateOf(state).activeValidatorId;
      if (signer === "") return epFail("ENTITY_PROVIDER_ACTION_SUBMITTER_MISSING");
      return map(epEntityNumber(state.id), (entityNumber) => {
        const unsigned = {
          version: 1 as const, entityId: lower(state.id), entityNumber, chainId: domain.chainId, entityProviderAddress: domain.entityProviderAddress, boardEpoch, actionNonce: pending.actionNonce, generation: current.generation + 1, createdAt: Number(timestamp),
          payload: { kind: "cancelPendingAction" as const, cancel: { cancelledActionHash: lower(pending.actionHash), cancelledActionKind: pending.payload.kind === "entityTransferTokens" ? 0 as const : 1 as const } },
        };
        const intent: EntityProviderActionIntent = { ...unsigned, actionHash: entityProviderActionHash(unsigned) };
        return epIssue(state, replicas, domain, current, intent, signer, `🛑 EntityProvider cancel → hashesToSign [nonce=${intent.actionNonce}]`, `entityProviderAction:${state.id.slice(-4)}:cancel:nonce:${intent.actionNonce}`, timestamp);
      });
    });
  })));
/** og executableIdentity: the action a receipt must name (a cancel intent names the action it cancels). */
const epExecutable = (p: EntityProviderActionIntent): { readonly actionHash: string; readonly actionKind: 0 | 1 } => (p.payload.kind === "cancelPendingAction"
  ? { actionHash: lower(p.payload.cancel.cancelledActionHash), actionKind: p.payload.cancel.cancelledActionKind }
  : { actionHash: lower(p.actionHash), actionKind: p.payload.kind === "entityTransferTokens" ? 0 : 1 });
/**
 * og applyEntityProviderActionExecuted / applyEntityProviderActionCancelled: a finalized EntityProvider receipt confirms the next nonce and
 * clears the pending intent, which it must match when one is pending.
 */
export const applyEntityProviderActionJEvent = (state: EntityState, event: JEvent, blockNumber: number): Result<BoardJEventStep, EntityError> => {
  if (event.type !== "EntityProviderActionExecuted" && event.type !== "EntityProviderActionCancelled") return invariant(`J_EVENT_ENTITY_PROVIDER_ACTION_ROUTE_MISMATCH:${event.type}`);
  const executed = event.type === "EntityProviderActionExecuted", tag = executed ? "ACTION" : "CANCEL";
  const word = (v: unknown, code: string): Result<string, EntityError> => { const s = String(v ?? "").trim().toLowerCase(); return WORD32.test(s) ? ok(s) : epFail(`${code}:${s || "missing"}`); };
  return chain(word(event.entityId, `ENTITY_PROVIDER_${tag}_EVENT_ENTITY_INVALID`), (entity): Result<BoardJEventStep, EntityError> => {
    if (entity !== lower(state.id)) return epFail(`ENTITY_PROVIDER_${tag}_EVENT_ENTITY_MISMATCH:${entity}:${state.id}`);
    const kind = executed ? event.actionKind : event.cancelledActionKind;
    if (kind !== 0 && kind !== 1) return epFail(`ENTITY_PROVIDER_${tag}_EVENT_KIND_INVALID:${String(kind)}`);
    const nonce = event.actionNonce;
    const read = executed ? word(event.actionHash, "ENTITY_PROVIDER_ACTION_EVENT_HASH_INVALID")
      : (nonce < 1n || nonce > EP_MAX_UINT ? epFail(`ENTITY_PROVIDER_ACTION_EVENT_NONCE_INVALID:${nonce}`) : word(event.cancelledActionHash, "ENTITY_PROVIDER_CANCEL_EVENT_ACTION_HASH_INVALID"));
    return chain(read, (hash) => chain(executed ? ok("") : word(event.cancelHash, "ENTITY_PROVIDER_CANCEL_EVENT_HASH_INVALID"), (cancelHash): Result<BoardJEventStep, EntityError> => {
      if (nonce < 1n || nonce > EP_MAX_UINT) return epFail(`ENTITY_PROVIDER_ACTION_EVENT_NONCE_INVALID:${nonce}`);
      const current = epActionState(state);
      if (current.version !== 1 || current.confirmedNonce < 0n || current.confirmedNonce >= EP_MAX_UINT || !Number.isSafeInteger(current.generation) || current.generation < 0) return epFail("ENTITY_PROVIDER_ACTION_STATE_CORRUPT");
      const expected = current.confirmedNonce + 1n;
      if (nonce !== expected) return epFail(`ENTITY_PROVIDER_${tag}_EVENT_NONCE_MISMATCH:${nonce}:${expected}`);
      const pending = current.pending;
      if (pending !== undefined) {
        const x = epExecutable(pending), cancelMatches = executed || pending.payload.kind !== "cancelPendingAction" || lower(pending.actionHash) === cancelHash;
        if (pending.actionNonce !== nonce || x.actionHash !== hash || x.actionKind !== kind || !cancelMatches) {
          return epFail(executed ? `ENTITY_PROVIDER_ACTION_RECEIPT_MISMATCH:expected=${pending.actionNonce}:${x.actionHash}:${x.actionKind}:received=${nonce}:${hash}:${kind}`
            : `ENTITY_PROVIDER_CANCEL_RECEIPT_MISMATCH:expected=${pending.actionNonce}:${x.actionHash}:${x.actionKind}:${pending.payload.kind === "cancelPendingAction" ? pending.actionHash : "external-cancel"}:received=${nonce}:${hash}:${kind}:${cancelHash}`);
        }
      }
      return ok({
        state: putEpActionState(state, { version: 1, confirmedNonce: nonce, generation: current.generation }),
        events: [status(executed ? `✅ EntityProvider action finalized (nonce ${nonce}) | Block ${blockNumber}` : `🛑 EntityProvider action cancelled (nonce ${nonce}) | Block ${blockNumber}`)],
      });
    }));
  });
};
// ---- og entity/tx/handlers/control-board-proposal.ts + hanko/onchain-domain.ts encodeBoardProposalHankoPayload
const BOARD_PROPOSAL_DOMAIN = keccak256Hex(utf8("XLN_ENTITY_PROVIDER_BOARD_PROPOSAL_V1"));
/** og hashBoardProposalHankoPayload: keccak(abi.encode(domain, chainId, entityProvider, entityId, boardEpoch, newBoardHash, authority, actionNonce)). */
export const boardProposalHash = (i: { readonly chainId: bigint; readonly entityProviderAddress: string; readonly boardEpoch: bigint; readonly entityId: string; readonly newBoardHash: string; readonly authority: number; readonly actionNonce: bigint }): string =>
  keccak256Hex(abiEncode([A.b32(BOARD_PROPOSAL_DOMAIN), A.uint(i.chainId), A.address(i.entityProviderAddress), A.b32(i.entityId), A.uint(i.boardEpoch), A.b32(i.newBoardHash), A.uint(BigInt(i.authority)), A.uint(i.actionNonce)])).toLowerCase();
/** og toEntityId (protocol/identity): a 32-byte hex id, lowercased by the caller. */
const cbEntityId = (value: unknown): Result<string, EntityError> =>
  typeof value === "string" && /^0x[a-fA-F0-9]{64}$/.test(value) ? ok(value.toLowerCase()) : epFail(`FINTECH-SAFETY: Invalid EntityId format: ${String(value)}`);
const cbRecord = (state: EntityState, entityId: string, missing: string): Result<CertifiedBoardRecord, EntityError> =>
  chain(observerBoardRecord(state, entityId), (record) => (record === null ? epFail(`${missing}:${entityId}`) : ok(record)));
type ControlProposalTx = Extract<EntityTx, { readonly type: "entityProviderProposeControlBoard" }>;
/**
 * og handleEntityProviderProposeControlBoard: this Entity, a CONTROL shareholder of `targetEntityId`, signs a board proposal (with the verified
 * written consents of other shareholders) for EntityProvider; the proposal hash is a frame-signed hash and the proposal a J output.
 */
const proposeControlBoard = (state: EntityState, replicas: Replicas, tx: ControlProposalTx, ctx: FoldContext): Result<Draft, EntityError> => {
  const j = state.jurisdictionConfig, name = (j?.name ?? "").trim();
  if (j === undefined || name === "") return epFail("CONTROL_BOARD_PROPOSAL_JURISDICTION_MISSING");
  const chainId = BigInt(state.jurisdiction.chainId);
  if (chainId <= 0n) return epFail(`CONTROL_BOARD_PROPOSAL_CHAIN_ID_INVALID:${chainId}`);
  const provider = ethAddress(j.entityProviderAddress);
  if (provider === null || provider === EP_ZERO_ADDRESS) return epFail("INVALID_ENTITY_PROVIDER_ADDRESS");
  const d = tx.data;
  return chain(cbEntityId(d.targetEntityId), (target) => chain(cbEntityId(state.id), (shareholder): Result<Draft, EntityError> => {
    if (typeof d.newBoardHash !== "string" || !/^0x[0-9a-fA-F]{64}$/.test(d.newBoardHash)) return epFail(`PROTOCOL_BOARD_HASH_INVALID:${String(d.newBoardHash)}`);
    const newBoardHash = d.newBoardHash.toLowerCase(), nonce = d.actionNonce;
    if (typeof nonce !== "bigint" || nonce <= 0n || nonce > EP_MAX_UINT) return epFail(`CONTROL_BOARD_PROPOSAL_NONCE_INVALID:${String(nonce)}`);
    return chain(cbRecord(state, target, "CONTROL_BOARD_PROPOSAL_TARGET_AUTHORITY_MISSING"), (targetBoard) => chain(cbRecord(state, shareholder, "CONTROL_BOARD_PROPOSAL_SHAREHOLDER_AUTHORITY_MISSING"), (): Result<Draft, EntityError> => {
      const proposalHash = boardProposalHash({ chainId, entityProviderAddress: provider, boardEpoch: BigInt(targetBoard.boardEpoch), entityId: target, newBoardHash, authority: 1, actionNonce: nonce });
      const supplied = d.supporterVotes ?? [];
      if (supplied.length >= 256) return epFail(`CONTROL_BOARD_PROPOSAL_SUPPORTERS_OVERSIZED:${supplied.length}`);
      // og verifyControlSupporterVotes (8 Del. C. § 228 written consent): each supporter Hanko is checked against its certified current board
      const votes = foldResult<readonly { readonly entityId: string; readonly hankoSignature?: string }[], { readonly entityId: string; readonly hankoSignature: string }, EntityError>(supplied, [], (acc, vote) =>
        chain(cbEntityId(vote.entityId), (supporter) => {
          if (supporter === shareholder || acc.some((v) => v.entityId === supporter)) return epFail(`CONTROL_BOARD_PROPOSAL_SUPPORTER_DUPLICATE:${supporter}`);
          return chain(cbRecord(state, supporter, "CONTROL_BOARD_PROPOSAL_SUPPORTER_AUTHORITY_MISSING"), (record) =>
            ctx.verify(proposalHash, vote.hankoSignature, supporter as EntityId, { allowPreviousBoard: false, registeredBoardHash: record.boardHash })
              ? ok([...acc, { entityId: supporter, hankoSignature: vote.hankoSignature }]) : epFail(`CONTROL_BOARD_PROPOSAL_SUPPORTER_HANKO_INVALID:${supporter}`));
        }));
      return chain(votes, (verified): Result<Draft, EntityError> => {
        const signer = leaderStateOf(state).activeValidatorId;
        if (signer === "") return epFail("CONTROL_BOARD_PROPOSAL_SUBMITTER_MISSING");
        const supporterVotes = [...verified, { entityId: shareholder }].sort((a, b) => asc(a.entityId, b.entityId));
        const jTx = { type: "entityProviderProposeControlBoard", entityId: shareholder, data: { targetEntityId: target, newBoardHash, boardEpoch: BigInt(targetBoard.boardEpoch), actionNonce: nonce, proposalHash, supporterVotes, signerId: signer }, timestamp: Number(ctx.timestamp) } as unknown as Binary;
        return ok({
          state, accountReplicas: replicas, outputs: [], events: [status(`🗳️ CONTROL vote ${shareholder.slice(-4)} → ${target.slice(-4)}`)],
          jOutputs: [{ jurisdictionName: name, jTxs: [jTx] }], hashes: [{ hash: proposalHash, type: "entityProviderAction", context: `controlBoard:${target.slice(-4)}:nonce:${nonce}` }],
        });
      });
    }));
  }));
};
/** og handleEntityProviderActivateBoard: the permissionless activation call for a certified target once its proposal delay has passed. */
const activateBoard = (state: EntityState, replicas: Replicas, tx: Extract<EntityTx, { readonly type: "entityProviderActivateBoard" }>, timestamp: bigint): Result<Draft, EntityError> => {
  const name = (state.jurisdictionConfig?.name ?? "").trim();
  if (name === "") return epFail("CONTROL_BOARD_ACTIVATION_JURISDICTION_MISSING");
  return chain(cbEntityId(tx.data.targetEntityId), (target) => chain(cbRecord(state, target, "CONTROL_BOARD_ACTIVATION_TARGET_MISSING"), (): Result<Draft, EntityError> => {
    const signer = leaderStateOf(state).activeValidatorId;
    if (signer === "") return epFail("CONTROL_BOARD_ACTIVATION_SUBMITTER_MISSING");
    const jTx = { type: "entityProviderActivateBoard", entityId: state.id, data: { targetEntityId: target, signerId: signer }, timestamp: Number(timestamp) } as unknown as Binary;
    return ok({ state, accountReplicas: replicas, outputs: [], events: [status(`🔐 Activate board ${target.slice(-4)}`)], jOutputs: [{ jurisdictionName: name, jTxs: [jTx] }] });
  }));
};
/** og canonicalBoardHash: the quorum's board (validator addresses, shares, the board's delays). */
export const quorumBoardHash = (q: Authority): string => match(q, { teaching: () => configBoardHash(q), board: ({ board }) => boardHashOf(board).toLowerCase() });
/** og encodeQuorumEntityId: the Entity id as a 32-byte word. */
const quorumEntityWord = (entityId: string): Result<string, EntityError> => {
  const s = entityId.trim();
  if (!/^(?:0x[0-9a-f]+|[0-9]+)$/i.test(s) || BigInt(s) >= 1n << 256n) return invariant(`BUILD_QUORUM_HANKO_INVALID_ENTITY_ID: entityId=${entityId}`);
  return ok(`0x${BigInt(s).toString(16).padStart(64, "0")}`);
};
/**
 * og assertQuorumBoardBinding / assertEntityConfigBoardAuthority: the committed board must be the Entity's lazy id (id == board hash) or,
 * for any other Entity, the board its own certified registry holds. Proposers and validators run it before signing any manifest.
 */
export const assertBoardAuthority = (state: EntityState): Result<void, EntityError> => chain(quorumEntityWord(state.id), (word): Result<void, EntityError> => {
  const supplied = quorumBoardHash(state.quorum);
  if (supplied === word) return ok(undefined);
  if (state.jurisdictionConfig === undefined) return invariant(`BUILD_QUORUM_HANKO_BOARD_UNAVAILABLE: entityId=${state.id}`);
  return chain(signingBoardHash(state, state.id), (authoritative) => supplied === authoritative ? ok(undefined)
    : invariant(`BUILD_QUORUM_HANKO_BOARD_MISMATCH: entityId=${state.id} authoritative=${authoritative} supplied=${supplied}`));
});
// ---- og entity/command (command-codec.ts, index.ts), auth/authorization.ts, tx/processing/proposals.ts, system/basic.ts propose/vote ----
/** og applyEntityTxsInOrder authorization lanes: top-level frame txs, a signed command's individual txs, an approved proposal's collective txs. */
type TxLane = "top" | "command" | "collective" | "runtime";
/** A tx that changes nothing but the Entity state: no outputs, no events, no Account touched. */
const idle = (state: EntityState, replicas: Replicas): Draft => ({ state, accountReplicas: replicas, outputs: [], touched: [] });
const status = (message: string): FrameEvent => ({ type: "status", message });
const lendingMessage = (tx: LendingEntityTx): string => {
  switch (tx.type) {
    case "lendingOffer": return `Lending pool funding requested: ${tx.data.amount} token=${Number(tx.data.tokenId)}`;
    case "lendingBorrow": return `Loan requested: ${tx.data.amount} token=${Number(tx.data.tokenId)}`;
    case "lendingRepay": return `Loan repayment requested: ${tx.data.loanId}`;
    case "lendingClosePosition": return `Lending position close requested: ${tx.data.positionId}`;
  }
};
const invariant = (reason: string): Result<never, EntityError> => err({ _tag: "entity_invariant", reason });
const rejectCommand = (reason: string): Result<never, EntityError> => err({ _tag: "entity_command", reason });
/** og isEntityProtocolTx / isIndividualEntityCommandTx / isCollectiveEntityActionTx over the rewrite's tx types. */
const PROTOCOL_TXS: ReadonlySet<string> = new Set(["boardHandover", "entityCommand", "runtimeOutput", "scheduledWake", "proposeAccountsNow", "j_event", "accountInput"]);
const INDIVIDUAL_TXS: ReadonlySet<string> = new Set(["chat", "materializeCrossJurisdictionClear", "materializeCrossJurisdictionSwap", "propose", "vote"]);
const isProtocolTx = (tx: EntityTx): boolean => PROTOCOL_TXS.has(tx.type);
const isIndividualTx = (tx: EntityTx): boolean => INDIVIDUAL_TXS.has(tx.type);
const isCollectiveTx = (tx: EntityTx): boolean => !isProtocolTx(tx) && !isIndividualTx(tx);
/**
 * og assertEntityTxAuthorization. A signed command applies only individual txs and an approved proposal only collective ones. og refuses every
 * other top-level tx with ENTITY_COMMAND_REQUIRED; the rewrite's top-level `txs` lane stays the trusted local lane for the txs it already carried,
 * and refuses the governance txs og only accepts inside a command.
 */
const laneRefusal = (tx: EntityTx, lane: TxLane): EntityError | undefined => {
  if (isProtocolTx(tx)) return undefined;
  if (tx.type === "crossJurisdictionSalvage" && lane !== "runtime") return { _tag: "entity_invariant", reason: "CROSS_J_SALVAGE_RUNTIME_OUTPUT_REQUIRED" };
  if (lane === "command" && !isIndividualTx(tx)) return { _tag: "entity_invariant", reason: `ENTITY_COMMAND_COLLECTIVE_ACTION_REQUIRES_PROPOSAL:${tx.type}` };
  if (lane === "collective" && !isCollectiveTx(tx)) return { _tag: "entity_invariant", reason: `ENTITY_COLLECTIVE_ACTION_TX_FORBIDDEN:${tx.type}` };
  if (lane === "top" && (tx.type === "propose" || tx.type === "vote")) return { _tag: "entity_invariant", reason: `ENTITY_COMMAND_REQUIRED:${tx.type}` };
  return undefined;
};
/** og plain Errors (openAccount, lending, governance invariants) refuse the whole input; a reject disposition evicts only the outermost tx. */
const fatalTx = (tx: EntityTx, e: EntityError): boolean => tx.type === "openAccount" || e._tag === "lending_entity" || e._tag === "swap_request_account_missing" || e._tag === "entity_invariant" || accountThrew(e);
/** og's thrown Account handler Error or critical proposal failure (proposal/transactions.ts throwCriticalProposalFailure): it aborts the whole Entity input. */
const accountThrew = (e: EntityError): boolean => e._tag === "account_tx_thrown" || e._tag === "proposal_halt" || (e._tag === "rejected_after_ack" && accountThrew(e.cause));
/** The accumulated tx hashesToSign and jOutputs of two drafts (absent while empty). */
const frameEffects = (a: Draft, b: Draft): Pick<Draft, "hashes" | "jOutputs"> => {
  const hashes = [...(a.hashes ?? []), ...(b.hashes ?? [])], jOutputs = [...(a.jOutputs ?? []), ...(b.jOutputs ?? [])];
  return { ...(hashes.length === 0 ? {} : { hashes }), ...(jOutputs.length === 0 ? {} : { jOutputs }) };
};
/** og applyEntityTxsInOrder for a nested lane: every tx in order, atomically; a fatal child makes the whole outer tx fatal. */
const foldNested = (state: EntityState, replicas: Replicas, txs: readonly EntityTx[], ctx: FoldContext, lane: TxLane): Result<Draft, EntityError> =>
  foldResult<Draft, EntityTx, EntityError>(txs, { state, accountReplicas: replicas, outputs: [], events: [] }, (acc, tx) => {
    const r = foldTx(acc.state, acc.accountReplicas, tx, ctx, lane);
    if (!r.ok) return fatalTx(tx, r.error) && r.error._tag !== "entity_invariant" ? invariant(`${tx.type}:${r.error._tag}`) : r;
    return ok({ ...r.value, outputs: [...acc.outputs, ...r.value.outputs], events: [...(acc.events ?? []), ...(r.value.events ?? [])], runtimeEvents: [...(acc.runtimeEvents ?? []), ...(r.value.runtimeEvents ?? [])], touched: [...(acc.touched ?? []), ...(r.value.touched ?? [peerOf(tx, state.id)])], ...frameEffects(acc, r.value), swaps: joinSwapEvents(acc.swaps, r.value.swaps) });
  });
const WORD32 = /^0x[0-9a-f]{64}$/, EOA = /^0x[0-9a-f]{40}$/;
const COMMAND_DOMAIN = "xln:entity-command:binary", PROPOSAL_ACTION_DOMAIN = "xln:entity-proposal-action:v1";
const MAX_PENDING_PROPOSALS = 100;
/** og UNREGISTERED_ENTITY_COMMAND_STACK_KEY = ethers.id('xln:entity-command:unregistered-stack:v1'). */
const UNREGISTERED_STACK_KEY = keccak256Hex(utf8("xln:entity-command:unregistered-stack:v1"));
/** og getCertifiedBoardStackKey: keccak(abi.encode(STACK_DOMAIN, chainId, depository, entityProvider)). */
export const certifiedBoardStackKey = (j: { readonly chainId: number; readonly depositoryAddress: string; readonly entityProviderAddress: string }): string =>
  keccak256Hex(abiEncode([A.b32(keccak256Hex(utf8("xln.certified-board.stack.v1"))), A.uint(BigInt(j.chainId)), A.address(j.depositoryAddress.toLowerCase()), A.address(j.entityProviderAddress.toLowerCase())]));
const commandStackKey = (state: EntityState): string => (state.jurisdictionConfig === undefined ? UNREGISTERED_STACK_KEY
  : certifiedBoardStackKey({ chainId: state.jurisdiction.chainId, depositoryAddress: state.jurisdiction.depositoryAddress, entityProviderAddress: state.jurisdictionConfig.entityProviderAddress }));
type BoardMember = { readonly signerId: string; readonly signer: string; readonly share: bigint };
type CommandBoard = { readonly boardHash: string; readonly boardEpoch: number; readonly members: readonly BoardMember[] };
/** og hashBoard(encodeBoard(config)): the config's positional validators as zero-padded EOAs, uint16 powers, no delays. */
export const configBoardHash = (q: Authority): string => {
  const members = [...membersOf(q)];
  return boardHashOf({ votingThreshold: Number(thresholdOf(q)), entityIds: members.map(([a]) => (/^0x[0-9a-f]{64}$/i.test(a) ? lower(a) : addressAsId(a))), votingPowers: members.map(([, m]) => Number(m.shares)), boardChangeDelay: 0, controlChangeDelay: 0, dividendChangeDelay: 0 }).toLowerCase();
};
/** og resolveEntityCommandBoard: a lazy Entity (id == its config board hash) is at epoch 0; any other Entity takes the epoch of its certified board record, which must be its config board. */
const commandBoard = (state: EntityState): Result<CommandBoard, EntityError> => {
  const members = [...membersOf(state.quorum)].map(([a, m]): BoardMember => ({ signerId: signerId(a), signer: signerId(a), share: m.shares }));
  const boardHash = configBoardHash(state.quorum), entity = signerId(state.id);
  if (entity === boardHash) return ok({ boardHash, boardEpoch: 0, members });
  return chain(observerBoardRecord(state, entity), (record): Result<CommandBoard, EntityError> => record === null ? invariant(`ENTITY_COMMAND_CERTIFIED_BOARD_REQUIRED:${entity}`)
    : record.boardHash !== boardHash ? invariant(`ENTITY_COMMAND_CERTIFIED_BOARD_CONFIG_MISMATCH:${record.boardHash}:${boardHash}`) : ok({ boardHash, boardEpoch: record.boardEpoch, members }));
};
const consensusHash = (value: unknown): Result<string, EntityError> => chain(binaryBody(value), (b) => map(encodeConsensus(b), (bytes) => bytesToHex(keccak_256(bytes))));
/** og hashEntityCommandTxs. */
export const hashCommandTxs = (txs: readonly EntityTx[]): Result<string, EntityError> => consensusHash({ version: COMMAND_DOMAIN, txs: txs.map(wireEntityTx) });
type CommandBody = Omit<EntityCommand, "signature">;
/** og hashEntityCommand over the normalized body (txs enter through txsHash only). */
export const hashCommand = (c: CommandBody): Result<string, EntityError> => consensusHash({
  domain: COMMAND_DOMAIN, version: c.version, entityId: c.entityId, stackKey: c.stackKey, boardHash: c.boardHash, boardEpoch: c.boardEpoch,
  authorSignerId: c.authorSignerId, authorSigner: c.authorSigner, nonce: c.nonce, txsHash: c.txsHash,
});
/** og hashCollectiveEntityActionTxs. */
const hashCollectiveTxs = (txs: readonly EntityTx[]): Result<string, EntityError> => {
  if (txs.length === 0 || txs.length > ENTITY_MEMPOOL_SIZE) return invariant(`ENTITY_COLLECTIVE_ACTION_TX_COUNT_INVALID:${txs.length}`);
  const forbidden = txs.find((tx) => !isCollectiveTx(tx));
  return forbidden !== undefined ? invariant(`ENTITY_COLLECTIVE_ACTION_TX_FORBIDDEN:${forbidden.type}`) : consensusHash({ domain: PROPOSAL_ACTION_DOMAIN, version: 1, txs: txs.map(wireEntityTx) });
};
/** og buildEntityTransactionProposalAction. */
export const entityTransactionAction = (txs: readonly EntityTx[]): Result<ProposalAction, EntityError> => map(hashCollectiveTxs(txs), (actionHash) => ({ type: "entity_transaction", data: { version: 1, actionHash, txs } }));
/** og assertEntityProposalAction: the recomputed collective action hash must equal the carried one. */
const checkAction = (a: ProposalAction): Result<ProposalAction, EntityError> => {
  if (a.type === "collective_message") return typeof a.data.message === "string" ? ok({ type: a.type, data: { message: a.data.message } }) : invariant("ENTITY_PROPOSAL_MESSAGE_DATA_INVALID");
  if (a.type !== "entity_transaction") return invariant(`ENTITY_PROPOSAL_ACTION_TYPE_INVALID:${String((a as { type?: unknown }).type)}`);
  if (a.data.version !== 1) return invariant("ENTITY_PROPOSAL_TRANSACTION_DATA_INVALID");
  const carried = lower(a.data.actionHash);
  return chain(hashCollectiveTxs(a.data.txs), (computed) => (carried !== computed ? invariant(`ENTITY_PROPOSAL_ACTION_HASH_MISMATCH:${carried || "missing"}:${computed}`) : ok({ type: a.type, data: { version: 1, actionHash: carried, txs: a.data.txs } })));
};
/** og hashEntityProposalAction. */
export const hashProposalAction = (a: ProposalAction): Result<string, EntityError> => chain(checkAction(a), (checked) => consensusHash({ domain: PROPOSAL_ACTION_DOMAIN, action: wireAction(checked) }));
type NonceSlot = { readonly nonce: bigint; readonly commandHash: string };
/** og canonicalCommandNonceState: the committed fence for the current board, empty after a board rotation. */
const nonceSlots = (state: EntityState, board: CommandBoard): ReadonlyMap<string, NonceSlot> => {
  const stored = state.committed["entityCommandNonces"] as { readonly boardHash?: string; readonly boardEpoch?: number; readonly bySigner?: ReadonlyMap<string, NonceSlot> } | undefined;
  return stored?.bySigner !== undefined && stored.boardHash === board.boardHash && stored.boardEpoch === board.boardEpoch ? stored.bySigner : new Map();
};
const nonceBinary = (board: CommandBoard, slots: ReadonlyMap<string, NonceSlot>): Binary =>
  ({ version: 1, boardHash: board.boardHash, boardEpoch: board.boardEpoch, bySigner: new Map([...slots].map(([k, v]) => [k, { nonce: v.nonce, commandHash: v.commandHash }])) });
type Disposition = "next" | "retry" | "cancel";
/** og getEntityCommandDisposition: nonces only grow; an exact byte retry is idempotent, an older or rewritten slot is cancelled, a gap is refused. */
const disposition = (slots: ReadonlyMap<string, NonceSlot>, c: EntityCommand, commandHash: string): Result<Disposition, EntityError> => {
  const latest = slots.get(c.authorSignerId);
  if (latest === undefined) return c.nonce !== 1n ? rejectCommand(`ENTITY_COMMAND_NONCE_MISMATCH:${c.nonce}:1`) : ok("next");
  if (c.nonce === latest.nonce) return ok(commandHash === latest.commandHash ? "retry" : "cancel");
  if (c.nonce < latest.nonce) return ok("cancel");
  return c.nonce !== latest.nonce + 1n ? rejectCommand(`ENTITY_COMMAND_NONCE_MISMATCH:${c.nonce}:${latest.nonce + 1n}`) : ok("next");
};
/** og isCanonicalCompactSignatureHex: 0x + r,s + recovery 0/1, low-s, nonzero. */
const canonicalCompactSig = (sig: string): boolean => {
  if (!/^0x[0-9a-f]{130}$/.test(sig)) return false;
  const bytes = hexToBytes(sig), r = wordAt(bytes, 0), sv = wordAt(bytes, 32), rec = bytes[64];
  return (rec === 0 || rec === 1) && r !== 0n && sv !== 0n && r < secp256k1.CURVE.n && sv <= HALF_ORDER;
};
/** og assertEntityCommandTxs + normalizeEntityCommandBody + normalizeSignedEntityCommand. */
const normalizeCommand = (c: EntityCommand): Result<EntityCommand, EntityError> => {
  const bad = (code: string, v: unknown): Result<never, EntityError> => invariant(`${code}:${lower(v) || "missing"}`);
  const commandTxs = (): Result<void, EntityError> => {
    if (!Array.isArray(c.txs) || c.txs.length === 0 || c.txs.length > ENTITY_MEMPOOL_SIZE) return invariant(`ENTITY_COMMAND_TX_COUNT_INVALID:${Array.isArray(c.txs) ? c.txs.length : "not-array"}`);
    const protocol = c.txs.find(isProtocolTx);
    return protocol !== undefined ? invariant(`ENTITY_COMMAND_PROTOCOL_TX_FORBIDDEN:${protocol.type}`) : ok(undefined);
  };
  return chain(commandTxs(), () => {
    if (c.version !== 1) return invariant(`ENTITY_COMMAND_VERSION_INVALID:${String(c.version)}`);
    if (typeof c.nonce !== "bigint" || c.nonce < 1n) return invariant(`ENTITY_COMMAND_NONCE_INVALID:${String(c.nonce)}`);
    const entityId = lower(c.entityId), stackKey = lower(c.stackKey), boardHash = lower(c.boardHash), authorSignerId = lower(c.authorSignerId), authorSigner = lower(c.authorSigner), txsHash = lower(c.txsHash);
    if (!WORD32.test(entityId)) return bad("ENTITY_COMMAND_ENTITY_ID_INVALID", c.entityId);
    if (!WORD32.test(stackKey)) return bad("ENTITY_COMMAND_STACK_KEY_INVALID", c.stackKey);
    if (!WORD32.test(boardHash)) return bad("ENTITY_COMMAND_BOARD_HASH_INVALID", c.boardHash);
    if (typeof c.boardEpoch !== "number" || !Number.isSafeInteger(c.boardEpoch) || c.boardEpoch < 0) return invariant(`ENTITY_COMMAND_BOARD_EPOCH_INVALID:${String(c.boardEpoch)}`);
    if (authorSignerId === "") return invariant("ENTITY_COMMAND_AUTHOR_SIGNER_ID_REQUIRED");
    if (!EOA.test(authorSigner)) return bad("ENTITY_COMMAND_AUTHOR_SIGNER_INVALID", c.authorSigner);
    if (!WORD32.test(txsHash)) return bad("ENTITY_COMMAND_TXS_HASH_INVALID", c.txsHash);
    return chain(hashCommandTxs(c.txs), (computed): Result<EntityCommand, EntityError> => {
      if (txsHash !== computed) return invariant(`ENTITY_COMMAND_TXS_HASH_MISMATCH:${txsHash}:${computed}`);
      const signature = lower(c.signature);
      return canonicalCompactSig(signature) ? ok({ version: 1, entityId, stackKey, boardHash, boardEpoch: c.boardEpoch, authorSignerId, authorSigner, nonce: c.nonce, txsHash, txs: c.txs, signature }) : rejectCommand("ENTITY_COMMAND_SIGNATURE_INVALID");
    });
  });
};
/** og assertEntityCommandAuthorBindings: fields that claim an individual board identity must equal the author. */
const authorBindings = (author: string, txs: readonly EntityTx[]): Result<void, EntityError> => {
  for (const tx of txs) {
    const [field, claimed] = tx.type === "chat" ? ["chat.from", tx.data.from] : tx.type === "propose" ? ["propose.proposer", tx.data.proposer] : tx.type === "vote" ? ["vote.voter", tx.data.voter] : [undefined, undefined];
    if (field !== undefined && lower(claimed) !== author) return rejectCommand(`ENTITY_COMMAND_AUTHOR_FIELD_MISMATCH:${field}:${lower(claimed) || "missing"}:${author}`);
  }
  return ok(undefined);
};
/** og assertIndividualEntityCommandTxs. */
const individualOnly = (txs: readonly EntityTx[]): Result<void, EntityError> => foldResult(txs, undefined as void, (_, tx): Result<void, EntityError> =>
  !isIndividualTx(tx) ? rejectCommand(`ENTITY_COMMAND_COLLECTIVE_ACTION_REQUIRES_PROPOSAL:${tx.type}`) : tx.type === "propose" ? map(checkAction(tx.data.action), () => undefined) : ok(undefined));
type CheckedCommand = { readonly command: EntityCommand; readonly board: CommandBoard; readonly commandHash: string; readonly disposition: Disposition };
/** og assertSignedEntityCommand: entity, stack, board and epoch, author and its EOA, author bindings, individual txs, signature, nonce disposition. */
export const checkCommand = (state: EntityState, raw: EntityCommand): Result<CheckedCommand, EntityError> => chain(normalizeCommand(raw), (c) => {
  const entityId = lower(state.id), stackKey = commandStackKey(state);
  if (c.entityId !== entityId) return rejectCommand(`ENTITY_COMMAND_ENTITY_MISMATCH:${c.entityId}:${entityId}`);
  if (c.stackKey !== stackKey) return rejectCommand(`ENTITY_COMMAND_STACK_MISMATCH:${c.stackKey}:${stackKey}`);
  return chain(commandBoard(state), (board) => {
    if (c.boardHash !== board.boardHash) return rejectCommand(`ENTITY_COMMAND_BOARD_MISMATCH:${c.boardHash}:${board.boardHash}`);
    if (c.boardEpoch !== board.boardEpoch) return rejectCommand(`ENTITY_COMMAND_EPOCH_MISMATCH:${c.boardEpoch}:${board.boardEpoch}`);
    const author = board.members.find((m) => m.signerId === c.authorSignerId);
    if (author === undefined) return rejectCommand(`ENTITY_COMMAND_AUTHOR_NOT_ON_BOARD:${c.authorSignerId}`);
    if (c.authorSigner !== author.signer) return rejectCommand(`ENTITY_COMMAND_AUTHOR_EOA_MISMATCH:${c.authorSigner}:${author.signer}`);
    return chain(authorBindings(c.authorSignerId, c.txs), () => chain(individualOnly(c.txs), () => chain(hashCommand(c), (commandHash) => {
      if (!sameHex(recoverRawSigner(commandHash, c.signature) ?? undefined, author.signer)) return rejectCommand(`ENTITY_COMMAND_SIGNATURE_MISMATCH:${author.signerId}:${author.signer}`);
      return map(disposition(nonceSlots(state, board), c, commandHash), (d): CheckedCommand => ({ command: c, board, commandHash, disposition: d }));
    })));
  });
});
/** og applyNestedEntityTx: a non-next command (exact retry or cancelled slot) is a no-op; otherwise its txs apply in order, then the nonce advances. */
const foldCommand = (state: EntityState, replicas: Replicas, raw: EntityCommand, ctx: FoldContext): Result<Draft, EntityError> => chain(checkCommand(state, raw), ({ command, board, commandHash, disposition: d }) => {
  if (d !== "next") return ok(idle(state, replicas));
  return map(foldNested(state, replicas, command.txs, ctx, "command"), (applied) => {
    const slots = new Map(nonceSlots(applied.state, board)).set(command.authorSignerId, { nonce: command.nonce, commandHash });
    return { ...applied, state: { ...applied.state, committed: { ...applied.state.committed, entityCommandNonces: nonceBinary(board, slots) } } };
  });
});
/** og nextEntityCommandNonce. */
export const nextCommandNonce = (state: EntityState, author: string): Result<bigint, EntityError> => map(commandBoard(state), (board) => (nonceSlots(state, board).get(lower(author))?.nonce ?? 0n) + 1n);
/** og board shares keyed by canonical signer id. */
const boardShares = (state: EntityState): { readonly bySigner: ReadonlyMap<string, bigint>; readonly total: bigint } => {
  const bySigner = new Map([...membersOf(state.quorum)].map(([a, m]) => [signerId(a), m.shares] as const));
  return { bySigner, total: [...bySigner.values()].reduce((a, b) => a + b, 0n) };
};
type StoredVote = "yes" | "no" | { readonly choice: "yes" | "no"; readonly comment: string };
type StoredProposal = { readonly id: string; readonly proposer: string; readonly boardHash: string; readonly boardEpoch: number; readonly action: Binary; readonly actionHash: string; readonly votes: ReadonlyMap<string, StoredVote>; readonly created: number };
const proposalsOf = (state: EntityState): ReadonlyMap<string, StoredProposal> => (state.committed["proposals"] as ReadonlyMap<string, StoredProposal> | undefined) ?? new Map();
const withProposals = (state: EntityState, proposals: ReadonlyMap<string, StoredProposal>): EntityState =>
  ({ ...state, committed: { ...state.committed, proposals: proposals as unknown as Binary } });
/** og generateProposalId: sha256 of safeStringify({actionHash, proposer, boardHash, boardEpoch, commandNonce}) with og's sorted keys and tagged bigint. */
export const proposalId = (actionHash: string, proposer: string, board: { readonly boardHash: string; readonly boardEpoch: number }, commandNonce: bigint): string =>
  `prop_${nobleHex(sha256(utf8(JSON.stringify({ actionHash, boardEpoch: board.boardEpoch, boardHash: board.boardHash, commandNonce: { __xlnType: "BigInt", value: commandNonce.toString() }, proposer }))))}`;
/** og executeProposal (+ approvedEntityTxs): a collective message is a status event; an entity transaction applies its txs on the collective lane. */
const executeProposal = (state: EntityState, replicas: Replicas, action: ProposalAction, ctx: FoldContext, prior: readonly FrameEvent[]): Result<Draft, EntityError> =>
  action.type === "collective_message"
    ? ok({ state, accountReplicas: replicas, outputs: [], events: [...prior, status(`[COLLECTIVE] ${action.data.message}`)], touched: [] })
    : map(foldNested(state, replicas, action.data.txs, ctx, "collective"), (d) => ({ ...d, events: [...prior, ...(d.events ?? [])] }));
/** og handleProposeEntityTx: board member, action, capacity, id; a proposer holding the threshold executes at once, otherwise the proposal waits for votes. */
const foldPropose = (state: EntityState, replicas: Replicas, x: Extract<EntityTx, { type: "propose" }>["data"], ctx: FoldContext): Result<Draft, EntityError> => {
  const proposer = lower(x.proposer), power = boardShares(state).bySigner.get(proposer);
  if (power === undefined) return invariant(`ENTITY_PROPOSAL_PROPOSER_UNKNOWN:${proposer}`);
  return chain(commandBoard(state), (board) => chain(checkAction(x.action), (action) => {
    const proposals = proposalsOf(state);
    if (proposals.size >= MAX_PENDING_PROPOSALS) return invariant(`ENTITY_PROPOSAL_PENDING_LIMIT_EXCEEDED:${proposals.size}:${MAX_PENDING_PROPOSALS}`);
    const pending = [...proposals.values()].find((p) => lower(p.proposer) === proposer);
    if (pending !== undefined) return invariant(`ENTITY_PROPOSAL_PROPOSER_PENDING_LIMIT:${proposer}:${pending.id}`);
    return chain(hashProposalAction(action), (actionHash) => chain(binaryBody(wireAction(action)), (wired) => {
      const id = proposalId(actionHash, proposer, board, (nonceSlots(state, board).get(proposer)?.nonce ?? 0n) + 1n);
      if (proposals.has(id)) return invariant(`ENTITY_PROPOSAL_DUPLICATE:${id}`);
      if (power >= thresholdOf(state.quorum)) return executeProposal(state, replicas, action, ctx, []);
      const proposal: StoredProposal = { id, proposer, boardHash: board.boardHash, boardEpoch: board.boardEpoch, action: wired, actionHash, votes: new Map([[proposer, "yes"]]), created: Number(ctx.timestamp) };
      return ok(idle(withProposals(state, new Map(proposals).set(id, proposal)), replicas));
    }));
  }));
};
/** og handleVoteEntityTx: same board and epoch, one vote per member; yes power reaching the threshold executes, no power that makes it unreachable drops it. */
const foldVote = (state: EntityState, replicas: Replicas, x: Extract<EntityTx, { type: "vote" }>["data"], ctx: FoldContext): Result<Draft, EntityError> => {
  const voter = lower(x.voter), proposals = proposalsOf(state), proposal = proposals.get(x.proposalId), shares = boardShares(state);
  if (proposal === undefined) return invariant(`ENTITY_PROPOSAL_VOTE_TARGET_MISSING:${x.proposalId}`);
  if (!shares.bySigner.has(voter)) return invariant(`ENTITY_PROPOSAL_VOTER_UNKNOWN:${voter}`);
  return chain(commandBoard(state), (board) => {
    if (proposal.boardHash.toLowerCase() !== board.boardHash) return invariant(`ENTITY_PROPOSAL_BOARD_MISMATCH:${x.proposalId}:${proposal.boardHash}:${board.boardHash}`);
    if (proposal.boardEpoch !== board.boardEpoch) return invariant(`ENTITY_PROPOSAL_EPOCH_MISMATCH:${x.proposalId}:${proposal.boardEpoch}:${board.boardEpoch}`);
    if (proposal.votes.has(voter)) return invariant(`ENTITY_PROPOSAL_DUPLICATE_VOTE:${x.proposalId}:${voter}`);
    const votes = new Map(proposal.votes).set(voter, x.comment !== undefined ? { choice: x.choice, comment: x.comment } : x.choice);
    const power = (choice: "yes" | "no"): bigint => [...votes].reduce((n, [id, v]) => ((typeof v === "object" ? v.choice : v) === choice ? n + (shares.bySigner.get(id) ?? 0n) : n), 0n);
    const threshold = thresholdOf(state.quorum), rest = new Map(proposals);
    rest.delete(x.proposalId);
    if (power("yes") >= threshold) return executeProposal(withProposals(state, rest), replicas, actionOf(proposal.action), ctx, []);
    if (power("no") >= shares.total - threshold + 1n) return ok(idle(withProposals(state, rest), replicas));
    return ok(idle(withProposals(state, new Map(proposals).set(x.proposalId, { ...proposal, votes })), replicas));
  });
};
const actionOf = (wired: Binary): ProposalAction => {
  const a = wired as { readonly type: string; readonly data: { readonly message?: string; readonly version?: 1; readonly actionHash?: string; readonly txs?: readonly { readonly type: string; readonly data: unknown }[] } };
  return a.type === "collective_message" ? { type: "collective_message", data: { message: a.data.message ?? "" } }
    : { type: "entity_transaction", data: { version: 1, actionHash: a.data.actionHash ?? "", txs: (a.data.txs ?? []).map(unwireEntityTx) } };
};
/** og normalizeEntityProposalBoard + normalizeEntityCommandNonceBoard at frame start: work from another board or epoch is dropped. */
const normalizeGovernance = (state: EntityState): Result<EntityState, EntityError> => {
  const proposals = proposalsOf(state), nonces = state.committed["entityCommandNonces"] as { readonly boardHash?: string; readonly boardEpoch?: number } | undefined;
  if (proposals.size === 0 && nonces === undefined) return ok(state);
  return map(commandBoard(state), (board) => {
    const kept = new Map([...proposals].filter(([, p]) => p.boardHash.toLowerCase() === board.boardHash && p.boardEpoch === board.boardEpoch));
    const next = kept.size === proposals.size ? state : withProposals(state, kept);
    return nonces === undefined || (nonces.boardHash === board.boardHash && nonces.boardEpoch === board.boardEpoch) ? next
      : { ...next, committed: { ...next.committed, entityCommandNonces: nonceBinary(board, new Map()) } };
  });
};
/**
 * og prepareLocallyAuthoredEntityTxs (without its openAccount/directPayment materialization): protocol txs pass through, a signed command is
 * re-checked against the running cursor (a stale one is dropped), runs of individual txs become one command and runs of collective txs one
 * command carrying a `propose` of them. `sign` is the author's key on a 32-byte digest.
 */
export const authorEntityTxs = (state: EntityState, author: string, txs: readonly EntityTx[], sign: (digest: Hash) => Result<Signature, unknown>): Result<readonly EntityTx[], EntityError> => {
  let cursor = state, run: EntityTx[] = [], kind: "individual" | "collective" | undefined;
  const out: EntityTx[] = [], seen = new Map<string, string>();
  const advance = (c: CheckedCommand): void => {
    const slots = new Map(nonceSlots(cursor, c.board)).set(c.command.authorSignerId, { nonce: c.command.nonce, commandHash: c.commandHash });
    cursor = { ...cursor, committed: { ...cursor.committed, entityCommandNonces: nonceBinary(c.board, slots) } };
  };
  const flush = (): Result<void, EntityError> => {
    if (run.length === 0) return ok(undefined);
    const batch = run, collective = kind === "collective";
    run = []; kind = undefined;
    const commandTxs = collective ? map(entityTransactionAction(batch), (action): readonly EntityTx[] => [{ type: "propose", data: { proposer: lower(author), action } }]) : ok(batch);
    return chain(commandTxs, (ctxs) => chain(buildCommand(cursor, author, ctxs, sign), (command) => map(checkCommand(cursor, command), (checked) => { out.push({ type: "entityCommand", data: checked.command }); advance(checked); })));
  };
  for (const tx of txs) {
    if (tx.type === "entityCommand") {
      const slot = [tx.data.entityId, tx.data.stackKey, tx.data.boardHash, tx.data.boardEpoch, lower(tx.data.authorSignerId), tx.data.nonce].map(String).join("|"), identity = canon(wireEntityTx(tx));
      if (seen.has(slot)) continue;
      seen.set(slot, identity);
      const flushed = flush();
      if (!flushed.ok) return flushed;
      const checked = checkCommand(cursor, tx.data);
      if (!checked.ok) { if (checked.error._tag === "entity_command") continue; return checked; }
      if (checked.value.disposition !== "next") continue;
      out.push({ type: "entityCommand", data: checked.value.command });
      advance(checked.value);
      continue;
    }
    if (isProtocolTx(tx)) { const flushed = flush(); if (!flushed.ok) return flushed; out.push(tx); continue; }
    const k = isIndividualTx(tx) ? "individual" : "collective";
    if (kind !== undefined && kind !== k) { const flushed = flush(); if (!flushed.ok) return flushed; }
    kind = k;
    run.push(tx);
  }
  return map(flush(), () => out);
};
/** og buildSignedEntityCommand. */
export const buildCommand = (state: EntityState, author: string, txs: readonly EntityTx[], sign: (digest: Hash) => Result<Signature, unknown>): Result<EntityCommand, EntityError> => {
  const signer = lower(author);
  if (txs.length === 0 || txs.length > ENTITY_MEMPOOL_SIZE) return invariant(`ENTITY_COMMAND_TX_COUNT_INVALID:${txs.length}`);
  const protocol = txs.find(isProtocolTx);
  if (protocol !== undefined) return invariant(`ENTITY_COMMAND_PROTOCOL_TX_FORBIDDEN:${protocol.type}`);
  return chain(commandBoard(state), (board) => {
    const member = board.members.find((m) => m.signerId === signer);
    if (member === undefined) return rejectCommand(`ENTITY_COMMAND_AUTHOR_NOT_ON_BOARD:${signer}`);
    return chain(authorBindings(signer, txs), () => chain(individualOnly(txs), () => chain(hashCommandTxs(txs), (txsHash) => {
      const body: CommandBody = { version: 1, entityId: lower(state.id), stackKey: commandStackKey(state), boardHash: board.boardHash, boardEpoch: board.boardEpoch, authorSignerId: signer, authorSigner: member.signer, nonce: (nonceSlots(state, board).get(signer)?.nonce ?? 0n) + 1n, txsHash, txs };
      return chain(hashCommand(body), (h) => map(mapErr(sign(h as Hash), (): EntityError => ({ _tag: "sign_failed" })), (sig): EntityCommand => ({ ...body, signature: `0x${hexBody(sig).toLowerCase()}` })));
    })));
  });
};
// ---- og Entity htlcPayment: entity/paybook/payment-admission.ts, tx/handlers/htlc/payment.ts, types/entity/htlc-infra-context.ts ----
/** og EntityTx htlcPayment data: the route is the full path source..target; the preimage never enters consensus. */
export type HtlcPaymentData = {
  readonly targetEntityId: string; readonly tokenId: number; readonly amount: bigint; readonly maxSenderDebit: bigint; readonly route: readonly string[];
  readonly description?: string | undefined; readonly deliveryMode: ConditionalMode; readonly startedAtMs?: number | undefined; readonly hashlock?: string | undefined;
};
/** og PreparedOriginatedHtlcPayment: the proposer's public facts for one raw htlcPayment tx, committed in the frame's entityContext.htlc.originated. */
export type PreparedOriginated = {
  readonly txHash: string; readonly targetEntityId: string; readonly tokenId: number; readonly recipientAmount: bigint; readonly route: readonly string[]; readonly description: string;
  readonly deliveryMode: ConditionalMode; readonly startedAtMs: number; readonly hashlock: string; readonly senderLockAmount: bigint; readonly maxSenderDebit: bigint; readonly totalFee: bigint;
  readonly timelock: bigint; readonly revealBeforeHeight: number; readonly nextHopEntityId: string; readonly envelope: HtlcEnvelope;
};
/** og PaybookEntry (entity/types.ts): one hashlock-keyed payment machine entry. */
export type PaybookEntry = {
  readonly hashlock: string; readonly description?: string | undefined; readonly tokenId?: number | undefined; readonly amount?: bigint | undefined; readonly startedAtMs?: number | undefined;
  readonly originated?: true | undefined; readonly inboundEntity?: string | undefined; readonly outboundEntity?: string | undefined; readonly inboundSettled?: true | undefined; readonly outboundSettled?: true | undefined;
  readonly secret?: string | undefined; readonly secretAckPending?: boolean | undefined; readonly secretAckStartedAt?: number | undefined; readonly secretAckDeadlineAt?: number | undefined; readonly secretAckedAt?: number | undefined;
  readonly pendingFee?: bigint | undefined; readonly crossJurisdictionRelay?: Binary | undefined; readonly createdTimestamp: number;
};
export type Paybook = { readonly entries: ReadonlyMap<string, PaybookEntry>; readonly feesEarned: bigint };
export const EMPTY_PAYBOOK: Paybook = { entries: new Map(), feesEarned: 0n };
/** og state-root.ts projectEntityConsensusState paybook section: the hashlock-keyed collection commitment and the fee total. */
export const paybookSection = (p: Paybook): Result<Binary, EntityRootError> =>
  map(entityCollectionCommitment(new Map([...p.entries].map(([k, v]) => [k, v as unknown as Binary])), "paybookHashlock"), (entries) => ({ entries, feesEarned: p.feesEarned }));
/** The proposer's HTLC infrastructure (og proposal/infra-context.ts): gossip profiles as og Profile values, plus proposer-only entropy. */
export type HtlcProposerInfra = {
  readonly profiles: readonly Binary[];
  /** og getDeterministicHtlcTestSecret: a secret registered for this exact canonical payment; its ephemeral keys then derive from the tx hash. */
  readonly secretFor?: ((txHash: string) => string | undefined) | undefined;
  /** og secureRandomBytes32Hex: fresh proposer entropy (32-byte hex) for a label; the rewrite takes it as input. */
  readonly entropy?: ((label: string) => string | undefined) | undefined;
  /** og observeOnlineEntityIds: the proposer's liveness view of a peer, committed as a peer assertion. */
  readonly online?: ((entityId: string) => boolean) | undefined;
  /** og requireEntityEncryptionPrivateKey: the Entity's X25519 key every validator holds (the Runtime derives it from its encryption seed when not given). */
  readonly encryptionPrivateKey?: string | undefined;
};
/** The frame's committed HTLC context as the fold consumes it. */
export type HtlcFrameInfra = { readonly gossipProfiles: readonly Binary[]; readonly peerAssertions: readonly Binary[]; readonly originated: readonly PreparedOriginated[]; readonly entries: readonly PreparedHtlcEntry[] };
export const EMPTY_HTLC_INFRA: HtlcFrameInfra = { gossipProfiles: [], peerAssertions: [], originated: [], entries: [] };
const HTLC_NOTE_MAX_BYTES = 256, MAX_TOKEN_ID = 65535;
const htlcReject = (code: string): Result<never, EntityError> => err({ _tag: "htlc_payment", code });
const fromOnion = <T>(r: Result<T, OnionError>): Result<T, EntityError> => mapErr(r, (e): EntityError => ({ _tag: "htlc_payment", code: e.code }));
const bytes32Id = (value: unknown, code: string): Result<string, EntityError> => { const n = lower(value); return /^0x[0-9a-f]{64}$/.test(n) ? ok(n) : htlcReject(code); };
type HtlcPaymentTx = Extract<EntityTx, { type: "htlcPayment" }>;
/** og hashRawHtlcPaymentTx: keccak of the canonical consensus bytes of the exact raw tx. */
export const htlcPaymentTxHash = (tx: HtlcPaymentTx): Result<string, EntityError> =>
  chain(binaryBody(tx), (b) => map(encodeConsensus(b), keccak256Hex));
/** og assertRawHtlcPayment. */
const rawHtlcPayment = (tx: HtlcPaymentTx): Result<HtlcPaymentData, EntityError> => {
  const d = tx.data, allowed = new Set(["amount", "deliveryMode", "maxSenderDebit", "route", "targetEntityId", "tokenId", "description", "hashlock", "startedAtMs"]);
  if (Object.keys(d).some((k) => !allowed.has(k))) return htlcReject("HTLC_PAYMENT_FIELDS_INVALID");
  if (!Number.isSafeInteger(d.tokenId) || d.tokenId < 0 || d.tokenId > MAX_TOKEN_ID) return htlcReject("HTLC_PAYMENT_TOKEN_INVALID");
  if (typeof d.amount !== "bigint" || d.amount <= 0n) return htlcReject("HTLC_PAYMENT_AMOUNT_INVALID");
  if (typeof d.maxSenderDebit !== "bigint" || d.maxSenderDebit <= 0n) return htlcReject("HTLC_PAYMENT_MAX_SENDER_DEBIT_INVALID");
  if (d.maxSenderDebit < d.amount) return htlcReject("HTLC_PAYMENT_MAX_SENDER_DEBIT_BELOW_AMOUNT");
  if (d.deliveryMode !== "instant" && d.deliveryMode !== "async") return htlcReject("HTLC_PAYMENT_DELIVERY_MODE_INVALID");
  if (d.description !== undefined && (typeof d.description !== "string" || d.description !== d.description.trim() || utf8(d.description).byteLength > HTLC_NOTE_MAX_BYTES)) return htlcReject("HTLC_PAYMENT_DESCRIPTION_INVALID");
  if (d.hashlock !== undefined && !/^0x[0-9a-f]{64}$/.test(d.hashlock)) return htlcReject("HTLC_PAYMENT_HASHLOCK_INVALID");
  return ok(d);
};
/** og normalizeRoute: lowercase ids, source first, target last, at most 100 hops, loop-free (a self route needs two unique intermediates). */
const htlcRoute = (raw: readonly string[], source: string, target: string): Result<readonly string[], EntityError> => chain(traverse(raw, (v) => bytes32Id(v, "HTLC_PAYMENT_ROUTE_ENTITY_INVALID")), (route) => {
  if (route.length < 2 || route[0] !== source || route[route.length - 1] !== target) return htlcReject("HTLC_PAYMENT_ROUTE_INVALID");
  if (route.length - 1 > HTLC_MAX_HOPS) return htlcReject("HTLC_PAYMENT_ROUTE_TOO_LONG");
  const inner = route.slice(1, -1), selfRoute = source === target;
  const validSelf = selfRoute && inner.length >= 2 && new Set(inner).size === inner.length && !inner.includes(source);
  return (!selfRoute && new Set(route).size !== route.length) || (selfRoute && !validSelf) ? htlcReject("HTLC_PAYMENT_ROUTE_LOOP") : ok(route);
});
const sameDomainCanonical = (a: Domain, b: Domain): Result<boolean, EntityError> =>
  chain(mapErr(domainOf(a), (): EntityError => ({ _tag: "htlc_payment", code: "ACCOUNT_STATE_DOMAIN_INVALID" })), (x) => map(mapErr(domainOf(b), (): EntityError => ({ _tag: "htlc_payment", code: "ACCOUNT_STATE_DOMAIN_INVALID" })), (y) => x.chainId === y.chainId && x.depositoryAddress === y.depositoryAddress));
/** og Profile -> RoutingProfile: the fields pathfinding/htlc-quote.ts reads (routingFeePPM ?? 1, baseFee ?? 0n, tokenCapacities as a Map or a Record). */
export const routingProfileOf = (b: Binary): RoutingProfile | null => {
  if (b === null || typeof b !== "object" || Array.isArray(b) || b instanceof Map) return null;
  const p = b as { readonly [k: string]: unknown }, meta = (p["metadata"] ?? {}) as { readonly [k: string]: unknown };
  const accounts = Array.isArray(p["accounts"]) ? (p["accounts"] as readonly { readonly [k: string]: unknown }[]) : [];
  return {
    entityId: String(p["entityId"] ?? ""), entityEncryptionPublicKey: String(p["entityEncryptionPublicKey"] ?? ""),
    metadata: { ...opt("routingFeePPM", meta["routingFeePPM"] as number | undefined), ...opt("baseFee", typeof meta["baseFee"] === "bigint" ? meta["baseFee"] : undefined) },
    accounts: accounts.map((a): RoutingAccount => {
      const caps = a["tokenCapacities"], rows: [unknown, unknown][] = caps instanceof Map ? [...caps] : caps !== null && typeof caps === "object" ? Object.entries(caps) : [];
      return { counterpartyId: String(a["counterpartyId"] ?? ""), domain: a["domain"] as Domain, tokenCapacities: new Map(rows.map(([k, v]) => [Number(k), v as { readonly inCapacity: bigint; readonly outCapacity: bigint }])) };
    }),
  };
};
/** og hopAccountDomain: the lane's domain as whichever Profile advertises it; two advertisements must agree. */
const hopDomain = (index: RoutingIndex, from: string, to: string): Result<Domain, EntityError> => chain(fromOnion(uniqueProfile(index, from)), (fp) => chain(fromOnion(uniqueProfile(index, to)), (tp) => {
  const own = fp.accounts.find((a) => a.counterpartyId.toLowerCase() === to), mirror = tp.accounts.find((a) => a.counterpartyId.toLowerCase() === from);
  if (own === undefined && mirror === undefined) return htlcReject(`HTLC_PAYMENT_PROFILE_ACCOUNT_MISSING:${from}:${to}`);
  if (own !== undefined && mirror !== undefined) return chain(sameDomainCanonical(own.domain, mirror.domain), (same) => (same ? ok(own.domain) : htlcReject(`HTLC_PAYMENT_PROFILE_ACCOUNT_DOMAIN_MISMATCH:${from}:${to}`)));
  return ok((own ?? mirror)?.domain as Domain);
}));
type HtlcOriginView = { readonly id: EntityId; readonly timestamp: number; readonly jHeight: number; readonly encryptionKey: string; readonly paybook: Paybook; readonly replicas: Replicas };
/** og assertOriginRouteEvidence (shared with materialization): source profile key, every hop's domain, the first hop against the local Account. */
const originRouteEvidence = (v: HtlcOriginView, index: RoutingIndex, route: readonly string[]): Result<readonly Domain[], EntityError> =>
  chain(fromOnion(uniqueProfile(index, route[0] ?? "")), (source) => source.entityEncryptionPublicKey !== v.encryptionKey ? htlcReject("HTLC_PAYMENT_SOURCE_PROFILE_KEY_MISMATCH")
    : traverse(route.slice(0, -1), (from, i) => chain(hopDomain(index, from, route[i + 1] ?? ""), (domain): Result<Domain, EntityError> => {
      if (i !== 0) return ok(domain);
      const local = v.replicas.get((route[1] ?? "") as EntityId);
      return local === undefined ? htlcReject(`HTLC_PAYMENT_SOURCE_ACCOUNT_DOMAIN_MISMATCH:${route[0]}:${route[1]}`)
        : chain(sameDomainCanonical(local.state.terms.domain, domain), (same) => (same ? ok(domain) : htlcReject(`HTLC_PAYMENT_SOURCE_ACCOUNT_DOMAIN_MISMATCH:${route[0]}:${route[1]}`)));
    })));
type OriginEconomics = { readonly startedAtMs: number; readonly senderLockAmount: bigint; readonly hopForwardAmounts: ReadonlyMap<string, bigint>; readonly timelock: bigint; readonly revealBeforeHeight: number };
/** og quote + deadline window for one payment at the frame clock. */
const originEconomics = (v: HtlcOriginView, index: RoutingIndex, d: HtlcPaymentData, route: readonly string[]): Result<OriginEconomics, EntityError> => {
  const startedAtMs = d.startedAtMs ?? v.timestamp;
  if (!Number.isSafeInteger(startedAtMs) || startedAtMs !== v.timestamp) return htlcReject("HTLC_PAYMENT_STARTED_AT_INVALID");
  return chain(fromOnion(quoteHtlcRoute(index, route, d.tokenId, d.amount)), (quote) => quote.senderLockAmount > d.maxSenderDebit ? htlcReject("HTLC_PAYMENT_MAX_SENDER_DEBIT_EXCEEDED")
    : map(fromOnion(paymentDeadlineWindow(d.deliveryMode, v.jHeight, startedAtMs, route.length - 1)), (w) => ({
      startedAtMs, senderLockAmount: quote.senderLockAmount, hopForwardAmounts: quote.hopForwardAmounts, timelock: hopTimelock(w.baseTimelock, 0), revealBeforeHeight: hopRevealHeight(w.baseHeight, 0, route.length - 1),
    })));
};
/** og infra-context.ts resolveRoute: `findPaths(entityId, tx.data.targetEntityId, amount, tokenId)[0].path` (the raw target id, as og passes it). */
const resolveHtlcRoute = (profiles: readonly Binary[], source: string, d: HtlcPaymentData): Result<readonly string[], EntityError> =>
  chain(fromOnion(findPaths(profiles, source, d.targetEntityId, d.amount, d.tokenId)), (routes) => {
    const path = routes[0]?.path;
    return path === undefined ? htlcReject(`HTLC_PAYMENT_ROUTE_NOT_FOUND:${source}:${d.targetEntityId}`) : ok(path);
  });
/** og generateHtlcEphemeralPrivateKey: 32 bytes (keccak of the deterministic seed, or entropy), clamped per RFC 7748. */
const clampedX25519 = (hex32: string): string => { const b = hexToBytes(hex32); b[0] = (b[0] ?? 0) & 248; b[31] = ((b[31] ?? 0) & 127) | 64; return bytesToHex(b); };
/**
 * og materializeOriginatedHtlcPayments (proposer only): per htlcPayment in order, the raw checks, route, secret -> hashlock, quote, deadline window,
 * profile/domain evidence and the onion; sorted by tx hash. An empty route is resolved like og resolveRoute: the cheapest findPaths route over every gossip Profile.
 */
export const materializeOriginated = (v: HtlcOriginView, profiles: readonly Binary[], txs: readonly EntityTx[], infra: HtlcProposerInfra): { readonly originated: readonly PreparedOriginated[]; readonly refused: ReadonlyMap<EntityTx, EntityError> } => {
  const index = routingIndex(profiles.flatMap((b) => { const p = routingProfileOf(b); return p === null ? [] : [p]; }));
  const taken = new Set<string>(), hashes = new Set<string>(), refused = new Map<EntityTx, EntityError>(), originated: PreparedOriginated[] = [];
  for (const tx of txs) {
    if (tx.type !== "htlcPayment") continue;
    const one = chain(rawHtlcPayment(tx), (d) => chain(bytes32Id(v.id, "HTLC_PAYMENT_SOURCE_INVALID"), (source) => chain(bytes32Id(d.targetEntityId, "HTLC_PAYMENT_TARGET_INVALID"), (target) =>
      chain(d.route.length > 0 ? htlcRoute(d.route, source, target) : chain(resolveHtlcRoute(infra.profiles, source, d), (found) => htlcRoute(found, source, target)), (route) => chain(htlcPaymentTxHash(tx), (txHash): Result<PreparedOriginated, EntityError> => {
        const local = infra.secretFor?.(txHash), secret = local ?? infra.entropy?.(`htlc-secret:${txHash}`) ?? "";
        if (!/^0x[0-9a-f]{64}$/.test(secret)) return htlcReject("HTLC_PAYMENT_PREIMAGE_INVALID");
        const hashlock = (hashHtlcSecret(secret) ?? "").toLowerCase();
        if (d.hashlock !== undefined && d.hashlock !== hashlock) return htlcReject("HTLC_PAYMENT_HASHLOCK_MISMATCH");
        if (v.paybook.entries.has(hashlock) || taken.has(hashlock)) return htlcReject(`HTLC_PAYMENT_HASHLOCK_ALREADY_ACTIVE:${hashlock}`);
        return chain(originEconomics(v, index, d, route), (e) => chain(originRouteEvidence(v, index, route), (domains) => {
          const keys = new Map(route.map((id) => [id, unwrapOr(map(uniqueProfile(index, id), (p) => p.entityEncryptionPublicKey), () => "")]));
          let keyIndex = 0;
          const ephemeral = (): string => clampedX25519(local !== undefined ? keccak256Hex(utf8(`${txHash}:${keyIndex++}`)) : infra.entropy?.(`htlc-ephemeral:${txHash}:${keyIndex++}`) ?? ZERO_WORD);
          return map(fromOnion(createOnionEnvelopes(route, secret, keys, domains, e.hopForwardAmounts, d.description, e.startedAtMs,
            { hashlock, tokenId: d.tokenId, senderLockAmount: e.senderLockAmount, timelock: e.timelock, revealBeforeHeight: e.revealBeforeHeight }, ephemeral)), (envelope): PreparedOriginated => ({
            txHash, targetEntityId: target, tokenId: d.tokenId, recipientAmount: d.amount, route, description: d.description ?? "", deliveryMode: d.deliveryMode, startedAtMs: e.startedAtMs,
            hashlock, senderLockAmount: e.senderLockAmount, maxSenderDebit: d.maxSenderDebit, totalFee: e.senderLockAmount - d.amount, timelock: e.timelock, revealBeforeHeight: e.revealBeforeHeight,
            nextHopEntityId: route[1] ?? "", envelope,
          }));
        }));
      })))));
    if (!one.ok) { refused.set(tx, one.error); continue; }
    if (hashes.has(one.value.txHash)) { refused.set(tx, { _tag: "htlc_payment", code: `HTLC_PAYMENT_TX_DUPLICATE:${one.value.txHash}` }); continue; }
    hashes.add(one.value.txHash); taken.add(one.value.hashlock); originated.push(one.value);
  }
  return { originated: [...originated].sort((a, b) => asc(a.txHash, b.txHash)), refused };
};
/** og assertOriginatedHtlcPayments (validators): one prepared origin per htlcPayment, unique hashlocks, route evidence and economics recomputed from public facts. */
export const assertOriginated = (v: HtlcOriginView, infra: HtlcFrameInfra, txs: readonly EntityTx[]): Result<void, EntityError> => {
  const payments = txs.filter((tx): tx is HtlcPaymentTx => tx.type === "htlcPayment");
  if (payments.length !== infra.originated.length) return htlcReject("HTLC_PAYMENT_PREPARED_ORIGIN_COUNT_MISMATCH");
  const index = routingIndex(infra.gossipProfiles.flatMap((b) => { const p = routingProfileOf(b); return p === null ? [] : [p]; }));
  const origins = new Map(infra.originated.map((o) => [o.txHash, o])), taken = new Set<string>();
  return map(traverse(payments, (tx) => chain(rawHtlcPayment(tx), (d) => chain(htlcPaymentTxHash(tx), (txHash): Result<void, EntityError> => {
    const o = origins.get(txHash);
    if (o === undefined) return htlcReject(`HTLC_PAYMENT_PREPARED_CONTEXT_REQUIRED:${txHash}`);
    if (v.paybook.entries.has(o.hashlock) || taken.has(o.hashlock)) return htlcReject(`HTLC_PAYMENT_HASHLOCK_ALREADY_ACTIVE:${o.hashlock}`);
    taken.add(o.hashlock);
    return chain(bytes32Id(v.id, "HTLC_PAYMENT_SOURCE_INVALID"), (source) => chain(bytes32Id(d.targetEntityId, "HTLC_PAYMENT_TARGET_INVALID"), (target) => chain(htlcRoute(o.route, source, target), (route) => {
      if (d.route.length > 0 && canon(route) !== canon(d.route)) return htlcReject(`HTLC_PAYMENT_PREPARED_ROUTE_MISMATCH:${txHash}`);
      return chain(originRouteEvidence(v, index, route), () => chain(originEconomics(v, index, d, o.route), (e): Result<void, EntityError> => {
        if (d.hashlock !== undefined && d.hashlock !== o.hashlock) return htlcReject(`HTLC_PAYMENT_PREPARED_HASHLOCK_MISMATCH:${txHash}`);
        const expected: readonly (readonly [string, unknown, unknown])[] = [
          ["targetEntityId", o.targetEntityId, d.targetEntityId.toLowerCase()], ["tokenId", o.tokenId, d.tokenId], ["recipientAmount", o.recipientAmount, d.amount], ["description", o.description, d.description ?? ""],
          ["deliveryMode", o.deliveryMode, d.deliveryMode], ["startedAtMs", o.startedAtMs, e.startedAtMs], ["senderLockAmount", o.senderLockAmount, e.senderLockAmount], ["maxSenderDebit", o.maxSenderDebit, d.maxSenderDebit],
          ["totalFee", o.totalFee, e.senderLockAmount - d.amount], ["timelock", o.timelock, e.timelock], ["revealBeforeHeight", o.revealBeforeHeight, e.revealBeforeHeight], ["nextHopEntityId", o.nextHopEntityId, o.route[1]],
        ];
        const bad = expected.find(([, a, b]) => a !== b);
        return bad === undefined ? ok(undefined) : htlcReject(`HTLC_PAYMENT_PREPARED_${bad[0].toUpperCase()}_MISMATCH:${txHash}`);
      }));
    })));
  }))), () => undefined);
};
/** og validatePreparedHtlcPayment: the tx's prepared origin, a fresh hashlock, an active next-hop Account with committed out-capacity for the sender lock. */
export const validatePreparedHtlcPayment = (v: HtlcOriginView, tx: HtlcPaymentTx, infra: HtlcFrameInfra): Result<PreparedOriginated, EntityError> =>
  chain(rawHtlcPayment(tx), (d) => chain(htlcPaymentTxHash(tx), (txHash): Result<PreparedOriginated, EntityError> => {
    const p = infra.originated.find((o) => o.txHash === txHash);
    if (p === undefined) return htlcReject(`HTLC_PAYMENT_PREPARED_CONTEXT_REQUIRED:${txHash}`);
    if (p.targetEntityId !== d.targetEntityId.toLowerCase() || p.tokenId !== d.tokenId || p.recipientAmount !== d.amount || p.maxSenderDebit !== d.maxSenderDebit || p.deliveryMode !== d.deliveryMode
      || (d.startedAtMs !== undefined && p.startedAtMs !== d.startedAtMs)) return htlcReject(`HTLC_PAYMENT_PREPARED_CONTEXT_MISMATCH:${txHash}`);
    if (v.paybook.entries.has(p.hashlock)) return htlcReject(`HTLC_PAYMENT_HASHLOCK_ALREADY_ACTIVE:${p.hashlock}`);
    const child = v.replicas.get(p.nextHopEntityId as EntityId);
    if (child === undefined || child._tag === "preparing" || child._tag === "disputed") return htlcReject(`HTLC_PAYMENT_OUTBOUND_ACCOUNT_UNAVAILABLE:${txHash}`);
    const tk = String(p.tokenId) as TokenId, body = child.state, byLeft = isLeft(v.id, replicaId(child));
    return !body.account.deltas.has(tk) || outCapacity(getDelta(body.account, tk), byLeft, holds(body, tk, byLeft)) < p.senderLockAmount ? htlcReject(`HTLC_PAYMENT_OUTBOUND_CAPACITY_INSUFFICIENT:${txHash}`) : ok(p);
  }));
/** og handleHtlcPayment: record the originated paybook entry and queue the first-hop htlc_lock carrying the onion (no output; the Account frame proposes in this Entity frame). */
export const htlcPaymentStep = (p: PreparedOriginated, paybook: Paybook, timestamp: number): { readonly paybook: Paybook; readonly lock: AccountTx } => ({
  paybook: { ...paybook, entries: mapSet(paybook.entries, p.hashlock, {
    hashlock: p.hashlock, ...(p.description ? { description: p.description } : {}), tokenId: p.tokenId, amount: p.recipientAmount, startedAtMs: p.startedAtMs, originated: true, outboundEntity: p.nextHopEntityId, createdTimestamp: timestamp,
  }) },
  lock: { type: "htlc_lock", lockId: p.hashlock, hashlock: p.hashlock, timelock: p.timelock, revealBeforeHeight: BigInt(p.revealBeforeHeight), amount: p.senderLockAmount, tokenId: String(p.tokenId) as TokenId, deliveryMode: p.deliveryMode, envelope: p.envelope },
});
/** og selectInfraProfiles + the committed gossipProfiles: exactly the Profiles of every entity on an originated route, sorted by entity id. */
const frameProfiles = (profiles: readonly Binary[], originated: readonly PreparedOriginated[]): readonly Binary[] => {
  const ids = new Set(originated.flatMap((o) => o.route));
  return profiles.filter((b) => ids.has(lower((b as { readonly entityId?: unknown } | null)?.entityId))).sort((a, b) => asc(String((a as { entityId: string }).entityId), String((b as { entityId: string }).entityId)));
};
/** og materializeFreshEntityInfraContext: the committed context for the payments this frame actually includes (profiles of every route entity, liveness of every next hop). */
const frameHtlcInfra = (infra: HtlcProposerInfra | undefined, v: Omit<HtlcInboundView, "online">, originated: readonly PreparedOriginated[], included: readonly EntityTx[]): Result<HtlcFrameInfra, EntityError> => {
  const observer = onlineObserver(infra);
  return chain(htlcFrameTxs(included) ? inboundHtlcEntries({ ...v, online: observer.online }, included) : ok([]), (entries) => {
    const kept = new Set<string>();
    for (const tx of included) if (tx.type === "htlcPayment") { const h = htlcPaymentTxHash(tx); if (!h.ok) return h; kept.add(h.value); }
    const final = originated.filter((o) => kept.has(o.txHash));
    for (const o of final) observer.online(o.nextHopEntityId);
    const peerAssertions = [...observer.observed.keys()].sort(asc).map((entityId) => ({ entityId, online: observer.observed.get(entityId) === true }));
    return ok({ gossipProfiles: frameProfiles(infra?.profiles ?? [], final), peerAssertions, originated: final, entries });
  });
};
/** og validateHtlcPreparedInfraContext (originated half): exact field set, canonical ids, strictly sorted tx hashes, route and economics shape, opaque envelope. */
export const preparedOriginOf = (b: Binary): PreparedOriginated | null => {
  const KEYS = "deliveryMode,description,envelope,hashlock,maxSenderDebit,nextHopEntityId,recipientAmount,revealBeforeHeight,route,senderLockAmount,startedAtMs,targetEntityId,timelock,tokenId,totalFee,txHash";
  if (b === null || typeof b !== "object" || Array.isArray(b) || b instanceof Map || Object.keys(b).sort().join(",") !== KEYS) return null;
  const o = b as unknown as PreparedOriginated, id = (v: unknown): boolean => typeof v === "string" && /^0x[0-9a-f]{64}$/.test(v);
  const route = o.route, inner = Array.isArray(route) ? route.slice(1, -1) : [];
  if (![o.txHash, o.targetEntityId, o.hashlock, o.nextHopEntityId].every(id) || !Array.isArray(route) || route.length < 2 || route.length > 101 || !route.every(id)) return null;
  if (route[0] === route[route.length - 1] ? inner.length < 2 || new Set(inner).size !== inner.length || inner.includes(route[0] as string) : new Set(route).size !== route.length) return null;
  if (o.targetEntityId !== route[route.length - 1] || o.nextHopEntityId !== route[1]) return null;
  if (!Number.isSafeInteger(o.tokenId) || o.tokenId < 0 || o.tokenId > MAX_TOKEN_ID) return null;
  if (typeof o.recipientAmount !== "bigint" || o.recipientAmount <= 0n || typeof o.senderLockAmount !== "bigint" || o.senderLockAmount < o.recipientAmount || typeof o.maxSenderDebit !== "bigint" || o.maxSenderDebit < o.senderLockAmount
    || typeof o.totalFee !== "bigint" || o.totalFee !== o.senderLockAmount - o.recipientAmount || typeof o.timelock !== "bigint" || o.timelock <= 0n) return null;
  if ((o.deliveryMode !== "instant" && o.deliveryMode !== "async") || !Number.isSafeInteger(o.startedAtMs) || o.startedAtMs <= 0 || !Number.isSafeInteger(o.revealBeforeHeight) || o.revealBeforeHeight <= 0) return null;
  return typeof o.description !== "string" || utf8(o.description).byteLength > HTLC_NOTE_MAX_BYTES || htlcEnvelopeHash(o.envelope) === null ? null : o;
};
const frameInfraOf = (frame: EntityFrame): Result<HtlcFrameInfra, EntityError> => {
  const origins = frame.entityContext.htlc.originated.map(preparedOriginOf);
  if (origins.some((o, i) => o === null || (i > 0 && (origins[i - 1]?.txHash ?? "") >= o.txHash))) return htlcReject("HTLC_PREPARED_CONTEXT_INVALID");
  const entries: PreparedHtlcEntry[] = [];
  for (const b of frame.entityContext.htlc.entries) {
    const e = preparedEntryOf(b, entries.length === 0 ? "" : preparedHtlcKey((entries[entries.length - 1] as PreparedHtlcEntry).binding));
    if (e === null) return htlcReject("HTLC_PREPARED_CONTEXT_INVALID");
    entries.push(e);
  }
  return ok({ gossipProfiles: frame.entityContext.gossipProfiles, peerAssertions: frame.entityContext.peerAssertions, originated: origins as readonly PreparedOriginated[], entries });
};
// ---- og inbound HTLC: entity/paybook/{materialize-context,prepared-context-validation,lifecycle}.ts, tx/handlers/account/committed-{htlc,frame}-followups.ts ----
/** og PreparedHtlcInboundBinding: the committed Account facts of one inbound lock. */
export type PreparedHtlcBinding = {
  readonly fromEntityId: string; readonly toEntityId: string; readonly domain: Domain; readonly accountFrameHash: string; readonly accountHeight: number; readonly envelopeHash: string;
  readonly hashlock: string; readonly tokenId: number; readonly amount: bigint; readonly timelock: bigint; readonly revealBeforeHeight: number;
};
export type HtlcRejectReason = "ciphertext_invalid" | "decrypt_failed" | "next_hop_account_missing" | "next_hop_offline" | "insufficient_capacity" | "fee_below_policy" | "deadline_unsafe";
export type PreparedHtlcOutcome =
  | { readonly kind: "forward"; readonly nextHopEntityId: string; readonly forwardAmount: bigint; readonly innerEnvelope: HtlcEnvelope }
  | { readonly kind: "final"; readonly secret: string; readonly description?: string | undefined; readonly startedAtMs?: number | undefined }
  | { readonly kind: "reject"; readonly reason: HtlcRejectReason };
/** og PreparedHtlcEntry: the proposer's decrypted outcome for one inbound lock, committed in entityContext.htlc.entries. */
export type PreparedHtlcEntry = { readonly binding: PreparedHtlcBinding; readonly outcome: PreparedHtlcOutcome };
export const preparedHtlcKey = (b: PreparedHtlcBinding): string => `${b.accountFrameHash}:${b.hashlock}`;
const HTLC_REJECT_REASONS: ReadonlySet<string> = new Set(["ciphertext_invalid", "decrypt_failed", "next_hop_account_missing", "next_hop_offline", "insufficient_capacity", "fee_below_policy", "deadline_unsafe"]);
/** og validatePreparedEntries: exact fields, canonical ids and domain, strictly sorted binding keys, a well-formed outcome. */
export const preparedEntryOf = (b: Binary, previousKey: string): PreparedHtlcEntry | null => {
  const isRecord = (v: unknown): v is { readonly [k: string]: unknown } => v !== null && typeof v === "object" && !Array.isArray(v) && !(v instanceof Map);
  const keys = (v: { readonly [k: string]: unknown }): string => Object.keys(v).sort().join(",");
  const id = (v: unknown): boolean => typeof v === "string" && /^0x[0-9a-f]{64}$/.test(v);
  if (!isRecord(b) || keys(b) !== "binding,outcome" || !isRecord(b["binding"]) || !isRecord(b["outcome"])) return null;
  const x = b["binding"], o = b["outcome"];
  if (keys(x) !== "accountFrameHash,accountHeight,amount,domain,envelopeHash,fromEntityId,hashlock,revealBeforeHeight,timelock,toEntityId,tokenId") return null;
  if (![x["fromEntityId"], x["toEntityId"], x["accountFrameHash"], x["envelopeHash"], x["hashlock"]].every(id)) return null;
  const height = x["accountHeight"], token = x["tokenId"], reveal = x["revealBeforeHeight"], domain = x["domain"];
  if (!Number.isSafeInteger(height) || (height as number) < 1 || !Number.isSafeInteger(token) || (token as number) < 0 || (token as number) > MAX_TOKEN_ID) return null;
  if (typeof x["amount"] !== "bigint" || x["amount"] <= 0n || typeof x["timelock"] !== "bigint" || x["timelock"] <= 0n || !Number.isSafeInteger(reveal) || (reveal as number) < 1) return null;
  const dom = isRecord(domain) ? domain : undefined, canonical = dom === undefined ? undefined : domainOf({ chainId: Number(dom["chainId"]), depositoryAddress: String(dom["depositoryAddress"] || "") });
  if (dom === undefined || canonical === undefined || !canonical.ok || canonical.value.depositoryAddress !== dom["depositoryAddress"]) return null;
  const key = `${String(x["accountFrameHash"])}:${String(x["hashlock"])}`;
  if (key <= previousKey) return null;
  const outcomeOk = o["kind"] === "forward"
    ? keys(o) === "forwardAmount,innerEnvelope,kind,nextHopEntityId" && id(o["nextHopEntityId"]) && typeof o["forwardAmount"] === "bigint" && o["forwardAmount"] > 0n && o["forwardAmount"] <= (x["amount"] as bigint) && htlcEnvelopeHash(o["innerEnvelope"]) !== null
    : o["kind"] === "final"
      ? Object.keys(o).every((k) => ["kind", "secret", "description", "startedAtMs"].includes(k)) && id(o["secret"])
        && (o["description"] === undefined || (typeof o["description"] === "string" && utf8(o["description"]).byteLength <= HTLC_NOTE_MAX_BYTES))
        && (o["startedAtMs"] === undefined || (Number.isSafeInteger(o["startedAtMs"]) && (o["startedAtMs"] as number) >= 0))
      : o["kind"] === "reject" && keys(o) === "kind,reason" && HTLC_REJECT_REASONS.has(String(o["reason"]));
  return outcomeOk ? (b as unknown as PreparedHtlcEntry) : null;
};
/** The proposer's (or a validator's) inbound decryption view: the pre-frame Entity, the frame clock, the Entity keypair and the liveness it asserts. */
type HtlcInboundView = { readonly state: EntityState; readonly replicas: Replicas; readonly timestamp: number; readonly publicKey: string; readonly privateKey: string | undefined; readonly online: (entityId: string) => boolean };
const preparedReject = (binding: PreparedHtlcBinding, reason: HtlcRejectReason): PreparedHtlcEntry => ({ binding, outcome: { kind: "reject", reason } });
/** og buildInboundBinding + computeHtlcEnvelopeContextHash: a lock without an opaque layer, or whose id is not its hashlock, fails the whole frame. */
const inboundBinding = (m: Extract<AccountPeerInput, { kind: "ack_frame" }>, lock: HtlcLockTx): Result<{ readonly binding: PreparedHtlcBinding; readonly contextHash: string }, EntityError> => {
  const envelopeHash = htlcEnvelopeHash(lock.envelope);
  if (envelopeHash === null) return htlcReject(`HTLC_PREPARED_LAYER_REQUIRED:${lock.lockId}`);
  if (lock.lockId.toLowerCase() !== lock.hashlock.toLowerCase()) return htlcReject(`PAYBOOK_LOCK_ID_MUST_EQUAL_HASHLOCK:${lock.lockId}:${lock.hashlock}`);
  return chain(mapErr(domainOf(m.domain), (): EntityError => ({ _tag: "htlc_payment", code: "HTLC_PREPARED_DOMAIN_INVALID" })), (domain) => {
    const binding: PreparedHtlcBinding = {
      fromEntityId: m.fromEntityId.toLowerCase(), toEntityId: m.toEntityId.toLowerCase(), domain, accountFrameHash: m.frame.stateHash.toLowerCase(), accountHeight: Number(m.frame.height), envelopeHash,
      hashlock: lock.hashlock.toLowerCase(), tokenId: Number(lock.tokenId), amount: lock.amount, timelock: lock.timelock, revealBeforeHeight: Number(lock.revealBeforeHeight),
    };
    return map(fromOnion(htlcEnvelopeContextHash({ fromEntityId: binding.fromEntityId, toEntityId: binding.toEntityId, domain, hashlock: binding.hashlock, tokenId: binding.tokenId, amount: binding.amount, timelock: binding.timelock, revealBeforeHeight: binding.revealBeforeHeight })),
      (contextHash) => ({ binding, contextHash }));
  });
};
/** og validateEnvelope on a decoded layer: a final layer needs a canonical secret, a note of at most 256 characters and a positive start time. */
const envelopeValid = (layer: OnionLayer): boolean => ("finalRecipient" in layer
  ? /^0x[0-9a-f]{64}$/.test(layer.secret) && (layer.description === undefined || layer.description.length <= 256) && (layer.startedAtMs === undefined || layer.startedAtMs > 0)
  : layer.nextHop.length > 0 && BigInt(layer.forwardAmount) > 0n);
/** og calculateHopFee with the hub policy (routingFeePPM ?? 1, baseFee ?? 0n) at the Account's directional utilization. */
const hubForwardFee = (state: EntityState, amount: bigint, out: bigint, inn: bigint): bigint => {
  const cfg = state.committed["hubRebalanceConfig"] as { readonly [k: string]: unknown } | null | undefined;
  const ppm = directionalFeePpm(sanitizeFeePpm(cfg?.["routingFeePPM"] ?? 1, 1), out, inn), base = typeof cfg?.["baseFee"] === "bigint" && cfg["baseFee"] > 0n ? cfg["baseFee"] : 0n;
  return base + ((amount < 0n ? 0n : amount) * BigInt(sanitizeFeePpm(ppm, 1))) / 1_000_000n;
};
/** og materializeForwardOutcome: the next hop's Account, liveness, committed capacity, the hub fee and a safe onward deadline, in that order. */
const forwardOutcome = (v: HtlcInboundView, binding: PreparedHtlcBinding, layer: Extract<OnionLayer, { nextHop: string }>): PreparedHtlcEntry => {
  const nextHopEntityId = layer.nextHop.toLowerCase(), child = v.replicas.get(nextHopEntityId as EntityId);
  if (child === undefined) return preparedReject(binding, "next_hop_account_missing");
  if (!v.online(nextHopEntityId)) return preparedReject(binding, "next_hop_offline");
  const tk = String(binding.tokenId) as TokenId, body = child.state, byLeft = isLeft(v.state.id, replicaId(child)), forwardAmount = BigInt(layer.forwardAmount);
  if (!body.account.deltas.has(tk)) return preparedReject(binding, "insufficient_capacity");
  const d = getDelta(body.account, tk), out = outCapacity(d, byLeft, holds(body, tk, byLeft)), inn = outCapacity(d, !byLeft, holds(body, tk, !byLeft));
  if (out < forwardAmount) return preparedReject(binding, "insufficient_capacity");
  if (binding.amount - forwardAmount < hubForwardFee(v.state, binding.amount, out, inn)) return preparedReject(binding, "fee_below_policy");
  if (binding.timelock - BigInt(HTLC_TIMELOCK_DELTA_MS) <= BigInt(v.timestamp) + BigInt(HTLC_MIN_FORWARD_TIMELOCK_MS) || binding.revealBeforeHeight - HTLC_REVEAL_DELTA_BLOCKS <= Number(entityJHeight(v.state))) return preparedReject(binding, "deadline_unsafe");
  return { binding, outcome: { kind: "forward", nextHopEntityId, forwardAmount, innerEnvelope: layer.innerEnvelope } };
};
/** og materializeInboundEntry: decrypt the layer addressed to this Entity; only an AEAD failure is a peer-rejectable outcome. */
const inboundEntry = (v: HtlcInboundView, m: Extract<AccountPeerInput, { kind: "ack_frame" }>, lock: HtlcLockTx): Result<PreparedHtlcEntry, EntityError> => chain(inboundBinding(m, lock), ({ binding, contextHash }) => {
  if (v.privateKey === undefined) return htlcReject("HTLC_ENTITY_ENCRYPTION_PRIVATE_KEY_INVALID");
  const plain = decryptOpaqueHtlc(lock.envelope as HtlcEnvelope, v.publicKey, v.privateKey, contextHash);
  if (!plain.ok) return plain.error.code === "HTLC_CIPHERTEXT_AUTHENTICATION_FAILED" ? ok(preparedReject(binding, "decrypt_failed")) : fromOnion(plain);
  const layer = decodeOnionLayer(plain.value);
  if (!layer.ok || !envelopeValid(layer.value)) return ok(preparedReject(binding, "ciphertext_invalid"));
  const l = layer.value;
  return ok("finalRecipient" in l ? { binding, outcome: { kind: "final", secret: l.secret, ...opt("description", l.description), ...opt("startedAtMs", l.startedAtMs) } } : forwardOutcome(v, binding, l));
});
/** og collectInboundEntries + canonicalizeInboundEntries: every enveloped lock in a peer's Account frame, sorted by binding key; a repeat collapses, a contradiction fails. */
export const inboundHtlcEntries = (v: HtlcInboundView, txs: readonly EntityTx[]): Result<readonly PreparedHtlcEntry[], EntityError> => {
  const locks = txs.flatMap((tx) => (tx.type === "accountInput" && tx.data.kind === "ack_frame" ? tx.data.frame.txs.flatMap((a) => (a.type === "htlc_lock" && a.envelope !== undefined ? [[tx.data as Extract<AccountPeerInput, { kind: "ack_frame" }>, a] as const] : [])) : []));
  return chain(traverse(locks, ([m, lock]) => inboundEntry(v, m, lock)), (entries) => {
    const sorted = [...entries].sort((a, b) => asc(preparedHtlcKey(a.binding), preparedHtlcKey(b.binding))), out: PreparedHtlcEntry[] = [];
    for (const e of sorted) {
      const prev = out[out.length - 1];
      if (prev === undefined || preparedHtlcKey(prev.binding) !== preparedHtlcKey(e.binding)) { out.push(e); continue; }
      if (canon(prev) !== canon(e)) return htlcReject(`HTLC_PREPARED_BINDING_CONFLICT:${e.binding.accountFrameHash}:${e.binding.hashlock}`);
    }
    return ok(out);
  });
};
/**
 * og requireEntityEncryptionPrivateKey + assertEntityEncryptionKeypair (start.ts startInboundLayerPriming, replay.ts assertHtlcPreparedInfraContext):
 * every proposal and every replayed frame needs this validator's Entity key, and it must derive the committed public key, whatever the frame's txs.
 * Plain Errors in og, so the whole input fails. An Entity with no committed public key never exists in og (registration always provisions one).
 */
const entityKeypair = (state: EntityState, ctx: EntityContext): Result<void, EntityError> => {
  const publicKey = state.committed["entityEncryptionPublicKey"];
  if (publicKey === undefined) return ok(undefined);
  const privateKey = ctx.htlc?.encryptionPrivateKey;
  if (privateKey === undefined || privateKey === "") return invariant(`ENTITY_ENCRYPTION_PRIVATE_KEY_UNAVAILABLE:entity=${state.id}`);
  const checked = chain(x25519KeyBytes(String(publicKey), "HTLC_ENTITY_ENCRYPTION_PUBLIC_KEY_INVALID"), (pub) => chain(x25519KeyBytes(privateKey, "HTLC_ENTITY_ENCRYPTION_PRIVATE_KEY_INVALID"),
    (priv): Result<void, OnionError> => (bytesToHex(x25519.getPublicKey(priv)) === bytesToHex(pub) ? ok(undefined) : onionErr("HTLC_ENTITY_ENCRYPTION_KEYPAIR_MISMATCH"))));
  return checked.ok ? ok(undefined) : invariant(checked.error.code);
};
const htlcFrameTxs = (txs: readonly EntityTx[]): boolean => txs.some((tx) => tx.type === "htlcPayment" || (tx.type === "accountInput" && tx.data.kind === "ack_frame" && tx.data.frame.txs.some((a) => a.type === "htlc_lock" && a.envelope !== undefined)));
/** og entityEncryptionPrivateKey (registration/entity-creation/crypto.ts): HKDF-SHA256(seed, salt=entityId, "xln:entity-encryption:v1"). */
export const entityEncryptionPrivateKey = (seed: string, entity: string): string => bytesToHex(hkdf(sha256, hexToBytes(seed), utf8(lower(entity)), utf8("xln:entity-encryption:v1"), 32)).toLowerCase();
/** og createOnlineObservation: a peer without a known Profile is offline and never asserted; every other answer is recorded once. */
const onlineObserver = (infra: HtlcProposerInfra | undefined): { readonly online: (entityId: string) => boolean; readonly observed: Map<string, boolean> } => {
  const observed = new Map<string, boolean>(), known = new Set((infra?.profiles ?? []).map((b) => lower((b as { readonly entityId?: unknown } | null)?.entityId)));
  return {
    observed,
    online: (entityId) => {
      const id = entityId.toLowerCase(), seen = observed.get(id);
      if (seen !== undefined) return seen;
      if (!known.has(id)) return false;
      const up = infra?.online?.(id) === true;
      observed.set(id, up);
      return up;
    },
  };
};
// og paybook writes (books/book-intents.ts) and returned Account work (AccountTxTarget): the Entity-local hashlock-keyed payment machine.
/** One Account tx the Entity returns to an Account after a committed frame (og AccountTxTarget). */
export type AccountTxTarget = { readonly accountId: string; readonly tx: AccountTx };
/** `runtimeEvents`: og's Htlc* candidateEffects runtime events, in emission order. */
export type PaybookFlow = { readonly paybook: Paybook; readonly queue: readonly AccountTxTarget[]; readonly runtimeEvents?: readonly EntityRuntimeEvent[] | undefined };
const emitRuntime = (f: PaybookFlow, eventName: string, data: { readonly [k: string]: unknown }): PaybookFlow => ({ ...f, runtimeEvents: [...(f.runtimeEvents ?? []), { eventName, data }] });
/**
 * og protocol/htlc/events.ts buildHtlcReceivedEventPayload / buildHtlcFinalizedEventPayload common fields, in og's key order. og's jurisdictionId
 * is `config.jurisdiction.name` (else the Runtime's activeJurisdiction), trimmed; an empty one is omitted.
 */
const htlcEventFields = (a: { readonly entityId: string; readonly fromEntity?: string | undefined; readonly toEntity?: string | undefined; readonly hashlock: string; readonly secret?: string | undefined; readonly lockId?: string | undefined; readonly amount?: bigint | undefined; readonly tokenId?: number | undefined; readonly jurisdictionId?: string | undefined; readonly description?: string | undefined }): { readonly [k: string]: unknown } => ({
  entityId: a.entityId, ...(a.fromEntity ? { fromEntity: a.fromEntity } : {}), ...(a.toEntity ? { toEntity: a.toEntity } : {}), hashlock: a.hashlock, ...(a.secret ? { secret: a.secret } : {}), ...(a.lockId ? { lockId: a.lockId } : {}),
  ...(a.amount !== undefined ? { amount: a.amount.toString() } : {}), ...(a.tokenId !== undefined ? { tokenId: a.tokenId } : {}), ...(a.jurisdictionId ? { jurisdictionId: a.jurisdictionId } : {}), ...(a.description ? { description: a.description } : {}),
});
/** og jurisdictionIdFor / getJurisdictionId: the Entity's jurisdiction name, trimmed. */
const htlcJurisdictionId = (state: EntityState, active?: string): string => String(state.jurisdictionConfig?.name || active || "").trim();
/** og buildHtlcReceivedTimingFields / buildHtlcFinalizedTimingFields. */
const receivedTiming = (startedAtMs: number | undefined, receivedAtMs: number): { readonly [k: string]: number } => (!startedAtMs ? { receivedAtMs } : { startedAtMs, receivedAtMs, elapsedMs: Math.max(1, receivedAtMs - startedAtMs) });
const finalizedTiming = (startedAtMs: number | undefined, finalizedAtMs: number): { readonly [k: string]: number } => {
  if (!startedAtMs) return { finalizedAtMs };
  const elapsedMs = Math.max(1, finalizedAtMs - startedAtMs);
  return { startedAtMs, finalizedAtMs, elapsedMs, finalizedInMs: elapsedMs };
};
const putPaybookEntry = (f: PaybookFlow, entry: PaybookEntry): PaybookFlow => ({ ...f, paybook: { ...f.paybook, entries: mapSet(f.paybook.entries, entry.hashlock, entry) } });
const pushTarget = (f: PaybookFlow, accountId: string, tx: AccountTx): PaybookFlow => ({ ...f, queue: [...f.queue, { accountId, tx }] });
/** og programPaymentTermination. */
const terminatePaybookEntry = (f: PaybookFlow, hashlock: string): PaybookFlow => (f.paybook.entries.has(hashlock) ? { ...f, paybook: { ...f.paybook, entries: mapDelete(f.paybook.entries, hashlock) } } : f);
const hasInbound = (e: PaybookEntry): boolean => typeof e.inboundEntity === "string" && e.inboundEntity.length > 0;
/** og applyCommittedHtlcResolveFollowup (both roles, per committed frame): a committed secret closes the route leg it resolves. */
export const resolveFollowup = (f0: PaybookFlow, peer: string, tx: Extract<WireAccountTx, { type: "htlc_resolve" }>, self = "", timestamp = 0, jurisdictionId = ""): Result<PaybookFlow, EntityError> => {
  if (tx.outcome !== "secret") return ok(f0);
  const hashlock = hashHtlcSecret(tx.secret);
  if (hashlock !== tx.lockId.toLowerCase()) return htlcReject(`PAYBOOK_RESOLVE_ID_MISMATCH:${tx.lockId}:${hashlock}`);
  const route = f0.paybook.entries.get(hashlock);
  if (route === undefined) return ok(f0);
  const cp = peer.toLowerCase(), inbound = route.inboundEntity?.toLowerCase() === cp, outbound = route.outboundEntity?.toLowerCase() === cp;
  const originatedOutbound = outbound && (route.originated === true || !hasInbound(route));
  const forwardedOutbound = outbound && hasInbound(route) && typeof route.outboundEntity === "string" && route.outboundEntity.length > 0 && route.originated !== true;
  if (!inbound && !originatedOutbound && !forwardedOutbound) return ok(f0);
  const common = { lockId: tx.lockId, amount: route.amount, tokenId: route.tokenId, jurisdictionId, description: route.description };
  const received = inbound ? emitRuntime(f0, "HtlcReceived", { ...htlcEventFields({ ...common, entityId: self, fromEntity: peer, toEntity: self, hashlock }), ...receivedTiming(route.startedAtMs, timestamp) }) : f0;
  if (forwardedOutbound) return ok(received);
  // og emitOriginatedHtlcFinalized: only the committed outbound leg of an originated payment finalizes it
  const f = originatedOutbound && !(route.originated !== true && hasInbound(route)) && route.hashlock === tx.lockId
    ? emitRuntime(received, "HtlcFinalized", { ...htlcEventFields({ ...common, entityId: self, fromEntity: self, toEntity: route.outboundEntity, hashlock: route.hashlock, secret: tx.secret }), ...finalizedTiming(route.startedAtMs, timestamp) }) : received;
  if (route.originated === true && hasInbound(route)) {
    const settled: PaybookEntry = { ...route, ...(inbound ? { inboundSettled: true as const } : {}), ...(originatedOutbound ? { outboundSettled: true as const } : {}) };
    if (settled.inboundSettled !== true || settled.outboundSettled !== true) return ok(putPaybookEntry(f, settled));
  }
  return ok(terminatePaybookEntry(f, hashlock));
};
/** The committed Account facts a receiver checks a prepared entry against (og ctx.input + the committed frame). */
export type InboundLockFacts = { readonly from: string; readonly to: string; readonly domain: Domain; readonly frame: Pick<AccountFrame, "height" | "stateHash"> };
/** og applyCommittedHtlcLockFollowup (receiver only): consume the frame's prepared entry once, check its binding, then reject, pay out or forward. */
export const lockFollowup = (f: PaybookFlow, m: InboundLockFacts, lock: HtlcLockTx, entries: ReadonlyMap<string, PreparedHtlcEntry>, consumed: Set<string>, timestamp: number, self = ""): Result<PaybookFlow, EntityError> => {
  const envelopeHash = htlcEnvelopeHash(lock.envelope), frame = m.frame;
  if (envelopeHash === null) return htlcReject(`HTLC_ONION_ENCRYPTED_LAYER_REQUIRED:${lock.lockId}`);
  const key = `${frame.stateHash.toLowerCase()}:${lock.lockId.toLowerCase()}`, prepared = entries.get(key);
  if (prepared === undefined) return htlcReject(`HTLC_PREPARED_CONTEXT_REQUIRED:${lock.lockId}`);
  const b = prepared.binding, bindingKey = preparedHtlcKey(b);
  if (consumed.has(bindingKey)) return htlcReject(`HTLC_PREPARED_CONTEXT_REUSED:${bindingKey}`);
  consumed.add(bindingKey);
  const domain = domainOf(m.domain);
  if (b.fromEntityId !== m.from.toLowerCase() || b.toEntityId !== m.to.toLowerCase() || b.accountHeight !== Number(frame.height) || b.accountFrameHash !== frame.stateHash.toLowerCase() || b.envelopeHash !== envelopeHash
    || !domain.ok || domain.value.chainId !== b.domain.chainId || domain.value.depositoryAddress !== b.domain.depositoryAddress || b.hashlock !== lock.hashlock.toLowerCase() || b.tokenId !== Number(lock.tokenId)
    || b.amount !== lock.amount || b.timelock !== lock.timelock || b.revealBeforeHeight !== Number(lock.revealBeforeHeight)) return htlcReject(`HTLC_PREPARED_BINDING_MISMATCH:${lock.lockId}`);
  if (lock.lockId.toLowerCase() !== lock.hashlock.toLowerCase()) return htlcReject(`PAYBOOK_LOCK_ID_MUST_EQUAL_HASHLOCK:${lock.lockId}:${lock.hashlock}`);
  const inboundEntity = m.from.toLowerCase(), existing = f.paybook.entries.get(lock.hashlock), outcome = prepared.outcome;
  const closesSelfCycle = outcome.kind === "final" && existing?.originated === true && !hasInbound(existing);
  if ((existing !== undefined && !closesSelfCycle) || outcome.kind === "reject") {
    const reason = existing !== undefined ? "hashlock_already_active" : outcome.kind === "reject" ? outcome.reason : "hashlock_already_active";
    return ok(pushTarget(f, inboundEntity, { type: "htlc_resolve", lockId: lock.lockId, outcome: "error", reason }));
  }
  if (outcome.kind === "final") {
    const paid = closesSelfCycle && existing !== undefined ? putPaybookEntry(f, { ...existing, inboundEntity })
      : putPaybookEntry(f, { hashlock: lock.hashlock, tokenId: Number(lock.tokenId), amount: lock.amount, ...opt("startedAtMs", outcome.startedAtMs), ...(outcome.description ? { description: outcome.description } : {}), inboundEntity, createdTimestamp: timestamp });
    return ok(pushTarget(paid, inboundEntity, { type: "htlc_resolve", lockId: lock.lockId, outcome: "secret", secret: outcome.secret }));
  }
  const forwarding = emitRuntime(putPaybookEntry(f, { hashlock: lock.hashlock, tokenId: Number(lock.tokenId), amount: lock.amount, inboundEntity, outboundEntity: outcome.nextHopEntityId, pendingFee: lock.amount - outcome.forwardAmount, createdTimestamp: timestamp }),
    "HtlcForwardAccepted", { entityId: self, hashlock: lock.hashlock });
  return ok(pushTarget(forwarding, outcome.nextHopEntityId, {
    type: "htlc_lock", lockId: lock.hashlock, hashlock: lock.hashlock, tokenId: lock.tokenId, amount: outcome.forwardAmount, timelock: lock.timelock - BigInt(HTLC_TIMELOCK_DELTA_MS),
    revealBeforeHeight: lock.revealBeforeHeight - BigInt(HTLC_REVEAL_DELTA_BLOCKS), envelope: outcome.innerEnvelope,
  }));
};
/** og HTLC_SECRET_ACK_TIMEOUT_MS (paybook/lifecycle.ts). */
export const HTLC_SECRET_ACK_TIMEOUT_MS = 120_000;
/** og applyHtlcTimeoutFollowups: a failed lock fails its route upstream (downstream_error) or ends an originated payment; the entry terminates. */
export const timeoutFollowup = (f: PaybookFlow, hashlock: string, self = ""): PaybookFlow => {
  const route = f.paybook.entries.get(hashlock);
  if (route === undefined) return f;
  const failed = hasInbound(route) ? pushTarget(f, route.inboundEntity as string, { type: "htlc_resolve", lockId: hashlock, outcome: "error", reason: "downstream_error" })
    : emitRuntime(f, "HtlcFailed", { hashlock, lockId: hashlock, reason: "timeout", entityId: self, ...(route.description ? { description: route.description } : {}) });
  return terminatePaybookEntry(failed, hashlock);
};
/** og applyHtlcSecretFollowups: record the preimage once, earn the pending fee, pass the secret upstream and arm its ACK deadline, or end an originated payment. */
export const secretFollowup = (f: PaybookFlow, hashlock: string, secret: string, timestamp: number, self = "", jurisdictionId = ""): PaybookFlow => {
  const route = f.paybook.entries.get(hashlock);
  if (route === undefined || (route.secret !== undefined && route.secret !== "")) return f;
  const earns = route.pendingFee !== undefined && route.pendingFee !== 0n;
  const { pendingFee: _fee, ...rest } = route, learned: PaybookEntry = earns ? { ...rest, secret } : { ...route, secret };
  const earned: PaybookFlow = earns ? { ...f, paybook: { ...f.paybook, feesEarned: f.paybook.feesEarned + (route.pendingFee as bigint) } } : f;
  if (hasInbound(route)) {
    const armed: PaybookEntry = { ...learned, secretAckPending: true, secretAckStartedAt: timestamp, secretAckDeadlineAt: timestamp + HTLC_SECRET_ACK_TIMEOUT_MS };
    return pushTarget(putPaybookEntry(earned, armed), route.inboundEntity as string, { type: "htlc_resolve", lockId: hashlock, outcome: "secret", secret });
  }
  return emitRuntime(terminatePaybookEntry(putPaybookEntry(earned, learned), hashlock), "HtlcFinalized", {
    ...htlcEventFields({ entityId: self, fromEntity: self, toEntity: route.outboundEntity, hashlock, secret, lockId: hashlock, amount: route.amount, tokenId: route.tokenId, jurisdictionId, description: route.description }), ...finalizedTiming(route.startedAtMs, timestamp),
  });
};
/** A committed Account frame as the HTLC followups see it: our own frame (ACKed by the peer) or the peer's frame we just signed. */
export type CommittedHtlcFrame = { readonly frame: Pick<AccountFrame, "height" | "stateHash" | "txs">; readonly viaNewFrame: boolean };
/**
 * og applyCommittedFrameTransactions + applyCommittedHtlcFollowups for one Account input: per committed frame the resolve followups then the
 * receiver-only lock followups; then every timed-out lock of the committed frames; then the preimages of the peer frame.
 */
export const paybookFollowups = (f: PaybookFlow, peer: string, frames: readonly CommittedHtlcFrame[], received: Omit<InboundLockFacts, "frame"> | undefined, entries: readonly PreparedHtlcEntry[], timestamp: number, self = "", jurisdictionId = ""): Result<PaybookFlow, EntityError> => {
  const byKey = new Map(entries.map((e) => [preparedHtlcKey(e.binding), e])), consumed = new Set<string>();
  return map(foldResult(frames, f, (acc, c) => paybookFrameFollowups(acc, peer, c, received, byKey, consumed, timestamp, self, jurisdictionId)), (followed) => paybookTailFollowups(followed, frames, timestamp, self, jurisdictionId));
};
/** One committed frame of paybookFollowups: its resolve followups, then (receiver only) its lock followups. */
const paybookFrameFollowups = (f: PaybookFlow, peer: string, { frame, viaNewFrame }: CommittedHtlcFrame, received: Omit<InboundLockFacts, "frame"> | undefined, byKey: ReadonlyMap<string, PreparedHtlcEntry>, consumed: Set<string>, timestamp: number, self: string, jurisdictionId: string): Result<PaybookFlow, EntityError> =>
  chain(foldResult(frame.txs, f, (a, tx) => (tx.type === "htlc_resolve" ? resolveFollowup(a, peer, tx, self, timestamp, jurisdictionId) : ok(a))), (resolved) =>
    !viaNewFrame || received === undefined ? ok(resolved)
      : foldResult(frame.txs, resolved, (a, tx) => (tx.type === "htlc_lock" && tx.envelope !== undefined ? lockFollowup(a, { ...received, frame }, tx, byKey, consumed, timestamp, self) : ok(a))));
/** og applyHtlcTimeoutFollowups then applyHtlcSecretFollowups, after every committed frame: the timed-out locks, then the peer frame's preimages. */
const paybookTailFollowups = (f: PaybookFlow, frames: readonly CommittedHtlcFrame[], timestamp: number, self: string, jurisdictionId: string): PaybookFlow => {
  const timedOut = frames.flatMap(({ frame }) => frame.txs.flatMap((tx) => (tx.type === "htlc_resolve" && tx.outcome === "error" ? [tx.lockId.toLowerCase()] : [])));
  const secrets = frames.flatMap(({ frame, viaNewFrame }) => (viaNewFrame ? frame.txs.flatMap((tx) => (tx.type === "htlc_resolve" && tx.outcome === "secret" ? [{ hashlock: tx.lockId.toLowerCase(), secret: tx.secret }] : [])) : []));
  return secrets.reduce((a, s) => secretFollowup(a, s.hashlock, s.secret, timestamp, self, jurisdictionId), timedOut.reduce((a, h) => timeoutFollowup(a, h, self), f));
};
/** og applyLocalAccountEffects: each returned Account tx is admitted alone; a missing, frozen or refusing Account drops it silently. */
const queueReturned = (d: Draft, target: AccountTxTarget): Draft => {
  const peer = target.accountId.toLowerCase() as EntityId, child = d.accountReplicas.get(peer);
  if (child === undefined) return d;
  const admitted = admitAt(child, [target.tx], d.state.id, L0_CLOCK);
  return admitted.ok ? { ...d, ...putChild(d.state, d.accountReplicas, peer, admitted.value) } : d;
};
// ---- og entity/tx/handlers/payments/settle.ts: settle_propose / update / approve / execute / reject, deferred approvals, committed auto-approval, continuations ----
/** og hasPendingSettlementTransition: a settle_transition queued in the mempool or inside our pending frame. */
const settlePending = (c: AccountReplica): boolean => c.mempool.some((tx) => tx.type === "settle_transition") || (c._tag === "proposed" && c.candidate.frame.txs.some((tx) => tx.type === "settle_transition"));
const bodyReason = (e: BodyError): string => (e._tag === "settlement" ? e.reason : e._tag);
const bodyInvariant = <X,>(r: Result<X, BodyError>): Result<X, EntityError> => mapErr(r, (e): EntityError => ({ _tag: "entity_invariant", reason: bodyReason(e) }));
const settleSay = (d: Draft, message: string): Draft => ({ ...d, events: [...(d.events ?? []), status(message)] });
/** og EntityState.deferredAccountProposals / settlementContinuations: live maps in `committed`; the root commits them as entity collections (entityRootOf). */
const liveMap = <V,>(v: Binary | undefined): ReadonlyMap<string, V> => (v instanceof Map ? (v as ReadonlyMap<string, V>) : new Map());
const deferredOf = (state: EntityState): ReadonlyMap<string, string> => liveMap(state.committed["deferredAccountProposals"]);
const withDeferred = (state: EntityState, m: ReadonlyMap<string, string>): EntityState => ({ ...state, committed: { ...state.committed, deferredAccountProposals: m as unknown as Binary } });
/** og deferredAccountProposals.set with the conflicting-hash guard. */
const deferApproval = (state: EntityState, peer: string, hash: string, code: string): Result<EntityState, EntityError> => {
  const existing = deferredOf(state).get(peer);
  return existing !== undefined && existing !== hash ? invariant(`${code}:${existing}:${hash}`) : ok(withDeferred(state, mapSet(deferredOf(state), peer, hash)));
};
export type SettlementContinuationAction =
  | { readonly type: "r2r"; readonly toEntityId: string; readonly tokenId: number; readonly amount: bigint }
  | { readonly type: "r2e"; readonly receivingEntity: string; readonly tokenId: number; readonly amount: bigint }
  | { readonly type: "r2c"; readonly counterpartyId: string; readonly receivingEntityId?: string | undefined; readonly tokenId: number; readonly amount: bigint };
export type SettlementContinuationPlan = { readonly actions: readonly SettlementContinuationAction[]; readonly broadcast: boolean };
type PendingContinuation = SettlementContinuationPlan & { readonly workspaceHash: string };
const continuationsOf = (state: EntityState): ReadonlyMap<string, PendingContinuation> => liveMap(state.committed["settlementContinuations"]);
const withContinuations = (state: EntityState, m: ReadonlyMap<string, PendingContinuation>): EntityState => ({ ...state, committed: { ...state.committed, settlementContinuations: m as unknown as Binary } });
/** og assertSettlementContinuation: at most one action, a boolean broadcast, a safe token id, a positive amount, lowercase bytes32 entities. */
const continuationIssue = (c: SettlementContinuationPlan): string | undefined => {
  if (!Array.isArray(c.actions)) return "SETTLEMENT_CONTINUATION_ACTIONS_INVALID";
  if (c.actions.length > 1) return `SETTLEMENT_CONTINUATION_ACTION_LIMIT_EXCEEDED:${c.actions.length}`;
  if (typeof c.broadcast !== "boolean") return "SETTLEMENT_CONTINUATION_BROADCAST_INVALID";
  for (const [i, a] of c.actions.entries()) {
    if (!Number.isSafeInteger(a.tokenId) || a.tokenId < 0) return `SETTLEMENT_CONTINUATION_TOKEN_INVALID:${i}`;
    if (typeof a.amount !== "bigint" || a.amount <= 0n) return `SETTLEMENT_CONTINUATION_AMOUNT_INVALID:${i}`;
    const ids = a.type === "r2r" ? [a.toEntityId] : a.type === "r2e" ? [a.receivingEntity] : [a.counterpartyId, ...(a.receivingEntityId ? [a.receivingEntityId] : [])];
    if (ids.some((id) => !ENTITY_WORD.test(id))) return `SETTLEMENT_CONTINUATION_ENTITY_INVALID:${i}`;
  }
  return undefined;
};
type SettleTarget = { readonly child: AccountReplica; readonly iAmLeft: boolean };
const settleChild = (d: Draft, peer: EntityId): SettleTarget | undefined => { const child = d.accountReplicas.get(peer); return child === undefined ? undefined : { child, iAmLeft: isLeft(d.state.id, replicaId(child)) }; };
/** og compileOps on the proposer path: a refusal is a plain Error (the whole input). */
const opsCheck = (ops: readonly SettlementOp[], isLeftSide: boolean): Result<void, EntityError> => bodyInvariant(map(compileOps(ops, isLeftSide), () => undefined));
type SettleEnqueue = (d: Draft, peer: EntityId, tx: AccountTx) => Result<Draft, EntityError>;
/** og handleSettlePropose: skip an existing workspace; otherwise queue a revision-1 upsert (the proposer executes by default) and pin an optional continuation. */
const settlePropose = (d: Draft, x: Extract<EntityTx, { type: "settle_propose" }>["data"], queue: SettleEnqueue): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId, t = settleChild(d, peer);
  if (t === undefined) return invariant(`No account with ${peer.slice(-4)}`);
  if (t.child.state.settlement !== undefined) return ok(settleSay(d, `⏭️ Settlement propose skipped: workspace already exists (v${t.child.state.settlement.revision})`));
  if (settlePending(t.child)) return invariant("SETTLEMENT_TRANSITION_ALREADY_PENDING");
  return chain(opsCheck(x.ops, t.iAmLeft), () => {
    const executorIsLeft = x.executorIsLeft ?? t.iAmLeft, c = x.continuation;
    const pinned: Result<Draft, EntityError> = c === undefined ? ok(d) : (() => {
      const issue = continuationIssue(c);
      if (issue !== undefined) return invariant(issue);
      if (executorIsLeft !== t.iAmLeft) return invariant("SETTLEMENT_CONTINUATION_REQUIRES_LOCAL_EXECUTOR");
      if (continuationsOf(d.state).has(peer)) return invariant(`SETTLEMENT_CONTINUATION_ALREADY_PENDING:${peer}`);
      return map(bodyInvariant(workspaceHashOf(t.child.state.account.id, { revision: 1, ops: x.ops, lastModifiedByLeft: t.iAmLeft, executorIsLeft, memo: x.memo })), (workspaceHash): Draft =>
        ({ ...d, state: withContinuations(d.state, mapSet(continuationsOf(d.state), peer, { workspaceHash, actions: c.actions.map((a) => ({ ...a })), broadcast: c.broadcast })) }));
    })();
    return chain(pinned, (p) => map(queue(p, peer, { type: "settle_transition", kind: "upsert", revision: 1, ops: x.ops, executorIsLeft, ...opt("memo", x.memo) }), (q) => settleSay(q, "⚖️ Settlement proposal queued for bilateral Account consensus")));
  });
};
/** og handleSettleUpdate: an unsigned workspace is replaced by an exact previous-hash revision+1 upsert. */
const settleUpdate = (d: Draft, x: Extract<EntityTx, { type: "settle_update" }>["data"], queue: SettleEnqueue): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId, t = settleChild(d, peer), w = t?.child.state.settlement;
  if (t === undefined) return invariant(`No account with ${peer.slice(-4)}`);
  if (w === undefined) return invariant("No settlement workspace to update. Use settle_propose first.");
  if (settlePending(t.child)) return invariant("SETTLEMENT_TRANSITION_ALREADY_PENDING");
  if (w.leftHanko !== undefined || w.rightHanko !== undefined) return invariant("Cannot update after signing. Use settle_reject to start over.");
  return chain(opsCheck(x.ops, t.iAmLeft), () => chain(bodyInvariant(canonicalWorkspaceHash(t.child.state, w)), (previousWorkspaceHash) => {
    const revision = w.revision + 1, memo = x.memo !== undefined ? x.memo : w.memo;
    return map(queue(d, peer, { type: "settle_transition", kind: "upsert", revision, previousWorkspaceHash, ops: x.ops, executorIsLeft: x.executorIsLeft ?? w.executorIsLeft, ...opt("memo", memo) }), (q) => settleSay(q, `⚖️ Settlement update v${revision} queued for bilateral Account consensus`));
  }));
};
/** og handleSettleReject: an unsigned workspace is cleared by an exact Account-frame clear; no workspace is a silent no-op. */
const settleReject = (d: Draft, x: Extract<EntityTx, { type: "settle_reject" }>["data"], queue: SettleEnqueue): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId, t = settleChild(d, peer), w = t?.child.state.settlement;
  if (t === undefined) return invariant(`No account with ${peer.slice(-4)}`);
  if (w === undefined) return ok(d);
  if (settlePending(t.child)) return invariant("SETTLEMENT_TRANSITION_ALREADY_PENDING");
  if (w.settlementHash || w.leftHanko || w.rightHanko || w.postSettlementDisputeProof) return invariant("SETTLEMENT_REJECT_SIGNED_FORBIDDEN");
  return chain(bodyInvariant(canonicalWorkspaceHash(t.child.state, w)), (workspaceHash) =>
    map(queue(d, peer, { type: "settle_transition", kind: "clear", revision: w.revision, workspaceHash }), (q) => settleSay(q, `❌ Settlement clear queued${x.reason ? `: ${x.reason}` : ""}`)));
};
/** og handleSettleApprove: the approval is deferred until the Account is idle (materializeDeferredSettlementApprovals signs it). */
const settleApprove = (d: Draft, x: Extract<EntityTx, { type: "settle_approve" }>["data"]): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId, t = settleChild(d, peer), w = t?.child.state.settlement;
  if (t === undefined) return invariant(`No account with ${peer.slice(-4)}`);
  if (w === undefined) return invariant("No settlement workspace to approve.");
  if (settlePending(t.child)) return invariant("SETTLEMENT_TRANSITION_ALREADY_PENDING");
  if (w.status === "submitted") return ok(settleSay(d, "⏭️ settle_execute skipped: workspace already submitted"));
  return chain(bodyInvariant(canonicalWorkspaceHash(t.child.state, w)), (hash) => x.workspaceHash !== hash ? invariant(`SETTLEMENT_APPROVAL_WORKSPACE_HASH_MISMATCH:${x.workspaceHash}:${hash}`)
    : map(deferApproval(d.state, peer, hash, "SETTLEMENT_APPROVAL_ALREADY_DEFERRED"), (state) => settleSay({ ...d, state }, "⚖️ Settlement approval accepted; waiting for prior Account work")));
};
type OgSettlementDiff = { readonly tokenId: number; readonly leftDiff: bigint; readonly rightDiff: bigint; readonly collateralDiff: bigint; readonly ondeltaDiff: bigint };
type OgSettlementRow = { readonly leftEntity: string; readonly rightEntity: string; readonly diffs: readonly OgSettlementDiff[]; readonly forgiveDebtsInTokenIds: readonly number[]; readonly sig: string; readonly nonce: number };
type OgC2RRow = { readonly counterparty: string; readonly tokenId: number; readonly amount: bigint; readonly nonce: number; readonly sig: string };
const ogBatchOps = (b: CommittedJBatch["batch"]): number => BATCH_FIELDS.reduce((n, f) => n + (b[f] ?? []).length, 0);
/** og detectPureC2R: one negative-collateral diff that only pays the withdrawer's reserve. */
const pureC2R = (diffs: readonly OgSettlementDiff[], forgive: readonly number[]): { readonly withdrawer: "left" | "right"; readonly tokenId: number; readonly amount: bigint } | undefined => {
  const d = diffs[0];
  if (diffs.length !== 1 || forgive.length > 0 || d === undefined || d.collateralDiff >= 0n) return undefined;
  const amount = -d.collateralDiff;
  if (d.leftDiff === amount && d.rightDiff === 0n && d.ondeltaDiff === -amount) return { withdrawer: "left", tokenId: d.tokenId, amount };
  return d.leftDiff === 0n && d.rightDiff === amount && d.ondeltaDiff === 0n ? { withdrawer: "right", tokenId: d.tokenId, amount } : undefined;
};
/** og batchAddSettlement on the committed (og-shaped) jBatchState: exact retries are ignored, a conflict throws, a pure C2R the initiator withdraws is compressed into collateralToReserve. */
export const addSettlementRow = (jb: CommittedJBatch & { readonly status?: string }, row: OgSettlementRow, initiator: string, disableShortcut: boolean): Result<CommittedJBatch, EntityError> => {
  const L = J_BATCH_LIMITS, n = (s: string): string => s.trim().toLowerCase(), tag = `${row.leftEntity.slice(-4)}:${row.rightEntity.slice(-4)}`, b = jb.batch;
  if (row.diffs.length > L.maxSettlementDiffs) return invariant(`J_BATCH_LIMIT_EXCEEDED: settlement.diffs ${row.diffs.length}/${L.maxSettlementDiffs}`);
  if (row.forgiveDebtsInTokenIds.length > L.maxSettlementForgivenessIds) return invariant(`J_BATCH_LIMIT_EXCEEDED: settlement.forgiveDebtsInTokenIds ${row.forgiveDebtsInTokenIds.length}/${L.maxSettlementForgivenessIds}`);
  if (row.leftEntity >= row.rightEntity) return invariant(`Settlement entities must be ordered: ${row.leftEntity} >= ${row.rightEntity}`);
  if ((row.diffs.length > 0 || row.forgiveDebtsInTokenIds.length > 0) && (!row.sig || row.sig === "0x")) return invariant(`Settlement ${row.leftEntity.slice(-4)}↔${row.rightEntity.slice(-4)} missing hanko signature`);
  const settlements = (b["settlements"] ?? []) as readonly OgSettlementRow[], c2rs = (b["collateralToReserve"] ?? []) as readonly OgC2RRow[];
  const existing = settlements.find((s) => n(s.leftEntity) === n(row.leftEntity) && n(s.rightEntity) === n(row.rightEntity));
  if (existing !== undefined) {
    const same = existing.diffs.length === row.diffs.length && existing.diffs.every((x, i) => { const y = row.diffs[i]; return y !== undefined && BigInt(x.tokenId) === BigInt(y.tokenId) && x.leftDiff === y.leftDiff && x.rightDiff === y.rightDiff && x.collateralDiff === y.collateralDiff && x.ondeltaDiff === y.ondeltaDiff; })
      && existing.forgiveDebtsInTokenIds.length === row.forgiveDebtsInTokenIds.length && existing.forgiveDebtsInTokenIds.every((x, i) => row.forgiveDebtsInTokenIds[i] !== undefined && BigInt(x) === BigInt(row.forgiveDebtsInTokenIds[i]!))
      && existing.sig.toLowerCase() === row.sig.toLowerCase() && BigInt(existing.nonce) === BigInt(row.nonce);
    return same ? ok(jb) : invariant(`J_BATCH_SETTLEMENT_CONFLICT:${tag}`);
  }
  const c2r = pureC2R(row.diffs, row.forgiveDebtsInTokenIds), withdrawer = c2r === undefined ? undefined : c2r.withdrawer === "left" ? row.leftEntity : row.rightEntity;
  const shortcutCounterparty = c2r === undefined ? undefined : c2r.withdrawer === "left" ? row.rightEntity : row.leftEntity;
  const shortcut = c2r !== undefined && !!row.sig && !disableShortcut && (!initiator || n(initiator) === n(withdrawer!));
  const owner = n(initiator || row.leftEntity), pairCounterparty = shortcut ? shortcutCounterparty : owner === n(row.leftEntity) ? row.rightEntity : owner === n(row.rightEntity) ? row.leftEntity : undefined;
  const prior = pairCounterparty === undefined ? undefined : c2rs.find((op) => n(op.counterparty) === n(pairCounterparty));
  if (prior !== undefined) return shortcut && BigInt(prior.tokenId) === BigInt(c2r!.tokenId) && prior.amount === c2r!.amount && BigInt(prior.nonce) === BigInt(row.nonce) && prior.sig.toLowerCase() === row.sig.toLowerCase() ? ok(jb) : invariant(`J_BATCH_SETTLEMENT_CONFLICT:${tag}`);
  const accumulating = jb.status === "empty" ? { status: "accumulating" } : {};
  if (shortcut) {
    if (ogBatchOps(b) + 1 > L.maxTotalOps) return invariant(`J_BATCH_LIMIT_EXCEEDED: collateralToReserve would exceed total ops ${ogBatchOps(b) + 1}/${L.maxTotalOps}`);
    const op: OgC2RRow = { counterparty: shortcutCounterparty!, tokenId: c2r!.tokenId, amount: c2r!.amount, nonce: row.nonce, sig: row.sig };
    return ok({ ...jb, ...accumulating, batch: { ...b, collateralToReserve: [...c2rs, op] as unknown as readonly Binary[] } });
  }
  if (settlements.length + 1 > L.maxSettlements) return invariant(`J_BATCH_LIMIT_EXCEEDED: settlements ${settlements.length + 1}/${L.maxSettlements}`);
  if (ogBatchOps(b) + 1 > L.maxTotalOps) return invariant(`J_BATCH_LIMIT_EXCEEDED: settlement would exceed total ops ${ogBatchOps(b) + 1}/${L.maxTotalOps}`);
  return ok({ ...jb, ...accumulating, batch: { ...b, settlements: [...settlements, row] as unknown as readonly Binary[] } });
};
const sameDiff = (a: WorkspaceDiff, b: WorkspaceDiff): boolean => a.tokenId === b.tokenId && a.leftDiff === b.leftDiff && a.rightDiff === b.rightDiff && a.collateralDiff === b.collateralDiff && a.ondeltaDiff === b.ondeltaDiff;
/** og prepareSettlementExecution: recompiled diffs equal the cached ones, the signed nonce and hash reproduce, both Hankos and the exact N+1 proof are present. */
const prepareExecution = (state: EntityState, a: AccountBody, w: SettlementWorkspace, dt?: DeltaTransformerRef): Result<SettlementTargets & { readonly nonce: number }, EntityError> => chain(bodyInvariant(compileOps(w.ops, w.lastModifiedByLeft)), ({ diffs }) => {
  const cached = w.compiledDiffs;
  if (cached !== undefined && diffs.length !== cached.length) return invariant(`Recompiled diffs length mismatch: ${diffs.length} vs ${cached.length}`);
  const bad = cached === undefined ? -1 : diffs.findIndex((x, i) => !sameDiff(x, cached[i]!));
  if (bad >= 0) return invariant(`Recompiled diff mismatch at index ${bad}`);
  const nonce = w.nonceAtSign;
  if (typeof nonce !== "number" || !Number.isSafeInteger(nonce) || nonce < 1) return invariant(`SETTLEMENT_SIGNED_NONCE_MISSING:${String(nonce)}`);
  if (!w.settlementHash) return invariant("SETTLEMENT_SIGNED_HASH_MISSING");
  if (state.jurisdiction.depositoryAddress === "" || !state.jurisdictionConfig?.entityProviderAddress) return invariant("SETTLEMENT_JURISDICTION_MISSING");
  const p = w.postSettlementDisputeProof;
  return chain(bodyInvariant(settlementTargets(a, w, nonce, p?.proposerIsLeft ?? w.lastModifiedByLeft, dt)), (t) => {
    if (t.settlementHash.toLowerCase() !== w.settlementHash!.toLowerCase()) return invariant(`SETTLEMENT_SIGNED_HASH_MISMATCH:${w.settlementHash}:${t.settlementHash}`);
    if (w.status !== "ready_to_submit") return invariant(`SETTLEMENT_HANKOS_INCOMPLETE:${w.status}`);
    if (p === undefined || p.nonce !== nonce + 1) return invariant(`POST_SETTLEMENT_PROOF_NONCE_MISMATCH:${String(p?.nonce)}:${nonce + 1}`);
    if (p.proofBodyHash.toLowerCase() !== t.postProof.proofBodyHash.toLowerCase() || p.disputeHash.toLowerCase() !== t.postProof.disputeHash.toLowerCase()) return invariant("POST_SETTLEMENT_PROOF_HASH_MISMATCH");
    if (!p.leftHanko || !p.rightHanko) return invariant("POST_SETTLEMENT_PROOF_HANKO_MISSING");
    return ok({ ...t, nonce });
  });
});
/** og handleSettleExecute: the executor verifies the counterparty's settlement Hanko and both N+1 Hankos, appends the settlement to the draft jBatch, and queues the Account submit. */
const settleExecute = (d: Draft, x: Extract<EntityTx, { type: "settle_execute" }>["data"], verify: Verify, queue: SettleEnqueue, jReplicas?: ReadonlyMap<string, JReplica>): Result<Draft, EntityError> => {
  const peer = x.counterpartyEntityId, t = settleChild(d, peer), w = t?.child.state.settlement, tag = peer.slice(-4);
  if (t === undefined) return ok(settleSay(d, `⏭️ settle_execute skipped: no account with ${tag}`));
  if (w === undefined) return ok(settleSay(d, `⏭️ settle_execute skipped: no workspace with ${tag}`));
  // og rejectFailure: a reject disposition evicts only this tx
  if (settlePending(t.child)) return err({ _tag: "entity_command", reason: "SETTLEMENT_TRANSITION_ALREADY_PENDING" });
  return chain(bodyInvariant(canonicalWorkspaceHash(t.child.state, w)), (workspaceHash): Result<Draft, EntityError> => {
    if (w.status === "submitted") return ok(settleSay(d, "⏭️ settle_execute skipped: settlement already submitted"));
    if (w.executorIsLeft !== t.iAmLeft) return invariant(`SETTLEMENT_EXECUTOR_MISMATCH:expected=${w.executorIsLeft ? "left" : "right"}`);
    const counterpartyHanko = t.iAmLeft ? w.rightHanko : w.leftHanko, { left, right } = t.child.state.account.id;
    if (!counterpartyHanko) return ok(settleSay(d, "⏭️ settle_execute skipped: missing counterparty signature"));
    return chain(prepareExecution(d.state, t.child.state, w, accountDt({ jReplicas }, t.child)), (p) => {
      if (!verify(p.settlementHash, counterpartyHanko, peer)) return invariant("SETTLEMENT_NONEXECUTOR_HANKO_INVALID");
      if (!verify(p.postProof.disputeHash, w.postSettlementDisputeProof!.leftHanko!, left as EntityId)) return invariant("POST_SETTLEMENT_LEFT_HANKO_INVALID");
      if (!verify(p.postProof.disputeHash, w.postSettlementDisputeProof!.rightHanko!, right as EntityId)) return invariant("POST_SETTLEMENT_RIGHT_HANKO_INVALID");
      const jb = (committedJBatch(d.state) ?? (initJBatch() as unknown as CommittedJBatch)) as CommittedJBatch & { readonly status?: string };
      const seeded: Draft = { ...d, state: { ...d.state, committed: { ...d.state.committed, jBatchState: jb as unknown as Binary } } };
      // og: a nonce-bound settlement never waits behind another on-chain batch
      if (jb.sentBatch !== undefined) return ok(settleSay(seeded, "⏭️ settle_execute skipped: jBatch sentBatch pending"));
      const row: OgSettlementRow = { leftEntity: t.iAmLeft ? d.state.id : peer, rightEntity: t.iAmLeft ? peer : d.state.id, diffs: p.diffs, forgiveDebtsInTokenIds: p.forgive, sig: counterpartyHanko, nonce: p.nonce };
      return chain(addSettlementRow(jb, row, d.state.id, x.disableC2RShortcut ?? false), (next) => {
        const batched: Draft = { ...seeded, state: { ...seeded.state, committed: { ...seeded.state.committed, jBatchState: next as unknown as Binary } } };
        return map(queue(batched, peer, { type: "settle_transition", kind: "submit", revision: w.revision, workspaceHash }), (q) => settleSay(q, `✅ Settlement submission queued (${p.diffs.length} diffs) - use j_broadcast to commit`));
      });
    });
  });
};
/** og canAutoApproveWorkspace: never for forgiveness or raw diffs; otherwise every compiled diff passes userAutoApprove. */
export const canAutoApproveWorkspace = (w: Pick<SettlementWorkspace, "ops" | "lastModifiedByLeft">, iAmLeft: boolean): boolean => {
  if (w.ops.some((op) => op.type === "forgive" || op.type === "rawDiff")) return false;
  const compiled = compileOps(w.ops, w.lastModifiedByLeft);
  if (!compiled.ok) return false;
  return compiled.value.diffs.every((diff) => (iAmLeft ? diff.leftDiff : diff.rightDiff) >= 0n && (iAmLeft ? diff.ondeltaDiff : diff.collateralDiff - diff.ondeltaDiff) >= 0n);
};
/** og processCommittedSettlementTransitionFollowup: after a committed frame whose last transition is the peer's upsert / hanko, defer our own signature when the ops are safe (or ours). */
const settleFollowups = (d: Draft, peer: EntityId, frames: readonly { readonly frame: AccountFrame; readonly proposerIsLeft: boolean }[]): Result<Draft, EntityError> =>
  foldResult(frames, d, (acc, { frame, proposerIsLeft }) => foldResult(frame.txs.map((tx, i) => [tx, i] as const), acc, (cur, [tx, i]): Result<Draft, EntityError> => {
    if (tx.type !== "settle_transition" || (tx.kind !== "upsert" && tx.kind !== "hanko")) return ok(cur);
    if (frame.txs.slice(i + 1).some((later) => later.type === "settle_transition")) return ok(cur);
    const child = cur.accountReplicas.get(peer), w = child?.state.settlement;
    if (child === undefined || w === undefined) return invariant("SETTLEMENT_COMMITTED_WORKSPACE_MISSING");
    return chain(bodyInvariant(canonicalWorkspaceHash(child.state, w)), (hash) => {
      if (w.revision !== tx.revision) return invariant(`SETTLEMENT_COMMITTED_VERSION_MISMATCH:${w.revision}:${tx.revision}`);
      const iAmLeft = isLeft(cur.state.id, replicaId(child));
      if (proposerIsLeft === iAmLeft || (iAmLeft ? w.postSettlementDisputeProof?.leftHanko : w.postSettlementDisputeProof?.rightHanko) || settlePending(child)) return ok(cur);
      if (w.lastModifiedByLeft !== iAmLeft && !canAutoApproveWorkspace(w, iAmLeft)) return ok(cur);
      return map(deferApproval(cur.state, peer, hash, "SETTLEMENT_APPROVAL_ALREADY_DEFERRED"), (state) => ({ ...cur, state }));
    });
  }));
/** og buildSettlementHankoDraft: our side's hanko transition at the pinned (or next safe) nonce; own Hankos are the manifest placeholders `installFrame` replaces. */
const settlementHankoDraft = (child: AccountReplica, iAmLeft: boolean, peer: EntityId, dt?: DeltaTransformerRef): Result<{ readonly tx: AccountTx; readonly hashes: readonly HashToSign[] }, EntityError> => {
  const w = child.state.settlement;
  if (w === undefined) return invariant("SETTLEMENT_WORKSPACE_MISSING");
  return chain(bodyInvariant(canonicalWorkspaceHash(child.state, w)), (workspaceHash) => {
    if (w.status === "submitted") return invariant("SETTLEMENT_HANKO_SUBMITTED_FORBIDDEN");
    if (iAmLeft ? w.postSettlementDisputeProof?.leftHanko : w.postSettlementDisputeProof?.rightHanko) return invariant("SETTLEMENT_SIDE_HANKO_ALREADY_ATTACHED");
    const nonce = w.nonceAtSign ?? nextSettlementNonce(child);
    if (!Number.isSafeInteger(nonce) || nonce < 1) return invariant(`SETTLEMENT_SIGNED_NONCE_INVALID:${String(nonce)}`);
    return chain(bodyInvariant(settlementTargets(child.state, w, nonce, w.lastModifiedByLeft, dt)), (t) => {
      if (w.settlementHash && w.settlementHash.toLowerCase() !== t.settlementHash.toLowerCase()) return invariant(`SETTLEMENT_SIGNED_HASH_MISMATCH:${w.settlementHash}:${t.settlementHash}`);
      const pin = w.postSettlementDisputeProof;
      if (pin !== undefined && (pin.nonce !== t.postProof.nonce || pin.proofBodyHash.toLowerCase() !== t.postProof.proofBodyHash.toLowerCase() || pin.disputeHash.toLowerCase() !== t.postProof.disputeHash.toLowerCase())) return invariant("POST_SETTLEMENT_PROOF_PIN_MISMATCH");
      const executor = w.executorIsLeft === iAmLeft, tail = peer.slice(-8);
      const tx: AccountTx = { type: "settle_transition", kind: "hanko", revision: w.revision, workspaceHash, settlementNonce: nonce, settlementHash: t.settlementHash, ...(executor ? {} : { settlementHanko: pendingHanko(t.settlementHash) }), postProof: { ...t.postProof, hanko: pendingHanko(t.postProof.disputeHash) } };
      return ok({ tx, hashes: [...(executor ? [] : [{ hash: t.settlementHash, type: "settlement" as const, context: `settlement:${tail}:nonce:${nonce}` }]), { hash: t.postProof.disputeHash, type: "dispute", context: `settlement:${tail}:post-dispute:nonce:${t.postProof.nonce}` }] });
    });
  });
};
/**
 * og drainPostOrderbookAccountWork before proposePendingAccountFrames: refreshStaleUncommittedSettlementHankos (a stale-nonce hanko intent is
 * dropped and re-deferred), then materializeDeferredSettlementApprovals (each idle Account's deferred approval becomes its hanko transition).
 */
const materializeSettlements = (d: Draft, ctx: FoldContext): Result<Draft, EntityError> => {
  const self = d.state.id;
  let draft = d;
  for (const peer of [...draft.accountReplicas.keys()].sort(asc)) {
    const child = draft.accountReplicas.get(peer)!, w = child.state.settlement;
    if (child.mempool.length === 0 || w === undefined || w.nonceAtSign !== undefined || child._tag === "proposed") continue;
    const hash = canonicalWorkspaceHash(child.state, w);
    if (!hash.ok) return invariant(bodyReason(hash.error));
    const expected = nextSettlementNonce(child), stale = (tx: WireAccountTx): boolean => tx.type === "settle_transition" && tx.kind === "hanko" && tx.revision === w.revision && tx.workspaceHash.toLowerCase() === hash.value && tx.settlementNonce !== expected;
    if (!child.mempool.some(stale)) continue;
    const refreshed = deferApproval(draft.state, peer, hash.value, `SETTLEMENT_REFRESH_DEFERRED_CONFLICT:${peer}`);
    if (!refreshed.ok) return refreshed;
    draft = { ...draft, ...putChild(refreshed.value, draft.accountReplicas, peer, { ...child, mempool: child.mempool.filter((tx) => !stale(tx)) } as AccountReplica) };
  }
  const deferred = deferredOf(draft.state);
  for (const [peer, approved] of [...deferred].sort(([a], [b]) => asc(a, b))) {
    const child = draft.accountReplicas.get(peer as EntityId);
    if (child === undefined) return invariant(`SETTLEMENT_DEFERRED_ACCOUNT_MISSING:${peer}`);
    if (child._tag === "proposed" || settlePending(child)) continue;
    const w = child.state.settlement, current = w === undefined ? undefined : canonicalWorkspaceHash(child.state, w);
    if (current !== undefined && !current.ok) return invariant(bodyReason(current.error));
    if (w === undefined || current?.value !== approved) {
      draft = settleSay({ ...draft, state: withDeferred(draft.state, mapDelete(deferredOf(draft.state), peer)) }, "⚠️ Settlement approval expired because the workspace changed");
      continue;
    }
    // og: once a peer Hanko pins the proof, ordinary txs are frozen and cannot drain; the counter-Hanko goes ahead of them
    if (child.mempool.length > 0 && !(w.settlementHash || w.leftHanko || w.rightHanko || w.postSettlementDisputeProof)) continue;
    const iAmLeft = isLeft(self, replicaId(child)), built = settlementHankoDraft(child, iAmLeft, peer as EntityId, accountDt(ctx, child));
    if (!built.ok) return built;
    const admitted = admitAt(child, [built.value.tx], self, { timestamp: ctx.timestamp, jHeight: entityJHeight(draft.state) }, pendingVerify(ctx.verify, self));
    if (!admitted.ok || admitted.value.mempool.length !== child.mempool.length + 1) return invariant(`SETTLEMENT_DEFERRED_HANKO_NOT_ADMITTED:${peer}`);
    draft = { ...draft, ...putChild({ ...draft.state, ...withDeferred(draft.state, mapDelete(deferredOf(draft.state), peer)) }, draft.accountReplicas, peer as EntityId, admitted.value), hashes: [...(draft.hashes ?? []), ...built.value.hashes], touched: [...(draft.touched ?? []), peer as EntityId] };
  }
  return ok(draft);
};
/**
 * og selectSettlementContinuation + materializeSettlementContinuation: at most one continuation per frame (lowest counterparty), waiting passively
 * until its workspace is ready and the jBatch is idle; a missing / changed / submitted workspace discards it. The execute path runs settle_execute,
 * then og's follow-up r2r / r2e / r2c and j_broadcast Entity txs, and drops the continuation only when all of them succeeded.
 */
const materializeContinuation = (d: Draft, ctx: FoldContext, queue: SettleEnqueue): Result<Draft, EntityError> => {
  const entry = [...continuationsOf(d.state)].sort(([a], [b]) => asc(a, b))[0];
  if (entry === undefined) return ok(d);
  const [peer, c] = entry, child = d.accountReplicas.get(peer as EntityId);
  if (child === undefined) return invariant(`SETTLEMENT_CONTINUATION_ACCOUNT_MISSING:${peer}`);
  if (settlePending(child)) return ok(d);
  const discard = (reason: string): Result<Draft, EntityError> => ok(settleSay({ ...d, state: withContinuations(d.state, mapDelete(continuationsOf(d.state), peer)) }, `Settlement continuation cleared: ${reason.replaceAll("_", " ")}`));
  const w = child.state.settlement;
  if (w === undefined) return discard("workspace_missing");
  return chain(bodyInvariant(canonicalWorkspaceHash(child.state, w)), (hash) => {
    if (hash !== c.workspaceHash) return discard("workspace_changed");
    if (w.status === "submitted") return discard("already_submitted");
    if (w.status !== "ready_to_submit") return ok(d);
    if (w.executorIsLeft !== isLeft(d.state.id, replicaId(child))) return invariant(`SETTLEMENT_CONTINUATION_EXECUTOR_MISMATCH:${peer}`);
    const jb = committedJBatch(d.state);
    if (jb?.sentBatch !== undefined || (jb !== undefined && ogBatchOps(jb.batch) > 0)) return ok(d);
    // og continuationActionToTx: settle_execute (no C2R shortcut when actions follow), the r2r / r2e / r2c follow-ups, then j_broadcast, all as one collective run
    const follow: readonly EntityTx[] = [...c.actions.map((a): EntityTx => a.type === "r2r" ? { type: "r2r", data: { toEntityId: a.toEntityId, tokenId: a.tokenId, amount: a.amount } }
      : a.type === "r2e" ? { type: "r2e", data: { receivingEntity: a.receivingEntity, tokenId: a.tokenId, amount: a.amount } }
      : { type: "r2c", data: { counterpartyId: a.counterpartyId, ...(a.receivingEntityId ? { receivingEntityId: a.receivingEntityId } : {}), tokenId: a.tokenId, amount: a.amount } }), ...(c.broadcast ? [{ type: "j_broadcast", data: {} } satisfies EntityTx] : [])];
    return chain(settleExecute(d, { counterpartyEntityId: peer as EntityId, ...(c.actions.length > 0 ? { disableC2RShortcut: true } : {}) }, ctx.verify, queue, ctx.jReplicas), (x) => {
      const done = (y: Draft): Draft => ({ ...y, state: withContinuations(y.state, mapDelete(continuationsOf(y.state), peer)) });
      if (follow.length === 0) return ok(done(x));
      return map(foldNested(x.state, x.accountReplicas, follow, ctx, "collective"), (y) => done({ ...y, outputs: [...x.outputs, ...y.outputs], events: [...(x.events ?? []), ...(y.events ?? [])], runtimeEvents: [...(x.runtimeEvents ?? []), ...(y.runtimeEvents ?? [])],
        touched: [...(x.touched ?? []), ...(y.touched ?? [])], ...frameEffects(x, y), swaps: joinSwapEvents(x.swaps, y.swaps) }));
    });
  });
};
const settleQueue = (ctx: FoldContext): SettleEnqueue => (d, peer, tx) => withChild(d.accountReplicas, peer, (child) => map(admitAt(child, [tx], d.state.id, L0_CLOCK, ctx.verify), (admitted) => ({ ...d, ...putChild(d.state, d.accountReplicas, peer, admitted) })));
/** A peer frame this Entity just signed, with the Account envelope its receiver HTLC lock followups check the prepared entries against. */
export type ReceivedCommit = { readonly frame: AccountFrame; readonly from: EntityId; readonly to: EntityId; readonly domain: Domain };
type SwapOutputEffect = Of<Effect, "swap_offer_upsert" | "swap_cancelled" | "swap_cancel_requested">;
const swapOutputId = (e: SwapOutputEffect): string => (e._tag === "swap_offer_upsert" ? e.offer.offerId : e.offerId);
/**
 * og applySuccessfulAccountInput after the Account machine, for the committed frames of one accountInput (our frame the peer ACKed, then the peer's
 * frame we signed). applyCommittedFrameTransactions goes frame by frame: applyCommittedAccountFrameFollowups (each tx's lending followup and HTLC
 * resolve), then per tx the settlement auto-approval, the cross-j followup, the receiver's HTLC lock followup and the swap output (the next same-j
 * output in signed tx order, or the committed cross-j offer). Then queueInitialHubPolicies (a hub's new inbound Account), applyCommittedHtlcFollowups
 * (direct-payment forwards, timeouts, preimages) and scheduleCommittedAccountWork (the hub-rebalance-kick hook).
 * The returned Account txs are admitted after all of them, one at a time and in that order (og applyLocalAccountEffects), and each Account that
 * admits one joins the frame's worklist in that order.
 */
export const committedFollowups = (d0: Draft, peer: EntityId, own: AccountFrame | undefined, received: ReceivedCommit | undefined, effects: readonly Effect[], ctx: FoldContext, createdAccount = false): Result<Draft, EntityError> => {
  const self = d0.state.id, child0 = d0.accountReplicas.get(peer), mine = child0 !== undefined && isLeft(self, replicaId(child0)), now = Number(ctx.timestamp);
  const frames: readonly { readonly frame: AccountFrame; readonly viaNewFrame: boolean }[] = [...(own === undefined ? [] : [{ frame: own, viaNewFrame: false }]), ...(received === undefined ? [] : [{ frame: received.frame, viaNewFrame: true }])];
  const forwards = effects.flatMap((e) => (e._tag === "direct_payment_forward" && sameHex(e.route[0], self) ? [e] : []));
  const swapOutputs = effects.flatMap((e): SwapOutputEffect[] => (e._tag === "swap_offer_upsert" || e._tag === "swap_cancelled" || e._tag === "swap_cancel_requested" ? [e] : []));
  const byKey = new Map((ctx.htlc?.entries ?? []).map((e) => [preparedHtlcKey(e.binding), e])), consumed = new Set<string>(), jid = htlcJurisdictionId(d0.state, ctx.activeJurisdiction), paybook0 = d0.state.paybook ?? EMPTY_PAYBOOK;
  let d = d0, targets: readonly AccountTxTarget[] = [], flow: PaybookFlow = { paybook: paybook0, queue: [] }, cross: CommittedCrossStep | undefined, crontab0: Crontab | undefined, cursor = 0;
  let created: readonly SwapOfferEvent[] = [], cancelled: readonly SwapRef[] = [], cancelRequests: readonly SwapRef[] = [];
  for (const c of frames) {
    const lent = lendingFollowups(d.state, d.accountReplicas, peer, [{ frame: c.frame, proposer: c.viaNewFrame ? peer : self }], ctx.timestamp, targets);
    if (!lent.ok) return lent;
    if (lent.value.state !== d.state) d = { ...d, state: lent.value.state };
    targets = lent.value.accountTxs;
    if (c.frame.txs.some((tx) => tx.type === "htlc_lock" || tx.type === "htlc_resolve")) {
      const paid = paybookFrameFollowups({ ...flow, queue: [] }, peer, c, received, byKey, consumed, now, self, jid);
      if (!paid.ok) return paid;
      flow = paid.value;
      targets = [...targets, ...flow.queue];
    }
    for (const tx of c.frame.txs) {
      if (tx.type === "cross_pull_lock" || tx.type === "cross_pull_close") {
        if (cross === undefined) {
          const crontab = crontabOf(d.state);
          if (!crontab.ok) return crontab;
          crontab0 = crontab.value;
          cross = { host: { ...bookHostOf(d.state, d.accountReplicas, ctx.timestamp), auths: d.state.crossJurisdictionAuthorizations, crontab: crontab.value }, outputs: [], messages: [], created: [], handled: false };
        }
        const n = committedCrossFollowup(cross.host, peer, tx, Number(c.frame.timestamp));
        if (!n.ok) return n;
        cross = { ...n.value, outputs: [...cross.outputs, ...n.value.outputs], messages: [...cross.messages, ...n.value.messages], created: [] };
        created = [...created, ...n.value.created];
      } else if (tx.type === "swap_offer" && tx.crossJurisdiction !== undefined) {
        // og applyCommittedCrossJurisdictionSwapFollowup: buildCommittedSwapOfferEvent from the committed offer
        const child = d.accountReplicas.get(peer), offer = child?.state.offers.get(tx.offerId);
        if (child !== undefined && offer !== undefined) created = [...created, { offerId: tx.offerId, accountId: peer, makerIsLeft: offer.makerIsLeft, fromEntity: child.state.account.id.left, toEntity: child.state.account.id.right,
          createdHeight: offer.createdHeight, giveTokenId: Number(offer.giveTokenId), giveTokenDecimals: offer.giveTokenDecimals, giveAmount: offer.giveAmount, wantTokenId: Number(offer.wantTokenId), wantTokenDecimals: offer.wantTokenDecimals,
          wantAmount: offer.wantAmount, maxFee: offer.maxFee, minNetReceive: offer.minNetReceive, priceTicks: offer.priceTicks, ...opt("timeInForce", tif(offer.timeInForce)), ...opt("crossJurisdiction", offer.crossJurisdiction) }];
      } else if (tx.type === "swap_offer" || tx.type === "swap_resolve" || tx.type === "swap_cancel_request") {
        // og consumeSameJurisdictionSwapOutput: the Account's next swap output belongs to this tx
        const e = swapOutputs[cursor], kinds: readonly SwapOutputEffect["_tag"][] = tx.type === "swap_offer" ? ["swap_offer_upsert"] : tx.type === "swap_resolve" ? ["swap_offer_upsert", "swap_cancelled"] : ["swap_cancel_requested"];
        if (e === undefined) return invariant(`ACCOUNT_SWAP_OUTPUT_MISSING:${tx.offerId}`);
        if (!kinds.includes(e._tag)) return invariant(`ACCOUNT_SWAP_OUTPUT_KIND_MISMATCH:${tx.offerId}:${e._tag}`);
        if (swapOutputId(e) !== tx.offerId) return invariant(`ACCOUNT_SWAP_OUTPUT_ID_MISMATCH:${tx.offerId}:${swapOutputId(e)}`);
        cursor += 1;
        const ref: SwapRef = { offerId: tx.offerId, accountId: peer };
        if (e._tag === "swap_offer_upsert") created = [...created, swapOfferEvent(peer, e)];
        else if (e._tag === "swap_cancelled") cancelled = [...cancelled, ref];
        else cancelRequests = [...cancelRequests, ref];
      }
    }
    if (child0 !== undefined) {
      const settled = settleFollowups(d, peer, [{ frame: c.frame, proposerIsLeft: c.viaNewFrame ? !mine : mine }]);
      if (!settled.ok) return settled;
      d = settled.value;
    }
  }
  if (cursor !== swapOutputs.length) return invariant(`ACCOUNT_SWAP_OUTPUT_UNCONSUMED:${swapOutputs.length - cursor}`);
  // og queueInitialHubPolicies: a hub's new inbound Account gets its fee terms for each token its genesis frame adds, ascending
  if (createdAccount) {
    const genesis = received !== undefined && received.frame.height === 1n ? received.frame : undefined, hub = hubConfigOf(d.state);
    if (genesis === undefined) return invariant(`ACCOUNT_GENESIS_COMMIT_REQUIRED:${peer}`);
    const tokens = hub === undefined ? [] : [...new Set(genesis.txs.flatMap((tx) => (tx.type === "add_delta" ? [Number(tx.tokenId)] : [])))].sort((a, b) => a - b);
    const policies = traverse(tokens, (t) => hubPolicyTx(hub as HubConfig, String(t) as TokenId));
    if (!policies.ok) return policies;
    targets = [...targets, ...policies.value.map((tx) => ({ accountId: peer, tx }))];
  }
  // og applyCommittedHtlcFollowups: the direct-payment forwards, then the timed-out locks, then the peer frame's preimages
  for (const f of forwards) {
    const leg = forwardLeg(d, f);
    if (!leg.ok) return leg;
    targets = [...targets, leg.value];
  }
  flow = paybookTailFollowups({ ...flow, queue: [] }, frames, now, self, jid);
  targets = [...targets, ...flow.queue];
  if (cross !== undefined) {
    const s = cross, state: EntityState = { ...(s.host.crontab === undefined || s.host.crontab === crontab0 ? d.state : withCrontab(d.state, s.host.crontab)), ...opt("crossJurisdictionAuthorizations", s.host.auths) };
    d = hostDraft({ ...d, state }, { host: s.host, outputs: s.outputs, messages: s.messages, created: [] }, ctx.timestamp);
  }
  if (flow.paybook !== paybook0) d = { ...d, state: { ...d.state, paybook: flow.paybook } };
  if ((flow.runtimeEvents ?? []).length > 0) d = { ...d, runtimeEvents: [...(d.runtimeEvents ?? []), ...(flow.runtimeEvents ?? [])] };
  // og scheduleCommittedAccountWork: on a hub, an Account with rebalance work kicks the hubRebalance task, unless it already ran this tick
  const child = d.accountReplicas.get(peer);
  if (hubConfigOf(d.state) !== undefined && child !== undefined) {
    const work = hasRebalanceWork(self, child);
    if (!work.ok) return work;
    if (work.value) {
      const crontab = crontabOf(d.state);
      if (!crontab.ok) return crontab;
      const task = crontab.value.tasks.get("hubRebalance");
      if (task === undefined || task.lastRun < now) d = { ...d, state: withCrontab(d.state, scheduleHook(crontab.value, { id: "hub-rebalance-kick", triggerAt: now, type: "hub_rebalance_kick", data: { reason: "account_frame_committed", counterpartyId: peer } })) };
    }
  }
  const touched: EntityId[] = [peer];
  for (const t of targets) {
    const id = t.accountId.toLowerCase() as EntityId, before = d.accountReplicas.get(id)?.mempool.length;
    d = queueReturned(d, t);
    if (before !== undefined && d.accountReplicas.get(id)?.mempool.length !== before) touched.push(id);
  }
  const swaps: SwapEvents | undefined = created.length + cancelled.length + cancelRequests.length === 0 ? undefined : { created, cancelled, cancelRequests };
  return ok({ ...d, touched, ...opt("swaps", joinSwapEvents(d.swaps, swaps)) });
};
const originView = (state: EntityState, replicas: Replicas, timestamp: bigint): HtlcOriginView => ({
  id: state.id, timestamp: Number(timestamp), jHeight: Number(entityJHeight(state)), encryptionKey: String(state.committed["entityEncryptionPublicKey"] ?? ""), paybook: state.paybook ?? EMPTY_PAYBOOK, replicas,
});
// ---- og entity/tx/handlers/cross-j/setup.ts + extensions/cross-j/prepared-route.ts + j-events-htlc/cross-jurisdiction-helpers.ts ----
/** The Entity state a cross-j setup handler reads (og EntityState fields: entityId, timestamp, config.validators/jurisdiction, accounts, both collections). */
export type CrossEntityView = {
  readonly id: EntityId; readonly timestamp: number; readonly validators: readonly string[]; readonly jurisdiction: Domain; readonly jurisdictionName?: string | undefined;
  readonly replicas: Replicas; readonly swaps?: ReadonlyMap<string, CrossRoute> | undefined; readonly auths?: ReadonlyMap<string, CrossRoute> | undefined;
  /** og state.orderbookExt: prices a non-stable leg for the USD cap. */
  readonly ext?: OrderbookExt | undefined;
};
/** og buildCrossJurisdictionEntityOutput / the raw proposer wake: a certified Entity command (or an empty wake) for one target signer. */
export type CrossEntityOutput = { readonly entityId: string; readonly signerId: string; readonly txs: readonly EntityTx[] };
/** og CrossJSetupResult: the collections after the tx, frame messages, Entity outputs, Account work. */
export type CrossSetup = {
  readonly swaps?: ReadonlyMap<string, CrossRoute> | undefined; readonly auths?: ReadonlyMap<string, CrossRoute> | undefined;
  readonly messages: readonly string[]; readonly outputs: readonly CrossEntityOutput[]; readonly accountTxs: readonly AccountTxTarget[];
};
type CrossStep = Result<CrossSetup, EntityError>;
const crossView = (state: EntityState, replicas: Replicas, timestamp: bigint): CrossEntityView => ({
  id: state.id, timestamp: Number(timestamp), validators: rootConfig(state).validators, jurisdiction: state.jurisdiction, jurisdictionName: state.jurisdictionConfig?.name,
  replicas, swaps: state.crossJurisdictionSwaps, auths: state.crossJurisdictionAuthorizations, ext: state.orderbookExt,
});
const crossSetup = (v: CrossEntityView, o: Partial<CrossSetup> = {}): CrossSetup => ({ swaps: v.swaps, auths: v.auths, messages: [], outputs: [], accountTxs: [], ...o });
const crossNote = (v: CrossEntityView, message: string, o: Partial<CrossSetup> = {}): CrossStep => ok(crossSetup(v, { ...o, messages: [message] }));
/** og MalformedEntityFrameInputError (reject: the outer tx is evicted). og haltRuntimeFailure / plain Error map to `invariant` (the input is refused). */
const crossReject2 = (reason: string): Result<never, EntityError> => err({ _tag: "cross_j_entity", reason });
/** og normalizeEntityRef (entity/tx/account-key.ts): lowercase, not trimmed. */
const entityRef = (v: unknown): string => String(v || "").toLowerCase();
const fatalCross = <T,>(r: Result<T, CrossError>): Result<T, EntityError> => (r.ok ? r : invariant(r.error.reason));
/** og exactRouteBytes: safeStringify(cloneCrossJurisdictionRoute(route)). */
const exactRouteBytes = (r: CrossRoute): Result<string, EntityError> => map(fatalCross(cloneCrossRoute(r)), stableJson);
/** og materializedIntentBytes: the route without pulls, at the stored status and update time. */
const materializedIntentBytes = (route: CrossRoute, existing: CrossRoute): Result<string, EntityError> =>
  chain(fatalCross(cloneCrossRoute(route)), ({ sourcePull: _s, targetPull: _t, ...intent }) => exactRouteBytes({ ...intent, status: existing.status, updatedAt: existing.updatedAt }));
/** og mergeCrossJurisdictionRoute. */
const mergeCrossRoute = (existing: CrossRoute | undefined, next: CrossRoute): Result<CrossRoute, EntityError> =>
  chain(fatalCross(cloneCrossRoute(existing ?? next)), (base) => map(fatalCross(cloneCrossRoute(next)), (n) => ({ ...base, ...n })));
/** og validateCrossJurisdictionRouteTransition. */
export const crossRouteTransitionError = (existing: CrossRoute | undefined, next: CrossRoute): string | null => {
  if (existing === undefined) return null;
  if (existing.routeHash && next.routeHash && existing.routeHash.toLowerCase() !== next.routeHash.toLowerCase()) return "route hash mismatch";
  if (isCrossTerminal(existing.status)) return `terminal state ${existing.status}`;
  return crossTransitionAllowed(existing.status, next.status) ? null : `invalid transition ${existing.status}->${next.status}`;
};
/** og boundary.ts isCrossJurisdictionRouteParticipant. */
export const isCrossRouteParticipant = (route: CrossRoute, entityId: string): boolean => {
  const p = trimLower(entityId);
  return p !== "" && [route.source.entityId, route.source.counterpartyEntityId, route.target.entityId, route.target.counterpartyEntityId, route.bookOwnerEntityId, route.hubEntityId].some((c) => trimLower(c) === p);
};
/** og boundary.ts crossJurisdictionRouteSigner: the board signer the route commits for one Entity owner. */
export const crossRouteSigner = (route: CrossRoute, entityId: string): string | null => {
  const t = trimLower(entityId), n = (v: unknown): string | null => trimLower(v) || null;
  if (!t) return null;
  if (trimLower(route.source.entityId) === t) return n(route.sourceSignerId);
  if (trimLower(route.source.counterpartyEntityId) === t) return n(route.sourceHubSignerId);
  if (trimLower(route.target.entityId) === t) return n(route.targetHubSignerId);
  if (trimLower(route.target.counterpartyEntityId) === t) return n(route.targetSignerId);
  const owner = trimLower(route.bookOwnerEntityId || route.source.counterpartyEntityId || route.hubEntityId);
  if (owner !== t) return null;
  if (owner === trimLower(route.source.counterpartyEntityId)) return n(route.sourceHubSignerId);
  return owner === trimLower(route.target.entityId) ? n(route.targetHubSignerId) : null;
};
/** og validateCrossJurisdictionLocalBinding: the local leg's stack ref is this Entity's jurisdiction and its route signer is a validator. */
export const crossLocalBindingError = (v: CrossEntityView, route: CrossRoute): string | null => {
  const local = entityRef(v.id), is = (x: unknown): boolean => entityRef(x) === local;
  const sourceP = is(route.source.entityId) || is(route.source.counterpartyEntityId), targetP = is(route.target.entityId) || is(route.target.counterpartyEntityId);
  if (!sourceP && !targetP) return null;
  const expected = String((sourceP ? route.source.jurisdiction : route.target.jurisdiction) || "").trim();
  if (!expected) return "route jurisdiction missing";
  if (!/^stack:(?:\d+:)?0x[0-9a-fA-F]{40}$/.test(expected)) return `route jurisdiction must be stack ref, got ${expected}`;
  const localKey = stackIdOf(v.jurisdiction);
  if (!(localKey && localKey.toLowerCase() === expected.trim().toLowerCase())) return `route jurisdiction ${expected} does not match local jurisdiction ${v.jurisdictionName}`;
  const hint = is(route.source.entityId) ? trimLower(route.sourceSignerId) : is(route.source.counterpartyEntityId) ? trimLower(route.sourceHubSignerId)
    : is(route.target.entityId) ? trimLower(route.targetHubSignerId) : trimLower(route.targetSignerId);
  if (hint) {
    const validators = new Set(v.validators.map(trimLower).filter(Boolean));
    if (validators.size > 0 && !validators.has(hint)) return `route signer ${hint} is not a validator for local entity`;
  }
  return null;
};
/** og assertAccountClockMatchesRoute (prepared-route.ts): the halt message when this leg's Account clock is missing or differs from the route's. */
const crossLegClockError = (v: CrossEntityView, route: CrossRoute, leg: "source" | "target"): string | null => {
  const self = trimLower(v.id), l = route[leg], entity = trimLower(l.entityId), counterparty = trimLower(l.counterpartyEntityId), LEG = leg.toUpperCase();
  const peer = self === entity ? counterparty : self === counterparty ? entity : "";
  if (!peer) return `CROSS_J_PREPARED_${LEG}_PARTICIPANT_INVALID:${route.orderId}`;
  const child = v.replicas.get(peer as EntityId);
  if (child === undefined) return `CROSS_J_PREPARED_${LEG}_ACCOUNT_MISSING:${route.orderId}`;
  const actual = child.state.terms.disputeConfig, expected = leg === "source" ? route.sourceDisputeConfig : route.targetDisputeConfig;
  const text = disputeConfigText([actual, expected]);
  if (text !== undefined) return text;
  return actual.leftResponseSeconds !== Number(expected.leftResponseSeconds) || actual.rightResponseSeconds !== Number(expected.rightResponseSeconds)
    ? `CROSS_J_PREPARED_${LEG}_ACCOUNT_CLOCK_MISMATCH:${route.orderId}:actual=${actual.leftResponseSeconds},${actual.rightResponseSeconds}:route=${Number(expected.leftResponseSeconds)},${Number(expected.rightResponseSeconds)}`
    : null;
};
/** og validatePreparedCrossJurisdictionRoute: only public, signed preparation data (pull ids, amounts, signed amounts, clock, one ladder commitment). */
export const validatePreparedCrossRoute = (v: CrossEntityView, raw: CrossRoute): Result<CrossRoute, EntityError> => {
  const c = canonicalCrossRoute(raw);
  if (!c.ok) return invariant(crossRouteErrorText(raw, c.error.reason));
  const route = c.value, id = route.orderId, sp = route.sourcePull, tp = route.targetPull;
  if (sp === undefined || tp === undefined) return invariant(`CROSS_J_PREPARED_PULLS_MISSING:${id}`);
  const eq = (actual: unknown, expected: unknown, code: string): string | null => (actual !== expected ? `${code}:expected=${String(expected)}:actual=${String(actual)}` : null);
  const status = eq(route.status, "target_prepared", "CROSS_J_PREPARED_STATUS_INVALID");
  if (status) return invariant(status);
  const preparedAt = Number(route.updatedAt);
  if (!Number.isSafeInteger(preparedAt) || preparedAt <= 0) return invariant(`CROSS_J_PREPARED_TIMESTAMP_INVALID:${id}`);
  if (!Number.isSafeInteger(v.timestamp) || preparedAt - v.timestamp > 30_000) return invariant(`CROSS_J_PREPARED_TIMESTAMP_FUTURE:${id}`);
  return chain(fatalCross(crossPullId(route, "source")), (sourceId) => chain(fatalCross(crossPullId(route, "target")), (targetId): Result<CrossRoute, EntityError> => {
    const self = trimLower(v.id), on = (l: CrossLeg): boolean => self === trimLower(l.entityId) || self === trimLower(l.counterpartyEntityId);
    const hex32 = (x: unknown, code: string): string | null => (/^0x[0-9a-f]{64}$/.test(String(x ?? "").trim().toLowerCase()) ? null : code);
    const norm = (x: string): string => x.trim().toLowerCase();
    const checks: readonly (() => string | null)[] = [
      () => eq(sp.pullId, sourceId, "CROSS_J_PREPARED_SOURCE_PULL_ID"), () => eq(tp.pullId, targetId, "CROSS_J_PREPARED_TARGET_PULL_ID"),
      () => eq(sp.tokenId, Number(route.source.tokenId), "CROSS_J_PREPARED_SOURCE_TOKEN"), () => eq(tp.tokenId, Number(route.target.tokenId), "CROSS_J_PREPARED_TARGET_TOKEN"),
      () => eq(sp.amount, BigInt(route.source.amount), "CROSS_J_PREPARED_SOURCE_AMOUNT"), () => eq(tp.amount, BigInt(route.target.amount), "CROSS_J_PREPARED_TARGET_AMOUNT"),
      () => eq(sp.signedAmount, crossSignedAmount(route.source.counterpartyEntityId, route.source.entityId, BigInt(route.source.amount)), "CROSS_J_PREPARED_SOURCE_SIGNED_AMOUNT"),
      () => eq(tp.signedAmount, crossSignedAmount(route.target.counterpartyEntityId, route.target.entityId, BigInt(route.target.amount)), "CROSS_J_PREPARED_TARGET_SIGNED_AMOUNT"),
      () => (on(route.source) ? crossLegClockError(v, route, "source") : null), () => (on(route.target) ? crossLegClockError(v, route, "target") : null),
      () => hex32(sp.fullHash, "CROSS_J_PREPARED_FULL_HASH_INVALID"), () => hex32(sp.partialRoot, "CROSS_J_PREPARED_PARTIAL_ROOT_INVALID"),
      () => hex32(tp.fullHash, "CROSS_J_PREPARED_FULL_HASH_INVALID"), () => eq(norm(tp.fullHash), norm(sp.fullHash), "CROSS_J_PREPARED_FULL_HASH_MISMATCH"),
      () => hex32(tp.partialRoot, "CROSS_J_PREPARED_PARTIAL_ROOT_INVALID"), () => eq(norm(tp.partialRoot), norm(sp.partialRoot), "CROSS_J_PREPARED_PARTIAL_ROOT_MISMATCH"),
    ];
    for (const check of checks) { const e = check(); if (e !== null) return invariant(e); }
    return fatalCross(cloneCrossRoute(route));
  }));
};
/** og CROSS_J_BOOK_MAX_USD_MICROS. */
const CROSS_J_BOOK_MAX_USD_MICROS = 6_500_000n * 1_000_000n;
/** og getCrossJurisdictionLocalUsdCapError on the Entity's own state: a reference stable at par, any other token at the hub's orderbookExt authority ask. */
export const crossUsdCapError = (v: CrossEntityView, route: CrossRoute): Result<string | null, EntityError> => crossLocalUsdCapError({ id: v.id, jurisdiction: v.jurisdiction, ext: v.ext }, route);
/** og buildCrossJurisdictionEntityOutput: a trimmed, lowercased Entity and signer, both required. */
const crossOutput = (entityId: string, signer: string | undefined, txs: readonly EntityTx[]): Result<CrossEntityOutput, EntityError> => {
  const e = trimLower(entityId), s = trimLower(signer || "");
  return !e || !s ? invariant(`CROSS_J_ENTITY_OUTPUT_ROUTE_MISSING:${e || "entity"}:${s || "signer"}`) : ok({ entityId: e, signerId: s, txs });
};
/** og authorizeCrossJurisdictionIntent: the user's own authorization record; the source user asks its hub to prepare. */
const crossAuthorize = (v: CrossEntityView, route: CrossRoute, role: "source" | "target"): CrossStep => {
  const id = route.orderId;
  if (route.status !== "intent" || route.sourcePull || route.targetPull) return invariant(`CROSS_J_USER_AUTH_INTENT_INVALID:${id}`);
  if (isCrossExpired(route, v.timestamp)) return invariant(`CROSS_J_USER_AUTH_EXPIRED:${id}`);
  const clock = role === "source" ? crossLegClockError(v, route, "source") : null;
  if (clock) return invariant(clock);
  const signers: readonly [string, string | undefined][] = [["CROSS_J_SOURCE_SIGNER_MISSING", route.sourceSignerId], ["CROSS_J_SOURCE_HUB_SIGNER_MISSING", route.sourceHubSignerId], ["CROSS_J_TARGET_HUB_SIGNER_MISSING", route.targetHubSignerId], ["CROSS_J_TARGET_SIGNER_MISSING", route.targetSignerId]];
  for (const [code, s] of signers) if (!entityRef(s)) return invariant(`${code}:${id}`);
  const auths = v.auths ?? new Map<string, CrossRoute>(), existing = auths.get(id);
  return chain(fatalCross(cloneCrossRoute(route)), (cloned) => chain(exactRouteBytes(route), (bytes) => chain(existing === undefined ? ok("") : exactRouteBytes(existing), (held): CrossStep => {
    if (existing !== undefined && held !== bytes) return crossReject2(`CROSS_J_USER_AUTH_CONFLICT:${id}`);
    const next = existing === undefined ? mapSet(auths, id, cloned) : auths;
    const message = existing !== undefined ? `🌉 Cross-j swap ${id} auth retry re-emitted by ${role} user` : `🌉 Cross-j swap ${id} authorized by ${role} user`;
    if (role !== "source") return ok(crossSetup(v, { auths: next, messages: [message] }));
    return map(crossOutput(route.source.counterpartyEntityId, entityRef(route.sourceHubSignerId), [{ type: "prepareCrossJurisdictionSwap", data: { route: cloned } }]), (out) => crossSetup(v, { auths: next, outputs: [out], messages: [message] }));
  })));
};
/** og prepareRawCrossJurisdictionIntent: the source hub stores the raw intent and wakes its default proposer. */
const crossPrepareRaw = (v: CrossEntityView, route: CrossRoute): CrossStep => {
  const id = route.orderId;
  if (route.status !== "intent") return invariant(`CROSS_J_RAW_PREPARE_STATUS_INVALID:${id}:${route.status}`);
  if (isCrossExpired(route, v.timestamp)) return crossNote(v, `❌ Cross-j prepare ${id} expired`);
  if (trimLower(route.source.jurisdiction) === trimLower(route.target.jurisdiction) && Number(route.source.tokenId) === Number(route.target.tokenId)) return crossNote(v, `❌ Cross-j prepare ${id} must cross a jurisdiction or asset boundary`);
  const clock = crossLegClockError(v, route, "source");
  if (clock) return crossNote(v, `❌ Cross-j prepare ${id} blocked: ${clock}`);
  const swaps = v.swaps ?? new Map<string, CrossRoute>(), existing = swaps.get(id);
  if (existing !== undefined && isCrossTerminal(existing.status) && !existing.sourcePull && !existing.targetPull)
    return chain(materializedIntentBytes(route, existing), (a) => chain(exactRouteBytes(existing), (b) => (a !== b ? crossReject2(`CROSS_J_RAW_PREPARE_CONFLICT:${id}`) : ok(crossSetup(v, { swaps })))));
  if (existing?.sourcePull || existing?.targetPull) {
    const stored = entityRef(existing.routeHash || ""), replay = entityRef(route.routeHash || "");
    return stored && replay && stored === replay ? crossNote(v, `🌉 Cross-j prepare ${id} already materialized; replay ignored`, { swaps }) : crossReject2(`CROSS_J_RAW_PREPARE_AFTER_MATERIALIZATION:${id}`);
  }
  if (existing !== undefined) return chain(exactRouteBytes(existing), (a) => chain(exactRouteBytes(route), (b) => (a !== b ? crossReject2(`CROSS_J_RAW_PREPARE_CONFLICT:${id}`) : ok(crossSetup(v, { swaps })))));
  return chain(fatalCross(cloneCrossRoute(route)), (cloned): CrossStep => {
    const first = v.validators[0];
    if (!first) return invariant(`CROSS_J_SOURCE_HUB_PROPOSER_MISSING:${id}`);
    return crossNote(v, `🌉 Cross-j swap ${id} awaiting source-hub proposer commitments`, { swaps: mapSet(swaps, id, cloned), outputs: [{ entityId: v.id, signerId: first, txs: [] }] });
  });
};
/** og prepareMaterializedCrossJurisdictionRoute: store the public prepared route and ask both hubs to register it in one frame. */
const crossPrepareMaterialized = (v: CrossEntityView, route: CrossRoute): CrossStep => chain(validatePreparedCrossRoute(v, route), (prepared) => {
  const swaps = v.swaps ?? new Map<string, CrossRoute>(), existing = swaps.get(prepared.orderId), blocked = crossRouteTransitionError(existing, prepared);
  if (blocked) return crossNote(v, `❌ Cross-j prepare ${route.orderId} blocked: ${blocked}`, { swaps });
  return chain(fatalCross(cloneCrossRoute(prepared)), (pub) => chain(mergeCrossRoute(existing, pub), (merged) => chain(fatalCross(cloneCrossRoute(pub)), (copy) => {
    const ready: CrossRoute = { ...copy, status: "resting" };
    return chain(crossOutput(ready.source.counterpartyEntityId, ready.sourceHubSignerId, [{ type: "registerCrossJurisdictionSwap", data: { route: ready } }]), (toSource) =>
      map(crossOutput(ready.target.entityId, ready.targetHubSignerId, [{ type: "registerCrossJurisdictionSwap", data: { route: ready } }]), (toTarget) =>
        crossSetup(v, { swaps: mapSet(swaps, pub.orderId, merged), outputs: [toSource, toTarget], messages: [`🌉 Cross-j swap ${prepared.orderId} paired source and target proposals requested by hub`] })));
  })));
});
/** og handlePrepareCrossJurisdictionSwapEntityTx. `viaProposer` is og's default-proposer materialization lane, the only source of prepared payloads. */
export const crossPrepare = (v: CrossEntityView, raw: CrossRoute, viaProposer = false): CrossStep => {
  const c = canonicalCrossRoute(raw);
  if (!c.ok) return crossNote(v, `❌ Cross-j prepare invalid route: ${crossRouteErrorText(raw, c.error.reason)}`);
  const route = c.value, local = entityRef(v.id), sourceUser = entityRef(route.source.entityId), targetUser = entityRef(route.target.counterpartyEntityId), sourceHub = entityRef(route.source.counterpartyEntityId);
  if (local !== sourceUser && local !== targetUser && local !== sourceHub) return crossNote(v, `❌ Cross-j prepare ${route.orderId} wrong source hub`);
  const binding = crossLocalBindingError(v, route);
  if (binding) return crossNote(v, `❌ Cross-j prepare ${route.orderId} blocked: ${binding}`);
  const hasSource = route.sourcePull !== undefined, hasTarget = route.targetPull !== undefined;
  if (hasSource !== hasTarget) return invariant(`CROSS_J_PREPARED_PAYLOAD_PARTIAL:${route.orderId}`);
  if (local === sourceUser || local === targetUser) return hasSource ? invariant(`CROSS_J_USER_AUTH_PREPARED_FORBIDDEN:${route.orderId}`) : crossAuthorize(v, route, local === sourceUser ? "source" : "target");
  if (hasSource && !viaProposer) return crossNote(v, `❌ Cross-j prepare ${route.orderId} rejected: prepared payloads require the hub proposer lane`);
  return hasSource ? crossPrepareMaterialized(v, route) : crossPrepareRaw(v, route);
};
/** og handleMaterializeCrossJurisdictionSwapEntityTx: the default proposer's prepared route for exactly the stored raw intent, under the local USD cap. */
export const crossMaterialize = (v: CrossEntityView, data: { readonly proposerSignerId: string; readonly route: CrossRoute }): CrossStep => {
  const expected = entityRef(v.validators[0] || ""), claimed = entityRef(data.proposerSignerId), id = data.route.orderId;
  if (!expected || claimed !== expected) return invariant(`CROSS_J_MATERIALIZE_PROPOSER_INVALID:${claimed || "missing"}:${expected || "missing"}`);
  const existing = v.swaps?.get(id);
  if (existing !== undefined && isCrossTerminal(existing.status) && !existing.sourcePull && !existing.targetPull)
    return chain(materializedIntentBytes(data.route, existing), (a) => chain(exactRouteBytes(existing), (b) => (a !== b ? invariant(`CROSS_J_MATERIALIZE_INTENT_MISMATCH:${id}`) : ok(crossSetup(v)))));
  if (existing === undefined || existing.sourcePull || existing.targetPull || existing.status !== "intent") return invariant(`CROSS_J_MATERIALIZE_INTENT_MISSING:${id}`);
  return chain(materializedIntentBytes(data.route, existing), (a) => chain(exactRouteBytes(existing), (b): CrossStep => {
    if (a !== b) return invariant(`CROSS_J_MATERIALIZE_INTENT_MISMATCH:${id}`);
    return chain(crossUsdCapError(v, data.route), (cap): CrossStep => cap === null ? crossPrepare(v, data.route, true)
      : cap.startsWith("CROSS_J_BOOK_USD_CAP_EXCEEDED:") ? crossNote(v, `🌉 Cross-j materialization ${id} rejected before Account lock: ${cap}`) : invariant(cap));
  }));
};
/** og buildSourceRegistrationTxs / buildTargetRegistrationTxs: the hub's pull lock (and, on the source leg, the cross-j swap offer). */
const crossRegistrationTxs = (v: CrossEntityView, route: CrossRoute, leg: "source" | "target"): Result<readonly AccountTxTarget[], EntityError> => {
  const pull = leg === "source" ? route.sourcePull : route.targetPull, LEG = leg.toUpperCase();
  if (pull === undefined) return invariant(`CROSS_J_REGISTER_${LEG}_PULL_MISSING:${route.orderId}`);
  const peer = entityRef(leg === "source" ? route.source.entityId : route.target.counterpartyEntityId);
  if (!v.replicas.has(peer as EntityId)) return invariant(`CROSS_J_${LEG}_ACCOUNT_MISSING:${route.orderId}`);
  return chain(fatalCross(crossPullBinding(route, leg)), (binding) => chain(fatalCross(cloneCrossRoute(route)), (clone): Result<readonly AccountTxTarget[], EntityError> => {
    const lock: AccountTxTarget = { accountId: peer, tx: { type: "cross_pull_lock", pullId: pull.pullId, tokenId: String(pull.tokenId) as TokenId, amount: pull.signedAmount, fullHash: pull.fullHash, partialRoot: pull.partialRoot, crossJurisdiction: binding, crossJurisdictionRoute: clone } };
    if (leg === "target") return ok([lock]);
    return chain(tokenDecimals(route.source.tokenId), (give) => map(tokenDecimals(route.target.tokenId), (want): readonly AccountTxTarget[] => [lock, { accountId: peer, tx: {
      type: "swap_offer", offerId: route.orderId, giveTokenId: String(route.source.tokenId) as TokenId, giveTokenDecimals: Number(give), giveAmount: route.source.amount,
      wantTokenId: String(route.target.tokenId) as TokenId, wantTokenDecimals: Number(want), wantAmount: route.target.amount, maxFee: 0n, minNetReceive: route.target.amount,
      ...opt("priceTicks", route.priceTicks), timeInForce: 0, crossJurisdiction: clone,
    } }]));
  }));
};
/** og handleRegisterCrossJurisdictionSwapEntityTx: record the route; an opening `resting` registration queues this hub's Account legs after a pull pre-check. */
export const crossRegister = (v: CrossEntityView, raw: CrossRoute): CrossStep => {
  const c = canonicalCrossRoute(raw);
  if (!c.ok) return crossNote(v, `❌ Cross-j register invalid route: ${crossRouteErrorText(raw, c.error.reason)}`);
  const route = c.value, id = route.orderId;
  if (!isCrossRouteParticipant(route, v.id)) return crossNote(v, `❌ Cross-j register ${id} routed to non-participant entity`);
  const binding = crossLocalBindingError(v, route);
  if (binding) return crossNote(v, `❌ Cross-j register ${id} blocked: ${binding}`);
  const local = entityRef(v.id), sourceHub = entityRef(route.source.counterpartyEntityId), targetHub = entityRef(route.target.entityId);
  const existing = v.swaps?.get(id), blocked = crossRouteTransitionError(existing, route);
  if (blocked) return crossNote(v, `❌ Cross-j swap ${id} register blocked: ${blocked}`);
  const opening = existing === undefined || existing.status === "intent" || existing.status === "target_prepared";
  const leg: "source" | "target" | undefined = local === sourceHub ? "source" : local === targetHub ? "target" : undefined;
  const precheck = (): Result<string | null, EntityError> => {
    const pull = leg === undefined ? undefined : leg === "source" ? route.sourcePull : route.targetPull;
    if (!opening || route.status !== "resting" || pull === undefined || leg === undefined) return ok(null);
    const peer = leg === "source" ? route.source.entityId : route.target.counterpartyEntityId, child = v.replicas.get(entityRef(peer) as EntityId);
    if (child === undefined) return invariant(`CROSS_J_REGISTER_ACCOUNT_MISSING:${id}:${peer}`);
    return map(crossRegistrationTxs(v, route, leg), (txs) => { const lock = txs.find((t) => t.tx.type === "cross_pull_lock")?.tx; return lock?.type === "cross_pull_lock" ? pullAdmissionText(child.state, lock) : null; });
  };
  return chain(precheck(), (refused) => {
    if (refused !== null) return crossNote(v, `❌ Cross-j register ${id} rejected before Account queue: ${refused}`);
    return chain(mergeCrossRoute(existing, route), (merged): CrossStep => {
      const registered = crossSetup(v, { swaps: mapSet(v.swaps ?? new Map<string, CrossRoute>(), id, merged), messages: [`🌉 Cross-j swap ${id} registered`] });
      if (!opening || route.status !== "resting") return ok(registered);
      if (!route.sourcePull || !route.targetPull) return invariant(`CROSS_J_REGISTER_OPENING_PULLS_MISSING:${id}`);
      if (leg === undefined) return ok(registered);
      const after: CrossEntityView = { ...v, swaps: registered.swaps };
      const checked = leg === "source" ? map(validatePreparedCrossRoute(after, { ...route, status: "target_prepared" }), () => undefined) : ok(undefined);
      return chain(checked, () => map(crossRegistrationTxs(after, route, leg), (accountTxs) => ({ ...registered, accountTxs })));
    });
  });
};
/** og assertRuntimeOutputAuthorization for the rewrite's tx kinds: self control continuations, or the two sibling edges a committed route names. */
/** og requireSemanticRoute: the stored route, else the supplied one, for exactly this order; both present must share a route hash. */
const semanticRouteError = (state: EntityState, orderId: string, supplied?: CrossRoute | undefined): { readonly route: CrossRoute } | { readonly error: string } => {
  const id = String(orderId ?? ""), stored = state.crossJurisdictionSwaps?.get(id), route = stored ?? supplied;
  if (route === undefined || route.orderId !== id) return { error: `RUNTIME_OUTPUT_ROUTE_MISSING:${id || "missing"}` };
  if (stored !== undefined && supplied !== undefined) {
    const s = trimLower(stored.routeHash), t = trimLower(supplied.routeHash);
    if (!s || !t || s !== t) return { error: `RUNTIME_OUTPUT_ROUTE_HASH_MISMATCH:${id}:${t || "missing"}:${s || "missing"}` };
  }
  return { route };
};
/** og routeBookOwner (auth/authorization.ts). */
const authBookOwner = (route: CrossRoute): string => trimLower(route.bookOwnerEntityId || route.source.counterpartyEntityId || route.hubEntityId);
/** og selfRuntimeContinuationTxTypes this port carries: exact next-frame work a certified frame emitted back to its own Entity. */
const SELF_CONTINUATIONS: ReadonlySet<string> = new Set(["orderbookSweepCrossJurisdiction", "prepareDispute", "requestCrossJurisdictionClear"]);
/**
 * og assertRuntimeOutputAuthorization: a certified command lane between sibling hubs of one cross-j route (or an Entity's own continuation). The
 * semantic route comes from this Entity's stored route for the book-lifecycle variants that name only an order, else from the tx's own route.
 */
export const runtimeOutputAuthError = (state: EntityState, o: RuntimeOutputData): string | null => {
  const source = trimLower(o.sourceEntityId), signer = trimLower(o.sourceSignerId), target = trimLower(o.targetEntityId);
  if (!source || !signer || !target || target !== trimLower(state.id)) return `RUNTIME_OUTPUT_TARGET_MISMATCH:${target || "missing"}:${state.id}`;
  if (o.entityTxs.length === 0) return "RUNTIME_OUTPUT_TXS_MISSING";
  const srcOf = (type: string, allowed: readonly unknown[]): string | null => {
    const set = new Set(allowed.map(trimLower).filter(Boolean));
    return set.has(source) ? null : `RUNTIME_OUTPUT_SEMANTIC_SOURCE_MISMATCH:${type}:${source || "missing"}:${[...set].join(",") || "none"}`;
  };
  const tgtOf = (type: string, expected: unknown): string | null => { const e = trimLower(expected); return e && target === e ? null : `RUNTIME_OUTPUT_SEMANTIC_TARGET_MISMATCH:${type}:${target || "missing"}:${e || "missing"}`; };
  /** og assertRuntimeBookOutputAuthority + assertRuntimeBookLifecycleAuthority for the book variants; undefined for the rest. */
  const bookAuthority = (tx: EntityTx): string | null | undefined => {
    switch (tx.type) {
      case "admitCrossJurisdictionBookOrder": return srcOf(tx.type, [tx.data.route.source.counterpartyEntityId]) ?? tgtOf(tx.type, authBookOwner(tx.data.route));
      case "crossJurisdictionFillNotice": {
        const r = semanticRouteError(state, tx.data.orderId);
        if ("error" in r) return r.error;
        if (target !== trimLower(r.route.source.counterpartyEntityId)) return `RUNTIME_OUTPUT_CROSS_J_PROGRESS_TARGET_INVALID:${target}`;
        return srcOf(tx.type, [authBookOwner(r.route)]);
      }
      case "removeCrossJurisdictionBookOrder": {
        const r = semanticRouteError(state, tx.data.orderId, tx.data.route);
        if ("error" in r) return r.error;
        if (trimLower(tx.data.sourceEntityId) !== trimLower(r.route.source.entityId)) return `RUNTIME_OUTPUT_BOOK_SOURCE_ENTITY_MISMATCH:${tx.data.sourceEntityId}:${r.route.source.entityId}`;
        return srcOf(tx.type, [r.route.source.counterpartyEntityId]) ?? tgtOf(tx.type, authBookOwner(r.route));
      }
      case "crossJurisdictionBookOrderRemoved": {
        const r = semanticRouteError(state, tx.data.orderId, tx.data.route);
        if ("error" in r) return r.error;
        if (trimLower(tx.data.sourceEntityId) !== trimLower(r.route.source.entityId)) return `RUNTIME_OUTPUT_BOOK_REMOVAL_SOURCE_MISMATCH:${tx.data.sourceEntityId}:${r.route.source.entityId}`;
        if (trimLower(tx.data.sourceAccountId) !== trimLower(r.route.source.entityId)) return `RUNTIME_OUTPUT_BOOK_REMOVAL_ACCOUNT_MISMATCH:${tx.data.sourceAccountId}:${r.route.source.entityId}`;
        return srcOf(tx.type, [authBookOwner(r.route)]) ?? tgtOf(tx.type, r.route.source.counterpartyEntityId);
      }
      case "crossPullClose": {
        const r = semanticRouteError(state, tx.data.proof.orderId, tx.data.route);
        if ("error" in r) return r.error;
        const e = srcOf(tx.type, [r.route.source.counterpartyEntityId]) ?? tgtOf(tx.type, r.route.target.entityId);
        if (e) return e;
        return entityRef(tx.data.counterpartyEntityId) !== entityRef(r.route.target.counterpartyEntityId) ? `RUNTIME_OUTPUT_CROSS_PULL_COUNTERPARTY_MISMATCH:${tx.data.counterpartyEntityId}:${r.route.target.counterpartyEntityId}` : null;
      }
      case "requestCrossJurisdictionClear": {
        const r = semanticRouteError(state, tx.data.orderId, tx.data.route);
        if ("error" in r) return r.error;
        return srcOf(tx.type, [r.route.source.counterpartyEntityId, r.route.target.entityId, authBookOwner(r.route)]) ?? tgtOf(tx.type, r.route.source.counterpartyEntityId);
      }
      default: return undefined;
    }
  };
  /** og assertRuntimeCrossJRecoveryAuthority (salvage, cross-j HTLC resolve, assertRuntimeCrossJSourceDispute); undefined for the rest. */
  const recoveryAuthority = (tx: EntityTx): string | null | undefined => {
    switch (tx.type) {
      case "crossJurisdictionSalvage": {
        const r = semanticRouteError(state, tx.data.routeId);
        if ("error" in r) return r.error;
        if (trimLower(tx.data.sourceEntityId) !== trimLower(r.route.source.entityId)) return `RUNTIME_OUTPUT_SALVAGE_SOURCE_ENTITY_MISMATCH:${tx.data.sourceEntityId}:${r.route.source.entityId}`;
        if (trimLower(tx.data.sourceCounterpartyEntityId) !== trimLower(r.route.source.counterpartyEntityId)) return `RUNTIME_OUTPUT_SALVAGE_SOURCE_COUNTERPARTY_MISMATCH:${tx.data.sourceCounterpartyEntityId}:${r.route.source.counterpartyEntityId}`;
        if (target === trimLower(r.route.target.counterpartyEntityId)) return srcOf(tx.type, [r.route.source.entityId]) ?? tgtOf(tx.type, r.route.target.counterpartyEntityId);
        if (target === trimLower(r.route.source.entityId)) return srcOf(tx.type, [r.route.target.counterpartyEntityId]) ?? tgtOf(tx.type, r.route.source.entityId);
        return `RUNTIME_OUTPUT_SALVAGE_TARGET_INVALID:${target}`;
      }
      case "resolveHtlcLock": {
        const routeId = String(tx.data.crossJurisdictionRouteId ?? "");
        if (!routeId) return "RUNTIME_OUTPUT_CROSS_J_HTLC_ROUTE_REQUIRED";
        const r = semanticRouteError(state, routeId);
        if ("error" in r) return r.error;
        return srcOf(tx.type, [r.route.source.entityId]) ?? tgtOf(tx.type, r.route.target.counterpartyEntityId)
          ?? (trimLower(tx.data.counterpartyEntityId) !== trimLower(r.route.target.entityId) ? `RUNTIME_OUTPUT_CROSS_J_HTLC_COUNTERPARTY_MISMATCH:${routeId}` : null);
      }
      case "disputeStart": {
        const routeId = String(tx.data.crossJurisdictionRouteId ?? "");
        if (!routeId) return "RUNTIME_OUTPUT_CROSS_J_DISPUTE_ROUTE_REQUIRED";
        const r = semanticRouteError(state, routeId);
        if ("error" in r) return r.error;
        if (Object.keys(tx.data).some((k) => k !== "counterpartyEntityId" && k !== "crossJurisdictionRouteId")) return "RUNTIME_OUTPUT_CROSS_J_DISPUTE_DATA_FORBIDDEN";
        if (isCrossTerminal(r.route.status) || !r.route.targetPull) return `RUNTIME_OUTPUT_CROSS_J_DISPUTE_ROUTE_INACTIVE:${r.route.orderId}:${r.route.status}`;
        if (trimLower(tx.data.counterpartyEntityId) !== trimLower(r.route.source.counterpartyEntityId)) return `RUNTIME_OUTPUT_CROSS_J_DISPUTE_COUNTERPARTY_MISMATCH:${tx.data.counterpartyEntityId}:${r.route.source.counterpartyEntityId}`;
        return srcOf(tx.type, [r.route.target.counterpartyEntityId]) ?? tgtOf(tx.type, r.route.source.entityId);
      }
      default: return undefined;
    }
  };
  // og assertSelfRuntimeContinuations
  if (source === target && o.entityTxs.every((tx) => SELF_CONTINUATIONS.has(tx.type))) {
    const board = new Set(rootConfig(state).validators);
    if (!board.has(signer)) return `RUNTIME_OUTPUT_SOURCE_SIGNER_MISMATCH:${source}:${signer}:current-board`;
    for (const tx of o.entityTxs) {
      if (tx.type !== "requestCrossJurisdictionClear") continue;
      const route = state.crossJurisdictionSwaps?.get(tx.data.orderId), expected = route === undefined ? null : crossRouteSigner(route, source);
      if (route === undefined || !isCrossRouteParticipant(route, source)) return `RUNTIME_OUTPUT_NON_SIBLING_FORBIDDEN:${tx.type}:${source}:${target}`;
      if (!expected || expected !== signer) return `RUNTIME_OUTPUT_SOURCE_SIGNER_MISMATCH:${source}:${signer}:${expected || "missing"}`;
      const e = bookAuthority(tx);
      if (e) return e;
    }
    return null;
  }
  if (source === target && !o.entityTxs.every((tx) => tx.type === "registerCrossJurisdictionSwap" && trimLower(tx.data.route.source.counterpartyEntityId) === source))
    return `RUNTIME_OUTPUT_SELF_FORBIDDEN:${source}:${o.entityTxs.map((tx) => tx.type).join(",")}`;
  for (const tx of o.entityTxs) {
    if (isProtocolTx(tx)) return `RUNTIME_OUTPUT_NESTED_PROTOCOL_TX_FORBIDDEN:${tx.type}`;
    // og runtimeOutputRouteId / runtimeOutputSemanticRoute
    const routeId = tx.type === "crossJurisdictionFillNotice" || tx.type === "removeCrossJurisdictionBookOrder" || tx.type === "requestCrossJurisdictionClear" ? tx.data.orderId
      : tx.type === "crossJurisdictionSalvage" ? tx.data.routeId : tx.type === "resolveHtlcLock" || tx.type === "disputeStart" ? tx.data.crossJurisdictionRouteId : undefined;
    const route = routeId ? state.crossJurisdictionSwaps?.get(routeId)
      : "data" in tx && tx.data !== null && typeof tx.data === "object" && "route" in tx.data ? (tx.data as { readonly route?: CrossRoute }).route : undefined;
    if (route === undefined || typeof route !== "object" || !isCrossRouteParticipant(route, source) || !isCrossRouteParticipant(route, target)) return `RUNTIME_OUTPUT_NON_SIBLING_FORBIDDEN:${tx.type}:${source}:${target}`;
    const expected = crossRouteSigner(route, source);
    if (!expected || expected !== signer) return `RUNTIME_OUTPUT_SOURCE_SIGNER_MISMATCH:${source}:${signer}:${expected || "missing"}`;
    const book = bookAuthority(tx);
    if (book !== undefined) { if (book) return book; continue; }
    const recovery = recoveryAuthority(tx);
    if (recovery !== undefined) { if (recovery) return recovery; continue; }
    if (tx.type === "prepareCrossJurisdictionSwap") {
      const e = srcOf(tx.type, [route.source.entityId]), want = trimLower(route.source.counterpartyEntityId);
      if (e) return e;
      if (!want || target !== want) return `RUNTIME_OUTPUT_SEMANTIC_TARGET_MISMATCH:${tx.type}:${target || "missing"}:${want || "missing"}`;
    } else if (tx.type === "registerCrossJurisdictionSwap") {
      const e = srcOf(tx.type, [route.source.counterpartyEntityId]), sh = trimLower(route.source.counterpartyEntityId), th = trimLower(route.target.entityId);
      if (e) return e;
      if (target !== sh && target !== th) return `RUNTIME_OUTPUT_SEMANTIC_TARGET_MISMATCH:${tx.type}:${target}:${sh},${th}`;
    } else return `RUNTIME_OUTPUT_SEMANTIC_VARIANT_FORBIDDEN:${tx.type}`;
  }
  return null;
};
/** A cross-j setup's effects on the Entity draft: both collections, frame messages, raw outputs (wrapped at commit), the hub's Account work. */
const crossDraft = (state: EntityState, replicas: Replicas, s: CrossSetup, timestamp: bigint): Draft => {
  const next: EntityState = { ...state, ...opt("crossJurisdictionSwaps", s.swaps), ...opt("crossJurisdictionAuthorizations", s.auths) };
  const outputs = s.outputs.map((o): EntityOutput => o.txs.length === 0
    ? { to: o.entityId as EntityId, signerId: o.signerId as Address, input: { kind: "txs", timestamp, txs: [] } }
    : { to: o.entityId as EntityId, signerId: o.signerId as Address, input: { kind: "txs", timestamp, txs: [{ type: "runtimeOutput", data: { protocol: "cross-j", sourceEntityId: lower(state.id), sourceSignerId: "", targetEntityId: o.entityId, entityTxs: o.txs } }] } });
  const start: Draft = { state: next, accountReplicas: replicas, outputs, events: s.messages.map(status), touched: [] };
  return s.accountTxs.reduce((d, t) => ({ ...queueReturned(d, t), events: d.events, touched: [...(d.touched ?? []), t.accountId as EntityId] }), start);
};
/** The book lifecycle's view of this Entity (og EntityState fields it reads). */
const bookHostOf = (state: EntityState, replicas: Replicas, timestamp: bigint): BookHost => ({
  id: state.id, timestamp: Number(timestamp), validators: rootConfig(state).validators, ext: state.orderbookExt, swaps: state.crossJurisdictionSwaps, admissions: state.crossJurisdictionBookAdmissions,
  accounts: hubView(state, replicas, state.orderbookExt ?? EMPTY_EXT).accounts,
});
const EMPTY_EXT: OrderbookExt = { books: new Map(), pairDimensions: new Map(), referrals: new Map(), hubProfile: { entityId: "", name: "", spreadDistribution: { makerBps: 0, takerBps: 0, hubBps: 0, makerReferrerBps: 0, takerReferrerBps: 0 }, referenceTokenId: 0, usdQuoteAuthorityEntityId: "", minTradeSize: 0n, supportedPairs: [] } };
/** One book lifecycle step on a draft: the host's fields, its raw outputs (wrapped as og runtimeOutput commands), its messages, the offers it created for this frame's matcher. */
const hostDraft = (d: Draft, s: BookHostStep, timestamp: bigint): Draft => {
  const state: EntityState = { ...d.state, ...opt("orderbookExt", s.host.ext), ...opt("crossJurisdictionSwaps", s.host.swaps), ...opt("crossJurisdictionBookAdmissions", s.host.admissions) };
  const wrapped = crossDraft(state, d.accountReplicas, { swaps: s.host.swaps, auths: state.crossJurisdictionAuthorizations, messages: s.messages, outputs: s.outputs, accountTxs: [] }, timestamp);
  const created: SwapEvents | undefined = s.created.length === 0 ? undefined : { created: s.created, cancelled: [], cancelRequests: [] };
  return { ...d, state, outputs: [...d.outputs, ...wrapped.outputs], events: [...(d.events ?? []), ...(wrapped.events ?? [])], touched: d.touched ?? [], ...opt("swaps", joinSwapEvents(d.swaps, created)) };
};
/** A clear-lifecycle step on a draft: the book-host effects, then each returned Account tx admitted alone (og applyLocalAccountEffects). */
const clearDraft = (d: Draft, s: CrossHostStep, timestamp: bigint): Draft =>
  s.accountTxs.reduce((x, t) => ({ ...queueReturned(x, t), events: x.events, touched: [...(x.touched ?? []), t.accountId as EntityId] }), hostDraft(d, s, timestamp));
/** og materializeCommittedEntityOutputs: a cross-j command leaves only from the frame's emitter, stamped with its signer; wakes pass on every replica. */
const publishCommitted = (outputs: readonly EntityOutput[], self: Address, emitter: string): readonly EntityOutput[] => outputs.flatMap((o): readonly EntityOutput[] => {
  const tx = "input" in o && o.input.kind === "txs" && o.input.txs.length === 1 ? o.input.txs[0] : undefined;
  if (tx === undefined || tx.type !== "runtimeOutput" || tx.data.sourceSignerId !== "" || !("input" in o) || o.input.kind !== "txs") return [o];
  return signerId(self) !== signerId(emitter) ? [] : [{ ...o, input: { ...o.input, txs: [{ ...tx, data: { ...tx.data, sourceSignerId: signerId(self) } }] } }];
});
/** og appendDefaultProposerCrossJMaterializations: the source hub's default proposer prepares each stored raw intent it owns, outside a commit phase. */
const crossMaterializations = (r: EntityReplica, txs: readonly EntityTx[], ctx: EntityContext, now: bigint): Result<readonly EntityTx[], EntityError> => {
  const proposer = trimLower(rootConfig(r.state).validators[0]);
  if (!proposer || signerId(r.signerId) !== proposer) return ok(txs);
  const nested = (tx: EntityTx): readonly EntityTx[] => (tx.type === "entityCommand" ? tx.data.txs : tx.type === "runtimeOutput" && tx.data.protocol === "cross-j" ? tx.data.entityTxs : [tx]);
  if (txs.some((tx) => nested(tx).some((n) => n.type === "accountInput" || n.type === "crossJurisdictionFillNotice" || n.type === "registerCrossJurisdictionSwap"))) return ok(txs);
  const pending = new Set([...r.mempool, ...txs].flatMap((tx) => nested(tx).flatMap((n) => (n.type === "materializeCrossJurisdictionSwap" || n.type === "registerCrossJurisdictionSwap" ? [`setup:${n.data.route.orderId}`]
    : n.type === "materializeCrossJurisdictionClear" ? [`clear:${n.data.orderId}`] : []))));
  const routes = [...(r.state.crossJurisdictionSwaps?.values() ?? [])].sort((a, b) => a.orderId.localeCompare(b.orderId));
  const setups = foldResult<readonly EntityTx[], CrossRoute, EntityError>(routes, [], (added, route) => {
    if (route.status !== "intent" || route.sourcePull || route.targetPull || pending.has(`setup:${route.orderId}`) || trimLower(route.source.counterpartyEntityId) !== trimLower(r.state.id)) return ok(added);
    pending.add(`setup:${route.orderId}`);
    return map(fatalCross(prepareCrossRoute(route, { runtimeSeed: ctx.runtimeSeed, now: Number(now) })), (prepared): readonly EntityTx[] => [...added, { type: "materializeCrossJurisdictionSwap", data: { proposerSignerId: signerId(r.signerId), route: prepared } }]);
  });
  const clears = (added: readonly EntityTx[]): Result<readonly EntityTx[], EntityError> => map(crossClearReveals(bookHostOf(r.state, r.accountReplicas, now), signerId(r.signerId), ctx.runtimeSeed, pending), (c) => [...added, ...c]);
  return map(chain(setups, clears), (added) => (added.length > 0 ? [...txs, ...added] : txs));
};
/** og appendDefaultProposerCrossJMaterializations (clear branch): each clear_requested route with a committed fill, an open source pull and no queued close gets the proposer's ladder reveal. */
export const crossClearReveals = (h: BookHost, proposer: string, runtimeSeed: string | undefined, pending: ReadonlySet<string>): Result<readonly EntityTx[], EntityError> =>
  foldResult<readonly EntityTx[], CrossRoute, EntityError>([...(h.swaps?.values() ?? [])].sort((a, b) => a.orderId.localeCompare(b.orderId)), [], (added, route) => {
    const sp = route.sourcePull;
    if (route.status !== "clear_requested" || sp === undefined || !route.targetPull || pending.has(`clear:${route.orderId}`) || entityRef(route.source.counterpartyEntityId) !== entityRef(h.id)) return ok(added);
    return chain(fillRatioE(route), (fillRatio) => {
      const account = h.accounts.get(entityRef(route.source.entityId));
      if (fillRatio <= 0 || account === undefined || !(account.pulls?.has(sp.pullId) ?? false) || pullCloseQueued(account, sp.pullId)) return ok(added);
      return chain(fatalCross(crossPrivateSeed(runtimeSeed, route)), (seed) => chain(fatalCross(crossPullReveal(fillRatio, seed)), (reveal) => map(closeProofE(route, reveal.binary), (proof): readonly EntityTx[] =>
        [...added, { type: "materializeCrossJurisdictionClear", data: { proposerSignerId: proposer, orderId: route.orderId, binary: reveal.binary, proof } }])));
    });
  });
/** og selectCrossJCommitPhaseTxs: an Account transition and a cross-j setup never share a frame; setup (and a deferred author's later commands) waits. */
export const selectCommitPhaseTxs = (txs: readonly EntityTx[]): readonly EntityTx[] => {
  const nested = (tx: EntityTx): readonly EntityTx[] => (tx.type === "entityCommand" ? tx.data.txs : tx.type === "runtimeOutput" && tx.data.protocol === "cross-j" ? tx.data.entityTxs : [tx]);
  const transition = (tx: EntityTx): boolean => nested(tx).some((n) => n.type === "accountInput" || n.type === "crossJurisdictionFillNotice");
  const setup = (tx: EntityTx): boolean => nested(tx).some((n) => n.type === "materializeCrossJurisdictionSwap" || n.type === "materializeCrossJurisdictionClear" || n.type === "registerCrossJurisdictionSwap");
  if (!txs.some(transition) || !txs.some(setup)) return txs;
  const deferred = new Set<string>();
  return txs.filter((tx) => {
    if (tx.type !== "entityCommand") return !setup(tx);
    const author = `${tx.data.boardHash}:${tx.data.boardEpoch}:${tx.data.authorSignerId}`.toLowerCase();
    if (deferred.has(author) || setup(tx)) { deferred.add(author); return false; }
    return true;
  });
};
export const foldTx = (state: EntityState, replicas: Replicas, tx: EntityTx, ctx: FoldContext, lane: TxLane = "top"): Result<Draft, EntityError> => {
  const origin = originOf(tx, state.id), peer = peerOf(tx, state.id);
  const authorized = laneRefusal(tx, lane);
  if (authorized !== undefined) return err(authorized);
  const say = (d: Draft, ...messages: readonly string[]): Draft => ({ ...d, events: [...(d.events ?? []), ...messages.map(status)] });
  const enqueue = (target: EntityId, accountTxs: readonly AccountTx[], outputs: readonly EntityOutput[]): Result<Draft, EntityError> =>
    withChild(replicas, target, (child) => map(admitAt(child, accountTxs, state.id, L0_CLOCK, ctx.verify), (admitted) => ({ ...putChild(state, replicas, target, admitted), outputs })));
  const skip: Draft = { state, accountReplicas: replicas, outputs: [] };
  return matchBy("type", tx, {
    // og open-account.ts: the two status events around the insert
    // og insertLocalAccount: the AccountOpening runtime event
    openAccount: (x) => map(openChild(state, replicas, x, ctx.timestamp), (d) => ({ ...say(d, `💳 Opening account with Entity ${x.data.targetEntityId}...`, `✅ Account opening request sent to Entity ${lower(x.data.targetEntityId)}`),
      runtimeEvents: [...(d.runtimeEvents ?? []), { eventName: "AccountOpening", data: { entityId: state.id, counterpartyId: lower(x.data.targetEntityId) } }] })),
    extendCredit: (x) => (replicas.has(x.data.counterpartyEntityId) ? map(enqueue(x.data.counterpartyEntityId, [{ type: "set_credit_limit", tokenId: x.data.tokenId, limit: x.data.amount }], [wake(state, ctx.timestamp)]), (d) => say(d, `💳 Extended credit of ${x.data.amount} to ${x.data.counterpartyEntityId.slice(-4)}`)) : ok(skip)),
    directPayment: (x) => {
      const { route, targetEntityId, amount, deliveryMode, trustedGatewayEntityId, tokenId, description } = x.data;
      if (route.length === 0 || route.length > 100 || route[0] !== state.id || route[route.length - 1] !== targetEntityId) return err({ _tag: "payment_route" });
      if (deliveryMode !== "direct" && deliveryMode !== "trusted") return err({ _tag: "payment_route" });
      if (amount < 1n || amount > UINT256_MAX) return ok(say(skip, "❌ Payment failed: amount out of bounds"));
      // og requireTrustedPaymentGateway: exactly [source, gateway, target] with the declared gateway distinct from both ends
      if (deliveryMode === "trusted" ? route.length !== 3 || route[1] !== trustedGatewayEntityId || route[1] === route[0] || route[1] === targetEntityId : trustedGatewayEntityId !== undefined || route.length !== 2) return err({ _tag: "payment_route" });
      const next = route[1] as EntityId;
      // og buildNextHopPayment: the first leg carries the remaining route, the default description and the delivery mode
      const leg: AccountTx = { type: "payment", tokenId, amount, route: route.slice(1), description: description || `Payment to ${targetEntityId}`, fromEntityId: state.id, toEntityId: next, deliveryMode, ...opt("trustedGatewayEntityId", trustedGatewayEntityId) };
      return replicas.has(next) ? map(enqueue(next, [leg], [wake(state, ctx.timestamp)]), (d) => say(d, `💸 Sending ${amount} (token ${Number(tokenId)}) to ${targetEntityId} via ${route.length - 1} hops`)) : err({ _tag: "no_such_account", target: next });
    },
    lendingOffer: (x) => entityLending(state, replicas, x, (hub, accountTx) => map(enqueue(hub, [accountTx], [wake(state, ctx.timestamp)]), (d) => say(d, lendingMessage(x)))),
    lendingBorrow: (x) => entityLending(state, replicas, x, (hub, accountTx) => map(enqueue(hub, [accountTx], [wake(state, ctx.timestamp)]), (d) => say(d, lendingMessage(x)))),
    lendingRepay: (x) => entityLending(state, replicas, x, (hub, accountTx) => map(enqueue(hub, [accountTx], [wake(state, ctx.timestamp)]), (d) => say(d, lendingMessage(x)))),
    lendingClosePosition: (x) => entityLending(state, replicas, x, (hub, accountTx) => map(enqueue(hub, [accountTx], [wake(state, ctx.timestamp)]), (d) => say(d, lendingMessage(x)))),
    // og system/basic.ts: chat messages are frame events (certified in the frame hash, never in the state root); an invalid chat message is a silent no-op
    chat: (x) => ok(typeof x.data.message === "string" && x.data.message.length > 0 && x.data.message.length <= 1000 ? { ...skip, events: [{ type: "text", validatorId: lower(x.data.from), message: x.data.message }] } : skip),
    chatMessage: (x) => ok(say(skip, x.data.message)),
    entityCommand: (x) => foldCommand(state, replicas, x.data, ctx),
    entityProviderTransfer: (x) => entityProviderAction(state, replicas, x, ctx.timestamp),
    entityProviderReleaseControlShares: (x) => entityProviderAction(state, replicas, x, ctx.timestamp),
    entityProviderCancelAction: (x) => entityProviderCancel(state, replicas, x, ctx.timestamp),
    entityProviderProposeControlBoard: (x) => proposeControlBoard(state, replicas, x, ctx),
    entityProviderActivateBoard: (x) => activateBoard(state, replicas, x, ctx.timestamp),
    propose: (x) => foldPropose(state, replicas, x.data, ctx),
    vote: (x) => foldVote(state, replicas, x.data, ctx),
    // og admin.ts handleRequestCollateralEntityTx: a missing Account is a no-op; otherwise queue request_collateral and wake validators[0]
    requestCollateral: (x) => {
      const { counterpartyEntityId: to, tokenId, amount, feeTokenId, feeAmount, policyVersion } = x.data;
      return replicas.has(to) ? enqueue(to, [{ type: "request_collateral", tokenId, amount, ...opt("feeTokenId", feeTokenId), feeAmount, policyVersion }], [wake(state, ctx.timestamp)]) : ok(skip);
    },
    // og payments/swap-requests.ts: a missing Account halts (SWAP_REQUEST_ACCOUNT_MISSING, the whole input); otherwise queue the swap Account tx and wake validators[0]
    placeSwapOffer: (x) => {
      const { counterpartyEntityId: to, offerId, giveTokenId, giveTokenDecimals, giveAmount, wantTokenId, wantTokenDecimals, wantAmount, maxFee, minNetReceive, priceTicks, timeInForce } = x.data;
      const offer: AccountTx = { type: "swap_offer", offerId, giveTokenId, giveTokenDecimals, giveAmount, wantTokenId, wantTokenDecimals, wantAmount, maxFee, minNetReceive, ...opt("priceTicks", priceTicks), ...opt("timeInForce", timeInForce) };
      return replicas.has(to) ? enqueue(to, [offer], [wake(state, ctx.timestamp)]) : err({ _tag: "swap_request_account_missing", target: to });
    },
    proposeAccountsNow: (x) => map(proposeAccountsNowOk(state, x.data), () => ({ ...skip, outputs: x.data.counterparties.flatMap((peer): EntityOutput[] => {
      // og re-emits the retained bytes unchanged; a peer with no Account or no unanswered proposal is owed nothing
      const child = replicas.get(peer as EntityId), sent = child === undefined ? undefined : pendingAccountInput(child, state.id);
      return sent === undefined ? [] : [{ to: peer as EntityId, tx: { type: "accountInput", data: sent } }];
    }) })),
    initOrderbookExt: (x) => map(initOrderbookExt(state, x.data), (orderbookExt): Draft => (orderbookExt === state.orderbookExt ? skip : { ...skip, state: { ...state, orderbookExt } })),
    proposeCancelSwap: (x) => (replicas.has(x.data.counterpartyEntityId) ? enqueue(x.data.counterpartyEntityId, [{ type: "swap_cancel_request", offerId: x.data.offerId }], [wake(state, ctx.timestamp)]) : err({ _tag: "swap_request_account_missing", target: x.data.counterpartyEntityId })),
    "profile-update": (x) => map(profileUpdate(state, x.data.profile), (profile) => ({ ...skip, state: { ...state, committed: { ...state.committed, profile } } })),
    // og handleSetHubConfigEntityTx: commit the config, mark the profile a hub, then queue the fee terms on every Account's tokens (ids and tokens ascending) and wake
    setHubConfig: (x) => chain(buildHubConfig(hubConfigOf(state), x.data), (config) => {
      const committed = { ...state.committed, hubRebalanceConfig: config as unknown as Binary, profile: { ...((state.committed["profile"] ?? {}) as { readonly [k: string]: Binary }), isHub: true } };
      const targets = [...replicas.keys()].sort(asc).flatMap((peer) => [...(replicas.get(peer)?.state.account.deltas.keys() ?? [])].sort((a, b) => Number(a) - Number(b)).map((t) => [peer, t] as const));
      const message = `🏦 Hub config activated: ${config.matchingStrategy} strategy v${config.policyVersion}, ${config.routingFeePPM}ppm routing fee, swapTakerFee=${config.swapTakerFeeBps}bps, rebalance(base=token-default, liqBps=${config.rebalanceLiquidityFeeBps}, gas=token-default, c2rWithdrawSoftLimit=token-default)`;
      const start: Draft = { state: { ...state, committed }, accountReplicas: replicas, outputs: [], touched: [...new Set(targets.map(([peer]) => peer))] };
      return map(foldResult<Draft, readonly [EntityId, TokenId], EntityError>(targets, start, (d, [peer, t]) => chain(hubPolicyTx(config, t), (policyTx) =>
        withChild(d.accountReplicas, peer, (child) => map(admitAt(child, [policyTx], state.id, { ...L0_CLOCK, timestamp: ctx.timestamp }, ctx.verify), (admitted) => ({ ...d, ...putChild(d.state, d.accountReplicas, peer, admitted) }))))),
      (d) => say({ ...d, outputs: targets.length > 0 ? [wake(state, ctx.timestamp)] : [] }, message));
    }),
    prepareDispute: (x) => prepareDispute(skip, x.data, ctx),
    disputeFinalize: (x) => finalizeDispute(skip, x.data, ctx),
    crossJurisdictionSalvage: (x) => crossSalvage(skip, x.data, ctx),
    crossJurisdictionForceSiblingDispute: (x) => forceSiblingDispute(skip, x.data, ctx),
    resolveHtlcLock: (x) => resolveHtlcLockTx(skip, x.data, ctx),
    scheduledWake: (x) => foldWake(state, replicas, x.data, ctx),
    // og handleProcessHtlcTimeoutsEntityTx: each expired lock's timeout resolve through og applyLocalAccountEffects
    processHtlcTimeouts: (x) => ok((x.data.expiredLocks ?? []).reduce((d, l) => queueReturned(d, { accountId: l.accountId, tx: { type: "htlc_resolve", lockId: l.lockId, outcome: "error", reason: "timeout" } }), skip)),
    disputeStart: (x) => startDispute(skip, x.data.counterpartyEntityId, { description: x.data.description ?? "", ...opt("crossJurisdictionRouteId", x.data.crossJurisdictionRouteId), ...opt("starterInitialArguments", x.data.starterInitialArguments), ...opt("starterCounterArguments", x.data.starterCounterArguments) }, ctx),
    // og handleSetRebalancePolicyEntityTx: a missing Account is a no-op; an invalid policy is a plain Error; without a hub config, run checkAutoRebalance
    setRebalancePolicy: (x) => {
      const { counterpartyEntityId: to, tokenId, r2cRequestSoftLimit, hardLimit, maxAcceptableFee } = x.data, child = replicas.get(to);
      if (child === undefined) return ok(skip);
      if (r2cRequestSoftLimit < 0n || hardLimit < r2cRequestSoftLimit || maxAcceptableFee < 0n) return invariant(`REBALANCE_POLICY_INVALID: token=${Number(tokenId)}`);
      const updated: AccountReplica = { ...child, rebalancePolicy: mapSet(child.rebalancePolicy ?? new Map<number, RebalancePolicy>(), Number(tokenId), { r2cRequestSoftLimit, hardLimit, maxAcceptableFee }) };
      const requests = hubConfigOf(state) === undefined ? autoRebalance(updated, state.id) : [];
      const put = putChild(state, replicas, to, updated);
      return requests.length === 0 ? ok({ ...put, outputs: [] }) : map(admitAt(updated, requests, state.id, L0_CLOCK, ctx.verify), (admitted) => ({ ...putChild(state, replicas, to, admitted), outputs: [wake(state, ctx.timestamp)] }));
    },
    settle_propose: (x) => settlePropose(skip, x.data, settleQueue(ctx)),
    settle_update: (x) => settleUpdate(skip, x.data, settleQueue(ctx)),
    settle_approve: (x) => settleApprove(skip, x.data),
    settle_execute: (x) => settleExecute(skip, x.data, ctx.verify, settleQueue(ctx), ctx.jReplicas),
    settle_reject: (x) => settleReject(skip, x.data, settleQueue(ctx)),
    // og cross-j setup handlers (setup.ts) and the certified runtimeOutput lane (consensus/frame/application.ts applyRuntimeOutput)
    prepareCrossJurisdictionSwap: (x) => map(crossPrepare(crossView(state, replicas, ctx.timestamp), x.data.route), (s) => crossDraft(state, replicas, s, ctx.timestamp)),
    materializeCrossJurisdictionSwap: (x) => map(crossMaterialize(crossView(state, replicas, ctx.timestamp), x.data), (s) => crossDraft(state, replicas, s, ctx.timestamp)),
    registerCrossJurisdictionSwap: (x) => map(crossRegister(crossView(state, replicas, ctx.timestamp), x.data.route), (s) => crossDraft(state, replicas, s, ctx.timestamp)),
    // og cross-j book lifecycle handlers (book-order.ts, book-removal-ack.ts, fill.ts)
    admitCrossJurisdictionBookOrder: (x) => map(admitBookOrder(bookHostOf(state, replicas, ctx.timestamp), x.data), (s) => hostDraft(skip, s, ctx.timestamp)),
    removeCrossJurisdictionBookOrder: (x) => map(removeCrossBookOrder(bookHostOf(state, replicas, ctx.timestamp), x.data), (s) => hostDraft(skip, s, ctx.timestamp)),
    crossJurisdictionBookOrderRemoved: (x) => disputeRemovalAck(skip, x.data, ctx) ?? map(bookOrderRemoved(bookHostOf(state, replicas, ctx.timestamp), x.data), (s) => hostDraft(skip, s, ctx.timestamp)),
    crossJurisdictionFillNotice: (x) => map(crossFillNotice(bookHostOf(state, replicas, ctx.timestamp), x.data), (s) => hostDraft(skip, s, ctx.timestamp)),
    // og clear.ts (clear request, proposer reveal), payments/pull.ts crossPullClose, sweep.ts: book-host steps whose Account work goes through applyLocalAccountEffects
    requestCrossJurisdictionClear: (x) => map(requestCrossClear(bookHostOf(state, replicas, ctx.timestamp), x.data), (s) => clearDraft(skip, s, ctx.timestamp)),
    materializeCrossJurisdictionClear: (x) => map(materializeCrossClear(bookHostOf(state, replicas, ctx.timestamp), x.data), (s) => clearDraft(skip, s, ctx.timestamp)),
    crossPullClose: (x) => map(crossPullCloseTx(bookHostOf(state, replicas, ctx.timestamp), x.data), (s) => clearDraft(skip, s, ctx.timestamp)),
    orderbookSweepCrossJurisdiction: (x) => map(crossSweep(bookHostOf(state, replicas, ctx.timestamp), x.data.reason), (s) => clearDraft(skip, s, ctx.timestamp)),
    // og entity/tx/handlers/j-batch on the committed jBatchState
    r2r: (x) => entityR2R(skip, x.data), r2e: (x) => entityR2E(skip, x.data), e2r: (x) => entityE2R(skip, x.data), r2c: (x) => entityR2C(skip, x.data),
    j_broadcast: (x) => entityJBroadcast(skip, x.data, ctx.timestamp), j_rebroadcast: (x) => entityJRebroadcast(skip, x.data, ctx.timestamp),
    j_abort_sent_batch: (x) => ok(entityJAbort(skip, x.data)), j_clear_batch: (x) => ok(entityJClear(skip, x.data)), mintReserves: (x) => ok(entityMint(skip, x.data, ctx.timestamp)),
    j_event: (x) => entityJEvent(skip, x.data as JRec, ctx),
    boardHandover: (x) => entityBoardHandover(skip, x.data.board, ctx.boardHandover),
    runtimeOutput: (x) => {
      if (x.data.protocol !== "cross-j") return invariant(`RUNTIME_OUTPUT_PROTOCOL_INVALID:${String(x.data.protocol)}`);
      const refused = runtimeOutputAuthError(state, x.data);
      return refused !== null ? invariant(refused) : foldNested(state, replicas, x.data.entityTxs, ctx, "runtime");
    },
    htlcPayment: (x) => chain(validatePreparedHtlcPayment(originView(state, replicas, ctx.timestamp), x, ctx.htlc ?? EMPTY_HTLC_INFRA), (p) => {
      const next = htlcPaymentStep(p, state.paybook ?? EMPTY_PAYBOOK, Number(ctx.timestamp));
      // og recordOriginatedHtlc: the HtlcInitiated runtime event
      const initiated: EntityRuntimeEvent = { eventName: "HtlcInitiated", data: { entityId: state.id, fromEntity: state.id, toEntity: p.targetEntityId, tokenId: p.tokenId, amount: p.recipientAmount.toString(), senderAmount: p.senderLockAmount.toString(),
        fee: p.totalFee.toString(), hashlock: p.hashlock, lockId: p.hashlock, route: p.route, ...(p.description ? { description: p.description } : {}), startedAtMs: p.startedAtMs } };
      return map(enqueue(p.nextHopEntityId as EntityId, [next.lock], []), (d) => ({ ...d, state: { ...d.state, paybook: next.paybook }, runtimeEvents: [initiated] }));
    }),
    // og input-phases.ts: the sender's record in this Entity's certified registry is the Account's counterpartyCertifiedBoard
    accountInput: (x) => chain(deliveredBy(x.data, state.id, origin), () => chain(observerBoardRecord(state, x.data.fromEntityId), (record) => {
      const at = replicas.get(peer), door: DoorContext = { verify: ctx.verify, self: state.id, now: ctx.timestamp, autoRebalance: hubConfigOf(state) === undefined, ...(record === null ? {} : { counterpartyBoard: { boardHash: record.boardHash, activatedAtJHeight: record.activatedAtJHeight, logIndex: record.logIndex } }), ...(at === undefined ? {} : { deltaTransformer: accountDt(ctx, at) }), ...opt("boardAuthority", ctx.boardAuthority) };
      const held: Folded = { state, accountReplicas: replicas };
      // og finishDisputedAccountInput: a 'dispute' disposition ends the input in handleUnsafeAccountFrame (no committed followups)
      const unsafeOr = (at: Folded, created: boolean, go: (child: AccountReplica, applied: Result<AccountApply, AccountReplicaError>) => Result<Draft, EntityError>): Result<Draft, EntityError> => {
        const child = at.accountReplicas.get(peer);
        if (child === undefined) return err({ _tag: "no_such_account", target: peer });
        const applied = applyAccountInput(child, x.data, door);
        return (applied.ok ? undefined : unsafeAccountFrame(held, at, peer, applied.error, created, ctx)) ?? go(child, applied);
      };
      const applyRaw = (at: Folded, then: (r: Routed) => Result<Draft, EntityError>, created = false): Result<Draft, EntityError> =>
        unsafeOr(at, created, (child, applied) => chain(routedRaw(at.state, at.accountReplicas, peer, disputeUnsafe(child, applied, door)), then));
      const apply = (at: Folded): Result<Draft, EntityError> => unsafeOr(at, false, (child, applied) => routed(at.state, at.accountReplicas, peer, disputeUnsafe(child, applied, door)));
      // og committedFrames: our own frame commits when the peer's ACK for it lands; the peer's frame commits when we sign it (answerFrame).
      const before = replicas.get(peer), pendingOwn = before !== undefined && before._tag === "proposed" ? before.candidate.frame : undefined;
      const ownCommitted = (d: Draft): AccountFrame | undefined => { const after = d.accountReplicas.get(peer); return pendingOwn !== undefined && after !== undefined && after.head.height >= pendingOwn.height ? pendingOwn : undefined; };
      return matchBy("kind", x.data, {
        ack: () => applyRaw(held, ({ draft, effects }) => committedFollowups(draft, peer, ownCommitted(draft), undefined, effects, ctx)),
        // og routes the standalone peer dispute witness through the same accountInput lane; an unknown Account has no genesis for it (og ACCOUNT_GENESIS_FRAME_REQUIRED).
        dispute: () => apply(held),
        // og board-hanko-refresh.ts: checked against the sender's certified board (certified_board_missing without a record)
        board_hanko_refresh: () => apply(held),
        ack_frame: (i) => match(origin, {
          local: (): Result<Draft, EntityError> => err({ _tag: "from_not_converted" }),
          received: ({ from }) => chain(!i.frame.txs.every(entityAcceptsPeerTx) ? err({ _tag: "not_l0" }) : replicas.has(from) ? ok(held) : inboundChild(state, replicas, from, i), (at) => applyRaw(at, ({ draft: d, effects: own }) => {
            const pending = d.accountReplicas.get(from), frame = pending !== undefined && pending._tag === "received" ? pending.candidate.frame : undefined;
            return chain(answerFrame(d, from, ctx), ({ draft: answered, effects: signed }) => {
              const after = answered.accountReplicas.get(from), installed = frame !== undefined && after !== undefined && after.head.height >= frame.height;
              return committedFollowups(answered, from, ownCommitted(answered), installed ? { frame, from: i.fromEntityId, to: i.toEntityId, domain: i.domain } : undefined, [...own, ...signed], ctx, !replicas.has(from));
            });
          }, !replicas.has(from))),
        }),
      });
    })),
  });
};
/** og swapTakerFeeBps as the matcher reads it: a finite number floored into 0..10000, else 0. */
const takerFeeBpsOf = (raw: unknown): number => { const n = Number(raw); return Number.isFinite(n) ? Math.max(0, Math.min(10_000, Math.floor(n))) : 0; };
/** The matcher's view of this Entity: og status 'active' is a live Account; queued work is the mempool plus our pending frame. */
export const hubView = (state: EntityState, replicas: Replicas, ext: OrderbookExt, timestamp?: bigint): Hub => ({
  id: state.id, ext, takerFeeBps: takerFeeBpsOf(hubConfigOf(state)?.swapTakerFeeBps), ...opt("timestamp", timestamp === undefined ? undefined : Number(timestamp)),
  ...opt("crossSwaps", state.crossJurisdictionSwaps), ...opt("crossAdmissions", state.crossJurisdictionBookAdmissions),
  accounts: new Map([...replicas].map(([peer, c]): [string, HubAccount] => [peer, {
    active: isLive(c), left: c.state.account.id.left, right: c.state.account.id.right, offers: c.state.offers, queued: c._tag === "proposed" ? [...c.mempool, ...c.candidate.frame.txs] : c.mempool, ...opt("pulls", c.state.pulls),
  }])),
});
/** og replaceOrderbookPair for each final book: an existing pair keeps its place, a new pair is appended. */
const withBooks = (ext: OrderbookExt, books: ReadonlyMap<string, Book>): OrderbookExt => (books.size === 0 ? ext : { ...ext, books: new Map([...ext.books, ...books]) });
/** og commitOrderbookMatchResult: a pair's trade counter never moves backwards; the new trades across the updated pairs are the frame's SwapMatched count. */
export const tradesMatched = (ext: OrderbookExt, books: ReadonlyMap<string, Book>): Result<number, EntityError> => {
  let matched = 0;
  for (const [pairId, book] of books) {
    const previous = ext.books.get(pairId)?.tradeCount ?? 0;
    if (book.tradeCount < previous) return invariant(`ORDERBOOK_TRADE_COUNT_REGRESSION:pair=${pairId}:previous=${previous}:next=${book.tradeCount}`);
    matched += book.tradeCount - previous;
  }
  return ok(matched);
};
/**
 * og applyPostEntityTxPhases book work, after every Entity tx and before Account proposals:
 * committed offer removals leave the book (a still-visible cross-j offer's admission closes); cancel requests whose cross-j book lives on a sibling hub
 * become removal requests there, the rest queue their zero-fill resolve or a hub-internal cross-j cancel; then the frame's committed offers are matched
 * and the matcher's cross-j fills are applied after its books are installed. Matcher resolves are admitted as one batch per Account (og
 * admitOrderbookAccountTxBatch); a short admission halts.
 */
const bookPhase = (d: Draft, timestamp: bigint): Result<Draft, EntityError> => {
  const swaps = d.swaps;
  if (swaps === undefined) return ok(d);
  const fills = (start: Draft, list: readonly CrossFillInstruction[]): Result<Draft, EntityError> => foldResult<Draft, CrossFillInstruction, EntityError>(list, start, (acc, f) =>
    map(orderbookFill(bookHostOf(acc.state, acc.accountReplicas, timestamp), f), (r) => hostDraft(acc, hostStep(r.host, { outputs: r.outputs }), timestamp)));
  // og applyCommittedSwapCancelsToOrderbook
  const ext0 = d.state.orderbookExt;
  const removals: Result<{ readonly d: Draft; readonly resumePairIds: readonly string[] }, EntityError> = ext0 === undefined || swaps.cancelled.length === 0 ? ok({ d, resumePairIds: [] })
    : chain(closeCommittedCrossCancels(bookHostOf(d.state, d.accountReplicas, timestamp), swaps.cancelled), (host) => map(applyCommittedSwapCancels(ext0, swaps.cancelled), ({ ext, resumePairIds }) => ({
      d: { ...d, state: { ...d.state, orderbookExt: ext, ...opt("crossJurisdictionBookAdmissions", host.admissions) } }, resumePairIds })));
  return chain(removals, ({ d: d1, resumePairIds }) => {
    // og applySwapCancelRequests
    const cancelled: Result<Draft, EntityError> = swaps.cancelRequests.length === 0 ? ok(d1) : chain(routeRemoteCancels(bookHostOf(d1.state, d1.accountReplicas, timestamp), swaps.cancelRequests), (routed) => {
      const d2 = hostDraft(d1, hostStep(routed.host, { outputs: routed.outputs }), timestamp), ext1 = d2.state.orderbookExt;
      if (routed.local.length === 0 || ext1 === undefined) return ok(d2);
      return chain(processOrderbookCancels(hubView(d2.state, d2.accountReplicas, ext1, timestamp), routed.local), (cancels) => {
        let at: Folded = { state: d2.state, accountReplicas: d2.accountReplicas };
        for (const { accountId, tx } of cancels.accountTxs) {
          const child = at.accountReplicas.get(accountId as EntityId), admitted = child === undefined ? undefined : admit(child, [tx], at.state.id);
          if (admitted !== undefined && admitted.ok) at = putChild(at.state, at.accountReplicas, accountId as EntityId, admitted.value);
        }
        return map(fills({ ...d2, state: at.state, accountReplicas: at.accountReplicas }, cancels.crossFills), (d3) => {
          const ext = d3.state.orderbookExt;
          return ext === undefined ? d3 : { ...d3, state: { ...d3.state, orderbookExt: withBooks(ext, cancels.books) } };
        });
      });
    });
    return chain(cancelled, (d3): Result<Draft, EntityError> => {
      const ext2 = d3.state.orderbookExt;
      if (ext2 === undefined) return ok(d3);
      if (swaps.created.length === 0 && resumePairIds.length === 0 && ![...ext2.books.keys()].some((pairId) => pairId.startsWith("cross:"))) return ok(d3);
      const hub = hubView(d3.state, d3.accountReplicas, ext2, timestamp);
      return chain(offersForMatching(hub, swaps.created), (offers) => chain(processOrderbookSwaps(hub, offers, resumePairIds), (match): Result<Draft, EntityError> => {
        let at: Folded = { state: d3.state, accountReplicas: d3.accountReplicas };
        const verified = new Set(offers.filter((o) => o.crossJurisdiction === undefined).map((o) => `${o.accountId}:${o.offerId}`)), batches = new Map<string, AccountTx[]>();
        for (const { accountId, tx } of match.accountTxs) {
          const visible = hub.accounts.get(accountId);
          if (tx.type === "swap_resolve") {
            if (visible?.offers.get(tx.offerId)?.crossJurisdiction !== undefined) continue;
            if (!visible?.offers.has(tx.offerId) && !verified.has(`${accountId}:${tx.offerId}`)) return invariant(`ORDERBOOK_SWAP_OWNER_NOT_LOCAL: account=${accountId} offer=${tx.offerId} entity=${at.state.id}`);
          }
          if (visible === undefined) return invariant(`ORDERBOOK_ACCOUNT_TX_ACCOUNT_MISSING: account=${accountId} entity=${at.state.id} tx=${tx.type}`);
          batches.set(accountId, [...(batches.get(accountId) ?? []), tx]);
        }
        for (const [accountId, txs] of batches) {
          const child = at.accountReplicas.get(accountId as EntityId);
          if (child === undefined) return invariant(`ORDERBOOK_ACCOUNT_TX_ACCOUNT_MISSING: account=${accountId} entity=${at.state.id} tx=${txs[0]?.type ?? ""}`);
          const queued = chain(partyOf(replicaId(child), at.state.id), (p) => enqueue(child, txs, p.left)), admitted = queued.ok ? queued.value.queued.length : 0;
          if (!queued.ok || admitted !== txs.length) return invariant(`ORDERBOOK_ACCOUNT_TX_ADMISSION_FAILED: account=${child.state.account.id.left}:${child.state.account.id.right} expected=${txs.length} admitted=${admitted}`);
          at = putChild(at.state, at.accountReplicas, accountId as EntityId, queued.value.replica);
        }
        // og commitOrderbookMatchResult: a pair's trade counter never moves backwards; the cross-j fills apply after the books are installed
        for (const [pairId, book] of match.books) {
          const previous = ext2.books.get(pairId)?.tradeCount ?? 0;
          if (book.tradeCount < previous) return invariant(`ORDERBOOK_TRADE_COUNT_REGRESSION:pair=${pairId}:previous=${previous}:next=${book.tradeCount}`);
        }
        const installed = at;
        return chain(tradesMatched(ext2, match.books), (matched) => map(fills({ ...d3, state: { ...installed.state, orderbookExt: { ...withBooks(ext2, match.books), pairDimensions: match.pairDimensions } }, accountReplicas: installed.accountReplicas }, match.crossFills),
          (d4): Draft => (matched === 0 ? d4 : { ...d4, runtimeEvents: [...(d4.runtimeEvents ?? []), { eventName: "SwapMatched", data: { entityId: installed.state.id, count: matched } }] })));
      }));
    });
  });
};
/** `accountFrames`: og accountsToProposeFramesCount (the Account frames this Entity frame proposed; none in a cross-j setup phase). */
export type FoldedTxs = { readonly draft: Draft; readonly included: readonly EntityTx[]; readonly evicted: readonly EntityTx[]; readonly accountFrames?: number | undefined };
/**
 * og buildEntityProposalEvictingRejected: a refused tx is evicted and the rest still fold. An openAccount refusal is a plain
 * Error in og (not a reject disposition), so it refuses the whole input, as does an og lending entity-tx refusal; so does a frame whose every tx was refused.
 */
export const foldTxs = (state: EntityState, replicas: Replicas, txs: readonly EntityTx[], ctx: FoldContext): Result<FoldedTxs, EntityError> => {
  type Acc = FoldedTxs & { readonly first?: EntityError | undefined };
  // og proposePendingAccountFrames worklist: Accounts proposable before the frame (sorted), then the Accounts the included txs touched, in order.
  const primed = [...replicas].filter(([, c]) => proposableChild(c)).map(([peer]) => peer).sort(asc);
  // og assertEntityFrameTxByteBudget + assertEntityFrameJRangeBudget, then assertScheduledWakeFrameOrder (prepareEntityFrameWorkingSet): plain Errors for the whole frame
  const budgets = frameBudgets(txs);
  if (!budgets.ok) return budgets;
  const misordered = wakeOrderIssue(txs);
  if (misordered !== undefined) return err(misordered);
  // og prepareEntityFrameWorkingSet: a cross-j setup phase proposes no Account frame and never shares a frame with an Account transition
  const setupPhase = txs.some(crossSetupTx);
  if (setupPhase && txs.some(accountTransitionTx)) return invariant("CROSS_J_SETUP_ACCOUNT_TRANSITION_MIXED");
  // og getBoardHandoverFrameConfig over the normalized pre-frame state: the authority a [j_event, boardHandover] frame is certified under
  return chain(normalizeGovernance(state), (normalized) => chain(handoverFrameConfig(normalized, txs), (handover) => chain(selfAuthorityTransitionFrame(normalized, txs), (authorityOnly) => chain(foldResult<Acc, EntityTx, EntityError>(txs, { draft: { state: normalized, accountReplicas: replicas, outputs: [], events: [], touched: [] }, included: [], evicted: [] }, (acc, tx) => {
    const r = foldTx(acc.draft.state, acc.draft.accountReplicas, tx, handover === null ? ctx : { ...ctx, boardHandover: handover });
    if (r.ok) return ok({ ...acc, included: [...acc.included, tx], draft: { ...r.value, outputs: [...acc.draft.outputs, ...r.value.outputs], events: [...(acc.draft.events ?? []), ...(r.value.events ?? [])], runtimeEvents: [...(acc.draft.runtimeEvents ?? []), ...(r.value.runtimeEvents ?? [])], touched: [...(acc.draft.touched ?? []), ...(r.value.touched ?? [peerOf(tx, state.id)])], ...frameEffects(acc.draft, r.value), swaps: joinSwapEvents(acc.draft.swaps, r.value.swaps) } });
    return fatalTx(tx, r.error) ? r : ok({ ...acc, evicted: [...acc.evicted, tx], first: acc.first ?? r.error });
  }), ({ first, ...folded }) => {
    if (folded.included.length === 0 && first !== undefined) return err(first);
    // og finishAuthorityTransitionOnly (a handover, or a J range certifying the config board): after the settlement continuation, no Account
    // work and no post-tx phases; the current profile is re-certified even when its bytes did not change
    if (authorityOnly) return chain(materializeContinuation(folded.draft, ctx, settleQueue(ctx)), (d) => map(profileHashToSign(state, replicas, d, true), (draft) => ({ ...folded, draft })));
    // og materializeSettlementContinuation, then drainPostOrderbookAccountWork's settlement approvals, before proposePendingAccountFrames.
    // og applyPostEntityTxPhases: cancels + orderbook matching, then drainPostOrderbookAccountWork.
    return chain(chain(chain(materializeContinuation(folded.draft, ctx, settleQueue(ctx)), (d) => bookPhase(d, ctx.timestamp)), (d) => materializeSettlements(d, ctx)), (settled) => {
      // Accounts that received follow-up work (a gateway's forwarded leg, a matcher resolve) join after the directly touched ones.
      const followups = [...settled.accountReplicas].filter(([, c]) => proposableChild(c)).map(([peer]) => peer).sort(asc);
      const order = [...new Set([...primed, ...(settled.touched ?? []), ...followups])];
      // og refreshChangedAccountCommitments: after the Account proposals, a changed certified frame re-arms its board Hanko refresh
      return chain(setupPhase ? ok({ draft: settled, frames: 0 }) : proposeAccounts(settled, order, ctx), (proposed) =>
        chain(rearmBoardRefreshes(replicas, { ...proposed.draft, ...opt("hashes", settled.hashes), ...opt("jOutputs", settled.jOutputs) }, Number(ctx.timestamp)), (draft) => chain(profileHashToSign(state, replicas, draft, false), (withProfile) => ok({ ...folded, draft: withProfile, accountFrames: proposed.frames }))));
    });
  }))));
};
// ---- og entity/profile/profile-descriptor.ts: the public profile descriptor; its hash is a 'profile' secondary hash to sign ----
/** og MAX_ENTITY_PROFILE_DESCRIPTOR_BYTES: LIMITS.MAX_PROFILE_BYTES (1 MiB) minus the fixed route-envelope overhead og measures once. */
const MAX_PROFILE_DESCRIPTOR_BYTES = 960_602;
type ProfileCap = { readonly inCapacity: bigint; readonly outCapacity: bigint };
type RankedCap = { readonly tokenId: string; readonly capacity: ProfileCap; readonly liquidity: bigint };
type ProfileRow = { readonly counterpartyId: string; readonly domain: Domain; readonly tokenCapacities: Readonly<Record<string, ProfileCap>> };
/** og floorProfileCapacity: advertised capacities are floored to 1000 (no per-payment leak). */
const floorProfileCap = (v: bigint): bigint => (v <= 0n ? 0n : v - (v % 1000n));
const compareTokenText = (l: string, r: string): number => { const a = Number(l), b = Number(r); return Number.isSafeInteger(a) && Number.isSafeInteger(b) && a !== b ? a - b : asc(l, r); };
/** og rankedLiquidProfileCapacities: deriveDelta from our side, floored, liquid tokens by liquidity desc, at most 16. */
const rankedCaps = (self: EntityId, child: AccountReplica): readonly RankedCap[] => {
  const body = child.state, me = isLeft(self, replicaId(child));
  return [...body.account.deltas].map(([tk, d]): RankedCap => {
    const out = outCapacity(d, me, holds(body, tk, me)), inn = outCapacity(d, !me, holds(body, tk, !me));
    return { tokenId: String(tk), capacity: { inCapacity: floorProfileCap(inn), outCapacity: floorProfileCap(out) }, liquidity: inn + out };
  }).filter((c) => c.liquidity > 0n).sort((l, r) => (l.liquidity === r.liquidity ? compareTokenText(l.tokenId, r.tokenId) : l.liquidity > r.liquidity ? -1 : 1)).slice(0, 16);
};
/** og buildEntityProfileDescriptor then computeEntityProfileDescriptorHash. Only pinned Accounts are advertised, at most 100, then extra capacities up to the byte budget. */
export const entityProfileHash = (state: EntityState, replicas: Replicas): Result<string, EntityError> => {
  let rows: ProfileRow[] = [], pub: string[] = [], extras: (RankedCap & { readonly counterpartyId: string })[] = [];
  for (const [peer, child] of replicas) {
    if (child.publicPinned !== true) continue;
    const ranked = rankedCaps(state.id, child), [first] = ranked;
    if (first === undefined) continue;
    extras.push(...ranked.slice(1).map((c) => ({ counterpartyId: peer, ...c })));
    rows.push({ counterpartyId: peer, domain: child.state.terms.domain, tokenCapacities: { [first.tokenId]: first.capacity } });
    if (first.capacity.inCapacity > 0n) pub.push(peer);
  }
  if (rows.length > 100) {
    const liquidity = (r: ProfileRow): bigint => Object.values(r.tokenCapacities).reduce((n, c) => n + c.inCapacity + c.outCapacity, 0n);
    rows = [...rows].sort((l, r) => { const a = liquidity(l), b = liquidity(r); return a !== b ? (a > b ? -1 : 1) : asc(l.counterpartyId, r.counterpartyId); }).slice(0, 100);
    const advertised = new Set(rows.map((r) => r.counterpartyId));
    pub = pub.filter((id) => advertised.has(id)); extras = extras.filter((e) => advertised.has(e.counterpartyId));
  }
  rows.sort((l, r) => asc(l.counterpartyId, r.counterpartyId)); pub.sort(asc);
  extras.sort((l, r) => (l.liquidity === r.liquidity ? asc(l.counterpartyId, r.counterpartyId) || compareTokenText(l.tokenId, r.tokenId) : l.liquidity > r.liquidity ? -1 : 1));
  const profile = (state.committed["profile"] ?? {}) as { readonly [k: string]: unknown }, hub = state.committed["hubRebalanceConfig"] as { readonly [k: string]: unknown } | undefined;
  const isHub = profile["isHub"] === true, j = rootConfig(state).jurisdiction, jName = String(j?.name || "").trim(), sectors = profile["sectors"] as readonly unknown[] | undefined;
  const text = (v: unknown): string => (v === undefined ? "" : String(v));
  const base = {
    entityId: lower(state.id), entityEncryptionPublicKey: text(state.committed["entityEncryptionPublicKey"]), name: String(profile["name"] || "").trim(),
    avatar: text(profile["avatar"]), bio: text(profile["bio"]), website: text(profile["website"]), publicAccounts: pub, accounts: rows,
    metadata: {
      isHub, ...(profile["entityKind"] ? { entityKind: profile["entityKind"] } : {}), ...(sectors?.length ? { sectors: [...sectors] } : {}),
      routingFeePPM: hub?.["routingFeePPM"] ?? 1, baseFee: hub?.["baseFee"] ?? 0n, ...(hub?.["swapTakerFeeBps"] !== undefined ? { swapTakerFeeBps: hub["swapTakerFeeBps"] } : {}),
      ...(j === undefined || jName === "" ? {} : { jurisdiction: { name: jName, ...(j.chainId !== undefined ? { chainId: j.chainId } : {}), ...(j.entityProviderAddress ? { entityProviderAddress: lower(j.entityProviderAddress) } : {}), ...(j.depositoryAddress ? { depositoryAddress: lower(j.depositoryAddress) } : {}) } }),
      ...(isHub && hub !== undefined ? {
        ...(hub["hubName"] ? { hubName: hub["hubName"] } : {}), policyVersion: hub["policyVersion"], ...(hub["rebalanceBaseFee"] !== undefined ? { rebalanceBaseFee: String(hub["rebalanceBaseFee"]) } : {}),
        rebalanceLiquidityFeeBps: String(hub["rebalanceLiquidityFeeBps"]), rebalanceGasFee: String(hub["rebalanceGasFee"] ?? 0n), rebalanceTimeoutMs: hub["rebalanceTimeoutMs"] ?? 10 * 60 * 1000,
      } : {}),
    },
  };
  const withExtras = (count: number): Binary => {
    const byPeer = new Map(base.accounts.map((a) => [a.counterpartyId, { ...a, tokenCapacities: { ...a.tokenCapacities } as Record<string, ProfileCap> }]));
    for (const e of extras.slice(0, count)) { const row = byPeer.get(e.counterpartyId); if (row !== undefined) row.tokenCapacities[e.tokenId] = e.capacity; }
    return { ...base, accounts: [...byPeer.values()].map((a) => ({ ...a, tokenCapacities: Object.fromEntries(Object.entries(a.tokenCapacities).sort(([l], [r]) => compareTokenText(l, r))) })) } as unknown as Binary;
  };
  const size = (d: Binary): Result<number, EntityError> => map(encodeConsensus(d), (b) => b.byteLength);
  const full = withExtras(extras.length);
  return chain(size(full), (fullBytes) => fullBytes <= MAX_PROFILE_DESCRIPTOR_BYTES ? bytesKeccak(full) : chain(size(base as unknown as Binary), (baseBytes) => {
    if (baseBytes > MAX_PROFILE_DESCRIPTOR_BYTES) return invariant(`ENTITY_PROFILE_REQUIRED_CAPACITY_BUDGET_EXCEEDED:${baseBytes}:${MAX_PROFILE_DESCRIPTOR_BYTES}`);
    let low = 0, high = extras.length;
    while (low < high) {
      const mid = Math.ceil((low + high) / 2), bytes = size(withExtras(mid));
      if (!bytes.ok) return bytes;
      if (bytes.value <= MAX_PROFILE_DESCRIPTOR_BYTES) low = mid; else high = mid - 1;
    }
    return bytesKeccak(withExtras(low));
  }));
};
/** og appendFinalProfileHash / finishAuthorityTransitionOnly: a changed descriptor (always at genesis, always on an authority transition) is re-certified. */
const profileHashToSign = (before: EntityState, beforeReplicas: Replicas, after: Draft, always: boolean): Result<Draft, EntityError> =>
  chain(entityProfileHash(after.state, after.accountReplicas), (hash) => chain(always || before.height === 0n ? ok(null) : map(entityProfileHash(before, beforeReplicas), (h): string | null => h), (previous) =>
    ok(previous === hash ? after : { ...after, hashes: [...(after.hashes ?? []), { hash, type: "profile" as const, context: `profile:${hash}` }] })));
const EMPTY_COLLECTION = { radix: 16, leafCount: 0, root: ZERO_WORD } as const;
/** og applyEntityFrame `state.crontabState ??= initCrontab()`: the hubRebalance task at the 1s cadence, no hooks. */
const DEFAULT_CRONTAB: Binary = { tasks: new Map([["hubRebalance", { method: "hubRebalance", intervalMs: 1000, lastRun: 0, enabled: true, params: {} }]]), hooks: EMPTY_COLLECTION };
const rootConfig = (state: EntityState): EntityRootConfig => {
  const members = [...membersOf(state.quorum)].map(([id, member]) => [signerId(id), member.shares] as const), j = state.jurisdictionConfig;
  return {
    mode: "proposer-based", threshold: thresholdOf(state.quorum), validators: members.map(([id]) => id), shares: Object.fromEntries(members),
    ...(j === undefined ? {} : { jurisdiction: { chainId: state.jurisdiction.chainId, depositoryAddress: state.jurisdiction.depositoryAddress, ...j } }),
  };
};
/** og AccountReplica dispute fields (dispute/hanko.ts replaceLocalDisputeDraft, storeCounterpartyDisputeHanko): our unsigned draft tuple, the peer's full witness. */
const disputeLeafFields = (w: DisputeWitnesses): Partial<Record<EntityLeafOptional, unknown>> => ({
  ...(w.current === undefined ? {} : { currentDisputeHash: w.current.hash, currentDisputeProofBodyHash: w.current.proofBodyHash, currentDisputeProofNonce: w.current.proofNonce, currentDisputeProofProposerIsLeft: w.current.proposerIsLeft }),
  ...(w.counterparty === undefined ? {} : { counterpartyDisputeProofHanko: w.counterparty.hanko, counterpartyDisputeHash: w.counterparty.hash, counterpartyDisputeProofBodyHash: w.counterparty.proofBodyHash, counterpartyDisputeProofNonce: w.counterparty.proofNonce, counterpartyDisputeProofProposerIsLeft: w.counterparty.proposerIsLeft }),
});
/** og witness-projection.ts counterpartySettlementHankos: the peer's settlement and post-proof Hankos, when any. */
const peerSettlementHankos = (w: SettlementWorkspace | undefined, localIsLeft: boolean): unknown => {
  if (w === undefined) return undefined;
  const settlementHanko = localIsLeft ? w.rightHanko : w.leftHanko, postProofHanko = localIsLeft ? w.postSettlementDisputeProof?.rightHanko : w.postSettlementDisputeProof?.leftHanko;
  return settlementHanko === undefined && postProofHanko === undefined ? undefined : { ...opt("settlementHanko", settlementHanko), ...opt("postProofHanko", postProofHanko) };
};
/**
 * og projectAccountConsensusState for one Account replica. og's genesis replica carries `currentFrame.stateHash = ""` (open-account.ts, inbound-account.ts), so H=0 commits the empty frame hash.
 * `counterpartyFrameHanko` is the peer's Hanko on the committed head (ack-commit.ts, index.ts). Rebalance requests, fee state and fee policies are committed Account state; the local shadow is not part of the body.
 */
export const installedAccount = (self: EntityId, peer: EntityId, child: AccountReplica): Result<EntityRootAccount, EntityError> => {
  const body = child.state;
  const status: EntityRootAccount["status"] = match(child, { open: () => "active", proposed: () => "active", received: () => "active", preparing: () => "dispute_preparing", disputed: () => "disputed" });
  const localIsLeft = isLeft(self, replicaId(child));
  const linked: Result<{ readonly height: number; readonly frame: string; readonly peerHanko?: string | undefined }, EntityFrameHashError> = match(child.head, {
    genesis: () => ok({ height: 0, frame: "" }),
    installed: (head) => map(frameNumber(head.height), (height) => ({ height, frame: head.prevFrameHash, peerHanko: at(head.certificate.right, head.certificate.left, localIsLeft) })),
  });
  return chain(linked, (link): Result<EntityRootAccount, EntityError> => chain(mapErr(committedView(body), (): EntityError => ({ _tag: "account_envelope", target: peer })), (state): Result<EntityRootAccount, EntityError> => ok({
    fromEntity: self, toEntity: peer, status, currentHeight: link.height, nextProofNonce: child.dispute.nextProofNonce, currentFrameHash: link.frame,
    pendingWithdrawals: ZERO_WORD, policyRoot: unwrapOr(mapRoot(child.rebalancePolicy ?? new Map<number, RebalancePolicy>()), () => ZERO_WORD), submittedAtByTokenRoot: unwrapOr(submittedAtRoot(body), () => ZERO_WORD), state,
    committed: { ...opt("publicPinned", child.publicPinned), ...opt("boardHankoRefreshMigration", child.refreshMigration), ...opt("counterpartyBoardHankoRefresh", child.boardRefresh), ...opt("counterpartyFrameHanko", link.peerHanko), ...disputeLeafFields(child.dispute), ...(child._tag === "disputed" ? opt("activeDispute", child.active ?? child.queued) : {}), ...(child._tag === "preparing" ? opt("disputePrepare", child.prepare) : {}) },
    ...opt("counterpartySettlementHankos", peerSettlementHankos(body.settlement, localIsLeft)), ...opt("rejectedFrameEvidence", child.rejectedFrame),
  })));
};
/** og projectEntityConsensusState: deferredAccountProposals / settlementContinuations are committed as entity collections when present. */
const settleCollections = (committed: EntityCommitted): Result<EntityCommitted, EntityError> => foldResult(["deferredAccountProposals", "settlementContinuations"] as const, committed, (c, field): Result<EntityCommitted, EntityError> => {
  const live = c[field];
  return live instanceof Map ? map(entityCollectionCommitment(live as ReadonlyMap<string, Binary>), (commitment): EntityCommitted => ({ ...c, [field]: commitment as unknown as Binary })) : ok(c);
});
/** og computeCanonicalEntityConsensusStateHash over the draft: entityId, height, timestamp, config, accounts and every committed section. */
export const entityRootOf = (state: EntityState, replicas: Replicas): Result<string, EntityError> =>
  chain(frameNumber(state.height), (height) => chain(frameNumber(state.timestamp), (timestamp) => chain(traverse([...replicas], ([peer, child]) => installedAccount(state.id, peer, child)),
    (accounts) => chain(chain(chain(chain(settleCollections(state.committed), (base) => state.paybook === undefined ? ok(base) : map(paybookSection(state.paybook), (paybook): EntityCommitted => ({ ...base, paybook }))), (withPaybook) => map(crossSections(withPaybook, state), (c): EntityCommitted => (state.orderbookExt === undefined ? c : { ...c, orderbookExt: orderbookSection(state.orderbookExt) }))), crontabSection),
      (committed) => entityStateRoot({ config: rootConfig(state), accounts, entityId: state.id, height, timestamp, committed, leaderState: state.leaderState })))));
/** og state-root.ts: crossJurisdictionSwaps / crossJurisdictionAuthorizations commit as text-keyed collections once they exist. */
const crossSections = (committed: EntityCommitted, state: EntityState): Result<EntityCommitted, EntityError> => {
  const section = (m: ReadonlyMap<string, CrossRoute> | undefined): Result<Binary | undefined, EntityError> =>
    m === undefined ? ok(undefined) : map(entityCollectionCommitment(new Map([...m].map(([k, v]) => [k, v as unknown as Binary]))), (c) => c as unknown as Binary);
  const admissionsSection = (m: BookAdmissions | undefined): Result<Binary | undefined, EntityError> =>
    m === undefined ? ok(undefined) : map(entityCollectionCommitment(new Map([...m].map(([k, v]) => [k, v as unknown as Binary]))), (c) => c as unknown as Binary);
  return chain(section(state.crossJurisdictionSwaps), (swaps) => chain(section(state.crossJurisdictionAuthorizations), (auths) => map(admissionsSection(state.crossJurisdictionBookAdmissions), (admissions): EntityCommitted => ({
    ...committed, ...(swaps === undefined ? {} : { crossJurisdictionSwaps: swaps }), ...(auths === undefined ? {} : { crossJurisdictionAuthorizations: auths }),
    ...(admissions === undefined ? {} : { crossJurisdictionBookAdmissions: admissions }),
  }))));
};
/** og computeEntityFrameAuthorityRoot(buildEntityFrameAuthority(state)): config + normalizeAuthorityLeader(leaderState). */
const authorityRoot = (state: EntityState): Result<string, EntityRootError> => {
  const config = rootConfig(state), leader = signerId(state.leaderState?.activeValidatorId ?? config.validators[0] ?? "");
  if (leader.length === 0) return err({ _tag: "bad_config" });
  return chain(consensusConfig(config), (normalized) => map(encodeConsensus({
    domain: "xln.entity.frame-authority:binary",
    authority: { config: normalized, leaderState: { activeValidatorId: leader, view: state.leaderState?.view ?? 0, changedAtHeight: state.leaderState?.changedAtHeight ?? 0 } },
  }), (bytes) => bytesToHex(keccak_256(bytes))));
};
/** og account/consensus hashesToSign: the Account frames and dispute proofs this frame signs for, as secondary manifest entries. */
const messageHashes = (peer: EntityId, m: AccountPeerInput): readonly HashToSign[] => {
  const tail = peer.slice(-8);
  const acked = (a: AccountAck): readonly HashToSign[] => [
    { hash: a.frameHash, type: "accountFrame", context: `account:${tail}:ack:${a.height}` },
    ...(a.disputeHanko === undefined ? [] : [{ hash: a.disputeHanko.hash, type: "dispute", context: `account:${tail}:ack-dispute` } as const]),
  ];
  return matchBy("kind", m, {
    ack: (a) => acked(a),
    ack_frame: (f) => [...(f.ack === null ? [] : acked(f.ack)), { hash: f.frame.stateHash, type: "accountFrame", context: `account:${tail}:frame:${f.frame.height}` },
      ...(f.disputeHanko === undefined ? [] : [{ hash: f.disputeHanko.hash, type: "dispute", context: `account:${tail}:dispute` } as const])],
    dispute: (d) => [{ hash: d.disputeHanko.hash, type: "dispute", context: `account:${tail}:dispute` }],
    // og: a refresh re-Hankos an already committed frame; its hashes are signed where the board-rotation hook drafts it (boardRefreshHook), not per message
    board_hanko_refresh: () => [],
  });
};
/** og buildEntityHashesToSign: the frame hash first, then the secondary hashes sorted, a duplicate is fatal. */
const hashesToSignOf = (entityId: EntityId, height: bigint, frameHash: string, outputs: readonly EntityOutput[], txHashes: readonly HashToSign[] = []): Result<readonly HashToSign[], EntityError> => {
  const secondary = [...txHashes, ...outputs.flatMap((o) => ("tx" in o ? messageHashes(o.to, o.tx.data) : []))];
  const hashes = [frameHash, ...secondary.map((h) => h.hash)];
  if (new Set(hashes).size !== hashes.length) return err({ _tag: "secondary_hash_duplicate" });
  return ok([{ hash: frameHash, type: "entityFrame", context: `entity:${entityId.slice(-4)}:frame:${height}` }, ...[...secondary].sort((a, b) => asc(a.hash, b.hash))]);
};
const GENESIS_PARENT = "genesis";
/** og certifyEntityProposal: the proposal state takes height+1 and the frame timestamp, then state root, authority root, frame hash, manifest. */
const buildFrame = (r: EntityEnv, leader: FrameLeader, leaderState: LeaderState, timestamp: bigint, txs: readonly EntityTx[], folded: Draft, infra: HtlcFrameInfra = EMPTY_HTLC_INFRA, jPrefixCertificate?: JPrefixCertificate): Result<EntityCandidate, EntityError> => {
  const height = r.head.height + 1n, parent = parentOf(r.head), signer = signerId(leader.proposerSignerId);
  const committed: EntityCommitted = "crontabState" in folded.state.committed ? folded.state.committed : { ...folded.state.committed, crontabState: DEFAULT_CRONTAB };
  const draft: Draft = { ...folded, state: { ...folded.state, height, timestamp, committed, leaderState } };
  return chain(frameNumber(height), (heightNo) => chain(entityRootOf(draft.state, draft.accountReplicas), (stateRoot) => chain(authorityRoot(draft.state), (root) => {
    const body = {
      height, prevFrameHash: parent as EntityFrameHash, timestamp, txs, events: (folded.events ?? []).map((e): Binary => ({ ...e })), stateRoot, authorityRoot: root, leader,
      entityContext: { version: 1, proposerReplicaId: `${draft.state.id}:${signer}`, entityId: draft.state.id, proposerSignerId: signer, parentFrameHash: parent, height: heightNo, gossipProfiles: infra.gossipProfiles, peerAssertions: infra.peerAssertions, htlc: { version: 1, entries: infra.entries as unknown as readonly Binary[], originated: infra.originated as unknown as readonly Binary[] } },
      ...opt("jPrefixCertificate", jPrefixCertificate),
    };
    return chain(hashEntityFrame({ ...body, hashesToSign: [] }), (frameHash) =>
      map(hashesToSignOf(draft.state.id, height, frameHash, draft.outputs, draft.hashes), (hashesToSign): EntityCandidate => ({ frame: { ...body, hashesToSign }, signatures: new Map(), draft })));
  })));
};
const frameKey = (tx: EntityTx): string => encodeEntityTx(tx);
/** og removeCommittedTxsFromMempool: drop one mempool entry per committed (or evicted) tx. */
const withoutTxs = (mempool: readonly EntityTx[], gone: readonly EntityTx[]): readonly EntityTx[] => {
  const left = new Map<string, number>();
  for (const tx of gone) left.set(frameKey(tx), (left.get(frameKey(tx)) ?? 0) + 1);
  return mempool.filter((tx) => { const k = frameKey(tx), n = left.get(k) ?? 0; if (n === 0) return true; left.set(k, n - 1); return false; });
};
const ENTITY_MEMPOOL_SIZE = 10_000;
/** og appendEntityMempoolTransactions: exact Account-input retries collapse, every other tx keeps order and multiplicity. */
const appendMempool = (mempool: readonly EntityTx[], admitted: readonly EntityTx[]): readonly EntityTx[] => {
  const accountKey = (tx: EntityTx): string | undefined => (tx.type === "accountInput" ? frameKey(tx) : undefined);
  return [...mempool, ...firstBy(admitted, accountKey, mempool.flatMap((tx) => { const k = accountKey(tx); return k === undefined ? [] : [k]; }))];
};
/** og finalizeCommitNotification: install the candidate, emit its Account outputs, optionally broadcast the certified frame to the other validators. */
const installFrame = (r: EntityEnv & EntityCandidate, frameHash: EntityFrameHash, signatures: Precommits, broadcast: boolean | Quorum): Result<EntityApply<OpenEntity>, EntityError> =>
  chain(secondaryHankos(r.draft, r.frame, signatures), (byHash) => chain(fillHankos(r.draft, byHash), (draft) => chain(commitEffects(r, frameHash, byHash, draft), (committed) => map(publishFrame(r, frameHash, signatures, broadcast, draft), (a) => ({ ...a, committed })))));
/** og isWitnessHashType + requireDraftWitness over one frame's fresh witnesses: this height's Hanko for the hash, of the expected type. */
const draftWitness = (witnesses: ReadonlyMap<string, Omit<HankoWitness, "createdAt">>, hash: unknown, type: HankoWitness["type"], height: number, existing: unknown): Result<Hanko, EntityError> => {
  const entry = witnesses.get(String(hash));
  if (typeof existing === "string" && existing !== "") {
    if (entry === undefined) return ok(existing);
    if (entry.type !== type || entry.entityHeight > height) return invariant(`HANKO_WITNESS_BINDING_MISMATCH:hash=${String(hash)}`);
    return entry.hanko === existing ? ok(existing) : invariant(`HANKO_WITNESS_VALUE_MISMATCH:hash=${String(hash)}:type=${type}`);
  }
  if (entry === undefined) return invariant(`HANKO_DRAFT_WITNESS_MISSING:hash=${String(hash)}:type=${type}:entityHeight=${height}`);
  return entry.type !== type || entry.entityHeight !== height ? invariant(`HANKO_WITNESS_BINDING_MISMATCH:hash=${String(hash)}`) : ok(entry.hanko);
};
type JTxFields = { readonly [field: string]: Binary };
/** og attachHankoWitnessToOutputs (J half): a batch gets its batch-hash Hanko, an EntityProvider action its action-hash Hanko, a control-board proposal its own supporter vote. */
const attachJHankos = (jOutputs: readonly JInput[], witnesses: ReadonlyMap<string, Omit<HankoWitness, "createdAt">>, height: number): Result<readonly JInput[], EntityError> =>
  traverse(jOutputs, (input) => map(traverse(input.jTxs, (raw): Result<Binary, EntityError> => {
    const jTx = raw as { readonly type?: unknown; readonly entityId?: unknown; readonly data?: JTxFields };
    const data = jTx.data ?? {};
    if (jTx.type === "batch" && data["batchHash"]) return map(draftWitness(witnesses, data["batchHash"], "jBatch", height, data["hankoSignature"]), (hankoSignature) => ({ ...(raw as JTxFields), data: { ...data, hankoSignature } }));
    if (jTx.type === "entityProviderTransfer" || jTx.type === "entityProviderReleaseControlShares" || jTx.type === "entityProviderCancelAction") {
      const intent = data["intent"] as { readonly actionHash?: unknown } | undefined;
      return map(draftWitness(witnesses, intent?.actionHash, "entityProviderAction", height, data["hankoSignature"]), (hankoSignature) => ({ ...(raw as JTxFields), data: { ...data, hankoSignature } }));
    }
    if (jTx.type === "entityProviderProposeControlBoard") {
      const votes = (data["supporterVotes"] ?? []) as readonly { readonly entityId?: unknown; readonly hankoSignature?: unknown }[];
      const own = votes.findIndex((v) => v.entityId === jTx.entityId);
      if (own < 0 || votes[own]?.hankoSignature) return invariant(`CONTROL_BOARD_PROPOSAL_OWN_VOTE_INVALID:${String(jTx.entityId)}`);
      return map(draftWitness(witnesses, data["proposalHash"], "entityProviderAction", height, undefined), (hankoSignature) =>
        ({ ...(raw as JTxFields), data: { ...data, supporterVotes: votes.map((v, i) => (i === own ? { ...v, hankoSignature } : v)) as unknown as Binary } }));
    }
    return ok(raw);
  }), (jTxs) => ({ jurisdictionName: input.jurisdictionName, jTxs })));
/** og attachCommitProofsAndOutputs: witness every secondary hash of a witness type; the emitter (relay next leader, else proposer) carries the jOutputs. */
const commitEffects = (r: EntityEnv & EntityCandidate, frameHash: EntityFrameHash, byHash: ReadonlyMap<string, Hanko>, draft: Draft): Result<CommitEffects, EntityError> => {
  const height = Number(r.frame.height);
  const witnesses = r.frame.hashesToSign.flatMap((h): (readonly [string, Omit<HankoWitness, "createdAt">])[] => {
    const hanko = byHash.get(h.hash), type = h.type as string;
    return hanko === undefined || type === "entityFrame" || type === "entityOutput" ? [] : [[h.hash, { hanko, type: type as HankoWitness["type"], entityHeight: height }]];
  });
  const relay = r.frame.leader.relayCertificate, emitter = relay !== undefined && relay.preparedFrameHash === frameHash ? relay.nextLeaderId : r.frame.leader.proposerSignerId;
  return map(attachJHankos(draft.jOutputs ?? [], new Map(witnesses), height), (jOutputs) => ({ height, witnesses, jOutputs: lower(emitter) === lower(r.signerId) ? jOutputs : [], runtimeEvents: draft.runtimeEvents ?? [] }));
};
/**
 * og buildCertifiedEntityFrameLink + projectCertifiedEntityFrameLinkIdentity: the frame's manifest head, the first signature of each signer,
 * the entity-frame Hanko over the post-frame board (og installCommittedState `hankos[0]`), the leader and the post-frame authority.
 */
const certifiedLink = (frame: EntityFrame, frameHash: EntityFrameHash, signatures: Precommits, state: EntityState): Result<{ readonly link: Binary; readonly hanko: Hanko }, EntityError> => {
  const head = frame.hashesToSign[0];
  if (head === undefined || head.type !== "entityFrame" || head.hash !== frameHash) return invariant(`ENTITY_CERTIFIED_LINK_FRAME_MANIFEST_INVALID:${frame.height}:${frameHash}`);
  if (signatures.size === 0) return invariant(`ENTITY_CERTIFIED_LINK_SIGNATURES_MISSING:${frame.height}:${frameHash}`);
  const first = new Map([...signatures].flatMap(([id, bundle]) => { const s = bundle[0]; return s === undefined ? [] : [[id, s] as const]; }));
  const missing = [...signatures.keys()].find((id) => !first.has(id));
  if (missing !== undefined) return invariant(`ENTITY_CERTIFIED_LINK_FRAME_SIGNATURE_MISSING:${frame.height}:${missing}`);
  const config = rootConfig(state), active = signerId(state.leaderState?.activeValidatorId ?? config.validators[0] ?? "");
  if (active.length === 0) return invariant("ENTITY_FRAME_AUTHORITY_LEADER_MISSING");
  return chain(quorumHanko(state, frameHash, first), (hanko) => map(leaderBinary(frame.leader, false), (leader) => ({ hanko, link: {
    frameHash: lower(frameHash), parentFrameHash: frame.prevFrameHash, stateRoot: lower(frame.stateRoot), authorityRoot: lower(frame.authorityRoot), leader,
    hashesToSign: [{ hash: head.hash, type: head.type, context: head.context }], collectedSigs: precommitsBinary(new Map([...first].map(([id, s]) => [id, [s]]))), hankos: [hanko],
    postAuthority: { config: binaryOf(config), leaderState: { activeValidatorId: active, view: state.leaderState?.view ?? 0, changedAtHeight: state.leaderState?.changedAtHeight ?? 0 } },
  } as Binary })));
};
const publishFrame = (r: EntityEnv & EntityCandidate, frameHash: EntityFrameHash, signatures: Precommits, broadcast: boolean | Quorum, draft: Draft): Result<EntityApply<OpenEntity>, EntityError> => chain(certifiedLink(r.frame, frameHash, signatures, draft.state), ({ link, hanko }) => {
  // og finalizeCommitNotification broadcastValidators: the committed (post-frame) board unless a handover names the retired one
  const board = broadcast === true ? draft.state.quorum : broadcast === false ? undefined : broadcast;
  const others = board === undefined ? [] : [...membersOf(board).keys()].filter((v) => signerId(v) !== signerId(r.signerId));
  // og finalizeCommitNotification: votes reset; a relay certificate for exactly this frame stays pending.
  const relay = r.frame.leader.relayCertificate, pending = relay !== undefined && relay.preparedFrameHash === frameHash ? relay : undefined;
  const opened: OpenEntity = { ...openEntity(r.signerId, draft.state, { height: r.frame.height, prevFrameHash: frameHash }, withoutTxs(r.mempool, r.frame.txs), draft.accountReplicas), certifiedFrameHead: link, leaderVotes: new Map(), ...opt("pendingLeaderCertificate", pending),
    // og clearCommittedJPrefixRound: a round for a height this frame committed is done
    ...opt("jPrefixRound", r.jPrefixRound !== undefined && r.jPrefixRound.targetEntityHeight > Number(r.frame.height) ? r.jPrefixRound : undefined) };
  // og finalizeCommitNotification emitter: the relay certificate's next leader for exactly this frame, else the frame's proposer.
  const emitter = pending !== undefined ? pending.nextLeaderId : r.frame.leader.proposerSignerId;
  return ok(done(opened, [...publishCommitted(draft.outputs, r.signerId, emitter), ...others.map((v): EntityOutput => ({ to: r.state.id, signerId: v, input: { kind: "proposal", frame: r.frame, signatures, hankos: [hanko] } }))]));
});
/** og admitEntityTransactions + startEntityProposalIfReady: queue, forward a non-leader mempool to the leader, or propose from the mempool. */
const admitTxs = <R extends EntityReplica>(r: R, input: Extract<EntityInput, { kind: "txs" }>, ctx: EntityContext): Result<R, EntityError> => {
  if (input.timestamp < 0n || input.timestamp > BigInt(Number.MAX_SAFE_INTEGER)) return err({ _tag: "frame_timestamp_invalid", timestamp: input.timestamp });
  const from = ctx.from;
  // og external ingress: Account inputs from their sender, or exactly one certified runtimeOutput naming the sending Entity as its source
  const certified = input.txs.length === 1 && input.txs[0]?.type === "runtimeOutput" && from !== undefined && namesEntity(input.txs[0].data.sourceEntityId, from);
  if (from !== undefined && !certified && (from === ctx.self || !input.txs.every((tx) => tx.type === "accountInput" && namesEntity(tx.data.fromEntityId, from) && namesEntity(tx.data.toEntityId, ctx.self)))) return err({ _tag: "from_not_converted" });
  return chain(crossMaterializations(r, input.txs, ctx, input.timestamp), (txs): Result<R, EntityError> => {
    if (txs.length > ENTITY_MEMPOOL_SIZE || r.mempool.length + txs.length > ENTITY_MEMPOOL_SIZE) return err({ _tag: "mempool_full" });
    // og admitEntityTransactions: a pure Account-input delta is appended; any other admission re-orders the mempool wake-first
    const appended = appendMempool(r.mempool, txs);
    return txs.every((tx) => tx.type === "accountInput") ? ok({ ...r, mempool: appended }) : map(prioritizeWake(appended), (mempool) => ({ ...r, mempool }));
  });
};
/** og admission (forwardValidatorMempool over the authority replica): a replica that is not the proposal leader forwards its mempool to that leader. */
const forwarded = (r: EntityEnv, timestamp: bigint): Result<readonly EntityOutput[], EntityError> => map(authorityReplica(r, r.mempool), (a) => {
  const leader = proposalLeader(a).activeValidatorId, to = memberId(a.state.quorum, leader);
  return isProposalLeader(a) || r.mempool.length === 0 || to === undefined ? [] : [{ to: r.state.id, signerId: to, input: { kind: "txs", timestamp, txs: r.mempool } }];
});
/** og shouldStartProposal: a certified view change (without a prepared frame to relay) proposes even an empty frame. */
const certifiedTransition = (r: EntityEnv): boolean => { const c = r.pendingLeaderCertificate; return c !== undefined && c.targetHeight === Number(r.head.height) + 1 && c.preparedFrameHash === undefined; };
/** og startEntityProposalIfReady + certifyEntityProposal: fold the mempool, commit the proposal leader's view, self-sign, commit alone or broadcast. */
/** og: after admission (with its mempool forward), a J-prefix input or local consensus work first signs this validator's head (ensureLocalJPrefixAttestation). */
const startProposal = (admitted: OpenEntity, runtimeTimestamp: bigint, ctx: EntityContext, force = false): Result<EntityApply<OpenEntity | ProposedEntity>, EntityError> =>
  chain(force || admitted.mempool.length > 0 || hasProposableAccount(admitted) ? ensureLocalJPrefix(admitted, ctx, force) : ok({ replica: admitted, outputs: [], signed: false }), ({ replica: queued, outputs: votes }) =>
  chain(authorityReplica(queued, queued.mempool), (authority) => {
    if (!isProposalLeader(authority)) return map(forwarded(queued, runtimeTimestamp), (out) => done<OpenEntity | ProposedEntity, EntityOutput>(queued, [...out, ...votes]));
    // og selectEntityProposal: the J-prefix certificate and certified range first, no selection while a required certificate is missing
    return chain(jPrefixSelection(queued, authority, ctx), ({ replica: selecting, certificate, blocked, frozen }) => chain(blocked ? ok<ProposableSelection>({ txs: [], currentAuthorityReady: false }) : selectProposable(selecting.state, selecting.mempool), (selection) => {
      // og: a frozen-base roll proposes exactly an empty frame
      const txs = frozen ? [] : selection.txs;
      // og selectEntityProposal: an atomic cross-j leg's accountInput must be in the proposal
      const required = ctx.required === undefined ? undefined : encodeEntityTx(ctx.required);
      if (required !== undefined && !txs.some((tx) => encodeEntityTx(tx) === required))
        return invariant(`RUNTIME_CROSS_J_ATOMIC_ACCOUNT_INPUT_NOT_SELECTED:${selecting.state.id}:mempool=${selecting.mempool.length}:selected=${txs.length}:reason=${blocked ? "J_PREFIX_QUORUM_REQUIRED" : "none"}`);
      // og shouldStartProposal: selected txs, a frozen-base roll, or a certified board with queued Account work or a certified leader transition
      if (txs.length === 0 && !frozen && !(selection.currentAuthorityReady && (hasProposableAccount(selecting) || certifiedTransition(selecting)))) return ok(done<OpenEntity | ProposedEntity, EntityOutput>(selecting, votes));
      return chain(assertProposalPrefix(selecting, txs, certificate, ctx), () =>
        map(proposeSelected(selecting, authority, txs, runtimeTimestamp, ctx, certificate ?? undefined), (a) => ({ ...a, outputs: [...votes, ...a.outputs] })));
    }));
  }));
/** `keep`: og shouldKeepPreparedEntityFrame -- an account-work preview that proposes no Account frame is discarded (no Entity frame). */
const proposeSelected = (queued: OpenEntity, authority: OpenEntity, selected: readonly EntityTx[], runtimeTimestamp: bigint, ctx: EntityContext, jPrefixCertificate?: JPrefixCertificate, keep?: (accountFrames: number) => boolean): Result<EntityApply<OpenEntity | ProposedEntity>, EntityError> => {
  /** og resolveEntityProposalTimestamp: never behind the committed clock. */
  const timestamp = runtimeTimestamp > queued.state.timestamp ? runtimeTimestamp : queued.state.timestamp;
  // og getReplicaProposalLeader(authorityReplica): the view the proposer claims; buildProposalState: a handover frame installs its own leader
  const view = proposalLeader(authority).view, self = signerId(queued.signerId), pending = queued.pendingLeaderCertificate;
  const leader: FrameLeader = { proposerSignerId: self, view, ...opt("certificate", pending) };
  const ordinary: LeaderState = { activeValidatorId: self, view, changedAtHeight: pending !== undefined ? Number(queued.head.height) + 1 : queued.state.leaderState?.changedAtHeight ?? 0 };
  // og startInboundLayerPriming reads requireEntityEncryptionPrivateKey for every proposal, before any materialization
  const keypair = entityKeypair(queued.state, ctx);
  if (!keypair.ok) return keypair;
  // og materializeEntityInfraContext: the proposer prepares every htlcPayment before the fold; a payment it cannot prepare is evicted like any refused tx.
  const phase = selected;
  const prepared = phase.some((tx) => tx.type === "htlcPayment") ? materializeOriginated(originView(queued.state, queued.accountReplicas, timestamp), ctx.htlc?.profiles ?? [], phase, ctx.htlc ?? { profiles: [] }) : { originated: [], refused: new Map<EntityTx, EntityError>() };
  const txs = phase.filter((tx) => !prepared.refused.has(tx)), [firstRefusal] = prepared.refused.values();
  if (txs.length === 0 && firstRefusal !== undefined) return err(firstRefusal);
  // og materializeHtlcPreparedInfraContext: the proposer decrypts every inbound onion layer against the pre-frame state; validators replay the same bytes.
  const inbound = { state: queued.state, replicas: queued.accountReplicas, timestamp: Number(timestamp), publicKey: String(queued.state.committed["entityEncryptionPublicKey"] ?? ""), privateKey: ctx.htlc?.encryptionPrivateKey };
  return chain(htlcFrameTxs(txs) ? inboundHtlcEntries({ ...inbound, online: onlineObserver(ctx.htlc).online }, txs) : ok([]), (entries) =>
  chain(foldTxs(queued.state, queued.accountReplicas, txs, { verify: ctx.verify, timestamp, htlc: { ...EMPTY_HTLC_INFRA, originated: prepared.originated, entries }, ...opt("activeJurisdiction", ctx.activeJurisdiction), ...opt("siblings", ctx.siblings), ...opt("boardAuthority", ctx.boardAuthority), ...opt("jReplicas", ctx.jReplicas), ...opt("runtimeSeed", ctx.runtimeSeed) }), ({ draft, included, evicted, accountFrames }) => chain(frameHtlcInfra(ctx.htlc, inbound, prepared.originated, included), (infra) => {
    if (keep !== undefined && !keep(accountFrames ?? 0)) return ok(done<OpenEntity | ProposedEntity, EntityOutput>(queued));
    const pool = withoutTxs(queued.mempool, [...prepared.refused.keys(), ...evicted]);
    return chain(handoverLeaderState(queued.state, included), (handoverLeader) => chain(buildFrame(queued, leader, handoverLeader ?? ordinary, timestamp, included, draft, infra, jPrefixCertificate), (candidate) => chain(signManifest(candidate.frame.hashesToSign, queued.signerId, ctx, candidate.draft.state), (own) => chain(hashEntityFrame(candidate.frame), (frameHash): Result<EntityApply<OpenEntity | ProposedEntity>, EntityError> => {
      const proposed: ProposedEntity = { ...queued, _tag: "proposed", mempool: pool, ...candidate, signatures: new Map([[self, own]]) };
      // og: a single-signer (authority) board commits alone; a handover also delivers the certified transition to the retired board
      if (isSingleSigner(authority.state.quorum)) return installFrame(proposed, frameHash, proposed.signatures, handoverLeader !== null ? queued.state.quorum : false);
      // og: a handover proposal goes to the new board, any other proposal to the committed one
      const board = included.some((tx) => tx.type === "boardHandover") ? candidate.draft.state.quorum : queued.state.quorum;
      const others = [...membersOf(board).keys()].filter((v) => signerId(v) !== self);
      return ok(done<OpenEntity | ProposedEntity, EntityOutput>(proposed, others.map((v): EntityOutput => ({ to: queued.state.id, signerId: v, input: { kind: "proposal", frame: candidate.frame, signatures: proposed.signatures } }))));
    }))));
  })));
};
// ---- og entity/consensus/j-prefix/{prefix-round, prefix-input, prefix-validation}.ts + proposal/selection.ts: the J prefix inside Entity consensus ----
const jpView = (r: EntityEnv): JPrefixView => ({ state: r.state, head: r.head });
/** og verifyAccountSignature / signAccountFrame for this replica: it signs only as its own validator. */
const jpCrypto = (r: EntityEnv, ctx: EntityContext): JPrefixCrypto => ({
  verify: jPrefixVerify,
  sign: (id, digest) => (nText(id) === signerId(r.signerId) ? map(ctx.sign(digest as Hash, r.signerId), (s) => ogSig(s)) : err(`J_PREFIX_SIGNER_NOT_LOCAL:${id}`)),
});
const jpError = (f: JPrefixFailure): EntityError => ({ _tag: "j_prefix", code: f.code, message: f.message, disposition: f.disposition });
const jpE = <T>(r: JP<T>): Result<T, EntityError> => mapErr(r, jpError);
/** og rejectEntityConsensusInput / deferEntityConsensusInput with its reason code. */
const jpInput = (code: string, disposition: "reject" | "defer" = "reject", message: string = code): EntityError => ({ _tag: "j_prefix", code, message, disposition });
const errText = (e: EntityError): string => (e._tag === "entity_invariant" ? e.reason : e._tag);
/** og MAX_VALIDATORS (replica-validation hasWellFormedJPrefixAttestations). */
const MAX_J_PREFIX_ATTESTATIONS = 100;
type Attested<R> = { readonly replica: R; readonly outputs: readonly EntityOutput[]; readonly signed: boolean };
const jpSend = (r: EntityEnv, board: Quorum, me: string, attestation: JPrefixAttestation): readonly EntityOutput[] =>
  [...membersOf(board).keys()].filter((v) => signerId(v) !== me).map((v): EntityOutput => ({ to: r.state.id, signerId: v, input: { kind: "jPrefixAttestations", attestations: new Map([[me, attestation]]) } }));
/**
 * og ensureLocalJPrefixAttestation: an open replica with no current-round head signs its validator-local prefix for the next Entity height
 * (under a pending handover's board) and sends it to the other validators; a history behind the certified base retries the input.
 */
const ensureLocalJPrefix = (r: OpenEntity, ctx: EntityContext, force: boolean): Result<Attested<OpenEntity>, EntityError> => {
  const none: Attested<OpenEntity> = { replica: r, outputs: [], signed: false }, h = ctx.jHistory, finalized = jpFinalized(jpView(r));
  return chain(pendingHandoverConfig(r.state, r.mempool), (pending) => chain(jpE(hasCurrentRoundJPrefixAttestation(jpView(r), r.signerId, r.jPrefixRound, h)), (current): Result<Attested<OpenEntity>, EntityError> => {
    if (pending === null && current) return ok(none);
    if (!force && !entityRequiresJPrefixCertificate(r.state) && !hasPendingLocalJEvent(jpView(r), h)) return ok(none);
    if (h === undefined) return ok(none);
    if (h.scannedThroughHeight < finalized) return err(jpError({ disposition: "retry", code: "J_PREFIX_LOCAL_HISTORY_BEHIND", message: `J_PREFIX_LOCAL_HISTORY_BEHIND:${h.scannedThroughHeight}:${finalized}` }));
    return chain(jpE(localJPrefixAttestableHeight(jpView(r), h)), (height) => height === null ? ok(none) : chain(authorityReplica(r, r.mempool), (auth) =>
      chain(jpE(buildLocalJPrefixAttestation(jpView(auth), r.signerId, h, jpCrypto(r, ctx))), (attestation): Result<Attested<OpenEntity>, EntityError> => {
        if (attestation === null) return invariant(`J_PREFIX_LOCAL_ATTESTATION_MISSING:${r.state.id}:${h.scannedThroughHeight}`);
        const me = signerId(r.signerId), existing = pending === null ? r.jPrefixRound : undefined;
        const voted = existing !== undefined && existing.targetEntityHeight === Number(r.state.height) + 1 && existing.baseHeight === finalized && existing.attestations.has(me);
        return map(jpE(mergeJPrefixAttestations(jpView(auth), voted ? undefined : existing, new Map([[me, attestation]]), jpCrypto(r, ctx))), (round) =>
          ({ replica: { ...r, jPrefixRound: round }, outputs: jpSend(r, auth.state.quorum, me, attestation), signed: true }));
      })));
  }));
};
type JPrefixSelection<R> = { readonly replica: R; readonly certificate: JPrefixCertificate | null; readonly blocked: boolean; readonly frozen: boolean; readonly range?: EntityTx | undefined };
/**
 * og selectEntityProposal's J half (a proposal leader only): the round's certificate under the pending handover's board, a certified range above
 * the finalized height first in the mempool (replacing every other j_event), no selection without a required certificate, and the frozen-base roll.
 */
/** `trusted`: og addCertifiedJRange for a trusted local cross-j command -- the certified range leads the command's frame and never enters the mempool. */
const jPrefixSelection = <R extends EntityEnv>(r: R, authority: EntityEnv, ctx: EntityContext, trusted = false): Result<JPrefixSelection<R>, EntityError> => {
  const round = r.jPrefixRound;
  return chain(round === undefined ? ok(null) : jpE(buildJPrefixCertificate(jpView(authority), round.attestations)), (certificate) => {
    const certified = (): Result<{ readonly replica: R; readonly range?: EntityTx | undefined }, EntityError> => {
      if (certificate === null || round === undefined) return ok({ replica: r });
      const withCertificate: R = { ...r, jPrefixRound: { ...round, certificate } };
      if (certificate.selected.scannedThroughHeight <= jpFinalized(jpView(r))) return ok({ replica: withCertificate });
      return chain(jpE(buildCertifiedJPrefixTx(jpView(authority), ctx.jHistory, certificate, proposalLeader(authority).activeValidatorId, jpCrypto(r, ctx))), (range) =>
        trusted ? ok({ replica: withCertificate, range }) : map(prioritizeWake([range, ...r.mempool.filter((tx) => tx.type !== "j_event")]), (mempool) => ({ replica: { ...withCertificate, mempool } as R, range })));
    };
    return chain(certified(), ({ replica, range }) => {
      const blocked = certificate === null && (entityRequiresJPrefixCertificate(authority.state) || hasPendingLocalJEvent(jpView(r), ctx.jHistory));
      return map(jpE(isFrozenBaseJPrefixRollAuthorized(jpView(replica), replica.signerId, replica.jPrefixRound, ctx.jHistory, certificate)), (frozen) => ({ replica, certificate, blocked, frozen, ...opt("range", range) }));
    });
  });
};
/** og selection on a replica holding a frame: the leader still installs the round's certificate and certified range, it proposes nothing. */
const heldSelection = <R extends ProposedEntity | LockedEntity>(a: EntityApply<OpenEntity | R>, ctx: EntityContext): Result<EntityApply<OpenEntity | R>, EntityError> => {
  const r = a.replica;
  if (r._tag === "open") return ok(a);
  return chain(authorityReplica(r, r.mempool), (authority) => !isProposalLeader(authority) ? ok(a) : map(jPrefixSelection(r as R, authority, ctx), (s) => ({ ...a, replica: s.replica })));
};
/** og getReplicaJRangeValidationError: the J-range budget, then every range against this validator's history under the frame's authority (a message, or null). */
const replicaJRangeError = (r: EntityEnv, txs: readonly EntityTx[], ctx: EntityContext): Result<string | null, EntityError> => {
  const ranges = txs.filter((tx): tx is JEventTx => tx.type === "j_event");
  const budget = frameJRangeIssue(ranges);
  if (!budget.ok) return ok(errText(budget.error));
  if (budget.value !== null) return ok(budget.value);
  const config = handoverFrameConfig(r.state, txs);
  if (!config.ok) return ok(errText(config.error));
  const authority = config.value === null ? ok(r.state) : withBoardAuthority(r.state, config.value, Number(r.state.height) + 1);
  if (!authority.ok) return ok(errText(authority.error));
  for (const tx of ranges) {
    const issue = jpE(jEventRangeLocalHistoryError(authority.value, ctx.jHistory, tx.data as JRec));
    if (!issue.ok || issue.value !== null) return issue;
  }
  return ok(null);
};
const jpFrame = (frame: EntityFrame): JPrefixFrame => ({ height: Number(frame.height), parentFrameHash: frame.prevFrameHash, proposerSignerId: frame.leader.proposerSignerId, txs: frame.txs, jPrefixCertificate: frame.jPrefixCertificate });
/** og getFrameJPrefixValidationError: assertFrameJPrefix under the frame's handover board (whose round is retired), as a failure or null. */
const frameJPrefixFailure = (r: EntityEnv, frame: EntityFrame, ctx: EntityContext): Result<JPrefixFailure | null, EntityError> => {
  const config = handoverFrameConfig(r.state, frame.txs);
  const authority = !config.ok ? config : config.value === null ? ok({ state: r.state, round: r.jPrefixRound }) : map(withBoardAuthority(r.state, config.value, Number(frame.height)), (state) => ({ state, round: undefined }));
  if (!authority.ok) return ok({ disposition: "reject", code: "J_PREFIX_INVALID", message: errText(authority.error) });
  const checked = assertFrameJPrefix({ state: authority.value.state, head: r.head }, r.signerId, authority.value.round, ctx.jHistory, jpFrame(frame), jpCrypto(r, ctx));
  return checked.ok ? ok(null) : checked.error.disposition === "halt" ? err(jpError(checked.error)) : ok(checked.error);
};
/**
 * og validateProposalViewAndJRange (lane PROPOSAL) / validateCatchUpJRange (lane COMMIT): a range unlike this validator's history rejects; a history
 * behind the certificate defers; any other prefix failure rejects, except that catch-up applies a frame its newer local prefix only outruns.
 */
const jPrefixFrameChecks = (r: EntityEnv, frame: EntityFrame, ctx: EntityContext, lane: "PROPOSAL" | "COMMIT"): Result<void, EntityError> =>
  chain(replicaJRangeError(r, frame.txs, ctx), (rangeError) => rangeError !== null ? err(jpInput(`${lane}_J_RANGE_MISMATCH`, "reject", rangeError)) : chain(frameJPrefixFailure(r, frame, ctx), (failure): Result<void, EntityError> => {
    if (failure === null) return ok(undefined);
    if (failure.code === "J_PREFIX_LOCAL_HISTORY_BEHIND") return err(jpInput(`${lane}_J_PREFIX_HISTORY_WAIT`, "defer", failure.message));
    return lane === "COMMIT" && failure.disposition === "retry" ? ok(undefined) : err(jpInput(`${lane}_J_RANGE_MISMATCH`, "reject", failure.message));
  }));
/** og replayPreparedFrameForRelay's J checks: the prepared frame's ranges and prefix, both fatal to the input. */
const preparedJPrefix = (r: EntityEnv, frame: EntityFrame, ctx: EntityContext): Result<void, EntityError> =>
  chain(replicaJRangeError(r, frame.txs, ctx), (rangeError) => rangeError !== null ? invariant(`ENTITY_PREPARED_J_RANGE_MISMATCH:${rangeError}`) : jpE(assertFrameJPrefix(jpView(r), r.signerId, r.jPrefixRound, ctx.jHistory, jpFrame(frame), jpCrypto(r, ctx))));
/** og assertProposalPrefix: the proposer's own ranges against its committed history, then assertFrameJPrefix under the frame's board. */
const assertProposalPrefix = (r: OpenEntity, txs: readonly EntityTx[], certificate: JPrefixCertificate | null, ctx: EntityContext): Result<void, EntityError> =>
  chain(replicaJRangeError(r, txs, ctx), (rangeError) => rangeError !== null ? invariant(`ENTITY_PROPOSER_J_RANGE_INVALID:${rangeError}`) :
    chain(handoverFrameConfig(r.state, txs), (config) => chain(config === null ? ok(r.state) : withBoardAuthority(r.state, config, Number(r.state.height) + 1), (state) =>
      chain(authorityReplica(r, r.mempool), (pending) => jpE(assertFrameJPrefix({ state, head: r.head }, r.signerId, r.jPrefixRound, ctx.jHistory,
        { height: Number(r.head.height) + 1, parentFrameHash: parentOf(r.head), proposerSignerId: signerId(pending.signerId), txs, jPrefixCertificate: certificate }, jpCrypto(r, ctx)))))));
type JPrefixInput = Extract<EntityInput, { kind: "jPrefixAttestations" }>;
/**
 * og handleJPrefixAttestations, then the rest of applyEntityInput: one target height per input; an out-of-round vote is authenticated against the
 * board (a future one defers, a stale one is a no-op unless due local J work re-signs); a current one merges into the round (a changed round is
 * frozen while a frame is held) and this validator's own vote is re-sent when new. The input then continues as an empty `txs` input would.
 */
const jPrefixAttestationsInput = (r: EntityReplica, input: JPrefixInput, ctx: EntityContext): Result<EntityApply, EntityError> => {
  const incoming = input.attestations, timestamp = input.timestamp ?? r.state.timestamp, rejected = jpInput("J_PREFIX_ATTESTATION_REJECTED");
  if (!(incoming instanceof Map) || incoming.size === 0 || incoming.size > MAX_J_PREFIX_ATTESTATIONS) return err(jpInput("J_PREFIX_ATTESTATION_INVALID"));
  const continued = (next: EntityReplica, before: readonly EntityOutput[]): Result<EntityApply, EntityError> => {
    const flow: Result<EntityApply, EntityError> = next._tag === "open" ? startProposal(next, timestamp, ctx, true)
      : chain(forwarded(next, timestamp), (out) => chain(heldQuorum(next, out), (a) => heldSelection(a, ctx)));
    return map(flow, (a) => ({ ...a, outputs: [...before, ...a.outputs] }));
  };
  return chain(authorityReplica(r, r.mempool), (auth) => {
    const v = jpView(auth), crypto = jpCrypto(r, ctx);
    const dispositions = new Set<string>();
    for (const a of incoming.values()) { const d = jPrefixTemporalDisposition(v, a); if (!d.ok) return err(rejected); dispositions.add(d.value); }
    const [disposition] = dispositions;
    if (dispositions.size !== 1 || disposition === undefined) return err(rejected);
    if (disposition !== "current") {
      for (const [raw, a] of incoming) { const verified = verifyOutOfRoundJPrefixAttestation(v, a, [auth.state.quorum], crypto); if (!verified.ok || nText(raw) !== verified.value.validatorId) return err(rejected); }
      if (disposition === "future") return err(jpInput("J_PREFIX_FUTURE_HEIGHT", "defer"));
      if (r._tag !== "open") return ok(done(r));
      return chain(jpE(hasDueLocalJPrefixAdvance(jpView(r), ctx.jHistory)), (due) => !due ? ok(done<EntityReplica, EntityOutput>(r)) : chain(ensureLocalJPrefix(r, ctx, false), (s) => (s.signed ? continued(s.replica, s.outputs) : ok(done<EntityReplica, EntityOutput>(r)))));
    }
    const prior = r.jPrefixRound, merged = mergeJPrefixAttestations(v, auth === r ? prior : undefined, incoming, crypto);
    if (!merged.ok) return err(rejected);
    const changed = !consensusEqual(prior?.attestations ?? new Map(), merged.value.attestations);
    if (changed && r._tag !== "open") return err(jpInput("J_PREFIX_ROUND_FROZEN"));
    const me = signerId(r.signerId), mine = [...incoming].filter(([id]) => nText(id) === me), previous = prior?.attestations.get(me);
    const resend = mine.flatMap(([, a]) => (previous !== undefined && consensusEqual(previous, a) ? [] : jpSend(r, auth.state.quorum, me, a as JPrefixAttestation)));
    return continued({ ...r, jPrefixRound: merged.value }, resend);
  });
};
/**
 * og finalizeCommitNotification tail: pruneReplicaFinalizedJHistory, then advanceLocalJPrefixRoundAfterCommit (the committed round was cleared by
 * publishFrame): due J work re-signs for the next height, and the leader holding a certificate above the finalized height wakes itself.
 */
const afterCommit = (a: EntityApply, ctx: EntityContext): Result<EntityApply, EntityError> => {
  const r = a.replica;
  if (r._tag !== "open") return ok(a);
  return chain(jpE(pruneFinalizedJHistory(ctx.jHistory, jpFinalized(jpView(r)))), (h) => chain(jpE(hasDueLocalJPrefixAdvance(jpView(r), h)), (due) => !due ? ok(a) :
    map(ensureLocalJPrefix(r, { ...ctx, jHistory: h }, false), (s): EntityApply => {
      const c = s.replica.jPrefixRound?.certificate;
      const wake = s.signed && isActiveLeader(s.replica) && c !== undefined && c.selected.scannedThroughHeight > jpFinalized(jpView(s.replica));
      return { ...a, replica: s.replica, outputs: [...a.outputs, ...s.outputs, ...(wake ? [{ to: r.state.id, signerId: r.signerId, input: { kind: "txs", timestamp: r.state.timestamp, txs: [] } } as EntityOutput] : [])] };
    })));
};
/** og hasEntityLeaderWork's J terms: a round certificate above the finalized height, or an authorized frozen-base roll. */
const jPrefixLeaderWork = (r: EntityReplica, h: ValidatorJHistory | undefined): boolean => {
  const c = r.jPrefixRound?.certificate;
  if (c !== undefined && c.selected.scannedThroughHeight > jpFinalized(jpView(r))) return true;
  return unwrapOr(isFrozenBaseJPrefixRollAuthorized(jpView(r), r.signerId, r.jPrefixRound, h, c), () => false);
};
/**
 * og applyEntityInput for a `txs` input, by lane: `defer` stops after admission (its mempool forward included), `cross-j` and `account-work` are
 * og's trusted Runtime-local protocols, any other input admits and proposes.
 */
export const applyTxsOpen = (r: OpenEntity, input: Extract<EntityInput, { kind: "txs" }>, ctx: EntityContext): Result<EntityApply<OpenEntity | ProposedEntity>, EntityError> => {
  if (ctx.lane === "cross-j") return trustedCrossCommand(r, input, ctx);
  if (ctx.lane === "account-work") return accountWorkProposal(r, input.timestamp, ctx);
  return chain(admitTxs(r, input, ctx), (queued) => (ctx.lane === "defer" ? map(forwarded(queued, input.timestamp), (out) => done<OpenEntity | ProposedEntity, EntityOutput>(queued, out)) : startProposal(queued, input.timestamp, ctx)));
};
/** og isCrossJurisdictionLocalRuntimeTx. */
const crossLocalRuntimeTx = (tx: EntityTx): boolean => tx.type === "runtimeOutput" && tx.data.protocol === "cross-j";
/** og shouldStartProposal's work test (the proposer holds no frame here). */
const proposalWork = (r: OpenEntity, txs: readonly EntityTx[], frozen: boolean, ready: boolean): boolean => txs.length > 0 || frozen || (ready && (hasProposableAccount(r) || certifiedTransition(r)));
/**
 * og applyEntityInput with trustedLocalRuntimeProtocol 'cross-j': a single-signer proposer takes exactly the command's txs (plus its default
 * materializations and a certified J range first), never through the mempool, and must commit them in this input.
 */
const trustedCrossCommand = (r: OpenEntity, input: Extract<EntityInput, { kind: "txs" }>, ctx: EntityContext): Result<EntityApply<OpenEntity | ProposedEntity>, EntityError> => {
  const id = r.state.id, notFinalized = (): Result<never, EntityError> => invariant(`CROSS_J_LOCAL_COMMAND_NOT_FINALIZED:${id}:txs=${input.txs.length}`);
  if (!isSingleSigner(r.state.quorum)) return invariant(`CROSS_J_LOCAL_COMMAND_SINGLE_SIGNER_REQUIRED:${id}`);
  if (!input.txs.every(crossLocalRuntimeTx)) return invariant("ENTITY_MEMPOOL_ADMISSION_REJECTED");
  if (input.timestamp < 0n || input.timestamp > BigInt(Number.MAX_SAFE_INTEGER)) return err({ _tag: "frame_timestamp_invalid", timestamp: input.timestamp });
  return chain(crossMaterializations(r, input.txs, ctx, input.timestamp), (admitted) => chain(authorityReplica(r, [...r.mempool, ...input.txs]), (admission) => {
    if (!isProposalLeader(admission)) return invariant(`CROSS_J_LOCAL_COMMAND_PROPOSER_REQUIRED:${id}:${r.signerId}`);
    const trusted = appendMempool([], admitted);
    const work = trusted.length > 0 || r.mempool.length > 0 || hasProposableAccount(r);
    return chain(work ? ensureLocalJPrefix(r, ctx, false) : ok({ replica: r, outputs: [], signed: false }), ({ replica: queued, outputs: votes }) => chain(authorityReplica(queued, queued.mempool), (authority) =>
      chain(jPrefixSelection(queued, authority, ctx, true), ({ replica: selecting, certificate, blocked, frozen, range }) => {
        const required = range === undefined ? trusted : [range, ...trusted];
        return chain(blocked ? ok<ProposableSelection>({ txs: [], currentAuthorityReady: false }) : selectProposable(selecting.state, required), (selection) => {
          const txs = frozen ? [] : selection.txs;
          if (txs.length !== required.length) return invariant(`CROSS_J_LOCAL_COMMAND_PARTIAL_FRAME_FORBIDDEN:${id}:selected=${txs.length}:required=${required.length}`);
          if (!proposalWork(selecting, txs, frozen, selection.currentAuthorityReady)) return notFinalized();
          return chain(assertProposalPrefix(selecting, txs, certificate, ctx), () => chain(proposeSelected(selecting, authority, txs, input.timestamp, ctx, certificate ?? undefined), (a) =>
            a.replica.head.height > r.head.height ? ok({ ...a, outputs: [...votes, ...a.outputs] }) : notFinalized()));
        });
      })));
  }));
};
/** og applyEntityInput with trustedLocalRuntimeProtocol 'account-work': no admission and no forward; the leader proposes only queued Account work. */
const accountWorkProposal = (r: OpenEntity, timestamp: bigint, ctx: EntityContext): Result<EntityApply<OpenEntity | ProposedEntity>, EntityError> =>
  chain(r.mempool.length > 0 || hasProposableAccount(r) ? ensureLocalJPrefix(r, ctx, false) : ok({ replica: r, outputs: [], signed: false }), ({ replica: queued, outputs: votes }) =>
    chain(authorityReplica(queued, queued.mempool), (authority) => {
      if (!isProposalLeader(authority)) return ok(done<OpenEntity | ProposedEntity, EntityOutput>(queued, votes));
      return chain(jPrefixSelection(queued, authority, ctx), ({ replica: selecting, certificate, blocked, frozen }) =>
        chain(blocked ? ok<ProposableSelection>({ txs: [], currentAuthorityReady: false }) : selectProposable(selecting.state, []), (selection) => {
          const txs = frozen ? [] : selection.txs;
          if (!proposalWork(selecting, txs, frozen, selection.currentAuthorityReady)) return ok(done<OpenEntity | ProposedEntity, EntityOutput>(selecting, votes));
          return chain(assertProposalPrefix(selecting, txs, certificate, ctx), () =>
            map(proposeSelected(selecting, authority, txs, timestamp, ctx, certificate ?? undefined, (frames) => txs.length > 0 || frozen || frames > 0), (a) => ({ ...a, outputs: [...votes, ...a.outputs] })));
        }));
    }));
/** og runs handleHashPrecommits on every input: a held frame whose collected signatures already reach quorum installs now. */
const heldQuorum = <R extends ProposedEntity | LockedEntity>(r: R, before: readonly EntityOutput[]): Result<EntityApply<OpenEntity | R>, EntityError> =>
  quorumPower(r.draft.state.quorum, r.signatures) < thresholdOf(r.draft.state.quorum) ? ok(done<OpenEntity | R, EntityOutput>(r, before))
    : chain(hashEntityFrame(r.frame), (frameHash) => map(installFrame(r, frameHash, r.signatures, true), (c): EntityApply<OpenEntity | R> => ({ ...c, outputs: [...before, ...c.outputs] })));
/** og applyEntityInput on a replica holding a frame, by lane (a deferred input stops after admission; a trusted cross-j command cannot commit). */
const queueOnly = <R extends ProposedEntity | LockedEntity>(r: R, input: Extract<EntityInput, { kind: "txs" }>, ctx: EntityContext): Result<EntityApply<OpenEntity | R>, EntityError> => {
  if (ctx.lane === "cross-j") return !isSingleSigner(r.state.quorum) ? invariant(`CROSS_J_LOCAL_COMMAND_SINGLE_SIGNER_REQUIRED:${r.state.id}`) : invariant(`CROSS_J_LOCAL_COMMAND_NOT_FINALIZED:${r.state.id}:txs=${input.txs.length}`);
  if (ctx.lane === "account-work") return chain(heldQuorum(r, []), (a) => heldSelection(a, ctx));
  return chain(admitTxs(r, input, ctx), (queued) => chain(forwarded(queued, input.timestamp), (out) => ctx.lane === "defer" ? ok(done<OpenEntity | R, EntityOutput>(queued, out)) : chain(heldQuorum(queued, out), (a) => heldSelection(a, ctx))));
};
/** og preauthenticateEntityProposal: canonical digests, parent, leader, recomputed hash, manifest head, the proposer's frame signature. */
const DIGEST = /^0x[0-9a-f]{64}$/;
const preauthenticate = (r: EntityEnv, frame: EntityFrame, signatures: Precommits, ctx: EntityContext): Result<EntityFrameHash, EntityError> => chain(hashEntityFrame(frame), (frameHash): Result<EntityFrameHash, EntityError> => {
  if (!DIGEST.test(frame.stateRoot) || !DIGEST.test(frame.authorityRoot)) return err({ _tag: "proposal_digest" });
  if (frame.prevFrameHash !== parentOf(r.head)) return err({ _tag: "proposal_parent" });
  // og: one frame may not both activate a counterparty's board and carry that counterparty's Account row
  if (counterpartyBoardActivationConflict(r.state.id, frame.txs) !== null) return invariant("PROPOSAL_COUNTERPARTY_BOARD_ACTIVATION_MIXED");
  // og validateProposedFrameLeader: a handover frame is proposed by the new board's first validator at view 0 with no certificate, under that board;
  // otherwise the leader certificate (or the committed leader and view) and any relay certificate.
  const proposer = frame.leader.proposerSignerId;
  return chain(handoverFrameConfig(r.state, frame.txs), (handover) => {
    if (handover !== null && (handover.validators[0] === undefined || frame.leader.view !== 0 || frame.leader.certificate !== undefined || frame.leader.relayCertificate !== undefined || signerId(proposer) !== signerId(handover.validators[0]))) return err({ _tag: "proposal_leader" });
    return chain(handover === null ? ok(r.state) : withBoardAuthority(r.state, handover, Number(frame.height)), (authority): Result<EntityFrameHash, EntityError> => {
      const at = { state: authority, head: r.head };
      if (!verifyLeaderCertificate(at, frame.leader, ctx) || !verifyRelayCertificate(at, frame, frameHash, ctx) || frame.entityContext.entityId !== r.state.id) return err({ _tag: "proposal_leader" });
      const head = frame.hashesToSign[0];
      if (head === undefined || head.hash !== frameHash || head.type !== "entityFrame" || head.context !== `entity:${r.state.id.slice(-4)}:frame:${frame.height}`) return err({ _tag: "proposal_manifest" });
      const own = [...signatures].filter(([id]) => signerId(id) === signerId(proposer)), sig = own.length === 1 ? own[0]?.[1][0] : undefined, addr = memberId(authority.quorum, proposer);
      return sig === undefined || addr === undefined || !memberSigned(authority.quorum, frameHash, sig, addr, ctx) ? err({ _tag: "proposal_signature" }) : ok(frameHash);
    });
  });
});
/** og replayProposedEntityFrame: a validator folds the frame's txs itself; any difference in the manifest refuses the proposal. */
const replayFrame = (r: EntityEnv, frame: EntityFrame, frameHash: EntityFrameHash, ctx: EntityContext): Result<EntityCandidate, EntityError> => {
  if (frame.timestamp < r.state.timestamp) return err({ _tag: "frame_timestamp_regression", timestamp: frame.timestamp });
  // og assertHtlcPreparedInfraContext: validators check the committed origins against public facts, never recreating proposer entropy.
  return chain(frameInfraOf(frame), (infra) => chain(entityKeypair(r.state, ctx), () => chain(assertInboundEntries(r, frame, infra, ctx), () => chain(assertOriginated(originView(r.state, r.accountReplicas, frame.timestamp), infra, frame.txs), () =>
    chain(foldTxs(r.state, r.accountReplicas, frame.txs, { verify: ctx.verify, timestamp: frame.timestamp, htlc: infra, ...opt("activeJurisdiction", ctx.activeJurisdiction), ...opt("siblings", ctx.siblings), ...opt("boardAuthority", ctx.boardAuthority), ...opt("jReplicas", ctx.jReplicas), ...opt("runtimeSeed", ctx.runtimeSeed) }), ({ draft, evicted }) => {
      if (evicted.length > 0) return err({ _tag: "local_manifest_mismatch" });
      return chain(handoverLeaderState(r.state, frame.txs), (handoverLeader) => chain(buildFrame(r, frame.leader, handoverLeader ?? committedLeaderFor(r.state, frame), frame.timestamp, frame.txs, draft, infra, frame.jPrefixCertificate), (candidate) => chain(hashEntityFrame(candidate.frame), (local) =>
        local !== frameHash || canon(candidate.frame.hashesToSign) !== canon(frame.hashesToSign) ? err({ _tag: "local_manifest_mismatch" }) : ok({ ...candidate, frame }))));
    })))));
};
/** og assertHtlcPreparedInfraContext (inbound half): a validator decrypts the frame's inbound locks itself, taking liveness only from the peer assertions. */
const assertInboundEntries = (r: EntityEnv, frame: EntityFrame, infra: HtlcFrameInfra, ctx: EntityContext): Result<void, EntityError> => {
  if (!htlcFrameTxs(frame.txs)) return infra.entries.length === 0 ? ok(undefined) : htlcReject("HTLC_PREPARED_INBOUND_REPLAY_MISMATCH");
  const asserted = new Map(infra.peerAssertions.map((a) => { const x = a as { readonly entityId?: unknown; readonly online?: unknown } | null; return [String(x?.entityId ?? ""), x?.online === true] as const; }));
  const v: HtlcInboundView = { state: r.state, replicas: r.accountReplicas, timestamp: Number(frame.timestamp), publicKey: String(r.state.committed["entityEncryptionPublicKey"] ?? ""), privateKey: ctx.htlc?.encryptionPrivateKey, online: (id) => asserted.get(id) === true };
  return chain(inboundHtlcEntries(v, frame.txs), (expected) => (canon(expected) === canon(infra.entries) ? ok(undefined) : htlcReject("HTLC_PREPARED_INBOUND_REPLAY_MISMATCH")));
};
const heldFrame = (r: EntityReplica): (EntityEnv & EntityCandidate) | undefined => match(r, { open: () => undefined, proposed: (p): (EntityEnv & EntityCandidate) | undefined => p, locked: (l): (EntityEnv & EntityCandidate) | undefined => l });
/** og handleCommitNotification: a frame carrying a quorum certificate installs (after replay unless already locked on it). */
const commitNotification = <R extends EntityReplica>(r: R, frame: EntityFrame, bundles: Precommits, ctx: EntityContext): Result<EntityApply<OpenEntity | R>, EntityError> | undefined => {
  if (bundles.size === 0) return undefined;
  const fq = frameQuorum(r.state, frame.txs), q = fq.ok ? fq.value : r.state.quorum;
  const normalized = chain(fq, () => normalizeBundles(q, bundles));
  if (normalized.ok && quorumPower(q, normalized.value) < thresholdOf(q)) return undefined;
  if (!DIGEST.test(frame.stateRoot) || !DIGEST.test(frame.authorityRoot)) return err({ _tag: "proposal_digest" });
  if (frame.height > r.head.height + 1n) return err({ _tag: "commit_wait" });
  if (frame.height < r.head.height) return ok(done<OpenEntity | R, EntityOutput>(r));
  return chain(hashEntityFrame(frame), (frameHash): Result<EntityApply<OpenEntity | R>, EntityError> => {
    if (frame.height === r.head.height) return frameHash === r.head.prevFrameHash ? ok(done<OpenEntity | R, EntityOutput>(r)) : err({ _tag: "commit_conflict" });
    return chain(preauthenticate(r, frame, bundles, ctx), () => chain(normalized, (sigs): Result<EntityApply<OpenEntity | R>, EntityError> => {
      if ([...sigs].some(([id, s]) => !bundleValid(q, frame.hashesToSign, id, s, ctx))) return err({ _tag: "invalid_signature", address: "" });
      const held = heldFrame(r);
      if (held !== undefined) return unwrapOr(hashEntityFrame(held.frame), () => "") === frameHash ? installFrame(held, frameHash, sigs, false) : err({ _tag: "commit_conflict" });
      return chain(jPrefixFrameChecks(r, frame, ctx, "COMMIT"), () => chain(replayFrame(r, frame, frameHash, ctx), (candidate) => installFrame({ ...r, ...candidate }, frameHash, sigs, false)));
    }));
  });
};
/** og handleProposedFramePrecommit then handleHashPrecommits: replay, sign the manifest, lock, send the precommit to every other validator; commit when the lock already holds a quorum. */
const signProposal = (r: OpenEntity, frame: EntityFrame, bundles: Precommits, ctx: EntityContext): Result<EntityApply<OpenEntity | LockedEntity>, EntityError> => {
  if (frame.height < r.head.height) return ok(done<OpenEntity | LockedEntity, EntityOutput>(r));
  return chain(hashEntityFrame(frame), (frameHash): Result<EntityApply<OpenEntity | LockedEntity>, EntityError> => {
    if (frame.height === r.head.height) return frameHash === r.head.prevFrameHash ? ok(done<OpenEntity | LockedEntity, EntityOutput>(r)) : err({ _tag: "proposal_conflict" });
    if (frame.height !== r.head.height + 1n) return err({ _tag: "proposal_wait" });
    return chain(preauthenticate(r, frame, bundles, ctx), () => chain(notSuperseded(r, frame), () => chain(jPrefixFrameChecks(r, frame, ctx, "PROPOSAL"), () => chain(replayFrame(r, frame, frameHash, ctx), (candidate) => chain(signManifest(candidate.frame.hashesToSign, r.signerId, ctx, candidate.draft.state), (own) =>
      chain(normalizeBundles(candidate.draft.state.quorum, bundles), (sigs): Result<EntityApply<OpenEntity | LockedEntity>, EntityError> => {
        const q = candidate.draft.state.quorum;
        if ([...sigs].some(([id, s]) => !bundleValid(q, frame.hashesToSign, id, s, ctx))) return err({ _tag: "invalid_signature", address: "" });
        const self = signerId(r.signerId), mine = sigs.get(self);
        if (mine !== undefined && !sameSigs(mine, own)) return err({ _tag: "local_precommit_conflict" });
        const locked: LockedEntity = { ...r, _tag: "locked", ...candidate, signatures: mapSet(sigs, self, own) };
        const precommits = [...membersOf(q).keys()].filter((v) => signerId(v) !== self)
          .map((v): EntityOutput => ({ to: r.state.id, signerId: v, input: { kind: "precommit", height: frame.height, frameHash, signatures: new Map([[self, own]]) } }));
        if (quorumPower(q, locked.signatures) < thresholdOf(q)) return ok(done<OpenEntity | LockedEntity, EntityOutput>(locked, precommits));
        return map(installFrame(locked, frameHash, locked.signatures, true), (committed): EntityApply<OpenEntity | LockedEntity> => ({ ...committed, outputs: [...precommits, ...committed.outputs] }));
      }))))));
  });
};
/** og validateProposalViewAndJRange: a view this validator already voted past (or holds a certificate for) supersedes the proposal. */
const notSuperseded = (r: EntityEnv, frame: EntityFrame): Result<void, EntityError> => {
  const height = Number(frame.height), self = signerId(r.signerId);
  const effective = Math.max(frame.leader.view, frame.leader.certificate?.toView ?? -1, frame.leader.relayCertificate?.toView ?? -1);
  const voted = Math.max(-1, ...[...(r.leaderVotes?.values() ?? [])].filter((v) => signerId(v.voterId) === self && v.targetHeight === height && v.signature.length > 0).map((v) => v.toView));
  const certified = r.pendingLeaderCertificate?.targetHeight === height ? r.pendingLeaderCertificate.toView : -1;
  return guard(Math.max(voted, certified) <= effective, { _tag: "proposal_superseded" });
};
/** og respondToActiveDuplicate: the same proposal again re-sends this validator's precommit. */
const resendPrecommit = (r: LockedEntity, frame: EntityFrame): Result<EntityApply<LockedEntity>, EntityError> => chain(hashEntityFrame(r.frame), (held) => chain(hashEntityFrame(frame), (frameHash): Result<EntityApply<LockedEntity>, EntityError> => {
  if (frame.height < r.head.height || (frame.height === r.head.height && frameHash === r.head.prevFrameHash)) return ok(done(r));
  if (frameHash !== held) return frame.height > r.frame.height ? err({ _tag: "proposal_wait" }) : err({ _tag: "proposal_conflict" });
  const self = signerId(r.signerId), own = r.signatures.get(self) ?? [];
  return ok(done(r, [...membersOf(r.draft.state.quorum).keys()].filter((v) => signerId(v) !== self)
    .map((v): EntityOutput => ({ to: r.state.id, signerId: v, input: { kind: "precommit", height: frame.height, frameHash, signatures: new Map([[self, own]]) } }))));
}));
type ProposalInput = Extract<EntityInput, { kind: "proposal" }>;
const proposalOpen = (r: OpenEntity, input: ProposalInput, ctx: EntityContext): Result<EntityApply<OpenEntity | LockedEntity>, EntityError> =>
  commitNotification(r, input.frame, input.signatures, ctx) ?? signProposal(r, input.frame, input.signatures, ctx);
const proposalLocked = (r: LockedEntity, input: ProposalInput, ctx: EntityContext): Result<EntityApply<OpenEntity | LockedEntity>, EntityError> =>
  commitNotification(r, input.frame, input.signatures, ctx) ?? resendPrecommit(r, input.frame);
const proposalProposed = (r: ProposedEntity, input: ProposalInput, ctx: EntityContext): Result<EntityApply<OpenEntity | ProposedEntity>, EntityError> =>
  commitNotification(r, input.frame, input.signatures, ctx) ?? chain(hashEntityFrame(r.frame), (held) => chain(hashEntityFrame(input.frame), (frameHash): Result<EntityApply<OpenEntity | ProposedEntity>, EntityError> =>
    input.frame.height < r.head.height || (input.frame.height === r.head.height && frameHash === r.head.prevFrameHash) || frameHash === held ? heldQuorum(r, [])
      : input.frame.height > r.frame.height ? err({ _tag: "proposal_wait" }) : err({ _tag: "proposal_conflict" })));
/** og handleHashPrecommits: verify each signer's bundle against the held manifest, refuse equivocation, commit and broadcast at quorum. */
export const applyPrecommitHeld = <R extends ProposedEntity | LockedEntity>(r: R, input: Extract<EntityInput, { kind: "precommit" }>, ctx: EntityContext): Result<EntityApply<OpenEntity | R>, EntityError> =>
  chain(hashEntityFrame(r.frame), (frameHash) => {
    if (input.signatures.size > 0 && (input.height !== r.frame.height || !sameHex(input.frameHash, frameHash))) return err({ _tag: "precommit_frame_mismatch" });
    return chain(normalizeBundles(r.draft.state.quorum, input.signatures), (incoming) => chain(foldResult(incoming, r.signatures, (held, [id, sigs]): Result<Precommits, EntityError> => {
      if (!bundleValid(r.draft.state.quorum, r.frame.hashesToSign, id, sigs, ctx)) return err({ _tag: "invalid_signature", address: id });
      const existing = held.get(id);
      return existing !== undefined && !sameSigs(existing, sigs) ? err({ _tag: "precommit_signer_equivocation" }) : ok(mapSet(held, id, sigs));
    }), (signatures) => heldQuorum<R>({ ...r, signatures }, [])));
  });
/** og: no active frame; a precommit for a height already committed is a no-op so reliable ingress can terminalize it. */
const precommitOpen = (r: OpenEntity, input: Extract<EntityInput, { kind: "precommit" }>): Result<EntityApply<OpenEntity>, EntityError> =>
  input.signatures.size === 0 || r.head.height > input.height ? ok(done(r)) : err({ _tag: "precommit_not_active" });
type VoteInput = Extract<EntityInput, { kind: "leaderTimeoutVote" }>;
/** og handleLeaderTimeoutVote: the vote must be for exactly this view change from a validator; a local intent is signed here and broadcast. */
const acceptVote = (r: EntityReplica, input: VoteInput, ctx: EntityContext): Result<{ readonly vote: LeaderVote; readonly outputs: readonly EntityOutput[] }, EntityError> => {
  const incoming = input.vote, q = r.state.quorum, voter = signerId(incoming.voterId), invalid: EntityError = { _tag: "leader_vote_invalid" }, addr = memberId(q, voter);
  if (!voteMatchesState(r.state, r.head, incoming) || addr === undefined) return err(invalid);
  return chain(hashLeaderVote(incoming), (h): Result<{ readonly vote: LeaderVote; readonly outputs: readonly EntityOutput[] }, EntityError> => {
    if (input.local !== true) { const sig = sigOf(incoming.signature); return sig !== undefined && memberSigned(q, h, sig, addr, ctx) ? ok({ vote: incoming, outputs: [] }) : err(invalid); }
    if (voter !== signerId(r.signerId) || incoming.signature !== "") return err(invalid);
    return map(mapErr(ctx.sign(h as Hash, r.signerId), (): EntityError => ({ _tag: "sign_failed" })), (sig) => {
      const vote: LeaderVote = { ...incoming, signature: sig };
      return { vote, outputs: [...membersOf(q).keys()].filter((v) => signerId(v) !== voter).map((v): EntityOutput => ({ to: r.state.id, signerId: v, input: { kind: "leaderTimeoutVote", timestamp: input.timestamp, vote } })) };
    });
  });
};
/** og: votes are collected under one collection key (a newer view change resets them); a different second vote from a voter is equivocation, an identical one a no-op. */
const addVote = (r: EntityEnv, vote: LeaderVote): Result<{ readonly votes: ReadonlyMap<string, LeaderVote>; readonly fresh: boolean }, EntityError> => chain(collectionKey(vote), (key) => {
  const [first] = r.leaderVotes?.values() ?? [], voter = signerId(vote.voterId);
  const votes = first === undefined || unwrapOr(collectionKey(first), () => "") === key ? r.leaderVotes ?? new Map<string, LeaderVote>() : new Map<string, LeaderVote>();
  const prev = votes.get(voter);
  if (prev === undefined) return ok({ votes: mapSet(votes, voter, vote), fresh: true as boolean });
  const bytes = (v: LeaderVote): Result<string, EntityError> => chain(voteBinary(v), (b) => map(encodeConsensus(b), bytesToHex));
  return chain(bytes(prev), (a) => chain(bytes(vote), (b) => (a === b ? ok({ votes, fresh: false as boolean }) : err({ _tag: "leader_vote_equivocation" }))));
});
/** og installLeaderCertificate + selectPreparedFrame: at quorum, certify the view change; a prepared quorum frame becomes this replica's lock, a sub-quorum lock is dropped. */
const certify = (r: EntityReplica, vote: LeaderVote, votes: ReadonlyMap<string, LeaderVote>, ctx: EntityContext): Result<EntityReplica, EntityError> => {
  const q = r.state.quorum, base: EntityReplica = { ...r, leaderVotes: votes };
  if ([...votes.keys()].reduce((n, id) => n + sharesOf(q, id), 0n) < thresholdOf(q)) return ok(base);
  const cert = buildLeaderCertificate(vote, votes), lock = r._tag === "locked" ? r : undefined, rejected: EntityError = { _tag: "leader_prepared_rejected" };
  return chain(lock === undefined ? ok(false) : preparedQuorum(q, lock.frame, lock.signatures, ctx), (lockQuorum) => chain(selectPrepared(r, cert, ctx), (prepared): Result<EntityReplica, EntityError> => {
    if (prepared === null) {
      if (lockQuorum) return err(rejected);
      return ok(lock === undefined ? { ...base, pendingLeaderCertificate: cert } : { ...openEntity(lock.signerId, lock.state, lock.head, lock.mempool, lock.accountReplicas), leaderVotes: votes, pendingLeaderCertificate: cert, ...opt("jPrefixRound", lock.jPrefixRound), ...opt("certifiedFrameHead", lock.certifiedFrameHead) });
    }
    return chain(hashEntityFrame(prepared.frame), (hash): Result<EntityReplica, EntityError> => {
      const lockHash = lock === undefined ? undefined : unwrapOr(hashEntityFrame(lock.frame), () => "");
      if (lock !== undefined && lockQuorum && lockHash !== hash && lock.frame.leader.view >= prepared.frame.leader.view) return err(rejected);
      const pending: LeaderCertificate = { ...cert, preparedFrameHash: hash }, frame: EntityFrame = { ...prepared.frame, leader: { ...prepared.frame.leader, relayCertificate: pending } };
      if (r._tag === "proposed") return ok({ ...base, pendingLeaderCertificate: pending });
      if (lock !== undefined && lockHash === hash) return ok({ ...lock, leaderVotes: votes, pendingLeaderCertificate: pending, frame, signatures: prepared.signatures });
      return chain(preparedJPrefix(r, prepared.frame, ctx), () => map(replayFrame(r, prepared.frame, hash as EntityFrameHash, ctx), (candidate): EntityReplica =>
        ({ ...openEntity(r.signerId, r.state, r.head, r.mempool, r.accountReplicas), ...candidate, _tag: "locked", frame, signatures: prepared.signatures, leaderVotes: votes, pendingLeaderCertificate: pending, ...opt("jPrefixRound", r.jPrefixRound), ...opt("certifiedFrameHead", r.certifiedFrameHead) })));
    });
  }));
};
/** og relayPreparedFrameIfReady: the certified leader re-proposes exactly the prepared frame with its relay certificate. */
const relayPrepared = (l: LockedEntity): Result<EntityApply<LockedEntity | ProposedEntity>, EntityError> | undefined => {
  const c = l.pendingLeaderCertificate;
  if (!isProposalLeader(l) || isSingleSigner(l.state.quorum) || c === undefined || c.targetHeight !== Number(l.head.height) + 1 || c.preparedFrameHash === undefined) return undefined;
  return chain(hashEntityFrame(l.frame), (hash): Result<EntityApply<LockedEntity | ProposedEntity>, EntityError> => {
    if (hash !== c.preparedFrameHash) return err({ _tag: "leader_prepared_rejected" });
    const frame: EntityFrame = { ...l.frame, leader: { ...l.frame.leader, relayCertificate: c } }, self = signerId(l.signerId);
    return ok(done<LockedEntity | ProposedEntity, EntityOutput>({ ...l, _tag: "proposed", frame }, [...membersOf(l.state.quorum).keys()].filter((v) => signerId(v) !== self)
      .map((v): EntityOutput => ({ to: l.state.id, signerId: v, input: { kind: "proposal", frame, signatures: l.signatures } }))));
  });
};
/** og applyEntityInput with a `leaderTimeoutVote` lane: vote, maybe certify, then the ordinary proposal step (relay, a certified empty frame, or a held quorum). */
const leaderVote = (r: EntityReplica, input: VoteInput, ctx: EntityContext): Result<EntityApply<EntityReplica>, EntityError> => chain(acceptVote(r, input, ctx), ({ vote, outputs }) =>
  chain(addVote(r, vote), ({ votes, fresh }) => chain(fresh ? certify(r, vote, votes, ctx) : ok(r), (next): Result<EntityApply<EntityReplica>, EntityError> => {
    const after = match(next, {
      open: (o): Result<EntityApply<EntityReplica>, EntityError> => startProposal(o, input.timestamp, ctx),
      proposed: (p): Result<EntityApply<EntityReplica>, EntityError> => heldQuorum(p, []),
      locked: (l): Result<EntityApply<EntityReplica>, EntityError> => relayPrepared(l) ?? heldQuorum(l, []),
    });
    return map(after, (a): EntityApply<EntityReplica> => ({ ...a, outputs: [...outputs, ...a.outputs] }));
  })));
const entityVerb = grammar<EntityGrammar>(EntityTransition);
const applyTxs = entityVerb("txs", { open: applyTxsOpen, proposed: (r: ProposedEntity, i, c) => queueOnly(r, i, c), locked: (r: LockedEntity, i, c) => queueOnly(r, i, c) });
const applyProposal = entityVerb("proposal", { open: proposalOpen, proposed: proposalProposed, locked: proposalLocked });
const applyPrecommit = entityVerb("precommit", { open: precommitOpen, proposed: (r: ProposedEntity, i, c) => applyPrecommitHeld(r, i, c), locked: (r: LockedEntity, i, c) => applyPrecommitHeld(r, i, c) });
/** A proposed replica keeps its own proposal (og never replaces `proposal` on a view change), so it only ever leaves as open or proposed. */
const applyLeaderVote = entityVerb("leaderTimeoutVote", {
  open: (r: OpenEntity, i, c) => leaderVote(r, i, c), locked: (r: LockedEntity, i, c) => leaderVote(r, i, c),
  proposed: (r: ProposedEntity, i, c) => leaderVote(r, i, c) as Result<EntityApply<OpenEntity | ProposedEntity>, EntityError>,
});
const applyJPrefix = entityVerb("jPrefixAttestations", {
  open: (r: OpenEntity, i, c) => jPrefixAttestationsInput(r, i, c) as Result<EntityApply<OpenEntity | ProposedEntity>, EntityError>,
  proposed: (r: ProposedEntity, i, c) => jPrefixAttestationsInput(r, i, c) as Result<EntityApply<OpenEntity | ProposedEntity>, EntityError>,
  locked: (r: LockedEntity, i, c) => jPrefixAttestationsInput(r, i, c) as Result<EntityApply<OpenEntity | LockedEntity>, EntityError>,
});
/** One input to one validator replica (og applyEntityInput); `ctx.signerId` must name this replica. A commit runs og's post-commit J-prefix hook. */
export const applyEntityInput = (r: EntityReplica, input: EntityInput, ctx: EntityContext): Result<EntityApply, EntityError> => {
  if (ctx.self !== r.state.id) return err({ _tag: "wrong_entity" });
  if (signerId(ctx.signerId) !== signerId(r.signerId)) return err({ _tag: "wrong_replica", address: ctx.signerId });
  const applied: Result<EntityApply, EntityError> = matchBy("kind", input, { txs: (i) => applyTxs(r, i, ctx), proposal: (i) => applyProposal(r, i, ctx), precommit: (i) => applyPrecommit(r, i, ctx), leaderTimeoutVote: (i) => applyLeaderVote(r, i, ctx), jPrefixAttestations: (i) => applyJPrefix(r, i, ctx) });
  return chain(applied, (a) => (a.replica.head.height > r.head.height ? afterCommit(a, ctx) : ok(a)));
};

/** og runtime/types.ts JInput: one deterministic child-machine input for a J replica; the rewrite carries the J txs uninterpreted (J area). */
export type JInput = { readonly jurisdictionName: string; readonly jTxs: readonly Binary[] };
/** og RoutedEntityInput.sourceRuntimeFrame: the sender Runtime frame whose outbox carried the input (set by transport; absent for local loopback). */
export type SourceRuntimeFrame = { readonly height: number; readonly timestamp: number };
/** og atomicCrossJurisdictionPair: the cohort marker of one leg of an atomic cross-j Account pair. */
export type AtomicCrossPair = { readonly phase: "proposal" | "ack"; readonly pairKey: string };
/**
 * og RoutedEntityInput: an EntityInput addressed to one validator replica; `from` is the source Runtime (absent for local work), `runtimeId` the
 * addressed Runtime, `sourceRuntimeFrame` the sender frame, `atomicCrossJurisdictionPair` the atomic cross-j cohort marker.
 */
export type RoutedEntityInput = {
  readonly entityId: EntityId; readonly signerId: string; readonly input: EntityInput; readonly from?: string | undefined;
  readonly runtimeId?: string | undefined; readonly sourceRuntimeFrame?: SourceRuntimeFrame | undefined; readonly atomicCrossJurisdictionPair?: AtomicCrossPair | undefined;
};
/** og ConsensusConfig as carried by importReplica. */
export type ImportConfig = EntityRootConfig & { readonly jurisdiction?: ImportJurisdiction | undefined };
/** og JurisdictionConfig as supplied to importReplica: `address` is og's J endpoint label (an RPC URL or `jreplica://name`). */
export type ImportJurisdiction = Partial<EntityRootJurisdiction> & { readonly name?: string | undefined; readonly address?: string | undefined };
/** og jurisdiction-runtime.ts JTokenInfo: one JReplica.tokenRegistry row. */
export type JTokenInfo = { readonly symbol: string; readonly name: string; readonly address: string; readonly decimals: number; readonly tokenId: number; readonly tokenType: number; readonly externalTokenId: bigint };
export type JContracts = { readonly depository?: string | undefined; readonly entityProvider?: string | undefined; readonly account?: string | undefined; readonly deltaTransformer?: string | undefined };
export type FullJContracts = { readonly depository: string; readonly entityProvider: string; readonly account: string; readonly deltaTransformer: string };
/**
 * og JReplica (types/jurisdiction-runtime.ts). `stateRoot` is the 32-byte BrowserVM root as 0x hex (og holds the bytes; the post-state view
 * packs them as bytes), null for RPC stacks. `mempool` carries og JTx values uninterpreted.
 */
export type JReplica = {
  readonly name: string; readonly blockNumber: bigint; readonly stateRoot: string | null; readonly mempool: readonly Binary[]; readonly blockDelayMs: number;
  readonly blockTimeMs?: number | undefined; readonly lastBlockTimestamp: number; readonly blockReady?: boolean | undefined;
  readonly rpcs?: readonly string[] | undefined; readonly chainId?: number | undefined; readonly watcherConfirmationDepth?: number | undefined;
  readonly watcherReceiptCommitment?: "tron-rpc-attested" | undefined; readonly tokenRegistry?: readonly JTokenInfo[] | undefined;
  readonly position: { readonly x: number; readonly y: number; readonly z: number }; readonly entityProviderDeploymentBlock?: number | undefined; readonly contracts?: JContracts | undefined;
};
export type RpcPolicy = "single" | "failover" | { readonly mode: "quorum"; readonly min: number };
/** og runtime/types.ts JurisdictionImportRequest (the importJ payload). */
export type JurisdictionImportRequest = {
  readonly name: string; readonly chainId: number; readonly ticker: string; readonly rpcs: readonly string[]; readonly entityProviderDeploymentBlock?: number | undefined;
  readonly blockTimeMs?: number | undefined; readonly startAtCurrentBlock?: boolean | undefined; readonly rpcPolicy?: RpcPolicy | undefined; readonly contracts?: JContracts | undefined;
  readonly tokens?: readonly { readonly symbol: string; readonly decimals: number; readonly initialSupply?: bigint | undefined }[] | undefined;
};
/** og runtime/types.ts JurisdictionImportResult (the completeImportJ payload). */
export type JurisdictionImportResult = {
  readonly importId: string; readonly requestHash: string; readonly name: string; readonly chainId: number; readonly ticker: string; readonly rpcs: readonly string[];
  readonly blockTimeMs?: number | undefined; readonly blockNumber: string; readonly stateRoot: string | null; readonly watcherConfirmationDepth: number;
  readonly watcherReceiptCommitment?: "tron-rpc-attested" | undefined; readonly tokenRegistry: readonly JTokenInfo[]; readonly entityProviderDeploymentBlock: number;
  readonly contracts: FullJContracts; readonly browserVMState?: { readonly [field: string]: Binary } | undefined;
};
export type PendingJurisdictionImport = { readonly importId: string; readonly requestHash: string; readonly request: JurisdictionImportRequest };
type SubmitResultTail = { readonly attemptId: string; readonly attemptNumber: number; readonly attemptedAt: number; readonly message?: string | undefined; readonly adapterFailure?: JAdapterFailure | undefined; readonly txHash?: string | undefined };
/** og recordJSubmitResult / recordEntityProviderActionSubmitResult / recordGovernanceJSubmitResult payloads. */
export type JSubmitResultData = SubmitResultTail & { readonly entityId: string; readonly signerId: string; readonly jurisdictionName: string; readonly batchHash: string; readonly entityNonce: number; readonly batchGeneration: number; readonly outcome: SubmitOutcome };
export type EpActionResultData = SubmitResultTail & { readonly entityId: string; readonly signerId: string; readonly jurisdictionName: string; readonly actionHash: string; readonly actionNonce: bigint; readonly generation: number; readonly outcome: Exclude<SubmitOutcome, "eventBarrier"> };
export type GovernanceResultData = SubmitResultTail & { readonly jurisdictionName: string; readonly entityId: string; readonly signerId: string; readonly proposalHash: string; readonly payloadHash: string; readonly outcome: SubmitOutcome };
/** og JAdapterFailure / RuntimeFailureSignal. */
export type JAdapterFailure = { readonly category: "transient" | "terminal"; readonly code: string; readonly message: string };
export type RuntimeFailureSignal = { readonly category: "ExpectedEmpty" | "TransientRace" | "Contradiction"; readonly code: string; readonly message: string; readonly retryable: boolean; readonly fatal: boolean };
export type SubmitFailure = { readonly message: string; readonly failedAt: number; readonly failure?: RuntimeFailureSignal | undefined; readonly adapterFailure?: JAdapterFailure | undefined };
export type SubmitOutcome = "submitted" | "eventBarrier" | "transientFailure" | "terminalFailure" | "reconciled";
type SubmitJournal = {
  readonly submitAttempts: number; readonly lastSubmittedAt: number; readonly txHash?: string | undefined; readonly lastFailure?: SubmitFailure | undefined; readonly terminalFailure?: SubmitFailure | undefined;
  readonly lastResultAttemptId?: string | undefined; readonly lastResultAt?: number | undefined; readonly lastResultOutcome?: SubmitOutcome | undefined; readonly lastResultFingerprint?: string | undefined;
  readonly resultFingerprints?: { readonly [attemptId: string]: string } | undefined; readonly resultFingerprintOrder?: readonly string[] | undefined;
};
/** og EntityReplica.jSubmitState: this validator's attempts at submitting the sealed batch. */
export type JSubmitState = SubmitJournal & { readonly jurisdictionName: string; readonly batchHash: string; readonly entityNonce: number; readonly batchGeneration: number };
/** og EntityProviderActionSubmitState. */
export type EntityProviderActionSubmitState = SubmitJournal & { readonly jurisdictionName: string; readonly actionHash: string; readonly actionNonce: bigint; readonly generation: number };
/** og EntityReplica validator-local fields the Runtime owns (never in the Entity root): J submit ledgers, quorum Hanko witnesses, J history. */
export type ReplicaPosition = { readonly x: number; readonly y: number; readonly z: number; readonly jurisdiction?: string | undefined };
/** `lastConsensusProgressAt`: og EntityReplica.lastConsensusProgressAt (Runtime clock of this validator's last consensus progress; RAM and snapshot only, never in the replica-meta commitment). */
export type ReplicaLocal = {
  readonly position?: ReplicaPosition | undefined; readonly lastConsensusProgressAt?: number | undefined;
  readonly jSubmitState?: JSubmitState | undefined; readonly entityProviderActionSubmitState?: EntityProviderActionSubmitState | undefined;
  readonly hankoWitness?: ReadonlyMap<string, HankoWitness> | undefined; readonly jHistory?: ValidatorJHistory | undefined;
};
type RuntimeData = { readonly [field: string]: Binary };
/** og runtime/types.ts RuntimeTx: every kind with og's field names. */
export type RuntimeTx =
  | { readonly type: "checkpointBarrier"; readonly data: Record<string, never> }
  | { readonly type: "recordRuntimeAdapterCommand"; readonly data: { readonly laneId: string; readonly sequence: number; readonly commandId: string; readonly inputHash: string; readonly expiresAtMs: number | null } }
  | { readonly type: "recordNumberedRegistrationIntent"; readonly data: RuntimeData }
  | { readonly type: "resolveNumberedRegistrationIntent"; readonly data: RuntimeData }
  | { readonly type: "recordAuthenticatedJAuthority"; readonly data: RuntimeData }
  | { readonly type: "importReplica"; readonly entityId: string; readonly signerId: string; readonly data: { readonly config: ImportConfig; readonly isProposer: boolean; readonly entitySeed: string; readonly profileName?: string | undefined; readonly position?: { readonly x: number; readonly y: number; readonly z: number; readonly jurisdiction?: string | undefined } | undefined } }
  | { readonly type: "observeJRange"; readonly data: { readonly entityId: string; readonly signerId: string; readonly jurisdictionRef: string; readonly scannedThroughHeight: number; readonly tipBlockHash: string; readonly headers?: readonly Binary[] | undefined; readonly blocks: readonly Binary[] } }
  | { readonly type: "advanceJWatcherCursor"; readonly data: { readonly depositoryAddress: string; readonly chainId: number; readonly blockNumber: number } }
  | { readonly type: "rewindJHistory"; readonly data: { readonly entityId: string; readonly signerId: string; readonly jurisdictionRef: string; readonly conflictingHeight: number; readonly conflictingBlockHash: string } }
  | { readonly type: "retryJSubmit"; readonly data: { readonly entityId: string; readonly signerId: string; readonly jurisdictionName: string; readonly batchHash: string; readonly entityNonce: number; readonly batchGeneration: number; readonly feeOverrides?: Binary | undefined } }
  | { readonly type: "recordJSubmitResult"; readonly data: JSubmitResultData }
  | { readonly type: "retryEntityProviderAction"; readonly data: { readonly entityId: string; readonly signerId: string; readonly jurisdictionName: string; readonly actionHash: string; readonly actionNonce: bigint; readonly generation: number } }
  | { readonly type: "recordEntityProviderActionSubmitResult"; readonly data: EpActionResultData }
  | { readonly type: "recordGovernanceJSubmitResult"; readonly data: GovernanceResultData }
  | { readonly type: "importJ"; readonly data: JurisdictionImportRequest }
  | { readonly type: "completeImportJ"; readonly data: JurisdictionImportResult };
export type RuntimeTxType = RuntimeTx["type"];
/** og RuntimeInput: runtime txs first, then entity inputs, then J inputs (queued to the J mempool). `timestamp` is the ingress seed. */
export type RuntimeInput = { readonly runtimeTxs: readonly RuntimeTx[]; readonly entityInputs: readonly RoutedEntityInput[]; readonly jInputs?: readonly JInput[] | undefined; readonly timestamp?: bigint | undefined };
/** og infrastructure.runtimeAdapterCommandFrontiers row. */
export type AdapterFrontier = { readonly lastContiguousSequence: number; readonly lastInputHash: string; readonly lastCommandId: string; readonly observedHeight: number; readonly expiresAtMs: number | null };
/**
 * og RuntimeReplica: `entities` is `eReplicas` (one replica per `entityId:signerId`, signer lowercased); `height`/`timestamp` are RuntimeState;
 * `jReplicas` is og state.jReplicas (insertion-ordered); `activeJurisdiction` / `runtimeId` / `browserVMState` are og RuntimeReplica fields;
 * `adapterFrontiers`, `encryptionSeeds` and `pendingJImports` are og infrastructure maps; `frameHash` is the WAL head.
 */
export type Runtime = {
  readonly entities: ReadonlyMap<string, EntityReplica>; readonly height: bigint; readonly timestamp: bigint; readonly jReplicas: ReadonlyMap<string, JReplica>;
  readonly activeJurisdiction?: string | undefined; readonly runtimeId?: string | undefined; readonly browserVMState?: { readonly [field: string]: Binary } | undefined;
  readonly pendingJImports: ReadonlyMap<string, PendingJurisdictionImport>;
  /** og infrastructure.pendingCommittedJOutbox: durable J submit attempts awaiting their recorded result. */
  readonly pendingCommittedJOutbox: readonly JInput[];
  /** og per-EntityReplica local fields, keyed like `entities`. */
  readonly replicaLocal: ReadonlyMap<string, ReplicaLocal>;
  readonly adapterFrontiers: ReadonlyMap<string, AdapterFrontier>; readonly encryptionSeeds: ReadonlyMap<string, string>; readonly frameHash: string;
  /** og infrastructure.certifiedRegistrationEvidence: receipt-proven EntityProvider registrations, keyed `stackKey:entityId`. */
  readonly registrationEvidence: ReadonlyMap<string, RegistrationEvidence>;
  /** og infrastructure.numberedRegistrationIntents: durable numbered-registration intents (pending / completed / quarantined), keyed by intentId. */
  readonly numberedRegistrationIntents: ReadonlyMap<string, RuntimeData>;
  /** og env.pendingNetworkOutputs: the retained network outbox (remote outputs still owed), committed as each frame's runtime outputs. */
  readonly pendingNetworkOutputs?: readonly NetworkOutput[] | undefined;
};
/** A whole-frame refusal carries og's error code (og throws out of the Runtime reducer, so nothing of the frame applies). */
export type RuntimeError = EntityError | Tagged<"no_such_entity", { id: EntityId }> | Tagged<"runtime_frame" | "runtime_tx", { code: string }>;
export type Verifiers = { readonly verify: Verify; readonly verifyMember: MemberVerify; readonly sign: MemberSign };
/** og capability markers: `local` holds the exact RuntimeTx (and proposeAccountsNow EntityTx) objects this process authorized (og's Symbol tags); replay trusts the WAL. */
export type RuntimeCtx = Verifiers & { readonly replay?: boolean | undefined; readonly local?: ReadonlySet<RuntimeTx | EntityTx> | undefined;
  /** og EntityRuntimeContext gossip + liveness + proposer entropy, per Entity (the HTLC proposer infrastructure). */
  readonly htlcInfra?: ((entityId: EntityId) => HtlcProposerInfra | undefined) | undefined;
  /** og env.runtimeSeed: the process's private seed; a source hub's default proposer derives cross-j hash-ladder seeds from it. */
  readonly runtimeSeed?: string | undefined;
  /** og env.infrastructure transport view (verified gossip profile routes), read when binding an outbox row's signer; never committed. */
  readonly routes?: RuntimeRoutes | undefined };
/**
 * og transport view (never committed): `verifiedProfileSigner` is an Entity's verified gossip profile runtime signer (og verifiedProfileRoutes /
 * resolveGossipBoardSignerIds); `verifiedRuntime` og p2p getVerifiedRuntimeRoute; `resolvedRuntime` og resolveRuntimeIdForEntity; `crossJRuntime`
 * og resolveRuntimeIdForCrossJurisdictionEntity; `replayRuntime` og resolveReplayOutputRuntimeRoute (a replayed frame's committed rows).
 */
export type RuntimeRoutes = {
  readonly verifiedProfileSigner: (entityId: string) => string | undefined;
  readonly verifiedRuntime?: ((entityId: string) => string | undefined) | undefined;
  readonly resolvedRuntime?: ((entityId: string) => string | undefined) | undefined;
  readonly crossJRuntime?: ((entityId: string, signerId: string) => string | undefined) | undefined;
  readonly replayRuntime?: ((entityId: string, signerId: string) => string | undefined) | undefined;
  /** og replayOutputSignerHint: the signer a replayed frame's own Account rows committed for an Entity. */
  readonly replaySigner?: ((entityId: string) => string | undefined) | undefined;
};
export const ZERO_FRAME_HASH = `0x${"00".repeat(32)}`;
export const replicaKey = (entity: EntityId, signer: string): string => `${entity}:${signerId(signer)}`;
/** og buildJurisdictionImportAdapterConfig's bare replica: a name alone is an unconfigured J replica at block 0. */
export const bareJReplica = (name: string): JReplica => ({ name, blockNumber: 0n, stateRoot: null, mempool: [], blockDelayMs: 300, lastBlockTimestamp: 0, position: { x: 0, y: 50, z: 0 } });
export const createRuntime = (jurisdictions: Iterable<string | JReplica> = [], runtimeId?: string): Runtime => ({
  entities: new Map(), height: 0n, timestamp: 0n, jReplicas: new Map([...jurisdictions].map((j): [string, JReplica] => (typeof j === "string" ? [j, bareJReplica(j)] : [j.name, j]))),
  ...opt("runtimeId", runtimeId), pendingJImports: new Map(), pendingCommittedJOutbox: [], replicaLocal: new Map(), adapterFrontiers: new Map(), encryptionSeeds: new Map(), frameHash: ZERO_FRAME_HASH,
  registrationEvidence: new Map(), numberedRegistrationIntents: new Map(),
});
export const spawn = (rt: Runtime, r: EntityReplica): Runtime => ({ ...rt, entities: mapSet(rt.entities, replicaKey(r.state.id, r.signerId), r) });
/** og resolveEntityProposerId: an Account message goes to the receiver's active leader (the CEO `validators[0]` until a view change); a consensus input to the named validator. */
export const convertOutput = (rt: Runtime, item: EntityOutput, from: EntityId, timestamp: bigint): Result<RoutedEntityInput, RuntimeError> => {
  const marker = opt("atomicCrossJurisdictionPair", item.atomicCrossJurisdictionPair);
  if ("input" in item) return ok({ entityId: item.to, signerId: item.signerId, input: item.input, ...marker });
  const receiver = [...rt.entities.values()].find((r) => r.state.id === item.to);
  if (receiver === undefined) return err({ _tag: "no_such_entity", id: item.to });
  const leader = memberId(receiver.state.quorum, leaderStateOf(receiver.state).activeValidatorId) ?? allowedProposer(receiver.state.quorum);
  return ok({ entityId: item.to, from, signerId: leader, input: { kind: "txs", timestamp, txs: [item.tx] }, ...marker });
};

// ---- og runtime/frame/intake: shape limits, capabilities, merge ----
const MAX_RUNTIME_INPUT_RUNTIME_TXS = 10_000, MAX_RUNTIME_INPUT_ENTITY_INPUTS = 10_000, MAX_RUNTIME_J_INPUTS = 256, MAX_RUNTIME_J_TXS = 1_024, MAX_RUNTIME_J_TXS_PER_JURISDICTION = 512;
const frameErr = (code: string): Result<never, RuntimeError> => err({ _tag: "runtime_frame", code });
/** og validateRuntimeInputShapeAndLimits + collectJOutbox: a checkpoint barrier stands alone; bounded counts; every J input names a known J replica. */
export const validateRuntimeInput = (rt: Runtime, input: RuntimeInput): Result<readonly JInput[], RuntimeError> => {
  const barriers = input.runtimeTxs.filter((tx) => tx.type === "checkpointBarrier").length, jInputs = input.jInputs ?? [];
  if (barriers > 0 && (barriers !== 1 || input.runtimeTxs.length !== 1 || input.entityInputs.length !== 0 || jInputs.length !== 0)) return frameErr("CHECKPOINT_BARRIER_NOT_ALONE");
  if (input.jInputs !== undefined) {
    if (jInputs.length > MAX_RUNTIME_J_INPUTS) return frameErr("RUNTIME_J_INPUTS_MAX");
    let total = 0;
    const perJ = new Map<string, number>();
    for (const j of jInputs) {
      if (!rt.jReplicas.has(j.jurisdictionName)) return frameErr("RUNTIME_J_UNKNOWN_JURISDICTION");
      total += j.jTxs.length;
      if (total > MAX_RUNTIME_J_TXS) return frameErr("RUNTIME_J_TXS_MAX");
      const n = (perJ.get(j.jurisdictionName) ?? 0) + j.jTxs.length;
      if (n > MAX_RUNTIME_J_TXS_PER_JURISDICTION) return frameErr("RUNTIME_J_TXS_PER_JURISDICTION_MAX");
      perJ.set(j.jurisdictionName, n);
    }
  }
  if (input.runtimeTxs.length > MAX_RUNTIME_INPUT_RUNTIME_TXS) return frameErr("RUNTIME_TXS_MAX");
  return input.entityInputs.length > MAX_RUNTIME_INPUT_ENTITY_INPUTS ? frameErr("RUNTIME_ENTITY_INPUTS_MAX") : ok(jInputs);
};
/** og internal-tx-auth.ts: every RuntimeTx except importReplica/importJ needs a local capability, or replay. */
const CAPABILITY_CODES: { readonly [T in RuntimeTxType]: string | null } = {
  checkpointBarrier: "CHECKPOINT_BARRIER_EXTERNAL_RUNTIME_TX_REJECTED", recordRuntimeAdapterCommand: "RADAPTER_COMMAND_RUNTIME_TX_UNAUTHORIZED",
  recordNumberedRegistrationIntent: "NUMBERED_REGISTRATION_EXTERNAL_RUNTIME_TX_REJECTED", resolveNumberedRegistrationIntent: "NUMBERED_REGISTRATION_EXTERNAL_RUNTIME_TX_REJECTED",
  recordAuthenticatedJAuthority: "J_AUTHORITY_RUNTIME_TX_EXTERNAL_INGRESS_REJECTED", observeJRange: "J_AUTHORITY_RUNTIME_TX_EXTERNAL_INGRESS_REJECTED",
  advanceJWatcherCursor: "J_AUTHORITY_RUNTIME_TX_EXTERNAL_INGRESS_REJECTED", rewindJHistory: "J_AUTHORITY_RUNTIME_TX_EXTERNAL_INGRESS_REJECTED",
  retryJSubmit: "J_SUBMIT_RUNTIME_TX_EXTERNAL_INGRESS_REJECTED", recordJSubmitResult: "J_SUBMIT_RUNTIME_TX_EXTERNAL_INGRESS_REJECTED",
  retryEntityProviderAction: "ENTITY_PROVIDER_ACTION_RUNTIME_TX_EXTERNAL_INGRESS_REJECTED", recordEntityProviderActionSubmitResult: "ENTITY_PROVIDER_ACTION_RUNTIME_TX_EXTERNAL_INGRESS_REJECTED",
  recordGovernanceJSubmitResult: "GOVERNANCE_SUBMIT_RESULT_EXTERNAL_INGRESS_REJECTED", completeImportJ: "J_IMPORT_RESULT_EXTERNAL_INGRESS_REJECTED",
  importReplica: null, importJ: null,
};
export const runtimeTxAuthorized = (tx: RuntimeTx, ctx: Pick<RuntimeCtx, "replay" | "local">): Result<void, RuntimeError> => {
  const code = CAPABILITY_CODES[tx.type];
  return code === null || ctx.replay === true || ctx.local?.has(tx) === true ? ok(undefined) : err({ _tag: "runtime_tx", code });
};
const inputFingerprint = (tx: EntityTx): string => encodeEntityTx(tx);
const frameIdOf = (frame: EntityFrame): string => { const h = hashEntityFrame(frame); return h.ok ? h.value : canon(frame); };
type Lane = { readonly entityId: EntityId; readonly signerId: string; readonly from?: string | undefined; readonly runtimeId?: string | undefined; readonly sourceRuntimeFrame?: SourceRuntimeFrame | undefined; readonly atomic?: AtomicCrossPair | undefined; readonly timestamp: bigint; readonly txs?: readonly EntityTx[] | undefined; readonly proposal?: Extract<EntityInput, { kind: "proposal" }> | undefined; readonly precommit?: Extract<EntityInput, { kind: "precommit" }> | undefined; readonly vote?: VoteInput | undefined; readonly jPrefix?: JPrefixInput | undefined };
const laneOf = (i: RoutedEntityInput): Lane => {
  const at = { entityId: i.entityId, signerId: i.signerId, from: i.from, runtimeId: i.runtimeId, sourceRuntimeFrame: i.sourceRuntimeFrame, atomic: i.atomicCrossJurisdictionPair };
  return matchBy("kind", i.input, {
    txs: (x): Lane => ({ ...at, timestamp: x.timestamp, txs: x.txs }),
    proposal: (x): Lane => ({ ...at, timestamp: x.frame.timestamp, proposal: x }),
    precommit: (x): Lane => ({ ...at, timestamp: 0n, precommit: x }),
    leaderTimeoutVote: (x): Lane => ({ ...at, timestamp: x.timestamp, vote: x }),
    jPrefixAttestations: (x): Lane => ({ ...at, timestamp: x.timestamp ?? 0n, jPrefix: x }),
  });
};
/** The routed input a lane stands for, provenance included (og keeps the first input's envelope fields). */
const laneProvenance = (l: Lane): Partial<RoutedEntityInput> => ({ ...opt("from", l.from), ...opt("runtimeId", l.runtimeId), ...opt("sourceRuntimeFrame", l.sourceRuntimeFrame), ...opt("atomicCrossJurisdictionPair", l.atomic) });
/** og getEffectiveEntityInputTxs: a runtimeOutput counts as its nested Entity txs. */
const effectiveTxs = (txs: readonly EntityTx[]): readonly EntityTx[] => txs.flatMap((tx) => (tx.type === "runtimeOutput" ? tx.data.entityTxs : [tx]));
/** og hasCrossJurisdictionSourcePullProposal: an Account proposal whose frame locks a source-leg cross pull (it consumes a target ACK of the same Runtime frame). */
const sourcePullConsumer = (l: Lane): boolean => effectiveTxs(l.txs ?? []).some((tx) => tx.type === "accountInput" && tx.data.kind === "ack_frame"
  && tx.data.frame.txs.some((a) => a.type === "cross_pull_lock" && a.crossJurisdiction.leg === "source"));
const runtimeOutputOf = (l: Lane): EntityTx | undefined => l.txs?.find((tx) => tx.type === "runtimeOutput");
/**
 * og entityInputMergeKey: one authenticated runtimeOutput is its own envelope (keyed by its sender, Runtime, source frame and exact tx); an atomic
 * cross-j leg is keyed by its cohort and source frame; one J-prefix head per input; each timeout vote is its own lane; precommits by frame; tx
 * envelopes by origin, a source-pull consumer apart.
 */
const mergeKey = (l: Lane): Result<string, RuntimeError> => {
  const base = `${lower(l.entityId)}:${lower(l.signerId)}`;
  const output = runtimeOutputOf(l);
  if (output !== undefined) return ok(`${base}:runtime-output:${String(l.from || "").toLowerCase()}:${String(l.runtimeId || "").toLowerCase()}:${stableJson(l.sourceRuntimeFrame)}:${inputFingerprint(output)}`);
  if (l.atomic !== undefined) {
    const frame = l.sourceRuntimeFrame;
    if (frame === undefined) return frameErr("ENTITY_INPUT_ATOMIC_CROSS_J_SOURCE_FRAME_MISSING");
    return ok(`${base}:cross-j-atomic:${l.atomic.phase}:${l.atomic.pairKey}:${frame.height}:${frame.timestamp}`);
  }
  if (l.jPrefix !== undefined) {
    // og: one signed head per J-prefix input, keyed by its signer, target height and unsigned body hash
    const [entry, ...rest] = l.jPrefix.attestations;
    if (entry === undefined) return frameErr("ENTITY_INPUT_J_PREFIX_MISSING");
    if (rest.length > 0) return frameErr("ENTITY_INPUT_J_PREFIX_MUST_BE_SPLIT");
    const [raw, a] = entry, { signature: _signature, ...unsigned } = a;
    return ok(`${base}:j-prefix:${raw.toLowerCase()}:${a.targetEntityHeight}:${unwrapOr(jPrefixAttestationHash(unsigned), () => canon(unsigned))}`);
  }
  return ok(laneKey(l, base));
};
const laneKey = (l: Lane, base: string): string => {
  if (l.vote !== undefined) return `${base}:leader:${l.vote.vote.targetHeight}:${lower(l.vote.vote.voterId)}:${unwrapOr(hashLeaderVote(l.vote.vote), () => canon(l.vote?.vote))}`;
  if (l.precommit !== undefined) return `${base}:precommit:${l.precommit.height}:${lower(l.precommit.frameHash)}`;
  return l.txs !== undefined && l.txs.length > 0 ? `${base}:tx-origin:${lower(l.from)}${sourcePullConsumer(l) ? ":cross-j-source-consumer" : ""}` : base;
};
/** og mergeJEventTxs: one signed J observation (proposer, range, roots and signature) once per lane, first kept. */
const mergeJEvents = (txs: readonly EntityTx[]): readonly EntityTx[] => firstBy(txs, (tx) => {
  if (tx.type !== "j_event") return undefined;
  const d = tx.data, t = (v: unknown): string => String(v || "").toLowerCase();
  return canon({ from: t(d["from"]), jurisdictionRef: t(d["jurisdictionRef"]), baseHeight: d["baseHeight"] ?? null, scannedThroughHeight: d["scannedThroughHeight"] ?? null, tipBlockHash: t(d["tipBlockHash"]), eventHistoryRoot: t(d["eventHistoryRoot"]), rangeHash: t(d["rangeHash"]), signature: t(d["signature"]) });
});
/** og consensusInputOrder: a verified commit (0), then a timeout vote (1), then an unverified proposal (2), per Entity, signer and target height. */
const consensusOrder = (i: RoutedEntityInput, verified: (i: RoutedEntityInput) => boolean): { readonly height: bigint; readonly priority: number } | null =>
  i.input.kind === "proposal" ? { height: i.input.frame.height, priority: verified(i) ? 0 : 2 } : i.input.kind === "leaderTimeoutVote" ? { height: BigInt(i.input.vote.targetHeight), priority: 1 } : null;
/** og prioritizeEntityConsensusInputs: reorder only the slots of one consensus race; unrelated inputs keep their exact positions. */
export const prioritizeConsensusInputs = (inputs: readonly RoutedEntityInput[], verified: (i: RoutedEntityInput) => boolean = () => false): readonly RoutedEntityInput[] => {
  const result = [...inputs], races = new Map<string, number[]>();
  result.forEach((i, index) => { const o = consensusOrder(i, verified); if (o === null) return; const key = `${lower(i.entityId.trim())}:${lower(i.signerId.trim())}:${o.height}`; races.set(key, [...(races.get(key) ?? []), index]); });
  for (const positions of races.values()) {
    if (positions.length < 2) continue;
    const ordered = positions.map((position, stable) => ({ input: result[position] as RoutedEntityInput, stable, priority: consensusOrder(result[position] as RoutedEntityInput, verified)?.priority ?? 3 }))
      .sort((a, b) => a.priority - b.priority || a.stable - b.stable).map((e) => e.input);
    positions.forEach((position, k) => { result[position] = ordered[k] as RoutedEntityInput; });
  }
  return result;
};
/**
 * og hasVerifiedEntityCommitPrecertificate: a proposal input is a verified commit only when this replica already holds the same frame (body and
 * leader) and manifest by replay, and the carried signatures verify over every hash with the committed board's quorum.
 */
export const verifiedCommit = (entities: ReadonlyMap<string, EntityReplica>, ctx: Pick<EntityContext, "verify" | "verifyMember">) => (i: RoutedEntityInput): boolean => {
  if (i.input.kind !== "proposal") return false;
  const r = entities.get(replicaKey(i.entityId, i.signerId)), held = r === undefined ? undefined : heldFrame(r), frame = i.input.frame;
  if (r === undefined || held === undefined) return false;
  const local = hashEntityFrame(held.frame), incoming = hashEntityFrame(frame), hashes = held.frame.hashesToSign;
  if (!local.ok || !incoming.ok || local.value !== incoming.value || canon(held.frame.leader) !== canon(frame.leader) || hashes.length === 0 || canon(hashes) !== canon(frame.hashesToSign)) return false;
  const q = r.state.quorum, seen = new Set<string>();
  let power = 0n;
  for (const [raw, sigs] of i.input.signatures) {
    const id = signerId(raw), shares = sharesOf(q, id);
    if (shares === 0n || seen.has(id) || sigs.length !== hashes.length || !bundleValid(q, hashes, id, sigs, ctx as EntityContext)) return false;
    seen.add(id);
    power += shares;
  }
  return power >= thresholdOf(q);
};
/** og mergePrecommitBundles: signer ids trimmed/lowercased; a second different bundle from one signer is equivocation. */
const mergeBundles = (existing: Precommits, incoming: Precommits): Result<Precommits, RuntimeError> => {
  const normalize = (m: Precommits, source: string): Result<Map<string, readonly Signature[]>, RuntimeError> => {
    const out = new Map<string, readonly Signature[]>();
    for (const [raw, sigs] of m) { const id = signerId(raw); if (out.has(id)) return frameErr(`ENTITY_INPUT_PRECOMMIT_DUPLICATE_SIGNER:${source}`); out.set(id, sigs); }
    return ok(out);
  };
  return chain(normalize(existing, "existing"), (merged) => chain(normalize(incoming, "incoming"), (next) => {
    for (const [id, sigs] of next) {
      const previous = merged.get(id);
      if (previous === undefined) merged.set(id, sigs);
      else if (!sameSigs(previous, sigs)) return frameErr("ENTITY_INPUT_PRECOMMIT_EQUIVOCATION");
    }
    return ok(merged);
  }));
};
/** og isExactTransactionReplay: the same cohort marker, origin, Runtime and source frame re-delivering the exact same tx list is one input. */
const exactReplay = (a: Lane, b: Lane): boolean => {
  if ((a.atomic === undefined) !== (b.atomic === undefined)) return false;
  if (a.atomic !== undefined && b.atomic !== undefined && (a.atomic.phase !== b.atomic.phase || a.atomic.pairKey !== b.atomic.pairKey)) return false;
  return lower(a.from) === lower(b.from) && lower(a.runtimeId) === lower(b.runtimeId) && stableJson(a.sourceRuntimeFrame) === stableJson(b.sourceRuntimeFrame)
    && canon((a.txs ?? []).map(inputFingerprint)) === canon((b.txs ?? []).map(inputFingerprint));
};
/** og mergeExactAccountInputReplays: an exact duplicate accountInput inside one lane is dropped. */
const dedupAccountInputs = (txs: readonly EntityTx[]): readonly EntityTx[] => firstBy(txs, (tx) => (tx.type === "accountInput" ? inputFingerprint(tx) : undefined));
/**
 * og mergeEntityInputs: inputs for one replica lane collapse into one, in first-arrival order; a second different proposal for the lane
 * is kept as a conflict after every merged input; a precommit equivocation refuses the whole Runtime frame.
 */
export const mergeEntityInputs = (inputs: readonly RoutedEntityInput[], verified: (i: RoutedEntityInput) => boolean = () => false): Result<readonly RoutedEntityInput[], RuntimeError> => {
  const merged = new Map<string, Lane>(), conflicts: Lane[] = [];
  const routed = (l: Lane): RoutedEntityInput => ({ entityId: l.entityId, signerId: l.signerId, input: l.proposal ?? { kind: "txs", timestamp: l.timestamp, txs: [] }, ...laneProvenance(l) });
  // og: a runtimeOutput envelope is an effect boundary; ordinary lanes never merge across it
  let boundary = 0;
  for (const input of inputs) {
    const lane = laneOf(input), laneMergeKey = mergeKey(lane);
    if (!laneMergeKey.ok) return laneMergeKey;
    const isOutput = runtimeOutputOf(lane) !== undefined, key = isOutput ? laneMergeKey.value : `${boundary}:${laneMergeKey.value}`;
    if (isOutput && !merged.has(key)) boundary += 1;
    const existing = merged.get(key);
    if (existing === undefined) { merged.set(key, lane); continue; }
    if (existing.proposal !== undefined && lane.proposal !== undefined && (frameIdOf(existing.proposal.frame) !== frameIdOf(lane.proposal.frame) || existing.proposal.frame.height !== lane.proposal.frame.height)) { conflicts.push(lane); continue; }
    if ((lane.vote !== undefined || existing.vote !== undefined) && canon(lane.vote?.vote) !== canon(existing.vote?.vote)) return frameErr(`ENTITY_LEADER_VOTE_EQUIVOCATION:${lane.vote?.vote.voterId ?? "missing"}`);
    if ((lane.jPrefix !== undefined || existing.jPrefix !== undefined) && canon(lane.jPrefix?.attestations) !== canon(existing.jPrefix?.attestations)) return frameErr("ENTITY_INPUT_J_PREFIX_EQUIVOCATION");
    let next: Lane = existing;
    if (lane.txs !== undefined && !exactReplay(existing, lane)) next = { ...next, txs: mergeJEvents([...(existing.txs ?? []), ...lane.txs]) };
    if (lane.precommit !== undefined && existing.precommit !== undefined) {
      const bundles = mergeBundles(existing.precommit.signatures, lane.precommit.signatures);
      if (!bundles.ok) return bundles;
      next = { ...next, precommit: { ...existing.precommit, signatures: bundles.value } };
    }
    // og: a verified commit replaces an unverified copy of the same frame
    if (lane.proposal !== undefined && (existing.proposal === undefined || (verified(routed(lane)) && !verified(routed(existing))))) next = { ...next, proposal: lane.proposal };
    merged.set(key, next);
  }
  const out: RoutedEntityInput[] = [];
  for (const l of [...merged.values(), ...conflicts]) {
    // og: exact Account replays and repeated J observations collapse, then the scheduled wake runs first (conflicting wakes refuse the frame)
    const txs = l.txs === undefined || l.txs.length === 0 ? ok(l.txs ?? []) : mapErr(prioritizeWake(dedupAccountInputs(mergeJEvents(l.txs))), (): RuntimeError => ({ _tag: "runtime_frame", code: "SCHEDULED_WAKE_CONFLICTING_INPUTS" }) as RuntimeError);
    if (!txs.ok) return txs;
    const input: EntityInput = l.vote ?? l.jPrefix ?? l.proposal ?? l.precommit ?? { kind: "txs", timestamp: l.timestamp, txs: txs.value };
    out.push({ entityId: l.entityId, signerId: l.signerId, input, ...laneProvenance(l) });
  }
  // og applyCausalEntityInputOrder: a source-pull consumer runs after the target ACK it consumes; then the consensus-race priority
  const consumers = new Set(out.filter((i) => i.input.kind === "txs" && sourcePullConsumer({ entityId: i.entityId, signerId: i.signerId, timestamp: 0n, txs: i.input.txs })));
  return ok(prioritizeConsensusInputs([...out.filter((i) => !consumers.has(i)), ...out.filter((i) => consumers.has(i))], verified));
};

// ---- og runtime/tx/tx-handlers.ts ----
const HASH_32 = /^0x[0-9a-f]{64}$/, COMMAND_ID = /^[A-Za-z0-9._:-]{16,128}$/, MAX_ACTIVE_RUNTIME_ADAPTER_COMMAND_LANES = 1_024;
const txErr = (code: string): Result<never, RuntimeError> => err({ _tag: "runtime_tx", code });
/** og applyRuntimeAdapterCommandMarker: validate, prune other expired lanes, require the next contiguous sequence, record the frontier. */
const adapterCommand = (rt: Runtime, raw: Extract<RuntimeTx, { type: "recordRuntimeAdapterCommand" }>["data"]): Result<Runtime, RuntimeError> => {
  const laneId = lower(raw.laneId), commandId = String(raw.commandId || "").trim(), inputHash = lower(raw.inputHash), sequence = Number(raw.sequence);
  const expiresAtMs = raw.expiresAtMs === null ? null : Number(raw.expiresAtMs);
  if (!Number.isSafeInteger(sequence) || sequence <= 0) return txErr("RADAPTER_COMMAND_SEQUENCE_INVALID");
  if (!HASH_32.test(laneId)) return txErr("RADAPTER_COMMAND_LANE_INVALID");
  if (!COMMAND_ID.test(commandId)) return txErr("RADAPTER_COMMAND_ID_INVALID");
  if (!HASH_32.test(inputHash)) return txErr("RADAPTER_COMMAND_INPUT_HASH_INVALID");
  if (expiresAtMs !== null && (!Number.isSafeInteger(expiresAtMs) || expiresAtMs <= 0)) return txErr("RADAPTER_COMMAND_EXPIRY_INVALID");
  const nowMs = rt.timestamp < 0n ? 0 : Number(rt.timestamp);
  const kept = new Map([...rt.adapterFrontiers].filter(([id, f]) => id === laneId || f.expiresAtMs === null || f.expiresAtMs > nowMs));
  const prior = kept.get(laneId);
  if (sequence !== (prior?.lastContiguousSequence ?? 0) + 1) return txErr("RADAPTER_COMMAND_FRONTIER_NONCONTIGUOUS");
  if (prior === undefined && kept.size >= MAX_ACTIVE_RUNTIME_ADAPTER_COMMAND_LANES) return txErr("RADAPTER_COMMAND_FRONTIER_CAPACITY_EXCEEDED");
  kept.set(laneId, { lastContiguousSequence: sequence, lastInputHash: inputHash, lastCommandId: commandId, observedHeight: Number(rt.height) + 1, expiresAtMs });
  return ok({ ...rt, adapterFrontiers: kept });
};
/** og registration/entity-creation/crypto.ts + entity/auth/crypto.ts: HKDF-SHA256(seed, salt=entityId, info) then X25519. */
export const entityEncryptionPublicKey = (seed: string, entity: string): string => {
  const priv = hkdf(sha256, hexToBytes(seed), utf8(lower(entity)), utf8("xln:entity-encryption:v1"), 32);
  return bytesToHex(x25519.getPublicKey(priv)).toLowerCase();
};
const MAX_NUMBERED_ENTITY = 1_000_000n;
/** Called only on a 0x-prefixed 32-byte hex id (checked first), so the BigInt parse cannot fail. */
const isNumberedEntity = (id: string): boolean => { const n = BigInt(id); return n > 0n && n < MAX_NUMBERED_ENTITY; };
/** og ethers.getAddress: a mixed-case address must carry its EIP-55 checksum. */
const checksumValid = (a: string): boolean => { const body = a.slice(2); return body === body.toLowerCase() || body === body.toUpperCase() || checksum(a) === a; };
/** og ethers.computeAddress over a 33/65-byte secp256k1 public key; an off-curve key is refused (og throws). */
const pubkeyAddress = (key: string): string | null => {
  try { return bytesToHex(keccak256(secp256k1.ProjectivePoint.fromHex(hexBody(key)).toRawBytes(false).slice(1)).slice(12)); } catch { return null; }
};
/** og toBoardEntityId + resolveValidatorAddress: a bytes32 id as is, an EOA (or public key) as its zero-padded address. */
const boardValidatorId = (v: string): Result<string, RuntimeError> => {
  if (/^0x[0-9a-f]{64}$/i.test(v)) return ok(v.toLowerCase());
  if (v.startsWith("0x") && v.length === 42) return /^0x[0-9a-fA-F]{40}$/.test(v) && checksumValid(v) ? ok(addressAsId(v)) : txErr("BOARD_VALIDATOR_ADDRESS_INVALID");
  if (v.startsWith("0x") && (v.length === 68 || v.length === 132)) { const a = pubkeyAddress(v); return a === null ? txErr("BOARD_VALIDATOR_PUBLIC_KEY_INVALID") : ok(addressAsId(a)); }
  return txErr("BOARD_VALIDATOR_ADDRESS_REQUIRED");
};
/** og factory.ts encodeBoard -> hashBoard: the lazy Entity id of a board config (validators positional, shares as uint16 powers, zero delays). */
export const lazyBoardEntityId = (config: EntityRootConfig): Result<string, RuntimeError> => map(lazyBoardEncoding(config), (encoded) => keccak256Hex(hexToBytes(encoded)));
/** og factory.ts encodeBoard: abi.encode(Board) of a board config. */
export const lazyBoardEncoding = (config: EntityRootConfig): Result<string, RuntimeError> => {
  if (config.validators.length === 0) return txErr("BOARD_EMPTY");
  const seen = new Set<string>();
  for (const v of config.validators) { const id = lower(v); if (id === "" || seen.has(id)) return txErr("BOARD_VALIDATOR_DUPLICATE_OR_EMPTY"); seen.add(id); }
  const proposer = config.validators[0] ?? "";
  if (!/^0x[0-9a-f]{40}$/i.test(proposer)) return txErr("BOARD_PROPOSER_EOA_REQUIRED");
  if (!checksumValid(proposer)) return txErr("BOARD_VALIDATOR_ADDRESS_INVALID");
  const shares = new Map<string, bigint>();
  for (const [raw, share] of Object.entries(config.shares)) {
    const id = lower(raw);
    if (id === "" || shares.has(id)) return txErr("BOARD_SHARE_DUPLICATE_OR_EMPTY");
    if (!seen.has(id)) return txErr("BOARD_SHARE_NOT_VALIDATOR");
    if (typeof share !== "bigint" || share <= 0n) return txErr("BOARD_VOTING_POWER_NOT_POSITIVE");
    shares.set(id, share);
  }
  return chain(traverse(config.validators, boardValidatorId), (ids): Result<string, RuntimeError> => {
    const powers: number[] = [];
    for (const v of config.validators) { const s = shares.get(lower(v)); if (s === undefined) return txErr("BOARD_VOTING_POWER_MISSING"); if (s > 0xffffn) return txErr("BOARD_WEIGHT_OUT_OF_RANGE"); powers.push(Number(s)); }
    if (config.threshold <= 0n) return txErr("BOARD_THRESHOLD_NOT_POSITIVE");
    if (config.threshold > 0xffffn) return txErr("BOARD_THRESHOLD_OUT_OF_RANGE");
    if (config.threshold > powers.reduce((t, p) => t + BigInt(p), 0n)) return txErr("BOARD_THRESHOLD_EXCEEDS_POWER");
    return ok(encodeBoardBytes({ votingThreshold: Number(config.threshold), entityIds: ids, votingPowers: powers, boardChangeDelay: 0, controlChangeDelay: 0, dividendChangeDelay: 0 }));
  });
};
const quorumOf = (config: ImportConfig): Result<Authority, RuntimeError> => {
  const members = new Map<Address, { readonly shares: bigint }>();
  for (const v of config.validators) {
    const a = address(v), share = Object.entries(config.shares).find(([k]) => lower(k) === lower(v))?.[1];
    if (!a.ok || share === undefined) return txErr("IMPORT_REPLICA_VALIDATOR_NOT_EOA");
    members.set(a.value, { shares: share });
  }
  return ok({ _tag: "teaching", threshold: config.threshold, members });
};
/**
 * og importReplicaRuntimeTx: normalize the identity, bind the jurisdiction, prove board authority (signer on board, proposer flag = board index 0,
 * lazy id = hashBoard(encodeBoard(config)); a numbered Entity needs certified registration evidence), check the seed-derived encryption key
 * against siblings and the retained seed, then reuse / checkpoint-import / create the genesis replica.
 */
const importReplica = (rt: Runtime, tx: Extract<RuntimeTx, { type: "importReplica" }>): Result<Runtime, RuntimeError> => {
  const entity = lower(tx.entityId), signer = lower(tx.signerId), { config } = tx.data;
  if (entity === "" || signer === "") return txErr("IMPORT_REPLICA_INVALID_ID");
  const key = `${entity}:${signer}`, existing = [...rt.entities].find(([k]) => lower(k) === key);
  return chain(requireBoundJurisdiction(rt, entity, config), (j) => importBoundReplica(rt, tx, entity, signer, key, existing, j));
};
/** og getSwapPairOrientation: a liquid reference stable (USDC 1, USDT 3) quotes; otherwise the lower id is the base. */
const swapPairOrientation = (a: number, b: number): { readonly baseTokenId: number; readonly quoteTokenId: number; readonly pairId: string } => {
  const left = Math.min(a, b), right = Math.max(a, b), pairId = `${left}/${right}`, liquid = (t: number): boolean => t === 1 || t === 3;
  return liquid(a) && !liquid(b) ? { baseTokenId: b, quoteTokenId: a, pairId } : !liquid(a) && liquid(b) ? { baseTokenId: a, quoteTokenId: b, pairId } : { baseTokenId: left, quoteTokenId: right, pairId };
};
/** og buildDefaultEntitySwapPairs(getTokenIdsForJurisdiction(j)): USDC/WETH/USDT everywhere, plus TRX/SUN on a Tron stack; WETH/USDC first, then by quote, then base. */
export const defaultEntitySwapPairs = (name: string | undefined, chainId: number | undefined): readonly { readonly baseTokenId: number; readonly quoteTokenId: number; readonly pairId: string }[] => {
  const n = String(name ?? "").trim().toLowerCase(), tron = n.includes("tron") || n === "rpc2" || (n.length === 0 && chainId === 31338);
  const tokens = tron ? [1, 2, 3, 4, 5] : [1, 2, 3], seen = new Set<string>(), pairs: { baseTokenId: number; quoteTokenId: number; pairId: string }[] = [];
  for (let i = 0; i < tokens.length; i++) for (let j = i + 1; j < tokens.length; j++) {
    const o = swapPairOrientation(tokens[i] as number, tokens[j] as number), k = `${o.baseTokenId}/${o.quoteTokenId}`;
    if (!seen.has(k)) { seen.add(k); pairs.push(o); }
  }
  const primary = "2/1", key = (p: { readonly baseTokenId: number; readonly quoteTokenId: number }): string => `${p.baseTokenId}/${p.quoteTokenId}`;
  return pairs.sort((a, b) => (key(a) === primary ? -1 : key(b) === primary ? 1 : a.quoteTokenId - b.quoteTokenId || a.baseTokenId - b.baseTokenId));
};
/** og buildGenesisReplica `replica.position`: validator-local, its jurisdiction defaulting to the active one or 'default'. */
const withPosition = (rt: Runtime, key: string, position: ReplicaPosition | undefined, active: string | undefined): Runtime =>
  position === undefined ? rt : withLocal(rt, key, { position: { ...position, jurisdiction: position.jurisdiction || active || "default" } });
const importBoundReplica = (rt: Runtime, tx: Extract<RuntimeTx, { type: "importReplica" }>, entity: string, signer: string, key: string, existing: readonly [string, EntityReplica] | undefined, j: ImportJurisdiction): Result<Runtime, RuntimeError> => {
  const { config, isProposer, entitySeed } = tx.data;
  const domain: Domain = { chainId: j.chainId ?? 0, depositoryAddress: j.depositoryAddress ?? "" }, jurisdictionConfig = jurisdictionConfigOf(j);
  const siblings = [...rt.entities.values()].filter((r) => lower(r.state.id) === entity);
  const boardIndex = config.validators.findIndex((v) => lower(v) === signer);
  if (boardIndex < 0) return txErr("IMPORT_REPLICA_SIGNER_NOT_ON_BOARD");
  if (isProposer !== (boardIndex === 0)) return txErr("IMPORT_REPLICA_PROPOSER_FLAG_INVALID");
  if (!/^0x[0-9a-f]{64}$/i.test(entity)) return txErr("FINTECH_SAFETY_INVALID_ENTITY_ID");
  // og assertNumberedReplicaImportAuthority: a lazy id is its board hash; a numbered id needs receipt-proven registration evidence for this board.
  const boardAuthority = isNumberedEntity(entity)
    ? chain(mapErr(boardStackKey({ chainId: j.chainId, depositoryAddress: j.depositoryAddress, entityProviderAddress: j.entityProviderAddress }), (e): RuntimeError => ({ _tag: "runtime_tx", code: e.code })),
      (stackKey) => chain(registrationEvidenceKey(stackKey, entity), (evidenceKey): Result<void, RuntimeError> => {
        const evidence = rt.registrationEvidence.get(evidenceKey);
        if (evidence === undefined) return txErr(`NUMBERED_REPLICA_REGISTRATION_EVIDENCE_MISSING:${entity}`);
        return chain(lazyBoardEntityId(config), (boardHash) => (lower(String(evidence["boardHash"])) === lower(boardHash) ? ok(undefined) : txErr(`NUMBERED_REPLICA_REGISTRATION_BOARD_MISMATCH:${entity}`)));
      }))
    : chain(lazyBoardEntityId(config), (boardId): Result<void, RuntimeError> => (lower(boardId) === entity ? ok(undefined) : txErr("IMPORT_REPLICA_LAZY_BOARD_ID_MISMATCH")));
  return chain(boardAuthority, (): Result<Runtime, RuntimeError> => {
    if (!/^0x[0-9a-f]{128}$/.test(entitySeed)) return txErr("IMPORT_REPLICA_ENTITY_SEED_INVALID");
    const publicKey = entityEncryptionPublicKey(entitySeed, entity);
    if (siblings.some((r) => r.state.committed["entityEncryptionPublicKey"] !== publicKey)) return txErr("IMPORT_REPLICA_ENTITY_ENCRYPTION_PUBLIC_KEY_MISMATCH");
    const retained = rt.encryptionSeeds.get(entity);
    if (retained !== undefined && retained !== entitySeed) return txErr("ENTITY_ENCRYPTION_SEED_CONFLICT");
    const finish = (r: EntityReplica, drop?: string): Runtime => ({
      ...rt, entities: mapSet(drop === undefined ? rt.entities : mapDelete(rt.entities, drop), key, r), encryptionSeeds: mapSet(rt.encryptionSeeds, entity, entitySeed),
    });
    return chain(quorumOf(config), (authority) => chain(admitQuorum(authority), (quorum): Result<Runtime, RuntimeError> => {
      const certified = siblings.reduce<EntityReplica | undefined>((best, r) => (best === undefined || r.head.height > best.head.height ? r : best), undefined);
      const sameAuthority = (from: EntityReplica): Result<void, RuntimeError> => chain(authorityRoot({ ...from.state, quorum }), (supplied) =>
        chain(authorityRoot(from.state), (held) => (supplied === held ? ok(undefined) : txErr("IMPORT_REPLICA_CONFIG_CHECKPOINT_MISMATCH"))));
      const at = (from: EntityReplica, state: EntityState, mempool: readonly EntityTx[]): OpenEntity => openEntity(memberId(quorum, signer as Address) ?? (signer as Address), state, from.head, mempool, from.accountReplicas);
      if (existing !== undefined) {
        const [oldKey, replica] = existing;
        // A certified Entity keeps its state: re-import changes validator-local routing only (og reuseExistingReplica).
        if (replica.head.height > 0n || siblings.some((r) => r.head.height > 0n)) return map(sameAuthority(replica), () => finish(replica, oldKey === key ? undefined : oldKey));
        return ok(finish(at(replica, { ...replica.state, quorum, jurisdiction: domain, jurisdictionConfig }, replica.mempool), oldKey === key ? undefined : oldKey));
      }
      // og buildCheckpointReplica: any sibling makes this a checkpoint import; its position defaults to the bound jurisdiction name.
      if (certified !== undefined) return map(sameAuthority(certified), () => { const p = tx.data.position; const rt2 = finish(at(certified, certified.state, [])); return p === undefined ? rt2 : withLocal(rt2, key, { position: { ...p, ...(p.jurisdiction || jurisdictionConfig.name ? { jurisdiction: p.jurisdiction || jurisdictionConfig.name } : {}) } }); });
      // og importReplica genesis: lastFinalizedJHeight starts at the EntityProvider registration base (getJHistoryRegistrationBaseHeight).
      // og buildGenesisReplica also commits the default profile, the crontab and the jurisdiction's default swap pairs.
      const name = typeof tx.data.profileName === "string" && tx.data.profileName.trim().length > 0 ? tx.data.profileName.trim() : `Entity ${entity.slice(-4)}`;
      const committed: EntityCommitted = {
        nonces: new Map(), proposals: new Map(), reserves: new Map(), deferredAccountProposals: new Map(), entityEncryptionPublicKey: publicKey, lastFinalizedJHeight: jHistoryRegistrationBase(jurisdictionConfig),
        profile: { name, isHub: false, avatar: "", bio: "", website: "" }, crontabState: DEFAULT_CRONTAB, swapTradingPairs: defaultEntitySwapPairs(jurisdictionConfig.name, domain.chainId) as unknown as Binary,
      };
      return map(mapErr(createEntity({ id: entity as EntityId, jurisdiction: domain, threshold: config.threshold, members: (authority as Extract<Authority, { _tag: "teaching" }>).members, signerId: signer as Address, timestamp: rt.timestamp, jurisdictionConfig, committed }), (e): RuntimeError => e),
        (r) => withPosition(finish({ ...r, state: { ...r.state, crossJurisdictionBookAdmissions: new Map() } }), key, tx.data.position, rt.activeJurisdiction));
    }));
  });
};
// ---- og runtime/j-submit/jurisdiction-import(-request).ts: the J import registry ----
const ZERO_ADDRESS = `0x${"00".repeat(20)}`;
/** og normalizeJurisdictionImportAddress: ethers.getAddress, lowercased, never the zero address. */
const importAddress = (value: unknown, label: string): Result<string, RuntimeError> => {
  const a = ethAddress(value);
  if (a === null) return txErr(`IMPORT_J_${label}_ADDRESS_INVALID:${String(value ?? "")}`);
  return a === ZERO_ADDRESS ? txErr(`IMPORT_J_${label}_ADDRESS_ZERO`) : ok(a);
};
/** og normalizeJurisdictionImportContracts: all four stack contracts, each a checksummed non-zero address. */
const importContracts = (c: JContracts | undefined, required: boolean): Result<FullJContracts | undefined, RuntimeError> => {
  if (!c) return required ? txErr("IMPORT_J_RPC_CONTRACTS_REQUIRED") : ok(undefined);
  const missing = (["depository", "entityProvider", "account", "deltaTransformer"] as const).filter((k) => !c[k]);
  if (missing.length > 0) return txErr(`IMPORT_J_CONTRACTS_INCOMPLETE:${missing.join(",")}`);
  return chain(importAddress(c.depository, "DEPOSITORY"), (depository) => chain(importAddress(c.entityProvider, "ENTITY_PROVIDER"), (entityProvider) =>
    chain(importAddress(c.account, "ACCOUNT"), (account) => map(importAddress(c.deltaTransformer, "DELTA_TRANSFORMER"), (deltaTransformer) => ({ depository, entityProvider, account, deltaTransformer })))));
};
const requireContracts = (c: JContracts | undefined): Result<FullJContracts, RuntimeError> => map(importContracts(c, true), (x) => x as FullJContracts);
const parseUrl = (s: string): URL | null => { try { return new URL(s); } catch { return null; } };
/** og normalizeJurisdictionImportRequest: one http(s) RPC (or none for BrowserVM), full contracts + deployment block for RPC stacks, no custom tokens. */
export const normalizeJurisdictionImportRequest = (raw: JurisdictionImportRequest): Result<JurisdictionImportRequest, RuntimeError> => {
  const name = String(raw.name ?? "").trim(), ticker = String(raw.ticker ?? "").trim().toUpperCase(), chainId = Number(raw.chainId);
  if (!name || name.length > 128) return txErr("IMPORT_J_NAME_INVALID");
  if (!ticker || ticker.length > 16) return txErr("IMPORT_J_TICKER_INVALID");
  if (!Number.isSafeInteger(chainId) || chainId <= 0) return txErr(`IMPORT_J_CHAIN_ID_INVALID:${String(raw.chainId)}`);
  if (!Array.isArray(raw.rpcs)) return txErr("IMPORT_J_RPCS_INVALID");
  const rpcs: string[] = [];
  for (const [index, value] of (raw.rpcs as readonly unknown[]).entries()) {
    const rpc = String(value ?? "").trim(), url = rpc === "" ? null : parseUrl(rpc);
    if (url === null) return txErr(`IMPORT_J_RPC_INVALID:${index}`);
    if (url.protocol !== "http:" && url.protocol !== "https:") return txErr(`IMPORT_J_RPC_PROTOCOL_INVALID:${index}:${url.protocol}`);
    rpcs.push(url.toString());
  }
  if (new Set(rpcs).size !== rpcs.length) return txErr("IMPORT_J_RPC_DUPLICATED");
  if (rpcs.length > 8) return txErr(`IMPORT_J_RPC_LIMIT_EXCEEDED:${rpcs.length}`);
  const isBrowserVM = rpcs.length === 0;
  return chain(importContracts(raw.contracts, !isBrowserVM), (contracts): Result<JurisdictionImportRequest, RuntimeError> => {
    const deployment = Number(raw.entityProviderDeploymentBlock);
    if (!isBrowserVM && raw.entityProviderDeploymentBlock === undefined) return txErr("IMPORT_J_ENTITY_PROVIDER_DEPLOYMENT_BLOCK_REQUIRED");
    if (raw.entityProviderDeploymentBlock !== undefined && (!Number.isSafeInteger(deployment) || deployment < 1)) return txErr(`IMPORT_J_ENTITY_PROVIDER_DEPLOYMENT_BLOCK_INVALID:${String(raw.entityProviderDeploymentBlock)}`);
    if (isBrowserVM && raw.entityProviderDeploymentBlock !== undefined) return txErr("IMPORT_J_BROWSERVM_DEPLOYMENT_BLOCK_UNEXPECTED");
    if ((raw.tokens?.length ?? 0) > 0) return txErr("IMPORT_J_CUSTOM_TOKENS_UNSUPPORTED");
    if (raw.blockTimeMs !== undefined && (!Number.isSafeInteger(raw.blockTimeMs) || raw.blockTimeMs <= 0)) return txErr(`IMPORT_J_BLOCK_TIME_INVALID:${String(raw.blockTimeMs)}`);
    if (raw.startAtCurrentBlock !== undefined && typeof raw.startAtCurrentBlock !== "boolean") return txErr("IMPORT_J_START_AT_CURRENT_BLOCK_INVALID");
    const policy = raw.rpcPolicy as unknown;
    if (policy !== undefined) {
      if (policy === "failover") return txErr("IMPORT_J_RPC_POLICY_UNSUPPORTED:failover");
      const q = typeof policy === "object" && policy !== null ? (policy as { readonly mode?: unknown; readonly min?: unknown }) : null;
      if (q !== null && q.mode === "quorum" && Number.isSafeInteger(q.min) && (q.min as number) > 0 && (q.min as number) <= rpcs.length) return txErr("IMPORT_J_RPC_POLICY_UNSUPPORTED:quorum");
      if (policy !== "single") return txErr("IMPORT_J_RPC_POLICY_INVALID");
      if (rpcs.length !== 1) return txErr(`IMPORT_J_RPC_POLICY_SINGLE_REQUIRES_ONE_RPC:${rpcs.length}`);
    }
    if (rpcs.length > 1) return txErr(`IMPORT_J_MULTIPLE_RPCS_UNSUPPORTED:${rpcs.length}`);
    return ok({
      name, chainId, ticker, rpcs, ...(isBrowserVM ? {} : { entityProviderDeploymentBlock: deployment }), ...opt("blockTimeMs", raw.blockTimeMs),
      ...opt("startAtCurrentBlock", raw.startAtCurrentBlock), ...opt("rpcPolicy", raw.rpcPolicy), ...opt("contracts", contracts),
    });
  });
};
/** og buildJurisdictionImportRequestHash: keccak256(safeStringify({domain, request: normalized})). */
export const jurisdictionImportRequestHash = (request: JurisdictionImportRequest): Result<string, RuntimeError> =>
  map(normalizeJurisdictionImportRequest(request), (normalized) => `0x${keccakUtf8(stableJson({ domain: "xln/jurisdiction-import/v1", request: normalized }))}`);
const jNameKey = (name: string): string => name.trim().toLowerCase();
const findJReplica = (rt: Runtime, name: string): readonly [string, JReplica] | undefined => [...rt.jReplicas].find(([k]) => jNameKey(k) === jNameKey(name));
/** og assertReplicaMatchesRequest: an existing J replica must be the same chain, deployment block and (when given) contract stack. */
const jReplicaMatchesRequest = (replica: JReplica, request: Pick<JurisdictionImportRequest, "name" | "chainId" | "entityProviderDeploymentBlock" | "contracts">): Result<void, RuntimeError> => {
  if (Number(replica.chainId) !== request.chainId) return txErr(`IMPORT_J_EXISTING_CHAIN_CONFLICT:${request.name}`);
  if (request.entityProviderDeploymentBlock !== undefined && Number(replica.entityProviderDeploymentBlock) !== request.entityProviderDeploymentBlock) return txErr(`IMPORT_J_EXISTING_DEPLOYMENT_BLOCK_CONFLICT:${request.name}`);
  if (!request.contracts) return ok(undefined);
  const c = replica.contracts;
  const held = { ...(c?.depository ? { depository: c.depository } : {}), ...(c?.entityProvider ? { entityProvider: c.entityProvider } : {}), ...(c?.account ? { account: c.account } : {}), ...(c?.deltaTransformer ? { deltaTransformer: c.deltaTransformer } : {}) };
  return chain(requireContracts(held), (existing) => (stableJson(existing) === stableJson(request.contracts) ? ok(undefined) : txErr(`IMPORT_J_EXISTING_CONTRACTS_CONFLICT:${request.name}`)));
};
/** og applyImportJurisdictionIntent: at most one BrowserVM stack; an existing replica must match; one pending intent per name, keyed by its request hash. */
const importJ = (rt: Runtime, raw: JurisdictionImportRequest): Result<Runtime, RuntimeError> => chain(normalizeJurisdictionImportRequest(raw), (request) => {
  if (request.rpcs.length === 0) {
    const replica = [...rt.jReplicas].find(([n, r]) => jNameKey(n) !== jNameKey(request.name) && Array.isArray(r.rpcs) && r.rpcs.length === 0);
    const intent = [...rt.pendingJImports.values()].find((i) => jNameKey(i.request.name) !== jNameKey(request.name) && i.request.rpcs.length === 0);
    if (replica !== undefined || intent !== undefined) return txErr(`IMPORT_J_MULTIPLE_BROWSERVM_UNSUPPORTED:${request.name}:${replica?.[0] ?? intent?.request.name ?? "unknown"}`);
  }
  const existing = findJReplica(rt, request.name);
  if (existing !== undefined) return map(jReplicaMatchesRequest(existing[1], request), () => rt);
  return chain(jurisdictionImportRequestHash(request), (requestHash): Result<Runtime, RuntimeError> => {
    for (const pending of rt.pendingJImports.values()) {
      if (jNameKey(pending.request.name) !== jNameKey(request.name)) continue;
      return pending.importId === requestHash && pending.requestHash === requestHash ? ok(rt) : txErr(`IMPORT_J_PENDING_CONFLICT:${request.name}`);
    }
    return ok({ ...rt, pendingJImports: mapSet(rt.pendingJImports, requestHash, { importId: requestHash, requestHash, request }) });
  });
});
/** og validateImportResult: the result answers its pending intent exactly; tokens are unique, well-formed, address-normalized and sorted by id. */
const validateImportResult = (pending: PendingJurisdictionImport, raw: JurisdictionImportResult): Result<JurisdictionImportResult, RuntimeError> => {
  const request = pending.request, id = pending.importId;
  if (raw.importId !== pending.importId || raw.requestHash !== pending.requestHash || raw.name !== request.name || raw.chainId !== request.chainId || raw.ticker !== request.ticker
    || stableJson(raw.rpcs) !== stableJson(request.rpcs) || raw.blockTimeMs !== request.blockTimeMs) return txErr(`IMPORT_J_RESULT_INTENT_MISMATCH:${id}`);
  return chain(requireContracts(raw.contracts), (contracts): Result<JurisdictionImportResult, RuntimeError> => {
    if (request.contracts && stableJson(contracts) !== stableJson(request.contracts)) return txErr(`IMPORT_J_RESULT_CONTRACTS_MISMATCH:${id}`);
    if (!/^(0|[1-9][0-9]*)$/.test(String(raw.blockNumber))) return txErr(`IMPORT_J_RESULT_BLOCK_NUMBER_INVALID:${raw.blockNumber}`);
    const isBrowserVM = request.rpcs.length === 0;
    if (raw.watcherReceiptCommitment !== undefined && (raw.watcherReceiptCommitment !== "tron-rpc-attested" || isBrowserVM || raw.watcherConfirmationDepth !== 0)) return txErr("IMPORT_J_RESULT_RECEIPT_COMMITMENT_INVALID");
    if (isBrowserVM) {
      if (!raw.stateRoot || !/^0x[0-9a-fA-F]{64}$/.test(raw.stateRoot)) return txErr("IMPORT_J_RESULT_STATE_ROOT_INVALID");
      if (!raw.browserVMState) return txErr("IMPORT_J_RESULT_BROWSERVM_STATE_MISSING");
    } else if (raw.stateRoot !== null || raw.browserVMState !== undefined) return txErr("IMPORT_J_RESULT_RPC_STATE_INVALID");
    for (const [label, value, minimum] of [["WATCHER_CONFIRMATION_DEPTH", raw.watcherConfirmationDepth, 0], ["ENTITY_PROVIDER_DEPLOYMENT_BLOCK", raw.entityProviderDeploymentBlock, 1]] as const)
      if (!Number.isSafeInteger(value) || value < minimum) return txErr(`IMPORT_J_RESULT_${label}_INVALID:${String(value)}`);
    const ids = new Set<number>(), addresses = new Set<string>(), tokens: JTokenInfo[] = [];
    for (const [index, token] of raw.tokenRegistry.entries()) {
      const prefix = `IMPORT_J_RESULT_TOKEN_${index}`;
      if (!Number.isSafeInteger(token.tokenId) || token.tokenId < 1 || ids.has(token.tokenId)) return txErr(`${prefix}_ID_INVALID:${String(token.tokenId)}`);
      if (![0, 1, 2].includes(token.tokenType)) return txErr(`${prefix}_TYPE_INVALID:${String(token.tokenType)}`);
      if (!Number.isSafeInteger(token.decimals) || token.decimals < 0 || token.decimals > 255) return txErr(`${prefix}_DECIMALS_INVALID:${String(token.decimals)}`);
      const a = importAddress(token.address, `${prefix}_ADDRESS`);
      if (!a.ok) return a;
      if (addresses.has(a.value)) return txErr(`${prefix}_ADDRESS_DUPLICATE:${a.value}`);
      if (typeof token.symbol !== "string" || typeof token.name !== "string" || token.externalTokenId < 0n) return txErr(`${prefix}_METADATA_INVALID`);
      ids.add(token.tokenId); addresses.add(a.value); tokens.push({ ...token, address: a.value });
    }
    return ok({ ...raw, tokenRegistry: tokens.sort((l, r) => l.tokenId - r.tokenId), contracts });
  });
};
/** og assertReplicaMatchesResult. */
const jReplicaMatchesResult = (replica: JReplica, result: JurisdictionImportResult): Result<void, RuntimeError> => chain(jReplicaMatchesRequest(replica, result), () =>
  replica.blockNumber.toString() !== result.blockNumber || Number(replica.watcherConfirmationDepth) !== result.watcherConfirmationDepth || replica.watcherReceiptCommitment !== result.watcherReceiptCommitment
    || Number(replica.entityProviderDeploymentBlock) !== result.entityProviderDeploymentBlock || stableJson(replica.tokenRegistry) !== stableJson(result.tokenRegistry)
    ? txErr(`IMPORT_J_RESULT_EXISTING_REPLICA_CONFLICT:${result.name}`) : ok(undefined));
/** og applyCompleteImportJurisdiction: install the prepared J replica (unless it exists and matches), drop the intent, default the active jurisdiction. */
const completeImportJ = (rt: Runtime, raw: JurisdictionImportResult): Result<Runtime, RuntimeError> => {
  const existing = findJReplica(rt, String(raw.name ?? "")), pending = rt.pendingJImports.get(raw.importId);
  if (pending === undefined) return existing !== undefined ? map(jReplicaMatchesResult(existing[1], raw), () => rt) : txErr(`IMPORT_J_RESULT_STALE:${raw.importId}`);
  return chain(validateImportResult(pending, raw), (result): Result<Runtime, RuntimeError> => {
    let jReplicas = rt.jReplicas;
    if (existing !== undefined) {
      const matched = jReplicaMatchesResult(existing[1], result);
      if (!matched.ok) return matched;
    } else {
      for (const [name, replica] of rt.jReplicas) {
        if (Number(replica.chainId) !== result.chainId || !replica.contracts?.depository) continue;
        const depository = importAddress(replica.contracts.depository, "EXISTING_DEPOSITORY");
        if (!depository.ok) return depository;
        if (depository.value === result.contracts.depository) return txErr(`IMPORT_J_WATCHER_IDENTITY_CONFLICT:${result.name}:${name}:${result.chainId}:${result.contracts.depository}`);
      }
      jReplicas = mapSet(rt.jReplicas, result.name, {
        name: result.name, blockNumber: BigInt(result.blockNumber), stateRoot: result.stateRoot ? result.stateRoot.toLowerCase() : null, mempool: [], blockDelayMs: 300,
        ...(result.blockTimeMs ? { blockTimeMs: result.blockTimeMs } : {}), lastBlockTimestamp: Number(rt.timestamp), position: { x: 0, y: 50, z: 0 },
        entityProviderDeploymentBlock: result.entityProviderDeploymentBlock, contracts: result.contracts, rpcs: [...result.rpcs], chainId: result.chainId,
        watcherConfirmationDepth: result.watcherConfirmationDepth, ...(result.watcherReceiptCommitment ? { watcherReceiptCommitment: result.watcherReceiptCommitment } : {}), tokenRegistry: result.tokenRegistry,
      });
    }
    return ok({ ...rt, jReplicas, ...(result.browserVMState ? { browserVMState: result.browserVMState } : {}), pendingJImports: mapDelete(rt.pendingJImports, result.importId), activeJurisdiction: rt.activeJurisdiction || result.name });
  });
};

// ---- og jurisdiction/adapter/watcher/observe/watcher-replica.ts + watcher-cursor.ts ----
const label = (v: unknown): string => String(v || "").trim().toLowerCase();
const watcherChainIdOf = (r: JReplica | undefined): number | null => { const c = Number(r?.chainId); return Number.isFinite(c) && c > 0 ? Math.floor(c) : null; };
/** og findWatcherJurisdictionReplica: by depository and/or chain id (ambiguity refused), else the active replica, else the first. */
const findWatcherJReplica = (rt: Runtime, depositoryAddress?: string, chainId?: number): Result<readonly [string, JReplica] | null, RuntimeError> => {
  const entries = [...rt.jReplicas];
  if (entries.length === 0) return ok(null);
  const depository = label(depositoryAddress), wanted = typeof chainId === "number" && Number.isFinite(chainId) && chainId > 0 ? Math.floor(chainId) : null;
  if (depository || wanted !== null) {
    const exact = entries.filter(([, r]) => (!depository || label(r.contracts?.depository) === depository) && (wanted === null || watcherChainIdOf(r) === wanted));
    if (exact.length > 1) return txErr(`J_WATCHER_JURISDICTION_AMBIGUOUS:${wanted ?? "any"}:${depository || "any"}`);
    return ok(exact[0] ?? null);
  }
  const active = rt.activeJurisdiction ? rt.jReplicas.get(rt.activeJurisdiction) : undefined;
  return ok(active !== undefined && rt.activeJurisdiction !== undefined ? [rt.activeJurisdiction, active] : (entries[0] ?? null));
};
const requireWatcherJReplica = (rt: Runtime, depositoryAddress: string | undefined, chainId: number | undefined, context: string): Result<readonly [string, JReplica], RuntimeError> =>
  chain(findWatcherJReplica(rt, depositoryAddress, chainId), (found) => {
    if (found !== null) return ok(found);
    const available = [...rt.jReplicas.values()].map((r) => `${r.name || "unnamed"}/${String(r.chainId ?? "missing")}/${label(r.contracts?.depository) || "missing"}`).join(",");
    return txErr(`J_WATCHER_JURISDICTION_NOT_FOUND:${context}:chain=${String(chainId ?? "any")}:depository=${label(depositoryAddress) || "any"}:available=${available || "none"}`);
  });
/** og applyWatcherJurisdictionCursor: the committed watcher cursor only moves forward. */
const advanceJWatcherCursor = (rt: Runtime, data: Extract<RuntimeTx, { type: "advanceJWatcherCursor" }>["data"]): Result<Runtime, RuntimeError> => {
  if (!Number.isSafeInteger(data.blockNumber) || data.blockNumber < 0) return txErr(`J_WATCHER_CURSOR_INVALID:${String(data.blockNumber)}`);
  return map(requireWatcherJReplica(rt, data.depositoryAddress, data.chainId, "cursor-apply"), ([key, replica]) =>
    (replica.blockNumber >= BigInt(data.blockNumber) ? rt : { ...rt, jReplicas: mapSet(rt.jReplicas, key, { ...replica, blockNumber: BigInt(data.blockNumber) }) }));
};

// ---- og jurisdiction/machine/jurisdiction-runtime/index.ts: requireBoundEntityConfig ----
/** og isUsableContractAddress / firstUsableContractAddress: the first value that is a valid non-zero address, as given. */
const usableAddress = (...values: readonly unknown[]): string | null => {
  for (const v of values) if (typeof v === "string") { const a = ethAddress(v); if (a !== null && a !== ZERO_ADDRESS) return v; }
  return null;
};
const STACK_REF = /^stack:(?:\d+:)?0x[0-9a-fA-F]{40}$/;
const stackChainId = (v: unknown): number | null => { const n = Number(v); return Number.isSafeInteger(n) && n > 0 ? n : null; };
const jNameOf = (v: unknown): string => (typeof v === "string" ? v.trim().toLowerCase() : "");
/** og getJReplicaByName: the exact key, else the first replica whose name matches case-insensitively. */
const jReplicaByName = (rt: Runtime, name: unknown): JReplica | undefined => {
  const n = jNameOf(name);
  if (!n) return undefined;
  return rt.jReplicas.get(name as string) ?? [...rt.jReplicas.values()].find((r) => jNameOf(r.name) === n);
};
const jReplicaByRef = (rt: Runtime, ref: string): JReplica | undefined =>
  [...rt.jReplicas.values()].find((r) => stackIdOf({ depositoryAddress: usableAddress(r.contracts?.depository) ?? "", chainId: r.chainId }) === ref.trim().toLowerCase());
/** og resolveRuntimeJurisdictionConfig: complete a jurisdiction config from the named (else active, else first) J replica; replica contracts win. */
const resolveRuntimeJurisdiction = (rt: Runtime, current: ImportJurisdiction | undefined): ImportJurisdiction | undefined => {
  const active = rt.activeJurisdiction ? rt.jReplicas.get(rt.activeJurisdiction) : undefined;
  const replica = jReplicaByName(rt, current?.name) ?? active ?? [...rt.jReplicas.values()][0];
  const depositoryAddress = usableAddress(replica?.contracts?.depository, current?.depositoryAddress);
  const entityProviderAddress = usableAddress(replica?.contracts?.entityProvider, current?.entityProviderAddress);
  const rawChain = replica?.chainId !== undefined ? replica.chainId : current?.chainId;
  const chainId = typeof rawChain === "bigint" ? Number(rawChain) : typeof rawChain === "number" && Number.isFinite(rawChain) ? rawChain : undefined;
  const deployment = replica?.entityProviderDeploymentBlock !== undefined ? replica.entityProviderDeploymentBlock : current?.entityProviderDeploymentBlock;
  const currentName = current?.name?.trim() || undefined, currentAddress = current?.address?.trim() || undefined, replicaAddress = replica?.rpcs?.[0]?.trim() || undefined;
  const name = currentName !== undefined ? currentName : replica?.name !== undefined ? replica.name : rt.activeJurisdiction;
  const address = currentAddress ?? replicaAddress ?? (name ? `jreplica://${name}` : undefined);
  if (!name || !address || !depositoryAddress || !entityProviderAddress) return current;
  return { ...current, name, address, entityProviderAddress, depositoryAddress, ...opt("chainId", chainId), ...opt("entityProviderDeploymentBlock", deployment) };
};
/** og requireRuntimeJurisdictionConfigByName: the J replica must exist (by name or stack ref) and resolve to a complete stack of the same name. */
const requireJurisdictionByName = (rt: Runtime, name: string, current: ImportJurisdiction | undefined): Result<ImportJurisdiction, RuntimeError> => {
  const configured = String(name || current?.name || "").trim();
  if (!configured) return txErr("ENTITY_JURISDICTION_MISSING");
  const isRef = STACK_REF.test(configured), replica = isRef ? jReplicaByRef(rt, configured) : jReplicaByName(rt, configured);
  if (replica === undefined) return txErr(`ENTITY_JURISDICTION_UNAVAILABLE: ${configured}`);
  const replicaName = replica.name || configured;
  const chainId = current?.chainId !== undefined ? current.chainId : replica.chainId;
  const candidate: ImportJurisdiction = {
    name: replicaName, address: current?.address || replica.rpcs?.[0] || `jreplica://${replicaName}`,
    entityProviderAddress: current?.entityProviderAddress || replica.contracts?.entityProvider || "", depositoryAddress: current?.depositoryAddress || replica.contracts?.depository || "",
    ...opt("chainId", chainId), ...opt("blockTimeMs", current?.blockTimeMs), ...opt("registrationBlock", current?.registrationBlock),
    ...opt("entityProviderDeploymentBlock", current?.entityProviderDeploymentBlock !== undefined ? current.entityProviderDeploymentBlock : replica.entityProviderDeploymentBlock),
    ...(current?.rebalancePolicyUsd ? { rebalancePolicyUsd: current.rebalancePolicyUsd } : {}),
  };
  const resolved = resolveRuntimeJurisdiction(rt, candidate);
  const same = resolved !== undefined && (isRef ? stackIdOf(resolved) === configured.toLowerCase() : jNameOf(resolved.name) === jNameOf(configured));
  if (resolved === undefined || !same) return txErr(`ENTITY_JURISDICTION_RESOLVE_FAILED: ${configured}`);
  if (!resolved.depositoryAddress || !resolved.entityProviderAddress || !resolved.chainId) return txErr(`ENTITY_JURISDICTION_INCOMPLETE: ${configured}`);
  return ok(resolved);
};
/** og readStrictJurisdictionStackId: chain id and a usable depository, else nothing. */
const strictStackId = (chainId: unknown, depository: unknown): string => {
  const c = stackChainId(chainId), d = usableAddress(depository);
  return c === null || d === null ? "" : stackIdOf({ depositoryAddress: d, chainId: c });
};
/** og requireBoundEntityConfig: resolve the config's jurisdiction against the J replicas and refuse a stack other than the one sibling replicas are bound to. */
const requireBoundJurisdiction = (rt: Runtime, entity: string, config: ImportConfig): Result<ImportJurisdiction, RuntimeError> => {
  const merged = resolveRuntimeJurisdiction(rt, config.jurisdiction) ?? config.jurisdiction;
  const name = typeof merged?.name === "string" ? merged.name.trim() : "";
  if (!name) return txErr(`ENTITY_JURISDICTION_MISSING: entity=${entity}`);
  return chain(requireJurisdictionByName(rt, name, merged), (bound) => {
    const incoming = strictStackId(bound.chainId, bound.depositoryAddress);
    for (const r of rt.entities.values()) {
      if (lower(r.state.id) !== entity || !(r.state.jurisdictionConfig?.name ?? "").trim()) continue;
      const held = strictStackId(r.state.jurisdiction.chainId, r.state.jurisdiction.depositoryAddress);
      if (!held || !incoming || held !== incoming) return txErr(`ENTITY_JURISDICTION_CONFLICT: entity=${entity}`);
    }
    return ok(bound);
  });
};
/** og ConsensusConfig.jurisdiction beyond the account Domain, as the Entity state carries it. */
const jurisdictionConfigOf = (j: ImportJurisdiction): JurisdictionConfig => ({
  ...opt("name", j.name), entityProviderAddress: j.entityProviderAddress ?? "", ...opt("registrationBlock", j.registrationBlock),
  ...opt("entityProviderDeploymentBlock", j.entityProviderDeploymentBlock), ...opt("blockTimeMs", j.blockTimeMs), ...opt("rebalancePolicyUsd", j.rebalancePolicyUsd),
});

// ---- og runtime/j-submit/j-submit-{state,result}.ts, registration/entity-provider-action-submit-{state,result}.ts, governance-submit-state.ts ----
const ENTITY_J_SUBMIT_RETRY_MS = 60_000, SUBMIT_RESULT_FINGERPRINT_LIMIT = 256, SUBMIT_MAX_UINT256 = (1n << 256n) - 1n;
/** og normalizeSubmitId. */
const submitId = (v: unknown): string => String(v || "").trim().toLowerCase();
type SubmitAttempt = { readonly attemptId: string; readonly attemptNumber: number; readonly attemptedAt: number; readonly batchGeneration?: number | undefined; readonly generation?: number | undefined; readonly eligibleAt?: number | undefined };
type JTxRow = { readonly type: string; readonly entityId: string; readonly timestamp: number; readonly data: { readonly [field: string]: unknown; readonly runtimeSubmitAttempt?: SubmitAttempt | undefined } };
const rowOf = (b: Binary): JTxRow => b as unknown as JTxRow;
const binOf = (v: unknown): Binary => v as Binary;
/** keccak256(utf8(safeStringify(v))), lowercase. */
const stableHash = (v: unknown): string => `0x${keccakUtf8(stableJson(v))}`.toLowerCase();
const isEpActionJTx = (t: string): boolean => t === "entityProviderTransfer" || t === "entityProviderReleaseControlShares" || t === "entityProviderCancelAction";
const isGovernanceJTx = (t: string): boolean => t === "entityProviderProposeControlBoard";
type JSubmitIdentity = { readonly jurisdictionName: string; readonly entityId: string; readonly signerId: string; readonly entityNonce: number; readonly batchGeneration: number; readonly batchHash: string };
/** og buildJSubmitAttemptId: signer and jurisdiction scoped, generation separated. */
export const jSubmitAttemptId = (i: JSubmitIdentity & { readonly attemptNumber: number }): Result<string, RuntimeError> => {
  const jurisdictionName = submitId(i.jurisdictionName), entityId = submitId(i.entityId), signerId = submitId(i.signerId), batchHash = submitId(i.batchHash);
  if (!jurisdictionName) return txErr("J_SUBMIT_ATTEMPT_JURISDICTION_MISSING");
  if (!entityId) return txErr("J_SUBMIT_ATTEMPT_ENTITY_MISSING");
  if (!signerId) return txErr("J_SUBMIT_ATTEMPT_SIGNER_MISSING");
  if (!batchHash) return txErr("J_SUBMIT_ATTEMPT_BATCH_HASH_MISSING");
  if (!Number.isSafeInteger(i.entityNonce) || i.entityNonce < 0) return txErr(`J_SUBMIT_ATTEMPT_ENTITY_NONCE_INVALID:${i.entityNonce}`);
  if (!Number.isSafeInteger(i.batchGeneration) || i.batchGeneration <= 0) return txErr(`J_SUBMIT_ATTEMPT_BATCH_GENERATION_INVALID:${i.batchGeneration}`);
  if (!Number.isSafeInteger(i.attemptNumber) || i.attemptNumber <= 0) return txErr(`J_SUBMIT_ATTEMPT_NUMBER_INVALID:${i.attemptNumber}`);
  return ok(stableHash({ domain: "xln/j-submit-attempt/v1", jurisdictionName, entityId, signerId, entityNonce: i.entityNonce, batchGeneration: i.batchGeneration, batchHash, attemptNumber: i.attemptNumber }));
};
type EpIdentity = { readonly jurisdictionName: string; readonly entityId: string; readonly signerId: string; readonly actionHash: string; readonly actionNonce: bigint; readonly generation: number };
/** og buildEntityProviderActionAttemptId. */
export const epActionAttemptId = (i: EpIdentity & { readonly attemptNumber: number }): Result<string, RuntimeError> => {
  const jurisdictionName = submitId(i.jurisdictionName), entityId = submitId(i.entityId), signerId = submitId(i.signerId), actionHash = submitId(i.actionHash);
  if (!jurisdictionName) return txErr("ENTITY_PROVIDER_ACTION_ATTEMPT_JURISDICTION_MISSING");
  if (!entityId) return txErr("ENTITY_PROVIDER_ACTION_ATTEMPT_ENTITY_MISSING");
  if (!signerId) return txErr("ENTITY_PROVIDER_ACTION_ATTEMPT_SIGNER_MISSING");
  if (!/^0x[0-9a-f]{64}$/.test(actionHash)) return txErr(`ENTITY_PROVIDER_ACTION_ATTEMPT_HASH_INVALID:${actionHash || "missing"}`);
  if (i.actionNonce <= 0n || i.actionNonce > SUBMIT_MAX_UINT256) return txErr("ENTITY_PROVIDER_ACTION_ATTEMPT_NONCE_INVALID");
  if (!Number.isSafeInteger(i.generation) || i.generation <= 0) return txErr(`ENTITY_PROVIDER_ACTION_ATTEMPT_GENERATION_INVALID:${i.generation}`);
  if (!Number.isSafeInteger(i.attemptNumber) || i.attemptNumber <= 0) return txErr(`ENTITY_PROVIDER_ACTION_ATTEMPT_NUMBER_INVALID:${i.attemptNumber}`);
  return ok(stableHash({ domain: "xln/entity-provider-action-submit-attempt/v1", jurisdictionName, entityId, signerId, actionHash, actionNonce: i.actionNonce, generation: i.generation, attemptNumber: i.attemptNumber }));
};
/** og requireCanonicalEntityProviderActionAttempt. */
const canonicalEpAttempt = (jurisdictionName: string, jTx: JTxRow): Result<SubmitAttempt, RuntimeError> => {
  const attempt = jTx.data.runtimeSubmitAttempt, intent = jTx.data["intent"] as EntityProviderActionIntent;
  if (!attempt) return txErr("ENTITY_PROVIDER_ACTION_PENDING_ATTEMPT_METADATA_MISSING");
  const expectedKind = jTx.type === "entityProviderTransfer" ? "entityTransferTokens" : jTx.type === "entityProviderReleaseControlShares" ? "releaseControlShares" : "cancelPendingAction";
  if (intent.payload.kind !== expectedKind) return txErr(`ENTITY_PROVIDER_ACTION_JTX_KIND_MISMATCH:${jTx.type}:${intent.payload.kind}`);
  if (submitId(jTx.entityId) !== submitId(intent.entityId) || intent.actionHash.toLowerCase() !== entityProviderActionHash(intent) || attempt.generation !== intent.generation || attempt.attemptedAt < 0 || !Number.isSafeInteger(attempt.attemptedAt))
    return txErr("ENTITY_PROVIDER_ACTION_PENDING_ATTEMPT_INVALID");
  return chain(epActionAttemptId({ jurisdictionName, entityId: jTx.entityId, signerId: String(jTx.data["signerId"] ?? ""), actionHash: intent.actionHash, actionNonce: intent.actionNonce, generation: intent.generation, attemptNumber: attempt.attemptNumber }),
    (expected) => (attempt.attemptId === expected ? ok(attempt) : txErr(`ENTITY_PROVIDER_ACTION_PENDING_ATTEMPT_ID_MISMATCH:${attempt.attemptId}:${expected}`)));
};
/** og governancePayloadHash: the unsigned control-board proposal as submitted. */
const governancePayloadHash = (jTx: JTxRow): string => {
  const d = jTx.data;
  return stableHash({ type: jTx.type, entityId: jTx.entityId, data: { targetEntityId: d["targetEntityId"], newBoardHash: d["newBoardHash"], boardEpoch: d["boardEpoch"], actionNonce: d["actionNonce"], proposalHash: d["proposalHash"], supporterVotes: d["supporterVotes"], signerId: d["signerId"] }, timestamp: jTx.timestamp });
};
type GovernanceIdentity = { readonly jurisdictionName: string; readonly entityId: string; readonly signerId: string; readonly proposalHash: string; readonly payloadHash: string };
const governanceAttemptId = (i: GovernanceIdentity & { readonly attemptNumber: number }): Result<string, RuntimeError> => {
  const n = { domain: "xln/governance-j-submit-attempt/v1", jurisdictionName: submitId(i.jurisdictionName), entityId: submitId(i.entityId), signerId: submitId(i.signerId), proposalHash: submitId(i.proposalHash), payloadHash: submitId(i.payloadHash), attemptNumber: i.attemptNumber };
  if (!n.jurisdictionName) return txErr("GOVERNANCE_SUBMIT_JURISDICTION_MISSING");
  if (!n.entityId) return txErr("GOVERNANCE_SUBMIT_ENTITY_MISSING");
  if (!n.signerId) return txErr("GOVERNANCE_SUBMIT_SIGNER_MISSING");
  if (!/^0x[0-9a-f]{64}$/.test(n.proposalHash)) return txErr("GOVERNANCE_SUBMIT_PROPOSAL_HASH_INVALID");
  if (!/^0x[0-9a-f]{64}$/.test(n.payloadHash)) return txErr("GOVERNANCE_SUBMIT_PAYLOAD_HASH_INVALID");
  if (!Number.isSafeInteger(n.attemptNumber) || n.attemptNumber <= 0) return txErr("GOVERNANCE_SUBMIT_ATTEMPT_NUMBER_INVALID");
  return ok(stableHash(n));
};
const governanceIdentity = (jurisdictionName: string, jTx: JTxRow): Result<GovernanceIdentity, RuntimeError> => {
  const signerId = submitId(jTx.data["signerId"]);
  return signerId ? ok({ jurisdictionName, entityId: jTx.entityId, signerId, proposalHash: String(jTx.data["proposalHash"]), payloadHash: governancePayloadHash(jTx) }) : txErr("GOVERNANCE_SUBMIT_SIGNER_MISSING");
};
/** og requireCanonicalGovernanceAttempt. */
const canonicalGovernanceAttempt = (jurisdictionName: string, jTx: JTxRow): Result<SubmitAttempt, RuntimeError> => {
  const a = jTx.data.runtimeSubmitAttempt;
  if (!a) return txErr("GOVERNANCE_SUBMIT_ATTEMPT_MISSING");
  const eligibleAt = a.eligibleAt ?? Number.NaN;
  if (!Number.isSafeInteger(a.attemptedAt) || a.attemptedAt < 0 || !Number.isSafeInteger(eligibleAt) || eligibleAt < a.attemptedAt) return txErr("GOVERNANCE_SUBMIT_ATTEMPT_TIME_INVALID");
  return chain(governanceIdentity(jurisdictionName, jTx), (identity) => chain(governanceAttemptId({ ...identity, attemptNumber: a.attemptNumber }),
    (expected) => (a.attemptId === expected ? ok(a) : txErr(`GOVERNANCE_SUBMIT_ATTEMPT_ID_MISMATCH:${a.attemptId}:${expected}`))));
};
/** og materializeInitialGovernanceAttempt: attempt one, eligible at the proposal's own timestamp. */
const initialGovernanceAttempt = (jurisdictionName: string, jTx: JTxRow): Result<Binary, RuntimeError> => {
  if (jTx.data.runtimeSubmitAttempt) return map(canonicalGovernanceAttempt(jurisdictionName, jTx), () => binOf(jTx));
  return chain(governanceIdentity(jurisdictionName, jTx), (identity) => map(governanceAttemptId({ ...identity, attemptNumber: 1 }), (attemptId) =>
    binOf({ ...jTx, data: { ...jTx.data, runtimeSubmitAttempt: { attemptId, attemptNumber: 1, attemptedAt: jTx.timestamp, eligibleAt: jTx.timestamp } } })));
};
/** og requireCanonicalPendingAttempt. */
const canonicalPendingAttempt = (jurisdictionName: string, jTx: JTxRow): Result<SubmitAttempt, RuntimeError> => {
  const attempt = jTx.type === "batch" || isEpActionJTx(jTx.type) || isGovernanceJTx(jTx.type) ? jTx.data?.runtimeSubmitAttempt : undefined;
  if (!attempt) return txErr("J_SUBMIT_PENDING_ATTEMPT_METADATA_MISSING");
  if (isEpActionJTx(jTx.type)) return canonicalEpAttempt(jurisdictionName, jTx);
  if (isGovernanceJTx(jTx.type)) return canonicalGovernanceAttempt(jurisdictionName, jTx);
  return chain(jSubmitAttemptId({ jurisdictionName, entityId: jTx.entityId, signerId: submitId(jTx.data["signerId"]), entityNonce: Number(jTx.data["entityNonce"]), batchGeneration: attempt.batchGeneration ?? Number.NaN, batchHash: String(jTx.data["batchHash"] || ""), attemptNumber: attempt.attemptNumber }), (expected) => {
    if (jTx.data["batchGeneration"] !== attempt.batchGeneration) return txErr(`J_SUBMIT_PENDING_BATCH_GENERATION_MISMATCH:${String(jTx.data["batchGeneration"])}:${attempt.batchGeneration}`);
    return attempt.attemptId === expected ? ok(attempt) : txErr(`J_SUBMIT_PENDING_ATTEMPT_ID_MISMATCH:${attempt.attemptId}:${expected}`);
  });
};
const attemptFingerprint = (jurisdictionName: string, jTx: Binary): string => stableJson({ jurisdictionName, jTx });
/** og registerPendingCommittedJOutbox: canonical attempts only, one per attempt id, an exact repeat dropped, a different payload refused. */
export const registerPendingJOutbox = (pending: readonly JInput[], additions: readonly JInput[]): Result<readonly JInput[], RuntimeError> => {
  if (additions.length === 0) return ok(pending);
  const known = new Map<string, string>();
  for (const input of pending) for (const jTx of input.jTxs) {
    const attempt = canonicalPendingAttempt(input.jurisdictionName, rowOf(jTx));
    if (!attempt.ok) return attempt;
    const fingerprint = attemptFingerprint(input.jurisdictionName, jTx), previous = known.get(attempt.value.attemptId);
    if (previous !== undefined) return txErr(`${previous !== fingerprint ? "J_SUBMIT_PENDING_ATTEMPT_CONFLICT" : "J_SUBMIT_PENDING_ATTEMPT_DUPLICATED"}:${attempt.value.attemptId}`);
    known.set(attempt.value.attemptId, fingerprint);
  }
  const accepted: JInput[] = [];
  for (const input of additions) {
    const jTxs: Binary[] = [];
    for (const jTx of input.jTxs) {
      const attempt = canonicalPendingAttempt(input.jurisdictionName, rowOf(jTx));
      if (!attempt.ok) return attempt;
      const fingerprint = attemptFingerprint(input.jurisdictionName, jTx), previous = known.get(attempt.value.attemptId);
      if (previous !== undefined) { if (previous !== fingerprint) return txErr(`J_SUBMIT_PENDING_ATTEMPT_CONFLICT:${attempt.value.attemptId}`); continue; }
      known.set(attempt.value.attemptId, fingerprint);
      jTxs.push(jTx);
    }
    if (jTxs.length > 0) accepted.push({ jurisdictionName: input.jurisdictionName, jTxs });
  }
  return ok([...pending, ...accepted]);
};
export type JOutboxSplit = { readonly maintenance: readonly JInput[]; readonly durable: readonly JInput[]; readonly retries: readonly RuntimeTx[] };
/**
 * og splitJOutboxForDurableSubmit: governance proposals become durable attempt one; an EntityProvider action or batch with an attempt is durable,
 * without one it becomes a local retry RuntimeTx for the next frame; mint / debt enforcement / board activation are maintenance.
 */
export const splitJOutbox = (jOutbox: readonly JInput[]): Result<JOutboxSplit, RuntimeError> => {
  const maintenance: JInput[] = [], durable: JInput[] = [], retries: RuntimeTx[] = [];
  for (const input of jOutbox) {
    const keep: Binary[] = [], hold: Binary[] = [];
    for (const raw of input.jTxs) {
      const jTx = rowOf(raw);
      if (isGovernanceJTx(jTx.type)) {
        const materialized = initialGovernanceAttempt(input.jurisdictionName, jTx);
        if (!materialized.ok) return materialized;
        hold.push(materialized.value);
      } else if (isEpActionJTx(jTx.type)) {
        if (jTx.data.runtimeSubmitAttempt) {
          const canonical = canonicalEpAttempt(input.jurisdictionName, jTx);
          if (!canonical.ok) return canonical;
          hold.push(raw);
        } else {
          const signer = submitId(jTx.data["signerId"]), intent = jTx.data["intent"] as EntityProviderActionIntent;
          if (!signer) return txErr(`ENTITY_PROVIDER_ACTION_SUBMITTER_MISSING:${jTx.entityId}`);
          if (!jTx.data["hankoSignature"]) return txErr(`ENTITY_PROVIDER_ACTION_CONSENSUS_HANKO_MISSING:${jTx.entityId}`);
          retries.push({ type: "retryEntityProviderAction", data: { entityId: jTx.entityId, signerId: signer, jurisdictionName: input.jurisdictionName, actionHash: intent.actionHash, actionNonce: intent.actionNonce, generation: intent.generation } });
        }
      } else if (jTx.type === "mint" || jTx.type === "debtEnforcement" || jTx.type === "entityProviderActivateBoard") {
        keep.push(raw);
      } else if (jTx.data?.runtimeSubmitAttempt) {
        hold.push(raw);
      } else {
        const signer = submitId(jTx.data?.["signerId"]), generation = Number(jTx.data?.["batchGeneration"]), fee = jTx.data?.["feeOverrides"] as { readonly [k: string]: Binary } | undefined;
        if (!signer) return txErr(`J_SUBMIT_INTENT_SIGNER_MISSING:${jTx.entityId}`);
        if (!Number.isSafeInteger(generation) || generation <= 0) return txErr(`J_SUBMIT_INTENT_BATCH_GENERATION_INVALID:${String(jTx.data["batchGeneration"])}`);
        retries.push({ type: "retryJSubmit", data: { entityId: jTx.entityId, signerId: signer, jurisdictionName: input.jurisdictionName, batchHash: String(jTx.data["batchHash"] || ""), entityNonce: Number(jTx.data["entityNonce"]), batchGeneration: generation, ...(fee ? { feeOverrides: { ...fee } } : {}) } });
      }
    }
    if (keep.length > 0) maintenance.push({ jurisdictionName: input.jurisdictionName, jTxs: keep });
    if (hold.length > 0) durable.push({ jurisdictionName: input.jurisdictionName, jTxs: hold });
  }
  return ok({ maintenance, durable, retries });
};
type TxStep = { readonly runtime: Runtime; readonly jOutputs: readonly JInput[] };
const noJ = (runtime: Runtime): TxStep => ({ runtime, jOutputs: [] });
/** og findJSubmitReplica / findEntityProviderActionReplica: the replica of exactly this Entity and signer. */
const findSubmitReplica = (rt: Runtime, entityId: unknown, signerId: unknown): readonly [string, EntityReplica] | undefined => {
  const entity = submitId(entityId), signer = submitId(signerId);
  return [...rt.entities].find(([, r]) => submitId(r.state.id) === entity && submitId(r.signerId) === signer);
};
const localOf = (rt: Runtime, key: string): ReplicaLocal => rt.replicaLocal.get(key) ?? {};
const withLocal = (rt: Runtime, key: string, patch: ReplicaLocal): Runtime => ({ ...rt, replicaLocal: mapSet(rt.replicaLocal, key, { ...localOf(rt, key), ...patch }) });
const jBatchOf = (state: EntityState): JBatchState | undefined => state.committed["jBatchState"] as JBatchState | undefined;
const sentMatches = (sent: SentJBatch | undefined, batchHash: unknown, entityNonce: unknown): sent is SentJBatch =>
  sent !== undefined && submitId(sent.batchHash) === submitId(batchHash) && Number(sent.entityNonce) === Number(entityNonce);
/** og getMatchingJSubmitState. */
const matchingJSubmitState = (state: EntityState, local: JSubmitState | undefined): JSubmitState | undefined => {
  const jb = jBatchOf(state);
  return local !== undefined && sentMatches(jb?.sentBatch, local.batchHash, local.entityNonce) && local.batchGeneration === jb?.broadcastCount ? local : undefined;
};
const journalOf = (p: SubmitJournal | undefined): Partial<SubmitJournal> => (p === undefined ? {} : {
  ...opt("txHash", p.txHash || undefined), ...opt("lastFailure", p.lastFailure), ...opt("lastResultAttemptId", p.lastResultAttemptId || undefined), ...opt("lastResultAt", p.lastResultAt),
  ...opt("lastResultOutcome", p.lastResultOutcome || undefined), ...opt("lastResultFingerprint", p.lastResultFingerprint || undefined),
});
/** og applyRetryJSubmitRuntimeTx: the active leader re-attempts its exact sealed batch at most once per retry window; stale hints are no-ops. */
const retryJSubmit = (rt: Runtime, d: Extract<RuntimeTx, { type: "retryJSubmit" }>["data"]): Result<TxStep, RuntimeError> => {
  const found = findSubmitReplica(rt, d.entityId, d.signerId);
  if (found === undefined) return txErr(`J_SUBMIT_LOCAL_REPLICA_MISSING:${d.entityId}:${d.signerId}`);
  const [key, r] = found, jb = jBatchOf(r.state), sent = jb?.sentBatch, local = localOf(rt, key), now = Number(rt.timestamp);
  if (leaderStateOf(r.state).activeValidatorId !== submitId(r.signerId)) return txErr(`J_SUBMIT_NOT_ACTIVE_LEADER:${d.signerId}`);
  if (!sentMatches(sent, d.batchHash, d.entityNonce) || d.batchGeneration !== jb?.broadcastCount || sent.terminalFailure) return ok(noJ(rt));
  const entityId = lower(r.state.id), signer = lower(r.signerId), previous = matchingJSubmitState(r.state, local.jSubmitState);
  const identity: JSubmitIdentity = { jurisdictionName: d.jurisdictionName, entityId, signerId: signer, batchHash: sent.batchHash, entityNonce: sent.entityNonce, batchGeneration: d.batchGeneration };
  const pending = rt.pendingCommittedJOutbox.some((input) => input.jTxs.some((raw) => {
    const t = rowOf(raw);
    return t.type === "batch" && submitId(input.jurisdictionName) === submitId(identity.jurisdictionName) && submitId(t.entityId) === submitId(identity.entityId) && submitId(t.data["signerId"]) === submitId(identity.signerId)
      && submitId(t.data["batchHash"]) === submitId(identity.batchHash) && Number(t.data["entityNonce"]) === Number(identity.entityNonce) && Number(t.data["batchGeneration"]) === Number(identity.batchGeneration);
  }));
  if (pending || previous?.terminalFailure || previous?.lastResultOutcome === "reconciled") return ok(noJ(rt));
  if (previous && previous.lastResultOutcome !== "eventBarrier" && previous.submitAttempts > 0 && now < previous.lastSubmittedAt + ENTITY_J_SUBMIT_RETRY_MS) return ok(noJ(rt));
  const witness = local.hankoWitness?.get(sent.batchHash);
  if (witness === undefined || witness.type !== "jBatch") return txErr(`J_SUBMIT_HANKO_WITNESS_MISSING:${d.entityId}:${sent.batchHash}`);
  const attemptNumber = (previous?.submitAttempts ?? 0) + 1;
  return map(jSubmitAttemptId({ ...identity, attemptNumber }), (attemptId): TxStep => {
    const recorded = local.jSubmitState;
    const next: JSubmitState = {
      jurisdictionName: d.jurisdictionName, batchHash: sent.batchHash, entityNonce: sent.entityNonce, batchGeneration: d.batchGeneration, submitAttempts: attemptNumber, lastSubmittedAt: now,
      ...journalOf(previous), ...opt("resultFingerprints", recorded?.resultFingerprints), ...opt("resultFingerprintOrder", recorded?.resultFingerprintOrder),
    };
    const batchTx = {
      type: "batch", entityId, data: {
        batch: sent.batch, batchHash: sent.batchHash, encodedBatch: sent.encodedBatch, entityNonce: sent.entityNonce, batchGeneration: d.batchGeneration, hankoSignature: witness.hanko,
        batchSize: batchOpCount(sent.batch), signerId: signer, ...(d.feeOverrides ? { feeOverrides: { ...(d.feeOverrides as object) } } : {}),
        runtimeSubmitAttempt: { attemptId, attemptNumber, attemptedAt: now, batchGeneration: d.batchGeneration },
      }, timestamp: now,
    };
    return { runtime: withLocal(rt, key, { jSubmitState: next }), jOutputs: [{ jurisdictionName: d.jurisdictionName, jTxs: [binOf(batchTx)] }] };
  });
};
/** og normalizeRuntimeFailureCode + classifyRuntimeJBatchFailure. */
const J_BATCH_FAILURE_CATEGORIES: { readonly [code: string]: RuntimeFailureSignal["category"] } = {
  J_BATCH_EMPTY: "ExpectedEmpty", J_BATCH_SENT_PENDING: "TransientRace", J_BATCH_JURISDICTION_MISSING: "Contradiction", J_BATCH_JURISDICTION_UNAVAILABLE: "TransientRace",
  J_BATCH_CHAIN_ID_MISSING: "Contradiction", J_BATCH_SIGNER_MISSING: "Contradiction", J_BATCH_LIMIT_EXCEEDED: "Contradiction", J_BATCH_CONSENSUS_HANKO_MISSING: "Contradiction",
  J_SUBMIT_MISSING_JREPLICA: "TransientRace", J_SUBMIT_MISSING_JADAPTER: "TransientRace", J_SUBMIT_TRANSIENT: "TransientRace", J_SUBMIT_FATAL: "Contradiction",
};
const failureCode = (v: unknown): string => (String(v || "").trim().split(/[\s:]/)[0] || "UNKNOWN").replace(/[^A-Z0-9_]/gi, "_").toUpperCase();
export const classifyJBatchFailure = (code: string, message?: string): RuntimeFailureSignal => {
  const c = failureCode(code), category = J_BATCH_FAILURE_CATEGORIES[c] ?? "Contradiction";
  return { category, code: c, message: String((message ?? c) || c).trim() || c, retryable: category === "TransientRace", fatal: category === "Contradiction" };
};
type ResultShape = SubmitResultTail & { readonly outcome: string };
/** og assertValidAdapterFailure (both families, by code prefix). */
const adapterFailureValid = (d: ResultShape, prefix: string): Result<void, RuntimeError> => {
  const f = d.adapterFailure;
  if (!f) return ok(undefined);
  if (f.category !== "transient" && f.category !== "terminal") return txErr(`${prefix}_ADAPTER_FAILURE_CATEGORY_INVALID:${String(f.category)}`);
  if (!String(f.code ?? "").trim()) return txErr(`${prefix}_ADAPTER_FAILURE_CODE_MISSING`);
  if (!String(f.message ?? "").trim()) return txErr(`${prefix}_ADAPTER_FAILURE_MESSAGE_MISSING`);
  if (f.message !== d.message) return txErr(`${prefix}_ADAPTER_FAILURE_MESSAGE_MISMATCH`);
  const expected = f.category === "transient" ? "transientFailure" : "terminalFailure";
  return d.outcome === expected ? ok(undefined) : txErr(`${prefix}_ADAPTER_FAILURE_OUTCOME_MISMATCH:${f.category}:${d.outcome}`);
};
/** og findRecordedResultFingerprint / findRecordedFingerprint: one fingerprint per attempt id across every replica journal. */
const recordedFingerprint = (rt: Runtime, attemptId: string, pick: (l: ReplicaLocal) => SubmitJournal | undefined, prefix: string): Result<string | null, RuntimeError> => {
  let found: string | null = null;
  for (const key of rt.entities.keys()) {
    const local = pick(localOf(rt, key));
    if (local === undefined) continue;
    const journal = Object.prototype.hasOwnProperty.call(local.resultFingerprints ?? {}, attemptId) ? local.resultFingerprints?.[attemptId] : undefined;
    const last = local.lastResultAttemptId === attemptId ? local.lastResultFingerprint : undefined;
    if (local.lastResultAttemptId === attemptId && !last) return txErr(`${prefix}_RESULT_FINGERPRINT_MISSING:${attemptId}`);
    if (journal !== undefined && last !== undefined && journal !== last) return txErr(`${prefix}_RESULT_RECORDED_CONFLICT:${attemptId}`);
    const fingerprint = journal ?? last;
    if (fingerprint === undefined) continue;
    if (found !== null && found !== fingerprint) return txErr(`${prefix}_RESULT_RECORDED_CONFLICT:${attemptId}`);
    found = fingerprint;
  }
  return ok(found);
};
const dropPendingAttempt = (pending: readonly JInput[], match: (t: JTxRow) => boolean): readonly JInput[] =>
  pending.flatMap((input) => { const jTxs = input.jTxs.filter((raw) => !match(rowOf(raw))); return jTxs.length > 0 ? [{ jurisdictionName: input.jurisdictionName, jTxs }] : []; });
/** og buildBoundedResultJournal / buildResultJournal: the newest fingerprints plus every still-pending attempt of this replica, at most 256. */
const boundedJournal = (local: SubmitJournal, attemptId: string, fingerprint: string, active: Set<string>, jSubmit: boolean): Result<Pick<SubmitJournal, "resultFingerprints" | "resultFingerprintOrder">, RuntimeError> => {
  const existing = local.resultFingerprints ?? {}, order = local.resultFingerprintOrder ?? Object.keys(existing), prefix = jSubmit ? "J_SUBMIT" : "ENTITY_PROVIDER_ACTION";
  if (jSubmit) {
    const seen = new Set<string>();
    for (const id of order) {
      if (seen.has(id)) return txErr(`J_SUBMIT_RESULT_JOURNAL_ORDER_DUPLICATE:${id}`);
      if (!Object.prototype.hasOwnProperty.call(existing, id)) return txErr(`J_SUBMIT_RESULT_JOURNAL_ORDER_UNKNOWN:${id}`);
      seen.add(id);
    }
    if (seen.size !== Object.keys(existing).length) return txErr("J_SUBMIT_RESULT_JOURNAL_ORDER_INCOMPLETE");
  } else {
    if (new Set(order).size !== order.length) return txErr("ENTITY_PROVIDER_ACTION_RESULT_JOURNAL_ORDER_DUPLICATE");
    if (order.some((id) => existing[id] === undefined) || order.length !== Object.keys(existing).length) return txErr("ENTITY_PROVIDER_ACTION_RESULT_JOURNAL_ORDER_INVALID");
  }
  const nextOrder = [...order.filter((id) => id !== attemptId), attemptId];
  if (active.size > SUBMIT_RESULT_FINGERPRINT_LIMIT) return txErr(`${prefix}_ACTIVE_ATTEMPT_CAPACITY_EXCEEDED:${active.size}`);
  const retained = new Set(active);
  for (let i = nextOrder.length - 1; i >= 0 && retained.size < SUBMIT_RESULT_FINGERPRINT_LIMIT; i -= 1) if (nextOrder[i]) retained.add(nextOrder[i] as string);
  const resultFingerprintOrder = nextOrder.filter((id) => retained.has(id)), all: { readonly [id: string]: string } = { ...existing, [attemptId]: fingerprint };
  return ok({ resultFingerprintOrder, resultFingerprints: Object.fromEntries(resultFingerprintOrder.map((id) => [id, all[id] as string])) });
};
const activeAttempts = (rt: Runtime, r: EntityReplica, isFamily: (t: string) => boolean): Set<string> => new Set(rt.pendingCommittedJOutbox.flatMap((input) => input.jTxs.flatMap((raw) => {
  const t = rowOf(raw);
  return isFamily(t.type) && submitId(t.entityId) === submitId(r.state.id) && submitId(t.data["signerId"]) === submitId(r.signerId) && t.data.runtimeSubmitAttempt ? [t.data.runtimeSubmitAttempt.attemptId] : [];
})));
const withoutKeys = <T extends object>(o: T, keys: readonly string[]): T => Object.fromEntries(Object.entries(o).filter(([k]) => !keys.includes(k))) as T;
/** og applyRecordJSubmitResultRuntimeTx: validate, idempotent by fingerprint, retire a stale attempt, else journal the result for the live attempt. */
const recordJSubmitResult = (rt: Runtime, d: JSubmitResultData): Result<Runtime, RuntimeError> => {
  if (!submitId(d.entityId)) return txErr("J_SUBMIT_RESULT_ENTITY_MISSING");
  if (!submitId(d.signerId)) return txErr("J_SUBMIT_RESULT_SIGNER_MISSING");
  if (!String(d.jurisdictionName || "").trim()) return txErr("J_SUBMIT_RESULT_JURISDICTION_MISSING");
  if (!submitId(d.batchHash)) return txErr("J_SUBMIT_RESULT_BATCH_HASH_MISSING");
  if (!String(d.attemptId || "").trim()) return txErr("J_SUBMIT_RESULT_ATTEMPT_ID_MISSING");
  if (!Number.isSafeInteger(d.entityNonce) || d.entityNonce < 0) return txErr(`J_SUBMIT_RESULT_ENTITY_NONCE_INVALID:${d.entityNonce}`);
  if (!Number.isSafeInteger(d.batchGeneration) || d.batchGeneration <= 0) return txErr(`J_SUBMIT_RESULT_BATCH_GENERATION_INVALID:${d.batchGeneration}`);
  if (!Number.isSafeInteger(d.attemptNumber) || d.attemptNumber <= 0) return txErr(`J_SUBMIT_RESULT_ATTEMPT_NUMBER_INVALID:${d.attemptNumber}`);
  if (!Number.isSafeInteger(d.attemptedAt) || d.attemptedAt < 0) return txErr(`J_SUBMIT_RESULT_ATTEMPT_TIMESTAMP_INVALID:${d.attemptedAt}`);
  return chain(jSubmitAttemptId(d), (expected): Result<Runtime, RuntimeError> => {
    if (d.attemptId !== expected) return txErr(`J_SUBMIT_RESULT_ATTEMPT_ID_MISMATCH:${d.attemptId}:${expected}`);
    if (!["submitted", "eventBarrier", "transientFailure", "terminalFailure", "reconciled"].includes(d.outcome)) return txErr(`J_SUBMIT_RESULT_OUTCOME_INVALID:${String(d.outcome)}`);
    return chain(adapterFailureValid(d, "J_SUBMIT"), () => {
      const fingerprint = stableJson(d), isAttempt = (t: JTxRow): boolean => t.type === "batch" && t.data?.runtimeSubmitAttempt?.attemptId === d.attemptId;
      const matches = rt.pendingCommittedJOutbox.flatMap((input) => input.jTxs.filter((raw) => isAttempt(rowOf(raw))).map((raw) => ({ jurisdictionName: input.jurisdictionName, jTx: rowOf(raw) })));
      if (matches.length > 1) return txErr(`J_SUBMIT_PENDING_ATTEMPT_DUPLICATED:${d.attemptId}`);
      const pending = matches[0], a = pending?.jTx.data.runtimeSubmitAttempt;
      if (pending !== undefined && !(a && submitId(pending.jTx.entityId) === submitId(d.entityId) && submitId(pending.jTx.data["signerId"]) === submitId(d.signerId) && pending.jurisdictionName === d.jurisdictionName
        && submitId(pending.jTx.data["batchHash"]) === submitId(d.batchHash) && Number(pending.jTx.data["entityNonce"]) === Number(d.entityNonce) && Number(pending.jTx.data["batchGeneration"]) === d.batchGeneration
        && a.attemptId === d.attemptId && a.attemptNumber === d.attemptNumber && a.attemptedAt === d.attemptedAt)) return txErr(`J_SUBMIT_RESULT_PENDING_CONFLICT:${d.attemptId}`);
      return chain(recordedFingerprint(rt, d.attemptId, (l) => l.jSubmitState, "J_SUBMIT"), (recorded): Result<Runtime, RuntimeError> => {
        if (recorded !== null) return recorded === fingerprint ? ok(rt) : txErr(`J_SUBMIT_RESULT_DUPLICATE_CONFLICT:${d.attemptId}`);
        const found = findSubmitReplica(rt, d.entityId, d.signerId);
        if (found === undefined) return txErr(`J_SUBMIT_LOCAL_REPLICA_MISSING:${d.entityId}:${d.signerId}`);
        const [key, r] = found, local = localOf(rt, key).jSubmitState, jb = jBatchOf(r.state);
        const matchesSent = sentMatches(jb?.sentBatch, d.batchHash, d.entityNonce) && jb?.broadcastCount === d.batchGeneration;
        const matchesLocal = local !== undefined && submitId(local.batchHash) === submitId(d.batchHash) && local.entityNonce === d.entityNonce && local.batchGeneration === d.batchGeneration;
        const retired = (): Runtime => (pending === undefined ? rt : { ...rt, pendingCommittedJOutbox: dropPendingAttempt(rt.pendingCommittedJOutbox, isAttempt) });
        if (!matchesSent || (matchesLocal && d.attemptNumber < local.submitAttempts)) return ok(retired());
        if (local === undefined || !matchesLocal || local.submitAttempts !== d.attemptNumber || local.lastSubmittedAt !== d.attemptedAt) return txErr(`J_SUBMIT_RESULT_ATTEMPT_MISMATCH:${d.attemptId}`);
        if (pending === undefined) return txErr(`J_SUBMIT_PENDING_ATTEMPT_MISSING:${d.attemptId}`);
        return map(boundedJournal(local, d.attemptId, fingerprint, activeAttempts(rt, r, (t) => t === "batch"), true), (journal) => {
          const now = Number(rt.timestamp), base: JSubmitState = { ...local, lastResultAttemptId: d.attemptId, lastResultAt: now, lastResultOutcome: d.outcome, lastResultFingerprint: fingerprint, ...journal };
          let next: JSubmitState = base;
          if (d.outcome === "submitted") next = { ...withoutKeys(base, ["lastFailure"]), ...(d.txHash ? { txHash: d.txHash } : {}) };
          else if (d.outcome === "eventBarrier") next = withoutKeys(base, ["lastFailure"]);
          else if (d.outcome === "transientFailure" || d.outcome === "terminalFailure") {
            const message = String(d.message || "unknown"), code = d.outcome === "transientFailure" ? "J_SUBMIT_TRANSIENT" : "J_SUBMIT_FATAL";
            const failure: SubmitFailure = { message, failedAt: now, failure: classifyJBatchFailure(code, message), ...opt("adapterFailure", d.adapterFailure) };
            next = { ...base, lastFailure: failure, ...(d.outcome === "terminalFailure" ? { terminalFailure: failure } : {}) };
          }
          return withLocal({ ...rt, pendingCommittedJOutbox: dropPendingAttempt(rt.pendingCommittedJOutbox, isAttempt) }, key, { jSubmitState: next });
        });
      });
    });
  });
};
/** og requireRuntimeJurisdictionConfigByName over the Entity's own committed jurisdiction. */
const entityJurisdiction = (rt: Runtime, state: EntityState, missing: string): Result<ImportJurisdiction, RuntimeError> => {
  const j = state.jurisdictionConfig, name = (j?.name ?? "").trim();
  if (j === undefined || !name) return txErr(missing);
  return requireJurisdictionByName(rt, name, {
    name, chainId: state.jurisdiction.chainId, depositoryAddress: state.jurisdiction.depositoryAddress, entityProviderAddress: j.entityProviderAddress, ...opt("registrationBlock", j.registrationBlock),
    ...opt("entityProviderDeploymentBlock", j.entityProviderDeploymentBlock), ...opt("blockTimeMs", j.blockTimeMs), ...opt("rebalancePolicyUsd", j.rebalancePolicyUsd),
  });
};
/** og requireUsableContractAddress. */
const usableOrFail = (label: string, v: unknown): Result<string, RuntimeError> => (usableAddress(v) === null ? txErr(`INVALID_${label.toUpperCase()}_ADDRESS`) : ok(String(v)));
/** og requireTrustedPending: the committed pending intent, re-checked against the J replica's stack and the current certified board epoch. */
const trustedEpPending = (rt: Runtime, r: EntityReplica): Result<{ readonly pending: EntityProviderActionIntent; readonly jurisdictionName: string }, RuntimeError> => {
  const pending = epActionState(r.state).pending, entityId = lower(r.state.id);
  if (pending === undefined) return txErr(`ENTITY_PROVIDER_ACTION_PENDING_MISSING:${entityId}`);
  return chain(entityJurisdiction(rt, r.state, "ENTITY_PROVIDER_ACTION_JURISDICTION_MISSING"), (j) => {
    const chainId = Number(j.chainId);
    if (!Number.isSafeInteger(chainId) || chainId <= 0) return txErr(`ENTITY_PROVIDER_ACTION_CHAIN_ID_INVALID:${String(j.chainId)}`);
    return chain(observerBoardRecord(r.state, entityId), (board): Result<{ readonly pending: EntityProviderActionIntent; readonly jurisdictionName: string }, RuntimeError> => {
      if (board === null) return txErr(`ENTITY_PROVIDER_ACTION_CERTIFIED_BOARD_MISSING:${entityId}`);
      if (pending.boardEpoch !== BigInt(board.boardEpoch)) return txErr(`ENTITY_PROVIDER_ACTION_PENDING_BOARD_EPOCH_STALE:${pending.boardEpoch.toString()}:${board.boardEpoch}`);
      return chain(usableOrFail("entity_provider", j.entityProviderAddress), (provider) => chain(usableOrFail("depository", j.depositoryAddress), (depository) =>
        map(epIntentValid(pending, { name: j.name ?? "", chainId: BigInt(chainId), entityProviderAddress: lower(provider), depositoryAddress: lower(depository) }, entityId, BigInt(board.boardEpoch)), () => ({ pending, jurisdictionName: j.name ?? "" }))));
    });
  });
};
/** og getMatchingEntityProviderActionSubmitState. */
const matchingEpSubmitState = (state: EntityState, local: EntityProviderActionSubmitState | undefined): EntityProviderActionSubmitState | undefined => {
  const pending = epActionState(state).pending;
  return pending !== undefined && local !== undefined && submitId(local.actionHash) === submitId(pending.actionHash) && local.actionNonce === pending.actionNonce && local.generation === pending.generation ? local : undefined;
};
/** og applyRetryEntityProviderActionRuntimeTx: the active leader submits the committed pending action with its quorum Hanko, once per retry window. */
const retryEntityProviderAction = (rt: Runtime, d: Extract<RuntimeTx, { type: "retryEntityProviderAction" }>["data"]): Result<TxStep, RuntimeError> => {
  const found = findSubmitReplica(rt, d.entityId, d.signerId);
  if (found === undefined) return txErr(`ENTITY_PROVIDER_ACTION_LOCAL_REPLICA_MISSING:${d.entityId}:${d.signerId}`);
  const [key, r] = found, local = localOf(rt, key), now = Number(rt.timestamp);
  if (leaderStateOf(r.state).activeValidatorId !== submitId(r.signerId)) return txErr(`ENTITY_PROVIDER_ACTION_NOT_ACTIVE_LEADER:${d.signerId}`);
  return chain(trustedEpPending(rt, r), ({ pending, jurisdictionName }): Result<TxStep, RuntimeError> => {
    if (submitId(jurisdictionName) !== submitId(d.jurisdictionName) || submitId(pending.actionHash) !== submitId(d.actionHash) || pending.actionNonce !== d.actionNonce || pending.generation !== d.generation)
      return txErr(`ENTITY_PROVIDER_ACTION_COMMITTED_INTENT_MISMATCH:${d.entityId}`);
    const entityId = lower(r.state.id), signer = lower(r.signerId);
    const identity: EpIdentity = { jurisdictionName, entityId, signerId: signer, actionHash: pending.actionHash, actionNonce: pending.actionNonce, generation: pending.generation };
    const held = rt.pendingCommittedJOutbox.some((input) => input.jTxs.some((raw) => {
      const t = rowOf(raw), intent = t.data?.["intent"] as EntityProviderActionIntent | undefined;
      return isEpActionJTx(t.type) && intent !== undefined && submitId(input.jurisdictionName) === submitId(identity.jurisdictionName) && submitId(t.entityId) === submitId(identity.entityId)
        && submitId(t.data["signerId"]) === submitId(identity.signerId) && submitId(intent.actionHash) === submitId(identity.actionHash) && intent.actionNonce === identity.actionNonce && intent.generation === identity.generation;
    }));
    if (held) return ok(noJ(rt));
    const previous = matchingEpSubmitState(r.state, local.entityProviderActionSubmitState);
    if (previous?.terminalFailure) return ok(noJ(rt));
    if (previous && previous.submitAttempts > 0 && now < previous.lastSubmittedAt + ENTITY_J_SUBMIT_RETRY_MS) return ok(noJ(rt));
    const witness = local.hankoWitness?.get(pending.actionHash);
    if (witness === undefined || witness.type !== "entityProviderAction") return txErr(`ENTITY_PROVIDER_ACTION_HANKO_WITNESS_MISSING:${entityId}:${pending.actionHash}`);
    const attemptNumber = (previous?.submitAttempts ?? 0) + 1;
    return map(epActionAttemptId({ ...identity, attemptNumber }), (attemptId): TxStep => {
      const next: EntityProviderActionSubmitState = {
        jurisdictionName, actionHash: pending.actionHash, actionNonce: pending.actionNonce, generation: pending.generation, submitAttempts: attemptNumber, lastSubmittedAt: now,
        ...journalOf(previous), ...opt("terminalFailure", previous?.terminalFailure), ...opt("resultFingerprints", previous?.resultFingerprints), ...opt("resultFingerprintOrder", previous?.resultFingerprintOrder),
      };
      const type = pending.payload.kind === "entityTransferTokens" ? "entityProviderTransfer" : pending.payload.kind === "releaseControlShares" ? "entityProviderReleaseControlShares" : "entityProviderCancelAction";
      const jTx = { type, entityId, data: { intent: pending, signerId: signer, hankoSignature: witness.hanko, runtimeSubmitAttempt: { attemptId, attemptNumber, attemptedAt: now, generation: pending.generation } }, timestamp: now };
      return { runtime: withLocal(rt, key, { entityProviderActionSubmitState: next }), jOutputs: [{ jurisdictionName, jTxs: [binOf(jTx)] }] };
    });
  });
};
/** og applyRecordEntityProviderActionResultRuntimeTx. */
const recordEpActionResult = (rt: Runtime, d: EpActionResultData): Result<Runtime, RuntimeError> => {
  if (!submitId(d.entityId)) return txErr("ENTITY_PROVIDER_ACTION_RESULT_ENTITY_MISSING");
  if (!submitId(d.signerId)) return txErr("ENTITY_PROVIDER_ACTION_RESULT_SIGNER_MISSING");
  if (!submitId(d.jurisdictionName)) return txErr("ENTITY_PROVIDER_ACTION_RESULT_JURISDICTION_MISSING");
  if (!/^0x[0-9a-f]{64}$/.test(submitId(d.actionHash))) return txErr("ENTITY_PROVIDER_ACTION_RESULT_HASH_INVALID");
  if (d.actionNonce <= 0n || d.actionNonce > SUBMIT_MAX_UINT256) return txErr("ENTITY_PROVIDER_ACTION_RESULT_NONCE_INVALID");
  if (!Number.isSafeInteger(d.generation) || d.generation <= 0) return txErr(`ENTITY_PROVIDER_ACTION_RESULT_GENERATION_INVALID:${d.generation}`);
  if (!Number.isSafeInteger(d.attemptNumber) || d.attemptNumber <= 0) return txErr(`ENTITY_PROVIDER_ACTION_RESULT_ATTEMPT_NUMBER_INVALID:${d.attemptNumber}`);
  if (!Number.isSafeInteger(d.attemptedAt) || d.attemptedAt < 0) return txErr(`ENTITY_PROVIDER_ACTION_RESULT_ATTEMPT_TIMESTAMP_INVALID:${d.attemptedAt}`);
  if (!["submitted", "transientFailure", "terminalFailure", "reconciled"].includes(d.outcome)) return txErr(`ENTITY_PROVIDER_ACTION_RESULT_OUTCOME_INVALID:${String(d.outcome)}`);
  return chain(epActionAttemptId(d), (expected): Result<Runtime, RuntimeError> => {
    if (d.attemptId !== expected) return txErr(`ENTITY_PROVIDER_ACTION_RESULT_ATTEMPT_ID_MISMATCH:${d.attemptId}:${expected}`);
    return chain(adapterFailureValid(d, "ENTITY_PROVIDER_ACTION"), () => {
      const fingerprint = stableJson(d), isAttempt = (t: JTxRow): boolean => isEpActionJTx(t.type) && t.data?.runtimeSubmitAttempt?.attemptId === d.attemptId;
      const matches = rt.pendingCommittedJOutbox.flatMap((input) => input.jTxs.filter((raw) => isAttempt(rowOf(raw))).map((raw) => ({ jurisdictionName: input.jurisdictionName, jTx: rowOf(raw) })));
      if (matches.length > 1) return txErr(`ENTITY_PROVIDER_ACTION_PENDING_ATTEMPT_DUPLICATED:${d.attemptId}`);
      const pending = matches[0], a = pending?.jTx.data.runtimeSubmitAttempt, intent = pending?.jTx.data["intent"] as EntityProviderActionIntent | undefined;
      if (pending !== undefined && !(a && intent && submitId(pending.jurisdictionName) === submitId(d.jurisdictionName) && submitId(pending.jTx.entityId) === submitId(d.entityId) && submitId(pending.jTx.data["signerId"]) === submitId(d.signerId)
        && submitId(intent.actionHash) === submitId(d.actionHash) && intent.actionNonce === d.actionNonce && intent.generation === d.generation && a.attemptId === d.attemptId && a.attemptNumber === d.attemptNumber && a.attemptedAt === d.attemptedAt))
        return txErr(`ENTITY_PROVIDER_ACTION_RESULT_PENDING_CONFLICT:${d.attemptId}`);
      return chain(recordedFingerprint(rt, d.attemptId, (l) => l.entityProviderActionSubmitState, "ENTITY_PROVIDER_ACTION"), (recorded): Result<Runtime, RuntimeError> => {
        if (recorded !== null) return recorded === fingerprint ? ok(rt) : txErr(`ENTITY_PROVIDER_ACTION_RESULT_DUPLICATE_CONFLICT:${d.attemptId}`);
        const found = findSubmitReplica(rt, d.entityId, d.signerId);
        if (found === undefined) return txErr(`ENTITY_PROVIDER_ACTION_LOCAL_REPLICA_MISSING:${d.entityId}:${d.signerId}`);
        const [key, r] = found, local = localOf(rt, key).entityProviderActionSubmitState, consensus = epActionState(r.state).pending;
        const matchesConsensus = consensus !== undefined && submitId(consensus.actionHash) === submitId(d.actionHash) && consensus.actionNonce === d.actionNonce && consensus.generation === d.generation;
        const matchesLocal = local !== undefined && submitId(local.actionHash) === submitId(d.actionHash) && local.actionNonce === d.actionNonce && local.generation === d.generation;
        if (!matchesConsensus || (matchesLocal && d.attemptNumber < local.submitAttempts)) return ok(pending === undefined ? rt : { ...rt, pendingCommittedJOutbox: dropPendingAttempt(rt.pendingCommittedJOutbox, isAttempt) });
        if (local === undefined || !matchesLocal || local.submitAttempts !== d.attemptNumber || local.lastSubmittedAt !== d.attemptedAt) return txErr(`ENTITY_PROVIDER_ACTION_RESULT_ATTEMPT_MISMATCH:${d.attemptId}`);
        if (pending === undefined) return txErr(`ENTITY_PROVIDER_ACTION_PENDING_ATTEMPT_MISSING:${d.attemptId}`);
        return map(boundedJournal(local, d.attemptId, fingerprint, activeAttempts(rt, r, isEpActionJTx), false), (journal) => {
          const now = Number(rt.timestamp), base: EntityProviderActionSubmitState = { ...local, lastResultAttemptId: d.attemptId, lastResultAt: now, lastResultOutcome: d.outcome, lastResultFingerprint: fingerprint, ...journal };
          let next: EntityProviderActionSubmitState;
          if (d.outcome === "submitted" || d.outcome === "reconciled") next = { ...withoutKeys(base, ["lastFailure"]), ...(d.txHash ? { txHash: d.txHash } : {}) };
          else {
            const failure: SubmitFailure = { message: String(d.message ?? "unknown"), failedAt: now, ...opt("adapterFailure", d.adapterFailure) };
            next = { ...base, lastFailure: failure, ...(d.outcome === "terminalFailure" ? { terminalFailure: failure } : {}) };
          }
          return withLocal({ ...rt, pendingCommittedJOutbox: dropPendingAttempt(rt.pendingCommittedJOutbox, isAttempt) }, key, { entityProviderActionSubmitState: next });
        });
      });
    });
  });
};
/** og applyGovernanceSubmitResultRuntimeTx: a transient failure re-arms the attempt one retry window later; any other outcome retires it. */
const recordGovernanceResult = (rt: Runtime, d: GovernanceResultData): Result<Runtime, RuntimeError> => {
  const isAttempt = (t: JTxRow): boolean => isGovernanceJTx(t.type) && t.data?.runtimeSubmitAttempt?.attemptId === d.attemptId;
  const matches = rt.pendingCommittedJOutbox.flatMap((input) => input.jTxs.filter((raw) => isAttempt(rowOf(raw))).map((raw) => ({ jurisdictionName: input.jurisdictionName, jTx: rowOf(raw) })));
  if (matches.length > 1) return txErr(`GOVERNANCE_SUBMIT_ATTEMPT_DUPLICATED:${d.attemptId}`);
  const pending = matches[0];
  if (pending === undefined) return txErr(`GOVERNANCE_SUBMIT_RESULT_ATTEMPT_MISSING:${d.attemptId}`);
  return chain(canonicalGovernanceAttempt(pending.jurisdictionName, pending.jTx), (attempt) => chain(governanceIdentity(pending.jurisdictionName, pending.jTx), (identity): Result<Runtime, RuntimeError> => {
    if (submitId(d.jurisdictionName) !== submitId(identity.jurisdictionName) || submitId(d.entityId) !== submitId(identity.entityId) || submitId(d.signerId) !== submitId(identity.signerId)
      || submitId(d.proposalHash) !== submitId(identity.proposalHash) || submitId(d.payloadHash) !== submitId(identity.payloadHash) || d.attemptNumber !== attempt.attemptNumber || d.attemptedAt !== attempt.attemptedAt)
      return txErr(`GOVERNANCE_SUBMIT_RESULT_IDENTITY_MISMATCH:${d.attemptId}`);
    if (d.outcome !== "transientFailure") return ok({ ...rt, pendingCommittedJOutbox: dropPendingAttempt(rt.pendingCommittedJOutbox, isAttempt) });
    const attemptNumber = attempt.attemptNumber + 1, now = Number(rt.timestamp);
    if (!Number.isSafeInteger(attemptNumber)) return txErr("GOVERNANCE_SUBMIT_ATTEMPT_EXHAUSTED");
    return map(governanceAttemptId({ ...identity, attemptNumber }), (attemptId) => ({
      ...rt, pendingCommittedJOutbox: rt.pendingCommittedJOutbox.map((input) => ({ jurisdictionName: input.jurisdictionName, jTxs: input.jTxs.map((raw) => {
        const t = rowOf(raw);
        return isAttempt(t) ? binOf({ ...t, data: { ...t.data, runtimeSubmitAttempt: { attemptId, attemptNumber, attemptedAt: now, eligibleAt: now + ENTITY_J_SUBMIT_RETRY_MS } } }) : raw;
      }) })),
    }));
  }));
};
/** og pruneHankoWitnessToReachableState: keep the sealed batch's and the pending EntityProvider action's witnesses and the newest profile one. */
const pruneWitnesses = (witness: ReadonlyMap<string, HankoWitness>, state: EntityState): ReadonlyMap<string, HankoWitness> => {
  const reachable = new Set<string>(), sent = jBatchOf(state)?.sentBatch?.batchHash, action = epActionState(state).pending?.actionHash;
  if (sent) reachable.add(sent);
  if (action) reachable.add(action);
  const profile = [...witness].filter(([, w]) => w.type === "profile").sort(([lh, l], [rh, r]) => r.entityHeight - l.entityHeight || r.createdAt - l.createdAt || asc(lh, rh))[0];
  if (profile !== undefined) reachable.add(profile[0]);
  return new Map([...witness].filter(([h]) => reachable.has(h)));
};

// ---- og jurisdiction/machine/{local-history,event-observation,events/event-normalization,event-normalizers,event-normalizers-wallet,batch-validation}.ts ----
type JRec = Readonly<Record<string, unknown>>;
type Decoded = Result<unknown, RuntimeError>;
type Field = (v: unknown) => Decoded;
const recOf = (v: unknown): JRec | null => (v !== null && typeof v === "object" && !Array.isArray(v) ? (v as JRec) : null);
const nText = (v: unknown): string => String(v ?? "").trim().toLowerCase();
const nPattern = (re: RegExp) => (v: unknown): string | null => { if (typeof v !== "string") return null; const s = v.trim().toLowerCase(); return re.test(s) ? s : null; };
const nEntity = (v: unknown): string | null => (typeof v === "string" ? v.trim().toLowerCase() || null : null);
const nAddress = nPattern(/^0x[0-9a-f]{40}$/), nBytes32 = nPattern(/^0x[0-9a-f]{64}$/), nHexBytes = nPattern(/^0x(?:[0-9a-f]{2})*$/);
/** og normalizeBigNumberish: a decimal integer (bigint, integral number, decimal text or `BigInt(n)` text) as its decimal string. */
const nBig = (v: unknown): string | null => {
  if (typeof v === "bigint") return v.toString();
  if (typeof v === "number") return Number.isFinite(v) && Number.isInteger(v) ? String(v) : null;
  if (typeof v !== "string") return null;
  const t = v.trim(), wrapped = /^BigInt\((-?\d+)\)$/.exec(t);
  if (!t) return null;
  if (wrapped) return BigInt(wrapped[1] ?? "0").toString();
  return /^-?\d+$/.test(t) ? BigInt(t).toString() : null;
};
const nInt = (v: unknown): number | null => {
  if (typeof v === "number") return Number.isFinite(v) && Number.isInteger(v) ? v : null;
  const i = nBig(v);
  return i === null || !Number.isSafeInteger(Number(i)) ? null : Number(i);
};
const nBool = (v: unknown): boolean | null => (typeof v === "boolean" ? v : null);
const nString = (v: unknown): string | null => (typeof v === "string" ? v : null);
const plain = (f: (v: unknown) => unknown): Field => (v) => ok(f(v));
/** og decodeFields: every schema field in order; the first null decodes the whole payload to null. */
const decodeFields = (data: JRec, schema: Readonly<Record<string, Field>>): Result<Record<string, unknown> | null, RuntimeError> => {
  const out: Record<string, unknown> = {};
  for (const [key, field] of Object.entries(schema)) {
    const r = field(data[key]);
    if (!r.ok) return r;
    if (r.value === null) return ok(null);
    out[key] = r.value;
  }
  return ok(out);
};
/** og requireBoundaryRecord after structuredClone: an object that is not an array, Map, Set, Date, RegExp, Error or binary view. */
const boundaryRec = (v: unknown): JRec | null =>
  v !== null && typeof v === "object" && !Array.isArray(v) && !(v instanceof Map) && !(v instanceof Set) && !(v instanceof Date) && !(v instanceof RegExp) && !(v instanceof Error) && !ArrayBuffer.isView(v) && !(v instanceof ArrayBuffer) ? (v as JRec) : null;
const jExactKeys = (v: JRec, required: readonly string[], optional: readonly string[], code: string): Result<void, RuntimeError> => {
  const allowed = new Set([...required, ...optional]), missing = required.filter((k) => !Object.hasOwn(v, k)), extra = Object.keys(v).filter((k) => !allowed.has(k));
  return missing.length > 0 || extra.length > 0 ? txErr(`${code}:missing=${missing.join(",") || "none"}:extra=${extra.join(",") || "none"}`) : ok(undefined);
};
type JCheck = (v: unknown, code: string) => Decoded;
const PB_UINT256_MAX = (1n << 256n) - 1n, PB_INT256_MAX = (1n << 255n) - 1n, PB_INT256_MIN = -(1n << 255n);
const checkString: JCheck = (v, code) => (typeof v === "string" && v.length > 0 ? ok(v) : txErr(code));
const checkBig = (min?: bigint): JCheck => (v, code) => (typeof v === "bigint" && (min === undefined || v >= min) ? ok(v) : txErr(code));
const checkInteger: JCheck = (v, code) => {
  if (typeof v === "bigint") return v < 0n || v > BigInt(Number.MAX_SAFE_INTEGER) ? txErr(`${code}:${v.toString()}`) : ok(Number(v));
  return Number.isSafeInteger(v) && Number(v) >= 0 ? ok(Number(v)) : txErr(`${code}:${String(v)}`);
};
const moneyRange = (v: unknown, min: bigint, max: bigint, label: string): Result<bigint, RuntimeError> =>
  typeof v !== "bigint" ? txErr(`ABI_MONEY_INTEGER:${label}`) : v < min || v > max ? txErr(`ABI_MONEY_WIDTH:${label}`) : ok(v);
const checkMoney: JCheck = (v, code) => chain(checkBig()(v, code), (b) => moneyRange(b, 0n, PB_UINT256_MAX, code));
/** og decodeInt512 then encodeInt512: a positional pair or an exact {high, low} record, re-emitted as {high, low}. */
const int512 = (v: unknown): Decoded => {
  let limbs: readonly unknown[];
  if (Array.isArray(v)) {
    if (v.length !== 2) return txErr("ABI_MONEY_TUPLE_LENGTH:Int512");
    if (Object.keys(v).some((k, i) => k !== String(i))) return txErr("ABI_MONEY_TUPLE_FIELDS:Int512");
    limbs = v;
  } else {
    const r = boundaryRec(v);
    if (r === null) return txErr("ABI_MONEY_TUPLE:Int512");
    const keys = jExactKeys(r, ["high", "low"], [], "ABI_MONEY_TUPLE_FIELDS:Int512");
    if (!keys.ok) return keys;
    limbs = [r["high"], r["low"]];
  }
  return chain(moneyRange(limbs[0], PB_INT256_MIN, PB_INT256_MAX, "Int512.high"), (high) => map(moneyRange(limbs[1], 0n, PB_UINT256_MAX, "Int512.low"), (low) => {
    const n = (high << 256n) + low;
    return { high: n >> 256n, low: n & PB_UINT256_MAX };
  }));
};
const eachOf = (v: unknown, code: string, f: (x: unknown, i: number) => Decoded): Result<readonly unknown[], RuntimeError> =>
  Array.isArray(v) ? traverse(v.map((x, i) => [x, i] as const), ([x, i]) => f(x, i)) : txErr(code);
/** og validateRecordArray: exact keys per record, each field checked in schema order. */
const recordArray = (v: unknown, code: string, fields: Readonly<Record<string, JCheck>>): Result<readonly unknown[], RuntimeError> =>
  eachOf(v, code, (raw, index) => {
    const itemCode = `${code}_${index}`, item = boundaryRec(raw);
    if (item === null) return txErr(itemCode);
    return chain(jExactKeys(item, Object.keys(fields), [], `${itemCode}_FIELDS`), () => foldResult(Object.entries(fields), { ...item } as Record<string, unknown>, (acc, [key, check]) =>
      map(check(item[key], `${itemCode}_${key.toUpperCase()}`), (x) => ({ ...acc, [key]: x }))));
  });
/** og validateProofBody(structuredClone(value), 'J_EVENT_PROOFBODY'). */
const proofBodyField: Field = (value) => {
  const code = "J_EVENT_PROOFBODY", proof = boundaryRec(value);
  if (proof === null) return txErr(code);
  return chain(jExactKeys(proof, ["watchSeed", "leftResponseSeconds", "rightResponseSeconds", "offdeltas", "tokenIds", "transformers"], [], `${code}_FIELDS`), () =>
    chain(checkString(proof["watchSeed"], `${code}_WATCH_SEED`), () =>
      chain(checkInteger(proof["leftResponseSeconds"], `${code}_LEFT_RESPONSE_SECONDS`), (leftResponseSeconds) =>
        chain(checkInteger(proof["rightResponseSeconds"], `${code}_RIGHT_RESPONSE_SECONDS`), (rightResponseSeconds) =>
          chain(eachOf(proof["offdeltas"], `${code}_OFFDELTAS`, int512), (offdeltas) =>
            chain(eachOf(proof["tokenIds"], `${code}_TOKEN_IDS`, (x, i) => checkBig(0n)(x, `${code}_TOKEN_IDS_${i}`)), (tokenIds) =>
              map(recordArray(proof["transformers"], `${code}_TRANSFORMERS`, {
                transformerAddress: checkString, encodedBatch: checkString,
                allowances: (x, c) => recordArray(x, c, { deltaIndex: checkBig(), rightAllowance: checkMoney, leftAllowance: checkMoney }),
              }), (transformers) => ({ ...proof, leftResponseSeconds, rightResponseSeconds, offdeltas, tokenIds, transformers }))))))));
};
type Normalizer = (data: JRec) => Result<Record<string, unknown> | null, RuntimeError>;
const fields = (schema: Readonly<Record<string, Field>>, then: (d: Record<string, unknown>, data: JRec) => Record<string, unknown> | null = (d) => d): Normalizer =>
  (data) => map(decodeFields(data, schema), (d) => (d === null ? null : then(d, data)));
const withBatchNonce = (d: Record<string, unknown>, data: JRec): Record<string, unknown> => { const n = nInt(data["batchNonce"]); return n === null ? d : { ...d, batchNonce: n }; };
const positiveUint256 = (v: unknown): boolean => typeof v === "string" && BigInt(v) >= 1n && BigInt(v) <= PB_UINT256_MAX;
const entityPair = { debtor: plain(nEntity), creditor: plain(nEntity), tokenId: plain(nInt) };
const walletList = (entry: JRec, schema: Readonly<Record<string, Field>>): Record<string, unknown> | null => { const d = decodeFields(entry, schema); return d.ok ? d.value : null; };
const tokenBalances = (v: unknown): unknown => {
  const out: Record<string, unknown>[] = [];
  for (const raw of Array.isArray(v) ? v : []) {
    const entry = recOf(raw), d = entry === null ? null : walletList(entry, { tokenAddress: plain(nAddress), balance: plain(nBig) });
    if (entry === null || d === null) return null;
    const tokenId = nInt(entry["tokenId"]);
    out.push({ ...d, ...(tokenId !== null ? { tokenId } : {}) });
  }
  const text = (r: Record<string, unknown>, k: string): string => String(r[k]);
  return out.sort((l, r) => asc(text(l, "tokenAddress"), text(r, "tokenAddress")) || (Number(l["tokenId"] ?? -1) - Number(r["tokenId"] ?? -1)) || asc(text(l, "balance"), text(r, "balance")));
};
const walletAllowances = (v: unknown): unknown => {
  const out: Record<string, unknown>[] = [];
  for (const raw of Array.isArray(v) ? v : []) {
    const entry = recOf(raw), d = entry === null ? null : walletList(entry, { tokenAddress: plain(nAddress), spender: plain(nAddress), allowance: plain(nBig) });
    if (entry === null || d === null) return null;
    out.push(d);
  }
  const text = (r: Record<string, unknown>, k: string): string => String(r[k]);
  return out.sort((l, r) => asc(text(l, "tokenAddress"), text(r, "tokenAddress")) || asc(text(l, "spender"), text(r, "spender")) || asc(text(l, "allowance"), text(r, "allowance")));
};
/** og EVENT_NORMALIZERS: the only J event decoding registry; an unknown type decodes to null. */
const EVENT_NORMALIZERS: ReadonlyMap<string, Normalizer> = new Map<string, Normalizer>([
  ["FoundationBootstrapped", fields({ recipient: plain(nAddress), boardHash: plain(nBytes32), controlTokenId: plain(nBig), dividendTokenId: plain(nBig) })],
  ["EntityRegistered", fields({ entityId: plain(nBytes32), entityNumber: plain(nBig), boardHash: plain(nBytes32) })],
  ["BoardActivated", fields({ entityId: plain(nBytes32), previousBoardHash: plain(nBytes32), newBoardHash: plain(nBytes32), previousBoardValidUntil: plain(nBig) }, (d) => (BigInt(String(d["previousBoardValidUntil"])) > 0n ? d : null))],
  ["ReserveUpdated", fields({ entity: plain(nEntity), tokenId: plain(nInt), newBalance: plain(nBig) })],
  ["ExternalWalletSnapshot", fields({ entityId: plain(nEntity), owner: plain(nAddress), tokenBalances: plain(tokenBalances), allowances: plain(walletAllowances) }, (d, data) => {
    const native = data["nativeBalance"] === undefined ? null : nBig(data["nativeBalance"]);
    if (data["nativeBalance"] !== undefined && native === null) return null;
    const balances = d["tokenBalances"] as readonly unknown[], allowances = d["allowances"] as readonly unknown[];
    return { entityId: d["entityId"], owner: d["owner"], ...(native !== null ? { nativeBalance: native } : {}), ...(balances.length ? { tokenBalances: balances } : {}), ...(allowances.length ? { allowances } : {}) };
  })],
  ["ExternalWalletDelta", fields({ entityId: plain(nEntity), owner: plain(nAddress), tokenAddress: plain(nAddress) }, (d, data) => {
    const tokenId = nInt(data["tokenId"]), balanceDelta = data["balanceDelta"] === undefined ? null : nBig(data["balanceDelta"]);
    const spender = data["spender"] === undefined ? null : nAddress(data["spender"]), allowance = data["allowance"] === undefined ? null : nBig(data["allowance"]);
    const hasBalance = data["balanceDelta"] !== undefined, hasAllowance = data["allowance"] !== undefined || data["spender"] !== undefined;
    if ((hasBalance && balanceDelta === null) || (hasAllowance && (!spender || allowance === null)) || (!hasBalance && !hasAllowance)) return null;
    return { ...d, ...(tokenId !== null ? { tokenId } : {}), ...(balanceDelta !== null ? { balanceDelta } : {}), ...(spender && allowance !== null ? { spender, allowance } : {}) };
  })],
  ["SecretRevealed", fields({ hashlock: plain(nString), revealer: plain(nString), secret: plain(nString) }, (d) => ({ ...d, revealer: String(d["revealer"]).toLowerCase() }))],
  ["AccountSettled", fields({ leftEntity: plain(nEntity), rightEntity: plain(nEntity), tokenId: plain(nInt), leftReserve: plain(nBig), rightReserve: plain(nBig), collateral: plain(nBig), ondelta: plain(nBig), nonce: plain(nInt) })],
  ["DisputeStarted", fields({
    sender: plain(nEntity), counterentity: plain(nEntity), nonce: plain(nBig), proposerIsLeft: plain(nBool), proofbodyHash: plain(nString), watchSeed: plain(nBytes32), starterInitialArguments: plain(nHexBytes),
    starterCounterArguments: plain(nHexBytes), starterCounterProofCommitment: plain(nBytes32), initialProofbody: proofBodyField, disputeTimeout: plain(nInt), disputeStartTimestamp: plain(nInt), leftResponseSeconds: plain(nInt), rightResponseSeconds: plain(nInt),
  }, (d, data) => {
    const start = Number(d["disputeStartTimestamp"]), left = Number(d["leftResponseSeconds"]), right = Number(d["rightResponseSeconds"]);
    return start <= 0 || left < 0 || right < 0 || Number(d["disputeTimeout"]) !== start + left + right ? null : withBatchNonce(d, data);
  })],
  ["DisputeFinalized", fields({ sender: plain(nEntity), counterentity: plain(nEntity), initialNonce: plain(nBig), initialProofbodyHash: plain(nString), finalProofbodyHash: plain(nString), finalizationEvidenceHash: plain(nString), finalProofbody: proofBodyField }, withBatchNonce)],
  ["CounterDisputeRegistered", fields({ sender: plain(nEntity), counterentity: plain(nEntity), nonce: plain(nInt), proposerIsLeft: plain(nBool), proofbodyHash: plain(nBytes32), counterProofbody: proofBodyField })],
  ["HashLadderRevealRegistered", fields({
    entity: plain(nEntity), counterpartyEntity: plain(nEntity), ladderHash: plain(nBytes32), fillRatio: plain(nInt), fullSecret: plain(nBytes32),
    reveals: plain((v) => { if (!Array.isArray(v) || v.length !== 4) return null; const r = v.map(nBytes32); return r.some((x) => x === null) ? null : r; }), targetRole: plain(nBool), revealedAt: plain(nInt),
  }, (d) => { const ratio = Number(d["fillRatio"]); return ratio <= 0 || ratio > 0xffff || Number(d["revealedAt"]) <= 0 ? null : d; })],
  ["DebtCreated", fields({ ...entityPair, amount: plain(nBig), debtIndex: plain(nInt) })],
  ["DebtEnforced", fields({ ...entityPair, amountPaid: plain(nBig), remainingAmount: plain(nBig), newDebtIndex: plain(nInt) })],
  ["DebtForgiven", fields({ ...entityPair, amountForgiven: plain(nBig), debtIndex: plain(nInt) })],
  ["HankoBatchProcessed", fields({ entityId: plain(nBytes32), batchHash: plain(nBytes32), nonce: plain(nInt) }, (d) => (Number(d["nonce"]) >= 1 ? d : null))],
  ["EntityProviderActionExecuted", fields({ entityId: plain(nBytes32), actionNonce: plain(nBig), actionHash: plain(nBytes32) }, (d, data) => {
    const actionKind = nInt(data["actionKind"]);
    return positiveUint256(d["actionNonce"]) && (actionKind === 0 || actionKind === 1) ? { ...d, actionKind } : null;
  })],
  ["EntityProviderActionCancelled", fields({ entityId: plain(nBytes32), actionNonce: plain(nBig), cancelledActionHash: plain(nBytes32), cancelHash: plain(nBytes32) }, (d, data) => {
    const cancelledActionKind = nInt(data["cancelledActionKind"]);
    return positiveUint256(d["actionNonce"]) && (cancelledActionKind === 0 || cancelledActionKind === 1) ? { ...d, cancelledActionKind } : null;
  })],
]);
type WireJEvent = { readonly type: string; readonly data: Record<string, unknown>; readonly blockNumber?: number; readonly blockHash?: string; readonly transactionHash?: string; readonly logIndex?: number; readonly eventIndex?: number };
/** og normalizeMetadata. */
const eventMetadata = (raw: JRec): Omit<WireJEvent, "type" | "data"> => {
  const blockNumber = nInt(raw["blockNumber"]), logIndex = nInt(raw["logIndex"]), eventIndex = nInt(raw["eventIndex"]);
  const text = (k: string): string | undefined => { const v = raw[k]; return typeof v === "string" && v.trim() ? v : undefined; };
  return { ...opt("blockNumber", blockNumber ?? undefined), ...opt("blockHash", text("blockHash")), ...opt("transactionHash", text("transactionHash")),
    ...opt("logIndex", logIndex !== null && logIndex >= 0 ? logIndex : undefined), ...opt("eventIndex", eventIndex !== null && eventIndex >= 0 ? eventIndex : undefined) };
};
/** og normalizeJurisdictionEvent. */
const normalizeJEvent = (value: unknown): Result<WireJEvent | null, RuntimeError> => {
  const raw = recOf(value), data = raw === null ? null : recOf(raw["data"]), type = raw !== null && typeof raw["type"] === "string" ? raw["type"] : "";
  const normalize = EVENT_NORMALIZERS.get(type);
  if (raw === null || data === null || normalize === undefined) return ok(null);
  return map(normalize(data), (d) => (d === null ? null : { ...eventMetadata(raw), type, data: d }));
};
/** og requireCanonicalJurisdictionEvents: every member canonical, none filtered. */
const canonicalJEvents = (value: unknown): Result<WireJEvent[], RuntimeError> => {
  if (!Array.isArray(value)) return txErr("JURISDICTION_EVENTS_ARRAY_REQUIRED");
  const out: WireJEvent[] = [];
  for (const [index, item] of value.entries()) {
    const e = normalizeJEvent(item);
    if (!e.ok) return e;
    if (e.value === null) return txErr(`JURISDICTION_EVENT_INVALID:${index}`);
    out.push(e.value);
  }
  return ok(out);
};
const jEventPayloadKey = (e: WireJEvent): string => e.type === "AccountSettled"
  ? ["AccountSettled", String(e.data["leftEntity"]).toLowerCase(), String(e.data["rightEntity"]).toLowerCase(), e.data["tokenId"], e.data["leftReserve"], e.data["rightReserve"], e.data["collateral"], e.data["ondelta"], e.data["nonce"]].join(":")
  : `${e.type}:${stableJson(e.data)}`;
const jEventKey = (e: WireJEvent): string => stableJson([e.blockNumber ?? null, e.blockHash?.toLowerCase() ?? null, e.transactionHash?.toLowerCase() ?? null, e.logIndex ?? null, e.eventIndex ?? null, jEventPayloadKey(e)]);
const optionalIndex = (l: number | undefined, r: number | undefined): number => (l !== undefined && r !== undefined ? l - r : l !== undefined ? -1 : r !== undefined ? 1 : 0);
/** og compareCanonicalJurisdictionEvents: EVM order, then transaction hash, then payload. */
const compareJEvents = (l: WireJEvent, r: WireJEvent): number =>
  optionalIndex(l.blockNumber, r.blockNumber) || optionalIndex(l.logIndex, r.logIndex) || optionalIndex(l.eventIndex, r.eventIndex)
  || asc(l.transactionHash?.toLowerCase() ?? "", r.transactionHash?.toLowerCase() ?? "") || asc(jEventPayloadKey(l), jEventPayloadKey(r));
/** og canonicalJurisdictionEventsHash. */
const jEventsHash = (events: unknown): Result<string, RuntimeError> =>
  map(canonicalJEvents(events), (es) => `0x${keccakUtf8(JSON.stringify(es.sort(compareJEvents).map(jEventKey)))}`);
const normHex = (v: unknown): string => { const t = String(v || "").trim(); return t.startsWith("0x") ? t.toLowerCase() : t; };
const normDecimal = (v: unknown): string => (typeof v === "bigint" ? v.toString() : typeof v === "number" ? (Number.isFinite(v) ? Math.floor(v).toString() : "") : String(v || "").trim());
const evidenceKey = (e: JRec): string => JSON.stringify([normHex(e["sender"]), normHex(e["counterentity"]), normDecimal(e["initialNonce"]), normDecimal(e["finalNonce"]), normHex(e["initialProofbodyHash"]),
  normHex(e["finalProofbodyHash"]), e["proposerIsLeft"], normHex(e["leftArguments"]), normHex(e["rightArguments"]), e["startedByLeft"], normHex(e["sig"])]);
/** og normalizeDisputeFinalizationEvidence: sorted by canonical key, no duplicates. */
const normalizeEvidence = (evidence: unknown): Result<readonly { readonly key: string; readonly entry: JRec }[], RuntimeError> => {
  if (!Array.isArray(evidence)) return txErr("J_DISPUTE_FINALIZATION_EVIDENCE_INVALID");
  const ordered = evidence.map((entry: JRec) => ({ key: evidenceKey(entry), entry })).sort((l, r) => asc(l.key, r.key));
  return ordered.some((x, i) => i > 0 && ordered[i - 1]?.key === x.key) ? txErr("J_DISPUTE_FINALIZATION_EVIDENCE_DUPLICATE") : ok(ordered);
};
export type ValidatorJBlock = { readonly jurisdictionRef: string; readonly jHeight: number; readonly jBlockHash: string; readonly eventsHash: string; readonly events: readonly unknown[]; readonly disputeFinalizationEvidence?: readonly unknown[] | undefined; readonly disputeFinalizationEvidenceHash?: string | undefined };
/** og ValidatorJHistory: this validator's private J-chain view above the Entity-certified anchor. */
export type ValidatorJHistory = { readonly jurisdictionRef: string; readonly scannedThroughHeight: number; readonly contiguousThroughHeight: number; readonly tipBlockHash: string; readonly eventBlocks: ReadonlyMap<number, ValidatorJBlock>; readonly blockHashes: ReadonlyMap<number, string> };
/** og normalizeEventBlock: the block's events and dispute evidence must hash to what it claims. */
const normalizeEventBlock = (jurisdictionRef: string, raw: unknown): Result<ValidatorJBlock, RuntimeError> => {
  const block = recOf(raw) ?? {}, jHeight = Number(block["jHeight"]);
  if (!Number.isSafeInteger(jHeight) || jHeight <= 0) return txErr("J_HISTORY_LOCAL_BLOCK_HEIGHT_INVALID");
  if (nText(block["jurisdictionRef"]) !== jurisdictionRef) return txErr("J_HISTORY_LOCAL_JURISDICTION_MISMATCH");
  const jBlockHash = nText(block["jBlockHash"]);
  if (!jBlockHash) return txErr("J_HISTORY_LOCAL_BLOCK_HASH_MISSING");
  return chain(canonicalJEvents(block["events"]), (events) => chain(jEventsHash(events.sort(compareJEvents)), (eventsHash) => {
    if (nText(block["eventsHash"]) !== eventsHash) return txErr("J_HISTORY_LOCAL_EVENTS_HASH_MISMATCH");
    return chain(normalizeEvidence(block["disputeFinalizationEvidence"] ?? []), (evidence) => {
      const evidenceHash = evidence.length > 0 ? `0x${keccakUtf8(JSON.stringify(evidence.map((x) => x.key)))}` : "";
      if (nText(block["disputeFinalizationEvidenceHash"]) !== evidenceHash) return txErr("J_HISTORY_LOCAL_EVIDENCE_HASH_MISMATCH");
      return ok({ jurisdictionRef, jHeight, jBlockHash, eventsHash, events, ...(evidence.length > 0 ? { disputeFinalizationEvidence: evidence.map((x) => x.entry) } : {}), ...(evidenceHash ? { disputeFinalizationEvidenceHash: evidenceHash } : {}) });
    });
  }));
};
const blockIdentity = (b: ValidatorJBlock): string => `${nText(b.jBlockHash)}:${nText(b.eventsHash)}:${nText(b.disputeFinalizationEvidenceHash)}`;
type JAnchor = { readonly height: number; readonly hash: string; readonly jurisdictionRef: string; readonly eventHistoryRoot: string };
/** og getJHistoryRegistrationBaseHeight: certify EntityProvider history from its deployment block. */
const jHistoryRegistrationBase = (config: JurisdictionConfig | undefined): number => {
  const d = Number(config?.entityProviderDeploymentBlock ?? 0);
  return !Number.isSafeInteger(d) || d <= 1 ? 0 : d - 1;
};
/** og getJEventJurisdictionRef: the Entity's jurisdiction stack id, or `unconfigured`. */
const jEventJurisdictionRef = (state: EntityState): string =>
  stackIdOf({ depositoryAddress: usableAddress(state.jurisdiction.depositoryAddress) ?? "", ...opt("chainId", stackChainId(state.jurisdiction.chainId) ?? undefined) }) || "unconfigured";
/** og getEntityCertifiedJAnchor: the one Entity-certified J head (null before the first certified range). */
const certifiedJAnchor = (state: EntityState): Result<JAnchor | null, RuntimeError> => {
  const finality = recOf(state.committed["jHistoryFinality"]), raw = state.committed["lastFinalizedJHeight"], stateHeight = Number(raw || 0);
  if (!Number.isSafeInteger(stateHeight) || stateHeight < 0) return txErr(`J_HISTORY_FINALITY_HEIGHT_CORRUPTION:state=${String(raw)}`);
  if (finality === null) {
    const base = jHistoryRegistrationBase(state.jurisdictionConfig);
    return stateHeight !== base ? txErr(`J_HISTORY_FINALITY_MISSING:state=${stateHeight}:registrationBase=${base}`) : ok(null);
  }
  const height = Number(finality["finalizedThroughHeight"]);
  if (!Number.isSafeInteger(height) || height <= 0 || height !== stateHeight) return txErr(`J_HISTORY_FINALITY_HEIGHT_CORRUPTION:state=${stateHeight}:anchor=${String(finality["finalizedThroughHeight"])}`);
  const hash = nText(finality["tipBlockHash"]), jurisdictionRef = nText(finality["jurisdictionRef"]), eventHistoryRoot = nText(finality["eventHistoryRoot"]);
  if (!/^0x[0-9a-f]{64}$/.test(hash)) return txErr("J_HISTORY_FINALITY_HASH_CORRUPTION");
  if (!jurisdictionRef) return txErr("J_HISTORY_FINALITY_JURISDICTION_CORRUPTION");
  if (!/^0x[0-9a-f]{64}$/.test(eventHistoryRoot)) return txErr("J_HISTORY_FINALITY_ROOT_CORRUPTION:certified-root-invalid");
  return ok({ height, hash, jurisdictionRef, eventHistoryRoot });
};
/** og assertValidatorJHistoryMatchesAnchor: a sane cached frontier that never contradicts the certified anchor. */
const historyMatchesAnchor = (anchor: JAnchor | null, h: ValidatorJHistory | undefined): Result<void, RuntimeError> => {
  if (h === undefined) return ok(undefined);
  const scanned = Number(h.scannedThroughHeight), contiguous = Number(h.contiguousThroughHeight);
  if (!Number.isSafeInteger(scanned) || scanned <= 0) return txErr(`J_HISTORY_LOCAL_SCANNED_HEIGHT_CORRUPTION:${String(h.scannedThroughHeight)}`);
  if (!Number.isSafeInteger(contiguous) || contiguous < 0 || contiguous > scanned) return txErr(`J_HISTORY_LOCAL_CONTIGUOUS_HEIGHT_CORRUPTION:${String(h.contiguousThroughHeight)}:${scanned}`);
  const tip = h.blockHashes.get(scanned);
  if (!tip || nText(tip) !== nText(h.tipBlockHash)) return txErr(`J_HISTORY_LOCAL_TIP_CORRUPTION:${scanned}`);
  if (anchor === null) return ok(undefined);
  if (nText(h.jurisdictionRef) !== anchor.jurisdictionRef) return txErr("J_HISTORY_FINALITY_JURISDICTION_CONFLICT");
  const localHash = h.blockHashes.get(anchor.height), localBlock = h.eventBlocks.get(anchor.height);
  return (localHash && nText(localHash) !== anchor.hash) || (localBlock && nText(localBlock.jBlockHash) !== anchor.hash) ? txErr(`J_HISTORY_FINALIZED_REORG:${anchor.height}`) : ok(undefined);
};
type JRangeObservation = { readonly jurisdictionRef: unknown; readonly scannedThroughHeight: unknown; readonly tipBlockHash: unknown; readonly headers?: readonly unknown[] | undefined; readonly blocks: readonly unknown[] };
/** og recordValidatorJHistory: merge one watcher page; a different hash at a known height is a reorg, never an overwrite. */
const recordJHistory = (current: ValidatorJHistory | undefined, input: JRangeObservation, state?: EntityState): Result<ValidatorJHistory, RuntimeError> => {
  const jurisdictionRef = nText(input.jurisdictionRef), scannedThroughHeight = Number(input.scannedThroughHeight), tipBlockHash = nText(input.tipBlockHash);
  if (!jurisdictionRef) return txErr("J_HISTORY_LOCAL_JURISDICTION_MISSING");
  if (!Number.isSafeInteger(scannedThroughHeight) || scannedThroughHeight <= 0) return txErr("J_HISTORY_LOCAL_SCANNED_HEIGHT_INVALID");
  if (!tipBlockHash) return txErr("J_HISTORY_LOCAL_TIP_HASH_MISSING");
  if (current && nText(current.jurisdictionRef) !== jurisdictionRef) return txErr("J_HISTORY_LOCAL_JURISDICTION_REBIND");
  return chain(state === undefined ? ok(null) : certifiedJAnchor(state), (anchor): Result<ValidatorJHistory, RuntimeError> => {
    if (anchor && anchor.jurisdictionRef !== jurisdictionRef) return txErr("J_HISTORY_FINALITY_JURISDICTION_CONFLICT");
    if (anchor && scannedThroughHeight < anchor.height) return txErr(`J_HISTORY_LOCAL_BEHIND_FINALIZED_ANCHOR:${scannedThroughHeight}:${anchor.height}`);
    const matched = historyMatchesAnchor(anchor, current);
    if (!matched.ok) return matched;
    const minimum = state === undefined ? 0 : Number(state.committed["lastFinalizedJHeight"]);
    const eventBlocks = new Map([...(current?.eventBlocks ?? [])].filter(([h]) => h > minimum));
    const blockHashes = new Map([...(current?.blockHashes ?? [])].filter(([h]) => h >= minimum));
    const reorg = (h: number, local: string): Result<never, RuntimeError> => txErr(anchor?.height === h ? `J_HISTORY_FINALIZED_REORG:${h}` : `${local}:${h}`);
    if (anchor) blockHashes.set(anchor.height, anchor.hash);
    for (const raw of input.headers ?? []) {
      const header = recOf(raw) ?? {}, jHeight = Number(header["jHeight"]), jBlockHash = nText(header["jBlockHash"]);
      if (!Number.isSafeInteger(jHeight) || jHeight <= 0 || jHeight > scannedThroughHeight) return txErr("J_HISTORY_LOCAL_HEADER_HEIGHT_INVALID");
      if (!jBlockHash) return txErr("J_HISTORY_LOCAL_HEADER_HASH_MISSING");
      if (anchor && jHeight < anchor.height) continue;
      if (anchor && jHeight === anchor.height && jBlockHash !== anchor.hash) return txErr(`J_HISTORY_FINALIZED_REORG:${jHeight}`);
      const existing = blockHashes.get(jHeight);
      if (existing && nText(existing) !== jBlockHash) return reorg(jHeight, "J_HISTORY_LOCAL_REORG_AT_BLOCK");
      blockHashes.set(jHeight, jBlockHash);
    }
    for (const raw of input.blocks) {
      const normalized = normalizeEventBlock(jurisdictionRef, raw);
      if (!normalized.ok) return normalized;
      const block = normalized.value;
      if (block.jHeight > scannedThroughHeight) return txErr("J_HISTORY_LOCAL_BLOCK_ABOVE_SCAN_TIP");
      if (anchor && block.jHeight <= anchor.height) {
        if (block.jHeight === anchor.height && block.jBlockHash !== anchor.hash) return txErr(`J_HISTORY_FINALIZED_REORG:${block.jHeight}`);
        continue;
      }
      const existing = eventBlocks.get(block.jHeight);
      if (existing && blockIdentity(existing) !== blockIdentity(block)) return reorg(block.jHeight, "J_HISTORY_LOCAL_REORG_AT_EVENT_BLOCK");
      const existingHash = blockHashes.get(block.jHeight);
      if (existingHash && nText(existingHash) !== block.jBlockHash) return reorg(block.jHeight, "J_HISTORY_LOCAL_REORG_AT_BLOCK");
      eventBlocks.set(block.jHeight, block);
      blockHashes.set(block.jHeight, block.jBlockHash);
    }
    const existingTip = blockHashes.get(scannedThroughHeight);
    if (existingTip && nText(existingTip) !== tipBlockHash) return reorg(scannedThroughHeight, "J_HISTORY_LOCAL_REORG_AT_TIP");
    blockHashes.set(scannedThroughHeight, tipBlockHash);
    const previous = current?.scannedThroughHeight ?? 0, scanned = Math.max(previous, scannedThroughHeight);
    let contiguous = Math.max(current?.contiguousThroughHeight ?? minimum, minimum);
    while (contiguous < scanned && blockHashes.has(contiguous + 1)) contiguous += 1;
    const recorded: ValidatorJHistory = { jurisdictionRef, scannedThroughHeight: scanned, contiguousThroughHeight: contiguous, tipBlockHash: scannedThroughHeight >= previous || current === undefined ? tipBlockHash : current.tipBlockHash, eventBlocks, blockHashes };
    return map(historyMatchesAnchor(anchor, recorded), () => recorded);
  });
};
/** og rewindValidatorJHistory: drop the validator-private suffix and resume from the Entity-certified head (none before one). */
const rewindJHistoryTo = (state: EntityState, h: ValidatorJHistory | undefined): Result<ValidatorJHistory | undefined, RuntimeError> => {
  if (h === undefined) return ok(undefined);
  return chain(certifiedJAnchor(state), (anchor) => {
    if (anchor === null) return ok(undefined);
    const local = h.blockHashes.get(anchor.height);
    if (local && nText(local) !== anchor.hash) return txErr(`J_HISTORY_FINALIZED_REORG:${anchor.height}`);
    return ok({ jurisdictionRef: nText(h.jurisdictionRef), scannedThroughHeight: anchor.height, contiguousThroughHeight: anchor.height, tipBlockHash: anchor.hash, eventBlocks: new Map(), blockHashes: new Map([[anchor.height, anchor.hash]]) });
  });
};
/** og findExistingReplicaCaseInsensitive: the replica keyed exactly `entity:signer`, ignoring case. */
const replicaByKeyCI = (rt: Runtime, entityId: string, signerId: string): readonly [string, EntityReplica] | undefined =>
  [...rt.entities].find(([key]) => { const [e = "", s = ""] = key.split(":"); return e.toLowerCase() === entityId && s.toLowerCase() === signerId; });
/**
 * og observeJRangeRuntimeTx: a watcher page for this validator's Entity. A page behind the certified anchor is validated and dropped;
 * otherwise it is merged into the validator's local J history.
 */
const observeJRange = (rt: Runtime, d: Extract<RuntimeTx, { type: "observeJRange" }>["data"]): Result<Runtime, RuntimeError> => {
  const entityId = String(d.entityId || "").trim().toLowerCase(), signerId = String(d.signerId || "").trim().toLowerCase(), match = replicaByKeyCI(rt, entityId, signerId);
  if (match === undefined) return txErr(`J_HISTORY_LOCAL_REPLICA_MISSING:${entityId}:${signerId}`);
  const [key, replica] = match, expected = jEventJurisdictionRef(replica.state), observed = nText(d.jurisdictionRef);
  if (observed !== expected) return txErr(`J_HISTORY_OBSERVATION_JURISDICTION_MISMATCH:${entityId}:${signerId}:expected=${expected}:observed=${observed || "missing"}`);
  const observation: JRangeObservation = { jurisdictionRef: d.jurisdictionRef, scannedThroughHeight: d.scannedThroughHeight, tipBlockHash: d.tipBlockHash, ...(d.headers ? { headers: d.headers } : {}), blocks: d.blocks };
  const history = localOf(rt, key).jHistory;
  return chain(certifiedJAnchor(replica.state), (anchor) => {
    if (anchor && Number(observation.scannedThroughHeight) < anchor.height)
      return chain(recordJHistory(undefined, observation), () => map(historyMatchesAnchor(anchor, history), () => rt));
    return map(recordJHistory(history, observation, replica.state), (jHistory) => withLocal(rt, key, { jHistory }));
  });
};
/** og rewindJHistoryRuntimeTx: never below the certified anchor, never into a range this validator's locked frame signed (a precommit cannot be revoked). */
const rewindJHistory = (rt: Runtime, d: Extract<RuntimeTx, { type: "rewindJHistory" }>["data"]): Result<Runtime, RuntimeError> => {
  const entityId = String(d.entityId || "").trim().toLowerCase(), signerId = String(d.signerId || "").trim().toLowerCase(), match = replicaByKeyCI(rt, entityId, signerId);
  if (match === undefined) return txErr(`J_HISTORY_LOCAL_REPLICA_MISSING:${entityId}:${signerId}`);
  const [key, replica] = match, history = localOf(rt, key).jHistory;
  if (nText(history?.jurisdictionRef) !== nText(d.jurisdictionRef)) return txErr(`J_HISTORY_REWIND_JURISDICTION_MISMATCH:${entityId}:${signerId}`);
  return chain(certifiedJAnchor(replica.state), (anchor) => {
    if (anchor && d.conflictingHeight <= anchor.height) return txErr(`J_HISTORY_FINALIZED_REORG:${d.conflictingHeight}`);
    const signed = replica._tag === "locked" && replica.frame.txs.some((tx) => tx.type === "j_event" && d.conflictingHeight > Number(tx.data["baseHeight"]) && d.conflictingHeight <= Number(tx.data["scannedThroughHeight"]));
    if (signed && replica._tag === "locked") return txErr(`J_HISTORY_SIGNED_LOCK_REORG:entity=${entityId}:signer=${signerId}:frameHeight=${replica.frame.height}:frameHash=${unwrapOr(hashEntityFrame(replica.frame), () => "")}:jHeight=${d.conflictingHeight}`);
    return map(rewindJHistoryTo(replica.state, history), (jHistory) => withLocal(rt, key, { jHistory }));
  });
};

// ---- og entity/tx/j-events.ts applyJEvent over jurisdiction/machine/{range-budget, j-event-range-validation, history-consensus, local-history}: the Entity-certified J range ----
const jCode = (e: RuntimeError): string => ("code" in e ? e.code : e._tag);
const jHistoryText = (v: unknown): string => keccak256Hex(utf8(String(v ?? "").trim().toLowerCase()));
/** og EMPTY_J_HISTORY_ROOT. */
export const EMPTY_J_HISTORY_ROOT = keccak256Hex(utf8("xln:j-history-empty:v1"));
const jRoot = (v: unknown, label: string): Result<string, string> => { const s = String(v ?? "").trim().toLowerCase(); return WORD32.test(s) ? ok(s) : err(`J_HISTORY_INVALID_${label}`); };
const jHeight = (v: unknown, label: string): Result<number, string> => { const n = Number(v); return Number.isSafeInteger(n) && n >= 0 ? ok(n) : err(`J_HISTORY_INVALID_${label}`); };
type JHistoryIdentity = { readonly jurisdictionRef: string; readonly jHeight: unknown; readonly jBlockHash: string; readonly eventsHash: string; readonly disputeFinalizationEvidenceHash?: string | undefined };
/** og canonicalJHistoryObservationLeaf. */
const jHistoryLeaf = (o: JHistoryIdentity): Result<string, string> => chain(jHeight(o.jHeight, "OBSERVATION_HEIGHT"), (h) => chain(jRoot(o.eventsHash, "EVENTS_ROOT"), (events) =>
  map(o.disputeFinalizationEvidenceHash ? jRoot(o.disputeFinalizationEvidenceHash, "EVIDENCE_ROOT") : ok(ZERO_WORD), (evidence) =>
    keccak256Hex(abiEncode([A.b32(jHistoryText("xln:j-history-event-block:v1")), A.b32(jHistoryText(o.jurisdictionRef)), A.uint(BigInt(h)), A.b32(jHistoryText(o.jBlockHash)), A.b32(events), A.b32(evidence)])))));
/** og foldJHistoryRoot: observations by height; the same leaf twice is one leaf, two leaves at one height are an equivocation. */
export const foldJHistoryRoot = (base: string, observations: readonly JHistoryIdentity[]): Result<string, string> => chain(jRoot(base, "BASE_ROOT"), (start) => {
  for (const o of observations) { const h = jHeight(o.jHeight, "OBSERVATION_HEIGHT"); if (!h.ok) return h; }
  const ordered = [...observations].sort((l, r) => Number(l.jHeight) - Number(r.jHeight)), byHeight = new Map<number, string>();
  let root = start;
  for (const o of ordered) {
    const leaf = jHistoryLeaf(o);
    if (!leaf.ok) return leaf;
    const h = Number(o.jHeight), existing = byHeight.get(h);
    if (existing !== undefined && existing !== leaf.value) return err(`J_HISTORY_EQUIVOCATION_AT_HEIGHT:${h}`);
    if (existing !== undefined) continue;
    byHeight.set(h, leaf.value);
    root = keccak256Hex(abiEncode([A.b32(jHistoryText("xln:j-history-fold:v1")), A.b32(root), A.b32(leaf.value)]));
  }
  return ok(root);
});
/** og JurisdictionEventBlock after normalizeStrictJEventBlock: canonical events in canonical order, dispute evidence with its hash. */
type JRangeBlock = { readonly blockNumber: number; readonly blockHash: string; readonly eventsHash: string; readonly events: readonly WireJEvent[]; readonly disputeFinalizationEvidence?: readonly unknown[] | undefined; readonly disputeFinalizationEvidenceHash?: string | undefined };
const jBlockIdentity = (jurisdictionRef: string) => (b: JRangeBlock): JHistoryIdentity => ({ jurisdictionRef, jHeight: b.blockNumber, jBlockHash: nText(b.blockHash), eventsHash: nText(b.eventsHash), ...opt("disputeFinalizationEvidenceHash", b.disputeFinalizationEvidenceHash ? nText(b.disputeFinalizationEvidenceHash) : undefined) });
/** og canonicalJEventRangeHash: keccak(abi.encode(domain, heights[], blockHashes[], eventsHashes[], evidenceHashes[])). */
const jRangeHash = (jurisdictionRef: string, blocks: readonly JRangeBlock[]): Result<string, string> => {
  const heights: bigint[] = [], hashes: string[] = [], events: string[] = [], evidence: string[] = [];
  let previous = -1;
  for (const b of blocks) {
    const h = jHeight(b.blockNumber, "RANGE_BLOCK_HEIGHT");
    if (!h.ok) return h;
    if (h.value <= previous) return err("J_HISTORY_RANGE_BLOCK_ORDER_INVALID");
    previous = h.value;
    const e = jRoot(b.eventsHash, "RANGE_EVENTS_ROOT");
    if (!e.ok) return e;
    const x = nText(b.disputeFinalizationEvidenceHash), ev = x ? jRoot(x, "RANGE_EVIDENCE_ROOT") : ok(ZERO_WORD);
    if (!ev.ok) return ev;
    heights.push(BigInt(h.value)); hashes.push(jHistoryText(b.blockHash)); events.push(e.value); evidence.push(ev.value);
  }
  return ok(keccak256Hex(abiEncode([A.b32(jHistoryText("xln:j-history-range-body:v1")), A.array(heights.map(A.uint)), A.array(hashes.map(A.b32)), A.array(events.map(A.b32)), A.array(evidence.map(A.b32))])));
};
type JRangeSigned = { readonly entityId: string; readonly jurisdictionRef: string; readonly signerId: string; readonly baseHeight: number; readonly scannedThroughHeight: number; readonly tipBlockHash: string; readonly eventHistoryRoot: string; readonly rangeHash: string };
/** og buildJEventRangeDigest: what the Entity's active proposer signs over one J range. */
export const jRangeDigest = (d: JRangeSigned): Result<string, string> => chain(jHeight(d.baseHeight, "BASE_HEIGHT"), (base) => chain(jHeight(d.scannedThroughHeight, "SCANNED_HEIGHT"), (scanned) => {
  if (scanned <= base) return err("J_HISTORY_RANGE_EMPTY");
  if (!String(d.tipBlockHash || "").trim()) return err("J_HISTORY_RANGE_TIP_HASH_MISSING");
  return chain(jRoot(d.eventHistoryRoot, "EVENT_HISTORY_ROOT"), (root) => map(jRoot(d.rangeHash, "RANGE_ROOT"), (range) => keccak256Hex(abiEncode([
    A.b32(jHistoryText("xln:j-history-range:v1")), A.b32(jHistoryText(d.entityId)), A.b32(jHistoryText(d.jurisdictionRef)), A.b32(jHistoryText(d.signerId)), A.uint(BigInt(base)), A.uint(BigInt(scanned)), A.b32(jHistoryText(d.tipBlockHash)), A.b32(root), A.b32(range),
  ]))));
}));
/** og normalizeStrictJEventBlock: exact fields, a height above the previous block and within the scan, canonical events already in canonical order, both hashes as claimed (codes under `prefix`, J_RANGE or J_PREFIX). */
const strictJBlock = (value: unknown, previous: number, scanned: number, prefix = "J_RANGE"): Result<JRangeBlock, string> =>
  mapErr(strictJRangeBlock(value, previous, scanned), (code) => (prefix !== "J_RANGE" && /^J_RANGE_(?:BLOCK_|EVENT_|EVENTS_HASH_|EVIDENCE_)/.test(code) ? `${prefix}${code.slice("J_RANGE".length)}` : code));
const strictJRangeBlock = (value: unknown, previous: number, scanned: number): Result<JRangeBlock, string> => {
  const raw = boundaryRec(value);
  if (raw === null) return err("J_RANGE_BLOCK_INVALID");
  const fields = jExactKeys(raw, ["blockNumber", "blockHash", "eventsHash", "events"], ["disputeFinalizationEvidence", "disputeFinalizationEvidenceHash"], "J_RANGE_BLOCK_FIELDS_INVALID");
  if (!fields.ok) return err(jCode(fields.error));
  const blockNumber = Number(raw["blockNumber"]);
  if (!Number.isSafeInteger(blockNumber) || blockNumber < 0) return err("J_RANGE_BLOCK_HEIGHT_INVALID");
  if (blockNumber <= previous || blockNumber > scanned) return err("J_RANGE_BLOCK_ORDER_INVALID");
  const blockHash = nText(raw["blockHash"]), rawEvents = raw["events"];
  if (!WORD32.test(blockHash)) return err("J_RANGE_BLOCK_HASH_INVALID");
  if (!Array.isArray(rawEvents) || rawEvents.length === 0) return err("J_RANGE_EVENT_BLOCK_EMPTY");
  const canonical = canonicalJEvents(rawEvents);
  if (!canonical.ok) return err(jCode(canonical.error));
  const events = canonical.value, ordered = [...events].sort(compareJEvents);
  if (events.length !== rawEvents.length) return err("J_RANGE_EVENT_INVALID");
  if (!events.every((e, i) => jEventKey(e) === jEventKey(ordered[i] as WireJEvent))) return err("J_RANGE_EVENT_ORDER_INVALID");
  if (events.some((e) => Number(e.blockNumber) !== blockNumber || nText(e.blockHash) !== blockHash)) return err("J_RANGE_EVENT_BLOCK_MISMATCH");
  const eventsHash = jEventsHash(events);
  if (!eventsHash.ok) return err(jCode(eventsHash.error));
  const claimed = nText(raw["eventsHash"]);
  if (!WORD32.test(claimed)) return err("J_RANGE_EVENTS_HASH_INVALID");
  if (claimed !== eventsHash.value) return err("J_RANGE_EVENTS_HASH_MISMATCH");
  const rawEvidence = raw["disputeFinalizationEvidence"];
  if (rawEvidence !== undefined && !Array.isArray(rawEvidence)) return err("J_RANGE_EVIDENCE_INVALID");
  const evidence = normalizeEvidence(rawEvidence ?? []);
  if (!evidence.ok) return err(jCode(evidence.error));
  const evidenceHash = evidence.value.length > 0 ? `0x${keccakUtf8(JSON.stringify(evidence.value.map((x) => x.key)))}` : "";
  if (nText(raw["disputeFinalizationEvidenceHash"]) !== evidenceHash) return err("J_RANGE_EVIDENCE_HASH_MISMATCH");
  return ok({ blockNumber, blockHash, eventsHash: eventsHash.value, events, ...(evidence.value.length > 0 ? { disputeFinalizationEvidence: evidence.value.map((x) => x.entry) } : {}), ...(evidenceHash ? { disputeFinalizationEvidenceHash: evidenceHash } : {}) });
};
/** og JurisdictionEventData after validateJEventRangeEnvelope. */
type JRange = JRangeSigned & { readonly observedAt: number; readonly blocks: readonly JRangeBlock[]; readonly signature: string };
/** og validateJEventRangeEnvelope: the active proposer's signed, strictly canonical range for this Entity's jurisdiction (a refusal is og's code). */
const jRangeEnvelope = (state: EntityState, data: JRec): Result<JRange, string> => {
  const signerId = nText(data["from"]), jurisdictionRef = nText(data["jurisdictionRef"]);
  if (!signerId || signerId !== nText(leaderStateOf(state).activeValidatorId)) return err("J_RANGE_NOT_ACTIVE_PROPOSER");
  if (jurisdictionRef !== nText(jEventJurisdictionRef(state))) return err("J_RANGE_JURISDICTION_MISMATCH");
  const height = (v: unknown, code: string): Result<number, string> => { const n = Number(v); return Number.isSafeInteger(n) && n >= 0 ? ok(n) : err(code); };
  const word = (v: unknown, code: string): Result<string, string> => { const s = nText(v); return WORD32.test(s) ? ok(s) : err(code); };
  return chain(height(data["baseHeight"], "J_RANGE_BASE_HEIGHT_INVALID"), (baseHeight) => chain(height(data["scannedThroughHeight"], "J_RANGE_SCANNED_HEIGHT_INVALID"), (scannedThroughHeight) => {
    if (scannedThroughHeight <= baseHeight) return err("J_RANGE_HEIGHT_INVALID");
    return chain(height(data["observedAt"], "J_RANGE_OBSERVED_AT_INVALID"), (observedAt) => {
      if (observedAt !== scannedThroughHeight) return err("J_RANGE_OBSERVED_AT_MISMATCH");
      return chain(word(data["tipBlockHash"], "J_RANGE_TIP_HASH_INVALID"), (tipBlockHash) => {
        const rawBlocks = data["blocks"];
        if (!Array.isArray(rawBlocks)) return err("J_RANGE_BLOCKS_INVALID");
        const blocks: JRangeBlock[] = [];
        let previous = baseHeight;
        for (const raw of rawBlocks as readonly unknown[]) { const b = strictJBlock(raw, previous, scannedThroughHeight); if (!b.ok) return b; blocks.push(b.value); previous = b.value.blockNumber; }
        return chain(jRangeHash(jurisdictionRef, blocks), (rangeHash) => chain(word(data["rangeHash"], "J_RANGE_BODY_HASH_INVALID"), (claimed) => {
          if (claimed !== rangeHash) return err("J_RANGE_BODY_HASH_MISMATCH");
          return chain(word(data["eventHistoryRoot"], "J_RANGE_HISTORY_ROOT_INVALID"), (eventHistoryRoot) => {
            const signature = nText(data["signature"]);
            if (!signature) return err("J_RANGE_PROPOSER_SIGNATURE_INVALID");
            const signed: JRangeSigned = { entityId: state.id, jurisdictionRef, signerId, baseHeight, scannedThroughHeight, tipBlockHash, eventHistoryRoot, rangeHash };
            // og verifyAccountSignature for an EOA signer: a canonical compact signature recovering to the signer id
            return chain(jRangeDigest(signed), (digest) => (witnessSigned(digest, signature, signerId) ? ok({ ...signed, observedAt, blocks, signature }) : err("J_RANGE_PROPOSER_SIGNATURE_INVALID")));
          });
        }));
      });
    });
  }));
};
const J_RANGE_FRAME_PAYLOAD_DOMAIN = "xln.entity-frame.j-range-payload.v1", MAX_ENTITY_FRAME_J_RANGE_BYTES = 10 * 1024 * 1024;
/** og assertEntityFrameJRangeBudget for one range: a valid span, and its canonical frame payload within 10 MiB. */
const jRangeBudgetIssue = (data: JRec): string | undefined => {
  const base = Number(data["baseHeight"]), scanned = Number(data["scannedThroughHeight"]);
  if (!Number.isSafeInteger(base) || base < 0) return `J_RANGE_FRAME_BASE_HEIGHT_INVALID:${String(data["baseHeight"])}`;
  if (!Number.isSafeInteger(scanned) || scanned <= base) return `J_RANGE_FRAME_SCANNED_HEIGHT_INVALID:${String(data["scannedThroughHeight"])}`;
  const bytes = authConsensusBytes({ domain: J_RANGE_FRAME_PAYLOAD_DOMAIN, version: 1, ranges: [data] });
  if (!bytes.ok) return "CANONICAL_ENCODING_INVALID";
  return bytes.value.length > MAX_ENTITY_FRAME_J_RANGE_BYTES ? `J_RANGE_FRAME_BYTE_LIMIT_EXCEEDED:${bytes.value.length}:${MAX_ENTITY_FRAME_J_RANGE_BYTES}` : undefined;
};
type JSuffix = { readonly baseHeight: number; readonly scannedThroughHeight: number; readonly tipBlockHash: string; readonly eventHistoryRoot: string; readonly blocks: readonly JRangeBlock[] };
/** og reconcileJEventRangeWithFinalizedState: a fully applied range is a no-op; a crossing one is rebased onto the certified head and must still fold to its signed root. */
const reconcileJRange = (state: EntityState, d: JRange): Result<JSuffix | null, string> => {
  const finalized = Number(state.committed["lastFinalizedJHeight"] || 0);
  if (d.scannedThroughHeight <= finalized) return ok(null);
  if (d.baseHeight > finalized) return err(`J_RANGE_BASE_HEIGHT_AHEAD:${d.baseHeight}:${finalized}`);
  return chain(mapErr(certifiedJAnchor(state), jCode), (anchor) => {
    if (anchor !== null && d.jurisdictionRef !== anchor.jurisdictionRef) return err("J_HISTORY_FINALITY_JURISDICTION_CONFLICT");
    const blocks = d.blocks.filter((b) => b.blockNumber > finalized);
    return chain(foldJHistoryRoot(anchor?.eventHistoryRoot ?? EMPTY_J_HISTORY_ROOT, blocks.map(jBlockIdentity(d.jurisdictionRef))), (root) =>
      root !== d.eventHistoryRoot ? err("J_RANGE_HISTORY_ROOT_MISMATCH") : ok({ baseHeight: finalized, scannedThroughHeight: d.scannedThroughHeight, tipBlockHash: d.tipBlockHash, eventHistoryRoot: root, blocks }));
  });
};
/** A canonical (og-shaped) J event as the rewrite's typed JEvent; null for a type the Entity handlers below do not read typed. */
const typedJEvent = (e: WireJEvent): JEvent | null => {
  const d = e.data, s = (k: string): string => String(d[k] ?? ""), b = (k: string): bigint => BigInt(String(d[k] ?? "0"));
  const meta: JEventMeta = { ...opt("blockNumber", e.blockNumber), ...opt("blockHash", e.blockHash), ...opt("transactionHash", e.transactionHash), ...opt("logIndex", e.logIndex) };
  switch (e.type) {
    case "FoundationBootstrapped": return { type: e.type, recipient: s("recipient"), boardHash: s("boardHash"), controlTokenId: b("controlTokenId"), dividendTokenId: b("dividendTokenId"), meta };
    case "EntityRegistered": return { type: e.type, entityId: s("entityId"), entityNumber: b("entityNumber"), boardHash: s("boardHash"), meta };
    case "BoardActivated": return { type: e.type, entityId: s("entityId"), previousBoardHash: s("previousBoardHash"), newBoardHash: s("newBoardHash"), previousBoardValidUntil: b("previousBoardValidUntil"), meta };
    case "DebtCreated": return { type: e.type, debtor: s("debtor"), creditor: s("creditor"), tokenId: b("tokenId"), amount: b("amount"), debtIndex: b("debtIndex"), meta };
    case "DebtEnforced": return { type: e.type, debtor: s("debtor"), creditor: s("creditor"), tokenId: b("tokenId"), amountPaid: b("amountPaid"), remainingAmount: b("remainingAmount"), newDebtIndex: b("newDebtIndex"), meta };
    case "DebtForgiven": return { type: e.type, debtor: s("debtor"), creditor: s("creditor"), tokenId: b("tokenId"), amountForgiven: b("amountForgiven"), debtIndex: b("debtIndex"), meta };
    case "HankoBatchProcessed": return { type: e.type, entityId: s("entityId"), batchHash: s("batchHash"), nonce: b("nonce"), meta };
    case "EntityProviderActionExecuted": return { type: e.type, entityId: s("entityId"), actionNonce: b("actionNonce"), actionHash: s("actionHash"), actionKind: Number(d["actionKind"]) as 0 | 1, meta };
    case "EntityProviderActionCancelled": return { type: e.type, entityId: s("entityId"), actionNonce: b("actionNonce"), cancelledActionHash: s("cancelledActionHash"), cancelledActionKind: Number(d["cancelledActionKind"]) as 0 | 1, cancelHash: s("cancelHash"), meta };
    default: return null;
  }
};
const TOKEN_SYMBOLS: ReadonlyMap<number, string> = new Map([[1, "USDC"], [2, "WETH"], [3, "USDT"], [4, "TRX"], [5, "SUN"]]);
/** og formatTokenAmount: ethers formatUnits over the token's decimals, then its symbol (an unknown token is a plain Error). */
const tokenAmountText = (tokenId: number, amount: bigint): Result<string, EntityError> => chain(tokenDecimals(tokenId), (decimals) => {
  const d = Number(decimals), digits = (amount < 0n ? -amount : amount).toString().padStart(d + 1, "0");
  const whole = d === 0 ? digits : digits.slice(0, -d), frac = d === 0 ? "0" : digits.slice(-d).replace(/0+$/, "") || "0";
  return ok(`${amount < 0n ? "-" : ""}${whole}.${frac} ${TOKEN_SYMBOLS.get(tokenId) ?? ""}`);
});
const rawUnits = (tokenId: number, amount: unknown): string => `${BigInt(String(amount ?? "0")).toString()} raw units of token #${tokenId}`;
/** One finalized event's Entity effects: the Draft so far, the Account claims it queued, the Accounts it touched. */
type JEventStep = { readonly draft: Draft; readonly claims: readonly AccountTxTarget[]; readonly dirty: readonly string[] };
/** og applyAccountSettledJEvent: own reserve from the row, then a j_event_claim for an active Account (a non-active one only notes the suppression). */
const settledJEvent = (step: JEventStep, e: WireJEvent, blockNumber: number): Result<JEventStep, EntityError> => {
  const d = e.data, state = step.draft.state, me = lower(state.id), left = lower(d["leftEntity"]), right = lower(d["rightEntity"]);
  if (me !== left && me !== right) return ok(step);
  const tokenId = Number(d["tokenId"]), counterparty = me === left ? right : left, own = me === left ? d["leftReserve"] : d["rightReserve"];
  const reserved = own === undefined || own === null ? state : { ...state, committed: { ...state.committed, reserves: mapSet(committedReserves(state), tokenId, BigInt(String(own))) as unknown as Binary } };
  const draft: Draft = { ...step.draft, state: reserved }, child = draft.accountReplicas.get(counterparty as EntityId);
  if (child === undefined) return ok({ ...step, draft });
  if (!liveAccount(child)) return ok({ ...step, draft: jSay(draft, `⚖️ OBSERVED: non-active Account ${counterparty.slice(-4)} reserve updated; bilateral claim suppressed (${child._tag === "preparing" ? "dispute_preparing" : "disputed"})`) });
  const token: TokenSettlement = { tokenId: BigInt(tokenId), leftReserve: BigInt(String(d["leftReserve"])), rightReserve: BigInt(String(d["rightReserve"])), collateral: BigInt(String(d["collateral"])), ondelta: BigInt(String(d["ondelta"])), ...opt("eventIndex", e.eventIndex) };
  const meta: JEventMeta = { ...opt("blockNumber", e.blockNumber), ...opt("blockHash", e.blockHash), ...opt("transactionHash", e.transactionHash), ...opt("logIndex", e.logIndex) };
  const jHeight = BigInt(e.blockNumber ?? blockNumber);
  const claim: TxOf<"j_event_claim"> = { type: "j_event_claim", jHeight, jBlockHash: (e.blockHash || "") as Hash, events: [{ left, right, tokens: [token], nonce: BigInt(String(d["nonce"])), meta }], observedAt: jHeight };
  return map(tokenAmountText(tokenId, token.collateral), (coll) => ({
    draft: jSay(draft, `⚖️ OBSERVED: ${counterparty.slice(-4)} | coll=${coll} | j-block ${blockNumber} (awaiting 2-of-2)`), claims: [...step.claims, { accountId: counterparty, tx: claim }], dirty: [...step.dirty, counterparty],
  }));
};
/** og applyHankoBatchProcessedEvent on the committed jBatchState: finalize the exact pending batch (and queue its follow-up j_broadcast), or quarantine it. */
const batchProcessedJEvent = (step: JEventStep, e: Extract<JEvent, { readonly type: "HankoBatchProcessed" }>, blockNumber: number, timestamp: bigint, runtimeSeed?: string): Result<JEventStep, EntityError> => {
  const state = step.draft.state;
  if (lower(e.entityId) !== lower(state.id)) return ok(step);
  const before = entityJBatch(state), sent = before?.sentBatch, nonce = Number(e.nonce), hash = lower(e.batchHash);
  return chain(mapErr(applyHankoBatchProcessed(before, state.id, e, Number(timestamp)), (x): EntityError => ({ _tag: "entity_invariant", reason: x.reason })), (r) => {
    const draft: Draft = { ...step.draft, state: withEntityJBatch(state, r.jBatch) };
    if (sent === undefined || nonce < sent.entityNonce) return ok({ ...step, draft });
    if (sent.entityNonce !== nonce || lower(sent.batchHash) !== hash) return ok({ ...step, draft: jSay(draft, `❌ Pending jBatch nonce ${sent.entityNonce} quarantined: chain finalized different batch ${hash} at nonce ${nonce}`) });
    // og finalizePendingBatch: once sentBatch leaves, the stashed reveals flush into the draft and latch autoBroadcastDraft
    return chain(flushDeferredReveals(cjOf(draft, timestamp, runtimeSeed).host), ({ host, flushed }) => {
      const flushedJb = flushed > 0 && host.jb !== undefined ? { ...host.jb, status: "accumulating", autoBroadcastDraft: true } : host.jb;
      const drafted = flushed > 0 ? cjInto(draft, { host: { ...host, ...opt("jb", flushedJb) }, messages: [], outputs: [] }, timestamp) : draft;
      const after = entityJBatch(drafted.state), auto = flushed > 0 ? after !== undefined && after.autoBroadcastDraft === true && hasJBatchWork(after) : r.autoBroadcast;
      const leader = [...membersOf(state.quorum).keys()][0] ?? "";
      const outputs: readonly EntityOutput[] = auto ? [{ to: state.id, signerId: signerId(leader) as Address, input: { kind: "txs", timestamp, txs: [{ type: "j_broadcast", data: {} }] } }] : [];
      return ok({ ...step, draft: jSay({ ...drafted, outputs: [...drafted.outputs, ...outputs] }, `✅ jBatch finalized (nonce ${nonce}) | Block ${blockNumber}`) });
    });
  });
};
// ---- og entity/tx/j-events.ts DisputeStarted / CounterDisputeRegistered / DisputeFinalized / HashLadderRevealRegistered and j-events-htlc applyKnownHtlcSecret ----
/** One finalized event's working set: the Draft (Accounts, paybook, crontab), the og helper view (routes, jBatchState, messages, this event's outputs), queued Account txs, dirty Accounts. */
type JEv = { readonly draft: Draft; readonly cj: Cj; readonly ops: readonly AccountTxTarget[]; readonly dirty: readonly string[] };
const jevOf = (step: JEventStep, ctx: FoldContext): JEv => ({ draft: step.draft, cj: cjOf(step.draft, ctx.timestamp, ctx.runtimeSeed), ops: step.claims, dirty: step.dirty });
const jevDone = (x: JEv, ctx: FoldContext): JEventStep => ({ draft: cjInto(x.draft, x.cj, ctx.timestamp), claims: x.ops, dirty: x.dirty });
const jevSay = (x: JEv, ...messages: readonly string[]): JEv => ({ ...x, cj: cjSay(x.cj, ...messages) });
const jevBroadcast = (x: JEv): Result<JEv, EntityError> => map(localJBroadcast(x.cj), (b) => ({ ...x, cj: b.cj }));
/** og resolveDisputeAccountContext: the counterentity when we sent the event, else the sender. */
const disputePeer = (state: EntityState, data: JRec): string => { const self = lc(state.id), sender = lc(data["sender"]); return sender === self ? lc(data["counterentity"]) : sender; };
/** og buildAccountProofBodyFromJurisdictions on the Account's current state. */
const currentProofOf = (child: AccountReplica, ctx: FoldContext): Result<LocalProof, EntityError> =>
  chain(mapErr(committedView(child.state), (): EntityError => ({ _tag: "entity_invariant", reason: "ACCOUNT_PROOF_STATE_INVALID" })),
    (view) => mapErr(localProof(view, accountDt(ctx, child)), (e): EntityError => ({ _tag: "entity_invariant", reason: e._tag === "dispute_proof" && e.error._tag === "transformer" ? e.error.code : "ACCOUNT_PROOF_STATE_INVALID" })));
/** og requireFrozenAccountProofBody: the logged body hashes to the logged hash, which is the Account's current (frozen) proof body hash. */
const frozenJBody = (child: AccountReplica, raw: unknown, logged: unknown, peer: string, context: string, ctx: FoldContext): Result<ProofBody, EntityError> => {
  const hash = String(logged || "").trim().toLowerCase();
  if (!/^0x[0-9a-f]{64}$/.test(hash)) return halt(`J_EVENT_DISPUTE_FINAL_PROOFBODY_HASH_INVALID:${peer}:${hash || "missing"}`);
  if (raw === null || typeof raw !== "object") return halt(`J_EVENT_DISPUTE_FINAL_PROOFBODY_INVALID:${peer}:${hash}`);
  const body = proofBodyOfOg(raw as OgRow), computed = proofBodyHash(body).toLowerCase();
  if (computed !== hash) return halt(`J_EVENT_DISPUTE_FINAL_PROOFBODY_HASH_MISMATCH:${peer}:${hash}:${computed}`);
  return chain(currentProofOf(child, ctx), (current) => (current.bodyHash.toLowerCase() !== hash ? halt(`DISPUTE_FROZEN_ACCOUNT_STATE_MISMATCH:${context}:${peer}:${hash}:${current.bodyHash}`) : ok(body)));
};
/**
 * og applyEntityAccountEnvelopeUpdate(applyDisputeStarted | applyDisputeFinality): the Account's unilateral external finality (og's plain Error
 * texts). og's applyAccountDisputeFinality refuses nothing: an unsafe finalized nonce or token id halts upstream in applyDisputeFinalizedJEvent
 * (J_EVENT_DISPUTE_FINAL_NONCE_* / J_EVENT_DISPUTE_FINAL_TOKEN_ID_INVALID), which disputeFinalizedJEvent checks first, so those Account refusals
 * carry og's upstream text for the same fault. Any other refusal is unreachable here: the envelope is the Account's own terms (og
 * createAccountDisputeFinalityInput) and every replica tag takes external_finality.
 */
const childFinality = (d: Draft, peer: string, child: AccountReplica, finality: AccountFinality, ctx: FoldContext): Result<Draft, EntityError> => {
  const fail = (e: AccountReplicaError): EntityError => {
    if (e._tag === "finality" && finality.kind === "dispute_finalized") {
      const bad = finality.finalizedTokenIds.findIndex((t) => !Number.isSafeInteger(t) || t < 0);
      return { _tag: "entity_invariant", reason: e.reason === "token_id" && bad >= 0 ? `J_EVENT_DISPUTE_FINAL_TOKEN_ID_INVALID:${peer}:${bad}:${String(finality.finalizedTokenIds[bad])}` : `J_EVENT_DISPUTE_FINAL_NONCE_INVALID:${String(finality.finalizedJNonce)}` };
    }
    if (e._tag !== "finality" || finality.kind !== "dispute_started") return { _tag: "entity_invariant", reason: `ACCOUNT_EXTERNAL_FINALITY_REFUSED:${e._tag}${"reason" in e ? `:${String(e.reason)}` : ""}` };
    const f = finality, c = child.state.terms.disputeConfig;
    const reason = e.reason === "initial_nonce" ? `ACCOUNT_DISPUTE_INITIAL_NONCE_INVALID:${f.initialNonce}` : e.reason === "j_nonce" ? `ACCOUNT_DISPUTE_J_NONCE_INVALID:${f.jNonce}` : e.reason === "observed_block" ? `ACCOUNT_DISPUTE_OBSERVED_BLOCK_INVALID:${f.observedBlockNumber}`
      : e.reason === "timeout" ? `ACCOUNT_DISPUTE_TIMEOUT_INVALID:${f.disputeStartTimestamp}:${f.disputeTimeout}`
        : `ACCOUNT_DISPUTE_CLOCK_MISMATCH:${f.disputeStartTimestamp}:${f.disputeTimeout}:${f.leftResponseSeconds}:${f.rightResponseSeconds}:${c.leftResponseSeconds}:${c.rightResponseSeconds}`;
    return { _tag: "entity_invariant", reason };
  };
  return chain(mapErr(partyOf(replicaId(child), d.state.id), (): EntityError => ({ _tag: "entity_invariant", reason: `ACCOUNT_DISPUTE_PARTY_INVALID:${peer}` })), (party) =>
    map(mapErr(applyAccountInput(child, { kind: "external_finality", ...envelopeOf(child.state.terms, party), finality }, { verify: ctx.verify, self: d.state.id, now: ctx.timestamp, deltaTransformer: accountDt(ctx, child) }), fail),
      (a) => ({ ...d, ...putChild(d.state, d.accountReplicas, peer as EntityId, a.replica) })));
};
const requireDt = (ctx: FoldContext, child: AccountReplica): Result<string, EntityError> => { const dt = accountDt(ctx, child); return dt.ok ? ok(dt.value) : halt(dt.error); };
/** og queueSourceHubClaimRegistration (j-events.ts): every queued or deferred Source hub claim wakes the local jBatch and says so. */
const eventSourceClaims = (x: JEv, peer: string, body: Pick<ProofBody, "transformers">, transformer: string): Result<JEv, EntityError> =>
  chain(sourceHubClaims(x.cj.host, peer, body, transformer), ({ host, claims }) => foldResult(claims, { ...x, cj: cjHost(x.cj, host) }, (acc, c): Result<JEv, EntityError> => {
    if (c.result !== "queued" && c.result !== "deferred-batch-pending") return ok(acc);
    return map(jevBroadcast(acc), (b) => jevSay(b, c.result === "queued" ? `🌉 Cross-j claim ${c.routeId}: registering source reveal ratio ${c.fillRatio}` : `⏳ Cross-j claim ${c.routeId}: reveal deferred until disputeFinalizations leave the jBatch`));
  }));
/**
 * og applyKnownHtlcSecret: an unknown hashlock resolves every inbound lock carrying it; a known route learns the secret once (earning its
 * pending fee), then resolves its inbound lock or relays a cross-j target claim (resolveHtlcLock).
 */
const knownSecret = (x: JEv, hashlockRaw: string, secretRaw: string, blockNumber: number): Result<JEv, EntityError> => {
  const hashlock = lc(hashlockRaw), secret = lc(secretRaw), said = `🔓 HTLC reveal observed: ${hashlock.slice(0, 10)}... | Block ${blockNumber}`;
  const state = x.draft.state, paybook = state.paybook ?? EMPTY_PAYBOOK, route = paybook.entries.get(hashlock);
  if (route === undefined) {
    const found: AccountTxTarget[] = [];
    for (const [peer, child] of x.draft.accountReplicas) {
      const weAreLeft = child.state.account.id.left === state.id;
      for (const lock of child.state.locks.values()) {
        if (lc(lock.hashlock) !== hashlock || lock.senderIsLeft === weAreLeft) continue;
        found.push({ accountId: peer, tx: { type: "htlc_resolve", lockId: lock.lockId, outcome: "secret", secret } as AccountTx });
      }
    }
    return ok(found.length > 0 ? jevSay({ ...x, ops: [...x.ops, ...found] }, said) : x);
  }
  if (route.secret) return ok(jevSay(x, said));
  const earns = Boolean(route.pendingFee), { pendingFee: _fee, ...rest } = route, learned: PaybookEntry = earns ? { ...rest, secret } : { ...route, secret };
  const nextBook: Paybook = { entries: mapSet(paybook.entries, hashlock, learned), feesEarned: earns ? paybook.feesEarned + (route.pendingFee as bigint) : paybook.feesEarned };
  const learnedX: JEv = { ...x, draft: { ...x.draft, state: { ...state, paybook: nextBook } } };
  if (hasInbound(route)) return ok(jevSay({ ...learnedX, ops: [...learnedX.ops, { accountId: route.inboundEntity as string, tx: { type: "htlc_resolve", lockId: hashlock, outcome: "secret", secret } as AccountTx }] }, said));
  const relay = route.crossJurisdictionRelay as { readonly routeId: string; readonly targetEntityId: string; readonly targetCounterpartyEntityId: string; readonly targetLockId: string; readonly targetSignerId?: string; readonly fillRatio: number } | undefined;
  if (relay === undefined) return ok(jevSay(learnedX, said));
  const tx = { type: "resolveHtlcLock", data: { counterpartyEntityId: relay.targetCounterpartyEntityId, lockId: relay.targetLockId, secret, crossJurisdictionRouteId: relay.routeId, description: `Cross-j ${relay.routeId} target claim ${relay.fillRatio}/${MAX_FILL}` } } as EntityTx;
  const out: CjOut = { kind: "cross", entityId: String(relay.targetEntityId ?? "").trim().toLowerCase(), signerId: String(relay.targetSignerId || "").trim().toLowerCase(), txs: [tx] };
  // og buildCrossJurisdictionEntityOutput: both route ends are committed before the relay can exist
  if (!out.entityId || !out.signerId) return halt(`CROSS_J_ENTITY_OUTPUT_ROUTE_MISSING:${out.entityId || "entity"}:${out.signerId || "signer"}`);
  return ok(jevSay({ ...learnedX, cj: { ...learnedX.cj, outputs: [...learnedX.cj.outputs, out] } }, said));
};
/** og batchAddCounterDispute on the draft: one row per counterparty, only ever raised (a LEFT role may replace RIGHT at equal nonce); limits as og. */
const addCounterRow = (jb: CjJBatch, row: { readonly counterentity: string; readonly initialNonce: number; readonly initialProofbodyHash: string; readonly counterNonce: number; readonly proposerIsLeft: boolean; readonly counterProofbody: Binary; readonly sig: string }): Result<CjJBatch, EntityError> => {
  const cp = lc(row.counterentity), rows = (jb.batch["counterDisputes"] ?? []) as readonly (typeof row)[], at = rows.findIndex((r) => lc(r.counterentity) === cp);
  const bodyHash = (b: Binary): string => proofBodyHash(proofBodyOfOg(b as OgRow)).toLowerCase();
  const put = (next: readonly (typeof row)[]): CjJBatch => ({ ...jb, batch: { ...jb.batch, counterDisputes: next as unknown as readonly Binary[] }, ...(jb.status === "empty" ? { status: "accumulating" } : {}) });
  if (at >= 0) {
    const existing = rows[at] as typeof row;
    if (existing.initialNonce !== row.initialNonce || lc(existing.initialProofbodyHash) !== lc(row.initialProofbodyHash)) return invariant(`J_COUNTER_DISPUTE_INITIAL_BINDING_CONFLICT:${cp}`);
    if (row.counterNonce < existing.counterNonce) return invariant(`J_COUNTER_DISPUTE_NONCE_REGRESSION:${cp}`);
    if (row.counterNonce === existing.counterNonce) {
      if (row.proposerIsLeft !== existing.proposerIsLeft) { if (!row.proposerIsLeft) return invariant(`J_COUNTER_DISPUTE_ROLE_REGRESSION:${cp}`); }
      else if (bodyHash(row.counterProofbody) !== bodyHash(existing.counterProofbody)) return invariant(`J_COUNTER_DISPUTE_HASH_CONFLICT:${cp}:${row.counterNonce}`);
      else return ok(jb);
    }
    return ok(put(rows.map((r, i) => (i === at ? row : r))));
  }
  const total = BATCH_FIELDS.reduce((n, f) => n + (jb.batch[f] ?? []).length, 0) + 1;
  if (total > J_BATCH_LIMITS.maxTotalOps) return invariant(`J_BATCH_LIMIT_EXCEEDED: counterDispute would exceed total ops ${total}/${J_BATCH_LIMITS.maxTotalOps}`);
  if (rows.length + 1 > J_BATCH_LIMITS.maxCounterDisputes) return invariant(`J_BATCH_LIMIT_EXCEEDED: counterDisputes ${rows.length + 1}/${J_BATCH_LIMITS.maxCounterDisputes}`);
  return ok(put([...rows, row]));
};
/**
 * og queueSelectedPullCounterProof: the non-starter holding a newer counterparty proof (og selectFinalProof) that carries a Pull queues its
 * counterDispute before T, bundles the Source hub claims of that body and latches autoBroadcastDraft behind a sent batch.
 */
const lockPullCounter = (x: JEv, peer: string, ctx: FoldContext): Result<{ readonly x: JEv; readonly queued: boolean }, EntityError> => {
  const child = x.draft.accountReplicas.get(peer as EntityId) as DisputedAccount, active = child.active as ActiveDispute, w = child.dispute.counterparty, self = x.draft.state.id;
  const callerIsStarter = (child.state.account.id.left === self) === active.startedByLeft;
  if (callerIsStarter || w === undefined || !w.hanko || w.hanko === "0x" || !w.proofBodyHash) return ok({ x, queued: false });
  const usable = typeof w.proposerIsLeft === "boolean" && (w.proofNonce > active.initialNonce || (w.proofNonce === active.initialNonce && w.proposerIsLeft && !active.initialProposerIsLeft));
  const finalNonce = usable ? w.proofNonce : active.initialNonce, proposerIsLeft = usable ? w.proposerIsLeft : active.initialProposerIsLeft;
  return chain(currentProofOf(child, ctx), (current) => {
    if (finalNonce <= 0) return ok({ x: jevSay(x, `❌ Invalid dispute finalNonce=${finalNonce} — must be > 0`), queued: false });
    const finalHash = usable ? w.proofBodyHash : active.initialProofbodyHash;
    if (current.bodyHash.toLowerCase() !== finalHash.toLowerCase()) return halt(`DISPUTE_FROZEN_ACCOUNT_STATE_MISMATCH:finalize:${peer}:${finalHash}:${current.bodyHash}`);
    if (!usable) return ok({ x, queued: false });
    return chain(requireDt(ctx, child), (dt) => chain(proofBodyHasPulls(current.body, dt), (hasPulls): Result<{ readonly x: JEv; readonly queued: boolean }, EntityError> => {
      if (!hasPulls) return ok({ x, queued: false });
      // og verifyCounterProofIdentity
      if (w.hash) {
        if (x.draft.state.jurisdictionConfig === undefined) return halt("DISPUTE_COUNTER_FINALIZE_DEPOSITORY_MISSING");
        const view = committedView(child.state), expected = view.ok ? accountDisputeHash({ ...view.value, domain: x.draft.state.jurisdiction }, finalHash, finalNonce, proposerIsLeft) : undefined;
        if (expected === undefined || !expected.ok) return halt("DISPUTE_COUNTER_FINALIZE_DEPOSITORY_MISSING");
        if (lc(w.hash) !== lc(expected.value)) return halt(`DISPUTE_COUNTER_FINALIZE_HASH_MISMATCH:${peer}:${w.hash}:${expected.value}`);
      }
      const nowSec = Math.floor(Number(ctx.timestamp) / 1000);
      if (nowSec >= active.disputeTimeout) return ok({ x: jevSay(x, `❌ Pull counter-proof ${finalNonce} missed T=${active.disputeTimeout}`), queued: false });
      const jb = x.cj.host.jb ?? (initJBatch() as unknown as CjJBatch);
      return chain(addCounterRow(jb, { counterentity: peer, initialNonce: active.initialNonce, initialProofbodyHash: active.initialProofbodyHash, counterNonce: finalNonce, proposerIsLeft, counterProofbody: ogProofBody(current.body), sig: w.hanko }), (added) => {
        const latched = added.sentBatch !== undefined ? { ...added, autoBroadcastDraft: true } : added;
        return map(eventSourceClaims({ ...x, cj: cjHost(x.cj, { ...x.cj.host, jb: latched }) }, peer, current.body, dt), (claimed) => ({ x: jevSay(claimed, `🛡️ Locked newer Pull state N${finalNonce} before dispute T`), queued: true }));
      });
    }));
  });
};
/** The wire DisputeStarted as the Account finality builder reads it. */
const startedEventOf = (e: WireJEvent, body: ProofBody): Extract<JEvent, { readonly type: "DisputeStarted" }> & { readonly batchNonce?: number | undefined } => {
  const d = e.data, s = (k: string): string => String(d[k] ?? ""), b = (k: string): bigint => BigInt(String(d[k] ?? "0"));
  return { type: "DisputeStarted", sender: s("sender"), counterentity: s("counterentity"), nonce: b("nonce"), proposerIsLeft: d["proposerIsLeft"] === true, proofbodyHash: s("proofbodyHash"), watchSeed: s("watchSeed"),
    starterInitialArguments: s("starterInitialArguments"), starterCounterArguments: s("starterCounterArguments"), starterCounterProofCommitment: s("starterCounterProofCommitment"), disputeTimeout: b("disputeTimeout"),
    disputeStartTimestamp: b("disputeStartTimestamp"), leftResponseSeconds: b("leftResponseSeconds"), rightResponseSeconds: b("rightResponseSeconds"), initialProofbody: body, ...opt("batchNonce", typeof d["batchNonce"] === "number" ? d["batchNonce"] : undefined) };
};
/**
 * og applyDisputeStartedJEvent: the frozen body check, the Account's dispute_started finality, the nonce sync and stale start retirement, the
 * Pull counter-proof lock, the reveal flush (one continuation), then the follow-ups: the starter's secrets (applyKnownHtlcSecret), the Target
 * recovery plan, the Source hub claims, the sibling fanout, the `⚔️ DISPUTE` message and the dispute-deadline hook (1 ms / 5 s).
 */
const disputeStartedJEvent = (step: JEventStep, e: WireJEvent, ctx: FoldContext, blockNumber: number): Result<JEventStep, EntityError> => {
  const data = e.data, state = step.draft.state, self = lc(state.id), sender = lc(data["sender"]), peer = disputePeer(state, data), child = step.draft.accountReplicas.get(peer as EntityId);
  if (child === undefined) return ok(step);
  return chain(frozenJBody(child, data["initialProofbody"], data["proofbodyHash"], peer, "jEvent.disputeStarted", ctx), (initialBody) =>
    chain(mapErr(disputeStartedInput(startedEventOf(e, initialBody), blockNumber, String(data["proofbodyHash"]).toLowerCase()), (x): EntityError => ({ _tag: "entity_invariant", reason: x.reason })), (finality) =>
      chain(childFinality(step.draft, peer, child, finality, ctx), (frozen): Result<JEventStep, EntityError> => {
        const weAreStarter = sender === self, disputeTimeout = Number(data["disputeTimeout"]);
        let x = jevOf({ ...step, draft: frozen }, ctx);
        const retired = startedRetirement(x.cj.host.jb as RetiringJBatch | undefined, self, { sender, counterentity: String(data["counterentity"]), proofbodyHash: String(data["proofbodyHash"]), ...opt("batchNonce", typeof data["batchNonce"] === "number" ? data["batchNonce"] : undefined) });
        x = { ...x, cj: cjHost(x.cj, { ...x.cj.host, ...opt("jb", retired.jb as unknown as CjJBatch | undefined) }) };
        if (retired.synced !== undefined) x = jevSay(x, retired.synced);
        if (retired.broadcast) { const b = jevBroadcast(x); if (!b.ok) return b; x = b.value; }
        if (retired.removed > 0) x = jevSay(x, `🧹 Removed ${retired.removed} stale dispute-start op(s) for ${peer.slice(-4)}`);
        return chain(lockPullCounter(x, peer, ctx), ({ x: locked, queued }) => chain(flushDeferredReveals(locked.cj.host), ({ host, flushed }) => {
          const flushedX: JEv = { ...locked, cj: cjHost(locked.cj, host), dirty: [...locked.dirty, peer] };
          return chain((flushed > 0 || queued) && retired.removed === 0 ? jevBroadcast(flushedX) : ok(flushedX), (woke) => chain(requireDt(ctx, child), (dt) => {
            const secrets = starterSecrets(String(data["starterInitialArguments"] || "0x"));
            const learned = foldResult(secrets, woke, (acc, secret) => knownSecret(acc, hashHtlcSecret(secret) ?? "", secret, blockNumber));
            if (!learned.ok) return learned;
            let y = learned.value;
            if (!weAreStarter && !queued) {
              const visible = y.draft.accountReplicas.get(peer as EntityId), visibleActive = visible === undefined ? undefined : activeOf(visible);
              if (visible === undefined || visibleActive === undefined) return halt(`CROSS_J_TARGET_ACTIVE_DISPUTE_MISSING:${peer}`);
              const account = y.cj.host.accounts.get(peer);
              if (account === undefined) return halt(`CROSS_J_TARGET_ACTIVE_DISPUTE_MISSING:${peer}`);
              const plan = planTargetRecovery(y.cj.host, account, peer, (visibleActive as ActiveDispute).crossJurisdictionRecovery?.resultsByPullId ?? {});
              if (!plan.ok) return plan;
              if (plan.value !== null) y = { ...y, draft: { ...y.draft, ...putChild(y.draft.state, y.draft.accountReplicas, peer as EntityId, { ...(visible as DisputedAccount), active: { ...(visibleActive as ActiveDispute), crossJurisdictionRecovery: plan.value.recovery } }) } };
            }
            return chain(queued ? ok(y) : eventSourceClaims(y, peer, initialBody, dt), (claimed) => chain(siblingFanout(claimed.cj, peer, blockNumber), ({ cj }) => {
              const said = jevSay({ ...claimed, cj }, `⚔️ DISPUTE ${weAreStarter ? "STARTED" : "vs us"} with ${peer.slice(-4)}, timeout: unix ${disputeTimeout}`);
              const t = Number(ctx.timestamp), logical = Number.isFinite(t) && t >= 0 ? t : 0;
              return map(scheduleDisputeDeadline(said.draft.state, peer, logical + (weAreStarter ? 1 : 5000)), (hooked) => jevDone({ ...said, draft: { ...said.draft, state: hooked } }, ctx));
            }));
          }));
        }));
      })));
};
/** og scrubCounterDisputesSupersededByObserved: a strictly better queued branch survives (higher nonce, or LEFT over RIGHT at equal nonce); an exact retry only where `preserveExact`. */
const scrubSuperseded = (peer: string, nonce: number, left: boolean, hash: string, preserveExact: boolean): Scrub => scrubRows("counterDisputes", (r) => {
  const row = r as { readonly counterentity?: unknown; readonly counterNonce?: unknown; readonly proposerIsLeft?: unknown; readonly counterProofbody?: unknown };
  if (rowId(row.counterentity) !== peer) return true;
  const n = Number(row.counterNonce);
  if (n > nonce) return true;
  if (n < nonce) return false;
  if (row.proposerIsLeft !== left) return row.proposerIsLeft === true && !left;
  return preserveExact && proofBodyHash(proofBodyOfOg(row.counterProofbody as OgRow)).toLowerCase() === hash;
});
/**
 * og applyCounterDisputeRegisteredJEvent: the Account's active dispute takes the selected counter-proof (never regressing), the Target recovery
 * refreshes or re-plans, the selected body's Source hub claims queue, superseded counter-proof operations retire (a sent batch keeps its exact retry).
 */
const counterRegisteredJEvent = (step: JEventStep, e: WireJEvent, ctx: FoldContext): Result<JEventStep, EntityError> => {
  const data = e.data, state = step.draft.state, peer = disputePeer(state, data), nonce = Number(data["nonce"]), hash = lc(data["proofbodyHash"]), left = data["proposerIsLeft"] === true;
  const child = step.draft.accountReplicas.get(peer as EntityId), active = child === undefined ? undefined : activeOf(child);
  if (child === undefined || active === undefined) return invariant(`COUNTER_DISPUTE_ACTIVE_ACCOUNT_MISSING:${peer}`);
  return chain(frozenJBody(child, data["counterProofbody"], data["proofbodyHash"], peer, "jEvent.counterDisputeRegistered", ctx), (body): Result<JEventStep, EntityError> => {
    if (nonce < active.initialNonce || (nonce === active.initialNonce && (!left || active.initialProposerIsLeft))) return invariant(`COUNTER_DISPUTE_NONCE_STALE:${nonce}:${active.initialNonce}`);
    const a = active as ActiveDispute;
    if (a.selectedCounterNonce !== undefined) {
      if (nonce < a.selectedCounterNonce) return invariant(`COUNTER_DISPUTE_NONCE_REGRESSION:${nonce}:${a.selectedCounterNonce}`);
      if (nonce === a.selectedCounterNonce && left === a.selectedCounterProposerIsLeft && hash !== lc(a.selectedCounterProofbodyHash)) return invariant(`COUNTER_DISPUTE_HASH_CONFLICT:${nonce}`);
      if (nonce === a.selectedCounterNonce && left !== a.selectedCounterProposerIsLeft && !left) return invariant(`COUNTER_DISPUTE_ROLE_REGRESSION:${nonce}`);
    }
    const x0 = jevOf(step, ctx), account = x0.cj.host.accounts.get(peer);
    if (account === undefined) return invariant(`COUNTER_DISPUTE_ACTIVE_ACCOUNT_MISSING:${peer}`);
    const planned = a.crossJurisdictionRecovery !== undefined ? refreshTargetRecovery(x0.cj.host, account, peer, a.crossJurisdictionRecovery) : planTargetRecovery(x0.cj.host, account, peer, {});
    if (!planned.ok) return planned;
    const { crossJurisdictionRecovery: _r, ...base } = a;
    const selected: ActiveDispute = { ...base, selectedCounterNonce: nonce, selectedCounterProofbodyHash: hash, selectedCounterProposerIsLeft: left, ...(planned.value === null ? {} : { crossJurisdictionRecovery: planned.value.recovery }) };
    const x1: JEv = { ...x0, draft: { ...x0.draft, ...putChild(x0.draft.state, x0.draft.accountReplicas, peer as EntityId, { ...(child as DisputedAccount), active: selected }) } };
    return chain(requireDt(ctx, child), (dt) => chain(eventSourceClaims(x1, peer, body, dt), (claimed) => {
      const jb = claimed.cj.host.jb as RetiringJBatch | undefined;
      let x = claimed, removed = 0;
      if (jb !== undefined) {
        const drafted = retireJBatch(jb, [scrubSuperseded(peer, nonce, left, hash, false)], true);
        let next = drafted.jb, removedSent = 0;
        const sent = next.sentBatch;
        if (sent !== undefined) {
          const s2 = scrubSuperseded(peer, nonce, left, hash, true)(sent.batch);
          if (s2.removed > 0) {
            removedSent = s2.removed;
            const { sentBatch: _s, ...unsent } = next, queue = rowsEmpty(s2.batch) ? unsent.recoveryBatches : [s2.batch, ...(unsent.recoveryBatches ?? [])];
            const work = (queue ?? []).some((b) => !rowsEmpty(b)) || !rowsEmpty(unsent.batch);
            next = { ...unsent, ...(queue === undefined ? {} : { recoveryBatches: queue }), status: work ? "accumulating" : "empty" };
          }
        }
        removed = drafted.removed + removedSent;
        x = { ...x, cj: cjHost(x.cj, { ...x.cj.host, jb: next as unknown as CjJBatch }) };
        if (removedSent > 0) { const b = jevBroadcast(x); if (!b.ok) return b; x = b.value; }
      }
      if (removed > 0) x = jevSay(x, `🧹 Retired ${removed} superseded counter-proof operation(s)`);
      return ok(jevDone(jevSay({ ...x, dirty: [...x.dirty, peer] }, `🛡️ Counter-proof N${nonce} locked for ${peer.slice(-4)}`), ctx));
    }));
  });
};
type FinalitySettlement = { readonly orderId: string; readonly settledRatio: number };
/** og resolveCrossJurisdictionFinalitySettlements: per live route with a leg on this Account, the exact signed Pull's settled ratio (0 when the proof predates it). */
const finalitySettlements = (h: CjHost, localStack: string | undefined, peer: string, active: ActiveDispute | QueuedDispute | undefined, body: ProofBody, dt: string): Result<readonly FinalitySettlement[], EntityError> =>
  foldResult<readonly FinalitySettlement[], CrossRoute, EntityError>(routesOf(h), [], (acc, route) => {
    if (isCrossTerminal(route.status)) return ok(acc);
    return chain(fatalCross(finalizedRouteLeg({ route, self: h.id, counterparty: peer, ...opt("localStack", localStack) })), (role) => {
      if (role === undefined) return ok(acc);
      const expected = role === "source" ? route.sourcePull : route.targetPull;
      if (expected === undefined) return route.status === "intent" && !route.sourcePull && !route.targetPull ? ok([...acc, { orderId: route.orderId, settledRatio: 0 }]) : halt(`CROSS_J_FINALITY_PULL_MISSING:${route.orderId}:${role}`);
      const record = role === "source" ? route.sourceRegistryRecord : route.targetRegistryRecord;
      return chain(fatalCross(findSignedProofBodyPull(body, expected, role === "target", dt)), (signed) => signed === undefined ? ok([...acc, { orderId: route.orderId, settledRatio: 0 }])
        : map(fatalCross(finalizedPullFillRatio({ active: active as { readonly disputeStartTimestamp?: unknown; readonly disputeTimeout?: unknown } | undefined, proofbody: body, transformerAddress: dt, expectedPull: expected, targetRole: role === "target", ...opt("record", record) })),
          (settledRatio) => [...acc, { orderId: route.orderId, settledRatio }]));
    });
  });
/** og terminalizeCrossJurisdictionRoutesOnFinality: parked reveals go; an unlocked intent cancels; every other route clears and ends at its settled ratio. */
const terminalizeOnFinality = (x: JEv, settlements: readonly FinalitySettlement[]): Result<JEv, EntityError> => foldResult(settlements, x, (acc, { orderId, settledRatio }): Result<JEv, EntityError> => {
  const routes = acc.cj.host.swaps, at = acc.cj.host.timestamp;
  if (routes === undefined) return halt(`CROSS_J_FINALITY_COLLECTION_MISSING:${orderId}`);
  const stored = routes.get(orderId);
  if (stored === undefined) return halt(`CROSS_J_FINALITY_ROUTE_FORK_MISSING:${orderId}`);
  const { pendingSourceRegistryReveal: _s, pendingTargetRegistryReveal: _t, ...route } = stored;
  const put = (r: CrossRoute, message: string): JEv => jevSay({ ...acc, cj: cjHost(acc.cj, cjPutRoute(acc.cj.host, orderId, r)) }, message);
  if (route.status === "intent" && !route.sourcePull && !route.targetPull) return map(transitionE(route, "cancelled", at), (r) => put(r, `🌉 Cross-j route ${orderId} cancelled before Pull lock on Account finality`));
  return chain(route.status !== "clearing" ? transitionE(route, "clearing", at) : ok(route as CrossRoute), (clearing) => {
    const terminal: CrossStatus = settledRatio > 0 ? "settled" : isCrossExpired(clearing, at) ? "expired" : "cancelled";
    return map(transitionE(clearing, terminal, at), (ended) => put(terminal === "settled" ? { ...ended, settledAt: at } : ended, `🌉 Cross-j route ${orderId} terminal after dispute finality: ${terminal}`));
  });
});
/** og resolveFinalizationEvidence: the block's one reducer record for this finalization sets the finalized nonce (an exact unsigned initial unilateral, or none on the initial body, spends one more). */
const finalizedNonceOf = (child: AccountReplica, data: JRec, evidence: readonly unknown[], finalHash: string): Result<{ readonly finalizedJNonce: number; readonly initialNonce: number }, EntityError> => {
  const sender = lc(data["sender"]), counter = lc(data["counterentity"]), initialHash = lc(data["initialProofbodyHash"]);
  const hits = evidence.map((v) => recOf(v) ?? {}).filter((v) => lc(v["sender"]) === sender && lc(v["counterentity"]) === counter && String(v["initialNonce"]) === String(data["initialNonce"])
    && lc(v["initialProofbodyHash"]) === initialHash && lc(v["finalProofbodyHash"]) === finalHash);
  if (hits.length > 1) return halt(`J_EVENT_DISPUTE_FINALIZATION_EVIDENCE_AMBIGUOUS:${sender}:${counter}:${String(data["initialNonce"])}`);
  const uint = (v: unknown, code: string): Result<number, EntityError> => { const n = Number(v); return Number.isSafeInteger(n) && n >= 0 && String(v).trim() !== "" ? ok(n) : invariant(`${code}:${String(v)}`); };
  const bump = (n: number): Result<number, EntityError> => (n >= Number.MAX_SAFE_INTEGER ? invariant(`J_EVENT_DISPUTE_FINAL_NONCE_OVERFLOW:${n}`) : ok(n + 1));
  return chain(uint(data["initialNonce"], "J_EVENT_DISPUTE_INITIAL_NONCE_INVALID"), (initialNonce) => {
    const primary = hits[0], onInitial = finalHash === initialHash, active = activeOf(child) as ActiveDispute | undefined;
    const eventNonce: Result<number, EntityError> = primary === undefined ? (onInitial ? bump(initialNonce) : ok(initialNonce)) : chain(uint(primary["finalNonce"], "J_EVENT_DISPUTE_FINAL_NONCE_INVALID"), (finalNonce) => {
      const sig = lc(primary["sig"] ?? ""), unsigned = sig === "" || sig === "0x";
      const selected = active?.selectedCounterNonce === finalNonce && active.selectedCounterProposerIsLeft === primary["proposerIsLeft"] && lc(active.selectedCounterProofbodyHash) === finalHash;
      return unsigned && !selected && finalNonce === initialNonce && primary["proposerIsLeft"] === active?.initialProposerIsLeft && onInitial ? bump(initialNonce) : ok(finalNonce);
    });
    return map(eventNonce, (n) => ({ finalizedJNonce: Math.max(Number(child.state.jNonce ?? 0), n), initialNonce }));
  });
};
/**
 * og applyDisputeFinalizedJEvent: the frozen final body, the reducer evidence, then og applyResolvedDisputeFinality (nonce sync, the cross-j
 * settlements captured before the Account's finality, stale settlement intent, `✅ DISPUTE FINALIZED` and the hook's cancellation, route
 * terminalization) and og retireFinalizedDisputeState.
 */
const disputeFinalizedJEvent = (step: JEventStep, e: WireJEvent, ctx: FoldContext, evidence: readonly unknown[]): Result<JEventStep, EntityError> => {
  const data = e.data, state = step.draft.state, self = lc(state.id), sender = lc(data["sender"]), peer = disputePeer(state, data), child = step.draft.accountReplicas.get(peer as EntityId);
  if (child === undefined) return ok(step);
  return chain(frozenJBody(child, data["finalProofbody"], data["finalProofbodyHash"], peer, "jEvent.disputeFinalized", ctx), (body) => {
    const finalHash = lc(data["finalProofbodyHash"]).trim(), tokenIds: number[] = [];
    for (const [i, v] of body.tokenIds.entries()) { const n = Number(v); if (!Number.isSafeInteger(n) || n < 0) return halt(`J_EVENT_DISPUTE_FINAL_TOKEN_ID_INVALID:${peer}:${i}:${String(v)}`); tokenIds.push(n); }
    return chain(finalizedNonceOf(child, data, evidence, finalHash), ({ finalizedJNonce, initialNonce }) => {
      const batchNonce = typeof data["batchNonce"] === "number" ? data["batchNonce"] : undefined;
      let x = jevOf(step, ctx);
      const synced = syncBatchNonce(x.cj.host.jb as RetiringJBatch | undefined, sender, self, batchNonce);
      x = { ...x, cj: cjHost(x.cj, { ...x.cj.host, ...opt("jb", synced.jb as unknown as CjJBatch | undefined) }), dirty: [...x.dirty, peer] };
      if (synced.message !== undefined) x = jevSay(x, synced.message);
      return chain(requireDt(ctx, child), (dt) => chain(finalitySettlements(x.cj.host, stackIdOf(state.jurisdiction) || undefined, peer, activeOf(child), body, dt), (settlements) => {
        const hadActive = activeOf(child) !== undefined, hadWorkspace = child.state.settlement !== undefined, removedTxs = ("mempool" in child ? (child.mempool as readonly WireAccountTx[]) : []).filter((t) => t.type === "settle_transition").length;
        return chain(childFinality(x.draft, peer, child, { kind: "dispute_finalized", finalizedJNonce, finalizedTokenIds: tokenIds }, ctx), (finalized) => {
          const deferred = deferredOf(finalized.state), removedDeferred = deferred.has(peer);
          const cleared: Draft = removedDeferred ? { ...finalized, state: withDeferred(finalized.state, new Map([...deferred].filter(([k]) => k !== peer))) } : finalized;
          let y: JEv = { ...x, draft: cleared };
          if (hadWorkspace || removedDeferred || removedTxs > 0) y = jevSay(y, `🧹 Invalidated stale settlement intent after dispute finality with ${peer.slice(-4)}`);
          const unhooked = hadActive && y.draft.state.committed["crontabState"] !== undefined ? map(crontabOf(y.draft.state), (c) => withCrontab(y.draft.state, cancelHook(c, `dispute-deadline:${peer}`))) : ok(y.draft.state);
          if (!unhooked.ok) return unhooked;
          if (hadActive) y = jevSay({ ...y, draft: { ...y.draft, state: unhooked.value } }, `✅ DISPUTE FINALIZED with ${peer.slice(-4)} (nonce ${initialNonce})`);
          return chain(terminalizeOnFinality(y, settlements), (z) => {
            const retired = finalizedRetirement(z.cj.host.jb as RetiringJBatch | undefined, self, { sender, counterentity: String(data["counterentity"]), initialProofbodyHash: String(data["initialProofbodyHash"] ?? ""), ...opt("batchNonce", batchNonce) });
            let w: JEv = { ...z, cj: cjHost(z.cj, { ...z.cj.host, ...opt("jb", retired.jb as unknown as CjJBatch | undefined) }) };
            if (retired.broadcast) { const b = jevBroadcast(w); if (!b.ok) return b; w = b.value; }
            if (retired.removed > 0) w = jevSay(w, `🧹 Removed ${retired.removed} stale dispute-finalize op(s) for ${peer.slice(-4)}`);
            return ok(jevDone(w, ctx));
          });
        });
      }));
    });
  });
};
/** og updateRegistryRecord: a Source record is written once (a retry keeps its time); a Target record may only rise, and an exact-ratio Target republish refreshes its time. */
export const updateRegistryRecord = (existing: HashLadderRegistryRecord | undefined, data: { readonly fillRatio: unknown; readonly revealedAt: unknown; readonly targetRole: boolean }, orderId: string): Result<HashLadderRegistryRecord, EntityError> => {
  const next = { fillRatio: Math.floor(Number(data.fillRatio)), revealedAt: Number(data.revealedAt) };
  if (!Number.isSafeInteger(next.revealedAt) || next.revealedAt <= 0) return halt(`CROSS_J_REGISTRY_REVEALED_AT_INVALID:${orderId}:${String(data.revealedAt)}`);
  if (existing === undefined) return ok(next);
  if (existing.fillRatio === next.fillRatio) {
    if (!data.targetRole && existing.revealedAt !== next.revealedAt) return halt(`CROSS_J_REGISTRY_RETRY_TIME_CONFLICT:${orderId}`);
    if (next.revealedAt < existing.revealedAt) return halt(`CROSS_J_REGISTRY_RECORD_TIME_REGRESSION:${orderId}`);
    return ok(next.revealedAt === existing.revealedAt ? existing : next);
  }
  if (!data.targetRole || next.fillRatio < existing.fillRatio || next.revealedAt < existing.revealedAt) return halt(`CROSS_J_REGISTRY_RECORD_CONFLICT:${orderId}:${existing.fillRatio}:${next.fillRatio}`);
  return ok(next);
};
/**
 * og applyHashLadderRevealRegisteredJEvent: the Source user's reveal ports, the registering Entity's recovery results, and every matching live
 * route's registry record (the own-slot fill-ratio latch only when this Entity wrote it).
 */
export const ladderRegisteredOnRoutes = (h: CjHost, data: LadderRevealEvent & { readonly revealedAt?: unknown }): Result<CjHost, EntityError> => {
  const self = lc(h.id), ladder = lc(data.ladderHash), writerIsSelf = lc(data.entity) === self;
  return foldResult(routesOf(h), h, (host, found): Result<CjHost, EntityError> => {
    if (isCrossTerminal(found.status)) return ok(host);
    const rolePull = data.targetRole ? found.targetPull : found.sourcePull, roleLeg = data.targetRole ? found.target : found.source;
    if (!(rolePull && pullLadderHash(rolePull) === ladder && lc(roleLeg.counterpartyEntityId) === lc(data.entity) && lc(roleLeg.entityId) === lc(data.counterpartyEntity))) return ok(host);
    const observed = Math.floor(Number(data.fillRatio)), current = host.swaps?.get(found.orderId);
    if (current === undefined) return halt(`CROSS_J_REGISTRY_ROUTE_FORK_MISSING:${found.orderId}`);
    let route: CrossRoute = current;
    if (writerIsSelf) {
      if (data.targetRole) route = { ...route, targetRegistryFillRatio: observed };
      else if (route.sourceRegistryFillRatio === undefined) route = { ...route, sourceRegistryFillRatio: observed };
      else if (route.sourceRegistryFillRatio !== observed) return halt(`CROSS_J_SOURCE_REGISTRY_CONFLICT:${route.orderId}:${route.sourceRegistryFillRatio}:${observed}`);
    }
    const r0 = route;
    return map(updateRegistryRecord(data.targetRole ? r0.targetRegistryRecord : r0.sourceRegistryRecord, { fillRatio: data.fillRatio, revealedAt: data.revealedAt, targetRole: data.targetRole }, r0.orderId), (record) =>
      cjPutRoute(host, found.orderId, data.targetRole ? { ...r0, targetRegistryRecord: record } : { ...r0, sourceRegistryRecord: record }));
  });
};
const ladderRegisteredJEvent = (step: JEventStep, e: WireJEvent, ctx: FoldContext, blockNumber: number): Result<JEventStep, EntityError> => {
  const d = e.data, data: LadderRevealEvent = { entity: lc(d["entity"]), counterpartyEntity: lc(d["counterpartyEntity"]), ladderHash: lc(d["ladderHash"]), fillRatio: Number(d["fillRatio"]), fullSecret: String(d["fullSecret"] ?? ""),
    reveals: (Array.isArray(d["reveals"]) ? d["reveals"] : []).map(String), targetRole: d["targetRole"] === true, revealedAt: Number(d["revealedAt"]) };
  const x0 = jevOf(step, ctx);
  return chain(revealPorts(x0.cj, data, blockNumber), ({ cj }) => {
    let x: JEv = { ...x0, cj };
    if (data.entity === lc(x.draft.state.id)) {
      const accountId = data.counterpartyEntity, visible = x.draft.accountReplicas.get(accountId as EntityId), active = visible === undefined ? undefined : activeOf(visible) as ActiveDispute | undefined, recovery = active?.crossJurisdictionRecovery;
      if (visible !== undefined && active !== undefined && recovery !== undefined) {
        let results = recovery.resultsByPullId, touched = false;
        for (const pullId of recovery.requiredPullIds) {
          if (Object.hasOwn(results, pullId)) continue;
          const route = routesOf(x.cj.host).find((c) => c.targetPull?.pullId === pullId || c.sourcePull?.pullId === pullId);
          if (route === undefined) continue;
          const pull = route.targetPull?.pullId === pullId ? route.targetPull : route.sourcePull;
          if (pull === undefined || pullLadderHash(pull) !== data.ladderHash || data.targetRole !== (route.targetPull?.pullId === pullId)) continue;
          results = { ...results, [pullId]: String(d["fillRatio"]) };
          touched = true;
        }
        if (touched) x = { ...x, dirty: [...x.dirty, accountId], draft: { ...x.draft, ...putChild(x.draft.state, x.draft.accountReplicas, accountId as EntityId, { ...(visible as DisputedAccount), active: { ...active, crossJurisdictionRecovery: { ...recovery, resultsByPullId: results } } }) } };
      }
    }
    return map(ladderRegisteredOnRoutes(x.cj.host, data), (host) => jevDone({ ...x, cj: cjHost(x.cj, host) }, ctx));
  });
};
/** og applyFinalizedJEvent: one canonical event's Entity handler (`evidence`: the block's dispute finalization evidence). */
const finalizedJEvent = (step: JEventStep, e: WireJEvent, ctx: FoldContext, evidence: readonly unknown[] = []): Result<JEventStep, EntityError> => {
  const blockNumber = e.blockNumber ?? 0, txHash = e.transactionHash || "unknown", d = e.data, state = step.draft.state, typed = typedJEvent(e);
  const said = (s: EntityState, ...messages: readonly string[]): JEventStep => ({ ...step, draft: jSay({ ...step.draft, state: s }, ...messages) });
  switch (e.type) {
    case "FoundationBootstrapped": case "EntityRegistered": case "BoardActivated":
      return map(applyBoardJEvent(state, typed as JEvent, blockNumber, step.draft.accountReplicas, ctx.timestamp), (b) => ({ ...step, draft: { ...step.draft, state: b.state, accountReplicas: b.accountReplicas, events: [...(step.draft.events ?? []), ...b.events] } }));
    case "ReserveUpdated": {
      const tokenId = Number(d["tokenId"]), mine = lower(d["entity"]) === lower(state.id);
      const next = mine ? { ...state, committed: { ...state.committed, reserves: mapSet(committedReserves(state), tokenId, BigInt(String(d["newBalance"]))) as unknown as Binary } } : state;
      return ok(said(next, `📊 RESERVE: ${rawUnits(tokenId, d["newBalance"])} | Block ${blockNumber} | Tx ${txHash.slice(0, 10)}...`));
    }
    case "DebtCreated": case "DebtEnforced": case "DebtForgiven": {
      const held = committedDebts(state);
      return chain(mapErr(applyDebtEvent(held, state.id, typed as Extract<JEvent, { readonly type: "DebtCreated" | "DebtEnforced" | "DebtForgiven" }>), (x): EntityError => ({ _tag: "entity_invariant", reason: x.reason })), (debts) => {
        const next = debts === held ? state : { ...state, committed: { ...state.committed, outDebtsByToken: debts.out as unknown as Binary, inDebtsByToken: debts.in as unknown as Binary } };
        const tokenId = Number(d["tokenId"]), tail = (v: unknown): string => String(v).slice(-8);
        return ok(said(next, e.type === "DebtCreated" ? `🔴 DEBT: ${tail(d["debtor"])} owes ${rawUnits(tokenId, d["amount"])} to ${tail(d["creditor"])} | Block ${blockNumber}`
          : e.type === "DebtEnforced" ? `✅ DEBT PAID: ${rawUnits(tokenId, d["amountPaid"])} to ${tail(d["creditor"])} | Block ${blockNumber}`
            : `🩶 DEBT FORGIVEN: ${rawUnits(tokenId, d["amountForgiven"])} between ${tail(d["debtor"])} and ${tail(d["creditor"])} | Block ${blockNumber} · debt #${String(d["debtIndex"])}`));
      });
    }
    case "ExternalWalletSnapshot": case "ExternalWalletDelta": return externalWalletJEvent(step, e, blockNumber, txHash);
    case "AccountSettled": return settledJEvent(step, e, blockNumber);
    case "HankoBatchProcessed": return batchProcessedJEvent(step, typed as Extract<JEvent, { readonly type: "HankoBatchProcessed" }>, blockNumber, ctx.timestamp, ctx.runtimeSeed);
    case "EntityProviderActionExecuted": case "EntityProviderActionCancelled":
      return map(applyEntityProviderActionJEvent(state, typed as JEvent, blockNumber), (b) => said(b.state, ...b.events.map((x) => x.message)));
    case "SecretRevealed": return map(knownSecret(jevOf(step, ctx), String(d["hashlock"]), String(d["secret"]), blockNumber), (x) => jevDone(x, ctx));
    case "DisputeStarted": return disputeStartedJEvent(step, e, ctx, blockNumber);
    case "CounterDisputeRegistered": return counterRegisteredJEvent(step, e, ctx);
    case "DisputeFinalized": return disputeFinalizedJEvent(step, e, ctx, evidence);
    case "HashLadderRevealRegistered": return ladderRegisteredJEvent(step, e, ctx, blockNumber);
    default: return invariant(`FINALIZED_J_EVENT_HANDLER_MISSING:${e.type}`);
  }
};
/**
 * og handleJEventEntityTx + applyJEvent: the active proposer's signed range is validated before anything else, a fully applied range is a no-op,
 * the suffix's events apply block by block (each block advancing lastFinalizedJHeight), and the certified J head, history root and board-registry
 * finality then commit. Queued Account claims merge (og mergeJEventClaimOps) and enter their Accounts' mempools like og's returned accountTxs.
 */
const entityJEvent = (d: Draft, data: JRec, ctx: FoldContext): Result<Draft, EntityError> => {
  const state = d.state, rawBlocks = Array.isArray(data["blocks"]) ? (data["blocks"] as readonly unknown[]) : [];
  const received = rawBlocks.flatMap((b) => { const block = recOf(b) ?? {}; return (Array.isArray(block["events"]) ? (block["events"] as readonly unknown[]) : []).map((raw): EntityRuntimeEvent => {
    const ev = recOf(raw) ?? {};
    return { eventName: "JEventReceived", data: { ...(recOf(ev["data"]) ?? {}), entityId: state.id, eventType: ev["type"] ?? "unknown", blockNumber: block["blockNumber"], txHash: ev["transactionHash"] } };
  }); });
  const runtimeEvents = [...(d.runtimeEvents ?? []), ...(received.length > 0 ? received : [{ eventName: "JEventReceived", data: { entityId: state.id, eventType: "liveness" } }])];
  const budget = jRangeBudgetIssue(data);
  if (budget !== undefined) return invariant(budget);
  const range = jRangeEnvelope(state, data);
  if (!range.ok) return invariant(range.error === "J_RANGE_PROPOSER_SIGNATURE_INVALID" ? `j_event rejected: invalid proposer signature for ${nText(data["from"])}` : `j_event rejected: ${range.error}`);
  const r = range.value;
  return chain(mapErr(reconcileJRange(state, r), (reason): EntityError => ({ _tag: "entity_invariant", reason })), (suffix): Result<Draft, EntityError> => {
    if (suffix === null) return ok({ ...d, runtimeEvents, touched: [] });
    let step: JEventStep = { draft: { ...d, runtimeEvents }, claims: [], dirty: [] }, root = anchorRoot(state);
    for (const block of suffix.blocks) {
      const folded = foldJHistoryRoot(root, [jBlockIdentity(r.jurisdictionRef)(block)]);
      if (!folded.ok) return invariant(folded.error);
      root = folded.value;
      const s = step.draft.state;
      step = { ...step, draft: { ...step.draft, state: { ...s, committed: { ...s.committed, lastFinalizedJHeight: block.blockNumber } } } };
      for (const e of block.events) { const next = finalizedJEvent(step, e, ctx, block.disputeFinalizationEvidence ?? []); if (!next.ok) return next; step = next.value; }
    }
    if (root !== suffix.eventHistoryRoot) return invariant(`J_HISTORY_FINALITY_ROOT_CORRUPTION:expected=${root}:certified=${suffix.eventHistoryRoot}`);
    const applied = step.draft.state, stack = entityBoardStack(applied);
    const finality: Binary = { jurisdictionRef: r.jurisdictionRef, baseHeight: suffix.baseHeight, finalizedThroughHeight: suffix.scannedThroughHeight, tipBlockHash: suffix.tipBlockHash, eventHistoryRoot: suffix.eventHistoryRoot,
      proposerSignerId: r.signerId, proposerSignature: r.signature, entityHeight: Number(state.height) + 1 };
    if (stack === undefined) return invariant("CERTIFIED_BOARD_ENTITY_JURISDICTION_MISSING");
    return chain(fromRegistry(advanceBoardFinality(entityBoardRegistry(applied), stack, suffix.scannedThroughHeight, suffix.tipBlockHash, suffix.eventHistoryRoot)), (board) =>
      chain(mapErr(mergeJOps(step.claims), (x): EntityError => ({ _tag: "entity_invariant", reason: x.reason })), (claims) => {
        let draft: Draft = { ...step.draft, state: { ...applied, committed: { ...applied.committed, lastFinalizedJHeight: suffix.scannedThroughHeight, jHistoryFinality: finality, certifiedBoardState: { ...board } } } };
        // og applyLocalAccountEffects: a claim for a missing or non-active Account, or one the Account refuses, is skipped
        for (const op of claims) {
          const child = draft.accountReplicas.get(op.accountId as EntityId);
          if (child === undefined || !liveAccount(child)) continue;
          const admitted = admitAt(child, [op.tx], state.id, L0_CLOCK, ctx.verify);
          if (admitted.ok) draft = { ...draft, ...putChild(draft.state, draft.accountReplicas, op.accountId as EntityId, admitted.value) };
        }
        return ok({ ...draft, touched: [...new Set(step.dirty)] as EntityId[] });
      }));
  });
};
/** og mergeJEventClaimOps over every returned Account tx: j_event_claims merge at their first position (merged claims in og's order); htlc_resolves keep theirs. */
const mergeJOps = (ops: readonly AccountTxTarget[]): Result<readonly AccountTxTarget[], JObserveError> => {
  const isClaim = (o: AccountTxTarget): o is JClaimOp => o.tx.type === "j_event_claim", seen = new Set<string>(), slots: (AccountTxTarget | null)[] = [];
  for (const o of ops) {
    if (!isClaim(o)) { slots.push(o); continue; }
    const key = `${o.accountId.toLowerCase()}:${o.tx.jHeight}:${o.tx.jBlockHash.toLowerCase()}`;
    if (seen.has(key)) continue;
    seen.add(key);
    slots.push(null);
  }
  return map(mergeClaimOps(ops.filter(isClaim)), (merged) => { let i = 0; return slots.map((s) => s ?? (merged[i++] as JClaimOp)); });
};
/** og finalizedJHistoryRoot: the certified head's root, or the empty root before the first certified range. */
const anchorRoot = (state: EntityState): string => { const a = certifiedJAnchor(state); return a.ok && a.value !== null ? a.value.eventHistoryRoot : EMPTY_J_HISTORY_ROOT; };
const liveAccount = (c: AccountReplica): boolean => c._tag !== "preparing" && c._tag !== "disputed";
// ---- og jurisdiction/machine/history/j-prefix-consensus.ts over local-history (claim builders) and range-budget.ts (proposable budget): the per-frame J prefix ----
/** og JPrefixClaim: one exact validator-local J prefix body above the certified anchor. */
export type JPrefixClaim = { readonly jurisdictionRef: string; readonly baseHeight: number; readonly scannedThroughHeight: number; readonly tipBlockHash: string; readonly eventHistoryRoot: string; readonly rangeHash: string; readonly blocks: readonly JRangeBlock[] };
export type JPrefixHeader = { readonly jHeight: number; readonly jBlockHash: string };
/** og JPrefixAttestation: one validator's signed head for one Entity height (its vote for the round). */
export type JPrefixAttestation = JPrefixClaim & { readonly version: 1; readonly entityId: string; readonly targetEntityHeight: number; readonly parentFrameHash: string; readonly validatorId: string; readonly headers: readonly JPrefixHeader[]; readonly signature: string };
/** og JPrefixCertificate: the weighted certificate selecting the highest exact prefix common to its signed heads. */
export type JPrefixCertificate = { readonly version: 1; readonly entityId: string; readonly targetEntityHeight: number; readonly parentFrameHash: string; readonly jurisdictionRef: string; readonly baseHeight: number; readonly selected: JPrefixClaim; readonly attestations: ReadonlyMap<string, JPrefixAttestation> };
/** og JPrefixRound: the validator-private collection for one Entity-height round. */
export type JPrefixRound = { readonly targetEntityHeight: number; readonly parentFrameHash: string; readonly jurisdictionRef: string; readonly baseHeight: number; readonly attestations: ReadonlyMap<string, JPrefixAttestation>; readonly certificate?: JPrefixCertificate | undefined };
/** og's J-prefix error dispositions: a plain Error rejects (code `J_PREFIX_INVALID`), retryFailure retries, certified-history corruption halts. */
export type JPrefixFailure = { readonly disposition: "reject" | "retry" | "halt"; readonly code: string; readonly message: string };
type JP<T> = Result<T, JPrefixFailure>;
/** The committed Entity a J prefix is judged against: og's `state` (height, prevFrameHash, config under the round's authority). */
export type JPrefixView = { readonly state: EntityState; readonly head: Head };
/** og signer seams: verifyAccountSignature (an EOA recovers from a canonical compact signature) and signAccountFrame. */
export type JPrefixCrypto = { readonly verify: (signerId: string, digest: string, signature: string) => boolean; readonly sign?: ((signerId: string, digest: string) => Result<string, unknown>) | undefined };
/** og verifyAccountSignature for a J-prefix signer: a canonical compact signature recovering to the signer's EOA (any other signer id has no registered key). */
export const jPrefixVerify = (signerId: string, digest: string, signature: string): boolean => witnessSigned(nText(digest), nText(signature), nText(signerId));
const J_PREFIX_ATTESTATION_DOMAIN = "xln:j-prefix-attestation:v1";
const J_HISTORY_CORRUPTION = /^J_HISTORY_(?:FINALITY|FINALIZED)_/;
const jpFail = (message: string): JP<never> =>
  err(J_HISTORY_CORRUPTION.test(message) ? { disposition: "halt", code: message.split(":")[0] ?? message, message } : { disposition: "reject", code: "J_PREFIX_INVALID", message });
const jpRetry = (code: string, message: string = code): JP<never> => err({ disposition: "retry", code, message });
const jpOf = <T>(r: Result<T, RuntimeError | string>): JP<T> => (r.ok ? r : jpFail(typeof r.error === "string" ? r.error : jCode(r.error)));
const jpBytes = (v: unknown): JP<Uint8Array> => mapErr(authConsensusBytes(v), (): JPrefixFailure => ({ disposition: "reject", code: "J_PREFIX_INVALID", message: "CANONICAL_ENCODING_INVALID" }));
const jpHash = (v: unknown): JP<string> => map(jpBytes(v), (b) => bytesToHex(keccak_256(b)));
const jpHeight = (v: unknown, label: string): JP<number> => { const n = Number(v); return Number.isSafeInteger(n) && n >= 0 ? ok(n) : jpFail(`J_PREFIX_${label}_INVALID:${String(v)}`); };
const jpWord = (v: unknown, label: string): JP<string> => { const s = nText(v); return WORD32.test(s) ? ok(s) : jpFail(`J_PREFIX_${label}_INVALID:${String(v)}`); };
const jpFinalized = (v: JPrefixView): number => Number(v.state.committed["lastFinalizedJHeight"] || 0);
const jpEntityHeight = (v: JPrefixView): number => Number(v.state.height);
/** og currentParentFrameHash. */
const jpParent = (v: JPrefixView): string => (v.state.height === 0n ? GENESIS_PARENT : parentOf(v.head));
/** og state.config.validators / shares / threshold under the view's authority. */
const jpValidators = (v: JPrefixView): readonly string[] => [...membersOf(v.state.quorum).keys()].map((a) => lower(a));
/** og finalizedJHistoryRoot. */
const jpFinalizedRoot = (v: JPrefixView): JP<string> => map(jpOf(certifiedJAnchor(v.state)), (a) => a?.eventHistoryRoot ?? EMPTY_J_HISTORY_ROOT);
const jpIdentities = (ref: string, blocks: readonly JRangeBlock[]): readonly JHistoryIdentity[] => blocks.map(jBlockIdentity(ref));
/** og normalizeClaimEnvelope: the claim's jurisdiction, heights, strict blocks (J_PREFIX codes), range hash, tip and root. */
const jpClaimEnvelope = (expected: string, raw: JRec): JP<JPrefixClaim> => {
  const jurisdictionRef = nText(raw["jurisdictionRef"]);
  if (jurisdictionRef !== expected) return jpFail(`J_PREFIX_JURISDICTION_MISMATCH:${jurisdictionRef}:${expected}`);
  return chain(jpHeight(raw["baseHeight"], "BASE_HEIGHT"), (baseHeight) => chain(jpHeight(raw["scannedThroughHeight"], "SCANNED_HEIGHT"), (scannedThroughHeight) => {
    if (scannedThroughHeight < baseHeight) return jpFail("J_PREFIX_BEHIND_BASE");
    const rawBlocks = raw["blocks"];
    if (!Array.isArray(rawBlocks)) return jpFail("raw.blocks.map is not a function");
    const blocks: JRangeBlock[] = [];
    let prior = baseHeight;
    for (const b of rawBlocks as readonly unknown[]) { const n = strictJBlock(b, prior, scannedThroughHeight, "J_PREFIX"); if (!n.ok) return jpFail(n.error); blocks.push(n.value); prior = n.value.blockNumber; }
    return chain(jpOf(jRangeHash(jurisdictionRef, blocks)), (rangeHash) => chain(jpWord(raw["rangeHash"], "RANGE_HASH"), (claimed) => {
      if (claimed !== rangeHash) return jpFail("J_PREFIX_RANGE_HASH_MISMATCH");
      return chain(jpWord(raw["tipBlockHash"], "TIP_HASH"), (tipBlockHash) => map(jpWord(raw["eventHistoryRoot"], "HISTORY_ROOT"), (eventHistoryRoot): JPrefixClaim =>
        ({ jurisdictionRef, baseHeight, scannedThroughHeight, tipBlockHash, eventHistoryRoot, rangeHash, blocks })));
    }));
  }));
};
/** og normalizeClaim: the envelope anchored at the Entity-certified head; a base-only claim must equal the certified base. */
const jpClaim = (v: JPrefixView, raw: JRec): JP<JPrefixClaim> => chain(jpClaimEnvelope(jEventJurisdictionRef(v.state), raw), (claim) => {
  const finalized = jpFinalized(v);
  if (claim.baseHeight !== finalized) return jpFail(`J_PREFIX_BASE_HEIGHT_MISMATCH:${claim.baseHeight}:${finalized}`);
  return chain(jpFinalizedRoot(v), (root) => chain(jpOf(foldJHistoryRoot(root, jpIdentities(claim.jurisdictionRef, claim.blocks))), (eventHistoryRoot): JP<JPrefixClaim> => {
    if (claim.eventHistoryRoot !== eventHistoryRoot) return jpFail("J_PREFIX_HISTORY_ROOT_MISMATCH");
    const anchored: JPrefixClaim = { ...claim, eventHistoryRoot };
    if (claim.scannedThroughHeight !== claim.baseHeight) return ok(anchored);
    if (recOf(v.state.committed["jHistoryFinality"]) === null) return jpFail("J_PREFIX_BASE_ATTESTATION_WITHOUT_CERTIFIED_ANCHOR");
    return chain(jpCertifiedBase(v), (base) => (consensusEqual(anchored, base) ? ok(anchored) : jpFail("J_PREFIX_BASE_ATTESTATION_CONFLICT")));
  }));
});
/** og attestationBody + hashJPrefixAttestation. */
export const jPrefixAttestationHash = (a: Omit<JPrefixAttestation, "signature">): JP<string> => jpHash({
  domain: J_PREFIX_ATTESTATION_DOMAIN, version: 1, entityId: nText(a.entityId), targetEntityHeight: a.targetEntityHeight, parentFrameHash: nText(a.parentFrameHash), validatorId: nText(a.validatorId),
  jurisdictionRef: nText(a.jurisdictionRef), baseHeight: a.baseHeight, scannedThroughHeight: a.scannedThroughHeight, tipBlockHash: nText(a.tipBlockHash), eventHistoryRoot: nText(a.eventHistoryRoot), rangeHash: nText(a.rangeHash),
  headers: a.headers.map((h) => ({ jHeight: h.jHeight, jBlockHash: nText(h.jBlockHash) })), blocks: a.blocks,
});
/** og entityRequiresJPrefixCertificate: a registered Entity (registrationBlock) or one with a certified anchor certifies a J prefix in every frame. */
export const entityRequiresJPrefixCertificate = (state: EntityState): boolean =>
  state.jurisdictionConfig?.registrationBlock !== undefined || Boolean(state.committed["jHistoryFinality"]);
/** og buildCertifiedBaseClaim. */
const jpCertifiedBase = (v: JPrefixView): JP<JPrefixClaim> => {
  const baseHeight = jpFinalized(v), finality = recOf(v.state.committed["jHistoryFinality"]);
  if (finality === null || finality["finalizedThroughHeight"] !== baseHeight) return jpFail(`J_PREFIX_CERTIFIED_BASE_MISSING:${baseHeight}`);
  const jurisdictionRef = jEventJurisdictionRef(v.state);
  if (nText(finality["jurisdictionRef"]) !== jurisdictionRef) return jpFail("J_PREFIX_CERTIFIED_BASE_JURISDICTION_MISMATCH");
  return chain(jpWord(finality["tipBlockHash"], "CERTIFIED_BASE_TIP_HASH"), (tipBlockHash) => chain(jpWord(finality["eventHistoryRoot"], "CERTIFIED_BASE_HISTORY_ROOT"), (eventHistoryRoot) =>
    map(jpOf(jRangeHash(jurisdictionRef, [])), (rangeHash): JPrefixClaim => ({ jurisdictionRef, baseHeight, scannedThroughHeight: baseHeight, tipBlockHash, eventHistoryRoot, rangeHash, blocks: [] }))));
};
/** og buildBaseClaim: the certified base, or (before the first anchor) this validator's own header at the registration base. */
const jpBaseClaim = (v: JPrefixView, h: ValidatorJHistory): JP<JPrefixClaim> => {
  if (recOf(v.state.committed["jHistoryFinality"]) !== null) return jpCertifiedBase(v);
  const baseHeight = jpFinalized(v), tip = h.blockHashes.get(baseHeight);
  if (!tip) return jpFail(`J_PREFIX_LOCAL_TIP_HASH_MISSING:${baseHeight}`);
  const jurisdictionRef = jEventJurisdictionRef(v.state);
  return chain(jpWord(tip, "TIP_HASH"), (tipBlockHash) => chain(jpFinalizedRoot(v), (eventHistoryRoot) =>
    map(jpOf(jRangeHash(jurisdictionRef, [])), (rangeHash): JPrefixClaim => ({ jurisdictionRef, baseHeight, scannedThroughHeight: baseHeight, tipBlockHash, eventHistoryRoot, rangeHash, blocks: [] }))));
};
/** og getValidatorJContiguousThroughHeight. */
const jpContiguous = (v: JPrefixView, h: ValidatorJHistory): JP<number> => chain(jpOf(certifiedJAnchor(v.state)), (anchor) => chain(jpOf(historyMatchesAnchor(anchor, h)), (): JP<number> => {
  const base = jpFinalized(v);
  if (h.scannedThroughHeight < base) return jpFail(`J_HISTORY_LOCAL_BEHIND_FINALIZED_ANCHOR:${h.scannedThroughHeight}:${base}`);
  let height = Math.max(base, h.contiguousThroughHeight);
  while (height < h.scannedThroughHeight && h.blockHashes.has(height + 1)) height += 1;
  return ok(height);
}));
/** og getLocalJPrefixAttestableHeight: the highest head this validator proves without crossing a sparse gap. */
export const localJPrefixAttestableHeight = (v: JPrefixView, h: ValidatorJHistory): JP<number | null> => map(jpContiguous(v, h), (contiguous) => {
  const base = jpFinalized(v);
  if (contiguous > base) return contiguous;
  const sparse = [...h.eventBlocks.keys()].some((height) => height > base && height <= h.scannedThroughHeight);
  return sparse || recOf(v.state.committed["jHistoryFinality"]) === null ? null : base;
});
/** og toWireBlock. */
const jpWireBlock = (b: ValidatorJBlock): JRangeBlock => ({
  blockNumber: b.jHeight, blockHash: b.jBlockHash, eventsHash: b.eventsHash, events: b.events as readonly WireJEvent[],
  ...(b.disputeFinalizationEvidence?.length ? { disputeFinalizationEvidence: b.disputeFinalizationEvidence } : {}), ...(b.disputeFinalizationEvidenceHash ? { disputeFinalizationEvidenceHash: b.disputeFinalizationEvidenceHash } : {}),
});
/** og buildUnsignedJEventRangeAtHeight. */
const jpUnsignedAt = (v: JPrefixView, h: ValidatorJHistory, height: number): JP<JPrefixClaim | null> => {
  const baseHeight = jpFinalized(v);
  if (!Number.isSafeInteger(height) || height <= baseHeight) return ok(null);
  if (height > h.scannedThroughHeight) return jpRetry("J_PREFIX_LOCAL_HISTORY_BEHIND", `J_PREFIX_LOCAL_HISTORY_BEHIND:${h.scannedThroughHeight}:${height}`);
  const tipBlockHash = h.blockHashes.get(height);
  if (!tipBlockHash) return jpFail(`J_PREFIX_LOCAL_TIP_HASH_MISSING:${height}`);
  const blocks = [...h.eventBlocks.values()].filter((b) => b.jHeight > baseHeight && b.jHeight <= height).sort((a, b) => a.jHeight - b.jHeight).map(jpWireBlock);
  return chain(jpFinalizedRoot(v), (root) => chain(jpOf(foldJHistoryRoot(root, jpIdentities(h.jurisdictionRef, blocks))), (eventHistoryRoot) =>
    map(jpOf(jRangeHash(h.jurisdictionRef, blocks)), (rangeHash): JPrefixClaim => ({ jurisdictionRef: h.jurisdictionRef, baseHeight, scannedThroughHeight: height, tipBlockHash, eventHistoryRoot, rangeHash, blocks }))));
};
/** og buildValidatorJPrefixHeaders. */
const jpHeaders = (v: JPrefixView, h: ValidatorJHistory, height: number): JP<readonly JPrefixHeader[]> => {
  const baseHeight = jpFinalized(v);
  if (!Number.isSafeInteger(height) || height <= baseHeight) return ok([]);
  if (height > h.scannedThroughHeight) return jpRetry("J_PREFIX_LOCAL_HISTORY_BEHIND", `J_PREFIX_LOCAL_HISTORY_BEHIND:${h.scannedThroughHeight}:${height}`);
  const headers: JPrefixHeader[] = [];
  for (let jHeight = baseHeight + 1; jHeight <= height; jHeight++) {
    const jBlockHash = h.blockHashes.get(jHeight);
    if (!jBlockHash) return jpFail(`J_PREFIX_LOCAL_HEADER_MISSING:${jHeight}`);
    headers.push({ jHeight, jBlockHash });
  }
  return ok(headers);
};
const ZERO_ACCOUNT_SIGNATURE = `0x${"00".repeat(65)}`;
/** og getJRangeClaimsProposableBudgetError: the largest signed frame payload any eligible proposer could produce for these claims. */
const jpBudgetError = (claims: readonly JPrefixClaim[], proposers: readonly string[]): JP<string | null> => {
  for (const c of claims) {
    const base = Number(c.baseHeight), scanned = Number(c.scannedThroughHeight);
    if (!Number.isSafeInteger(base) || base < 0) return jpFail(`J_RANGE_FRAME_BASE_HEIGHT_INVALID:${String(c.baseHeight)}`);
    if (!Number.isSafeInteger(scanned) || scanned <= base) return jpFail(`J_RANGE_FRAME_SCANNED_HEIGHT_INVALID:${String(c.scannedThroughHeight)}`);
  }
  const [first] = proposers;
  if (first === undefined) return jpFail("J_RANGE_FRAME_PROPOSER_SET_EMPTY");
  const width = (s: string): number => { const b = authConsensusBytes(s.trim().toLowerCase()); return b.ok ? b.value.byteLength : 0; };
  const longest = proposers.reduce((sel, c) => (width(c) > width(sel) ? c : sel), first).trim().toLowerCase();
  return map(jpBytes({ domain: J_RANGE_FRAME_PAYLOAD_DOMAIN, version: 1, ranges: claims.map((c) => ({ from: longest, signature: ZERO_ACCOUNT_SIGNATURE, observedAt: c.scannedThroughHeight, jurisdictionRef: c.jurisdictionRef, baseHeight: c.baseHeight, scannedThroughHeight: c.scannedThroughHeight, tipBlockHash: c.tipBlockHash, eventHistoryRoot: c.eventHistoryRoot, rangeHash: c.rangeHash, blocks: c.blocks })) }),
    (bytes) => (bytes.byteLength > MAX_ENTITY_FRAME_J_RANGE_BYTES ? `J_RANGE_FRAME_BYTE_LIMIT_EXCEEDED:${bytes.byteLength}:${MAX_ENTITY_FRAME_J_RANGE_BYTES}` : null));
};
/** og buildBudgetedLocalClaim: the highest exact local prefix that fits one Entity frame (binary search; a lone oversized block is terminal). */
const jpBudgetedLocalClaim = (v: JPrefixView, h: ValidatorJHistory): JP<JPrefixClaim | null> => chain(localJPrefixAttestableHeight(v, h), (highest): JP<JPrefixClaim | null> => {
  const base = jpFinalized(v), validators = jpValidators(v);
  if (highest === null) return ok(null);
  if (highest === base) return jpBaseClaim(v, h);
  const claimAt = (height: number): JP<JPrefixClaim> => chain(jpUnsignedAt(v, h, height), (c) => (c === null ? jpFail(`J_PREFIX_BUDGET_CLAIM_MISSING:${height}`) : ok(c)));
  const budget = (c: JPrefixClaim): JP<string | null> => jpBudgetError([c], validators);
  return chain(claimAt(highest), (highestClaim) => chain(budget(highestClaim), (highestError): JP<JPrefixClaim | null> => {
    if (highestError === null) return ok(highestClaim);
    let lo = base + 1, hi = highest - 1, selected: JPrefixClaim | null = null;
    while (lo <= hi) {
      const mid = Math.floor((lo + hi) / 2), c = claimAt(mid);
      if (!c.ok) return c;
      const e = budget(c.value);
      if (!e.ok) return e;
      if (e.value !== null) hi = mid - 1; else { selected = c.value; lo = mid + 1; }
    }
    const failing = (selected?.scannedThroughHeight ?? base) + 1;
    return chain(claimAt(failing), (failingClaim) => chain(budget(failingClaim), (error): JP<JPrefixClaim | null> => {
      if (error === null) return jpFail(`J_PREFIX_BUDGET_SEARCH_NON_MONOTONIC:${failing}`);
      const block = failingClaim.blocks.find((b) => b.blockNumber === failing);
      const isolated: JP<string | null> = block === undefined ? ok(null)
        : chain(jpOf(jRangeHash(failingClaim.jurisdictionRef, [block])), (rangeHash) => budget({ jurisdictionRef: failingClaim.jurisdictionRef, baseHeight: block.blockNumber - 1, scannedThroughHeight: block.blockNumber, tipBlockHash: block.blockHash, eventHistoryRoot: failingClaim.eventHistoryRoot, rangeHash, blocks: [block] }));
      return chain(isolated, (isolatedError): JP<JPrefixClaim | null> => {
        if (isolatedError?.startsWith("J_RANGE_FRAME_BYTE_LIMIT_EXCEEDED:")) return jpFail(`J_RANGE_SINGLE_BLOCK_UNPROPOSABLE:${failing}:${isolatedError}`);
        return selected === null ? jpFail(`J_PREFIX_RANGE_UNPROPOSABLE:${failing}:${error}`) : ok(selected);
      });
    }));
  }));
});
/** og normalizeAttestationClaimEvidence: the budget, exactly one ordered header per height, the tip and event blocks on those headers, a compact signature. */
const jpEvidence = (raw: JRec, claim: JPrefixClaim, validators: readonly string[]): JP<{ readonly headers: readonly JPrefixHeader[]; readonly signature: string }> => {
  const budget: JP<string | null> = claim.scannedThroughHeight > claim.baseHeight ? jpBudgetError([claim], validators) : ok(null);
  return chain(budget, (budgetError): JP<{ readonly headers: readonly JPrefixHeader[]; readonly signature: string }> => {
    if (budgetError !== null) return jpFail(`J_PREFIX_ATTESTATION_BUDGET_INVALID:${budgetError}`);
    const expected = claim.scannedThroughHeight - claim.baseHeight, rawHeaders = raw["headers"];
    if (!Array.isArray(rawHeaders) || rawHeaders.length !== expected) return jpFail(`J_PREFIX_HEADER_COUNT_MISMATCH:${String((rawHeaders as { readonly length?: unknown } | null | undefined)?.length ?? -1)}:${expected}`);
    const headers: JPrefixHeader[] = [];
    for (const [index, value] of (rawHeaders as readonly unknown[]).entries()) {
      const header = recOf(value);
      if (header === null) return jpFail(`Cannot read properties of ${value === null ? "null" : typeof value} (reading 'jHeight')`);
      const jHeight = jpHeight(header["jHeight"], "HEADER_HEIGHT");
      if (!jHeight.ok) return jHeight;
      if (jHeight.value !== claim.baseHeight + index + 1) return jpFail(`J_PREFIX_HEADER_ORDER_MISMATCH:${jHeight.value}:${claim.baseHeight + index + 1}`);
      const jBlockHash = jpWord(header["jBlockHash"], "HEADER_HASH");
      if (!jBlockHash.ok) return jBlockHash;
      headers.push({ jHeight: jHeight.value, jBlockHash: jBlockHash.value });
    }
    if (claim.scannedThroughHeight > claim.baseHeight && headers.at(-1)?.jBlockHash !== claim.tipBlockHash) return jpFail("J_PREFIX_TIP_HEADER_MISMATCH");
    for (const block of claim.blocks) if (headers[block.blockNumber - claim.baseHeight - 1]?.jBlockHash !== block.blockHash) return jpFail(`J_PREFIX_EVENT_HEADER_MISMATCH:${block.blockNumber}`);
    const signature = nText(raw["signature"]);
    return /^0x[0-9a-f]{130}$/.test(signature) ? ok({ headers, signature }) : jpFail("J_PREFIX_SIGNATURE_INVALID");
  });
};
/** og normalizeJPrefixAttestation: version, Entity, target height, parent, validator, the anchored claim and its evidence. */
const jpAttestation = (v: JPrefixView, rawValue: unknown): JP<JPrefixAttestation> => {
  const raw = recOf(rawValue) ?? {};
  if (raw["version"] !== 1) return jpFail(`J_PREFIX_VERSION_UNSUPPORTED:${String(raw["version"])}`);
  const entityId = nText(raw["entityId"]);
  if (entityId !== nText(v.state.id)) return jpFail(`J_PREFIX_ENTITY_MISMATCH:${entityId}:${nText(v.state.id)}`);
  return chain(jpHeight(raw["targetEntityHeight"], "TARGET_ENTITY_HEIGHT"), (targetEntityHeight) => {
    if (targetEntityHeight !== jpEntityHeight(v) + 1) return jpFail(`J_PREFIX_TARGET_HEIGHT_MISMATCH:${targetEntityHeight}:${jpEntityHeight(v) + 1}`);
    const parentFrameHash = nText(raw["parentFrameHash"]), expectedParent = nText(jpParent(v));
    if (parentFrameHash !== expectedParent) return jpFail(`J_PREFIX_PARENT_MISMATCH:${parentFrameHash}:${expectedParent}`);
    const validatorId = nText(raw["validatorId"]);
    if (!validatorId) return jpFail("J_PREFIX_VALIDATOR_MISSING");
    return chain(jpClaim(v, raw), (claim) => map(jpEvidence(raw, claim, jpValidators(v)), ({ headers, signature }): JPrefixAttestation =>
      ({ version: 1, entityId, targetEntityHeight, parentFrameHash, validatorId, ...claim, headers, signature })));
  });
};
/** og assertBoardSigner: exactly one validator and one positive share entry under this id. */
const jpBoardSigner = (q: Quorum, validatorId: string): JP<void> => {
  const members = [...membersOf(q)].filter(([a]) => nText(a) === validatorId);
  if (members.length !== 1) return jpFail(`J_PREFIX_UNKNOWN_OR_DUPLICATE_VALIDATOR:${validatorId}`);
  return (members[0]?.[1].shares ?? 0n) > 0n ? ok(undefined) : jpFail(`J_PREFIX_INVALID_VALIDATOR_SHARES:${validatorId}`);
};
/** og getJPrefixAttestationTemporalDisposition. */
export const jPrefixTemporalDisposition = (v: JPrefixView, rawValue: unknown): JP<"stale" | "current" | "future"> => chain(jpHeight((recOf(rawValue) ?? {})["targetEntityHeight"], "TARGET_ENTITY_HEIGHT"), (target) => {
  if (target < 1) return jpFail(`J_PREFIX_TARGET_ENTITY_HEIGHT_INVALID:${target}`);
  const expected = jpEntityHeight(v) + 1;
  return ok(target < expected ? "stale" : target > expected ? "future" : "current");
});
/** og verifyOutOfRoundJPrefixAttestation: a stale or early vote, authenticated (canonical body and signature) without reinterpreting it against the current head. */
export const verifyOutOfRoundJPrefixAttestation = (v: JPrefixView, rawValue: unknown, authorities: readonly Quorum[], crypto: JPrefixCrypto): JP<JPrefixAttestation> => chain(jPrefixTemporalDisposition(v, rawValue), (disposition) => {
  const raw = recOf(rawValue) ?? {};
  if (disposition === "current") return jpFail("J_PREFIX_OUT_OF_ROUND_EXPECTED");
  if (raw["version"] !== 1) return jpFail(`J_PREFIX_VERSION_UNSUPPORTED:${String(raw["version"])}`);
  const entityId = nText(raw["entityId"]);
  if (entityId !== nText(v.state.id)) return jpFail(`J_PREFIX_ENTITY_MISMATCH:${entityId}:${nText(v.state.id)}`);
  return chain(jpHeight(raw["targetEntityHeight"], "TARGET_ENTITY_HEIGHT"), (targetEntityHeight) => {
    const parentFrameHash = nText(raw["parentFrameHash"]);
    const parent: JP<unknown> = targetEntityHeight === 1 ? (parentFrameHash === GENESIS_PARENT ? ok(undefined) : jpFail(`J_PREFIX_PARENT_MISMATCH:${parentFrameHash}:genesis`)) : jpWord(parentFrameHash, "PARENT_HASH");
    return chain(parent, () => {
      const validatorId = nText(raw["validatorId"]);
      if (!validatorId) return jpFail("J_PREFIX_VALIDATOR_MISSING");
      const authority = authorities.find((q) => { const m = [...membersOf(q)].filter(([a]) => nText(a) === validatorId); return m.length === 1 && (m[0]?.[1].shares ?? 0n) > 0n; });
      if (authority === undefined && disposition !== "stale") return jpFail(`J_PREFIX_OUT_OF_ROUND_AUTHORITY_MISSING:${validatorId}`);
      return chain(jpClaimEnvelope(jEventJurisdictionRef(v.state), raw), (claim) => chain(jpEvidence(raw, claim, authority === undefined ? [validatorId] : [...membersOf(authority).keys()].map((a) => lower(a))), ({ headers, signature }) => {
        const attestation: JPrefixAttestation = { version: 1, entityId, targetEntityHeight, parentFrameHash, validatorId, ...claim, headers, signature };
        return chain(jPrefixAttestationHash(attestation), (hash) => (crypto.verify(validatorId, hash, signature) ? ok(attestation) : jpFail(`J_PREFIX_SIGNATURE_REJECTED:${validatorId}`)));
      }));
    });
  });
});
/** og verifyJPrefixAttestation: normalized, a board signer, its own signature. */
const jpVerifyAttestation = (v: JPrefixView, raw: unknown, crypto: JPrefixCrypto): JP<JPrefixAttestation> => chain(jpAttestation(v, raw), (a) => chain(jpBoardSigner(v.state.quorum, a.validatorId), () =>
  chain(jPrefixAttestationHash(a), (hash) => (crypto.verify(a.validatorId, hash, a.signature) ? ok(a) : jpFail(`J_PREFIX_SIGNATURE_REJECTED:${a.validatorId}`)))));
/** og buildLocalJPrefixAttestation: this validator's signed head over its budgeted local claim (null while its headers are incomplete). */
export const buildLocalJPrefixAttestation = (v: JPrefixView, signer: string, h: ValidatorJHistory | undefined, crypto: JPrefixCrypto): JP<JPrefixAttestation | null> => {
  if (h === undefined) return ok(null);
  const finalized = jpFinalized(v);
  if (h.scannedThroughHeight < finalized) return jpRetry("J_PREFIX_LOCAL_HISTORY_BEHIND", `J_PREFIX_LOCAL_HISTORY_BEHIND:${h.scannedThroughHeight}:${finalized}`);
  return chain(jpBudgetedLocalClaim(v, h), (claim): JP<JPrefixAttestation | null> => {
    if (claim === null) return ok(null);
    return chain(jpHeaders(v, h, claim.scannedThroughHeight), (headers) => {
      const unsigned: Omit<JPrefixAttestation, "signature"> = { version: 1, entityId: nText(v.state.id), targetEntityHeight: jpEntityHeight(v) + 1, parentFrameHash: nText(jpParent(v)), validatorId: nText(signer), ...claim, headers };
      return chain(jpBoardSigner(v.state.quorum, unsigned.validatorId), () => chain(jPrefixAttestationHash(unsigned), (hash) => {
        if (crypto.sign === undefined) return jpFail(`CRYPTO_DETERMINISM_VIOLATION: signAccountFrame called without env.runtimeSeed for signer ${unsigned.validatorId}`);
        return map(mapErr(crypto.sign(unsigned.validatorId, hash), (): JPrefixFailure => ({ disposition: "reject", code: "J_PREFIX_INVALID", message: `J_PREFIX_SIGN_FAILED:${unsigned.validatorId}` })), (signature): JPrefixAttestation => ({ ...unsigned, signature }));
      }));
    });
  });
};
/** og localClaimAtHeight. */
const jpLocalClaimAt = (v: JPrefixView, h: ValidatorJHistory | undefined, height: number): JP<JPrefixClaim> => {
  if (h === undefined || h.scannedThroughHeight < height) return jpRetry("J_PREFIX_LOCAL_HISTORY_BEHIND", `J_PREFIX_LOCAL_HISTORY_BEHIND:${h?.scannedThroughHeight ?? 0}:${height}`);
  return chain(jpUnsignedAt(v, h, height), (c) => (c === null ? jpBaseClaim(v, h) : ok(c)));
};
/** og claimKey. */
const jpClaimKey = (c: JPrefixClaim): JP<string> => jpHash(c);
const jpClaimOf = (a: JPrefixClaim): JPrefixClaim => ({ jurisdictionRef: a.jurisdictionRef, baseHeight: a.baseHeight, scannedThroughHeight: a.scannedThroughHeight, tipBlockHash: a.tipBlockHash, eventHistoryRoot: a.eventHistoryRoot, rangeHash: a.rangeHash, blocks: a.blocks });
/** og hasCurrentRoundJPrefixAttestation: this validator's vote in the current round still equals its exact local claim at that height. */
export const hasCurrentRoundJPrefixAttestation = (v: JPrefixView, signer: string, round: JPrefixRound | undefined, h: ValidatorJHistory | undefined): JP<boolean> => {
  if (round === undefined) return ok(false);
  if (round.targetEntityHeight !== jpEntityHeight(v) + 1 || nText(round.parentFrameHash) !== nText(jpParent(v)) || nText(round.jurisdictionRef) !== jEventJurisdictionRef(v.state) || round.baseHeight !== jpFinalized(v)) return ok(false);
  const attestation = round.attestations.get(nText(signer));
  if (attestation === undefined || h === undefined) return ok(false);
  return chain(jpLocalClaimAt(v, h, attestation.scannedThroughHeight), (local) => chain(jpClaim(v, jpClaimOf(attestation) as unknown as JRec), (attested) =>
    chain(jpClaimKey(local), (a) => map(jpClaimKey(attested), (b) => a === b))));
};
/** og clipAttestation: a signed head clipped to a lower height through its own headers. */
const jpClip = (v: JPrefixView, a: JPrefixAttestation, height: number): JP<JPrefixClaim> => {
  if (height < a.baseHeight || height > a.scannedThroughHeight) return jpFail(`J_PREFIX_CLIP_HEIGHT_INVALID:${height}`);
  if (height === a.baseHeight) return jpCertifiedBase(v);
  const tip = a.headers[height - a.baseHeight - 1];
  if (tip === undefined || tip.jHeight !== height) return jpFail(`J_PREFIX_CLIP_HEADER_MISSING:${height}`);
  const blocks = a.blocks.filter((b) => b.blockNumber <= height);
  return chain(jpFinalizedRoot(v), (root) => chain(jpOf(foldJHistoryRoot(root, jpIdentities(a.jurisdictionRef, blocks))), (eventHistoryRoot) =>
    map(jpOf(jRangeHash(a.jurisdictionRef, blocks)), (rangeHash): JPrefixClaim => ({ jurisdictionRef: a.jurisdictionRef, baseHeight: a.baseHeight, scannedThroughHeight: height, tipBlockHash: tip.jBlockHash, eventHistoryRoot, rangeHash, blocks }))));
};
/** og calculateJPrefixQuorumPower. */
const jpQuorumPower = (q: Quorum, signers: readonly string[]): JP<bigint> => {
  const seen = new Set<string>();
  let total = 0n;
  for (const raw of signers) {
    const signer = nText(raw);
    if (seen.has(signer)) return jpFail(`J_PREFIX_DUPLICATE_SIGNER:${raw}`);
    seen.add(signer);
    const board = jpBoardSigner(q, signer);
    if (!board.ok) return board;
    total += sharesOf(q, signer);
  }
  return ok(total);
};
/** og selectHighestWeightedCommonJPrefix: from the highest attested height down, the one clipped claim a quorum shares. */
export const selectHighestWeightedCommonJPrefix = (v: JPrefixView, attestations: ReadonlyMap<string, unknown>): JP<{ readonly claim: JPrefixClaim; readonly signerIds: readonly string[] } | null> => {
  if (attestations.size === 0) return ok(null);
  const normalized = new Map<string, JPrefixAttestation>();
  for (const [rawKey, rawAttestation] of attestations) {
    const key = nText(rawKey), a = jpAttestation(v, rawAttestation);
    if (!a.ok) return a;
    if (key !== a.value.validatorId) return jpFail(`J_PREFIX_MAP_SIGNER_MISMATCH:${rawKey}`);
    if (normalized.has(key)) return jpFail(`J_PREFIX_DUPLICATE_SIGNER:${rawKey}`);
    const board = jpBoardSigner(v.state.quorum, key);
    if (!board.ok) return board;
    normalized.set(key, a.value);
  }
  const highest = Math.max(...[...normalized.values()].map((a) => a.scannedThroughHeight));
  const floor = recOf(v.state.committed["jHistoryFinality"]) !== null ? jpFinalized(v) : jpFinalized(v) + 1;
  const threshold = thresholdOf(v.state.quorum);
  for (let height = highest; height >= floor; height--) {
    const groups = new Map<string, { readonly claim: JPrefixClaim; readonly signerIds: string[] }>();
    for (const [signer, a] of normalized) {
      if (a.scannedThroughHeight < height) continue;
      const claim = jpClip(v, a, height);
      if (!claim.ok) return claim;
      const key = jpClaimKey(claim.value);
      if (!key.ok) return key;
      const group = groups.get(key.value) ?? { claim: claim.value, signerIds: [] };
      group.signerIds.push(signer);
      groups.set(key.value, group);
    }
    const certified: { readonly claim: JPrefixClaim; readonly signerIds: string[] }[] = [];
    for (const g of groups.values()) { const power = jpQuorumPower(v.state.quorum, g.signerIds); if (!power.ok) return power; if (power.value >= threshold) certified.push(g); }
    if (certified.length > 1) return jpFail(`J_PREFIX_CONFLICTING_QUORUMS:${height}`);
    const [one] = certified;
    if (one !== undefined) return ok({ claim: one.claim, signerIds: [...one.signerIds].sort(asc) });
  }
  return ok(null);
};
/** og buildJPrefixCertificate. */
export const buildJPrefixCertificate = (v: JPrefixView, attestations: ReadonlyMap<string, JPrefixAttestation>): JP<JPrefixCertificate | null> => map(selectHighestWeightedCommonJPrefix(v, attestations), (selection): JPrefixCertificate | null => selection === null ? null : ({
  version: 1, entityId: nText(v.state.id), targetEntityHeight: jpEntityHeight(v) + 1, parentFrameHash: nText(jpParent(v)), jurisdictionRef: selection.claim.jurisdictionRef, baseHeight: selection.claim.baseHeight, selected: selection.claim,
  attestations: new Map([...attestations].map(([k, a]) => [nText(k), a] as const).sort(([a], [b]) => asc(a, b))),
}));
/** og verifyJPrefixCertificate: every attestation verified, the rebuilt certificate selecting exactly this claim for exactly this round. */
export const verifyJPrefixCertificate = (v: JPrefixView, rawValue: unknown, crypto: JPrefixCrypto): JP<JPrefixCertificate> => {
  const cert = recOf(rawValue) ?? {};
  if (cert["version"] !== 1) return jpFail("J_PREFIX_CERTIFICATE_VERSION_INVALID");
  const rawAttestations = cert["attestations"];
  if (!(rawAttestations instanceof Map)) return jpFail("J_PREFIX_CERTIFICATE_ATTESTATIONS_INVALID");
  const verified = new Map<string, JPrefixAttestation>();
  for (const [rawSigner, rawAttestation] of rawAttestations as ReadonlyMap<unknown, unknown>) {
    const signer = nText(rawSigner);
    if (verified.has(signer)) return jpFail(`J_PREFIX_DUPLICATE_SIGNER:${String(rawSigner)}`);
    const a = jpVerifyAttestation(v, rawAttestation, crypto);
    if (!a.ok) return a;
    if (a.value.validatorId !== signer) return jpFail(`J_PREFIX_MAP_SIGNER_MISMATCH:${String(rawSigner)}`);
    verified.set(signer, a.value);
  }
  return chain(buildJPrefixCertificate(v, verified), (rebuilt) => {
    if (rebuilt === null) return jpFail("J_PREFIX_CERTIFICATE_QUORUM_MISSING");
    return chain(jpClaim(v, recOf(cert["selected"]) ?? {}), (selected) => chain(jpClaimKey(rebuilt.selected), (a) => chain(jpClaimKey(selected), (b): JP<JPrefixCertificate> => {
      if (a !== b) return jpFail("J_PREFIX_CERTIFICATE_NOT_HIGHEST_COMMON");
      const { attestations: _rebuilt, ...expected } = rebuilt;
      const received = { version: cert["version"], targetEntityHeight: cert["targetEntityHeight"], parentFrameHash: nText(cert["parentFrameHash"]), jurisdictionRef: nText(cert["jurisdictionRef"]), baseHeight: cert["baseHeight"], selected: rebuilt.selected, entityId: nText(cert["entityId"]) };
      return consensusEqual(received, expected) ? ok({ ...rebuilt, attestations: verified }) : jpFail("J_PREFIX_CERTIFICATE_ROUND_MISMATCH");
    })));
  });
};
/** og mergeJPrefixAttestations: verified votes join the current round (a different second vote is equivocation); the round certifies when it can. */
export const mergeJPrefixAttestations = (v: JPrefixView, current: JPrefixRound | undefined, incoming: ReadonlyMap<string, unknown>, crypto: JPrefixCrypto): JP<JPrefixRound> => {
  const targetEntityHeight = jpEntityHeight(v) + 1, parentFrameHash = nText(jpParent(v)), jurisdictionRef = jEventJurisdictionRef(v.state), baseHeight = jpFinalized(v);
  const expected = current !== undefined && current.targetEntityHeight === targetEntityHeight && nText(current.parentFrameHash) === parentFrameHash && nText(current.jurisdictionRef) === jurisdictionRef && current.baseHeight === baseHeight ? current : undefined;
  const attestations = new Map<string, JPrefixAttestation>(expected?.attestations ?? []), seen = new Set<string>();
  for (const [rawSigner, rawAttestation] of incoming) {
    const signer = nText(rawSigner);
    if (seen.has(signer)) return jpFail(`J_PREFIX_DUPLICATE_SIGNER:${rawSigner}`);
    seen.add(signer);
    const a = jpVerifyAttestation(v, rawAttestation, crypto);
    if (!a.ok) return a;
    if (a.value.validatorId !== signer) return jpFail(`J_PREFIX_MAP_SIGNER_MISMATCH:${rawSigner}`);
    const previous = attestations.get(signer);
    if (previous === undefined) { attestations.set(signer, a.value); continue; }
    if (!consensusEqual(previous, a.value)) return jpFail(`J_PREFIX_ATTESTATION_EQUIVOCATION:${signer}`);
  }
  const sorted = new Map([...attestations].sort(([a], [b]) => asc(a, b)));
  return map(buildJPrefixCertificate(v, sorted), (certificate): JPrefixRound => ({ targetEntityHeight, parentFrameHash, jurisdictionRef, baseHeight, attestations: sorted, ...(certificate === null ? {} : { certificate }) }));
};
/** og assertJPrefixCertificateMatchesLocalHistory. */
const jpCertificateMatchesLocal = (v: JPrefixView, h: ValidatorJHistory | undefined, certificate: unknown, crypto: JPrefixCrypto): JP<JPrefixCertificate> => chain(verifyJPrefixCertificate(v, certificate, crypto), (verified) =>
  chain(jpLocalClaimAt(v, h, verified.selected.scannedThroughHeight), (local) => chain(jpClaimKey(local), (a) => chain(jpClaimKey(verified.selected), (b): JP<JPrefixCertificate> => a === b ? ok(verified)
    : jpFail(`J_PREFIX_LOCAL_PREFIX_MISMATCH:${verified.selected.scannedThroughHeight}:localRange=${local.rangeHash}:certRange=${verified.selected.rangeHash}:localTip=${local.tipBlockHash}:certTip=${verified.selected.tipBlockHash}`
      + `:localBlocks=${local.blocks.length}:certBlocks=${verified.selected.blocks.length}:localBase=${local.baseHeight}:certBase=${verified.selected.baseHeight}:finalized=${jpFinalized(v)}:histScan=${h?.scannedThroughHeight ?? "none"}`)))));
/** og buildCertifiedJPrefixTx: the proposer's signed j_event carrying exactly the certified prefix. */
export const buildCertifiedJPrefixTx = (v: JPrefixView, h: ValidatorJHistory | undefined, certificate: JPrefixCertificate, proposerSignerId: string, crypto: JPrefixCrypto): JP<Extract<EntityTx, { type: "j_event" }>> =>
  chain(jpCertificateMatchesLocal(v, h, certificate, crypto), (verified) => {
    const from = nText(proposerSignerId), s = verified.selected;
    return chain(jpOf(jRangeDigest({ entityId: nText(v.state.id), signerId: from, jurisdictionRef: s.jurisdictionRef, baseHeight: s.baseHeight, scannedThroughHeight: s.scannedThroughHeight, tipBlockHash: s.tipBlockHash, eventHistoryRoot: s.eventHistoryRoot, rangeHash: s.rangeHash })), (digest) => {
      if (crypto.sign === undefined) return jpFail(`CRYPTO_DETERMINISM_VIOLATION: signAccountFrame called without env.runtimeSeed for signer ${from}`);
      return map(mapErr(crypto.sign(from, digest), (): JPrefixFailure => ({ disposition: "reject", code: "J_PREFIX_INVALID", message: `J_PREFIX_SIGN_FAILED:${from}` })), (signature): Extract<EntityTx, { type: "j_event" }> =>
        ({ type: "j_event", data: { from, signature, observedAt: s.scannedThroughHeight, jurisdictionRef: s.jurisdictionRef, baseHeight: s.baseHeight, scannedThroughHeight: s.scannedThroughHeight, tipBlockHash: s.tipBlockHash, eventHistoryRoot: s.eventHistoryRoot, rangeHash: s.rangeHash, blocks: s.blocks as unknown as Binary } }));
    });
  });
/** og hasPendingLocalJEvent. */
export const hasPendingLocalJEvent = (v: JPrefixView, h: ValidatorJHistory | undefined): boolean =>
  h !== undefined && [...h.eventBlocks.keys()].some((height) => height > jpFinalized(v) && height <= h.scannedThroughHeight);
/** og hasDueLocalJPrefixAdvance (hasAttestablePendingLocalJEvent): semantic J work already inside this validator's exact provable prefix. */
export const hasDueLocalJPrefixAdvance = (v: JPrefixView, h: ValidatorJHistory | undefined): JP<boolean> => {
  if (h === undefined) return ok(false);
  return map(localJPrefixAttestableHeight(v, h), (height) => height !== null && [...h.eventBlocks.keys()].some((x) => x > jpFinalized(v) && x <= height));
};
/** og isFrozenBaseJPrefixRollAuthorized: a base-only certificate may roll one empty frame when this validator's own signed head is in it byte for byte. */
export const isFrozenBaseJPrefixRollAuthorized = (v: JPrefixView, signer: string, round: JPrefixRound | undefined, h: ValidatorJHistory | undefined, certificate: JPrefixCertificate | null | undefined): JP<boolean> => {
  if (certificate === null || certificate === undefined || certificate.selected.scannedThroughHeight !== certificate.baseHeight) return ok(false);
  return map(hasDueLocalJPrefixAdvance(v, h), (due) => {
    if (!due) return false;
    const me = nText(signer), local = [...(round?.attestations ?? [])].find(([k]) => nText(k) === me)?.[1], certified = [...certificate.attestations].find(([k]) => nText(k) === me)?.[1];
    return local !== undefined && certified !== undefined && consensusEqual(local, certified);
  });
};
/** og reconcileJEventRangeWithFinalizedState over a frame's raw j_event data (J_RANGE codes; a jurisdiction conflict with the anchor is corruption). */
const jpReconcile = (v: JPrefixView, data: JRec): JP<{ readonly baseHeight: number; readonly scannedThroughHeight: number; readonly tipBlockHash: string; readonly eventHistoryRoot: string; readonly blocks: readonly JRangeBlock[] } | null> => {
  const base = Number(data["baseHeight"]), scanned = Number(data["scannedThroughHeight"]), finalized = jpFinalized(v);
  if (!Number.isSafeInteger(base) || base < 0) return jpFail(`J_RANGE_BASE_HEIGHT_INVALID:${String(data["baseHeight"])}`);
  if (!Number.isSafeInteger(scanned) || scanned <= base) return jpFail("J_RANGE_HEIGHT_INVALID");
  if (scanned <= finalized) return ok(null);
  if (base > finalized) return jpFail(`J_RANGE_BASE_HEIGHT_AHEAD:${base}:${finalized}`);
  return chain(jpOf(certifiedJAnchor(v.state)), (anchor) => {
    const ref = nText(data["jurisdictionRef"]);
    if (anchor !== null && ref !== anchor.jurisdictionRef) return jpFail("J_HISTORY_FINALITY_JURISDICTION_CONFLICT");
    const blocks = (Array.isArray(data["blocks"]) ? (data["blocks"] as readonly JRangeBlock[]) : []).filter((b) => Number(b.blockNumber) > finalized);
    return chain(jpFinalizedRoot(v), (root) => chain(jpOf(foldJHistoryRoot(root, blocks.map((b) => ({ jurisdictionRef: ref, jHeight: Number(b.blockNumber), jBlockHash: nText(b.blockHash), eventsHash: nText(b.eventsHash), ...opt("disputeFinalizationEvidenceHash", b.disputeFinalizationEvidenceHash ? nText(b.disputeFinalizationEvidenceHash) : undefined) })))), (eventHistoryRoot) =>
      eventHistoryRoot !== nText(data["eventHistoryRoot"]) ? jpFail("J_RANGE_HISTORY_ROOT_MISMATCH") : ok({ baseHeight: finalized, scannedThroughHeight: scanned, tipBlockHash: nText(data["tipBlockHash"]), eventHistoryRoot, blocks })));
  });
};
/** The frame fields og assertFrameJPrefix reads. */
export type JPrefixFrame = { readonly height: number; readonly parentFrameHash: string; readonly proposerSignerId: string; readonly txs: readonly EntityTx[]; readonly jPrefixCertificate?: JPrefixCertificate | null | undefined };
/**
 * og assertFrameJPrefix: every frame carrying a j_event carries the certificate its range equals, checked against this validator's own J history;
 * a registered Entity certifies a prefix in every frame, and a stronger local certificate or a due local event retries the proposal.
 */
export const assertFrameJPrefix = (v: JPrefixView, signer: string, round: JPrefixRound | undefined, h: ValidatorJHistory | undefined, frame: JPrefixFrame, crypto: JPrefixCrypto): JP<void> => {
  const ranges = frame.txs.filter((tx): tx is Extract<EntityTx, { type: "j_event" }> => tx.type === "j_event");
  const locallyCertified: JP<JPrefixCertificate | null> = round === undefined ? ok(null) : buildJPrefixCertificate(v, round.attestations);
  return chain(locallyCertified, (local): JP<void> => {
    if (frame.jPrefixCertificate === undefined || frame.jPrefixCertificate === null) {
      if (ranges.length > 0) return jpFail("J_PREFIX_CERTIFICATE_MISSING");
      if (entityRequiresJPrefixCertificate(v.state)) return jpFail("J_PREFIX_CERTIFICATE_REQUIRED_FOR_REGISTERED_ENTITY");
      if (local !== null) return jpRetry("J_PREFIX_STRONGER_LOCAL_CERTIFICATE");
      return hasPendingLocalJEvent(v, h) ? jpRetry("J_PREFIX_REQUIRED_LOCAL_EVENT") : ok(undefined);
    }
    return chain(jpCertificateMatchesLocal(v, h, frame.jPrefixCertificate, crypto), (certificate): JP<void> => {
      if (frame.height !== certificate.targetEntityHeight || nText(frame.parentFrameHash) !== certificate.parentFrameHash) return jpFail("J_PREFIX_FRAME_ROUND_MISMATCH");
      const emptyBaseRoll = frame.txs.length === 0 && certificate.selected.scannedThroughHeight === certificate.baseHeight;
      if (local !== null && local.selected.scannedThroughHeight > certificate.selected.scannedThroughHeight && !emptyBaseRoll) return jpRetry("J_PREFIX_STRONGER_LOCAL_CERTIFICATE");
      if (certificate.selected.scannedThroughHeight === certificate.baseHeight) {
        if (ranges.length !== 0) return jpFail(`J_PREFIX_RANGE_COUNT_INVALID:${ranges.length}`);
        const frozen: JP<boolean> = frame.txs.length === 0 ? isFrozenBaseJPrefixRollAuthorized(v, signer, round, h, certificate) : ok(false);
        return chain(frozen, (emptyFrozenRoll) => (hasPendingLocalJEvent(v, h) && !emptyFrozenRoll && !emptyBaseRoll ? jpRetry("J_PREFIX_REQUIRED_LOCAL_EVENT") : ok(undefined)));
      }
      const [only] = ranges;
      if (ranges.length !== 1 || only === undefined) return jpFail(`J_PREFIX_RANGE_COUNT_INVALID:${ranges.length}`);
      const range = only.data as JRec;
      return chain(jpClaimEnvelope(jEventJurisdictionRef(v.state), range), (received) => chain(jpReconcile(v, range), (reconciled): JP<void> => {
        if (reconciled === null) return jpFail("J_PREFIX_FRAME_RANGE_STALE");
        return chain(jpOf(jRangeHash(received.jurisdictionRef, reconciled.blocks)), (rangeHash) => {
          const current: JPrefixClaim = { jurisdictionRef: received.jurisdictionRef, baseHeight: reconciled.baseHeight, scannedThroughHeight: reconciled.scannedThroughHeight, tipBlockHash: reconciled.tipBlockHash, eventHistoryRoot: reconciled.eventHistoryRoot, rangeHash, blocks: reconciled.blocks };
          return chain(jpClaimKey(current), (a) => chain(jpClaimKey(certificate.selected), (b): JP<void> => {
            if (a !== b) return jpFail("J_PREFIX_FRAME_RANGE_MISMATCH");
            const proposer = nText(frame.proposerSignerId);
            if (nText(range["from"]) !== proposer) return jpFail("J_PREFIX_RANGE_PROPOSER_MISMATCH");
            return chain(jpOf(jRangeDigest({ entityId: nText(v.state.id), signerId: proposer, jurisdictionRef: received.jurisdictionRef, baseHeight: received.baseHeight, scannedThroughHeight: received.scannedThroughHeight, tipBlockHash: received.tipBlockHash, eventHistoryRoot: received.eventHistoryRoot, rangeHash: received.rangeHash })),
              (digest) => (crypto.verify(proposer, digest, String(range["signature"] ?? "")) ? ok(undefined) : jpFail("J_PREFIX_RANGE_SIGNATURE_REJECTED")));
          }));
        });
      }));
    });
  });
};
/** og getJEventRangeValidationError: a proposed range must equal this validator's own J history block for block (null when it does). */
export const jEventRangeLocalHistoryError = (state: EntityState, h: ValidatorJHistory | undefined, data: JRec): Result<string | null, JPrefixFailure> => chain(jpOf(certifiedJAnchor(state)), (anchor) => {
  const range = jRangeEnvelope(state, data);
  if (!range.ok) return ok(range.error);
  const r = range.value;
  if (anchor !== null && anchor.jurisdictionRef !== r.jurisdictionRef) return ok("J_RANGE_JURISDICTION_MISMATCH");
  const reconciled = reconcileJRange(state, r);
  if (!reconciled.ok) return J_HISTORY_CORRUPTION.test(reconciled.error) ? jpFail(reconciled.error) : ok(reconciled.error);
  if (reconciled.value === null) return ok(null);
  const suffixBase = reconciled.value.baseHeight;
  if (h === undefined || nText(h.jurisdictionRef) !== r.jurisdictionRef) return ok("J_RANGE_LOCAL_HISTORY_MISSING");
  const matched = historyMatchesAnchor(anchor, h);
  if (!matched.ok) { const code = jCode(matched.error); return J_HISTORY_CORRUPTION.test(code) ? jpFail(code) : ok(code); }
  if (h.scannedThroughHeight < r.scannedThroughHeight) return ok("J_RANGE_LOCAL_HISTORY_BEHIND");
  const proposed = r.blocks.filter((b) => b.blockNumber > suffixBase);
  const localBlocks = [...h.eventBlocks.values()].filter((b) => b.jHeight > suffixBase && b.jHeight <= r.scannedThroughHeight).sort((a, b) => a.jHeight - b.jHeight);
  if (localBlocks.length !== proposed.length) return ok("J_RANGE_EVENT_BLOCK_COUNT_MISMATCH");
  for (const [i, p] of proposed.entries()) {
    const l = localBlocks[i];
    if (l === undefined || p.blockNumber !== l.jHeight || blockIdentity({ jurisdictionRef: r.jurisdictionRef, jHeight: p.blockNumber, jBlockHash: p.blockHash, eventsHash: p.eventsHash, events: p.events, ...opt("disputeFinalizationEvidenceHash", p.disputeFinalizationEvidenceHash) }) !== blockIdentity(l)) return ok("J_RANGE_EVENT_BLOCK_MISMATCH");
  }
  const known = h.blockHashes.get(r.scannedThroughHeight);
  if (!known) return ok("J_RANGE_LOCAL_TIP_UNKNOWN");
  return ok(nText(known) !== r.tipBlockHash ? "J_RANGE_TIP_HASH_MISMATCH" : null);
});
/** og pruneFinalizedValidatorJHistory: drop what the Entity has certified (the frontier keeps the certified height). */
export const pruneFinalizedJHistory = (h: ValidatorJHistory | undefined, finalized: number): Result<ValidatorJHistory | undefined, JPrefixFailure> => {
  if (h === undefined) return ok(undefined);
  if (!Number.isSafeInteger(finalized) || finalized < 0 || finalized > h.scannedThroughHeight) return jpFail(`J_HISTORY_LOCAL_PRUNE_HEIGHT_INVALID:${String(finalized)}:${h.scannedThroughHeight}`);
  return ok({ ...h, contiguousThroughHeight: Math.max(h.contiguousThroughHeight, finalized), eventBlocks: new Map([...h.eventBlocks].filter(([x]) => x > finalized)), blockHashes: new Map([...h.blockHashes].filter(([x]) => x >= finalized)) });
};
// ---- og entity/consensus/authority/board-handover.ts + entity/tx/handlers/board-handover.ts: the on-chain BoardActivated handover ----
/** og ConsensusConfig as boardHandover carries it (mode, threshold, validators, shares). */
export type HandoverConfig = { readonly mode: string; readonly threshold: bigint; readonly validators: readonly string[]; readonly shares: { readonly [signer: string]: bigint } };
/** og FinancialDataCorruptionError / TypeSafetyViolationError messages. */
const safetyText = (message: string, context?: Record<string, unknown>): string => `🚨 FINANCIAL-SAFETY VIOLATION: ${message}${context === undefined ? "" : `\nContext: ${stableJson(context)}`}`;
const typeSafetyText = (message: string, value: unknown): string => `🛡️ TYPE-SAFETY VIOLATION: ${message}${value === undefined ? "" : `\nReceived: ${typeof value} = ${String(value)}`}`;
/** og validateConsensusConfig: mode, threshold, validators, shares, then voting power (a refusal is og's Error message). */
export const consensusConfigIssue = (value: unknown, context: string): Result<HandoverConfig, string> => {
  const c = recOf(value) ?? {}, mode = c["mode"], threshold = c["threshold"];
  if (mode !== "proposer-based" && mode !== "gossip-based") return err(safetyText(`${context}.mode must be proposer-based or gossip-based`));
  if (typeof threshold !== "bigint" || threshold <= 0n) return err(safetyText(`${context}.threshold must be positive bigint`));
  if (threshold > 0xffffn) return err(safetyText(`${context}.threshold exceeds uint16 board encoding`, { threshold }));
  const rawValidators = c["validators"];
  if (!Array.isArray(rawValidators)) return err(typeSafetyText(`${context}.validators must be an array`, rawValidators));
  if (rawValidators.length === 0) return err(safetyText(`${context}.validators cannot be empty`));
  const normalized = new Set<string>(), validators: string[] = [];
  for (const [index, validator] of (rawValidators as readonly unknown[]).entries()) {
    if (typeof validator !== "string" || validator.trim().length === 0) return err(safetyText(`${context}.validators[${index}] must be a non-empty string`));
    const id = validator.trim().toLowerCase();
    if (normalized.has(id)) return err(safetyText(`${context}.validators has duplicate signer`, { validator }));
    normalized.add(id);
    validators.push(validator);
  }
  const rawShares = c["shares"];
  if (!rawShares || typeof rawShares !== "object" || Array.isArray(rawShares)) return err(typeSafetyText(`${context}.shares must be a non-null object`, rawShares));
  const shares = rawShares as Readonly<Record<string, unknown>>;
  for (const [id, power] of Object.entries(shares)) if (typeof power !== "bigint") return err(safetyText(`${context}.shares.${id} must be bigint`));
  const byId = new Map<string, bigint>();
  for (const [raw, power] of Object.entries(shares)) {
    const id = raw.trim().toLowerCase();
    if (byId.has(id)) return err(safetyText(`${context}.shares has case-duplicate signer`, { rawSigner: raw }));
    byId.set(id, power as bigint);
  }
  let total = 0n;
  for (const validator of validators) {
    const power = byId.get(validator.trim().toLowerCase());
    if (power === undefined || power <= 0n) return err(safetyText(`${context}.shares missing positive power for validator`, { validator }));
    if (power > 0xffffn) return err(safetyText(`${context}.shares exceeds uint16 board encoding`, { validator, power }));
    total += power;
  }
  for (const id of Object.keys(shares)) if (!normalized.has(id.trim().toLowerCase())) return err(safetyText(`${context}.shares contains signer outside validators`, { shareSigner: id }));
  if (total < threshold) return err(safetyText(`${context}.threshold exceeds total validator power`, { threshold, totalPower: total }));
  return ok({ mode, threshold, validators, shares: shares as Readonly<Record<string, bigint>> });
};
/** og hashBoard(encodeBoard(config)); og's encodeBoard refusals keep the rewrite's lazyBoardEncoding codes except the proposer's (og `BOARD_PROPOSER_EOA_REQUIRED:<proposer>`). */
const handoverBoardHash = (config: HandoverConfig): Result<string, EntityError> => {
  const encoded = lazyBoardEncoding({ mode: "proposer-based", threshold: config.threshold, validators: config.validators, shares: config.shares });
  if (encoded.ok) return ok(keccak256Hex(hexToBytes(encoded.value)).toLowerCase());
  return invariant(jCode(encoded.error) === "BOARD_PROPOSER_EOA_REQUIRED" ? `BOARD_PROPOSER_EOA_REQUIRED:${config.validators[0] ?? ""}` : jCode(encoded.error));
};
/** og assertCanonicalConfig: a valid config in the Entity's own mode with lowercase validators and share signers. */
const canonicalHandoverConfig = (board: unknown): Result<HandoverConfig, EntityError> => {
  const config = consensusConfigIssue(board, "BOARD_HANDOVER_CONFIG");
  if (!config.ok) return invariant(`BOARD_HANDOVER_CONFIG_INVALID:${config.error}`);
  const c = config.value;
  if (c.mode !== "proposer-based") return invariant(`BOARD_HANDOVER_MODE_CHANGE_FORBIDDEN:proposer-based:${c.mode}`);
  for (const v of c.validators) if (v !== v.trim().toLowerCase()) return invariant(`BOARD_HANDOVER_VALIDATOR_NON_CANONICAL:${v}`);
  for (const s of Object.keys(c.shares)) if (s !== s.trim().toLowerCase()) return invariant(`BOARD_HANDOVER_SHARE_SIGNER_NON_CANONICAL:${s}`);
  return ok(c);
};
/**
 * og getBoardHandoverFrameConfig: a frame carrying boardHandover must be exactly [j_event, boardHandover], and the j_event's own
 * BoardActivated chain must run from the committed board to the new config's board. The authority the frame is certified under.
 */
export const handoverFrameConfig = (state: EntityState, txs: readonly EntityTx[]): Result<HandoverConfig | null, EntityError> => {
  const handovers = txs.filter((tx) => tx.type === "boardHandover");
  if (handovers.length === 0) return ok(null);
  if (handovers.length !== 1) return invariant(`BOARD_HANDOVER_COUNT_INVALID:${handovers.length}`);
  const [range, handover] = txs;
  if (txs.length !== 2 || range?.type !== "j_event" || handover?.type !== "boardHandover") return invariant(`BOARD_HANDOVER_FRAME_SHAPE_INVALID:${txs.map((tx) => tx.type).join(",")}`);
  return chain(canonicalHandoverConfig(handover.data.board), (config) => {
    const me = lower(state.id), blocks = Array.isArray(range.data["blocks"]) ? (range.data["blocks"] as readonly unknown[]) : [];
    const activations = blocks.flatMap((b) => { const events = recOf(b)?.["events"]; return Array.isArray(events) ? (events as readonly unknown[]) : []; })
      .map((e) => recOf(e) ?? {}).filter((e) => e["type"] === "BoardActivated" && lower((recOf(e["data"]) ?? {})["entityId"]) === me).map((e) => recOf(e["data"]) ?? {});
    if (activations.length === 0) return invariant("BOARD_HANDOVER_ACTIVATION_MISSING");
    let expected = configBoardHash(state.quorum).toLowerCase();
    for (const a of activations) {
      const received = lower(a["previousBoardHash"]);
      if (received !== expected) return invariant(`BOARD_HANDOVER_ACTIVATION_CHAIN_INVALID:${received}:${expected}`);
      expected = lower(a["newBoardHash"]);
    }
    return chain(handoverBoardHash(config), (hash) => (hash !== expected ? invariant(`BOARD_HANDOVER_CONFIG_HASH_MISMATCH:${hash}:${expected}`) : ok(config)));
  });
};
/**
 * og handleBoardHandoverEntityTx: the frame-authorized config replaces the board once the Entity's certified registry holds its own
 * BoardActivated record for exactly that board; the new CEO leads from view 0. A validator is an EOA or a nested Entity id (og toBoardEntityId).
 */
const entityBoardHandover = (d: Draft, board: unknown, authorized: HandoverConfig | undefined): Result<Draft, EntityError> => {
  const state = d.state, config = consensusConfigIssue(board, "BOARD_HANDOVER_CONFIG");
  if (!config.ok) return invariant(config.error);
  const c = config.value;
  if (c.mode !== "proposer-based") return invariant("BOARD_HANDOVER_MODE_CHANGE_FORBIDDEN");
  if (authorized === undefined) return invariant("BOARD_HANDOVER_TRANSITION_PROOF_REQUIRED");
  return chain(observerBoardRecord(state, state.id), (record) => {
    if (record === null || record.source !== "BoardActivated") return invariant("BOARD_HANDOVER_CERTIFIED_ACTIVATION_REQUIRED");
    const previous = configBoardHash(state.quorum).toLowerCase();
    return chain(handoverBoardHash(c), (next) => chain(handoverBoardHash(authorized), (auth) => {
      if (auth !== next || record.boardHash !== next) return invariant(`BOARD_HANDOVER_CERTIFIED_AUTHORITY_MISMATCH:previous=${record.previousBoardHash}:${previous}:certified=${record.boardHash}:authorized=${auth}:next=${next}`);
      return map(withBoardAuthority(state, c, Number(state.height) + 1), (next) => ({ ...d, state: next, touched: [] }));
    }));
  });
};
// ---- og entity/consensus/authority/board-handover.ts + proposal/policy.ts: the consensus side of a board handover ----
/** og withBoardAuthority's board: the config's validators (an EOA, or a nested Entity id as og toBoardEntityId keeps it) with their shares. */
const quorumOfConfig = (c: HandoverConfig): Result<Quorum, EntityError> => {
  const members = new Map<Address, { readonly shares: bigint }>();
  for (const v of c.validators) {
    const share = Object.entries(c.shares).find(([k]) => lower(k) === lower(v))?.[1], nested = /^0x[0-9a-f]{64}$/i.test(v), a = nested ? ok(lower(v) as Address) : address(v);
    if (!a.ok || share === undefined) return invariant(`BOARD_HANDOVER_VALIDATOR_INVALID:${v}`);
    members.set(a.value, { shares: share });
  }
  return admitQuorum({ _tag: "teaching", threshold: c.threshold, members });
};
/** og withBoardAuthority: the state under the handed-over board, its first validator leading from view 0. */
const withBoardAuthority = (state: EntityState, c: HandoverConfig, changedAtHeight: number): Result<EntityState, EntityError> => {
  const active = c.validators[0];
  if (active === undefined) return invariant("BOARD_HANDOVER_VALIDATOR_MISSING");
  return map(quorumOfConfig(c), (quorum) => ({ ...state, quorum, leaderState: { activeValidatorId: active, view: 0, changedAtHeight } }));
};
/** og getPendingBoardHandoverConfig: uncommitted local coordination; one queued handover names the board that assembles the transition. */
const pendingHandoverConfig = (state: EntityState, txs: readonly EntityTx[]): Result<HandoverConfig | null, EntityError> => {
  const handovers = txs.filter((tx): tx is Extract<EntityTx, { type: "boardHandover" }> => tx.type === "boardHandover");
  const [only] = handovers;
  if (only === undefined) return ok(null);
  return handovers.length !== 1 ? invariant(`BOARD_HANDOVER_COUNT_INVALID:${handovers.length}`) : canonicalHandoverConfig(only.data.board);
};
/** og `pendingConfig ? { ...replica, state: withBoardAuthority(replica.state, pendingConfig) } : replica` (admission and proposal selection). */
const authorityReplica = <R extends EntityEnv>(r: R, txs: readonly EntityTx[]): Result<R, EntityError> =>
  chain(pendingHandoverConfig(r.state, txs), (c) => (c === null ? ok(r) : map(withBoardAuthority(r.state, c, Number(r.state.height) + 1), (state) => ({ ...r, state }))));
/** og getEntityFrameConsensusConfig: the board a frame's signatures are counted under. */
const frameQuorum = (state: EntityState, txs: readonly EntityTx[]): Result<Quorum, EntityError> =>
  chain(handoverFrameConfig(state, txs), (c) => (c === null ? ok(state.quorum) : quorumOfConfig(c)));
/** og getBoardHandoverLeaderState. */
const handoverLeaderState = (state: EntityState, txs: readonly EntityTx[]): Result<LeaderState | null, EntityError> =>
  chain(handoverFrameConfig(state, txs), (c) => {
    if (c === null) return ok(null);
    const active = c.validators[0];
    return active === undefined ? invariant("BOARD_HANDOVER_VALIDATOR_MISSING") : ok({ activeValidatorId: active, view: 0, changedAtHeight: Number(state.height) + 1 });
  });
type JEventTx = Extract<EntityTx, { type: "j_event" }>;
const jRangeEvents = (tx: JEventTx): readonly JRec[] => {
  const blocks = Array.isArray(tx.data["blocks"]) ? (tx.data["blocks"] as readonly unknown[]) : [];
  return blocks.flatMap((b) => { const events = recOf(b)?.["events"]; return Array.isArray(events) ? (events as readonly unknown[]) : []; }).map((e) => recOf(e) ?? {});
};
/** og getSelfAuthorityTargetFromJRange: the last board this range registers, activates or (for the Foundation) bootstraps for the Entity. */
const selfAuthorityTarget = (tx: JEventTx, entityId: string): string | null => {
  const me = lower(entityId);
  let target: string | null = null;
  for (const e of jRangeEvents(tx)) {
    const d = recOf(e["data"]) ?? {};
    if (e["type"] === "FoundationBootstrapped" && me === FOUNDATION_ENTITY_ID) target = lower(d["boardHash"]);
    else if ((e["type"] === "EntityRegistered" || e["type"] === "BoardActivated") && lower(d["entityId"]) === me) target = lower(e["type"] === "EntityRegistered" ? d["boardHash"] : d["newBoardHash"]);
  }
  return target;
};
/** og nestedEntityTxs. */
const nestedEntityTxs = (tx: EntityTx): readonly EntityTx[] => (tx.type === "runtimeOutput" ? tx.data.entityTxs : tx.type === "entityCommand" ? tx.data.txs : []);
/** og collectCounterpartyBoardActivations. */
const counterpartyActivations = (txs: readonly EntityTx[], me: string, into: Set<string>): Set<string> => {
  for (const tx of txs) {
    if (tx.type !== "j_event") { counterpartyActivations(nestedEntityTxs(tx), me, into); continue; }
    for (const e of jRangeEvents(tx)) { if (e["type"] !== "BoardActivated") continue; const target = lower((recOf(e["data"]) ?? {})["entityId"]); if (target !== me) into.add(target); }
  }
  return into;
};
/** og findConflictingAccountInput. */
const conflictingAccountInput = (tx: EntityTx, activated: ReadonlySet<string>): string | null => {
  if (tx.type === "accountInput") { const from = lower(tx.data.fromEntityId); return activated.has(from) ? from : null; }
  for (const nested of nestedEntityTxs(tx)) { const c = conflictingAccountInput(nested, activated); if (c !== null) return c; }
  return null;
};
/** og findCounterpartyBoardActivationConflict: one frame may not both activate a counterparty's board and carry that counterparty's Account row. */
export const counterpartyBoardActivationConflict = (entityId: string, txs: readonly EntityTx[]): string | null => {
  const activated = counterpartyActivations(txs, lower(entityId), new Set());
  if (activated.size === 0) return null;
  for (const tx of txs) { const c = conflictingAccountInput(tx, activated); if (c !== null) return c; }
  return null;
};
/** og withoutCounterpartyBoardActivationConflicts: the certified range keeps priority, the counterparty's rows wait for the next frame. */
export const withoutCounterpartyBoardActivationConflicts = (entityId: string, txs: readonly EntityTx[]): readonly EntityTx[] => {
  const activated = counterpartyActivations(txs, lower(entityId), new Set());
  return activated.size === 0 ? txs : txs.filter((tx) => conflictingAccountInput(tx, activated) === null);
};
/** og getEntityFrameJRangeBudgetError over several ranges: valid spans, then their canonical frame payload within 10 MiB. */
const jRangePayloadBytes = (ranges: readonly JEventTx[]): Result<number, EntityError> => {
  for (const r of ranges) {
    const base = Number(r.data["baseHeight"]), scanned = Number(r.data["scannedThroughHeight"]);
    if (!Number.isSafeInteger(base) || base < 0) return invariant(`J_RANGE_FRAME_BASE_HEIGHT_INVALID:${String(r.data["baseHeight"])}`);
    if (!Number.isSafeInteger(scanned) || scanned <= base) return invariant(`J_RANGE_FRAME_SCANNED_HEIGHT_INVALID:${String(r.data["scannedThroughHeight"])}`);
  }
  const bytes = authConsensusBytes({ domain: J_RANGE_FRAME_PAYLOAD_DOMAIN, version: 1, ranges: ranges.map((r) => r.data) });
  return bytes.ok ? ok(bytes.value.length) : invariant("CANONICAL_ENCODING_INVALID");
};
const frameJRangeIssue = (ranges: readonly JEventTx[]): Result<string | null, EntityError> =>
  map(jRangePayloadBytes(ranges), (n) => (n > MAX_ENTITY_FRAME_J_RANGE_BYTES ? `J_RANGE_FRAME_BYTE_LIMIT_EXCEEDED:${n}:${MAX_ENTITY_FRAME_J_RANGE_BYTES}` : null));
/** og selectEntityTxsWithinJRangeBudget: an ordered prefix; once a range overflows the budget the whole suffix waits, a range that can never fit halts. */
export const jRangeBudgetPrefix = (txs: readonly EntityTx[]): Result<readonly EntityTx[], EntityError> => {
  const selected: EntityTx[] = [], ranges: JEventTx[] = [];
  for (const tx of txs) {
    if (tx.type !== "j_event") { selected.push(tx); continue; }
    const alone = frameJRangeIssue([tx]);
    if (!alone.ok) return alone;
    if (alone.value !== null) return invariant(`J_RANGE_SINGLE_RANGE_UNPROPOSABLE:${alone.value}`);
    const all = frameJRangeIssue([...ranges, tx]);
    if (!all.ok) return all;
    if (all.value !== null) break;
    selected.push(tx); ranges.push(tx);
  }
  return ok(selected);
};
/** og MAX_ENTITY_FRAME_TXS and MAX_ENTITY_FRAME_TX_BYTES (half of LIMITS.MAX_FRAME_SIZE_BYTES): each tx costs a 4-byte length prefix plus its canonical frame-hash bytes. */
export const MAX_ENTITY_FRAME_TXS = 10_000, MAX_ENTITY_FRAME_TX_BYTES = Math.floor(100_000_000 / 2);
const frameTxBytes = (txs: readonly EntityTx[]): Result<readonly number[], EntityError> =>
  traverse(txs, (tx) => chain(entityFrameTx(tx), (ftx) => chain(entityTxForHash(ftx), (hashed) => map(encodeBinary(hashed as Binary), (b) => 4 + b.byteLength))));
/** og selectEntityFrameTxByteBudgetWithMeter: at most MAX_ENTITY_FRAME_TXS, then the longest prefix within the byte budget; a head tx over it halts. */
export const frameTxBudgetPrefix = (all: readonly EntityTx[]): Result<readonly EntityTx[], EntityError> => {
  const txs = all.length > MAX_ENTITY_FRAME_TXS ? all.slice(0, MAX_ENTITY_FRAME_TXS) : all;
  return chain(frameTxBytes(txs), (sizes) => {
    let total = 0, count = 0;
    for (const n of sizes) { if (total + n > MAX_ENTITY_FRAME_TX_BYTES) break; total += n; count += 1; }
    if (count === txs.length) return ok(txs);
    return count === 0 ? invariant(`ENTITY_FRAME_HEAD_TX_BYTE_LIMIT_EXCEEDED:${sizes[0] ?? 0}:${MAX_ENTITY_FRAME_TX_BYTES}`) : ok(txs.slice(0, count));
  });
};
/** og assertEntityFrameTxByteBudget + assertEntityFrameJRangeBudget at the head of applyEntityFrame. */
const frameBudgets = (txs: readonly EntityTx[]): Result<void, EntityError> =>
  chain(frameTxBytes(txs), (sizes) => {
    const n = sizes.reduce((t, s) => t + s, 0);
    if (n > MAX_ENTITY_FRAME_TX_BYTES) return invariant(`ENTITY_FRAME_TX_BYTE_LIMIT_EXCEEDED:${n}:${MAX_ENTITY_FRAME_TX_BYTES}`);
    return chain(frameJRangeIssue(txs.filter((tx): tx is JEventTx => tx.type === "j_event")), (issue) => (issue === null ? ok(undefined) : invariant(issue)));
  });
const budgeted = (txs: readonly EntityTx[]): Result<readonly EntityTx[], EntityError> => chain(jRangeBudgetPrefix(txs), frameTxBudgetPrefix);
export type ProposableSelection = { readonly txs: readonly EntityTx[]; readonly currentAuthorityReady: boolean };
/** og currentAuthorityReady: a lazy Entity (id = its config board), or a certified self record for exactly the config board. */
const currentAuthorityReady = (state: EntityState): Result<boolean, EntityError> => {
  const configHash = quorumBoardHash(state.quorum);
  return configHash === lower(state.id) ? ok(true) : map(observerBoardRecord(state, lower(state.id)), (record) => record?.boardHash === configHash);
};
/**
 * og selectProposableEntityTxs: only txs whose authority prerequisites are committed. A self board range isolates itself (with its one
 * handover when it moves off the config board); a handover waits for its activation; an uncertified board proposes nothing; otherwise
 * counterparty board activations and cross-j setup keep priority, then the J-range and frame byte budgets cut the prefix.
 */
export const selectProposable = (state: EntityState, mempool: readonly EntityTx[]): Result<ProposableSelection, EntityError> =>
  chain(currentAuthorityReady(state), (ready): Result<ProposableSelection, EntityError> => {
    const configHash = quorumBoardHash(state.quorum), me = lower(state.id);
    const selfRanges = mempool.filter((tx): tx is JEventTx => tx.type === "j_event").map((tx) => ({ tx, target: selfAuthorityTarget(tx, me) })).filter((e) => e.target !== null);
    const handovers = mempool.filter((tx) => tx.type === "boardHandover");
    const latest = selfRanges[selfRanges.length - 1];
    if (latest !== undefined) {
      if (latest.target !== configHash) {
        const [handover] = handovers;
        if (handovers.length !== 1 || handover === undefined) return ok({ txs: [], currentAuthorityReady: ready });
        const transition = [latest.tx, handover];
        return map(handoverFrameConfig(state, transition), () => ({ txs: transition, currentAuthorityReady: true }));
      }
      return map(budgeted(selfRanges.map((e) => e.tx)), (txs) => ({ txs, currentAuthorityReady: ready }));
    }
    if (handovers.length > 1) return invariant(`BOARD_HANDOVER_COUNT_INVALID:${handovers.length}`);
    if (handovers.length === 1 || !ready) return ok({ txs: [], currentAuthorityReady: ready });
    return map(budgeted(selectCommitPhaseTxs(withoutCounterpartyBoardActivationConflicts(me, mempool))), (txs) => ({ txs, currentAuthorityReady: true }));
  });
/** og isSelfBoardAuthorityTransitionFrame: a handover frame, or a pure J-range frame that certifies the (not yet certified) config board. */
export const selfAuthorityTransitionFrame = (state: EntityState, txs: readonly EntityTx[]): Result<boolean, EntityError> =>
  chain(handoverFrameConfig(state, txs), (c): Result<boolean, EntityError> => {
    if (c !== null) return ok(true);
    if (txs.length === 0 || txs.some((tx) => tx.type !== "j_event")) return ok(false);
    const configHash = quorumBoardHash(state.quorum);
    if (configHash === lower(state.id)) return ok(false);
    return chain(observerBoardRecord(state, state.id), (record) => {
      if (record?.boardHash === configHash) return ok(false);
      const targets = (txs as readonly JEventTx[]).map((tx) => selfAuthorityTarget(tx, state.id)).filter((t): t is string => t !== null);
      return ok(targets[targets.length - 1] === configHash);
    });
  });
/** og entity/auth/signer-wallet.ts on the committed externalWallet {balances, allowances}: owner -> token (or token:spender) -> row. */
type WalletBook = ReadonlyMap<string, ReadonlyMap<string, Binary>>;
const NATIVE_EXTERNAL_TOKEN = `0x${"00".repeat(20)}`;
const walletAddress = (v: unknown, label: string): Result<string, EntityError> => { const s = String(v || "").trim().toLowerCase(); return /^0x[0-9a-f]{40}$/.test(s) ? ok(s) : invariant(`j_event rejected: invalid external wallet ${label}`); };
const walletTokenId = (v: unknown): number | undefined => (typeof v === "number" && Number.isInteger(v) && v >= 0 ? v : undefined);
/** og applyExternalWalletJEvent: a signer-Entity validator's on-chain wallet snapshot (baseline) or delta (on an existing baseline row). */
const externalWalletJEvent = (step: JEventStep, e: WireJEvent, blockNumber: number, txHash: string): Result<JEventStep, EntityError> => {
  const state = step.draft.state, d = e.data;
  if (lower(d["entityId"]) !== lower(state.id)) return ok(step);
  return chain(walletAddress(d["owner"], "owner"), (owner): Result<JEventStep, EntityError> => {
    if (![...membersOf(state.quorum).keys()].some((v) => lower(v) === owner)) return invariant(`EXTERNAL_WALLET_OWNER_NOT_SIGNER entity=${String(state.id).slice(0, 12)} owner=${owner}`);
    const held = recOf(state.committed["externalWallet"]), book = (k: string): WalletBook => { const v = held?.[k]; return v instanceof Map ? (v as WalletBook) : new Map(); };
    const balances = book("balances"), allowances = book("allowances"), jHeight = Number(e.blockNumber ?? blockNumber);
    if (!Number.isSafeInteger(jHeight) || jHeight < 0) return invariant("PROTOCOL_J_HEIGHT_INVALID");
    const own = new Map(balances.get(owner) ?? []), ownAllowed = new Map(allowances.get(owner) ?? []);
    const done = (nextBalances: WalletBook, nextAllowances: WalletBook, kind: string): JEventStep => {
      const changed = nextBalances !== balances || nextAllowances !== allowances, wallet: Binary = { balances: nextBalances, allowances: nextAllowances } as unknown as Binary;
      const next = changed ? { ...state, committed: { ...state.committed, externalWallet: wallet } } : state;
      return { ...step, draft: jSay({ ...step.draft, state: next }, `💼 EXTERNAL: ${owner.slice(0, 10)} ${kind} | Block ${blockNumber} | Tx ${txHash.slice(0, 10)}...`) };
    };
    if (e.type === "ExternalWalletSnapshot") {
      if (d["nativeBalance"] !== undefined) own.set(NATIVE_EXTERNAL_TOKEN, { tokenAddress: NATIVE_EXTERNAL_TOKEN, tokenId: 0, balance: BigInt(String(d["nativeBalance"])), jHeight, transactionHash: txHash } as unknown as Binary);
      for (const raw of (Array.isArray(d["tokenBalances"]) ? d["tokenBalances"] : []) as readonly unknown[]) {
        const entry = recOf(raw) ?? {}, token = walletAddress(entry["tokenAddress"], "tokenAddress");
        if (!token.ok) return token;
        const tokenId = walletTokenId(entry["tokenId"]);
        own.set(token.value, { tokenAddress: token.value, ...(tokenId !== undefined ? { tokenId } : {}), balance: BigInt(String(entry["balance"])), jHeight, transactionHash: txHash } as unknown as Binary);
      }
      for (const raw of (Array.isArray(d["allowances"]) ? d["allowances"] : []) as readonly unknown[]) {
        const entry = recOf(raw) ?? {}, token = walletAddress(entry["tokenAddress"], "tokenAddress");
        if (!token.ok) return token;
        const spender = walletAddress(entry["spender"], "spender");
        if (!spender.ok) return spender;
        ownAllowed.set(`${token.value}:${spender.value}`, { tokenAddress: token.value, spender: spender.value, allowance: BigInt(String(entry["allowance"])), jHeight, transactionHash: txHash } as unknown as Binary);
      }
      return ok(done(mapSet(balances, owner, own), mapSet(allowances, owner, ownAllowed), "snapshot"));
    }
    return chain(walletAddress(d["tokenAddress"], "tokenAddress"), (token): Result<JEventStep, EntityError> => {
      const entity = String(d["entityId"]).slice(0, 12);
      let nextBalances = balances, nextAllowances = allowances;
      if (d["balanceDelta"] !== undefined) {
        const current = balances.get(owner)?.get(token) as { readonly tokenId?: number; readonly balance: bigint } | undefined;
        if (current === undefined) return invariant(`EXTERNAL_WALLET_BASELINE_MISSING:balance entity=${entity} owner=${owner} token=${token}`);
        const balance = current.balance + BigInt(String(d["balanceDelta"]));
        if (balance < 0n) return invariant(`EXTERNAL_WALLET_BALANCE_UNDERFLOW entity=${entity} owner=${owner} token=${token}`);
        const tokenId = walletTokenId(d["tokenId"]) ?? current.tokenId;
        own.set(token, { tokenAddress: token, ...(tokenId !== undefined ? { tokenId } : {}), balance, jHeight, transactionHash: txHash } as unknown as Binary);
        nextBalances = mapSet(balances, owner, own);
      }
      if (d["allowance"] !== undefined || d["spender"] !== undefined) {
        const spender = walletAddress(d["spender"], "spender");
        if (!spender.ok) return spender;
        const current = allowances.get(owner)?.get(`${token}:${spender.value}`) as { readonly allowance: bigint } | undefined;
        if (current === undefined) return invariant(`EXTERNAL_WALLET_BASELINE_MISSING:allowance entity=${entity} owner=${owner} token=${token} spender=${spender.value}`);
        ownAllowed.set(`${token}:${spender.value}`, { tokenAddress: token, spender: spender.value, allowance: d["allowance"] !== undefined ? BigInt(String(d["allowance"])) : current.allowance, jHeight, transactionHash: txHash } as unknown as Binary);
        nextAllowances = mapSet(allowances, owner, ownAllowed);
      }
      return ok(done(nextBalances, nextAllowances, "delta"));
    });
  });
};
// ---- og jurisdiction/machine/registration-evidence, receipt-codec verifyCanonicalReceiptProof (@ethereumjs/mpt 10 + @ethereumjs/rlp 10) ----
type RlpItem = Uint8Array | readonly RlpItem[];
/** @ethereumjs/rlp _decode: canonical prefixes only; a failure is null. */
const rlpDecodeAt = (input: Uint8Array): { readonly data: RlpItem; readonly rest: Uint8Array } | null => {
  const first = input[0] ?? 0, slice = (s: number, e: number): Uint8Array | null => (e > input.length ? null : input.slice(s, e));
  const lengthOf = (v: Uint8Array | null): number | null => { if (v === null || v.length === 0 || v[0] === 0) return null; const n = Number.parseInt(nobleHex(v), 16); return Number.isNaN(n) ? null : n; };
  const list = (body: Uint8Array | null, end: number): { readonly data: RlpItem; readonly rest: Uint8Array } | null => {
    if (body === null) return null;
    const items: RlpItem[] = [];
    for (let rest = body; rest.length > 0;) { const d = rlpDecodeAt(rest); if (d === null) return null; items.push(d.data); rest = d.rest; }
    return { data: items, rest: input.subarray(end) };
  };
  if (first <= 0x7f) return { data: input.slice(0, 1), rest: input.subarray(1) };
  if (first <= 0xb7) {
    const length = first - 0x7f, data = first === 0x80 ? new Uint8Array(0) : slice(1, length);
    return data === null || (length === 2 && (data[0] ?? 0) < 0x80) ? null : { data, rest: input.subarray(length) };
  }
  if (first <= 0xbf) {
    const lLength = first - 0xb6;
    if (input.length - 1 < lLength) return null;
    const length = lengthOf(slice(1, lLength));
    if (length === null || length <= 55) return null;
    const data = slice(lLength, length + lLength);
    return data === null ? null : { data, rest: input.subarray(length + lLength) };
  }
  if (first <= 0xf7) { const length = first - 0xbf; return list(slice(1, length), length); }
  const lLength = first - 0xf6, length = lengthOf(slice(1, lLength));
  if (length === null || length < 56 || lLength + length > input.length) return null;
  return list(slice(lLength, lLength + length), lLength + length);
};
/** @ethereumjs/rlp decode: empty input is the empty string; anything left over is an error. */
const rlpDecode = (input: Uint8Array): RlpItem | null => { if (input.length === 0) return new Uint8Array(0); const d = rlpDecodeAt(input); return d === null || d.rest.length !== 0 ? null : d.data; };
/**
 * @ethereumjs/mpt verifyMPTWithMerkleProof: the first node must hash to the root; the key is walked through hashed or embedded nodes.
 * The value, or null for a proven absence; og throws on a missing node ("Invalid proof provided") or a malformed one.
 */
const mptProofGet = (root: Uint8Array, key: Uint8Array, proof: readonly Uint8Array[]): Result<Uint8Array | null, RuntimeError> => {
  const db = new Map(proof.map((node) => [nobleHex(keccak_256(node)), node] as const));
  if (proof[0] !== undefined && nobleHex(keccak_256(proof[0])) !== nobleHex(root)) return txErr("Invalid proof nodes given");
  const target = nibblesOf(key);
  let ref: RlpItem = root, progress = 0;
  for (;;) {
    let raw: RlpItem;
    if (Array.isArray(ref)) raw = ref;
    else {
      const bytes = db.get(nobleHex(ref as Uint8Array));
      if (bytes === undefined) return txErr("Invalid proof provided");
      const decoded = rlpDecode(bytes);
      if (decoded === null) return txErr("invalid RLP");
      if (!Array.isArray(decoded)) return txErr("Invalid node");
      raw = decoded;
    }
    const node = raw as readonly RlpItem[];
    if (node.length === 17) {
      if (progress === target.length) { const v = node[16]; return ok(v instanceof Uint8Array && v.length > 0 ? v : null); }
      const child = node[target[progress] ?? 0];
      if (child === undefined || child.length === 0) return ok(null);
      progress += 1;
      ref = child;
      continue;
    }
    if (node.length !== 2 || !(node[0] instanceof Uint8Array)) return txErr("Invalid node");
    const encoded = nibblesOf(node[0]), leaf = (encoded[0] ?? 0) > 1, path = (encoded[0] ?? 0) % 2 ? encoded.slice(1) : encoded.slice(2);
    if (leaf && target.length - progress > path.length) return ok(null);
    for (const nibble of path) { if (nibble !== target[progress]) return ok(null); progress += 1; }
    const next = node[1];
    if (leaf) return next instanceof Uint8Array ? ok(next) : txErr("Invalid node");
    if (next === undefined) return txErr("Invalid node");
    ref = next;
  }
};
/** og RLP.encode(BigInt(index)): 0 is the empty string. */
const receiptTrieKey = (index: number): Uint8Array => (index === 0 ? Uint8Array.of(0x80) : rlp(magnitude(BigInt(index))));
/** og CertifiedRegistrationEvidence (types/jurisdiction-runtime.ts): an MPT receipt proof or a Tron RPC attestation, witness-signed. */
export type RegistrationEvidence = { readonly [field: string]: Binary };
const J_AUTH_ZERO = `0x${"00".repeat(32)}`, FOUNDATION_ENTITY = `0x${"00".repeat(31)}01`;
const authBytes32 = (v: unknown, label: string): Result<string, RuntimeError> => { const s = String(v ?? "").trim().toLowerCase(); return /^0x[0-9a-f]{64}$/.test(s) ? ok(s) : txErr(`J_AUTHORITY_${label}_INVALID:${s || "missing"}`); };
const authAddress = (v: unknown, label: string): Result<string, RuntimeError> => { const a = ethAddress(String(v ?? "")); return a === null ? txErr(`J_AUTHORITY_${label}_INVALID:${String(v ?? "")}`) : ok(a); };
const authInt = (v: unknown, label: string): Result<number, RuntimeError> => { const n = Number(v); return Number.isSafeInteger(n) && n >= 0 ? ok(n) : txErr(`J_AUTHORITY_${label}_INVALID:${String(v)}`); };
const authHex = (v: unknown, label: string, maxBytes: number): Result<string, RuntimeError> => {
  const s = String(v ?? "");
  if (!/^0x(?:[0-9a-f]{2})*$/.test(s)) return txErr(`J_AUTHORITY_${label}_INVALID`);
  const n = (s.length - 2) / 2;
  return n > maxBytes ? txErr(`J_AUTHORITY_${label}_OVERSIZED:${n}:${maxBytes}`) : ok(s);
};
const authConsensusBytes = (v: unknown): Result<Uint8Array, BinaryError> => chain(binaryBody(v), encodeConsensus);
/** og keccakBytesHash(encodeCanonicalConsensusBytes(v)): undefined fields dropped. */
const authHash = (v: unknown): Result<string, RuntimeError> => mapErr(map(authConsensusBytes(v), (b) => bytesToHex(keccak_256(b))), (): RuntimeError => ({ _tag: "runtime_tx", code: "CANONICAL_ENCODING_INVALID" }));
/** og canonicalConsensusValuesEqual. */
const consensusEqual = (a: unknown, b: unknown): boolean => { const x = authConsensusBytes(a), y = authConsensusBytes(b); return x.ok && y.ok && compareBytes(x.value, y.value) === 0; };
const isTron = (e: RegistrationEvidence): boolean => e["receiptKind"] === "tron-rpc-attested";
const topicsOfEvidence = (e: RegistrationEvidence): readonly unknown[] => (Array.isArray(e["topics"]) ? e["topics"] : []);
export const registrationEvidenceKey = (stackKey: unknown, entityId: unknown): Result<string, RuntimeError> =>
  chain(authBytes32(stackKey, "STACK_KEY"), (s) => map(authBytes32(entityId, "ENTITY_ID"), (e) => `${s}:${e}`));
/** og buildRegistrationEvidenceRawLogDigest. */
const rawLogDigest = (e: RegistrationEvidence): Result<string, RuntimeError> =>
  chain(authAddress(e["emitter"], "EMITTER"), (emitter) => chain(traverse(topicsOfEvidence(e).map((t, i) => [t, i] as const), ([t, i]) => authBytes32(t, `TOPIC_${i}`)), (topics) =>
    chain(authInt(e["activationHeight"], "ACTIVATION_HEIGHT"), (activationHeight) => chain(authBytes32(e["blockHash"], "BLOCK_HASH"), (blockHash) =>
      chain(authBytes32(e["transactionHash"], "TRANSACTION_HASH"), (transactionHash) => chain(authInt(e["transactionIndex"], "TRANSACTION_INDEX"), (transactionIndex) =>
        chain(authInt(e["logIndex"], "LOG_INDEX"), (logIndex) => authHash({ domain: "xln.j-authority.raw-log.v1", emitter, topics, data: String(e["data"]).toLowerCase(), activationHeight, blockHash, transactionHash, transactionIndex, logIndex }))))))));
/** og buildRegistrationEvidenceDigest: what the witness Runtime signs (everything but its signature). */
const registrationEvidenceDigest = (e: RegistrationEvidence): Result<string, RuntimeError> => {
  const { witnessSignature: _signature, ...body } = e;
  return authHash({ domain: "xln.j-authority.witness.v1", evidence: body });
};
/** og computeRegistrationEvidenceClaimHash: the receipt claim, independent of which witness saw it when. */
export const registrationClaimHash = (e: RegistrationEvidence): Result<string, RuntimeError> => authHash({
  domain: "xln.j-authority.receipt-claim.v1", version: e["version"], source: e["source"], stackKey: e["stackKey"], entityId: e["entityId"], boardHash: e["boardHash"], activationHeight: e["activationHeight"],
  blockHash: e["blockHash"], transactionHash: e["transactionHash"], transactionIndex: e["transactionIndex"], logIndex: e["logIndex"], emitter: e["emitter"], topics: e["topics"], data: e["data"], rawLogDigest: e["rawLogDigest"],
  ...(isTron(e) ? { receiptKind: e["receiptKind"], chainId: e["chainId"], finality: e["finality"] } : { receiptsRoot: e["receiptsRoot"], encodedReceipt: e["encodedReceipt"], receiptProofNodes: e["receiptProofNodes"] }),
  receiptLogIndex: e["receiptLogIndex"],
});
/** og computeRegistrationEvidenceHash. */
export const registrationEvidenceHash = (e: RegistrationEvidence): Result<string, RuntimeError> => authHash({ domain: "xln.j-authority.evidence.v1", evidence: e });
/** og canonicalRegistrationReceipt: the Tron attestation fields, or a bounded MPT proof. */
const canonicalRegistrationReceipt = (e: RegistrationEvidence): Result<Readonly<Record<string, unknown>>, RuntimeError> => {
  if (isTron(e)) {
    if (e["finality"] !== "tron-solidified") return txErr("J_AUTHORITY_NATIVE_FINALITY_INVALID");
    if (["receiptsRoot", "encodedReceipt", "receiptProofNodes"].some((f) => Object.hasOwn(e, f))) return txErr("J_AUTHORITY_NATIVE_MPT_FIELDS_FORBIDDEN");
    return chain(authInt(e["chainId"], "CHAIN_ID"), (chainId) => map(authBytes32(e["rpcEndpointHash"], "RPC_ENDPOINT_HASH"), (rpcEndpointHash) => ({ receiptKind: e["receiptKind"], finality: e["finality"], chainId, rpcEndpointHash })));
  }
  const nodes = e["receiptProofNodes"];
  if (e["receiptKind"] !== undefined || !Array.isArray(nodes)) return txErr("J_AUTHORITY_RECEIPT_PROOF_SHAPE_INVALID");
  if (nodes.length === 0 || nodes.length > 128) return txErr(`J_AUTHORITY_PROOF_NODE_COUNT_INVALID:${nodes.length}`);
  return chain(traverse(nodes.map((n, i) => [n, i] as const), ([n, i]) => authHex(n, `PROOF_NODE_${i}`, 1_048_576)), (receiptProofNodes) => {
    const total = receiptProofNodes.reduce((sum, n) => sum + (n.length - 2) / 2, 0);
    if (total > 2_097_152) return txErr(`J_AUTHORITY_PROOF_OVERSIZED:${total}:2097152`);
    return chain(authBytes32(e["receiptsRoot"], "RECEIPTS_ROOT"), (receiptsRoot) => map(authHex(e["encodedReceipt"], "ENCODED_RECEIPT", 1_048_576), (encodedReceipt) => ({ receiptsRoot, encodedReceipt, receiptProofNodes })));
  });
};
/** og jReplicaStackKey: the certified board stack of a J replica with a chain id and both contracts, else null. */
const jReplicaStackKey = (r: JReplica): Result<string | null, RuntimeError> => {
  const depository = r.contracts?.depository, entityProvider = r.contracts?.entityProvider;
  if (!r.chainId || !depository || !entityProvider) return ok(null);
  return mapErr(boardStackKey({ chainId: r.chainId, depositoryAddress: depository, entityProviderAddress: entityProvider }), (e): RuntimeError => ({ _tag: "runtime_tx", code: e.code }));
};
const HALF_ORDER_J = secp256k1.CURVE.n >> 1n;
/** og verifyAccountSignature over a canonical compact signature: it must recover to the witness Runtime address. */
const witnessSigned = (digest: string, signature: string, witness: string): boolean => {
  if (!/^0x[0-9a-f]{64}$/i.test(digest) || !/^0x[0-9a-f]{130}$/i.test(signature)) return false;
  const bytes = hexToBytes(signature.toLowerCase()), r = bytes.subarray(0, 32), s = bytes.subarray(32, 64), rec = bytes[64];
  const rn = BigInt(bytesToHex(r)), sn = BigInt(bytesToHex(s));
  if ((rec !== 0 && rec !== 1) || rn === 0n || sn === 0n || rn >= secp256k1.CURVE.n || sn > HALF_ORDER_J) return false;
  const key = recoverPublicKey(hexToBytes(digest.toLowerCase()), r, s, rec);
  return key !== null && addressOf(key).toLowerCase() === witness;
};
/** og assertDecodedRegistrationLog via the EntityProvider ABI: EntityRegistered(bytes32 indexed, uint256 indexed, bytes32) / FoundationBootstrapped(address indexed, bytes32 indexed, uint256, uint256). */
const decodedRegistrationLog = (e: RegistrationEvidence): Result<{ readonly entityId: string; readonly boardHash: string; readonly entityNumber?: bigint }, RuntimeError> => {
  const topics = topicsOfEvidence(e).map((t) => String(t).toLowerCase()), data = hexToBytes(String(e["data"])), topic0 = topics[0];
  const name = topic0 === jEventTopic("EntityRegistered").toLowerCase() ? "EntityRegistered" : topic0 === jEventTopic("FoundationBootstrapped").toLowerCase() ? "FoundationBootstrapped" : "unknown";
  if (name !== e["source"]) return txErr(`J_AUTHORITY_EVENT_TYPE_MISMATCH:${String(e["source"])}:${name}`);
  if (topics.length < 3) return txErr("J_AUTHORITY_EVENT_DECODE_FAILED");
  if (name === "EntityRegistered") return data.length < 32 ? txErr("J_AUTHORITY_EVENT_DECODE_FAILED") : ok({ entityId: topics[1] ?? "", entityNumber: BigInt(topics[2] ?? "0x0"), boardHash: bytesToHex(data.subarray(0, 32)) });
  if (data.length < 64 || BigInt(topics[1] ?? "0x0") >> 160n !== 0n) return txErr("J_AUTHORITY_EVENT_DECODE_FAILED");
  return ok({ entityId: FOUNDATION_ENTITY, boardHash: topics[2] ?? "" });
};
/** og assertRegistrationEvidenceEnvelope: canonical fields, finality under the local J replica's policy, the decoded log, the witness signature. */
const registrationEnvelope = (rt: Runtime, e: RegistrationEvidence): Result<void, RuntimeError> => {
  if (e["version"] !== 1) return txErr(`J_AUTHORITY_VERSION_INVALID:${String(e["version"])}`);
  if (e["source"] !== "EntityRegistered" && e["source"] !== "FoundationBootstrapped") return txErr(`J_AUTHORITY_SOURCE_INVALID:${String(e["source"])}`);
  const rawTopics = e["topics"];
  if (!Array.isArray(rawTopics)) return txErr("J_AUTHORITY_RECEIPT_PROOF_SHAPE_INVALID");
  if (rawTopics.length === 0 || rawTopics.length > 4) return txErr(`J_AUTHORITY_TOPIC_COUNT_INVALID:${rawTopics.length}`);
  type Row = readonly [string, Result<unknown, RuntimeError>];
  return chain(authHex(e["data"], "EVENT_DATA", 65_536), (data) => chain(canonicalRegistrationReceipt(e), (receipt) => chain(authHex(e["witnessSignature"], "WITNESS_SIGNATURE", 65), (witnessSignature) => {
    const rows: readonly (() => Row)[] = [
      () => ["stackKey", authBytes32(e["stackKey"], "STACK_KEY")], () => ["entityId", authBytes32(e["entityId"], "ENTITY_ID")], () => ["boardHash", authBytes32(e["boardHash"], "BOARD_HASH")],
      () => ["blockHash", authBytes32(e["blockHash"], "BLOCK_HASH")], () => ["transactionHash", authBytes32(e["transactionHash"], "TRANSACTION_HASH")], () => ["observedTipBlockHash", authBytes32(e["observedTipBlockHash"], "OBSERVED_TIP_HASH")],
      () => ["rawLogDigest", authBytes32(e["rawLogDigest"], "RAW_LOG_DIGEST")], () => ["emitter", authAddress(e["emitter"], "EMITTER")], () => ["witnessRuntimeId", authAddress(e["witnessRuntimeId"], "WITNESS")],
      () => ["activationHeight", authInt(e["activationHeight"], "ACTIVATION_HEIGHT")], () => ["transactionIndex", authInt(e["transactionIndex"], "TRANSACTION_INDEX")], () => ["logIndex", authInt(e["logIndex"], "LOG_INDEX")],
      () => ["receiptLogIndex", authInt(e["receiptLogIndex"], "RECEIPT_LOG_INDEX")], () => ["observedThroughHeight", authInt(e["observedThroughHeight"], "OBSERVED_THROUGH_HEIGHT")],
      () => ["observedHeadHeight", authInt(e["observedHeadHeight"], "OBSERVED_HEAD_HEIGHT")], () => ["confirmationDepth", authInt(e["confirmationDepth"], "CONFIRMATION_DEPTH")],
      () => ["topics", traverse(rawTopics.map((t, i) => [t, i] as const), ([t, i]) => authBytes32(t, `TOPIC_${i}`))],
    ];
    const canonical: Record<string, unknown> = {};
    for (const row of rows) { const [field, r] = row(); if (!r.ok) return r; canonical[field] = r.value; }
    Object.assign(canonical, { data }, receipt, { witnessSignature });
    for (const [field, value] of Object.entries(canonical)) if (!consensusEqual(e[field], value)) return txErr(`J_AUTHORITY_NON_CANONICAL_FIELD:${field}`);
    const n = (f: string): number => canonical[f] as number;
    if (witnessSignature.length !== 132) return txErr(`J_AUTHORITY_WITNESS_SIGNATURE_LENGTH_INVALID:${witnessSignature.length}`);
    if (n("activationHeight") < 1 || e["receiptsRoot"] === J_AUTH_ZERO) return txErr(`J_AUTHORITY_UNCOMMITTED_RECEIPT:${n("activationHeight")}:${String(e["receiptsRoot"])}`);
    if (n("observedThroughHeight") < n("activationHeight") || n("observedHeadHeight") - n("activationHeight") < n("confirmationDepth") || n("observedThroughHeight") > n("observedHeadHeight") - n("confirmationDepth"))
      return txErr(`J_AUTHORITY_FINALITY_INSUFFICIENT:${n("activationHeight")}:${n("observedThroughHeight")}:${n("observedHeadHeight")}:${n("confirmationDepth")}`);
    const witness = canonical["witnessRuntimeId"] as string;
    return chain(rt.runtimeId ? authAddress(rt.runtimeId, "RUNTIME_ID") : txErr(`J_AUTHORITY_WITNESS_RUNTIME_MISMATCH:${witness}:missing`), (runtimeId) => {
      if (witness !== runtimeId) return txErr(`J_AUTHORITY_WITNESS_RUNTIME_MISMATCH:${witness}:${rt.runtimeId ?? "missing"}`);
      const matches: JReplica[] = [];
      for (const r of rt.jReplicas.values()) { const k = jReplicaStackKey(r); if (!k.ok) return k; if (k.value === e["stackKey"]) matches.push(r); }
      const local = matches[0];
      if (matches.length !== 1 || local === undefined) return txErr(`J_AUTHORITY_STACK_LOCAL_MATCH_INVALID:${String(e["stackKey"])}:${matches.length}`);
      return chain(authAddress(local.contracts?.entityProvider, "LOCAL_ENTITY_PROVIDER"), (provider): Result<void, RuntimeError> => {
        if (provider !== e["emitter"]) return txErr(`J_AUTHORITY_EMITTER_STACK_MISMATCH:${String(e["emitter"])}:${String(local.contracts?.entityProvider)}`);
        // og assertReceiptPolicy: the committed J replica's receipt policy; a witness cannot downgrade an EVM stack to RPC trust.
        if ((local.watcherReceiptCommitment === "tron-rpc-attested") !== isTron(e)) return txErr("J_AUTHORITY_RECEIPT_COMMITMENT_MISMATCH");
        if (isTron(e)) {
          if (e["chainId"] !== local.chainId) return txErr("J_AUTHORITY_NATIVE_CHAIN_MISMATCH");
          if (e["confirmationDepth"] !== 0) return txErr("J_AUTHORITY_NATIVE_FINALITY_DEPTH_INVALID");
          const configured = (local.rpcs ?? []).map((rpc) => { try { return `0x${keccakUtf8(new URL(rpc).toString())}`; } catch { return null; } });
          if (configured.some((h) => h === null)) return txErr("J_AUTHORITY_NATIVE_RPC_URL_INVALID");
          if (!configured.includes(String(e["rpcEndpointHash"]))) return txErr("J_AUTHORITY_NATIVE_RPC_NOT_CONFIGURED");
        }
        return chain(authInt(local.watcherConfirmationDepth, "LOCAL_CONFIRMATION_DEPTH"), (trusted) => {
          if (n("confirmationDepth") !== trusted) return txErr(`J_AUTHORITY_FINALITY_POLICY_MISMATCH:${n("confirmationDepth")}:${trusted}`);
          return chain(rawLogDigest(e), (digest) => {
            if (digest !== e["rawLogDigest"]) return txErr(`J_AUTHORITY_RAW_LOG_DIGEST_MISMATCH:${String(e["entityId"])}`);
            return chain(decodedRegistrationLog(e), (log) => {
              if (log.entityNumber !== undefined && (log.entityNumber <= 0n || log.entityNumber !== BigInt(log.entityId))) return txErr(`J_AUTHORITY_ENTITY_NUMBER_MISMATCH:${log.entityId}:${log.entityNumber}`);
              if (log.entityId !== e["entityId"] || log.boardHash !== e["boardHash"]) return txErr(`J_AUTHORITY_EVENT_BODY_MISMATCH:entity=${log.entityId}:${String(e["entityId"])}:board=${log.boardHash}:${String(e["boardHash"])}`);
              return chain(registrationEvidenceDigest(e), (signed) => (witnessSigned(signed, witnessSignature, witness) ? ok(undefined) : txErr(`J_AUTHORITY_WITNESS_SIGNATURE_INVALID:${witness}`)));
            });
          });
        });
      });
    });
  })));
};
/** og verifyCanonicalReceiptProof + assertReceiptContainsRawLog: the receipt is in the block's receipts trie and carries this exact log. */
const receiptProven = (e: RegistrationEvidence): Result<void, RuntimeError> => {
  const nodes = (e["receiptProofNodes"] as readonly string[]).map(hexToBytes), encoded = hexToBytes(String(e["encodedReceipt"]));
  if (nodes.length === 0) return txErr("J_RECEIPT_PROOF_NODES_MISSING");
  return chain(mptProofGet(hexToBytes(String(e["receiptsRoot"])), receiptTrieKey(Number(e["transactionIndex"])), nodes), (value) => {
    const actual = value === null ? "" : bytesToHex(value), expected = bytesToHex(encoded);
    if (actual !== expected) return txErr(`J_RECEIPT_PROOF_VALUE_MISMATCH:expected=${expected}:actual=${actual || "missing"}`);
    const decoded = rlpDecode(encoded[0] !== undefined && encoded[0] <= 0x7f ? encoded.slice(1) : encoded);
    if (decoded === null) return txErr("invalid RLP");
    const logs = Array.isArray(decoded) ? (decoded as readonly RlpItem[])[3] : undefined;
    if (!Array.isArray(logs)) return txErr("J_AUTHORITY_RECEIPT_LOGS_INVALID");
    const index = Number(e["receiptLogIndex"]), rawLog = (logs as readonly RlpItem[])[index];
    if (!Array.isArray(rawLog) || rawLog.length !== 3 || !Array.isArray(rawLog[1])) return txErr(`J_AUTHORITY_RECEIPT_LOG_MISSING:${index}`);
    const [emitterBytes, topicItems, dataBytes] = rawLog as unknown as readonly [RlpItem, readonly RlpItem[], RlpItem];
    if (!(emitterBytes instanceof Uint8Array) || !(dataBytes instanceof Uint8Array)) return txErr("J_AUTHORITY_RECEIPT_LOG_INVALID");
    return chain(authAddress(bytesToHex(emitterBytes), "RECEIPT_LOG_EMITTER"), (emitter) => chain(traverse(topicItems.map((t, i) => [t, i] as const), ([t, i]) => (t instanceof Uint8Array ? authBytes32(bytesToHex(t), `RECEIPT_TOPIC_${i}`) : txErr(`J_AUTHORITY_RECEIPT_TOPIC_${i}_INVALID`))), (topics) =>
      emitter !== e["emitter"] || !consensusEqual(topics, e["topics"]) || bytesToHex(dataBytes) !== String(e["data"]).toLowerCase() ? txErr(`J_AUTHORITY_RECEIPT_LOG_MISMATCH:${String(e["transactionHash"])}:${String(e["logIndex"])}`) : ok(undefined)));
  });
};
/** og assertCertifiedRegistrationEvidence. */
const certifiedRegistrationEvidence = (rt: Runtime, e: RegistrationEvidence): Result<void, RuntimeError> =>
  chain(registrationEnvelope(rt, e), () => (isTron(e) ? ok(undefined) : receiptProven(e)));
/** og recordAuthenticatedJAuthority: store verified evidence once per (stack, entity); the same claim again is a no-op, a different claim is refused. */
const recordAuthenticatedJAuthority = (rt: Runtime, e: RegistrationEvidence): Result<Runtime, RuntimeError> =>
  chain(certifiedRegistrationEvidence(rt, e), () => chain(registrationEvidenceKey(e["stackKey"], e["entityId"]), (key) => {
    const existing = rt.registrationEvidence.get(key);
    if (existing === undefined) return ok({ ...rt, registrationEvidence: mapSet(rt.registrationEvidence, key, e) });
    return chain(registrationClaimHash(existing), (held) => chain(registrationClaimHash(e), (incoming) => (held !== incoming ? txErr(`J_AUTHORITY_EVIDENCE_CONFLICT:${key}:${held}:${incoming}`) : ok(rt))));
  }));

// ---- og runtime/registration/numbered-registration-{codec,intent}.ts + ethers v6 Transaction.from: durable numbered-registration intents ----
/**
 * An EVM transaction as ethers Transaction.from reads it: `hash` / `from` are null for an unsigned transaction; `from` is ethers' lazy sender
 * recovery, so an unrecoverable signature is an error only when the sender is asked for.
 */
export type EvmTx = { readonly type: 0 | 1 | 2 | 3 | 4; readonly hash: string | null; readonly from: Result<string, string> | null; readonly chainId: bigint; readonly nonce: number; readonly to: string | null; readonly value: bigint; readonly data: string };
type EvmRlp = Uint8Array | readonly EvmRlp[];
/** ethers decodeRlp: length prefixes are taken as given (no canonical-form check), a child may not overrun its list, nothing may trail. */
const evmRlpDecode = (data: Uint8Array): EvmRlp | null => {
  const int = (at: number, n: number): number => { let r = 0; for (let i = 0; i < n; i++) r = r * 256 + (data[at + i] ?? 0); return r; };
  const decode = (offset: number): { readonly item: EvmRlp; readonly consumed: number } | null => {
    const b = data[offset] ?? 0, fits = (end: number): boolean => end <= data.length;
    const children = (start: number, length: number): { readonly item: EvmRlp; readonly consumed: number } | null => {
      const items: EvmRlp[] = [];
      for (let at = start; at < offset + 1 + length;) { const d = decode(at); if (d === null) return null; items.push(d.item); at += d.consumed; if (at > offset + 1 + length) return null; }
      return { item: items, consumed: 1 + length };
    };
    if (b >= 0xf8) { const ll = b - 0xf7; if (!fits(offset + 1 + ll)) return null; const n = int(offset + 1, ll); return fits(offset + 1 + ll + n) ? children(offset + 1 + ll, ll + n) : null; }
    if (b >= 0xc0) { const n = b - 0xc0; return fits(offset + 1 + n) ? children(offset + 1, n) : null; }
    if (b >= 0xb8) { const ll = b - 0xb7; if (!fits(offset + 1 + ll)) return null; const n = int(offset + 1, ll); return fits(offset + 1 + ll + n) ? { item: data.slice(offset + 1 + ll, offset + 1 + ll + n), consumed: 1 + ll + n } : null; }
    if (b >= 0x80) { const n = b - 0x80; return fits(offset + 1 + n) ? { item: data.slice(offset + 1, offset + 1 + n), consumed: 1 + n } : null; }
    return { item: data.slice(offset, offset + 1), consumed: 1 };
  };
  if (data.length === 0) return null;
  const d = decode(0);
  return d === null || d.consumed !== data.length ? null : d.item;
};
const EVM_MAX_UINT = (1n << 256n) - 1n, SECP_N = secp256k1.CURVE.n;
/** ethers toBeArray: the minimal big-endian bytes (0 is empty). */
const evmBytes = (n: bigint): Uint8Array => (n === 0n ? new Uint8Array(0) : magnitude(n));
const bytesItem = (x: EvmRlp | undefined, label: string): Result<Uint8Array, string> => (x instanceof Uint8Array ? ok(x) : err(`invalid ${label}`));
/** ethers handleUint: any byte string up to 2^256 - 1 (leading zeros are accepted). */
const evmUint = (x: EvmRlp | undefined, label: string): Result<bigint, string> => chain(bytesItem(x, label), (b) => {
  const n = b.length === 0 ? 0n : BigInt(bytesToHex(b));
  return n > EVM_MAX_UINT ? err(`value exceeds uint size: ${label}`) : ok(n);
});
/** ethers handleNumber: a safe integer. */
const evmNumber = (x: EvmRlp | undefined, label: string): Result<number, string> => chain(evmUint(x, label), (n) => (n > BigInt(Number.MAX_SAFE_INTEGER) ? err(`overflow: ${label}`) : ok(Number(n))));
/** ethers handleAddress: empty is contract creation, anything else must be 20 bytes. */
const evmTo = (x: EvmRlp | undefined): Result<Uint8Array | null, string> => chain(bytesItem(x, "to"), (b) => (b.length === 0 ? ok(null) : b.length === 20 ? ok(b) : err("invalid address")));
/** ethers accessListify on decoded fields: [address(20), [slot(32)...]] rows, re-encoded as read. */
const evmAccessList = (x: EvmRlp | undefined): Result<EvmRlp, string> => {
  if (!Array.isArray(x)) return err("invalid access list");
  for (const row of x as readonly EvmRlp[]) {
    if (!Array.isArray(row) || row.length !== 2) return err("invalid slot set");
    const [addr, keys] = row as readonly EvmRlp[];
    if (!(addr instanceof Uint8Array) || addr.length !== 20 || !Array.isArray(keys)) return err("invalid address-slot set");
    if ((keys as readonly EvmRlp[]).some((k) => !(k instanceof Uint8Array) || k.length !== 32)) return err("invalid slot");
  }
  return ok(x);
};
/** ethers zeroPadValue(_, 32) then Signature.from: r / s are at most 32 bytes. */
const evmSigWord = (x: EvmRlp | undefined, label: string): Result<bigint, string> => chain(bytesItem(x, label), (b) => (b.length > 32 ? err(`invalid ${label}`) : ok(b.length === 0 ? 0n : BigInt(bytesToHex(b)))));
type EvmSig = { readonly r: bigint; readonly s: bigint; readonly yParity: number };
/** ethers _parseEip4844 sidecar: the network format [tx, blobs, commitments, proofs] or EIP-7594 [tx, 1, blobs, commitments, cellProofs]; each blob's versioned hash is recomputed from its commitment (getVersionedHash). */
const EVM_CELL_COUNT = 128;
const evmBlobSidecar = (fields: EvmRlp | null): Result<{ readonly fields: EvmRlp | null; readonly hashes: readonly Uint8Array[] | null }, string> => {
  if (!Array.isArray(fields) || !Array.isArray(fields[0]) || (fields.length !== 4 && fields.length !== 5)) return ok({ fields, hashes: null });
  const f = fields as readonly EvmRlp[], seven = f.length === 5;
  if (seven) { const version = f[1]; if (!(version instanceof Uint8Array) || version.length === 0 || BigInt(bytesToHex(version)) !== 1n) return err("unsupported EIP-7594 network format version"); }
  const [blobs, commits, proofs] = seven ? [f[2], f[3], f[4]] : [f[1], f[2], f[3]];
  if (!Array.isArray(blobs) || !Array.isArray(commits) || !Array.isArray(proofs)) return err("invalid network format");
  if (blobs.length !== commits.length || blobs.length * (seven ? EVM_CELL_COUNT : 1) !== proofs.length) return err("invalid network format: length mismatch");
  // ethers hexlify(blob.data / commitment / proof) and concat(cell proofs): every part is a byte string
  if (![...blobs, ...commits, ...proofs].every((x) => x instanceof Uint8Array)) return err("invalid BytesLike value");
  return ok({ fields: f[0] ?? null, hashes: (commits as readonly Uint8Array[]).map((c) => { const h = sha256(c); h[0] = 1; return h; }) });
};
/** ethers handleAuthorizationList then authorizationify: [chainId, address(20), nonce, yParity(0|1), r, s (unchecked: ethers encodes ._s)] rows, re-encoded minimal (formatAuthorizationList). */
const evmAuthorizationList = (x: EvmRlp | undefined): Result<EvmRlp, string> => {
  if (!Array.isArray(x)) return err("authorizationList: invalid array");
  return traverse(x as readonly EvmRlp[], (auth): Result<EvmRlp, string> => {
    if (!Array.isArray(auth) || auth.length !== 6) return err("invalid authorization");
    const a = auth as readonly EvmRlp[];
    return chain(bytesItem(a[1], "address"), (address) => (address.length !== 20 ? err("invalid address") : chain(evmUint(a[2], "nonce"), (nonce) => chain(evmUint(a[0], "chainId"), (chainId) =>
      chain(evmNumber(a[3], "yParity"), (yParity) => (yParity !== 0 && yParity !== 1 ? err("invalid yParity") : chain(evmSigWord(a[4], "r"), (r) => chain(evmSigWord(a[5], "s"), (sw) =>
        ok([evmBytes(chainId), address, evmBytes(nonce), evmBytes(BigInt(yParity)), evmBytes(r), evmBytes(sw)] as EvmRlp)))))))));
  });
};
/** ethers Transaction.hash (keccak of the re-serialized signed form) and .from (recovery over the unsigned hash; a high s recovers the same key). */
type EvmSigned = { readonly hash: string | null; readonly from: Result<string, string> | null };
const evmSigned = (sig: EvmSig, unsigned: Uint8Array, signed: (sig: EvmSig) => Result<Uint8Array, string>): Result<EvmSigned, string> =>
  map(signed(sig), (serialized) => {
    const key = sig.r === 0n || sig.r >= SECP_N || sig.s === 0n || sig.s >= SECP_N ? null : recoverPublicKey(keccak_256(unsigned), wordOf(sig.r), wordOf(sig.s), sig.yParity);
    return { hash: bytesToHex(keccak_256(serialized)), from: key === null ? err("invalid signature") : ok(addressOf(key).toLowerCase()) };
  });
/**
 * ethers v6 Transaction.from(raw) for a legacy (pre-EIP-155 or EIP-155), EIP-2930 (type 1) or EIP-1559 (type 2) transaction: RLP-decode the fields,
 * derive the chain id from a legacy v, hash the re-serialized signed form, recover the sender. Blob (3, with or without its network sidecar) and set-code (4) transactions too.
 */
export const parseEvmTx = (raw: string): Result<EvmTx, string> => {
  if (!/^0x([0-9a-fA-F]{2})+$/.test(raw)) return err("invalid BytesLike value");
  const payload = hexToBytes(raw), first = payload[0] ?? 0;
  const rlpOf = (items: readonly EvmRlp[]): Uint8Array => rlp(items as Rlp);
  if (first >= 0x7f) {
    const fields = evmRlpDecode(payload);
    if (!Array.isArray(fields) || (fields.length !== 9 && fields.length !== 6)) return err("invalid field count for legacy transaction");
    const f = fields as readonly EvmRlp[];
    return chain(evmNumber(f[0], "nonce"), (nonce) => chain(evmUint(f[1], "gasPrice"), (gasPrice) => chain(evmUint(f[2], "gasLimit"), (gasLimit) => chain(evmTo(f[3]), (to) =>
      chain(evmUint(f[4], "value"), (value) => chain(bytesItem(f[5], "data"), (data) => {
        const body = [evmBytes(BigInt(nonce)), evmBytes(gasPrice), evmBytes(gasLimit), to ?? new Uint8Array(0), evmBytes(value), data];
        const tx = (chainId: bigint, signed: EvmSigned): EvmTx => ({ type: 0, ...signed, chainId, nonce, to: to === null ? null : bytesToHex(to), value, data: bytesToHex(data) });
        if (f.length === 6) return ok(tx(0n, { hash: null, from: null }));
        return chain(evmUint(f[6], "v"), (v) => chain(evmUint(f[7], "r"), (r) => chain(evmUint(f[8], "s"), (s) => {
          if (r === 0n && s === 0n) return ok(tx(v, { hash: null, from: null }));
          const derived = (v - 35n) / 2n, chainId = derived < 0n ? 0n : derived;
          if (chainId === 0n && v !== 27n && v !== 28n) return err("non-canonical legacy v");
          return chain(evmSigWord(f[7], "r"), (rw) => chain(evmSigWord(f[8], "s"), (sw) => {
            const yParity = v === 27n ? 0 : v === 28n ? 1 : v % 2n === 1n ? 0 : 1;
            const unsigned = rlpOf(chainId === 0n ? body : [...body, evmBytes(chainId), new Uint8Array(0), new Uint8Array(0)]);
            const vOut = chainId === 0n ? 27n + BigInt(yParity) : chainId * 2n + 35n + BigInt(yParity);
            return map(evmSigned({ r: rw, s: sw, yParity }, unsigned, (sig) => ok(rlpOf([...body, evmBytes(vOut), evmBytes(sig.r), evmBytes(sig.s)]))), (signed) => tx(chainId, signed));
          }));
        })));
      }))))));
  }
  if (first < 1 || first > 4) return err("unsupported transaction type");
  const typed = first as 1 | 2 | 3 | 4, plain = typed === 1 ? 8 : typed === 2 ? 9 : typed === 3 ? 11 : 10, unwrapped = typed === 3 ? evmBlobSidecar(evmRlpDecode(payload.subarray(1))) : ok({ fields: evmRlpDecode(payload.subarray(1)), hashes: null });
  if (!unwrapped.ok) return unwrapped;
  const { fields, hashes: sidecarHashes } = unwrapped.value;
  if (!Array.isArray(fields) || (fields.length !== plain && fields.length !== plain + 3)) return err(`invalid field count for transaction type: ${typed}`);
  const f = fields as readonly EvmRlp[], o = typed >= 2 ? 1 : 0;
  // ethers _parseEip4844: maxFeePerBlobGas, a non-null to, and 32-byte versioned hashes; _parseEip7702: the authorization list
  const blob = typed !== 3 ? ok([] as EvmRlp[]) : chain(evmUint(f[9], "maxFeePerBlobGas"), (maxFeePerBlobGas): Result<EvmRlp[], string> => {
    const hashes = f[10];
    if (!Array.isArray(hashes) || (hashes as readonly EvmRlp[]).some((h) => !(h instanceof Uint8Array) || h.length !== 32)) return err("invalid blobVersionedHashes");
    const t = f[5];
    return t instanceof Uint8Array && t.length === 0 ? err("invalid address for transaction type: 3") : ok([evmBytes(maxFeePerBlobGas), sidecarHashes ?? hashes]);
  });
  const extra = typed === 4 ? map(evmAuthorizationList(f[9]), (auths): EvmRlp[] => [auths]) : blob;
  if (!extra.ok) return extra;
  return chain(evmUint(f[0], "chainId"), (chainId) => chain(evmNumber(f[1], "nonce"), (nonce) => chain(evmUint(f[2], typed >= 2 ? "maxPriorityFeePerGas" : "gasPrice"), (fee0) =>
    chain(typed >= 2 ? evmUint(f[3], "maxFeePerGas") : ok(0n), (fee1) => chain(evmUint(f[3 + o], "gasLimit"), (gasLimit) => chain(evmTo(f[4 + o]), (to) => chain(evmUint(f[5 + o], "value"), (value) =>
      chain(bytesItem(f[6 + o], "data"), (data) => chain(evmAccessList(f[7 + o]), (accessList) => {
        const body: EvmRlp[] = [evmBytes(chainId), evmBytes(BigInt(nonce)), evmBytes(fee0), ...(typed >= 2 ? [evmBytes(fee1)] : []), evmBytes(gasLimit), to ?? new Uint8Array(0), evmBytes(value), data, accessList, ...extra.value];
        const tx = (signed: EvmSigned): EvmTx => ({ type: typed, ...signed, chainId, nonce, to: to === null ? null : bytesToHex(to), value, data: bytesToHex(data) });
        const envelope = (items: readonly EvmRlp[]): Uint8Array => concat([Uint8Array.of(typed), rlpOf(items)]);
        if (f.length === plain) return ok(tx({ hash: null, from: null }));
        return chain(evmNumber(f[plain], "yParity"), (yParity) => {
          if (yParity !== 0 && yParity !== 1) return err("invalid yParity");
          return chain(evmSigWord(f[plain + 1], "r"), (r) => chain(evmSigWord(f[plain + 2], "s"), (s) =>
            // ethers inferTypes refuses an EIP-1559 fee cap below its priority fee, and Signature.s a word with its top bit set, when the signed form is serialized.
            map(evmSigned({ r, s, yParity }, envelope(body), (sig) => (typed >= 2 && fee1 < fee0 ? err("priorityFee cannot be more than maxFee") : sig.s >> 255n !== 0n ? err("non-canonical s; use ._s")
              : ok(envelope([...body, evmBytes(BigInt(sig.yParity)), evmBytes(sig.r), evmBytes(sig.s)])))), tx)));
        });
      })))))))));
};
const REGISTER_NUMBERED_ENTITIES_BATCH_SELECTOR = nobleHex(keccak_256(utf8("registerNumberedEntitiesBatch(bytes[])")).slice(0, 4));
/** og encodeNumberedRegistrationCalldata: EntityProvider.registerNumberedEntitiesBatch(bytes[] encodedBoards), lowercased. */
export const numberedRegistrationCalldata = (encodedBoards: readonly string[]): string =>
  `0x${REGISTER_NUMBERED_ENTITIES_BATCH_SELECTOR}${hexBody(abiEncodeHex([A.array(encodedBoards.map((b) => A.bytes(b)))]))}`.toLowerCase();
const MAX_NUMBERED_REGISTRATION_ENTITIES = 128;
type Loose = { readonly [k: string]: unknown };
const looseRecord = (v: unknown): Loose => (v !== null && typeof v === "object" && !Array.isArray(v) ? (v as Loose) : {});
/** og numberedRegistrationBytes32. */
const numberedBytes32 = (v: unknown, label: string): Result<string, RuntimeError> => { const s = String(v || "").toLowerCase(); return /^0x[0-9a-f]{64}$/.test(s) ? ok(s) : txErr(`NUMBERED_REGISTRATION_${label}_INVALID`); };
/** og codec `address`: ethers.getAddress, lowercased. */
const numberedAddress = (v: unknown, label: string): Result<string, RuntimeError> => { const a = ethAddress(v); return a === null ? txErr(`NUMBERED_REGISTRATION_${label}_INVALID:${String(v)}`) : ok(a); };
const boardStack = (j: unknown): Result<string, RuntimeError> => { const x = looseRecord(j); return mapErr(boardStackKey({ chainId: x["chainId"] as number, depositoryAddress: x["depositoryAddress"] as string, entityProviderAddress: x["entityProviderAddress"] as string }), (e): RuntimeError => ({ _tag: "runtime_tx", code: e.code })); };
/** og assertNumberedRegistrationRequest. */
const numberedRegistrationRequestValid = (rt: Runtime, request: Loose): Result<void, RuntimeError> => {
  if (request["version"] !== 1) return txErr("NUMBERED_REGISTRATION_INTENT_VERSION_INVALID");
  return chain(numberedBytes32(request["intentId"], "INTENT_ID"), (intentId) => {
    if (request["intentId"] !== intentId) return txErr("NUMBERED_REGISTRATION_INTENT_ID_NON_CANONICAL");
    return chain(numberedBytes32(request["stackKey"], "STACK_KEY"), (stackKey) => {
      if (request["stackKey"] !== stackKey) return txErr("NUMBERED_REGISTRATION_STACK_KEY_NON_CANONICAL");
      return chain(numberedAddress(request["payerSignerId"], "PAYER"), () => chain(numberedAddress(request["entityProviderAddress"], "ENTITY_PROVIDER"), (): Result<void, RuntimeError> => {
        let committed = false;
        for (const r of rt.jReplicas.values()) { const k = jReplicaStackKey(r); if (!k.ok) return k; if (k.value === stackKey) { committed = true; break; } }
        if (!committed) return txErr("NUMBERED_REGISTRATION_COMMITTED_STACK_MISSING");
        const entities = Array.isArray(request["entities"]) ? (request["entities"] as readonly unknown[]) : [];
        if (entities.length === 0) return txErr("NUMBERED_REGISTRATION_INTENT_EMPTY");
        if (entities.length > MAX_NUMBERED_REGISTRATION_ENTITIES) return txErr(`NUMBERED_REGISTRATION_ENTITY_LIMIT_EXCEEDED:${entities.length}`);
        for (const [index, raw] of entities.entries()) { const r = numberedEntityValid(request, looseRecord(raw), index); if (!r.ok) return r; }
        return ok(undefined);
      }));
    });
  });
};
const numberedEntityValid = (request: Loose, entity: Loose, index: number): Result<void, RuntimeError> => {
  const name = entity["name"], config = looseRecord(entity["config"]), jurisdiction = config["jurisdiction"];
  if (!name || (typeof name === "string" && name.length > 256)) return txErr(`NUMBERED_REGISTRATION_NAME_INVALID:${index}`);
  if (!jurisdiction) return txErr(`NUMBERED_REGISTRATION_STACK_MISSING:${index}`);
  return chain(boardStack(jurisdiction), (stack) => {
    if (stack !== request["stackKey"]) return txErr(`NUMBERED_REGISTRATION_STACK_MISMATCH:${index}`);
    return chain(numberedAddress(looseRecord(jurisdiction)["entityProviderAddress"], "CONFIG_ENTITY_PROVIDER"), (provider) => {
      if (provider !== request["entityProviderAddress"]) return txErr(`NUMBERED_REGISTRATION_ENTITY_PROVIDER_MISMATCH:${index}`);
      return chain(numberedBytes32(entity["boardHash"], "BOARD_HASH"), (expectedBoard) => {
        const encoded = entity["encodedBoard"];
        if (typeof encoded !== "string" || !/^0x(?:[0-9a-f]{2})+$/.test(encoded)) return txErr(`NUMBERED_REGISTRATION_ENCODED_BOARD_INVALID:${index}`);
        const validators = Array.isArray(config["validators"]) ? (config["validators"] as readonly unknown[]).map(String) : [];
        return chain(lazyBoardEncoding({ mode: "proposer-based", validators, shares: looseRecord(config["shares"]) as EntityRootConfig["shares"], threshold: config["threshold"] as bigint }), (board): Result<void, RuntimeError> => {
          if (board.toLowerCase() !== encoded) return txErr(`NUMBERED_REGISTRATION_ENCODED_BOARD_MISMATCH:${index}`);
          if (keccak256Hex(hexToBytes(encoded)) !== expectedBoard) return txErr(`NUMBERED_REGISTRATION_BOARD_HASH_MISMATCH:${index}`);
          const seed = entity["entitySeed"], position = entity["position"];
          if (entity["localSignerId"] !== null) {
            const local = numberedAddress(entity["localSignerId"], "LOCAL_SIGNER");
            if (!local.ok) return local;
            if (!validators.some((v) => v.toLowerCase() === local.value)) return txErr(`NUMBERED_REGISTRATION_LOCAL_SIGNER_NOT_ON_BOARD:${index}`);
            // og canonicalEntitySeed(seed) !== seed: only a lowercase 0x-prefixed 64-byte hex seed is its own canonical form.
            if (typeof seed !== "string" || !/^0x[0-9a-f]{128}$/.test(seed)) return txErr(`NUMBERED_REGISTRATION_ENTITY_SEED_NON_CANONICAL:${index}`);
          } else if (seed !== null) return txErr(`NUMBERED_REGISTRATION_PAYER_ONLY_SEED_FORBIDDEN:${index}`);
          if (position) { const p = looseRecord(position); if (![p["x"], p["y"], p["z"]].every(Number.isFinite)) return txErr(`NUMBERED_REGISTRATION_POSITION_INVALID:${index}`); }
          return ok(undefined);
        });
      });
    });
  });
};
/** og computeNumberedRegistrationRequestHash. */
export const numberedRegistrationRequestHash = (request: unknown): Result<string, RuntimeError> => authHash({ domain: "xln.numbered-registration.intent.v1", request });
/** og parseNumberedRegistrationIntentTransaction: the durable raw transaction is exactly the payer's registerNumberedEntitiesBatch call. */
const numberedRegistrationTx = (pending: Loose): Result<EvmTx, RuntimeError> => {
  const raw = pending["rawTransaction"], request = looseRecord(pending["request"]);
  if (typeof raw !== "string" || !/^0x[0-9a-f]+$/i.test(raw) || raw.length > 524_290) return txErr("NUMBERED_REGISTRATION_RAW_TX_INVALID");
  return chain(mapErr(parseEvmTx(raw), (reason): RuntimeError => ({ _tag: "runtime_tx", code: `NUMBERED_REGISTRATION_RAW_TX_INVALID:${reason}` })), (tx) => {
    if (!tx.hash) return txErr("NUMBERED_REGISTRATION_TX_HASH_MISMATCH");
    return chain(numberedBytes32(pending["transactionHash"], "TX_HASH"), (txHash) => {
      if (tx.hash !== txHash) return txErr("NUMBERED_REGISTRATION_TX_HASH_MISMATCH");
      if (tx.from !== null && !tx.from.ok) return txErr(`NUMBERED_REGISTRATION_RAW_TX_INVALID:${tx.from.error}`);
      if (tx.from === null || tx.from.value !== request["payerSignerId"]) return txErr("NUMBERED_REGISTRATION_TX_SIGNER_MISMATCH");
      // The request check proved this chain id a positive safe integer (its certified stack key).
      const entities = request["entities"] as readonly unknown[], chainId = Number(looseRecord(looseRecord(looseRecord(entities[0])["config"])["jurisdiction"])["chainId"]);
      if (tx.chainId !== BigInt(chainId) || tx.to !== request["entityProviderAddress"]) return txErr("NUMBERED_REGISTRATION_TX_DOMAIN_MISMATCH");
      if (tx.value !== 0n || tx.data !== numberedRegistrationCalldata(entities.map((e) => String(looseRecord(e)["encodedBoard"])))) return txErr("NUMBERED_REGISTRATION_TX_CALLDATA_MISMATCH");
      return tx.nonce !== pending["transactionNonce"] ? txErr("NUMBERED_REGISTRATION_TX_NONCE_MISMATCH") : ok(tx);
    });
  });
};
/** og applyNumberedRegistrationIntent: a validated pending intent is stored once; the same payload again is a no-op, a different one is refused. */
const recordNumberedRegistrationIntent = (rt: Runtime, pending: RuntimeData): Result<Runtime, RuntimeError> => {
  const request = looseRecord(pending["request"]);
  return chain(numberedRegistrationRequestValid(rt, request), () => chain(numberedRegistrationRequestHash(request), (hash) => {
    if (hash !== pending["requestHash"]) return txErr("NUMBERED_REGISTRATION_REQUEST_HASH_MISMATCH");
    return chain(numberedRegistrationTx(pending), (): Result<Runtime, RuntimeError> => {
      const intentId = String(request["intentId"]), existing = rt.numberedRegistrationIntents.get(intentId);
      if (existing === undefined) return ok({ ...rt, numberedRegistrationIntents: mapSet(rt.numberedRegistrationIntents, intentId, pending) });
      if (existing["requestHash"] !== pending["requestHash"]) return txErr("NUMBERED_REGISTRATION_INTENT_PAYLOAD_CONFLICT");
      return existing["status"] === "pending" && existing["transactionHash"] !== pending["transactionHash"] ? txErr("NUMBERED_REGISTRATION_INTENT_TX_CONFLICT") : ok(rt);
    });
  }));
};
/** og applyNumberedRegistrationResolution: quarantine, or complete once every result has certified evidence and every local replica exists on the planned board. */
const resolveNumberedRegistrationIntent = (rt: Runtime, resolution: RuntimeData): Result<Runtime, RuntimeError> =>
  chain(numberedBytes32(resolution["intentId"], "INTENT_ID"), (intentId): Result<Runtime, RuntimeError> => {
    const pending = rt.numberedRegistrationIntents.get(intentId);
    if (pending === undefined || pending["status"] !== "pending") {
      if (pending?.["status"] === "completed" && resolution["kind"] === "completed" && pending["requestHash"] === resolution["requestHash"]) return ok(rt);
      return txErr("NUMBERED_REGISTRATION_PENDING_INTENT_MISSING");
    }
    if (pending["requestHash"] !== resolution["requestHash"] || pending["transactionHash"] !== resolution["transactionHash"]) return txErr("NUMBERED_REGISTRATION_RESOLUTION_IDENTITY_MISMATCH");
    const request = looseRecord(pending["request"]), key = String(request["intentId"]);
    if (resolution["kind"] === "quarantined") return ok({ ...rt, numberedRegistrationIntents: mapSet(rt.numberedRegistrationIntents, key, { ...pending, status: "quarantined", reason: resolution["reason"] as Binary }) });
    const results = Array.isArray(resolution["results"]) ? (resolution["results"] as readonly unknown[]) : [], planned = request["entities"] as readonly unknown[];
    if (results.length !== planned.length) return txErr("NUMBERED_REGISTRATION_RESULT_COUNT_MISMATCH");
    for (const [index, raw] of results.entries()) {
      const result = looseRecord(raw), plan = looseRecord(planned[index]), local = plan["localSignerId"], entityId = result["entityId"];
      const evidenceKey = registrationEvidenceKey(request["stackKey"], entityId);
      if (!evidenceKey.ok) return evidenceKey;
      const evidence = rt.registrationEvidence.get(evidenceKey.value);
      const replica = local !== null ? [...rt.entities.values()].find((r) => r.state.id.toLowerCase() === entityId && r.signerId.toLowerCase() === local) : undefined;
      if (evidence === undefined) return txErr(`NUMBERED_REGISTRATION_COMPLETION_INCOMPLETE:${String(entityId)}`);
      const evidenceHash = registrationEvidenceHash(evidence);
      if (!evidenceHash.ok) return evidenceHash;
      if (evidenceHash.value !== result["evidenceHash"] || (local !== null && replica === undefined)) return txErr(`NUMBERED_REGISTRATION_COMPLETION_INCOMPLETE:${String(entityId)}`);
      if (replica !== undefined && configBoardHash(replica.state.quorum) !== plan["boardHash"]) return txErr(`NUMBERED_REGISTRATION_COMPLETION_BOARD_MISMATCH:${String(entityId)}`);
    }
    const { kind: _kind, ...completed } = resolution;
    return ok({ ...rt, numberedRegistrationIntents: mapSet(rt.numberedRegistrationIntents, key, { status: "completed", ...completed }) });
  });

// ---- og runtime/mempool/wake.ts generateHookPings: the Runtime tick's due Entity wakes, leader timeout votes and J submit / EntityProvider action retries ----
/** og scheduled-wake.ts nextReplicaDeadline: the leader's earliest wake, or a validator's leader-timeout deadline from its last consensus progress. */
const replicaDeadline = (r: EntityReplica, local: ReplicaLocal | undefined): number | undefined => {
  if (isActiveLeader(r)) return nextWakeAt(r);
  const due = leaderTimeoutDue(r, BigInt(local?.lastConsensusProgressAt ?? Number(r.state.timestamp)), local?.jHistory);
  return due === undefined ? undefined : Number(due);
};
/** og isBatchEmpty over the committed sent batch. */
const sentBatchEmpty = (sent: SentJBatch): boolean => batchOpCount(sent.batch) === 0;
/**
 * og generateHookPingsWithDeps (createDueScheduledWakeInputs, collectDueJSubmitRuntimeTxs, collectDueEntityProviderActionRuntimeTxs) at Runtime time `now`:
 * `queued` is the Runtime mempool not yet applied; `canSubmit` is og canSubmitLocally (this Runtime holds the signer's key; default: the Runtime id).
 * The returned `local` set is og's process-local markers: pass it as RuntimeCtx.local so the frame admits exactly these wakes and retries.
 */
export const runtimeWake = (rt: Runtime, now: number, queued: RuntimeInput = { runtimeTxs: [], entityInputs: [] }, canSubmit: (signer: string) => boolean = (signer) => submitId(signer) === submitId(rt.runtimeId)):
  { readonly input: RuntimeInput; readonly local: ReadonlySet<RuntimeTx | EntityTx> } => {
  const replicas = [...rt.entities];
  // og createDueScheduledWakeInputs: the deadline heap in (dueAt, entityId, signerId) order; one wake or vote per replica, none while one is queued
  const busy = new Set([
    ...queued.entityInputs.filter((i) => i.input.kind === "leaderTimeoutVote" || (i.input.kind === "txs" && i.input.txs.some((tx) => tx.type === "scheduledWake"))).map((i) => replicaKey(i.entityId, i.signerId)),
    ...replicas.filter(([, r]) => r.mempool.some((tx) => tx.type === "scheduledWake")).map(([key]) => key),
  ]);
  const due = replicas.flatMap(([key, r]): { readonly key: string; readonly r: EntityReplica; readonly dueAt: number }[] => {
    const dueAt = replicaDeadline(r, rt.replicaLocal.get(key));
    return dueAt === undefined || dueAt > now ? [] : [{ key, r, dueAt }];
  }).sort((a, b) => a.dueAt - b.dueAt || compareText(lower(a.r.state.id), lower(b.r.state.id)) || compareText(signerId(a.r.signerId), signerId(b.r.signerId)));
  const at = BigInt(now);
  const entityInputs = due.flatMap(({ key, r }): RoutedEntityInput[] => {
    if (busy.has(key)) return [];
    const input = isActiveLeader(r) ? localScheduledWake(r, at) : localTimeoutVote(r, at, rt.replicaLocal.get(key)?.jHistory);
    return input === undefined ? [] : [{ entityId: r.state.id, signerId: r.signerId, input }];
  });
  // og collectDueJSubmitRuntimeTxs: the active leader's sealed batch, once per retry window, unless an abort, a retry or a committed attempt is pending
  const queuedRetry = (d: Extract<RuntimeTx, { type: "retryJSubmit" }>["data"]): boolean => queued.runtimeTxs.some((tx) => tx.type === "retryJSubmit" && submitId(tx.data.jurisdictionName) === submitId(d.jurisdictionName)
    && submitId(tx.data.entityId) === submitId(d.entityId) && submitId(tx.data.signerId) === submitId(d.signerId) && submitId(tx.data.batchHash) === submitId(d.batchHash) && Number(tx.data.entityNonce) === Number(d.entityNonce) && tx.data.batchGeneration === d.batchGeneration);
  const aborting = (entity: EntityId): boolean => queued.entityInputs.some((i) => submitId(i.entityId) === submitId(entity) && i.input.kind === "txs" && i.input.txs.some((tx) => (tx.type as string) === "j_abort_sent_batch"));
  const jRetries = replicas.flatMap(([key, r]): RuntimeTx[] => {
    const jb = jBatchOf(r.state), sent = jb?.sentBatch;
    if (!isActiveLeader(r) || !canSubmit(r.signerId) || sent === undefined || sent.terminalFailure || sentBatchEmpty(sent) || aborting(r.state.id)) return [];
    const data = { entityId: r.state.id, signerId: r.signerId, jurisdictionName: String(r.state.jurisdictionConfig?.name || ""), batchHash: sent.batchHash, entityNonce: sent.entityNonce, batchGeneration: jb?.broadcastCount ?? 0, ...(sent.feeOverrides ? { feeOverrides: { ...sent.feeOverrides } as unknown as Binary } : {}) };
    const local = matchingJSubmitState(r.state, rt.replicaLocal.get(key)?.jSubmitState);
    if (local?.terminalFailure || local?.lastResultOutcome === "reconciled" || queuedRetry(data)) return [];
    const pending = rt.pendingCommittedJOutbox.some((input) => input.jTxs.some((raw) => {
      const t = rowOf(raw);
      return t.type === "batch" && submitId(input.jurisdictionName) === submitId(data.jurisdictionName) && submitId(t.entityId) === submitId(data.entityId) && submitId(t.data["signerId"]) === submitId(data.signerId)
        && submitId(t.data["batchHash"]) === submitId(data.batchHash) && Number(t.data["entityNonce"]) === Number(data.entityNonce) && Number(t.data["batchGeneration"]) === Number(data.batchGeneration);
    }));
    const dueAt = local === undefined || local.submitAttempts <= 0 ? 0 : local.lastResultOutcome === "eventBarrier" ? local.lastResultAt ?? local.lastSubmittedAt : local.lastSubmittedAt + ENTITY_J_SUBMIT_RETRY_MS;
    return pending || dueAt > now ? [] : [{ type: "retryJSubmit", data }];
  });
  // og collectDueEntityProviderActionRuntimeTxs: the committed pending action, once per retry window
  const epRetries = replicas.flatMap(([key, r]): RuntimeTx[] => {
    const pending = epActionState(r.state).pending, name = jurisdictionNameOf(r.state);
    if (!isActiveLeader(r) || !canSubmit(r.signerId) || pending === undefined || !name) return [];
    const data = { entityId: r.state.id, signerId: r.signerId, jurisdictionName: name, actionHash: pending.actionHash, actionNonce: pending.actionNonce, generation: pending.generation };
    const local = matchingEpSubmitState(r.state, rt.replicaLocal.get(key)?.entityProviderActionSubmitState);
    if (local?.terminalFailure) return [];
    const held = rt.pendingCommittedJOutbox.some((input) => input.jTxs.some((raw) => {
      const t = rowOf(raw), intent = t.data?.["intent"] as EntityProviderActionIntent | undefined;
      return isEpActionJTx(t.type) && intent !== undefined && submitId(input.jurisdictionName) === submitId(data.jurisdictionName) && submitId(t.entityId) === submitId(data.entityId)
        && submitId(t.data["signerId"]) === submitId(data.signerId) && submitId(intent.actionHash) === submitId(data.actionHash) && intent.actionNonce === data.actionNonce && intent.generation === data.generation;
    }));
    const queuedEp = queued.runtimeTxs.some((tx) => tx.type === "retryEntityProviderAction" && submitId(tx.data.jurisdictionName) === submitId(data.jurisdictionName) && submitId(tx.data.entityId) === submitId(data.entityId)
      && submitId(tx.data.signerId) === submitId(data.signerId) && submitId(tx.data.actionHash) === submitId(data.actionHash) && tx.data.actionNonce === data.actionNonce && tx.data.generation === data.generation);
    const dueAt = local === undefined || local.submitAttempts <= 0 ? 0 : local.lastSubmittedAt + ENTITY_J_SUBMIT_RETRY_MS;
    return held || queuedEp || dueAt > now ? [] : [{ type: "retryEntityProviderAction", data }];
  });
  const runtimeTxs = [...jRetries, ...epRetries];
  const local = new Set<RuntimeTx | EntityTx>([...runtimeTxs, ...entityInputs.flatMap((i) => (i.input.kind === "txs" ? i.input.txs : []))]);
  return { input: { runtimeTxs, entityInputs, timestamp: at }, local };
};
/** og assertScheduledWakeTxAuthorized, then assertProposeAccountsNowTxAuthorized, per Entity tx in ingress order: outside replay only this Runtime's own marked txs enter. */
const forgedIngress = (input: RuntimeInput, ctx: RuntimeCtx): string | undefined => {
  if (ctx.replay === true) return undefined;
  for (const i of input.entityInputs) {
    if (i.input.kind !== "txs") continue;
    for (const tx of i.input.txs) {
      if (tx.type === "scheduledWake" && ctx.local?.has(tx) !== true) return "SCHEDULED_WAKE_EXTERNAL_INGRESS_REJECTED";
      if (tx.type === "proposeAccountsNow" && ctx.local?.has(tx) !== true) return "PROPOSE_ACCOUNTS_NOW_EXTERNAL_INGRESS_REJECTED";
    }
  }
  return undefined;
};
/** og lastConsensusProgressAt writes: commit (finalization.ts), an accepted proposal's precommit (proposal/input.ts), a signed local timeout vote or a new leader certificate (timeout-input.ts), and a non-proposer's first admitted txs (input/admission.ts). */
const consensusProgressed = (before: EntityReplica, after: EntityReplica, input: EntityInput, local: ReplicaLocal | undefined): boolean =>
  after.head.height > before.head.height
  || (input.kind === "proposal" && after._tag === "locked" && (before._tag !== "locked" || before.frame !== after.frame))
  || (input.kind === "leaderTimeoutVote" && (input.local === true || (after.pendingLeaderCertificate !== undefined && after.pendingLeaderCertificate !== before.pendingLeaderCertificate)))
  || (input.kind === "txs" && local?.lastConsensusProgressAt === undefined && !isProposalLeader(before) && after.mempool.length > before.mempool.length)
  // og ensureLocalJPrefixAttestation / handleJPrefixAttestations: a new or merged head in this validator's J-prefix round
  || (after.jPrefixRound !== undefined && after.jPrefixRound !== before.jPrefixRound && !consensusEqual(after.jPrefixRound.attestations, before.jPrefixRound?.attestations ?? new Map()));
/** og applyRuntimeTx. */
export const applyRuntimeTxStep = (rt: Runtime, tx: RuntimeTx, ctx: Pick<RuntimeCtx, "replay" | "local">): Result<TxStep, RuntimeError> => chain(runtimeTxAuthorized(tx, ctx), (): Result<TxStep, RuntimeError> => {
  const state = (r: Result<Runtime, RuntimeError>): Result<TxStep, RuntimeError> => map(r, noJ);
  switch (tx.type) {
    case "checkpointBarrier": return ok(noJ(rt));
    case "recordRuntimeAdapterCommand": return state(adapterCommand(rt, tx.data));
    case "importReplica": return state(importReplica(rt, tx));
    case "importJ": return state(importJ(rt, tx.data));
    case "completeImportJ": return state(completeImportJ(rt, tx.data));
    case "advanceJWatcherCursor": return state(advanceJWatcherCursor(rt, tx.data));
    case "observeJRange": return state(observeJRange(rt, tx.data));
    case "rewindJHistory": return state(rewindJHistory(rt, tx.data));
    case "recordAuthenticatedJAuthority": return state(recordAuthenticatedJAuthority(rt, tx.data));
    case "recordNumberedRegistrationIntent": return state(recordNumberedRegistrationIntent(rt, tx.data));
    case "resolveNumberedRegistrationIntent": return state(resolveNumberedRegistrationIntent(rt, tx.data));
    case "retryJSubmit": return retryJSubmit(rt, tx.data);
    case "recordJSubmitResult": return state(recordJSubmitResult(rt, tx.data));
    case "retryEntityProviderAction": return retryEntityProviderAction(rt, tx.data);
    case "recordEntityProviderActionSubmitResult": return state(recordEpActionResult(rt, tx.data));
    case "recordGovernanceJSubmitResult": return state(recordGovernanceResult(rt, tx.data));
    default: { const exhaustive: never = tx; return txErr(`RUNTIME_TX_UNKNOWN: ${(exhaustive as { readonly type?: string }).type ?? "unknown"}`); }
  }
});
/** og applyRuntimeTx, the Runtime state half (the J outputs of a retry are in applyRuntimeTxStep). */
export const applyRuntimeTx = (rt: Runtime, tx: RuntimeTx, ctx: Pick<RuntimeCtx, "replay" | "local">): Result<Runtime, RuntimeError> => map(applyRuntimeTxStep(rt, tx, ctx), (s) => s.runtime);

/**
 * One applied Runtime frame: `outbox` is positional (merged input order, then each input's outputs); `jOutbox` is og's frame jOutbox (every pending
 * committed J submit attempt, then the maintenance J txs); `queuedRetries` are og queuedJSubmitRetries, the local retry RuntimeTxs for the next frame.
 */
/** `events`: og env.emit(eventName, data) of every committed frame's runtimeEvent candidate effects, on every committing replica, in commit order (publishEntityCandidateEffects). */
export type RuntimeStep = { readonly runtime: Runtime; readonly applied: RuntimeInput; readonly outbox: readonly EntityOutput[]; readonly jOutbox: readonly JInput[]; readonly queuedRetries: readonly RuntimeTx[]; readonly rejected: readonly RuntimeError[]; readonly advanced: boolean; readonly events: readonly EntityRuntimeEvent[] };
/**
 * og createRuntimeInputReducer: validate shape/limits, apply every RuntimeTx in order (any failure refuses the whole frame), merge the entity inputs,
 * discard inputs whose replica is unknown (og drop policy for unroutable ingress), apply the rest, and advance the Runtime height only when the frame
 * did work (og advanceAppliedRuntimeFrame). The frame timestamp is max(previous, ingress seed) and stamps every txs input (og env.state.timestamp).
 */
/** og requireEntityEncryptionPrivateKey: the Entity key comes from the Runtime's encryption seed unless the host supplies it. */
const runtimeHtlcInfra = (ctx: RuntimeCtx, rt: Runtime, entityId: EntityId): HtlcProposerInfra | undefined => {
  const given = ctx.htlcInfra?.(entityId), seed = rt.encryptionSeeds.get(entityId);
  return given?.encryptionPrivateKey !== undefined || seed === undefined ? given : { profiles: [], ...given, encryptionPrivateKey: entityEncryptionPrivateKey(seed, entityId) };
};
/** og replica.jHistory as the Entity reads it: pruned to the committed finality (og prunes at every commit; a failed prune fails that commit). */
const replicaJHistory = (rt: Runtime, key: string, r: EntityReplica): ValidatorJHistory | undefined => {
  const h = rt.replicaLocal.get(key)?.jHistory;
  return unwrapOr(pruneFinalizedJHistory(h, Number(r.state.committed["lastFinalizedJHeight"] || 0)), () => h);
};
// ---- og runtime/frame/cross-j/atomic-admission.ts: the two legs of a cross-j swap commit together ----
//
// A cross-j swap moves value on two jurisdictions at once, so its two Account frames must commit in
// the same Runtime frame or not at all. Each such Account frame in a Runtime frame is a *leg*. Legs
// agreeing on phase, route set, origin and source frame form a *cohort*. A cohort of exactly two
// mirrored legs on sibling Entities is a *pair*. Pairs are admitted as two adjacent marked inputs;
// every other leg is stripped from its input with the reason it failed.

/** A (checksum-valid) address as lowercase 0x text, else ''. */
export const normalizeRuntimeId = (value: unknown): string => {
  const text = typeof value === "string" ? value.trim() : "";
  if (!text || !isAddressText(text)) return "";
  return `0x${text.replace(/^0x/, "").toLowerCase()}`;
};

/** The state admission reads: the Runtime's replicas and clock. */
export type CrossAdmissionView = {
  readonly entities: ReadonlyMap<string, EntityReplica>;
  readonly timestamp: bigint;
};

type CrossPhase = AtomicCrossPair["phase"];
type Side = "source" | "target";
type BySide<T> = { readonly source: readonly T[]; readonly target: readonly T[] };
type FrameRef = { readonly height: bigint; readonly stateHash: string };
type FrameTxs = FrameRef & { readonly txs: readonly AccountTx[] };

// og compares entity ids and hashes lowercased but not trimmed; route bindings trim the order id.
const idOf = (value: unknown): string => String(value || "").toLowerCase();
const sole = <X>(xs: readonly X[]): X | undefined => (xs.length === 1 ? xs[0] : undefined);
const ascending = (a: number, b: number): number => a - b;
const groupedBy = <K, V>(entries: readonly (readonly [K, V])[]): ReadonlyMap<K, readonly V[]> =>
  entries.reduce<ReadonlyMap<K, readonly V[]>>(
    (groups, [key, value]) => mapSet(groups, key, [...(groups.get(key) ?? []), value]),
    new Map(),
  );

// ---- reading an input ----

/** The Account inputs an Entity input carries, runtimeOutput-nested ones included. */
const accountInputsOf = (routed: RoutedEntityInput): readonly AccountPeerInput[] => {
  if (routed.input.kind !== "txs") return [];
  return effectiveTxs(routed.input.txs).flatMap((tx) => (tx.type === "accountInput" ? [tx.data] : []));
};
const proposedFrameOf = (input: AccountPeerInput): AccountFrame | undefined =>
  input.kind === "ack_frame" ? input.frame : undefined;
const ackIn = (input: AccountPeerInput): { readonly height: bigint; readonly frameHash: string } | undefined => {
  switch (input.kind) {
    case "ack": return input;
    case "ack_frame": return input.ack ?? undefined;
    default: return undefined;
  }
};
/** `local`, or the Runtime that transported the input. */
const transportOrigin = (routed: RoutedEntityInput): string =>
  routed.from ? `remote:${normalizeRuntimeId(routed.from) || "missing"}` : "local";
/** Two inputs share a source frame unless both name one and they differ. */
const sameSourceFrame = (a: RoutedEntityInput, b: RoutedEntityInput): boolean => {
  const [x, y] = [a.sourceRuntimeFrame, b.sourceRuntimeFrame];
  return x === undefined || y === undefined || (x.height === y.height && x.timestamp === y.timestamp);
};
const onSiblings = (a: RoutedEntityInput | undefined, b: RoutedEntityInput | undefined): boolean =>
  idOf(a?.entityId) !== idOf(b?.entityId);

// ---- a leg: one cross-j Account frame ----

const bindingKey = (orderId: unknown, routeHash: unknown): string =>
  `${String(orderId || "").trim()}\u0000${lower(routeHash)}`;
const pullKey = (pull: PullLockTx): string =>
  bindingKey(pull.crossJurisdiction.orderId, pull.crossJurisdiction.routeHash);
const closeKey = ({ proof, binary }: PullCloseTx): string => stableJson({
  operation: "close",
  orderId: proof.orderId,
  routeHash: idOf(proof.routeHash),
  sourcePullId: proof.sourcePullId,
  targetPullId: proof.targetPullId,
  fillRatio: proof.fillRatio,
  cumulativeSourceAmount: proof.cumulativeSourceAmount,
  cumulativeTargetAmount: proof.cumulativeTargetAmount,
  binaryHash: idOf(proof.binaryHash),
  closeMode: proof.closeMode,
  binary,
});
const pullsOn = (txs: readonly AccountTx[], side: Side): readonly PullLockTx[] =>
  txs.filter((tx): tx is PullLockTx =>
    tx.type === "cross_pull_lock" && tx.crossJurisdiction?.leg === side);
const closesOn = (txs: readonly AccountTx[], side: Side): readonly PullCloseTx[] =>
  txs.filter((tx): tx is PullCloseTx => tx.type === "cross_pull_close"
    && tx.pullId === (side === "source" ? tx.proof.sourcePullId : tx.proof.targetPullId));

/** A proposal's pulls on one side have unique routes, each source pull with its swap offer. */
const sideResolved = (txs: readonly AccountTx[], side: Side): boolean => {
  const bindings = pullsOn(txs, side).map((pull) => pull.crossJurisdiction);
  const hasOffer = (b: CrossPullBinding): boolean => txs.some((tx) => tx.type === "swap_offer"
    && tx.crossJurisdiction?.orderId === b.orderId
    && idOf(tx.crossJurisdiction?.routeHash) === idOf(b.routeHash));
  return bindings.length > 0
    && unique(bindings.map((b) => bindingKey(b.orderId, b.routeHash)))
    && (side === "target" || bindings.every(hasOffer));
};

type Leg = {
  readonly at: number;
  readonly account: AccountPeerInput;
  readonly phase: CrossPhase;
  readonly routeSet: string;
  readonly origin: string;
  readonly frame: FrameRef;
  readonly pulls: BySide<PullLockTx>;
  readonly closes: BySide<PullCloseTx>;
  readonly committed: boolean;
  readonly defects: readonly string[];
};

/** The frame as a leg, when it moves any pull. */
const legOf = (
  routed: RoutedEntityInput,
  at: number,
  account: AccountPeerInput,
  phase: CrossPhase,
  frame: FrameTxs,
  committed: boolean,
): readonly Leg[] => {
  const pulls = { source: pullsOn(frame.txs, "source"), target: pullsOn(frame.txs, "target") };
  const closes = { source: closesOn(frame.txs, "source"), target: closesOn(frame.txs, "target") };
  const routeKeys = [
    ...[...pulls.source, ...pulls.target].map((pull) => `open\u0000${pullKey(pull)}`),
    ...[...closes.source, ...closes.target].map(closeKey),
  ];
  if (routeKeys.length === 0) return [];
  const unresolved = (["source", "target"] as const).filter((side) =>
    phase === "proposal" && pulls[side].length > 0 && !sideResolved(frame.txs, side));
  const defects = [
    ...(unique(routeKeys) ? [] : ["duplicate-route-key"]),
    ...unresolved.map((side) => `${side}-sibling-unresolved`),
  ];
  const routeSet = `${phase}\u0000${sortedBy(routeKeys, (key) => key).join("\u0001")}`;
  const ref = { height: frame.height, stateHash: frame.stateHash };
  return [{ at, account, phase, routeSet, origin: transportOrigin(routed), frame: ref, pulls, closes, committed, defects }];
};
const proposalLeg = (routed: RoutedEntityInput, at: number, account: AccountPeerInput): readonly Leg[] => {
  const frame = proposedFrameOf(account);
  return frame === undefined ? [] : legOf(routed, at, account, "proposal", frame, false);
};

// ---- do two legs mirror each other? ----

/** Each source pull meets exactly one target pull on the same route, ladder and pull ids. */
const pullsMirror = (sources: readonly PullLockTx[], targets: readonly PullLockTx[]): boolean => {
  const mirrors = (source: PullLockTx): boolean => {
    const target = sole(targets.filter((t) => pullKey(t) === pullKey(source)));
    const route = source.crossJurisdictionRoute;
    if (target === undefined || route === undefined || target.crossJurisdictionRoute === undefined) return false;
    const sameRoute = stableJson(route) === stableJson(target.crossJurisdictionRoute);
    const onTheirSides = source.crossJurisdiction.leg === "source" && target.crossJurisdiction.leg === "target";
    const routeNamesBoth = source.pullId === route.sourcePull?.pullId && target.pullId === route.targetPull?.pullId;
    const sameLadder = idOf(source.fullHash) === idOf(target.fullHash)
      && idOf(source.partialRoot) === idOf(target.partialRoot);
    return sameRoute && onTheirSides && routeNamesBoth && sameLadder;
  };
  return sources.length === targets.length && sources.every(mirrors);
};
/** Each source close meets exactly one target close of the same proof. */
const closesMirror = (sources: readonly PullCloseTx[], targets: readonly PullCloseTx[]): boolean => {
  const mirrors = (source: PullCloseTx): boolean => {
    const target = sole(targets.filter((t) => closeKey(t) === closeKey(source)));
    return target !== undefined
      && source.pullId === source.proof.sourcePullId
      && target.pullId === target.proof.targetPullId;
  };
  return sources.length === targets.length && sources.every(mirrors);
};
const legsMirror = (a: Leg, b: Leg): boolean =>
  a.defects.length === 0 && b.defects.length === 0
  && a.phase === b.phase && a.routeSet === b.routeSet && a.origin === b.origin
  && pullsMirror(a.pulls.source, b.pulls.target) && pullsMirror(b.pulls.source, a.pulls.target)
  && closesMirror(a.closes.source, b.closes.target) && closesMirror(b.closes.source, a.closes.target);

// ---- potential pairs: what transport can see before any replica is read ----

export type PotentialCrossPair = {
  readonly pairKey: string;
  readonly sourceInputIndex: number;
  readonly targetInputIndex: number;
};
const pairStart = (pair: PotentialCrossPair): number =>
  Math.min(pair.sourceInputIndex, pair.targetInputIndex);

/** Two marked ACK inputs of one cohort and source frame pair up by their marker. */
const markedAckPair = (inputs: readonly RoutedEntityInput[]): readonly PotentialCrossPair[] => {
  const [first] = inputs;
  const marker = first?.atomicCrossJurisdictionPair;
  if (first === undefined || inputs.length !== 2 || marker?.phase !== "ack") return [];
  const inCohort = (routed: RoutedEntityInput): boolean =>
    routed.atomicCrossJurisdictionPair?.phase === "ack"
    && routed.atomicCrossJurisdictionPair.pairKey === marker.pairKey
    && sameSourceFrame(first, routed);
  if (!inputs.every(inCohort)) return [];
  const carriesAck = (routed: RoutedEntityInput): boolean =>
    accountInputsOf(routed).some((account) => ackIn(account) !== undefined);
  const ackIndexes = inputs.flatMap((routed, index) => (carriesAck(routed) ? [index] : []));
  const [source, target] = ackIndexes;
  if (ackIndexes.length !== 2 || source === undefined || target === undefined) return [];
  if (!onSiblings(inputs[source], inputs[target])) return [];
  return [{ pairKey: marker.pairKey, sourceInputIndex: source, targetInputIndex: target }];
};

/**
 * Each proposal leg pairs with the one later leg that mirrors it from the same Runtime and origin,
 * first within one source frame, then (when allowed) across source frames.
 */
export const potentialCrossPairs = (
  inputs: readonly RoutedEntityInput[],
  options: { readonly allowDifferentSourceRuntimeFrames?: boolean } = {},
): readonly PotentialCrossPair[] => {
  const legs = inputs.flatMap((routed, at) =>
    accountInputsOf(routed).flatMap((account) => proposalLeg(routed, at, account)));
  const inputOf = (leg: Leg): RoutedEntityInput => inputs[leg.at] as RoutedEntityInput;
  type Found = { readonly pairs: readonly PotentialCrossPair[]; readonly taken: ReadonlySet<number> };

  const partnerOf = (found: Found, left: Leg, leftIndex: number, sameFrameOnly: boolean): Leg | undefined => {
    const l = inputOf(left);
    const fits = (right: Leg, rightIndex: number): boolean => {
      const r = inputOf(right);
      const frameOk = sameSourceFrame(r, l) || (!sameFrameOnly && options.allowDifferentSourceRuntimeFrames === true);
      return rightIndex > leftIndex
        && right.at !== left.at
        && !found.taken.has(right.at)
        && onSiblings(l, r)
        && normalizeRuntimeId(r.runtimeId) === normalizeRuntimeId(l.runtimeId)
        && frameOk
        && transportOrigin(r) === transportOrigin(l)
        && legsMirror(left, right);
    };
    return found.taken.has(left.at) ? undefined : sole(legs.filter(fits));
  };
  const sweep = (found: Found, sameFrameOnly: boolean): Found =>
    legs.reduce<Found>((acc, left, leftIndex) => {
      const right = partnerOf(acc, left, leftIndex, sameFrameOnly);
      if (right === undefined) return acc;
      const pair = { pairKey: left.routeSet, sourceInputIndex: left.at, targetInputIndex: right.at };
      return { pairs: [...acc.pairs, pair], taken: new Set([...acc.taken, left.at, right.at]) };
    }, found);

  const { pairs } = sweep(sweep({ pairs: [], taken: new Set() }, true), false);
  return pairs.length > 0 ? pairs : markedAckPair(inputs);
};

type MarkedPair = PotentialCrossPair & { readonly phase: CrossPhase };
const withMarkers = (inputs: readonly RoutedEntityInput[], pairs: readonly MarkedPair[]): readonly RoutedEntityInput[] => {
  const markerAt = new Map(pairs.flatMap((pair) => {
    const marker = { phase: pair.phase, pairKey: pair.pairKey };
    return [[pair.sourceInputIndex, marker], [pair.targetInputIndex, marker]] as const;
  }));
  return inputs.map((routed, index) => {
    const marker = markerAt.get(index);
    return marker === undefined ? routed : { ...routed, atomicCrossJurisdictionPair: marker };
  });
};
/** An unmarked transported pair is isolated as one proposal cohort before the Entity merge. */
export const markPotentialCrossPairs = (inputs: readonly RoutedEntityInput[]): readonly RoutedEntityInput[] => {
  const transportedUnmarked = (index: number): boolean => {
    const routed = inputs[index];
    return routed !== undefined && !routed.atomicCrossJurisdictionPair && !!routed.sourceRuntimeFrame;
  };
  const fresh = potentialCrossPairs(inputs).filter((pair) =>
    transportedUnmarked(pair.sourceInputIndex) && transportedUnmarked(pair.targetInputIndex));
  return withMarkers(inputs, fresh.map((pair) => ({ ...pair, phase: "proposal" as const })));
};

// ---- reading legs against the replicas ----

type ReplicaIndex = { readonly find: (entity: unknown, signer: unknown) => EntityReplica | undefined };
/** The first replica per lowercase `entity:signer`. */
const replicasOf = (view: CrossAdmissionView): ReplicaIndex => {
  const keyOf = (entity: unknown, signer: unknown): string => `${idOf(entity)}:${idOf(signer)}`;
  const first = firstBy(view.entities.values(), (r) => keyOf(r.state.id, r.signerId));
  const byKey = new Map(first.map((r) => [keyOf(r.state.id, r.signerId), r] as const));
  return { find: (entity, signer) => byKey.get(keyOf(entity, signer)) };
};
const accountWith = (replica: EntityReplica | undefined, peer: string): AccountReplica | undefined =>
  [...(replica?.accountReplicas ?? [])].find(([id]) => idOf(id) === idOf(peer))?.[1];
const headIs = (account: AccountReplica, height: bigint, hash: string): boolean =>
  account.head.height === height && idOf(account.head.prevFrameHash) === idOf(hash);
const isHub = (replica: EntityReplica): boolean => {
  const profile = (replica.state.committed["profile"] ?? {}) as { readonly [k: string]: unknown };
  return profile["isHub"] === true;
};

/** A hub's ACK names its pending proposal, or replays the committed head (kept with its cross-j txs). */
const ackLeg = (replica: EntityReplica, routed: RoutedEntityInput, at: number, account: AccountPeerInput): readonly Leg[] => {
  const ack = ackIn(account);
  const peer = accountWith(replica, account.fromEntityId);
  if (ack === undefined || peer === undefined) return [];
  const pending = peer._tag === "proposed" ? peer.candidate.frame : undefined;
  if (pending !== undefined && pending.height === ack.height && idOf(pending.stateHash) === idOf(ack.frameHash)) {
    return legOf(routed, at, account, "ack", pending, false);
  }
  const head = peer.head;
  if (head._tag === "installed" && headIs(peer, ack.height, ack.frameHash)) {
    const replayed = { height: head.height, stateHash: head.prevFrameHash, txs: head.crossTxs ?? [] };
    return legOf(routed, at, account, "ack", replayed, true);
  }
  return [];
};
/** A proposal leg, marked committed when the replica's head already is its frame. */
const receivedProposalLeg = (replica: EntityReplica, routed: RoutedEntityInput, at: number, account: AccountPeerInput): readonly Leg[] =>
  proposalLeg(routed, at, account).map((leg) => {
    const peer = accountWith(replica, account.fromEntityId);
    return { ...leg, committed: peer !== undefined && headIs(peer, leg.frame.height, leg.frame.stateHash) };
  });
/** A hub reads ACK legs against its pending frame; any other Entity reads proposal legs. */
const legsOf = (replicas: ReplicaIndex, inputs: readonly RoutedEntityInput[]): readonly Leg[] =>
  inputs.flatMap((routed, at) => {
    const replica = replicas.find(routed.entityId, routed.signerId);
    if (replica === undefined) return [];
    const read = isHub(replica) ? ackLeg : receivedProposalLeg;
    return accountInputsOf(routed).flatMap((account) => read(replica, routed, at, account));
  });

// ---- opening a route: the ladder and authorization checks ----

type Ladder = { readonly byFull: ReadonlyMap<string, string>; readonly byPartial: ReadonlyMap<string, string> };
type LadderClaim = readonly [routeHash: string, full: string, partial: string];
const emptyLadder: Ladder = { byFull: new Map(), byPartial: new Map() };

/** A route claims its hash roots; a root another route already owns refuses the claim. */
const claimLadder = (ladder: Ladder, [routeHash, full, partial]: LadderClaim): Result<Ladder, string> => {
  const free = (owners: ReadonlyMap<string, string>, root: string, what: string): Result<void, string> => {
    const owner = owners.get(root);
    return guard(!owner || owner === routeHash, `opening-shared-hash-material:${owner}/${routeHash}:${what}`);
  };
  const claimed = { byFull: mapSet(ladder.byFull, full, routeHash), byPartial: mapSet(ladder.byPartial, partial, routeHash) };
  return map(checks(free(ladder.byFull, full, "fullHash"), free(ladder.byPartial, partial, "partialRoot")), () => claimed);
};
/** The roots already claimed by every live Account pull and every retained route. */
const liveLadder = (view: CrossAdmissionView): Ladder => {
  const pullClaims = (replica: EntityReplica): readonly LadderClaim[] =>
    [...replica.accountReplicas.values()].flatMap((account) =>
      [...(account.state.pulls?.values() ?? [])].flatMap((pull): readonly LadderClaim[] => {
        const routeHash = lower(pull.crossJurisdiction?.routeHash);
        return routeHash ? [[routeHash, lower(pull.fullHash), lower(pull.partialRoot)]] : [];
      }));
  const routeClaims = (replica: EntityReplica): readonly LadderClaim[] => {
    const routes = [
      ...(replica.state.crossJurisdictionSwaps?.values() ?? []),
      ...(replica.state.crossJurisdictionAuthorizations?.values() ?? []),
    ];
    return routes.flatMap((route) => [route.sourcePull, route.targetPull].flatMap((pull): readonly LadderClaim[] => {
      const full = lower(pull?.fullHash);
      const partial = lower(pull?.partialRoot);
      return pull !== undefined && full && partial ? [[lower(route.routeHash), full, partial]] : [];
    }));
  };
  const claims = [...view.entities.values()].flatMap((r) => [...pullClaims(r), ...routeClaims(r)]);
  return claims.reduce((ladder, claim) => unwrapOr(claimLadder(ladder, claim), () => ladder), emptyLadder);
};
/** Every opening pull names its order and route; one route, one ladder; no ladder shared with a live route. */
const laddersUnique = (view: CrossAdmissionView, cohort: readonly Leg[]): Result<unknown, string> => {
  type Seen = { readonly ladder: Ladder; readonly byRoute: ReadonlyMap<string, LadderClaim> };
  const pulls = cohort.flatMap((leg) => [...leg.pulls.source, ...leg.pulls.target]);
  return foldResult(pulls, { ladder: liveLadder(view), byRoute: new Map() } as Seen, (seen, pull) => {
    const orderId = String(pull.crossJurisdiction?.orderId || "").trim();
    const claim = [lower(pull.crossJurisdiction?.routeHash), lower(pull.fullHash), lower(pull.partialRoot)] as const;
    const [routeHash, full, partial] = claim;
    if (!orderId) return err("opening-order-id-missing");
    if (!routeHash) return err(`opening-route-hash-missing:${orderId}`);
    const earlier = seen.byRoute.get(routeHash);
    if (earlier !== undefined) {
      const sameLadder = earlier[1] === full && earlier[2] === partial;
      return sameLadder ? ok(seen) : err(`opening-intra-route-hash-divergent:${routeHash}`);
    }
    return map(claimLadder(seen.ladder, claim), (ladder) => ({ ladder, byRoute: mapSet(seen.byRoute, routeHash, claim) }));
  });
};

type Placed = { readonly leg: Leg; readonly routed: RoutedEntityInput };
/** A source pull opens only with one target pull, identical `intent` authorizations on both siblings, exact participants and a valid prepared route. */
const sourcePullAuthorized = (view: CrossAdmissionView, replicas: ReplicaIndex, source: Placed, target: Placed) =>
  (pull: PullLockTx): Result<unknown, string> => {
    const binding = pull.crossJurisdiction;
    const route = pull.crossJurisdictionRoute;
    if (!binding || !route) return err("opening-route-missing");
    const matchingTargets = target.leg.pulls.target.filter((t) => pullKey(t) === pullKey(pull));
    if (matchingTargets.length !== 1) return err(`opening-target-pull-mismatch:${binding.orderId}`);
    const sourceReplica = replicas.find(source.routed.entityId, source.routed.signerId);
    const targetReplica = replicas.find(target.routed.entityId, target.routed.signerId);
    if (sourceReplica === undefined || targetReplica === undefined) return err("opening-replica-unresolved");
    const routeHash = idOf(route.routeHash);
    if (!routeHash) return err("opening-route-hash-missing");
    const sourceAuth = sourceReplica.state.crossJurisdictionAuthorizations?.get(route.orderId);
    const targetAuth = targetReplica.state.crossJurisdictionAuthorizations?.get(route.orderId);
    if (sourceAuth === undefined || targetAuth === undefined) return err("opening-authorization-absent");
    const participants = [
      [source.routed.entityId, route.source.entityId],
      [source.routed.signerId, route.sourceSignerId],
      [source.leg.account.fromEntityId, route.source.counterpartyEntityId],
      [target.routed.entityId, route.target.counterpartyEntityId],
      [target.routed.signerId, route.targetSignerId],
      [target.leg.account.fromEntityId, route.target.entityId],
    ] as const;
    const notPulled = (auth: typeof sourceAuth): boolean => !auth.sourcePull && !auth.targetPull;
    const authorizations = checks(
      guard(sourceAuth.status === "intent" && targetAuth.status === "intent",
        `opening-authorization-status:${sourceAuth.status}/${targetAuth.status}`),
      guard(notPulled(sourceAuth) && notPulled(targetAuth), "opening-authorization-already-pulled"),
      guard(idOf(sourceAuth.routeHash) === routeHash && idOf(targetAuth.routeHash) === routeHash,
        "opening-authorization-route-hash-mismatch"),
      guard(stableJson(sourceAuth) === stableJson(targetAuth), "opening-authorization-divergent"),
      guard(participants.every(([a, b]) => idOf(a) === idOf(b)), "opening-route-participant-mismatch"),
    );
    const now = sourceReplica.state.timestamp > view.timestamp ? sourceReplica.state.timestamp : view.timestamp;
    const preparedRoute = (): Result<unknown, string> => mapErr(
      validatePreparedCrossRoute(crossView(sourceReplica.state, sourceReplica.accountReplicas, now), { ...route, status: "target_prepared" }),
      (e) => `opening-prepared-route-rejected:${runtimeErrorText(e)}`,
    );
    return chain(authorizations, preparedRoute);
  };
/** A proposal cohort that opens pulls is two legs with unique ladders, every source pull authorized both ways. */
const openingAuthorized = (view: CrossAdmissionView, replicas: ReplicaIndex, cohort: readonly Placed[]): Result<unknown, string> => {
  const opensPulls = cohort.some(({ leg }) => leg.pulls.source.length > 0 || leg.pulls.target.length > 0);
  if (cohort[0]?.leg.phase !== "proposal" || !opensPulls) return ok(undefined);
  if (cohort.length !== 2) return err(`opening-cohort-size:${cohort.length}`);
  const [left, right] = cohort as readonly [Placed, Placed];
  const bothWays = (): Result<unknown, string> => traverse([[left, right], [right, left]] as const, ([source, target]) =>
    traverse(source.leg.pulls.source, sourcePullAuthorized(view, replicas, source, target)));
  return chain(laddersUnique(view, cohort.map(({ leg }) => leg)), bothWays);
};

// ---- judging cohorts ----

export type CrossFrameExpectation = {
  readonly entityId: string;
  readonly signerId: string;
  readonly counterpartyEntityId: string;
  readonly height: bigint;
  readonly stateHash: string;
};
export type CrossPair = PotentialCrossPair & {
  readonly phase: CrossPhase;
  readonly sourceAccountFrame: CrossFrameExpectation;
  readonly targetAccountFrame: CrossFrameExpectation;
};
export type CrossRejectedLeg = {
  readonly inputIndex: number;
  readonly accountInput: AccountPeerInput;
  readonly reason: "unpaired" | "multiple-candidates-per-input" | "candidate-invalid" | "pair-match-failed" | "atomic-group-invalid";
  readonly detail: readonly string[];
};
export type CrossPairSelection = {
  readonly inputs: readonly RoutedEntityInput[];
  readonly pairs: readonly CrossPair[];
  readonly rejectedLegs: readonly CrossRejectedLeg[];
};

const cohortKey = (routed: RoutedEntityInput, phase: CrossPhase, routeSet: string): string =>
  JSON.stringify([phase, routeSet, transportOrigin(routed), routed.sourceRuntimeFrame?.height ?? null, routed.sourceRuntimeFrame?.timestamp ?? null]);

/** Two legs of one cohort on sibling Entities, one source frame, mirrored and authorized: a pair. */
const pairOf = (view: CrossAdmissionView, replicas: ReplicaIndex, inputs: readonly RoutedEntityInput[], cohort: readonly Leg[]): Result<CrossPair, string> => {
  const place = (leg: Leg): Placed => ({ leg, routed: inputs[leg.at] as RoutedEntityInput });
  const [source, target] = sortWith(cohort, (a, b) => a.at - b.at) as [Leg, Leg];
  const [s, t] = [place(source), place(target)];
  const expectation = ({ leg, routed }: Placed): CrossFrameExpectation => ({
    entityId: routed.entityId,
    signerId: routed.signerId,
    counterpartyEntityId: leg.account.fromEntityId,
    height: leg.frame.height,
    stateHash: leg.frame.stateHash,
  });
  const shape = checks(
    guard(onSiblings(s.routed, t.routed), "legs-target-same-entity"),
    guard(sameSourceFrame(s.routed, t.routed), "legs-source-frame-differs"),
    guard(legsMirror(source, target), "legs-frames-do-not-pair"),
  );
  const pair: CrossPair = {
    pairKey: source.routeSet,
    phase: source.phase,
    sourceInputIndex: source.at,
    targetInputIndex: target.at,
    sourceAccountFrame: expectation(s),
    targetAccountFrame: expectation(t),
  };
  // og checks the opening in the cohort's own order, which decides which error is reported first.
  const opening = (): Result<unknown, string> => openingAuthorized(view, replicas, cohort.map(place));
  return map(chain(shape, opening), () => pair);
};

/** What admission concluded about the input a leg rides on. */
type Standing = {
  readonly multi: ReadonlySet<number>;
  readonly defects: ReadonlyMap<number, readonly string[]>;
  readonly failures: ReadonlyMap<number, readonly string[]>;
  readonly pairs: readonly CrossPair[];
};
/** Each cohort in order; a failed cohort taints its inputs for every later cohort. */
const judgeCohorts = (view: CrossAdmissionView, replicas: ReplicaIndex, inputs: readonly RoutedEntityInput[], live: readonly Leg[]): Standing => {
  const legInputs = live.map((leg) => leg.at);
  const multi = new Set(legInputs.filter((at) => legInputs.indexOf(at) !== legInputs.lastIndexOf(at)));
  const defects = groupedBy(live.flatMap((leg) => leg.defects.map((defect) => [leg.at, defect] as const)));
  const cohorts = groupedBy(live.map((leg) => [cohortKey(inputs[leg.at] as RoutedEntityInput, leg.phase, leg.routeSet), leg] as const));
  const start: Standing = { multi, defects, failures: new Map(), pairs: [] };

  return [...cohorts.values()].reduce<Standing>((standing, cohort) => {
    const ats = [...new Set(cohort.map((leg) => leg.at))];
    const tainted = ats.some((at) => multi.has(at) || defects.has(at) || standing.failures.has(at));
    const verdict: Result<CrossPair, string> =
      cohort.length !== 2 ? err(`cohort-size:${cohort.length}`)
      : ats.length !== 2 ? err("cohort-legs-share-one-input")
      : tainted ? err("cohort-leg-already-invalid")
      : pairOf(view, replicas, inputs, cohort);
    if (verdict.ok) return { ...standing, pairs: [...standing.pairs, verdict.value] };
    const failures = ats.reduce((f, at) => mapSet(f, at, [...(f.get(at) ?? []), verdict.error]), standing.failures);
    return { ...standing, failures };
  }, start);
};

// ---- stripping rejected legs ----

/** Drop the rejected Account legs (nested ones included) and the marker; an emptied input is dropped. */
const stripLegs = (routed: RoutedEntityInput, rejected: ReadonlySet<AccountPeerInput>): readonly RoutedEntityInput[] => {
  const { atomicCrossJurisdictionPair: _marker, ...unmarked } = routed;
  if (routed.input.kind !== "txs") return [unmarked];
  const kept = (txs: readonly EntityTx[]): readonly EntityTx[] =>
    txs.filter((tx) => tx.type !== "accountInput" || !rejected.has(tx.data));
  const txs = routed.input.txs.flatMap((tx): readonly EntityTx[] => {
    if (tx.type !== "runtimeOutput") return kept([tx]);
    const nested = kept(tx.data.entityTxs);
    if (nested.length === tx.data.entityTxs.length) return [tx];
    return nested.length > 0 ? [{ ...tx, data: { ...tx.data, entityTxs: nested } }] : [];
  });
  return txs.length === 0 ? [] : [{ ...unmarked, input: { ...routed.input, txs } }];
};
/** The Account inputs an input loses: its legs, or every Account input when it has none. */
const lostAccountInputs = (legs: readonly Leg[], inputs: readonly RoutedEntityInput[], at: number): readonly AccountPeerInput[] => {
  const own = legs.filter((leg) => leg.at === at);
  if (own.length > 0) return own.map((leg) => leg.account);
  const routed = inputs[at];
  return routed === undefined ? [] : accountInputsOf(routed);
};
/** Every input that survives, with its original index; an input with nothing rejected is untouched. */
const survivors = (inputs: readonly RoutedEntityInput[], rejected: readonly Pick<CrossRejectedLeg, "inputIndex" | "accountInput">[]): readonly (readonly [number, RoutedEntityInput])[] =>
  inputs.flatMap((routed, at) => {
    const lost = new Set(rejected.flatMap((r) => (r.inputIndex === at ? [r.accountInput] : [])));
    const kept = lost.size === 0 ? [routed] : stripLegs(routed, lost);
    return kept.map((k) => [at, k] as const);
  });
/** The input without its cross-j legs, or undefined when nothing else is left. */
const withoutCrossLegs = (view: CrossAdmissionView, routed: RoutedEntityInput): RoutedEntityInput | undefined => {
  const legs = legsOf(replicasOf(view), [routed]);
  const rejected = lostAccountInputs(legs, [routed], 0).map((accountInput) => ({ inputIndex: 0, accountInput }));
  return survivors([routed], rejected)[0]?.[1];
};

// ---- the selection ----

/**
 * Every uncommitted cross-j leg must belong to an exact pair. Every other leg is stripped with its
 * reason; a marked cohort that did not survive loses both legs.
 */
export const matchedCrossPairs = (view: CrossAdmissionView, inputs: readonly RoutedEntityInput[]): CrossPairSelection => {
  const replicas = replicasOf(view);
  const legs = legsOf(replicas, inputs);
  const markedCohorts = groupedBy(inputs.flatMap((routed, at) => {
    const marker = routed.atomicCrossJurisdictionPair;
    return marker === undefined ? [] : [[cohortKey(routed, marker.phase, marker.pairKey), at] as const];
  }));
  if (legs.length === 0 && markedCohorts.size === 0) return { inputs: [...inputs], pairs: [], rejectedLegs: [] };

  // An exact replay of a committed frame is not a leg.
  const live = legs.filter((leg) => !leg.committed);
  const { multi, defects, failures, pairs } = judgeCohorts(view, replicas, inputs, live);
  const paired = new Set(pairs.flatMap((pair) => [pair.sourceInputIndex, pair.targetInputIndex]));
  const committed = new Set(legs.filter((leg) => leg.committed).map((leg) => leg.at));

  // Transport can deliver two inputs already marked as one atomic cohort. That cohort must come out
  // as exactly the pair it claims to be, or as a replay of frames both inputs already committed.
  // Anything else means the cohort was split on the way, and both inputs lose their legs.
  const cohortHeld = (ats: readonly number[]): boolean => {
    const bothCommitted = ats.every((at) => committed.has(at));
    const pairedTogether = pairs.some((pair) =>
      ats.includes(pair.sourceInputIndex) && ats.includes(pair.targetInputIndex));
    return ats.length === 2 && (bothCommitted || pairedTogether);
  };
  const splitCohortInputs = new Set([...markedCohorts.values()].filter((ats) => !cohortHeld(ats)).flat());

  // Why an input's legs are rejected, in og's order of precedence; undefined when it is admitted.
  const rejectionOf = (at: number): CrossRejectedLeg["reason"] | undefined => {
    switch (true) {
      case multi.has(at): return "multiple-candidates-per-input";
      case defects.has(at): return "candidate-invalid";
      case failures.has(at): return "pair-match-failed";
      case splitCohortInputs.has(at): return "atomic-group-invalid";
      case paired.has(at): return undefined;
      default: return "unpaired";
    }
  };
  const judged = sortWith(new Set([...live.map((leg) => leg.at), ...splitCohortInputs]), ascending);
  const rejections = judged.flatMap((at) => {
    const reason = rejectionOf(at);
    return reason === undefined ? [] : [{ at, reason }];
  });
  const rejectedAts = rejections.map(({ at }) => at);
  const rejectedLegs = rejections.flatMap(({ at, reason }) => {
    const detail = (reason === "pair-match-failed" ? failures.get(at) : defects.get(at)) ?? [];
    return lostAccountInputs(legs, inputs, at).map((accountInput): CrossRejectedLeg =>
      ({ inputIndex: at, accountInput, reason, detail }));
  });

  const kept = survivors(inputs, rejectedLegs);
  const newIndex = new Map(kept.map(([old], next) => [old, next] as const));
  const keptPairs = pairs.flatMap((pair) => {
    const source = newIndex.get(pair.sourceInputIndex);
    const target = newIndex.get(pair.targetInputIndex);
    const lost = rejectedAts.includes(pair.sourceInputIndex) || rejectedAts.includes(pair.targetInputIndex);
    return lost || source === undefined || target === undefined ? [] : [{ ...pair, sourceInputIndex: source, targetInputIndex: target }];
  });
  return { inputs: kept.map(([, routed]) => routed), pairs: keptPairs, rejectedLegs };
};

/** A transport retry of one proposal cohort keeps only its latest delivery, both legs together. */
const latestDeliveries = (inputs: readonly RoutedEntityInput[]): readonly RoutedEntityInput[] => {
  type Delivery = { readonly ats: readonly number[]; readonly height: number; readonly timestamp: number };
  type Seen = { readonly latest: ReadonlyMap<string, Delivery>; readonly dropped: readonly number[] };
  const isMarkedCohort = (pair: PotentialCrossPair, legs: readonly RoutedEntityInput[]): boolean =>
    legs.every((leg) => leg.atomicCrossJurisdictionPair?.phase === "proposal" && leg.atomicCrossJurisdictionPair.pairKey === pair.pairKey);
  // Two deliveries are one retry when the legs are identical apart from their source frame.
  const retryKey = (legs: readonly RoutedEntityInput[]): string =>
    canon(sortedBy(legs.map(({ sourceRuntimeFrame: _frame, ...exact }) => canon(exact)), (k) => k));

  const { dropped } = potentialCrossPairs(inputs).reduce<Seen>((seen, pair) => {
    const ats = [pair.sourceInputIndex, pair.targetInputIndex];
    const legs = ats.map((at) => inputs[at] as RoutedEntityInput);
    const frame = legs[0]?.sourceRuntimeFrame;
    if (frame === undefined || !isMarkedCohort(pair, legs)) return seen;
    const key = retryKey(legs);
    const delivery = { ats, height: frame.height, timestamp: frame.timestamp };
    const earlier = seen.latest.get(key);
    if (earlier === undefined) return { ...seen, latest: mapSet(seen.latest, key, delivery) };
    const newer = (delivery.height - earlier.height || delivery.timestamp - earlier.timestamp) > 0;
    return newer
      ? { latest: mapSet(seen.latest, key, delivery), dropped: [...seen.dropped, ...earlier.ats] }
      : { ...seen, dropped: [...seen.dropped, ...delivery.ats] };
  }, { latest: new Map(), dropped: [] });
  return inputs.filter((_, at) => !dropped.includes(at));
};
/** Each pair becomes two adjacent marked inputs, in first-leg order, ahead of every other input. */
const pairsFirst = (view: CrossAdmissionView, selection: CrossPairSelection): Result<CrossPairSelection, RuntimeError> => {
  if (selection.pairs.length === 0) return ok(selection);
  const pairAts = sortWith(selection.pairs, (a, b) => pairStart(a) - pairStart(b))
    .flatMap((pair) => sortWith([pair.sourceInputIndex, pair.targetInputIndex], ascending));
  if (!unique(pairAts.map(String))) return frameErr("RUNTIME_CROSS_J_ACCOUNT_PAIR_INPUT_OVERLAP");
  const reordered = [
    ...pairAts.map((at) => selection.inputs[at] as RoutedEntityInput),
    ...selection.inputs.filter((_, at) => !pairAts.includes(at)),
  ];
  // Reordering must not change what pairs; og re-selects to prove it.
  const regrouped = matchedCrossPairs(view, reordered);
  const diverged = regrouped.rejectedLegs.length > 0 || regrouped.pairs.length !== selection.pairs.length;
  if (diverged) return frameErr("RUNTIME_CROSS_J_ACCOUNT_PAIR_GROUPING_DIVERGED");
  return ok({ ...regrouped, inputs: withMarkers(regrouped.inputs, regrouped.pairs) });
};
/** Keep the latest retry, strip every unmatched leg (a replay refuses the frame instead), then put pairs first. */
export const admitAtomicCrossPairs = (
  view: CrossAdmissionView,
  inputs: readonly RoutedEntityInput[],
  isReplay: boolean,
): Result<{ readonly inputs: readonly RoutedEntityInput[]; readonly pairs: readonly CrossPair[] }, RuntimeError> => {
  const first = matchedCrossPairs(view, latestDeliveries(inputs));
  const stripped = first.rejectedLegs.length > 0;
  if (stripped && isReplay) return frameErr("RUNTIME_REPLAY_CROSS_J_ACCOUNT_PAIR_INVALID");
  // Stripping can orphan a leg, so select again on what is left.
  const settled = stripped ? matchedCrossPairs(view, first.inputs) : first;
  return chain(pairsFirst(view, settled), (selection) => selection.rejectedLegs.length > 0
    ? frameErr("RUNTIME_CROSS_J_ACCOUNT_PAIR_SELECTION_UNSTABLE")
    : ok({ inputs: selection.inputs, pairs: selection.pairs }));
};

// ---- after the frame: did each marked leg commit, and which outputs carry its ACK ----

/** The next input is this one's partner: the same marker on another Entity. */
const atomicPairInputsMatch = (routed: RoutedEntityInput, next: RoutedEntityInput | undefined): next is RoutedEntityInput => {
  const mine = routed.atomicCrossJurisdictionPair;
  const theirs = next?.atomicCrossJurisdictionPair;
  if (next === undefined || mine === undefined || theirs === undefined) return false;
  return mine.phase === theirs.phase && mine.pairKey === theirs.pairKey && onSiblings(routed, next);
};

type CommittedFrame = { readonly counterparty: string; readonly height: bigint; readonly stateHash: string };
const frameFrom = (account: AccountPeerInput, height: bigint, stateHash: unknown): CommittedFrame =>
  ({ counterparty: idOf(account.fromEntityId), height, stateHash: idOf(stateHash) });
const sameFrame = (a: CommittedFrame, b: CommittedFrame): boolean =>
  a.counterparty === b.counterparty && a.height === b.height && a.stateHash === b.stateHash;

/** The one frame a marked leg must commit: its proposal, or for an ACK leg the frame it ACKs. */
const expectedFrame = (routed: RoutedEntityInput): CommittedFrame | undefined => {
  const framesOf = (account: AccountPeerInput): readonly CommittedFrame[] => {
    switch (routed.atomicCrossJurisdictionPair?.phase) {
      case "proposal": {
        const frame = proposedFrameOf(account);
        return frame === undefined ? [] : [frameFrom(account, frame.height, frame.stateHash)];
      }
      default: {
        const ack = ackIn(account);
        return ack === undefined ? [] : [frameFrom(account, ack.height, ack.frameHash)];
      }
    }
  };
  return sole(accountInputsOf(routed).flatMap(framesOf));
};
/** The ACKed and proposed frames of an input that the replica's heads now hold. */
const committedFrames = (routed: RoutedEntityInput, replica: EntityReplica): readonly CommittedFrame[] =>
  accountInputsOf(routed).flatMap((account) => {
    const proposal = proposedFrameOf(account);
    const ack = ackIn(account);
    const peer = proposal === undefined && ack === undefined ? undefined : accountWith(replica, account.fromEntityId);
    if (peer === undefined) return [];
    const proposalCommitted = proposal !== undefined && headIs(peer, proposal.height, proposal.stateHash);
    // A proposal built directly on the ACKed frame commits that ACK with it.
    const builtOnAck = proposalCommitted && ack !== undefined
      && proposal.height === ack.height + 1n && idOf(proposal.prevFrameHash) === idOf(ack.frameHash);
    const ackCommitted = ack !== undefined && (headIs(peer, ack.height, ack.frameHash) || builtOnAck);
    return [
      ...(ackCommitted ? [frameFrom(account, ack.height, ack.frameHash)] : []),
      ...(proposalCommitted ? [frameFrom(account, proposal.height, proposal.stateHash)] : []),
    ];
  });
/** The replica's heads now hold the frame a marked leg had to commit. */
const committedExpectedAccountFrame = (routed: RoutedEntityInput, replica: EntityReplica): boolean => {
  const expected = expectedFrame(routed);
  return expected !== undefined && committedFrames(routed, replica).some((frame) => sameFrame(frame, expected));
};

/** The outputs carrying an Entity's ACK of its expected frame to the counterparty. */
const ackOutputsFor = (outbox: readonly EntityOutput[], expected: CrossFrameExpectation): readonly number[] => {
  const acksExpected = (account: AccountPeerInput): boolean => {
    const ack = ackIn(account);
    return ack !== undefined
      && idOf(account.fromEntityId) === idOf(expected.entityId)
      && idOf(account.toEntityId) === idOf(expected.counterpartyEntityId)
      && ack.height === expected.height
      && idOf(ack.frameHash) === expected.stateHash.toLowerCase();
  };
  const txsOf = (output: EntityOutput): readonly EntityTx[] => {
    if ("tx" in output) return [output.tx as EntityTx];
    return output.input.kind === "txs" ? effectiveTxs(output.input.txs) : [];
  };
  const carries = (output: EntityOutput): boolean => idOf(output.to) === idOf(expected.counterpartyEntityId)
    && txsOf(output).some((tx) => tx.type === "accountInput" && acksExpected(tx.data));
  return outbox.flatMap((output, index) => (carries(output) ? [index] : []));
};
/** Each committed proposal pair's two ACKs leave in two distinct outputs, which carry the pair's ACK marker. */
export const markCommittedAckOutputs = (
  outbox: readonly EntityOutput[],
  pairs: readonly CrossPair[],
): Result<readonly EntityOutput[], RuntimeError> => {
  type Marks = ReadonlyMap<number, string>;
  const markPair = (marks: Marks, pair: CrossPair): Result<Marks, RuntimeError> => {
    const sourceAck = sole(ackOutputsFor(outbox, pair.sourceAccountFrame));
    const targetAck = sole(ackOutputsFor(outbox, pair.targetAccountFrame));
    if (sourceAck === undefined || targetAck === undefined || sourceAck === targetAck) {
      return frameErr(`RUNTIME_CROSS_J_ATOMIC_ACK_OUTPUTS_INVALID:${pair.pairKey}`);
    }
    return ok(mapSet(mapSet(marks, sourceAck, pair.pairKey), targetAck, pair.pairKey));
  };
  const proposalPairs = pairs.filter((pair) => pair.phase === "proposal");
  return map(foldResult(proposalPairs, new Map() as Marks, markPair), (marks) =>
    outbox.map((output, index) => {
      const pairKey = marks.get(index);
      return pairKey === undefined ? output : { ...output, atomicCrossJurisdictionPair: { phase: "ack" as const, pairKey } };
    }));
};
// ---- og runtime/mempool/entity-inputs.ts applyMergedEntityInputs + admit/entity-input-output.ts: the R -> E -> A cascade of one Runtime frame ----
type BatchOut = { readonly key?: string | undefined; readonly rejected: readonly RuntimeError[]; readonly applied: readonly RoutedEntityInput[]; readonly committed: boolean; readonly progressed?: string | undefined; readonly effects?: readonly [string, CommitEffects] | undefined };
/** A local cross-j command (a certified runtimeOutput to a replica of this Runtime) or a local Account-work poke. */
type CrossCommand =
  | { readonly kind: "entity-txs"; readonly sourceEntityId: string; readonly sourceSignerId: string; readonly targetEntityId: string; readonly targetSignerId: string; readonly entityTxs: readonly EntityTx[] }
  | { readonly kind: "account-work"; readonly sourceEntityId: string; readonly targetEntityId: string; readonly targetSignerId: string };
type EntityTxsCommand = Extract<CrossCommand, { readonly kind: "entity-txs" }>;
const commandKey = (c: EntityTxsCommand): string => `${c.kind}\0${c.sourceEntityId}\0${c.sourceSignerId}\0${c.targetEntityId}\0${c.targetSignerId}`;
const workKey = (t: { readonly entityId: string; readonly signerId: string }): string => `${t.entityId}\0${t.signerId}`;
/** Each active validator with ready Account work and no frame in flight, once, sorted by (Entity, signer). */
export const readyAccountWorkTargets = (replicas: Iterable<EntityReplica>): readonly { readonly entityId: string; readonly signerId: string }[] => sortWith(firstBy([...replicas].flatMap((r) => {
  const entityId = lower(r.state.id), signerId = lower(r.signerId);
  return signerId === lower(leaderStateOf(r.state).activeValidatorId) && r._tag === "open" && hasProposableAccount(r) ? [{ entityId, signerId }] : [];
}), workKey), (a, b) => workKey(a).localeCompare(workKey(b)));
/** Exactly one cross-j runtimeOutput, no consensus lane. */
const crossCommandOf = (o: EntityOutput): readonly [Extract<EntityOutput, { readonly input: EntityInput }>, Extract<EntityTx, { readonly type: "runtimeOutput" }>["data"]] | undefined => {
  const tx = "input" in o && o.input.kind === "txs" && o.input.txs.length === 1 ? o.input.txs[0] : undefined;
  return "input" in o && tx?.type === "runtimeOutput" && tx.data.protocol === "cross-j" ? [o, tx.data] : undefined;
};
const localReplica = (store: ReadonlyMap<string, EntityReplica>, entity: string, signer: string): EntityReplica | undefined =>
  [...store.values()].find((r) => lower(r.state.id) === lower(entity) && lower(r.signerId) === lower(signer));
/** The state one Runtime frame threads through its Entity inputs; `deferred` holds the replicas a `txs` input left to propose. */
type InputBatch = {
  readonly store: ReadonlyMap<string, EntityReplica>; readonly outs: readonly BatchOut[]; readonly outbox: readonly EntityOutput[]; readonly queue: readonly CrossCommand[];
  readonly deferred: ReadonlyMap<string, { readonly entityId: EntityId; readonly signerId: string }>; readonly localEvents: number;
  readonly rejectedPairs: ReadonlySet<number>; readonly committedPairs: ReadonlySet<number>;
};
type Staged = { readonly key: string; readonly outputs: readonly EntityOutput[]; readonly committed: boolean };
const rejectOut = (b: InputBatch, e: RuntimeError): InputBatch => ({ ...b, outs: [...b.outs, { rejected: [e], applied: [], committed: false }] });
type StageOptions = {
  readonly lane?: EntityContext["lane"] | undefined;
  readonly recordApplied: boolean;
  readonly requiredTx?: EntityTx | undefined;
};
/** Whether a commit came from local Account work, which must not poke Account work again. */
type CommitCause = "input" | "account-work";
type PairRefusal = "CROSS_J_ACCOUNT_PAIR_NOT_COMMITTED" | "CROSS_J_ACCOUNT_PAIR_PROTOCOL_REJECTED";
/** A pair's legs so far: all committed their expected frames, one missed its frame, or one was refused. */
type PairRun =
  | { readonly _tag: "framed"; readonly batch: InputBatch; readonly staged: readonly Staged[] }
  | { readonly _tag: "unframed"; readonly batch: InputBatch; readonly staged: readonly Staged[] }
  | { readonly _tag: "refused"; readonly code: PairRefusal };
const topLevelAccountInputs = (leg: RoutedEntityInput): readonly EntityTx[] =>
  leg.input.kind === "txs" ? leg.input.txs.filter((tx) => tx.type === "accountInput") : [];
type PairLegContext = { readonly leg: RoutedEntityInput; readonly replay: boolean };
/**
 * An admission refusal leaves the pair NOT_COMMITTED; any other refusal of a transported leg is PROTOCOL_REJECTED.
 * An invariant, a thrown handler, an unroutable or local leg, or any replay refuses the whole frame.
 */
const pairLegRefusal = (
  error: RuntimeError,
  { leg, replay }: PairLegContext,
): Result<PairRefusal, RuntimeError> => {
  if (INGRESS_REJECTIONS.has(error._tag)) return ok("CROSS_J_ACCOUNT_PAIR_NOT_COMMITTED");
  const transported = String(leg.from ?? "").trim() !== "";
  const broken =
    error._tag === "entity_invariant" ||
    error._tag === "no_such_entity" ||
    accountThrew(error as EntityError);
  if (broken || !transported || replay) return frameErr(runtimeErrorText(error));
  return ok("CROSS_J_ACCOUNT_PAIR_PROTOCOL_REJECTED");
};
/**
 * Each merged input applies in order; a plain `txs` input only fills its mempool (touched replicas propose once, in first-touch order, before a pair
 * and at the end). After each input, cross-j commands to local replicas and local Account work drain in this frame; other outputs leave in the outbox.
 */
const entityInputBatch = (rt: Runtime, merged: readonly RoutedEntityInput[], timestamp: bigint, ctx: RuntimeCtx): Result<InputBatch, RuntimeError> => {
  const stage = (b: InputBatch, routed: RoutedEntityInput, { lane, recordApplied, requiredTx }: StageOptions): Result<readonly [InputBatch, Staged], RuntimeError> => {
    const key = replicaKey(routed.entityId, routed.signerId), r = b.store.get(key);
    if (r === undefined) return err({ _tag: "no_such_entity", id: routed.entityId });
    const stamped: RoutedEntityInput = routed.input.kind === "txs" || routed.input.kind === "jPrefixAttestations" ? { ...routed, input: { ...routed.input, timestamp } } : routed;
    const siblings: SiblingReplicas = (entity, signer) => localReplica(b.store, entity, signer);
    const boardAuthority: BoardAuthority = (source) => settlementBoardAuthority([...b.store.values()].filter((x) => lower(x.state.id) === lower(source)).map((x) => x.state), source);
    return map(applyEntityInput(r, stamped.input, {
      self: routed.entityId, signerId: routed.signerId as Address, ...ctx, htlc: runtimeHtlcInfra(ctx, rt, routed.entityId), ...opt("activeJurisdiction", rt.activeJurisdiction),
      ...opt("jHistory", replicaJHistory({ ...rt, entities: b.store }, key, r)), siblings, boardAuthority, ...opt("lane", lane), ...opt("required", requiredTx), jReplicas: rt.jReplicas,
    }), ({ replica, outputs, committed: effects }) => {
      const committed = replica.head.height > r.head.height, progressed = consensusProgressed(r, replica, stamped.input, rt.replicaLocal.get(key));
      const out: BatchOut = { key, rejected: [], applied: recordApplied ? [stamped] : [], committed, progressed: progressed ? key : undefined, ...(effects === undefined ? {} : { effects: [key, effects] as const }) };
      return [{ ...b, store: mapSet(b.store, key, replica), outs: [...b.outs, out] }, { key, outputs, committed }] as const;
    });
  };
  // a cross-j command to a local replica becomes a local command (coalesced inside this one Entity frame only)
  const route = (b: InputBatch, outputs: readonly EntityOutput[]): Result<InputBatch, RuntimeError> =>
    map(foldResult(outputs, { outbox: b.outbox, commands: [] as readonly EntityTxsCommand[] }, (s, o) => {
      const envelope = crossCommandOf(o);
      if (envelope === undefined || localReplica(b.store, o.to, envelope[0].signerId) === undefined) return ok({ ...s, outbox: [...s.outbox, o] });
      const [{ signerId }, d] = envelope;
      const command: EntityTxsCommand = { kind: "entity-txs", sourceEntityId: lower(d.sourceEntityId), sourceSignerId: lower(d.sourceSignerId), targetEntityId: lower(d.targetEntityId), targetSignerId: lower(signerId), entityTxs: d.entityTxs };
      const { sourceEntityId: source, targetEntityId: target } = command, key = commandKey(command);
      return !source || !command.sourceSignerId || !target || !command.targetSignerId || target !== lower(o.to)
        ? frameErr(`RUNTIME_CROSS_J_COMMAND_ROUTE_INVALID:source=${source || "missing"}:target=${target || "missing"}:envelope=${o.to}`)
        : d.entityTxs.length === 0 ? frameErr(`RUNTIME_CROSS_J_COMMAND_TXS_MISSING:${target}`)
        : ok({ ...s, commands: s.commands.some((c) => commandKey(c) === key) ? s.commands.map((c) => (commandKey(c) === key ? { ...c, entityTxs: [...c.entityTxs, ...d.entityTxs] } : c)) : [...s.commands, command] });
    }), ({ outbox, commands }) => ({ ...b, outbox, queue: [...b.queue, ...commands] }));
  // a commit not itself caused by Account work pokes every local active validator with ready Account work, once
  const collect = (b: InputBatch, staged: Staged, cause: CommitCause): Result<InputBatch, RuntimeError> => map(route(b, staged.outputs), (routed) => !staged.committed || cause === "account-work" ? routed : {
    ...routed, queue: [...routed.queue, ...readyAccountWorkTargets(routed.store.values())
      .filter((t) => !routed.queue.some((c) => c.kind === "account-work" && c.targetEntityId === t.entityId && c.targetSignerId === t.signerId))
      .map((t): CrossCommand => ({ kind: "account-work", sourceEntityId: t.entityId, targetEntityId: t.entityId, targetSignerId: t.signerId }))],
  });
  const settle = (b: InputBatch, staged: Result<readonly [InputBatch, Staged], RuntimeError>): Result<InputBatch, RuntimeError> =>
    staged.ok ? collect(staged.value[0], staged.value[1], "input") : ok(rejectOut(b, staged.error));
  // each local command must commit; cycles and runaway cascades refuse the frame
  const drain = (b: InputBatch, round = 1, seen: ReadonlySet<string> = new Set()): Result<InputBatch, RuntimeError> => {
    const [command, ...queue] = b.queue, localEvents = b.localEvents + 1, replicas = b.store.size;
    if (command === undefined) return ok(b);
    const fingerprint = command.kind === "entity-txs" ? canon(command) : undefined, r = localReplica(b.store, command.targetEntityId, command.targetSignerId);
    if (round > 64 + replicas || localEvents > 1_000 + 64 * replicas) return frameErr(`RUNTIME_CROSS_J_EVENT_CASCADE_LIMIT:rounds=${round}:events=${localEvents}`);
    if (fingerprint !== undefined && seen.has(fingerprint)) return frameErr(`RUNTIME_CROSS_J_EVENT_CYCLE:round=${round}:entity=${command.targetEntityId}`);
    if (r === undefined) return frameErr(`RUNTIME_CROSS_J_LOCAL_REPLICA_NOT_FOUND:${command.targetEntityId}:${command.targetSignerId}`);
    const txs: readonly EntityTx[] = command.kind === "account-work" ? []
      : [{ type: "runtimeOutput", data: { protocol: "cross-j", sourceEntityId: command.sourceEntityId, sourceSignerId: command.sourceSignerId, targetEntityId: command.targetEntityId, entityTxs: command.entityTxs } }];
    const staged = stage({ ...b, queue, localEvents }, { entityId: r.state.id, signerId: r.signerId, input: { kind: "txs", timestamp, txs } }, { lane: command.kind === "entity-txs" ? "cross-j" : "account-work", recordApplied: false });
    return !staged.ok ? frameErr(`RUNTIME_CROSS_J_LOCAL_EVENT_NOT_COMMITTED:entity=${command.targetEntityId}:round=${round}:outcome=rejected:detail=${runtimeErrorText(staged.error)}`)
      : chain(collect(staged.value[0], staged.value[1], command.kind === "account-work" ? "account-work" : "input"), (next) => drain(next, round + 1, fingerprint === undefined ? seen : new Set([...seen, fingerprint])));
  };
  // each touched replica proposes once from its mempool
  const flush = (b: InputBatch): Result<InputBatch, RuntimeError> => foldResult<InputBatch, { readonly entityId: EntityId; readonly signerId: string }, RuntimeError>(b.deferred.values(), { ...b, deferred: new Map() }, (s, { entityId, signerId }) =>
    chain(settle(s, stage(s, { entityId, signerId, input: { kind: "txs", timestamp, txs: [] } }, { recordApplied: false })), (next) => drain(next)));
  // a `txs` input carries no consensus evidence
  const single = (b: InputBatch, routed: RoutedEntityInput): Result<InputBatch, RuntimeError> => {
    const deferrable = routed.input.kind === "txs", staged = stage(b, routed, { lane: deferrable ? "defer" : undefined, recordApplied: true });
    if (!staged.ok) return ok(rejectOut(b, staged.error));
    const [next, { key, committed }] = staged.value;
    const deferred = committed ? mapDelete(next.deferred, key) : deferrable ? mapSet(next.deferred, key, { entityId: routed.entityId, signerId: routed.signerId }) : next.deferred;
    return collect({ ...next, deferred }, staged.value[1], "input");
  };
  // A pair commits both expected Account frames or neither: on any failure both staged legs are discarded,
  // the pair is rejected at its first leg's position and each leg re-applies without its Account legs.
  const applyPair = (
    before: InputBatch,
    pair: readonly [RoutedEntityInput, RoutedEntityInput],
    pairIndex: number,
  ): Result<InputBatch, RuntimeError> => {
    const accountInputs = pair.map(topLevelAccountInputs);
    const miscounted = accountInputs.findIndex((txs) => txs.length !== 1);
    if (miscounted >= 0) {
      const entityId = pair[miscounted]?.entityId;
      const count = accountInputs[miscounted]?.length;
      return frameErr(`RUNTIME_CROSS_J_ATOMIC_ACCOUNT_INPUT_COUNT_INVALID:${entityId}:${count}`);
    }
    const start: PairRun = { _tag: "framed", batch: before, staged: [] };
    const run = foldResult<PairRun, RoutedEntityInput, RuntimeError>(
      pair,
      start,
      (current, leg, legIndex) => stagePairLeg(current, leg, accountInputs[legIndex]?.[0]),
    );
    return chain(run, (finished) => finishPair(before, pair, pairIndex, finished));
  };
  const stagePairLeg = (
    run: PairRun,
    leg: RoutedEntityInput,
    requiredTx: EntityTx | undefined,
  ): Result<PairRun, RuntimeError> => {
    if (run._tag === "refused") return ok(run);
    const staged = stage(run.batch, leg, { recordApplied: true, requiredTx });
    if (!staged.ok) {
      const refusal = pairLegRefusal(staged.error, { leg, replay: ctx.replay === true });
      return map(refusal, (code): PairRun => ({ _tag: "refused", code }));
    }
    const [batch, stagedLeg] = staged.value;
    const replica = batch.store.get(stagedLeg.key);
    const legFramed =
      stagedLeg.committed && replica !== undefined && committedExpectedAccountFrame(leg, replica);
    const allFramed = run._tag === "framed" && legFramed;
    return ok({
      _tag: allFramed ? "framed" : "unframed",
      batch,
      staged: [...run.staged, stagedLeg],
    });
  };
  const finishPair = (
    before: InputBatch,
    pair: readonly RoutedEntityInput[],
    pairIndex: number,
    run: PairRun,
  ): Result<InputBatch, RuntimeError> => {
    if (run._tag === "framed") return commitPair(run.batch, run.staged, pairIndex);
    if (ctx.replay === true) return frameErr("RUNTIME_REPLAY_CROSS_J_ACCOUNT_PAIR_NOT_COMMITTED");
    const code = run._tag === "refused" ? run.code : "CROSS_J_ACCOUNT_PAIR_NOT_COMMITTED";
    return rollBackPair(before, pair, pairIndex, code);
  };
  const commitPair = (
    batch: InputBatch,
    stagedLegs: readonly Staged[],
    pairIndex: number,
  ): Result<InputBatch, RuntimeError> => {
    const collected = foldResult(stagedLegs, batch, (current, stagedLeg) =>
      collect(current, stagedLeg, "input"),
    );
    return map(collected, (after) => ({
      ...after,
      committedPairs: new Set([...after.committedPairs, pairIndex]),
    }));
  };
  const rollBackPair = (
    before: InputBatch,
    pair: readonly RoutedEntityInput[],
    pairIndex: number,
    code: PairRefusal,
  ): Result<InputBatch, RuntimeError> => {
    const pairRejected: InputBatch = {
      ...before,
      rejectedPairs: new Set([...before.rejectedPairs, pairIndex]),
    };
    return foldResult(pair, pairRejected, (batch, leg) =>
      reapplyWithoutCrossLegs(batch, leg, code),
    );
  };
  const reapplyWithoutCrossLegs = (
    batch: InputBatch,
    leg: RoutedEntityInput,
    code: PairRefusal,
  ): Result<InputBatch, RuntimeError> => {
    const rejected = rejectOut(batch, { _tag: "runtime_frame", code });
    const remainder = withoutCrossLegs({ entities: rejected.store, timestamp: rt.timestamp }, leg);
    if (remainder === undefined) return ok(rejected);
    const staged = stage(rejected, remainder, { recordApplied: true });
    return settle(rejected, staged);
  };
  // a marked pair is two adjacent inputs; it sees every earlier admission already framed
  type Unit = { readonly index: number; readonly legs: readonly [RoutedEntityInput] | readonly [RoutedEntityInput, RoutedEntityInput] };
  const units = merged.reduce<readonly Unit[]>((us, routed, index) => {
    const last = us.at(-1), next = merged[index + 1];
    return last?.legs.length === 2 && last.index === index - 1 ? us : [...us, { index, legs: atomicPairInputsMatch(routed, next) ? [routed, next] : [routed] }];
  }, []);
  const init: InputBatch = { store: rt.entities, outs: [], outbox: [], queue: [], deferred: new Map(), localEvents: 0, rejectedPairs: new Set(), committedPairs: new Set() };
  return chain(foldResult(units, init, (b, { index, legs }) => chain(legs.length === 2 ? chain(flush(b), (f) => applyPair(f, legs, index)) : single(b, legs[0]), (next) => drain(next))), flush);
};
/** `txs` admission refusals that reject an input rather than fail its ingress. */
const INGRESS_REJECTIONS: ReadonlySet<string> = new Set(["mempool_full", "frame_timestamp_invalid", "from_not_converted"]);
const runtimeErrorText = (e: RuntimeError | EntityError): string => ("code" in e && typeof e.code === "string" ? e.code : "reason" in e && typeof e.reason === "string" ? e.reason : e._tag);
/** Consensus progress, the J history each commit certified, and each committed frame's witnesses (stamped with the Runtime clock) pruned to what stays reachable. */
const commitLocal = (afterTxs: Runtime, store: ReadonlyMap<string, EntityReplica>, outs: readonly BatchOut[], timestamp: bigint): Runtime["replicaLocal"] => {
  const at = Number(timestamp);
  const progressed = outs.flatMap((o) => (o.progressed === undefined ? [] : [o.progressed]))
    .reduce((local, key) => mapSet(local, key, { ...(local.get(key) ?? {}), lastConsensusProgressAt: at }), afterTxs.replicaLocal);
  const pruned = [...new Set(outs.flatMap((o) => (o.committed && o.key !== undefined ? [o.key] : [])))].reduce((local, key) => {
    const current = local.get(key), replica = store.get(key);
    return current?.jHistory !== undefined && replica !== undefined ? mapSet(local, key, { ...current, jHistory: replicaJHistory({ ...afterTxs, replicaLocal: local }, key, replica) }) : local;
  }, progressed);
  return outs.flatMap((o) => (o.effects === undefined ? [] : [o.effects])).reduce((local, [key, effects]) => {
    const current = local.get(key) ?? {}, state = store.get(key)?.state;
    const witness = new Map([...(current.hankoWitness ?? []), ...[...effects.witnesses].map(([hash, w]) => [hash, { ...w, createdAt: at }] as const)]);
    return mapSet(local, key, { ...current, hankoWitness: state === undefined ? witness : pruneWitnesses(witness, state) });
  }, pruned);
};
export const applyRuntime = (rt: Runtime, input: RuntimeInput, ctx: RuntimeCtx): Result<RuntimeStep, RuntimeError> => chain(validateRuntimeInput(rt, input), (jOutbox) => {
  const forged = forgedIngress(input, ctx);
  if (forged !== undefined) return frameErr(forged);
  const seeds = input.entityInputs.flatMap((i) => (i.input.kind === "txs" ? [i.input.timestamp] : []));
  const timestamp = [input.timestamp ?? rt.timestamp, ...(input.timestamp === undefined ? seeds : [])].reduce((a, b) => (b > a ? b : a), rt.timestamp);
  type TxFold = { readonly runtime: Runtime; readonly jOutputs: readonly JInput[] };
  const txFold = foldResult(input.runtimeTxs, { runtime: { ...rt, timestamp }, jOutputs: [] } as TxFold, (at, tx) => map(applyRuntimeTxStep(at.runtime, tx, ctx), (s): TxFold => ({ runtime: s.runtime, jOutputs: [...at.jOutputs, ...s.jOutputs] })));
  // transported cross-j cohorts are isolated before the merge, then admitted as exact pairs before any Entity input applies
  return chain(txFold, ({ runtime: afterTxs, jOutputs: txJOutputs }) => chain(mergeEntityInputs(markPotentialCrossPairs(input.entityInputs), verifiedCommit(afterTxs.entities, ctx)), (mergedInputs) =>
    chain(admitAtomicCrossPairs(afterTxs, mergedInputs, ctx.replay === true), ({ inputs: merged, pairs }) => chain(entityInputBatch(afterTxs, merged, timestamp, ctx), ({ store, outs, outbox: batchOutbox, rejectedPairs, committedPairs }) => {
      // every admitted pair that was not rejected committed both expected Account frames; its two ACKs carry the pair marker
      const accepted = pairs.filter((p) => !rejectedPairs.has(pairStart(p)));
      if (accepted.some((p) => !committedPairs.has(pairStart(p)))) return frameErr("RUNTIME_CROSS_J_ACCOUNT_PAIR_COMMIT_DIVERGED_FROM_ADMISSION");
      return chain(markCommittedAckOutputs(batchOutbox, accepted), (outbox) => {
        const applied = outs.flatMap((o) => o.applied), replicaLocal = commitLocal(afterTxs, store, outs, timestamp);
        // ingress J inputs, RuntimeTx J outputs, then each emitter's committed Entity J outputs; split for durable submit.
        const frameJ = [...jOutbox, ...txJOutputs, ...outs.flatMap((o) => o.effects?.[1].jOutputs ?? [])];
        const meaningful = applied.filter((i) => i.input.kind !== "txs" || i.input.txs.length > 0).length;
        const entityInputCount = outs.some((o) => o.committed) ? Math.max(meaningful, applied.length) : meaningful;
        const advanced = input.runtimeTxs.length > 0 || entityInputCount > 0 || outbox.length > 0 || frameJ.length > 0;
        return chain(splitJOutbox(frameJ), (split) => map(registerPendingJOutbox(afterTxs.pendingCommittedJOutbox, split.durable), (pendingCommittedJOutbox): RuntimeStep => {
          const runtime: Runtime = { ...afterTxs, entities: store, replicaLocal, pendingCommittedJOutbox, height: advanced ? afterTxs.height + 1n : afterTxs.height };
          const appliedInput: RuntimeInput = { runtimeTxs: input.runtimeTxs, entityInputs: applied, ...(jOutbox.length > 0 ? { jInputs: jOutbox } : {}) };
          return { runtime, applied: appliedInput, outbox, jOutbox: [...pendingCommittedJOutbox, ...split.maintenance], queuedRetries: split.retries, rejected: outs.flatMap((o) => o.rejected), advanced, events: outs.flatMap((o) => o.effects?.[1].runtimeEvents ?? []) };
        }));
      });
    }))));
});

// ---- og storage/hashes.ts, canonical-hash.ts, replica-meta-digest.ts, wal/outbox-payload.ts: Runtime WAL commitments ----
/** og computeIntegrityDigest(encodeBinaryPayload(value)): 0x-sha256 over the 0x03-framed canonical msgpack. */
const hashStable = (value: Binary): Result<string, BinaryError> => map(encodeBinary(value), integrity);
export type StorageFrameEntityHash = { readonly entityId: string; readonly hash: string; readonly cellCount: number };
const sortedEntityHashes = (rows: readonly StorageFrameEntityHash[]): readonly StorageFrameEntityHash[] =>
  rows.map((r) => ({ entityId: r.entityId.toLowerCase(), hash: r.hash, cellCount: r.cellCount })).sort((a, b) => asc(a.entityId, b.entityId));
/** og RuntimeFrame (storage/types.ts): the WAL row. `runtimeInput` is the applied input; optional og fields are absent when empty. */
export type StorageFrame = {
  readonly height: number; readonly timestamp: number; readonly prevFrameHash?: string | undefined; readonly frameHash?: string | undefined;
  readonly replicaMetaDigest: string; readonly postStateHash: string; readonly materializedState: boolean;
  readonly canonicalStateHash?: string | undefined; readonly canonicalEntityHashes?: readonly StorageFrameEntityHash[] | undefined;
  readonly runtimeInput: Binary; readonly runtimeOutputCount: number; readonly runtimeOutputsDigest: string;
  readonly touchedEntities: readonly string[]; readonly touchedAccounts: readonly { readonly entityId: string; readonly counterpartyId: string }[]; readonly touchedBookEntities: readonly string[];
  readonly [extra: string]: Binary | undefined;
};
const definedFields = (record: { readonly [field: string]: Binary | undefined }): { readonly [field: string]: Binary } =>
  Object.fromEntries(Object.entries(record).filter((e): e is [string, Binary] => e[1] !== undefined));
/** og computeStorageFrameHash: the frame without `frameHash`, under domain `xln.storage.frame`, canonical Entity hashes normalized and sorted. */
export const storageFrameHash = (record: StorageFrame): Result<string, BinaryError> => {
  const { frameHash: _, ...rest } = record;
  return hashStable({ kind: "xln.storage.frame", ...definedFields(rest), canonicalEntityHashes: sortedEntityHashes(record.canonicalEntityHashes ?? []) });
};
/** og computeCanonicalRuntimeStateHash: keccak of the JSON text {kind, height, timestamp, entities}. */
export const canonicalRuntimeStateHash = (height: number, timestamp: number, entities: readonly StorageFrameEntityHash[]): string =>
  `0x${keccakUtf8(JSON.stringify({ kind: "xln.storage.canonicalRuntimeHash.v2", height, timestamp, entities: sortedEntityHashes(entities) }))}`;
export type ComponentDigest = { readonly key: string; readonly valueHash: string };
/** og computeRuntimePostStateComponentDigests: one integrity hash per Runtime component, keys sorted. */
export const runtimeComponentDigests = (view: { readonly [key: string]: Binary }): Result<readonly ComponentDigest[], BinaryError> =>
  traverse(Object.keys(view).sort(asc), (key) => map(hashStable(view[key] as Binary), (valueHash) => ({ key, valueHash })));
/** og computeStoragePostStateHash: the per-frame replay oracle. */
export const storagePostStateHash = (i: { readonly height: number; readonly timestamp: number; readonly replicaMetaDigest: string; readonly runtimeComponentDigests: readonly ComponentDigest[]; readonly runtimeOutputCount: number; readonly runtimeOutputsDigest: string }): Result<string, BinaryError> =>
  hashStable({ kind: "xln.storage.postState", height: i.height, timestamp: i.timestamp, replicaMetaDigest: i.replicaMetaDigest, runtimeComponentDigests: i.runtimeComponentDigests, runtimeOutputCount: i.runtimeOutputCount, runtimeOutputsDigest: i.runtimeOutputsDigest });
/** og computeStorageReplicaMetaDigest: rows sorted by hex key then value hash; values hashed independently. */
export const replicaMetaDigest = (rows: readonly { readonly key: Uint8Array; readonly value: Uint8Array }[]): Result<string, BinaryError> =>
  hashStable({ kind: "xln.storage.replicaMeta.v1", entries: rows.map((r) => ({ key: bytesToHex(r.key).toLowerCase(), valueHash: integrity(r.value) })).sort((a, b) => asc(a.key, b.key) || asc(a.valueHash, b.valueHash)) });
const MAX_RUNTIME_OUTPUT_ROWS = 10_000;
/** og computeRuntimeOutputsDigest: sha256("xln.runtime.outbox.v1" | u32 count | (u32 len | row)*), rows in `(height, index)` order. */
export const runtimeOutputsDigest = (rows: readonly Uint8Array[]): Result<string, RuntimeError> =>
  rows.length > MAX_RUNTIME_OUTPUT_ROWS ? frameErr("STORAGE_RUNTIME_OUTPUT_COUNT_MAX") : ok(bytesToHex(sha256(concat([utf8("xln.runtime.outbox.v1"), u32(rows.length), ...rows.flatMap((r) => [u32(r.byteLength), r])]))));

/** Rewrite values as og binary payload: optional fields the rewrite leaves `undefined` are absent (og builders spread them in only when set). */
const binaryOf = (v: unknown): Binary => {
  if (v === null || typeof v !== "object" || v instanceof Uint8Array) return v as Binary;
  if (Array.isArray(v)) return v.map(binaryOf);
  if (v instanceof Map) return new Map([...v].map(([k, x]) => [binaryOf(k), binaryOf(x)]));
  return Object.fromEntries(Object.entries(v).filter(([, x]) => x !== undefined).map(([k, x]) => [k, binaryOf(x)]));
};
/** og buildCertifiedEntityHeadPlan: one replica per Entity, the one with the highest certified height (first on a tie). */
const certifiedHeads = (rt: Runtime): readonly EntityReplica[] => {
  const heads = new Map<string, EntityReplica>();
  for (const r of rt.entities.values()) { const id = lower(r.state.id), held = heads.get(id); if (held === undefined || r.head.height > held.head.height) heads.set(id, r); }
  return [...heads.values()];
};
/** og computeCanonicalEntityHash: the Entity consensus state root of the certified head, one cell. */
export const canonicalEntityHashes = (rt: Runtime): Result<readonly StorageFrameEntityHash[], RuntimeError> =>
  map(traverse(certifiedHeads(rt), (r) => map(entityRootOf(r.state, r.accountReplicas), (hash) => ({ entityId: lower(r.state.id), hash, cellCount: 1 }))), sortedEntityHashes);
const KEY_LIVE_REPLICA_META = 0x26;
/**
 * og buildStorageLiveReplicaMetaCommitment rows: the replica's identity, Entity head, certified-link digest, leader votes (prepared frames on og's
 * EntityFrame wire), pending leader certificate, J-prefix round and J submit states.
 */
export const replicaMetaRows = (rt: Runtime): Result<readonly { readonly key: Uint8Array; readonly value: Uint8Array }[], RuntimeError> =>
  traverse([...rt.entities], ([key, r]) => {
    const entity = lower(r.state.id), signer = signerId(r.signerId);
    const rowKey = concat([Uint8Array.of(KEY_LIVE_REPLICA_META), hexToBytes(entity), new Uint8Array(12), hexToBytes(signer)]);
    const votes = r.leaderVotes === undefined ? ok(undefined) : map(traverse([...r.leaderVotes], ([k, v]) => map(voteBinary(v), (b) => [k, b] as const)), (rows): Binary => new Map(rows));
    const cert = r.pendingLeaderCertificate === undefined ? ok(undefined) : certificateBinary(r.pendingLeaderCertificate);
    const headDigest = r.certifiedFrameHead === undefined ? ok(undefined) : map(encodeBinary(r.certifiedFrameHead), integrity);
    return chain(frameNumber(r.state.height), (height) => chain(frameNumber(r.state.timestamp), (timestamp) => chain(votes, (leaderVotes) => chain(cert, (pendingLeaderCertificate) => chain(headDigest, (certifiedFrameHeadDigest) => map(encodeBinary({
      replicaKey: key.toLowerCase(), entityId: entity, signerId: signer, isProposer: signerId(r.state.quorum.proposer) === signer,
      entityHead: { entityId: entity, height, timestamp, frameHash: r.head.height === 0n ? "" : frameWord(r.head.prevFrameHash) },
      ...opt("certifiedFrameHeadDigest", certifiedFrameHeadDigest), ...opt("leaderVotes", leaderVotes), ...opt("pendingLeaderCertificate", pendingLeaderCertificate),
      ...opt("jPrefixRound", r.jPrefixRound === undefined ? undefined : binaryOf(r.jPrefixRound)),
      ...opt("jSubmitState", binaryOf(rt.replicaLocal.get(key)?.jSubmitState)), ...opt("entityProviderActionSubmitState", binaryOf(rt.replicaLocal.get(key)?.entityProviderActionSubmitState)),
    }), (value) => ({ key: rowKey, value })))))));
  });
/** og buildCanonicalJReplicaSnapshot + buildDurableJReplicaSnapshot: fixed field set (no token registry), wall-clock marker zeroed, state root as bytes. */
const jReplicaSnapshot = (r: JReplica): Binary => binaryOf({
  name: r.name, blockNumber: r.blockNumber, stateRoot: r.stateRoot === null ? null : (hexToBytes(r.stateRoot) as unknown as Binary), mempool: r.mempool, blockDelayMs: r.blockDelayMs,
  blockTimeMs: r.blockTimeMs, lastBlockTimestamp: 0, blockReady: r.blockReady, watcherConfirmationDepth: r.watcherConfirmationDepth, watcherReceiptCommitment: r.watcherReceiptCommitment,
  rpcs: r.rpcs, chainId: r.chainId, position: r.position, entityProviderDeploymentBlock: r.entityProviderDeploymentBlock,
  contracts: r.contracts === undefined ? undefined : Object.fromEntries(Object.entries(r.contracts).filter(([, v]) => Boolean(v))),
});
/** og buildDurableRuntimeStateSnapshot: only non-empty durable maps; absent when none. */
const durableInfrastructure = (rt: Runtime): { readonly [key: string]: Binary } | undefined => {
  const rows: [string, unknown, number][] = [
    ["runtimeAdapterCommandFrontiers", rt.adapterFrontiers, rt.adapterFrontiers.size], ["pendingCommittedJOutbox", rt.pendingCommittedJOutbox, rt.pendingCommittedJOutbox.length],
    ["pendingJurisdictionImports", rt.pendingJImports, rt.pendingJImports.size], ["numberedRegistrationIntents", rt.numberedRegistrationIntents, rt.numberedRegistrationIntents.size], ["certifiedRegistrationEvidence", rt.registrationEvidence, rt.registrationEvidence.size],
    ["entityEncryptionSeeds", rt.encryptionSeeds, rt.encryptionSeeds.size],
  ];
  const kept = rows.filter(([, , size]) => size > 0);
  return kept.length === 0 ? undefined : Object.fromEntries(kept.map(([k, m]) => [k, binaryOf(m)]));
};
/** og buildReplayVerifiableRuntimePostStateView: runtimeId, BrowserVM header (no trie), durable infrastructure, J replicas in insertion order. */
export const runtimeView = (rt: Runtime): { readonly [key: string]: Binary } => {
  const infrastructure = durableInfrastructure(rt);
  const vm = rt.browserVMState === undefined ? undefined : Object.fromEntries(Object.entries(rt.browserVMState).filter(([k]) => k !== "trieData"));
  return {
    ...(rt.runtimeId ? { runtimeId: rt.runtimeId } : {}), ...(vm !== undefined ? { browserVMState: binaryOf(vm) } : {}), ...opt("infrastructure", infrastructure),
    jReplicas: [...rt.jReplicas].map(([k, r]) => [k, jReplicaSnapshot(r)]),
  };
};
export type RuntimeFrameCommit = { readonly runtime: Runtime; readonly frame: StorageFrame; readonly applied: RuntimeInput; readonly outbox: readonly EntityOutput[];
  /** og frame.runtimeOutputs: the retained network outbox this frame committed (its rows, in order). */
  readonly runtimeOutputs: readonly NetworkOutput[]; readonly jOutbox: readonly JInput[]; readonly queuedRetries: readonly RuntimeTx[]; readonly rejected: readonly RuntimeError[] };
/** og EntityInput wire lanes (entity/types.ts): `entityTxs`, `proposedFrame` (with a commit notice's `hankos`), `hashPrecommitFrame` + `hashPrecommits`, `leaderTimeoutVote`, `jPrefixAttestations`. The rewrite's Runtime-clock stamps are not wire fields. */
const inputBinary = (input: EntityInput): Result<{ readonly [k: string]: Binary }, RuntimeError> => matchBy("kind", input, {
  txs: (i): Result<{ readonly [k: string]: Binary }, RuntimeError> => map(traverse(i.txs, entityFrameTx), (entityTxs) => ({ entityTxs: entityTxs as unknown as Binary })),
  proposal: (i): Result<{ readonly [k: string]: Binary }, RuntimeError> => map(frameBinary(i.frame, i.signatures, false), (f) => ({ proposedFrame: i.hankos === undefined ? f : { ...(f as { readonly [k: string]: Binary }), hankos: [...i.hankos] } })),
  precommit: (i): Result<{ readonly [k: string]: Binary }, RuntimeError> => map(frameNumber(i.height), (height) => ({ hashPrecommitFrame: { height, frameHash: i.frameHash }, hashPrecommits: precommitsBinary(i.signatures) })),
  leaderTimeoutVote: (i): Result<{ readonly [k: string]: Binary }, RuntimeError> => map(voteBinary(i.vote), (leaderTimeoutVote) => ({ leaderTimeoutVote })),
  jPrefixAttestations: (i): Result<{ readonly [k: string]: Binary }, RuntimeError> => ok({ jPrefixAttestations: jpBinary(i.attestations) }),
});
/**
 * og resolveObserverCertifiedAccountCounterpartyProposer: the counterparty's frame Hanko on the source Account's committed frame, verified with
 * the observer's certified board registry as board authority, names its proposer as the target claim's first member. Any Hanko refusal (og
 * HankoValidationError, e.g. after a board rotation) yields no route.
 */
const certifiedCounterpartySigner = (rt: Runtime, from: string, to: EntityId): string | undefined => {
  const source = [...rt.entities.values()].find((r) => lower(r.state.id) === lower(from));
  const account = source === undefined ? undefined : [...source.accountReplicas].find(([id]) => lower(id) === lower(to))?.[1];
  if (source === undefined || account === undefined || account.head._tag !== "installed") return undefined;
  const head = account.head, hanko = at(head.certificate.right, head.certificate.left, isLeft(source.state.id, replicaId(account)));
  if (!hanko) return undefined;
  const checked = checkAccountHanko(hanko, head.prevFrameHash, to, (entityId, boardHash) => { const rec = observerBoardRecord(source.state, entityId); return ok(rec.ok && rec.value !== null && rec.value.boardHash === boardHash); });
  const first = checked.ok ? checked.value.firstMember.toLowerCase() : "";
  return /^0x0{24}[0-9a-f]{40}$/.test(first) ? `0x${first.slice(-40)}` : undefined;
};
/**
 * og resolveEntityOutputSignerId for an Account message: the certified Account counterparty route first, then og resolveEntityProposerId: the
 * receiver's local active leader, else its first local replica, else a replayed frame's signer hint, else the verified gossip profile's runtime
 * signer (`routes`). None resolves:
 * og SIGNER_RESOLUTION_FAILED.
 */
const outputSigner = (rt: Runtime, o: Extract<EntityOutput, { readonly tx: EntityTx }>, routes?: RuntimeRoutes): Result<string, RuntimeError> => {
  const to = o.to, from = o.tx.type === "accountInput" ? o.tx.data.fromEntityId : undefined;
  const certified = from === undefined ? undefined : certifiedCounterpartySigner(rt, from, to);
  if (certified !== undefined) return ok(certified);
  const local = [...rt.entities.values()].filter((r) => lower(r.state.id) === lower(to));
  const chosen = local.find(isActiveLeader) ?? local[0], hinted = routes?.replaySigner?.(lower(to)) ?? routes?.verifiedProfileSigner(lower(to));
  const signer = chosen !== undefined ? lower(chosen.signerId) : hinted === undefined ? undefined : lower(hinted);
  return signer === undefined ? frameErr(`SIGNER_RESOLUTION_FAILED: Entity output ${from ?? "unknown"}->${to} entityId=${to}`) : ok(signer);
};
/** og RoutedEntityInput as prepareRuntimeOutputRows encodes it: destination, bound signer, the input's wire lane, the cohort marker. */
const outputBinary = (rt: Runtime, o: EntityOutput, routes?: RuntimeRoutes): Result<Binary, RuntimeError> => {
  const marker = o.atomicCrossJurisdictionPair === undefined ? {} : { atomicCrossJurisdictionPair: binaryOf(o.atomicCrossJurisdictionPair) };
  if ("input" in o) return map(inputBinary(o.input), (lane): Binary => ({ entityId: o.to, signerId: lower(o.signerId), ...lane, ...marker }));
  return chain(outputSigner(rt, o, routes), (signer) => map(entityFrameTx(o.tx), (tx): Binary => ({ entityId: o.to, signerId: signer, entityTxs: [tx as unknown as Binary], ...marker })));
};
/** og prepareRuntimeOutputRows: one encodeBuffer row per outbox output, in order. */
export const runtimeOutputRows = (rt: Runtime, outbox: readonly EntityOutput[], routes?: RuntimeRoutes): Result<readonly Uint8Array[], RuntimeError> => traverse(outbox, (o) => chain(outputBinary(rt, o, routes), encodeBinary));
// ---- og runtime/delivery (identity.ts, pending.ts, plan.ts, recovery-output.ts): the retained network outbox a Runtime frame commits ----
/** og RoutedEntityInput wire object as og keeps it in env.pendingNetworkOutputs and encodes it as one outbox row. */
export type NetworkOutput = { readonly [k: string]: Binary };
/** og LIMITS.MAX_PENDING_NETWORK_OUTPUTS. */
export const MAX_PENDING_NETWORK_OUTPUTS = 10_000;
type NetworkTx = { readonly type: string; readonly data: { readonly [k: string]: Binary } };
const netText = (v: Binary | undefined): string => (typeof v === "string" ? v : v === undefined || v === null ? "" : String(v));
const netTxs = (o: NetworkOutput): readonly NetworkTx[] => (Array.isArray(o["entityTxs"]) ? (o["entityTxs"] as unknown as readonly NetworkTx[]) : []);
const netMap = (v: Binary | undefined): ReadonlyMap<Binary, Binary> | undefined => (v instanceof Map ? (v as ReadonlyMap<Binary, Binary>) : undefined);
/** og netRuntimeId: a runtime id is a lowercased address; anything else is no id. */
const netRuntimeId = (v: Binary | undefined): string => { const s = netText(v).trim(); return /^0x[0-9a-fA-F]{40}$/.test(s) ? s.toLowerCase() : ""; };
const routeText = (v: Binary | undefined): string => netText(v).trim().toLowerCase();
/** og getEffectiveEntityInputTxs: a runtimeOutput's nested txs stand in for it. */
const netEffective = (o: NetworkOutput): readonly NetworkTx[] => netTxs(o).flatMap((tx) => (tx.type === "runtimeOutput" && Array.isArray(tx.data["entityTxs"]) ? (tx.data["entityTxs"] as unknown as readonly NetworkTx[]) : [tx]));
type NetProposal = { readonly from: string; readonly to: string; readonly height: Binary; readonly stateHash: string; readonly frameHanko: boolean; readonly disputeHanko: boolean; readonly ack: boolean };
/** og accountInputProposal / accountInputAck over an `accountInput` wire tx. */
const netProposal = (tx: NetworkTx): NetProposal | undefined => {
  const d = tx.data, proposal = d["proposal"] as { readonly [k: string]: Binary } | undefined;
  if (tx.type !== "accountInput" || d["kind"] !== "ack_frame" || proposal === undefined || proposal === null) return undefined;
  const frame = proposal["frame"] as { readonly [k: string]: Binary };
  return { from: netText(d["fromEntityId"]), to: netText(d["toEntityId"]), height: frame["height"] ?? null, stateHash: netText(frame["stateHash"]), frameHanko: Boolean(proposal["frameHanko"]), disputeHanko: Boolean(proposal["disputeHanko"]), ack: Boolean(d["ack"]) };
};
const netProposals = (o: NetworkOutput): readonly NetProposal[] | null => {
  const txs = netEffective(o);
  if (txs.length === 0) return null;
  const proposals = txs.flatMap((tx) => { const p = netProposal(tx); return p === undefined ? [] : [p]; });
  return proposals.length === txs.length ? proposals : null;
};
/** og accountProposalOutputIdentity. */
const proposalIdentity = (o: NetworkOutput): string | null => {
  const proposals = netProposals(o);
  if (proposals === null) return null;
  return `ap|${netRuntimeId(o["runtimeId"])}|${routeText(o["entityId"])}|${routeText(o["signerId"])}|${netRuntimeId(o["from"])}|`
    + proposals.map((p) => `${p.from.toLowerCase()}:${p.to.toLowerCase()}:${netText(p.height)}:${p.stateHash.toLowerCase()}`).join(",");
};
const netDigest = (v: Binary | undefined): Result<string, RuntimeError> => v === undefined ? ok("") : mapErr(map(encodeBinary(v), integrity), (): RuntimeError => ({ _tag: "runtime_frame", code: "ROUTE_OUTPUT_ENCODING_INVALID" }));
/**
 * og buildRouteOutputKey: an Account proposal by its identity, anything else by route, source frame and exact payload. og's tx fingerprints and
 * leader-vote body hash are replaced by equality-equivalent content digests (the key is compared, never stored).
 */
const routeKeyOf = (o: NetworkOutput): Result<string, RuntimeError> => {
  const identity = proposalIdentity(o);
  if (identity !== null) return ok(identity);
  const frame = o["sourceRuntimeFrame"] as { readonly height: Binary; readonly timestamp: Binary } | undefined, pf = o["proposedFrame"] as { readonly [k: string]: Binary } | undefined;
  const pc = o["hashPrecommitFrame"] as { readonly [k: string]: Binary } | undefined, vote = o["leaderTimeoutVote"] as { readonly [k: string]: Binary } | undefined;
  const voteBody = vote === undefined ? undefined : Object.fromEntries(Object.entries(vote).filter(([k]) => k !== "signature" && k !== "voterId")) as Binary;
  return chain(netDigest(voteBody), (voteHash) => chain(traverse(netTxs(o), (tx) => netDigest(tx as unknown as Binary)), (fingerprints) => ok(
    `ro|${netText(o["runtimeId"])}|${frame ? `${netText(frame.height)}:${netText(frame.timestamp)}` : ""}|${routeText(o["entityId"])}|${routeText(o["signerId"])}|${netText(o["from"])}`
    + `|${pf ? `${netText(pf["height"])}:${netText(pf["hash"])}` : ""}`
    + `|${pc ? `${netText(pc["height"])}:${netText(pc["frameHash"])}:${[...(netMap(o["hashPrecommits"])?.keys() ?? [])].map(netText).sort().join(",")}` : ""}`
    + `|${vote ? `${routeText(vote["voterId"])}:${voteHash}` : ""}`
    + `|${[...(netMap(o["jPrefixAttestations"])?.keys() ?? [])].map(netText).sort().join(",")}`
    + `|${fingerprints.join("\u0001")}`)));
};
/** og splitRoutedOutputByDeliveryLane: one output per payload kind (frame, precommit bundle, vote, each J-prefix attestation, the txs together). */
const splitLanes = (o: NetworkOutput): Result<readonly NetworkOutput[], RuntimeError> => {
  const { entityTxs, proposedFrame, hashPrecommits, hashPrecommitFrame, jPrefixAttestations, leaderTimeoutVote, ...route } = o;
  const split: NetworkOutput[] = [], precommits = netMap(hashPrecommits), jp = netMap(jPrefixAttestations), txs = Array.isArray(entityTxs) ? entityTxs : [];
  if (proposedFrame !== undefined) split.push({ ...route, proposedFrame });
  if (precommits !== undefined && precommits.size > 0) {
    if (hashPrecommitFrame === undefined) return frameErr("ROUTE_PRECOMMIT_FRAME_REFERENCE_MISSING");
    split.push({ ...route, hashPrecommitFrame, hashPrecommits: precommits });
  } else if (hashPrecommitFrame !== undefined) return frameErr("ROUTE_PRECOMMIT_FRAME_REFERENCE_WITHOUT_SIGNATURES");
  if (leaderTimeoutVote !== undefined) split.push({ ...route, leaderTimeoutVote });
  for (const [signer, attestation] of jp ?? new Map()) split.push({ ...route, jPrefixAttestations: new Map([[signer, attestation]]) });
  if (txs.length > 0 || split.length === 0) split.push({ ...route, entityTxs: txs });
  return ok(split);
};
const compareConsensus = (a: Binary, b: Binary): Result<number, RuntimeError> => {
  const x = encodeConsensus(a), y = encodeConsensus(b);
  return x.ok && y.ok ? ok(compareBytes(x.value, y.value)) : frameErr("ROUTE_OUTPUT_ENCODING_INVALID");
};
const proposalRank = (o: NetworkOutput): number => netEffective(o).reduce((rank, tx) => { const p = netProposal(tx); return p === undefined ? rank : rank + Number(p.frameHanko) + Number(p.disputeHanko); }, 0);
const isCommitNotice = (o: NetworkOutput): boolean => { const f = o["proposedFrame"] as { readonly [k: string]: Binary } | undefined; return Array.isArray(f?.["hankos"]) && (f["hankos"] as readonly Binary[]).length === 1; };
/** og mergeAccountProposalOutput: more Hanko evidence, else the newer source frame, else the canonically smaller envelope. */
const mergeProposal = (existing: NetworkOutput, incoming: NetworkOutput): Result<NetworkOutput, RuntimeError> => {
  const delta = proposalRank(incoming) - proposalRank(existing);
  if (delta !== 0) return ok(delta > 0 ? incoming : existing);
  const ef = existing["sourceRuntimeFrame"] as SourceRuntimeFrame | undefined, inf = incoming["sourceRuntimeFrame"] as SourceRuntimeFrame | undefined;
  if (inf !== undefined && (ef === undefined || inf.height > ef.height || (inf.height === ef.height && inf.timestamp > ef.timestamp))) return ok(incoming);
  return map(compareConsensus(existing, incoming), (c) => (c <= 0 ? existing : incoming));
};
/** og normalizePrecommitBundles. */
const precommitBundles = (m: ReadonlyMap<Binary, Binary>): Result<Map<string, readonly Binary[]>, RuntimeError> => {
  const out = new Map<string, readonly Binary[]>();
  for (const [raw, sigs] of m) {
    const signer = routeText(raw);
    if (out.has(signer)) return frameErr(`ROUTE_PRECOMMIT_DUPLICATE_SIGNER:${netText(raw)}`);
    out.set(signer, Array.isArray(sigs) ? sigs : []);
  }
  return ok(out);
};
/** og mergeOrdinaryOutput: vote equivocation refuses; txs append; precommit bundles union (equivocation refuses); a commit notice replaces a bare frame. */
const mergeOrdinary = (existing: NetworkOutput, incoming: NetworkOutput): Result<NetworkOutput, RuntimeError> => {
  const ev = existing["leaderTimeoutVote"], iv = incoming["leaderTimeoutVote"];
  const voteConflict: Result<boolean, RuntimeError> = ev === undefined && iv === undefined ? ok(false) : ev === undefined || iv === undefined ? ok(true) : map(compareConsensus(iv, ev), (c) => c !== 0);
  return chain(voteConflict, (conflict) => {
    if (conflict) return frameErr(`ROUTE_LEADER_VOTE_EQUIVOCATION:${netText((iv as { readonly [k: string]: Binary } | undefined)?.["voterId"]) || "missing"}`);
    let next: { [k: string]: Binary } = { ...existing };
    const itxs = netTxs(incoming);
    if (itxs.length > 0) next = { ...next, entityTxs: [...netTxs(existing), ...itxs] as unknown as Binary };
    const ipc = netMap(incoming["hashPrecommits"]);
    const precommitStep: Result<{ [k: string]: Binary }, RuntimeError> = ipc === undefined || ipc.size === 0 ? ok(next) : chain(
      existing["hashPrecommitFrame"] !== undefined ? map(compareConsensus(existing["hashPrecommitFrame"], incoming["hashPrecommitFrame"] ?? null), (c) => c !== 0) : ok(false), (clash) => {
        if (clash) return frameErr("ROUTE_PRECOMMIT_FRAME_CONFLICT");
        if (incoming["hashPrecommitFrame"] === undefined) return frameErr("ROUTE_PRECOMMIT_FRAME_REFERENCE_MISSING");
        return chain(precommitBundles(netMap(existing["hashPrecommits"]) ?? new Map()), (merged) => chain(precommitBundles(ipc), (adds) => {
          for (const [signer, sigs] of adds) {
            const prev = merged.get(signer);
            if (prev === undefined) merged.set(signer, [...sigs]);
            else if (prev.length !== sigs.length || prev.some((s, i) => s !== sigs[i])) return frameErr(`ROUTE_PRECOMMIT_EQUIVOCATION:${signer}`);
          }
          return ok({ ...next, hashPrecommitFrame: incoming["hashPrecommitFrame"] as Binary, hashPrecommits: new Map([...merged].sort(([a], [b]) => (a < b ? -1 : a > b ? 1 : 0))) as unknown as Binary });
        }));
      });
    return map(precommitStep, (n) => (incoming["proposedFrame"] !== undefined && (n["proposedFrame"] === undefined || (isCommitNotice(incoming) && !isCommitNotice(n))) ? { ...n, proposedFrame: incoming["proposedFrame"] } : n));
  });
};
/** og buildPendingNetworkOutputs / dedupeEntityOutputs: split lanes, merge by route key into the first slot. */
const dedupeNetwork = (outputs: readonly NetworkOutput[]): Result<readonly NetworkOutput[], RuntimeError> =>
  chain(traverse(outputs, splitLanes), (lanes) => {
    const slots = new Map<string, NetworkOutput>();
    for (const o of lanes.flat()) {
      const key = routeKeyOf(o);
      if (!key.ok) return key;
      const existing = slots.get(key.value);
      if (existing === undefined) { slots.set(key.value, o); continue; }
      const merged = proposalIdentity(existing) !== null && proposalIdentity(existing) === proposalIdentity(o) ? mergeProposal(existing, o) : mergeOrdinary(existing, o);
      if (!merged.ok) return merged;
      slots.set(key.value, merged.value);
    }
    return ok([...slots.values()]);
  });
const pendingNetwork = (outputs: readonly NetworkOutput[]): Result<readonly NetworkOutput[], RuntimeError> => chain(dedupeNetwork(outputs), (pending) =>
  pending.length > MAX_PENDING_NETWORK_OUTPUTS ? frameErr(`NETWORK_OUTBOX_CAPACITY_EXCEEDED: pending=${pending.length} max=${MAX_PENDING_NETWORK_OUTPUTS}`) : ok(pending));
/**
 * og accountProposalSettledBySender / pruneSettledOutputs: a proposal-only output is settled once the sender's Account no longer holds that frame
 * pending (committed or rolled back); an output that also carries an ACK is still owed.
 */
const pruneSettled = (rt: Runtime, outputs: readonly NetworkOutput[]): Result<readonly NetworkOutput[], RuntimeError> => {
  const kept: NetworkOutput[] = [];
  for (const o of outputs) {
    const proposals = netEffective(o).flatMap((tx) => { const p = netProposal(tx); return p === undefined ? [] : [p]; });
    if (proposals.length === 0 || proposals.some((p) => p.ack)) { kept.push(o); continue; }
    let settled = true;
    for (const p of proposals) {
      const account = [...rt.entities.values()].filter((r) => lower(r.state.id) === lower(p.from)).map((r) => [...r.accountReplicas].find(([id]) => lower(id) === lower(p.to))?.[1]).find((a) => a !== undefined);
      if (account === undefined) return frameErr(`ACCOUNT_PROPOSAL_OUTBOX_SOURCE_ACCOUNT_MISSING:${stableJson({ runtimeId: rt.runtimeId ?? null, fromEntityId: p.from, toEntityId: p.to, proposalHeight: p.height, proposalStateHash: p.stateHash })}`);
      const pending = account._tag === "proposed" ? account.candidate.frame : undefined;
      // og `every`: the first proposal still pending keeps the output, and later legs are not inspected
      if (pending !== undefined && netText(p.height) === String(pending.height) && pending.stateHash.toLowerCase() === p.stateHash.toLowerCase()) { settled = false; break; }
    }
    if (!settled) kept.push(o);
  }
  return ok(kept);
};
const isTriggerOnly = (o: NetworkOutput): boolean => netTxs(o).length === 0 && o["proposedFrame"] === undefined && o["leaderTimeoutVote"] === undefined && (netMap(o["jPrefixAttestations"])?.size ?? 0) === 0 && (netMap(o["hashPrecommits"])?.size ?? 0) === 0;
const txTypesOf = (o: NetworkOutput): string => netTxs(o).map((tx) => tx.type).join(",");
/** og extensions/cross-j/boundary.ts CROSS_J_INTRA_RUNTIME_ENTITY_TX_TYPES: raw cross-j Entity effects never leave their Runtime. */
const CROSS_J_INTRA_RUNTIME_TXS = new Set(["prepareCrossJurisdictionSwap", "materializeCrossJurisdictionSwap", "registerCrossJurisdictionSwap", "crossJurisdictionFillNotice", "requestCrossJurisdictionClear", "materializeCrossJurisdictionClear",
  "crossPullClose", "crossJurisdictionSalvage", "crossJurisdictionForceSiblingDispute", "orderbookSweepCrossJurisdiction", "admitCrossJurisdictionBookOrder", "removeCrossJurisdictionBookOrder", "crossJurisdictionBookOrderRemoved"]);
type NetworkPlan = { readonly local: readonly NetworkOutput[]; readonly remote: readonly NetworkOutput[] };
/**
 * og planEntityOutputs: a target with a local replica of the named signer is local (a trigger retargets to the sole local signer; anything else
 * refuses ROUTE_LOCAL_SIGNER_MISMATCH); every other output is remote: its signer aligned with the verified profile, its Runtime bound (replay
 * route, else the verified route, else the persisted or resolved one), with og's unknown-route, self-hint and cross-j refusals.
 */
const planNetwork = (rt: Runtime, outputs: readonly NetworkOutput[], routes: RuntimeRoutes | undefined): Result<NetworkPlan, RuntimeError> => chain(dedupeNetwork(outputs), (deduped) => {
  const local: NetworkOutput[] = [], remote: NetworkOutput[] = [];
  for (const initial of deduped) {
    const entity = routeText(initial["entityId"]), signer = routeText(initial["signerId"]);
    const signers = [...rt.entities.values()].filter((r) => lower(r.state.id) === entity).map((r) => lower(r.signerId));
    if (signer !== "" && signers.includes(signer)) { local.push(initial); continue; }
    if (signers.length === 1) {
      if (isTriggerOnly(initial)) { local.push({ ...initial, signerId: signers[0]! }); continue; }
      return frameErr(netTxs(initial).length > 0 ? `ROUTE_LOCAL_SIGNER_MISMATCH: entity=${netText(initial["entityId"])} signer=${netText(initial["signerId"])} txTypes=${txTypesOf(initial)}`
        : `ROUTE_LOCAL_SIGNER_MISMATCH: entity=${netText(initial["entityId"])} signer=${netText(initial["signerId"])} resolved=${signers[0]} consensusOnly=true`);
    }
    // og alignRemoteOutputSigner
    const preferred = routes?.verifiedProfileSigner(entity) ?? "", outSigner = netText(initial["signerId"]).trim();
    let output = initial;
    if (preferred !== "" && preferred.toLowerCase() !== outSigner.toLowerCase()) {
      if (isTriggerOnly(initial)) output = { ...initial, signerId: preferred };
      else if (netTxs(initial).length > 0) return frameErr(`ROUTE_REMOTE_SIGNER_MISMATCH: entity=${netText(initial["entityId"])} signer=${netText(initial["signerId"])} resolved=${preferred} txTypes=${txTypesOf(initial)}`);
    }
    // og bindVerifiedTargetRuntime
    const outSignerId = netText(output["signerId"]), persisted = netRuntimeId(output["runtimeId"]), replay = netRuntimeId(routes?.replayRuntime?.(entity, routeText(outSignerId)));
    let target: string;
    if (replay !== "") {
      if (persisted !== "" && persisted !== replay) return frameErr(`REPLAY_OUTPUT_RUNTIME_ROUTE_MISMATCH:${netText(output["entityId"])}:${outSignerId}`);
      target = replay;
    } else {
      const resolved = routes?.resolvedRuntime?.(entity) ?? "", verified = netRuntimeId(routes?.verifiedRuntime?.(entity));
      if (verified !== "" && persisted !== verified) output = { ...output, runtimeId: verified };
      target = netRuntimeId(output["runtimeId"]) || verified || resolved;
    }
    if (target === "") return frameErr(`ROUTE_TARGET_RUNTIME_UNKNOWN: entity=${netText(output["entityId"])} txTypes=${txTypesOf(output)}`);
    if (netRuntimeId(rt.runtimeId) !== "" && target === netRuntimeId(rt.runtimeId)) return frameErr(`ROUTE_STALE_SELF_HINT: entity=${netText(output["entityId"])} runtime=${target} txTypes=${txTypesOf(output)}`);
    const txs = netTxs(output);
    if (txs.length === 1 && txs[0]!.type === "runtimeOutput") {
      const bound = netRuntimeId(replay !== "" ? replay : routes?.crossJRuntime?.(entity, routeText(outSignerId)));
      if (bound === "" || bound !== target) return frameErr(`CROSS_J_RUNTIME_OUTPUT_TARGET_UNVERIFIED:${netText(output["entityId"])}:${outSignerId}:${target || "missing"}`);
    } else if (txs.some((tx) => tx.type === "runtimeOutput" && tx.data["protocol"] === "cross-j") || netEffective(output).some((tx) => CROSS_J_INTRA_RUNTIME_TXS.has(tx.type))) {
      return frameErr(`CROSS_J_REMOTE_OUTPUT_FORBIDDEN: entity=${routeText(output["entityId"])} targetRuntime=${target} txTypes=${txTypesOf(output)}`);
    }
    remote.push({ ...output, runtimeId: target });
  }
  return ok({ local, remote });
});
/**
 * og applyRecoveryRuntimeOutputPlan after a frame: this frame's outputs are stamped with their source frame, joined to the retained outbox, pruned
 * of settled proposals, deduplicated and planned; the local outputs are this Runtime's own continuations and the remote ones become the new
 * retained outbox, which is exactly what og commits as the frame's runtime outputs.
 */
export const retainedNetworkOutbox = (rt: Runtime, outbox: readonly EntityOutput[], routes?: RuntimeRoutes): Result<readonly NetworkOutput[], RuntimeError> =>
  chain(traverse(outbox, (o) => outputBinary(rt, o, routes)), (rows) => networkOutboxStep(rt, rows as readonly NetworkOutput[], routes));
/** og applyRecoveryRuntimeOutputPlan over og-wire outputs: stamp, join the retained outbox, prune, dedupe, plan; the result is the new retained outbox. */
export const networkOutboxStep = (rt: Runtime, outputs: readonly NetworkOutput[], routes?: RuntimeRoutes): Result<readonly NetworkOutput[], RuntimeError> =>
  chain(frameNumber(rt.height), (height) => chain(frameNumber(rt.timestamp), (timestamp) => {
    const originated = outputs.map((o) => (o["sourceRuntimeFrame"] ? o : { ...o, sourceRuntimeFrame: { height, timestamp } }));
    return chain(chain(pruneSettled(rt, [...(rt.pendingNetworkOutputs ?? []), ...originated]), pendingNetwork), (pending) => chain(planNetwork(rt, pending, routes), (plan) => pendingNetwork(plan.remote)));
  }));
/** og dispatchEntityOutputs retirement: outputs a transport accepted leave the retained outbox (by route key); the rest stay owed. */
export const retireNetworkOutputs = (rt: Runtime, accepted: (output: NetworkOutput) => boolean): Runtime => ({ ...rt, pendingNetworkOutputs: (rt.pendingNetworkOutputs ?? []).filter((o) => !accepted(o)) });
/**
 * og selectRetainedRecoveryOutbox: a frame's recorded rows from earlier frames must each be exact prior retained evidence (runtimeId aside), in
 * prior order; this frame's own rows are regenerated by replay.
 */
export const selectRetainedRecovery = (previous: readonly NetworkOutput[], recorded: readonly NetworkOutput[], height: number): Result<readonly NetworkOutput[], RuntimeError> => {
  const evidence = (o: NetworkOutput): NetworkOutput => ({ ...o, runtimeId: "" });
  const prior = new Map<string, { readonly output: NetworkOutput; readonly index: number }>();
  for (const [index, output] of previous.entries()) { const key = routeKeyOf(evidence(output)); if (!key.ok) return key; prior.set(key.value, { output, index }); }
  const retained: NetworkOutput[] = [];
  let priorIndex = -1;
  for (const output of recorded) {
    const source = output["sourceRuntimeFrame"] as SourceRuntimeFrame | undefined;
    if (source === undefined || source.height > height || !output["runtimeId"]) return frameErr(`RECOVERY_OUTBOX_SOURCE_FRAME_INVALID:height=${height}`);
    if (source.height === height) continue;
    const key = routeKeyOf(evidence(output));
    if (!key.ok) return key;
    const verified = prior.get(key.value), same = verified === undefined ? ok(1) : compareConsensus(evidence(verified.output), evidence(output));
    if (!same.ok) return same;
    if (verified === undefined || same.value !== 0) return frameErr(`RECOVERY_OUTBOX_RETAINED_OUTPUT_UNPROVEN:height=${height}`);
    if (verified.index <= priorIndex) return frameErr(`RECOVERY_OUTBOX_RETAINED_ORDER_INVALID:height=${height}`);
    priorIndex = verified.index;
    retained.push(verified.output["runtimeId"] === output["runtimeId"] ? verified.output : { ...verified.output, runtimeId: output["runtimeId"] as Binary });
  }
  return ok(retained);
};
/** og collectOutputSignerHints: each Account-bearing row of a replayed frame names its Entity's signer; two signers for one Entity conflict. */
export const replaySignerHints = (rows: readonly NetworkOutput[], height: number): Result<ReadonlyMap<string, string>, RuntimeError> => {
  const hints = new Map<string, string>();
  for (const o of rows) {
    if (!netTxs(o).some((tx) => tx.type === "accountInput")) continue;
    const entity = routeText(o["entityId"]), signer = routeText(o["signerId"]);
    if (entity === "" || signer === "") return frameErr(`RECOVERY_OUTPUT_SIGNER_HINT_INVALID:height=${height}`);
    const existing = hints.get(entity);
    if (existing !== undefined && existing !== signer) return frameErr(`RECOVERY_OUTPUT_SIGNER_HINT_CONFLICT:height=${height}:entity=${entity}:left=${existing}:right=${signer}`);
    hints.set(entity, signer);
  }
  return ok(hints);
};
/** og installReplayOutputRuntimeRoutes: a replayed frame's own rows bind each (Entity, signer) to its committed Runtime. */
export const replayOutputRoutes = (rows: readonly NetworkOutput[]): Result<ReadonlyMap<string, string>, RuntimeError> => {
  const routes = new Map<string, string>();
  for (const o of rows) {
    const key = `${routeText(o["entityId"])}:${routeText(o["signerId"])}`, runtimeId = netRuntimeId(o["runtimeId"]);
    if (key === ":" || runtimeId === "") return frameErr("REPLAY_OUTPUT_RUNTIME_ROUTE_INVALID");
    const existing = routes.get(key);
    if (existing !== undefined && existing !== runtimeId) return frameErr(`REPLAY_OUTPUT_RUNTIME_ROUTE_CONFLICT:${key}`);
    routes.set(key, runtimeId);
  }
  return ok(routes);
};
const sealFrame = (rt: Runtime, step: RuntimeStep, routes?: RuntimeRoutes): Result<RuntimeFrameCommit, RuntimeError> => {
  const after = step.runtime;
  return chain(frameNumber(after.height), (height) => chain(frameNumber(after.timestamp), (timestamp) => chain(retainedNetworkOutbox(after, step.outbox, routes), (retained) => chain(traverse(retained, (o) => mapErr(encodeBinary(o), (): RuntimeError => ({ _tag: "runtime_frame", code: "ROUTE_OUTPUT_ENCODING_INVALID" }))), (rows) => chain(runtimeOutputsDigest(rows), (outputsDigest) =>
    chain(replicaMetaRows(after), (metaRows) => chain(replicaMetaDigest(metaRows), (metaDigest) => chain(runtimeComponentDigests(runtimeView(after)), (components) =>
      chain(storagePostStateHash({ height, timestamp, replicaMetaDigest: metaDigest, runtimeComponentDigests: components, runtimeOutputCount: rows.length, runtimeOutputsDigest: outputsDigest }), (postStateHash) =>
        chain(canonicalEntityHashes(after), (entityHashes) => {
          const touched = [...new Set([...step.applied.entityInputs.map((i) => lower(i.entityId)), ...step.applied.runtimeTxs.flatMap((tx) => (tx.type === "importReplica" ? [lower(tx.entityId)] : []))])].sort(asc);
          const body: StorageFrame = {
            height, timestamp, prevFrameHash: rt.frameHash, replicaMetaDigest: metaDigest, postStateHash, materializedState: false,
            canonicalStateHash: canonicalRuntimeStateHash(height, timestamp, entityHashes), canonicalEntityHashes: entityHashes,
            runtimeInput: binaryOf(step.applied), runtimeOutputCount: rows.length, runtimeOutputsDigest: outputsDigest, touchedEntities: touched, touchedAccounts: [], touchedBookEntities: [],
          };
          return map(storageFrameHash(body), (frameHash) => ({ runtime: { ...after, frameHash, pendingNetworkOutputs: retained }, frame: { ...body, frameHash }, applied: step.applied, outbox: step.outbox, runtimeOutputs: retained, jOutbox: step.jOutbox, queuedRetries: step.queuedRetries, rejected: step.rejected }));
        }))))))))));
};
/**
 * og process + saveRuntimeFrame: apply one Runtime input and, when the frame advanced, seal its WAL row (prev hash chain from ZERO_FRAME_HASH,
 * canonical state hash, post-state oracle, ordered outbox digest). A frame that did no work writes no row.
 */
export const commitRuntimeFrame = (rt: Runtime, input: RuntimeInput, ctx: RuntimeCtx): Result<RuntimeFrameCommit | null, RuntimeError> =>
  chain(applyRuntime(rt, input, ctx), (step) => (step.advanced ? sealFrame(rt, step, ctx.routes) : ok(null)));
export type RuntimeRecovery = { readonly runtime: Runtime; readonly outbox: readonly EntityOutput[] };
/**
 * og verifyStorageTailIntegrity + replay: every row continues the chain (height+1, prevFrameHash), its canonical state hash recomputes from its own
 * coordinates, and its frame hash recomputes; replaying its applied input (with replay capabilities) must reproduce the row byte-for-byte.
 * The recovered outbox is every replayed frame's ordered outputs (nothing is terminal without a receipt) and must equal the persisted rows positionally.
 * `rows`, when given, are each frame's persisted runtime outputs (og frame.runtimeOutputs): og replayOneFrame seeds the retained outbox from
 * the prior evidence they select (og selectRetainedRecoveryOutbox, so transport retirements between frames replay) and binds their Runtime routes.
 */
export const recoverRuntime = (checkpoint: Runtime, frames: readonly StorageFrame[], inputs: readonly RuntimeInput[], outbox: readonly EntityOutput[], ctx: Verifiers & Pick<RuntimeCtx, "routes">, rows?: readonly (readonly NetworkOutput[])[]): Result<RuntimeRecovery, RuntimeError> => {
  if (frames.length !== inputs.length) return frameErr("STORAGE_VERIFY_FRAME_INPUT_MISSING");
  if (rows !== undefined && rows.length !== frames.length) return frameErr("STORAGE_VERIFY_FRAME_OUTPUTS_MISSING");
  type Replayed = { readonly runtime: Runtime; readonly outbox: readonly EntityOutput[] };
  return chain(foldResult(frames.map((f, i) => [f, inputs[i] as RuntimeInput, i] as const), { runtime: checkpoint, outbox: [] } as Replayed, ({ runtime, outbox: pending }, [frame, input, index]): Result<Replayed, RuntimeError> => {
    if (BigInt(frame.height) !== runtime.height + 1n) return frameErr("STORAGE_VERIFY_FRAME_HEIGHT_MISMATCH");
    if (frame.prevFrameHash !== runtime.frameHash) return frameErr("STORAGE_VERIFY_FRAME_CHAIN_BROKEN");
    if (frame.canonicalStateHash !== undefined && frame.canonicalStateHash !== canonicalRuntimeStateHash(frame.height, frame.timestamp, frame.canonicalEntityHashes ?? [])) return frameErr("STORAGE_VERIFY_CANONICAL_HASH_MISMATCH");
    const own = storageFrameHash(frame);
    if (!own.ok || own.value !== frame.frameHash) return frameErr("STORAGE_VERIFY_FRAME_HASH_MISMATCH");
    const recorded = rows?.[index];
    const seeded: Result<{ readonly runtime: Runtime; readonly routes: RuntimeRoutes | undefined }, RuntimeError> = recorded === undefined ? ok({ runtime, routes: ctx.routes })
      : chain(selectRetainedRecovery(runtime.pendingNetworkOutputs ?? [], recorded, frame.height), (retained) => chain(replaySignerHints(recorded, frame.height), (hints) => map(replayOutputRoutes(recorded), (bound): { readonly runtime: Runtime; readonly routes: RuntimeRoutes } => ({
        runtime: { ...runtime, pendingNetworkOutputs: retained },
        routes: { verifiedProfileSigner: (e) => ctx.routes?.verifiedProfileSigner(e), ...ctx.routes, replayRuntime: (e, sgn) => bound.get(`${lower(e)}:${lower(sgn)}`), replaySigner: (e) => hints.get(lower(e)) },
      }))));
    return chain(seeded, (seed) => chain(commitRuntimeFrame(seed.runtime, { ...input, timestamp: BigInt(frame.timestamp) }, { ...ctx, routes: seed.routes, replay: true }), (replayed): Result<Replayed, RuntimeError> =>
      replayed === null || replayed.frame.frameHash !== frame.frameHash ? frameErr("STORAGE_REPLAY_POST_STATE_MISMATCH")
        : ok({ runtime: recorded === undefined ? replayed.runtime : { ...replayed.runtime, pendingNetworkOutputs: recorded }, outbox: [...pending, ...replayed.outbox] })));
  }), (done) => (canon(done.outbox) !== canon(outbox) ? frameErr("STORAGE_RECOVERY_OUTBOX_MISMATCH") : ok(done)));
};


/** og entity j-batch txs (r2r / r2c / r2e / e2r queue into jBatchState; j_broadcast seals it) and the finalized J events that move reserves (entity-runtime ER-16). */
export type JOp =
  | { readonly type: "r2r"; readonly toEntity: EntityId; readonly tokenId: TokenId; readonly amount: bigint }
  | { readonly type: "r2c"; readonly counterparty: EntityId; readonly tokenId: TokenId; readonly amount: bigint; readonly receivingEntity?: EntityId | undefined; readonly rebalanceFee?: RebalanceFee | undefined }
  | { readonly type: "et2r"; readonly tokenAddress: string; readonly amount: bigint; readonly internalTokenId: TokenId }
  | { readonly type: "r2et"; readonly recipient: EntityId; readonly tokenId: TokenId; readonly amount: bigint }
  | { readonly type: "j_broadcast"; readonly chainId: number; readonly depository: string; readonly signerId: string }
  /** og j_rebroadcast / j_abort_sent_batch / j_clear_batch / mintReserves (entity/tx/handlers/j-batch): the J submit lifecycle. */
  | { readonly type: "j_rebroadcast"; readonly chainId: number; readonly depository: string; readonly signerId: string; readonly gasBumpBps?: number | undefined }
  | { readonly type: "j_abort_sent_batch"; readonly reason?: string | undefined; readonly requeueToCurrent?: boolean | undefined }
  | { readonly type: "j_clear_batch"; readonly reason?: string | undefined }
  | { readonly type: "mintReserves"; readonly tokenId: number; readonly amount: bigint }
  | { readonly type: "j_event"; readonly blockNumber: number; readonly event: JEvent };
/** og EntityState reserves / outDebtsByToken / inDebtsByToken / jBatchState: reserves change only through finalized J events. */
export type JState = { readonly reserves: ReadonlyMap<number, bigint>; readonly debts: DebtLedger; readonly jBatch?: JBatchState | undefined };
export type LadderTx = { readonly type: "ladder_reveal"; readonly revealer: EntityId; readonly counter: EntityId; readonly ladderHash: Hash; readonly targetRole: boolean; readonly fillRatio: number; readonly revealedAt: bigint };
export type EntityRouteTx =
  | { readonly type: "directPayment"; readonly recipient: EntityId; readonly tokenId: TokenId; readonly amount: bigint; readonly description?: string | undefined; readonly invoiceId?: string | undefined }
  | ({ readonly type: "placeSwapOffer" } & SwapOfferTerms);
export type HostInput = { readonly kind: "dispute" };
export type HostCtx = { readonly timestamp: bigint; readonly jHeight: bigint; readonly from?: EntityId | undefined };
/** `j_submit`: og jOutputs `{jurisdictionName, jTxs:[jTx]}` plus the jBatch hash the Entity quorum signs; `j_broadcast_request`: og finalizePendingBatch's self input `{entityTxs:[{type:"j_broadcast"}]}`. */
export type HostEffect = Effect | Tagged<"start_dispute", { start: DisputeStart }> | Tagged<"send", { message: AccountPeerInput }>
  | Tagged<"j_submit", { jTx: JBatchTx | JMintTx; hashToSign?: JBatchHashToSign | undefined }> | Tagged<"j_broadcast_request", { entityId: string }>;
export type OutboxEntry = { readonly id: Hash; readonly effect: HostEffect };
export type HostTx =
  | { readonly layer: "account"; readonly tx: WireAccountTx } | { readonly layer: "frame"; readonly input: AccountInput } | { readonly layer: "j"; readonly tx: JOp }
  | { readonly layer: "ladder"; readonly tx: LadderTx } | { readonly layer: "entity"; readonly tx: EntityRouteTx } | { readonly layer: "input"; readonly input: HostInput } | { readonly layer: "receipt"; readonly id: Hash };
/** `deltaTransformer`: og requireAccountDeltaTransformerAddress for the Host's Account (its Runtime's durable jurisdiction stack; never committed). */
export type Host = { readonly self: EntityId; readonly account: AccountReplica; readonly j: JState; readonly ladder: ReadonlyMap<string, RatioRecord>; readonly height: bigint; readonly frameHash: RuntimeFrameHash; readonly outbox: readonly OutboxEntry[]; readonly deltaTransformer?: DeltaTransformerRef | undefined };
export type Stamped = { readonly tx: HostTx; readonly ctx: HostCtx };
export type HostError = BodyError | Tagged<"unsigned" | "chain" | "root" | "version" | "reserve" | "status" | "recipient"> | Tagged<"candidate", { cause: AccountReplicaError }> | JBatchError | JObserveError;
export type HostStep = Step<Host, HostEffect>;
export const genesisHost = (self: EntityId, account: AccountReplica): Result<Host, AccountReplicaError> =>
  map(partyOf(replicaId(account), self), () => ({ self, account, j: { reserves: new Map(), debts: EMPTY_DEBTS }, ladder: new Map(), height: 0n, frameHash: ZERO_HASH as RuntimeFrameHash, outbox: [] }));
const jEntityOf = (j: JState, self: EntityId, peer: string): JEntity => ({ entityId: self, reserves: j.reserves, debts: j.debts, jBatch: j.jBatch, accounts: new Set([peer]) });
/** `release`: the Account latches a J recovery tx frees (og applyEntityAccountEnvelopeUpdate setRebalanceSubmittedAt / replaceDisputeLifecycle). */
export type JApplied = { readonly j: JState; readonly effects: readonly HostEffect[]; readonly release?: JLatchRelease | undefined };
/** og handleR2R / handleR2C / handleR2E / handleE2R / handleJBroadcast / handleJRebroadcast / handleJAbortSentBatch / handleJClearBatch / handleMintReserves and the Entity's reserve, debt and HankoBatchProcessed J-event handlers, for the Host's one Account. */
export const applyJ = (j: JState, op: JOp, self: EntityId, peer: string, ctx: HostCtx, account?: AccountBody): Result<JApplied, HostError> => {
  const e = jEntityOf(j, self, peer), just = (next: JState): JApplied => ({ j: next, effects: [] }), queued = (r: Result<JBatchState, JBatchError>): Result<JApplied, HostError> => map(r, (jBatch) => just({ ...j, jBatch }));
  const submit = (b: Broadcast): JApplied => ({ j: { ...j, jBatch: b.jBatch }, effects: b.jTx === undefined ? [] : [{ _tag: "j_submit", jTx: b.jTx, ...opt("hashToSign", b.hashToSign) }] });
  const recovered = (r: JRecovered): JApplied => ({ j: { ...j, jBatch: r.jBatch }, effects: [], release: r.release });
  const jNonceOf = (counterparty: string): number => (account !== undefined && counterparty === peer.toLowerCase() ? account.jNonce : 0);
  return matchBy("type", op, {
    r2r: (x) => queued(queueR2R(e, x.toEntity, Number(x.tokenId), x.amount)),
    r2c: (x) => map(queueR2C(e, x.counterparty, Number(x.tokenId), x.amount, x.receivingEntity, x.rebalanceFee), (q) => just(q.note === undefined ? { ...j, jBatch: q.jBatch } : j)),
    r2et: (x) => queued(queueR2E(e, x.recipient, Number(x.tokenId), x.amount)),
    et2r: (x) => queued(queueE2R(e, { contractAddress: x.tokenAddress, amount: x.amount, internalTokenId: Number(x.internalTokenId) })),
    j_broadcast: (x) => map(jBroadcast(j.jBatch, { entityId: self, chainId: x.chainId, depository: x.depository, signerId: x.signerId, timestamp: Number(ctx.timestamp) }), submit),
    j_rebroadcast: (x) => map(jRebroadcast(j.jBatch, { entityId: self, chainId: x.chainId, depository: x.depository, signerId: x.signerId, timestamp: Number(ctx.timestamp), gasBumpBps: x.gasBumpBps }), submit),
    j_abort_sent_batch: (x) => ok(recovered(jAbortSentBatch(j.jBatch, x, jNonceOf))),
    j_clear_batch: (x) => ok(recovered(jClearBatch(j.jBatch, x, account === undefined ? new Map() : new Map([[peer.toLowerCase(), [...(account.submittedAt ?? new Map<number, number>()).keys()]]])))),
    mintReserves: (x) => ok({ j, effects: [{ _tag: "j_submit", jTx: mintReservesTx(self, x.tokenId, x.amount, Number(ctx.timestamp)).jTx }] }),
    j_event: (x) => x.event.type === "HankoBatchProcessed"
      ? map(applyHankoBatchProcessed(j.jBatch, self, x.event, Number(ctx.timestamp)), (b): JApplied => ({ j: { ...j, jBatch: b.jBatch }, effects: b.autoBroadcast ? [{ _tag: "j_broadcast_request", entityId: self }] : [] }))
      : map(observeJBlocks({ entityId: self, reserves: j.reserves, debts: j.debts, accounts: new Map() }, [{ blockNumber: x.blockNumber, events: [x.event] }]), (o) => just({ ...j, reserves: o.observer.reserves, debts: o.observer.debts })),
  });
};
/** og releaseR2CSubmittedLatches / clear's submitted-marker reset on the Host's one Account (finalize latches: the rewrite never sets finalizeQueued). */
const releaseLatches = (account: AccountReplica, peer: string, release: JLatchRelease | undefined): AccountReplica => {
  const mine = (release?.submitted ?? []).filter((r) => r.accountId === peer.toLowerCase());
  return mine.length === 0 ? account : ({ ...account, state: mine.reduce((b, r) => setRebalanceSubmittedAt(b, r.tokenId, undefined), account.state) } as AccountReplica);
};
const ladderKey = (tx: LadderTx): string => `${tx.revealer}|${tx.counter}|${tx.ladderHash}|${tx.targetRole ? "t" : "s"}`;
const admitTx = (host: Host, tx: WireAccountTx, ctx: HostCtx, verify: Verify): Result<HostStep, AccountReplicaError> => map(admitAt(host.account, [tx], host.self, { timestamp: ctx.timestamp, jHeight: ctx.jHeight }, verify), (account) => step({ ...host, account }));
const accountStep = (host: Host, applied: Result<AccountApply, AccountReplicaError>): Result<HostStep, AccountReplicaError> => map(applied, (a) => {
  const send = (message: AccountMessage): HostEffect => ({ _tag: "send", message });
  return step({ ...host, account: a.replica }, a.outputs.flatMap((o): HostEffect[] => (o.kind === "message" ? [] : [matchBy("kind", o, { effect: (e) => e.effect, ack: send, ack_frame: send, start_dispute: ({ start }) => ({ _tag: "start_dispute", start }) })])));
});
const routeEntity = (tx: EntityRouteTx, self: EntityId, id: AccountId): Result<AccountTx, HostError> => matchBy("type", tx, {
  directPayment: (x) => {
    const party = partyOf(id, self);
    return !party.ok || x.recipient !== party.value.peer ? err({ _tag: "recipient" }) : ok({ type: "payment", tokenId: x.tokenId, amount: x.amount });
  },
  placeSwapOffer: ({ type: _, ...offer }) => ok({ type: "swap_offer", ...offer }),
  // og htlcPayment and the cross-j setup (prepare / materialize / register, foldTx) are Entity txs, never a one-Account Host route.
});
/**
 * og j-events.ts applyDisputeStartedJEvent / applyDisputeFinalizedJEvent (J7 dispatch): a DisputeStarted / DisputeFinalized event whose account
 * (og resolveDisputeAccountContext: the counterentity when we sent it, else the sender) is the Host's one Account becomes that Account's
 * external_finality, built by disputeStartedInput / disputeFinalizedInput against the frozen Account's current proof body. Other Accounts' events are
 * og's `account_missing` no-op. The Host's jBatchState then takes og's J-batch retirement (hostDisputeJBatch). Counter-proof selection and HTLC / cross-j follow-ups run on the
 * Entity path (disputeStartedJEvent); the one-Account Host holds no HTLC route or cross-j book to follow up on.
 */
const disputeFinalityOf = (host: Host, op: JOp, peer: EntityId): Result<AccountFinality | undefined, HostError> => {
  if (op.type !== "j_event" || (op.event.type !== "DisputeStarted" && op.event.type !== "DisputeFinalized")) return ok(undefined);
  const e = op.event, self = host.self.toLowerCase(), counterparty = e.sender.toLowerCase() === self ? e.counterentity.toLowerCase() : e.sender.toLowerCase();
  if (counterparty !== peer.toLowerCase()) return ok(undefined);
  const body = host.account.state, frozen = chain(mapErr(committedView(body), (): JObserveError => ({ _tag: "j_observe", reason: "DISPUTE_FROZEN_ACCOUNT_STATE_MISMATCH" })), (view) =>
    map(mapErr(localProof(view, host.deltaTransformer), (): JObserveError => ({ _tag: "j_observe", reason: "DISPUTE_FROZEN_ACCOUNT_STATE_MISMATCH" })), (p) => p.bodyHash));
  return chain(frozen, (frozenHash): Result<AccountFinality, HostError> => {
    if (e.type === "DisputeStarted") return disputeStartedInput(e, op.blockNumber, frozenHash);
    const active = host.account._tag === "disputed" ? host.account.active : undefined;
    return disputeFinalizedInput(e, { jNonce: body.jNonce, ...opt("initialProposerIsLeft", active?.initialProposerIsLeft) }, frozenHash);
  });
};
export const applyHost = (host: Host, tx: HostTx, ctx: HostCtx, verify: Verify): Result<HostStep, AccountReplicaError | HostError> => matchBy("layer", tx, {
  account: (i) => admitTx(host, i.tx, ctx, verify),
  frame: (i) => {
    const delivery: Delivery = ctx.from === undefined ? { _tag: "local" } : { _tag: "received", from: ctx.from }, door: DoorContext = { verify, self: host.self, now: ctx.timestamp, ...opt("deltaTransformer", host.deltaTransformer) };
    return accountStep(host, disputeUnsafe(host.account, applyDelivered(host.account, i.input, delivery, door), door));
  },
  j: (i) => chain(partyOf(replicaId(host.account), host.self), (party) => chain(applyJ(host.j, i.tx, host.self, party.peer, ctx, host.account.state), ({ j, effects, release }) => {
    const moved: Host = { ...host, j, account: releaseLatches(host.account, party.peer, release) };
    return chain(disputeFinalityOf(host, i.tx, party.peer), (finality): Result<HostStep, AccountReplicaError | HostError> => (finality === undefined || i.tx.type !== "j_event" || (i.tx.event.type !== "DisputeStarted" && i.tx.event.type !== "DisputeFinalized") ? ok(step(moved, effects))
      : chain(map(accountStep(moved, applyAccountInput(host.account, { kind: "external_finality", ...envelopeOf(host.account.state.terms, party), finality }, { verify, self: host.self, now: ctx.timestamp, ...opt("deltaTransformer", host.deltaTransformer) })), (s) => step(s.state, [...effects, ...s.effects])),
        (s) => map(hostDisputeJBatch(s.state.j, host.self, (i.tx as Extract<JOp, { readonly type: "j_event" }>).event as Extract<JEvent, { readonly type: "DisputeStarted" | "DisputeFinalized" }>), (b) => step({ ...s.state, j: b.j }, [...s.effects, ...b.effects])))));
  })),
  ladder: (i) => { const key = ladderKey(i.tx); return map(revealSlot(host.ladder.get(key), i.tx), (slot) => step({ ...host, ladder: mapSet(host.ladder, key, slot) })); },
  entity: (i) => chain(routeEntity(i.tx, host.self, replicaId(host.account)), (routed) => admitTx(host, routed, ctx, verify)),
  input: (i) => matchBy("kind", i.input, {
    dispute: () => accountStep(host, applyAccountInput(host.account, { kind: "freeze" }, { verify, self: host.self, now: ctx.timestamp, ...opt("deltaTransformer", host.deltaTransformer) })),
  }),

  receipt: (i) => ok(step({ ...host, outbox: host.outbox.filter((e) => e.id !== i.id) })),
});

// ---- watchtower: og storage/recovery/bundle/{types,crypto}.ts, watchtower/http.ts verifyTowerAppointment, watchtower/store/{appointments,decode,db}.ts ----
// The HTTP body reader, LevelDB, the store-wide quotas (maxLookupKeys, maxTotalStoredBytes), push, sweep and the on-chain action are I/O:
// the stored lookup document is threaded as a value, and the tower's clock and key are inputs.
export type TowerModeV1 = "blind_backup" | "delayed_last_resort";
export type EncryptedRuntimeRecoveryBundleV1 = {
  readonly version: 1; readonly kind?: "snapshot" | "journal_tail" | undefined; readonly runtimeId: string; readonly lookupKey: string; readonly height: number; readonly createdAt: number;
  readonly bundleHash: string; readonly baseRuntimeHeight?: number | undefined; readonly baseCheckpointHash?: string | undefined; readonly iv: string; readonly ciphertext: string; readonly compression?: "gzip" | undefined;
};
export type TowerLastResortWatchV1 = { readonly rpcUrl: string; readonly chainId: number; readonly depositoryAddress: string; readonly watchedEntityId: string; readonly counterentity: string };
export type TowerLastResortPayloadV1 = {
  readonly triggerHint: string; readonly watch: TowerLastResortWatchV1; readonly encryptedRemedy: string; readonly actionKind: "counter_dispute_only"; readonly appointmentSequence: number; readonly proofNonce: number;
  readonly proofBodyHash: string; readonly responseMode: "last_resort"; readonly lastResortWindowSeconds: number; readonly maxFeeToken?: string | undefined; readonly feeBudget?: string | undefined;
};
export type TowerAppointmentOwnerProofV1 = { readonly runtimeId: string; readonly signedAt: number; readonly signature: string };
export type TowerAppointmentV1 = {
  readonly type: "tower_appointment"; readonly version: 1; readonly towerMode?: TowerModeV1 | undefined; readonly lookupKey: string; readonly slot?: number | undefined;
  readonly bundle: EncryptedRuntimeRecoveryBundleV1; readonly lastResortPayload?: TowerLastResortPayloadV1 | undefined; readonly ownerProof: TowerAppointmentOwnerProofV1;
};
export type TowerReceiptV1 = {
  readonly type: "tower_receipt"; readonly version: 1; readonly towerId: string; readonly lookupKey: string; readonly runtimeId: string; readonly height: number; readonly bundleHash: string;
  readonly towerMode?: TowerModeV1 | undefined; readonly slot?: number | undefined; readonly storedAt?: number | undefined; readonly receivedAt: number; readonly expiresAt?: number | undefined;
  readonly sequence: number; readonly retainedSlots: number; readonly storedBytes?: number | undefined; readonly maxStoredBytes?: number | undefined; readonly quotaOk?: boolean | undefined;
  readonly appointmentSequence?: number | null | undefined; readonly towerSignature?: string | undefined;
};
export type TowerStoredBundle = {
  readonly slot: number; readonly towerMode: TowerModeV1; readonly bundle: EncryptedRuntimeRecoveryBundleV1; readonly ownerSignedAt: number; readonly encryptedEnvelopeHash: string;
  readonly lastResortPayloadDigest: string; readonly lastResortPayload?: TowerLastResortPayloadV1 | undefined;
};
/** og StoredLookupDoc: one lookup key's receipts (newest first) and retained bundles. */
export type TowerLookupDoc = { readonly lookupKey: string; readonly runtimeId: string; readonly updatedAt: number; readonly receipts: readonly TowerReceiptV1[]; readonly bundles: readonly TowerStoredBundle[] };
/** og WatchtowerStoreContext minus I/O: `now` is the one `context.now()` reading for this write, `towerPrivateKey` og's `signer`. */
export type TowerStoreConfig = { readonly towerId: string; readonly towerPrivateKey: Uint8Array; readonly maxBundlesPerLookupKey: number; readonly maxStoredBytesPerLookupKey: number; readonly receiptTtlMs: number; readonly now: number };
/** `code` is og's thrown message. */
export type TowerError = Tagged<"tower", { code: string }>;
type Rec = { readonly [k: string]: unknown };
const towerErr = (code: string): Result<never, TowerError> => err({ _tag: "tower", code });
/** Sequential guards: the first failing code, as og's first throw. */
const towerChecks = (...checks: readonly (() => string | undefined)[]): Result<void, TowerError> => {
  for (const check of checks) { const code = check(); if (code !== undefined) return towerErr(code); }
  return ok(undefined);
};
/** og requireBoundaryRecord. */
const plainRecord = (v: unknown): v is Rec => {
  if (!v || typeof v !== "object" || Array.isArray(v) || v instanceof Map) return false;
  const proto = Object.getPrototypeOf(v);
  return proto === Object.prototype || proto === null;
};
/** og requireExactBoundaryKeys. */
const exactKeys = (r: Rec, required: readonly string[], optional: readonly string[], code: string): string | undefined => {
  const allowed = new Set([...required, ...optional]), missing = required.filter((k) => !Object.hasOwn(r, k)), extra = Object.keys(r).filter((k) => !allowed.has(k));
  return missing.length > 0 || extra.length > 0 ? `${code}:missing=${missing.join(",") || "none"}:extra=${extra.join(",") || "none"}` : undefined;
};
const recordShape = (v: unknown, code: string, required: readonly string[], optional: readonly string[], fieldsCode: string): string | undefined =>
  !plainRecord(v) ? code : exactKeys(v, required, optional, fieldsCode);
const jsText = (v: unknown): string => String(v || "");
const jsInt = (v: unknown): number => Math.max(0, Math.floor(Number(v || 0)));
/** og normalizeTowerModeV1. */
export const normalizeTowerMode = (mode: unknown): Result<TowerModeV1, TowerError> => {
  const raw = jsText(mode).trim();
  return !raw || raw === "blind_backup" ? ok("blind_backup") : raw === "delayed_last_resort" ? ok("delayed_last_resort") : towerErr(`TOWER_MODE_INVALID:${raw}`);
};
const towerHashText = (text: string): string => keccak256Hex(utf8(text));
/** og computeEncryptedRuntimeRecoveryEnvelopeHash: keccak of the tagged-JSON envelope. */
export const towerEnvelopeHash = (bundle: EncryptedRuntimeRecoveryBundleV1): string => towerHashText(stableJson(bundle));
/** og computeTowerLastResortPayloadDigest: ZeroHash when absent. */
export const towerPayloadDigest = (payload: TowerLastResortPayloadV1 | null | undefined): string => (payload ? towerHashText(stableJson(payload)) : ZERO_WORD);
/** og buildTowerAppointmentOwnerMessage. */
export const towerAppointmentOwnerMessage = (runtimeId: string, towerMode: TowerModeV1, lookupKey: string, slot: number, bundle: EncryptedRuntimeRecoveryBundleV1, signedAt: number, payload?: TowerLastResortPayloadV1 | null): string =>
  `xln:tower:appointment:v1|${runtimeId.toLowerCase()}|${towerMode}|${lookupKey}|${jsInt(slot)}|${towerEnvelopeHash(bundle)}|${jsInt(signedAt)}|${towerPayloadDigest(payload)}`;
/** og appointments.ts buildReceiptMessage. */
export const towerReceiptMessage = (r: TowerReceiptV1): string =>
  `xln:watchtower:receipt:v1|${r.towerId}|${r.lookupKey}|${r.runtimeId}|${r.height}|${r.bundleHash}|${jsInt(r.sequence)}|${jsInt(r.slot)}|${String(r.towerMode || "blind_backup")}|${jsInt(r.storedBytes)}|${jsInt(r.maxStoredBytes)}|${jsInt(r.expiresAt)}`;
const eip191Digest = (message: string): Uint8Array => { const body = utf8(message); return keccak256(concat([utf8(`\x19Ethereum Signed Message:\n${body.length}`), body])); };
/** ethers Wallet.signMessage: EIP-191, RFC 6979 low-s, v = 27 + parity. */
export const signPersonalMessage = (message: string, privateKey: Uint8Array): string => {
  const s = signRaw(eip191Digest(message), privateKey);
  return joinHex([s.r.toString(16).padStart(64, "0"), s.s.toString(16).padStart(64, "0"), (27 + s.recovery).toString(16)]);
};
/** ethers verifyMessage (Signature.from: 64-byte EIP-2098 or 65-byte with v 0/1/27/28/EIP-155): the lowercase signer, or undefined where ethers throws. */
export const recoverPersonalMessage = (message: string, signature: string): string | undefined => {
  const bytes = /^0x([0-9a-fA-F]{2})*$/.test(signature) ? hexToBytes(signature) : undefined;
  if (bytes === undefined || (bytes.length !== 64 && bytes.length !== 65)) return undefined;
  const s = bytes.slice(32, 64), v = bytes.length === 64 ? ((s[0] ?? 0) & 0x80 ? 28 : 27) : bytes[64] ?? 0;
  if (bytes.length === 64) s[0] = (s[0] ?? 0) & 0x7f;
  const parity = v === 0 || v === 27 ? 0 : v === 1 || v === 28 ? 1 : v >= 35 ? (v & 1 ? 0 : 1) : undefined;
  if (parity === undefined) return undefined;
  const pub = recoverPublicKey(eip191Digest(message), bytes.slice(0, 32), s, parity);
  return pub === null ? undefined : addressOf(pub).toLowerCase();
};
const ibanChecksum = (address: string): string => {
  const letters = (c: string): string => (/[0-9]/.test(c) ? c : String(c.charCodeAt(0) - 55));
  let expanded = [...`${address.toUpperCase().substring(4)}${address.toUpperCase().substring(0, 2)}00`].map(letters).join("");
  while (expanded.length >= 15) { const block = expanded.substring(0, 15); expanded = String(parseInt(block, 10) % 97) + expanded.substring(block.length); }
  return String(98 - (parseInt(expanded, 10) % 97)).padStart(2, "0");
};
/** ethers isAddress: hex with optional 0x (mixed case must checksum), or a direct-mode ICAP. */
export const isEthersAddress = (value: string): boolean => {
  if (/^(0x)?[0-9a-fA-F]{40}$/.test(value)) { const a = value.startsWith("0x") ? value : `0x${value}`; return !/([A-F].*[a-f])|([a-f].*[A-F])/.test(a) || checksum(a) === a; }
  return /^XE[0-9]{2}[0-9A-Za-z]{30,31}$/.test(value) && value.substring(2, 4) === ibanChecksum(value);
};
const TOWER_BYTES32 = /^0x[0-9a-f]{64}$/;
const towerLookupKey = (v: unknown): Result<string, TowerError> => { const k = jsText(v).trim().toLowerCase(); return TOWER_BYTES32.test(k) ? ok(k) : towerErr(`TOWER_LOOKUP_KEY_INVALID: ${String(v)}`); };
const bytes32Code = (v: unknown, label: string): string | undefined => (TOWER_BYTES32.test(jsText(v).trim().toLowerCase()) ? undefined : `TOWER_${label}_INVALID: ${String(v)}`);
const hexBytesCode = (v: unknown, label: string): string | undefined => (/^0x([0-9a-f]{2})*$/.test(jsText(v).trim().toLowerCase()) ? undefined : `TOWER_${label}_INVALID`);
const nonNegative = (v: unknown): number | undefined => { const n = Number(v); return Number.isFinite(n) && n >= 0 ? Math.floor(n) : undefined; };
const nonNegativeCode = (v: unknown, label: string): string | undefined => (nonNegative(v) === undefined ? `TOWER_${label}_INVALID` : undefined);
const positiveCode = (v: unknown, label: string): string | undefined => { const n = nonNegative(v); return n === undefined ? `TOWER_${label}_INVALID` : n <= 0 ? `TOWER_${label}_INVALID` : undefined; };
const BUNDLE_FIELDS = ["version", "runtimeId", "lookupKey", "height", "createdAt", "bundleHash", "iv", "ciphertext"] as const, BUNDLE_OPTIONAL = ["kind", "baseRuntimeHeight", "baseCheckpointHash", "compression"] as const;
/** og http.ts decodeTowerAppointmentEnvelope: exact fields before any signed value is read. */
const towerEnvelopeCode = (a: Rec): string | undefined => {
  const payload = a["lastResortPayload"];
  const found = [
    () => exactKeys(a, ["type", "version", "lookupKey", "bundle", "ownerProof"], ["towerMode", "slot", "lastResortPayload"], "TOWER_APPOINTMENT_FIELDS_INVALID"),
    () => (a["type"] !== "tower_appointment" || a["version"] !== 1 ? "TOWER_APPOINTMENT_INVALID" : undefined),
    () => recordShape(a["bundle"], "TOWER_BUNDLE_INVALID", BUNDLE_FIELDS, BUNDLE_OPTIONAL, "TOWER_BUNDLE_FIELDS_INVALID"),
    () => recordShape(a["ownerProof"], "TOWER_APPOINTMENT_OWNER_PROOF_INVALID", ["runtimeId", "signedAt", "signature"], [], "TOWER_APPOINTMENT_OWNER_PROOF_FIELDS_INVALID"),
    () => payload === undefined ? undefined : recordShape(payload, "TOWER_LAST_RESORT_PAYLOAD_INVALID",
      ["triggerHint", "watch", "encryptedRemedy", "actionKind", "appointmentSequence", "proofNonce", "proofBodyHash", "responseMode", "lastResortWindowSeconds"], ["maxFeeToken", "feeBudget"], "TOWER_LAST_RESORT_PAYLOAD_FIELDS_INVALID"),
    () => payload === undefined ? undefined : recordShape((payload as Rec)["watch"], "TOWER_LAST_RESORT_PAYLOAD_WATCH_MISSING", ["rpcUrl", "chainId", "depositoryAddress", "watchedEntityId", "counterentity"], [], "TOWER_LAST_RESORT_PAYLOAD_WATCH_FIELDS_INVALID"),
  ].map((f) => f()).find((c) => c !== undefined);
  return found;
};
/** og http.ts verifyEncryptedBundleShape. */
const towerBundleShapeCode = (b: Rec): string | undefined => {
  if (b["version"] !== 1) return "TOWER_BUNDLE_VERSION_UNSUPPORTED";
  if (!jsText(b["runtimeId"]).trim()) return "TOWER_BUNDLE_RUNTIME_ID_REQUIRED";
  const key = towerLookupKey(b["lookupKey"]);
  if (!key.ok) return key.error.code;
  const first = [nonNegativeCode(b["height"], "BUNDLE_HEIGHT"), nonNegativeCode(b["createdAt"], "BUNDLE_CREATED_AT"), bytes32Code(b["bundleHash"], "BUNDLE_HASH"), hexBytesCode(b["iv"], "BUNDLE_IV"), hexBytesCode(b["ciphertext"], "BUNDLE_CIPHERTEXT")].find((c) => c !== undefined);
  if (first !== undefined) return first;
  if (jsText(b["ciphertext"]).trim().length <= 2) return "TOWER_BUNDLE_CIPHERTEXT_EMPTY";
  return b["compression"] !== undefined && b["compression"] !== "gzip" ? `TOWER_BUNDLE_COMPRESSION_UNSUPPORTED: ${String(b["compression"])}` : undefined;
};
/** og assertEncryptedLastResortPayload: `encryptedRemedy` is a tagged-JSON `tower_encrypted_payload` v1 record. */
const encryptedRemedyCode = (payload: Rec | undefined): string | undefined => {
  const raw = jsText(payload?.["encryptedRemedy"]).trim();
  if (!raw) return "TOWER_LAST_RESORT_PAYLOAD_REMEDY_MISSING";
  const parsed = parseTaggedJson(raw);
  return parsed.ok && plainRecord(parsed.value) && parsed.value["type"] === "tower_encrypted_payload" && parsed.value["version"] === 1 && parsed.value["alg"] === "watch-seed-aes-256-gcm"
    && typeof parsed.value["iv"] === "string" && typeof parsed.value["ciphertext"] === "string" ? undefined : "TOWER_LAST_RESORT_PAYLOAD_REMEDY_NOT_ENCRYPTED";
};
/** og http.ts verifyLastResortPayload + verifyLastResortWatch. */
const lastResortPayloadCode = (p: Rec | undefined): string | undefined => {
  if (!p) return "TOWER_LAST_RESORT_PAYLOAD_MISSING";
  if (p["actionKind"] !== "counter_dispute_only") return "TOWER_LAST_RESORT_PAYLOAD_ACTION_KIND_UNSUPPORTED";
  if (p["responseMode"] !== "last_resort") return "TOWER_LAST_RESORT_PAYLOAD_RESPONSE_MODE_UNSUPPORTED";
  const w = p["watch"];
  if (!w || typeof w !== "object") return "TOWER_LAST_RESORT_PAYLOAD_WATCH_MISSING";
  const watch = w as Rec, rpcUrl = jsText(watch["rpcUrl"]).trim(), hint = jsText(p["triggerHint"]).trim();
  const found = [
    () => (!rpcUrl || rpcUrl.length > 512 || !/^https?:\/\//i.test(rpcUrl) ? "TOWER_LAST_RESORT_PAYLOAD_WATCH_RPC_INVALID" : undefined),
    () => positiveCode(watch["chainId"], "LAST_RESORT_PAYLOAD_WATCH_CHAIN_ID"),
    () => (isEthersAddress(jsText(watch["depositoryAddress"])) ? undefined : "TOWER_LAST_RESORT_PAYLOAD_WATCH_DEPOSITORY_INVALID"),
    () => bytes32Code(watch["watchedEntityId"], "LAST_RESORT_PAYLOAD_WATCH_ENTITY"),
    () => bytes32Code(watch["counterentity"], "LAST_RESORT_PAYLOAD_WATCH_COUNTERENTITY"),
    () => (!hint || hint.length > 256 ? "TOWER_LAST_RESORT_PAYLOAD_TRIGGER_HINT_INVALID" : undefined),
    () => positiveCode(p["appointmentSequence"], "LAST_RESORT_PAYLOAD_APPOINTMENT_SEQUENCE"),
    () => positiveCode(p["proofNonce"], "LAST_RESORT_PAYLOAD_PROOF_NONCE"),
    () => bytes32Code(p["proofBodyHash"], "LAST_RESORT_PAYLOAD_PROOF_BODY_HASH"),
    () => positiveCode(p["lastResortWindowSeconds"], "LAST_RESORT_PAYLOAD_LAST_RESORT_WINDOW"),
    () => encryptedRemedyCode(p),
  ];
  for (const f of found) { const c = f(); if (c !== undefined) return c; }
  return undefined;
};
export const TOWER_APPOINTMENT_MAX_CLOCK_SKEW_MS = 24 * 60 * 60 * 1000;
/**
 * og http.ts verifyTowerAppointment: exact envelope, bundle shape, lookup/runtime binding, owner clock within 24h of the tower clock `nowMs`,
 * mode/payload rules, and the owner's EIP-191 signature recovering `ownerProof.runtimeId`. Returns og's normalized appointment.
 */
export const verifyTowerAppointment = (input: unknown, nowMs: number): Result<TowerAppointmentV1, TowerError> => {
  if (!plainRecord(input)) return towerErr("TOWER_APPOINTMENT_INVALID");
  const envelope = towerEnvelopeCode(input);
  if (envelope !== undefined) return towerErr(envelope);
  const a = input as unknown as TowerAppointmentV1, bundle = input["bundle"] as Rec, proof = input["ownerProof"] as Rec, payload = input["lastResortPayload"] as Rec | undefined;
  const shape = towerBundleShapeCode(bundle);
  if (shape !== undefined) return towerErr(shape);
  return chain(towerLookupKey(a.lookupKey), (lookupKey) => {
    if (bundle["lookupKey"] !== lookupKey) return towerErr("TOWER_APPOINTMENT_LOOKUP_MISMATCH");
    const runtimeId = jsText(proof["runtimeId"]).trim().toLowerCase();
    if (!runtimeId || runtimeId !== jsText(bundle["runtimeId"]).trim().toLowerCase()) return towerErr("TOWER_APPOINTMENT_RUNTIME_ID_MISMATCH");
    const signedAt = Math.max(0, Math.floor(Number(proof["signedAt"] || 0)));
    if (!Number.isSafeInteger(signedAt) || signedAt <= 0) return towerErr("TOWER_APPOINTMENT_SIGNED_AT_INVALID");
    if (Math.abs(nowMs - signedAt) > TOWER_APPOINTMENT_MAX_CLOCK_SKEW_MS) return towerErr("TOWER_APPOINTMENT_STALE");
    const slot = Math.max(0, Math.floor(Number(input["slot"] ?? 0)));
    return chain(normalizeTowerMode(input["towerMode"]), (towerMode) => {
      if (towerMode === "blind_backup" && payload) return towerErr("TOWER_BACKUP_LAST_RESORT_PAYLOAD_FORBIDDEN");
      const payloadCode = towerMode === "delayed_last_resort" ? lastResortPayloadCode(payload) : undefined;
      if (payloadCode !== undefined) return towerErr(payloadCode);
      const message = towerAppointmentOwnerMessage(runtimeId, towerMode, lookupKey, slot, a.bundle, signedAt, a.lastResortPayload);
      const recovered = recoverPersonalMessage(message, jsText(proof["signature"]));
      if (recovered === undefined) return towerErr("TOWER_APPOINTMENT_SIGNATURE_UNREADABLE");
      if (recovered !== runtimeId) return towerErr(`TOWER_APPOINTMENT_SIGNATURE_INVALID: recovered=${recovered} expected=${runtimeId}`);
      return ok({
        ...a, towerMode, lookupKey, slot, ownerProof: { ...a.ownerProof, runtimeId, signedAt, signature: jsText(proof["signature"]) },
        bundle: { ...a.bundle, runtimeId, lookupKey, height: jsInt(bundle["height"]), createdAt: jsInt(bundle["createdAt"]) },
      });
    });
  });
};
export const emptyTowerLookupDoc = (lookupKey: string): TowerLookupDoc => ({ lookupKey, runtimeId: "", updatedAt: 0, receipts: [], bundles: [] });
/** og computeStoredLookupBytes: UTF-8 bytes of the tagged-JSON document. */
export const towerLookupBytes = (doc: TowerLookupDoc): number => utf8(stableJson(doc)).length;
const bundleKind = (b: EncryptedRuntimeRecoveryBundleV1): string => b.kind ?? "snapshot";
/** og sortStoredBundles: height, then createdAt, then slot, all descending (stable). */
const sortTowerBundles = (bundles: readonly TowerStoredBundle[]): readonly TowerStoredBundle[] =>
  [...bundles].sort((l, r) => (r.bundle.height !== l.bundle.height ? r.bundle.height - l.bundle.height : r.bundle.createdAt !== l.bundle.createdAt ? r.bundle.createdAt - l.bundle.createdAt : r.slot - l.slot));
/** og retainAppointmentBundles: a blind backup pins the newest snapshot and the newest tail before the cut. */
const retainTowerBundles = (bundles: readonly TowerStoredBundle[], towerMode: TowerModeV1, limit: number): readonly TowerStoredBundle[] => {
  const pins = towerMode !== "blind_backup" ? [] : [bundles.find((e) => e.towerMode === "blind_backup" && e.bundle.kind !== "journal_tail"), bundles.find((e) => e.towerMode === "blind_backup" && e.bundle.kind === "journal_tail")];
  const pinned = pins.filter((e, i): e is TowerStoredBundle => e !== undefined && pins.indexOf(e) === i);
  return [...pinned, ...bundles.filter((e) => !pinned.includes(e))].slice(0, limit);
};
/** og store validateAppointmentMode. */
const towerStoreModeCode = (a: TowerAppointmentV1, towerMode: TowerModeV1): string | undefined => {
  if (towerMode === "blind_backup" && a.lastResortPayload) return "TOWER_BACKUP_LAST_RESORT_PAYLOAD_FORBIDDEN";
  if (towerMode === "delayed_last_resort" && !a.lastResortPayload) return "TOWER_LAST_RESORT_PAYLOAD_MISSING";
  return towerMode === "delayed_last_resort" ? encryptedRemedyCode(a.lastResortPayload as Rec | undefined) : undefined;
};
/**
 * og appointments.ts prepareAppointment: per (slot, mode, kind) the newest owner signature wins; an older `signedAt` is TOWER_APPOINTMENT_STALE,
 * the same `signedAt` over different envelope or payload bytes is TOWER_APPOINTMENT_REPLAY_MISMATCH. The tower signs the receipt (EIP-191).
 */
const prepareTowerAppointment = (cfg: TowerStoreConfig, a: TowerAppointmentV1, existing: TowerLookupDoc): Result<TowerLookupDoc, TowerError> =>
  chain(towerLookupKey(a.lookupKey), (lookupKey) => chain(normalizeTowerMode(a.towerMode), (towerMode) => {
    const slot = Math.max(0, Math.floor(Number(a.slot ?? 0)));
    const runtimeId = jsText(a.bundle.runtimeId).trim().toLowerCase();
    const sequence = Math.max(0, ...existing.receipts.map((r) => r.sequence || 0)) + 1, ownerSignedAt = jsInt(a.ownerProof.signedAt);
    const lastResortPayloadDigest = towerPayloadDigest(a.lastResortPayload), encryptedEnvelopeHash = towerEnvelopeHash(a.bundle);
    const sameSlot = (e: TowerStoredBundle): boolean => e.slot === slot && e.towerMode === towerMode && bundleKind(e.bundle) === bundleKind(a.bundle);
    const latest = existing.bundles.filter(sameSlot).reduce<TowerStoredBundle | undefined>((l, e) => (!l || e.ownerSignedAt > l.ownerSignedAt ? e : l), undefined);
    return chain(towerChecks(
      () => towerStoreModeCode(a, towerMode),
      () => (existing.runtimeId && existing.runtimeId !== runtimeId ? `TOWER_LOOKUP_RUNTIME_ID_MISMATCH:${existing.runtimeId}:${runtimeId}` : undefined),
      () => (latest && latest.ownerSignedAt > ownerSignedAt ? "TOWER_APPOINTMENT_STALE" : undefined),
      () => (latest && latest.ownerSignedAt === ownerSignedAt && (latest.encryptedEnvelopeHash !== encryptedEnvelopeHash || latest.lastResortPayloadDigest !== lastResortPayloadDigest) ? "TOWER_APPOINTMENT_REPLAY_MISMATCH" : undefined),
    ), () => {
      const candidate: TowerStoredBundle = { slot, towerMode, bundle: a.bundle, ownerSignedAt, encryptedEnvelopeHash, lastResortPayloadDigest, ...opt("lastResortPayload", a.lastResortPayload || undefined) };
      const bundles = retainTowerBundles(sortTowerBundles([candidate, ...existing.bundles.filter((e) => !sameSlot(e))]), towerMode, cfg.maxBundlesPerLookupKey);
      const draft: TowerLookupDoc = { lookupKey, runtimeId, updatedAt: cfg.now, receipts: existing.receipts, bundles };
      const storedBytes = towerLookupBytes(draft);
      if (storedBytes > cfg.maxStoredBytesPerLookupKey) return towerErr(`TOWER_QUOTA_EXCEEDED: bytes=${storedBytes} max=${cfg.maxStoredBytesPerLookupKey}`);
      const seq = Number(a.lastResortPayload?.appointmentSequence);
      const unsigned: TowerReceiptV1 = {
        type: "tower_receipt", version: 1, towerId: cfg.towerId, lookupKey, runtimeId, height: jsInt(a.bundle.height), bundleHash: a.bundle.bundleHash, towerMode, slot,
        storedAt: cfg.now, receivedAt: cfg.now, expiresAt: cfg.now + cfg.receiptTtlMs, sequence, retainedSlots: bundles.length, storedBytes, maxStoredBytes: cfg.maxStoredBytesPerLookupKey, quotaOk: true,
        appointmentSequence: Number.isFinite(seq) ? jsInt(a.lastResortPayload?.appointmentSequence) : null,
      };
      const receipt: TowerReceiptV1 = { ...unsigned, towerSignature: signPersonalMessage(towerReceiptMessage(unsigned), cfg.towerPrivateKey) };
      return ok({ ...draft, receipts: [receipt, ...existing.receipts].slice(0, cfg.maxBundlesPerLookupKey) });
    });
  }));
export type TowerWrite = { readonly doc: TowerLookupDoc; readonly receipt: TowerReceiptV1 };
/** og writeLookup's per-lookup quota on the final signed document (the store-wide quotas are the host's). */
const sealTowerWrite = (cfg: TowerStoreConfig, doc: TowerLookupDoc): Result<TowerWrite, TowerError> => {
  const bytes = towerLookupBytes(doc), receipt = doc.receipts[0];
  if (bytes > cfg.maxStoredBytesPerLookupKey) return towerErr(`TOWER_QUOTA_EXCEEDED: bytes=${bytes} max=${cfg.maxStoredBytesPerLookupKey}`);
  return receipt === undefined ? towerErr("TOWER_RECEIPT_MISSING") : ok({ doc, receipt });
};
/** og store upsertAppointment over the stored lookup document (`existing` undefined when the key is new). */
export const upsertTowerAppointment = (cfg: TowerStoreConfig, a: TowerAppointmentV1, existing?: TowerLookupDoc): Result<TowerWrite, TowerError> =>
  chain(towerLookupKey(a.lookupKey), (key) => chain(prepareTowerAppointment(cfg, a, existing ?? emptyTowerLookupDoc(key)), (doc) => sealTowerWrite(cfg, doc)));
/** og upsertRecoveryArchive: one owner-signed snapshot and its tail, written as one document or not at all. */
export const upsertTowerRecoveryArchive = (cfg: TowerStoreConfig, pair: readonly [TowerAppointmentV1, TowerAppointmentV1], existing?: TowerLookupDoc): Result<TowerWrite, TowerError> => {
  const [snapshot, tail] = pair;
  const modes = pair.map((x) => normalizeTowerMode(x.towerMode));
  const bad = modes.find((m) => !m.ok);
  if (bad !== undefined && !bad.ok) return bad;
  if (modes.some((m) => m.ok && m.value !== "blind_backup") || snapshot.bundle.kind !== "snapshot" || tail.bundle.kind !== "journal_tail" || snapshot.lookupKey !== tail.lookupKey || snapshot.bundle.runtimeId !== tail.bundle.runtimeId
    || Math.max(0, Math.floor(Number(snapshot.slot ?? 0))) !== Math.max(0, Math.floor(Number(tail.slot ?? 0))) || snapshot.ownerProof.signedAt !== tail.ownerProof.signedAt
    || tail.bundle.baseRuntimeHeight !== snapshot.bundle.height || tail.bundle.height <= snapshot.bundle.height) return towerErr("TOWER_ARCHIVE_PAIR_INVALID");
  return chain(towerLookupKey(snapshot.lookupKey), (key) => chain(foldResult(pair, existing ?? emptyTowerLookupDoc(key), (doc, a) => prepareTowerAppointment(cfg, a, doc)), (doc) =>
    pair.every((a) => doc.bundles.some((e) => e.encryptedEnvelopeHash === towerEnvelopeHash(a.bundle))) ? sealTowerWrite(cfg, doc) : towerErr("TOWER_ARCHIVE_PAIR_NOT_RETAINED")));
};
/** Client check of og's receipt signature (og signs in prepareAppointment and ships no verifier): the tower address the receipt message recovers to. */
export const verifyTowerReceiptSignature = (receipt: TowerReceiptV1, towerAddress: string): Result<TowerReceiptV1, TowerError> => {
  if (receipt.towerSignature === undefined) return towerErr("TOWER_RECEIPT_SIGNATURE_MISSING");
  const { towerSignature, ...unsigned } = receipt;
  return recoverPersonalMessage(towerReceiptMessage(unsigned as TowerReceiptV1), towerSignature) === towerAddress.toLowerCase() ? ok(receipt) : towerErr("TOWER_RECEIPT_SIGNATURE_INVALID");
};
/** og deserializeTaggedJson: JSON with BigInt/Map/Set/Buffer/Date/TypedArray envelopes revived. */
const parseTaggedJson = (raw: string): Result<unknown, TowerError> => {
  try {
    return ok(JSON.parse(raw, (_k, v: unknown) => {
      if (!plainRecord(v) || typeof v["__xlnType"] !== "string") return v;
      const x = v["value"];
      switch (v["__xlnType"]) {
        case "BigInt": return typeof x === "string" ? BigInt(x) : v;
        case "Map": return Array.isArray(x) ? new Map(x as [unknown, unknown][]) : v;
        case "Set": return Array.isArray(x) ? new Set(x) : v;
        case "Buffer": return Array.isArray(x) ? Uint8Array.from(x as number[]) : v;
        case "Date": return typeof x === "string" ? new Date(x) : v;
        case "TypedArray": return typeof v["kind"] === "string" && typeof x === "string" ? new Uint8Array(0) : v;
        default: return v;
      }
    }));
  } catch { return towerErr("TOWER_JSON_INVALID"); }
};
const safeIntCode = (v: unknown, code: string): string | undefined => (typeof v !== "number" || !Number.isSafeInteger(v) || v < 0 ? code : undefined);
const optionalSafeIntCode = (v: unknown, code: string): string | undefined => (v === undefined || v === null ? undefined : safeIntCode(v, code));
const textCode = (v: unknown, code: string): string | undefined => (typeof v !== "string" || v.trim().length === 0 ? code : undefined);
const firstCode = (checks: readonly (() => string | undefined)[]): string | undefined => { for (const c of checks) { const code = c(); if (code !== undefined) return code; } return undefined; };
/** og decode.ts decodeReceipt: a stored receipt bound to its lookup key and runtime; `towerSignature` is optional but non-empty when present. */
export const decodeTowerReceipt = (value: unknown, lookupKey: string, runtimeId: string): Result<TowerReceiptV1, TowerError> => {
  if (!plainRecord(value)) return towerErr("TOWER_STORED_RECEIPT_INVALID");
  const r = value;
  const code = firstCode([
    () => exactKeys(r, ["type", "version", "towerId", "lookupKey", "runtimeId", "height", "bundleHash", "receivedAt", "sequence", "retainedSlots"],
      ["towerMode", "slot", "storedAt", "expiresAt", "storedBytes", "maxStoredBytes", "quotaOk", "appointmentSequence", "towerSignature"], "TOWER_STORED_RECEIPT_FIELDS_INVALID"),
    () => (r["type"] !== "tower_receipt" || r["version"] !== 1 ? "TOWER_STORED_RECEIPT_VERSION_INVALID" : undefined),
    () => textCode(r["lookupKey"], "TOWER_STORED_RECEIPT_LOOKUP_INVALID") ?? (r["lookupKey"] !== lookupKey ? "TOWER_STORED_RECEIPT_LOOKUP_MISMATCH" : undefined),
    () => textCode(r["runtimeId"], "TOWER_STORED_RECEIPT_RUNTIME_INVALID") ?? ((r["runtimeId"] as string).toLowerCase() !== runtimeId ? "TOWER_STORED_RECEIPT_RUNTIME_MISMATCH" : undefined),
    () => textCode(r["towerId"], "TOWER_STORED_RECEIPT_TOWER_INVALID"),
    () => textCode(r["bundleHash"], "TOWER_STORED_RECEIPT_HASH_INVALID"),
    ...(["height", "receivedAt", "sequence", "retainedSlots"] as const).map((f) => () => safeIntCode(r[f], `TOWER_STORED_RECEIPT_${f.toUpperCase()}_INVALID`)),
    ...(["slot", "storedAt", "expiresAt", "storedBytes", "maxStoredBytes", "appointmentSequence"] as const).map((f) => () => optionalSafeIntCode(r[f], `TOWER_STORED_RECEIPT_${f.toUpperCase()}_INVALID`)),
    () => { if (r["towerMode"] === undefined) return undefined; const m = normalizeTowerMode(r["towerMode"]); return m.ok ? undefined : m.error.code; },
    () => (r["quotaOk"] !== undefined && typeof r["quotaOk"] !== "boolean" ? "TOWER_STORED_RECEIPT_QUOTA_INVALID" : undefined),
    () => (r["towerSignature"] !== undefined ? textCode(r["towerSignature"], "TOWER_STORED_RECEIPT_SIGNATURE_INVALID") : undefined),
  ]);
  return code === undefined ? ok(value as unknown as TowerReceiptV1) : towerErr(code);
};
/** og decode.ts decodeBundle. */
export const decodeTowerBundle = (value: unknown, lookupKey: string, runtimeId: string): Result<EncryptedRuntimeRecoveryBundleV1, TowerError> => {
  if (!plainRecord(value)) return towerErr("TOWER_STORED_BUNDLE_INVALID");
  const b = value;
  const code = firstCode([
    () => exactKeys(b, BUNDLE_FIELDS, BUNDLE_OPTIONAL, "TOWER_STORED_BUNDLE_FIELDS_INVALID"),
    () => (b["version"] !== 1 ? "TOWER_STORED_BUNDLE_VERSION_INVALID" : undefined),
    () => (b["kind"] !== undefined && b["kind"] !== "snapshot" && b["kind"] !== "journal_tail" ? "TOWER_STORED_BUNDLE_KIND_INVALID" : undefined),
    () => textCode(b["lookupKey"], "TOWER_STORED_BUNDLE_LOOKUP_INVALID") ?? (b["lookupKey"] !== lookupKey ? "TOWER_STORED_BUNDLE_LOOKUP_MISMATCH" : undefined),
    () => textCode(b["runtimeId"], "TOWER_STORED_BUNDLE_RUNTIME_INVALID") ?? ((b["runtimeId"] as string).toLowerCase() !== runtimeId ? "TOWER_STORED_BUNDLE_RUNTIME_MISMATCH" : undefined),
    () => safeIntCode(b["height"], "TOWER_STORED_BUNDLE_HEIGHT_INVALID"),
    () => safeIntCode(b["createdAt"], "TOWER_STORED_BUNDLE_CREATED_AT_INVALID"),
    () => optionalSafeIntCode(b["baseRuntimeHeight"], "TOWER_STORED_BUNDLE_BASE_HEIGHT_INVALID"),
    () => textCode(b["bundleHash"], "TOWER_STORED_BUNDLE_HASH_INVALID"),
    () => textCode(b["iv"], "TOWER_STORED_BUNDLE_IV_INVALID"),
    () => textCode(b["ciphertext"], "TOWER_STORED_BUNDLE_CIPHERTEXT_INVALID"),
    () => (b["compression"] !== undefined && b["compression"] !== "gzip" ? "TOWER_STORED_BUNDLE_COMPRESSION_INVALID" : undefined),
  ]);
  return code === undefined ? ok(value as unknown as EncryptedRuntimeRecoveryBundleV1) : towerErr(code);
};
/** og decode.ts decodeStoredLookupDoc: a persisted lookup document, every receipt and bundle bound to its key and runtime. */
export const decodeTowerLookupDoc = (raw: string, expectedLookupKey?: string): Result<TowerLookupDoc, TowerError> => chain(parseTaggedJson(raw), (value) => {
  if (!plainRecord(value)) return towerErr("TOWER_STORED_LOOKUP_INVALID");
  const d = value;
  const fields = exactKeys(d, ["lookupKey", "runtimeId", "updatedAt", "receipts", "bundles"], [], "TOWER_STORED_LOOKUP_FIELDS_INVALID");
  if (fields !== undefined) return towerErr(fields);
  const keyCode = textCode(d["lookupKey"], "TOWER_STORED_LOOKUP_KEY_INVALID");
  if (keyCode !== undefined) return towerErr(keyCode);
  const lookupKey = (d["lookupKey"] as string).toLowerCase();
  if (!TOWER_BYTES32.test(lookupKey)) return towerErr("TOWER_STORED_LOOKUP_KEY_INVALID");
  if (expectedLookupKey && lookupKey !== expectedLookupKey) return towerErr("TOWER_STORED_LOOKUP_KEY_MISMATCH");
  const runtimeCode = textCode(d["runtimeId"], "TOWER_STORED_RUNTIME_INVALID");
  if (runtimeCode !== undefined) return towerErr(runtimeCode);
  const runtimeId = (d["runtimeId"] as string).toLowerCase();
  const code = firstCode([
    () => (/^0x[0-9a-f]{40}$/.test(runtimeId) ? undefined : "TOWER_STORED_RUNTIME_INVALID"),
    () => safeIntCode(d["updatedAt"], "TOWER_STORED_UPDATED_AT_INVALID"),
    () => (Array.isArray(d["receipts"]) ? undefined : "TOWER_STORED_RECEIPTS_INVALID"),
    () => (Array.isArray(d["bundles"]) ? undefined : "TOWER_STORED_BUNDLES_INVALID"),
  ]);
  if (code !== undefined) return towerErr(code);
  return chain(traverse(d["receipts"] as readonly unknown[], (r) => decodeTowerReceipt(r, lookupKey, runtimeId)), (receipts) =>
    map(traverse(d["bundles"] as readonly unknown[], (v): Result<TowerStoredBundle, TowerError> => {
      if (!plainRecord(v)) return towerErr("TOWER_STORED_BUNDLE_ENTRY_INVALID");
      const e = v, entryFields = exactKeys(e, ["slot", "towerMode", "bundle", "ownerSignedAt", "encryptedEnvelopeHash", "lastResortPayloadDigest"], ["lastResortPayload"], "TOWER_STORED_BUNDLE_ENTRY_FIELDS_INVALID");
      if (entryFields !== undefined) return towerErr(entryFields);
      const slotCode = safeIntCode(e["slot"], "TOWER_STORED_BUNDLE_SLOT_INVALID");
      if (slotCode !== undefined) return towerErr(slotCode);
      return chain(normalizeTowerMode(e["towerMode"]), (towerMode) => chain(decodeTowerBundle(e["bundle"], lookupKey, runtimeId), (bundle) => {
        const c = firstCode([
          () => safeIntCode(e["ownerSignedAt"], "TOWER_STORED_OWNER_SIGNED_AT_INVALID"),
          () => textCode(e["encryptedEnvelopeHash"], "TOWER_STORED_ENCRYPTED_ENVELOPE_HASH_INVALID") ?? (TOWER_BYTES32.test((e["encryptedEnvelopeHash"] as string).toLowerCase()) ? undefined : "TOWER_STORED_ENCRYPTED_ENVELOPE_HASH_INVALID"),
          () => textCode(e["lastResortPayloadDigest"], "TOWER_STORED_LAST_RESORT_DIGEST_INVALID"),
          () => (e["lastResortPayload"] !== undefined && !plainRecord(e["lastResortPayload"]) ? "TOWER_STORED_LAST_RESORT_PAYLOAD_INVALID" : undefined),
        ]);
        return c !== undefined ? towerErr(c) : ok({
          slot: e["slot"] as number, towerMode, bundle, ownerSignedAt: e["ownerSignedAt"] as number, encryptedEnvelopeHash: (e["encryptedEnvelopeHash"] as string).toLowerCase(),
          lastResortPayloadDigest: e["lastResortPayloadDigest"] as string, ...opt("lastResortPayload", e["lastResortPayload"] as TowerLastResortPayloadV1 | undefined),
        });
      }));
    }), (bundles) => ({ lookupKey, runtimeId, updatedAt: d["updatedAt"] as number, receipts, bundles })));
});

// ---- orderbook: og orderbook/core.ts (price-page limit order book), orderbook/pages/{page,key}.ts, orderbook/commitment.ts ----
// og keeps liquidity in two Patricia trees of 16-slot FIFO price pages; the rewrite keeps each side as its pages in key-byte order
// (price, then page sequence) and seals the same radix-16 root. og throws on a broken invariant; here every such case is a `book` refusal.
export type BookSide = 0 | 1;
export type BookEntry = { readonly orderId: string; readonly ownerId: string; readonly qtyLots: bigint; readonly seq: number };
export type BookPage = { readonly headSlot: number; readonly nextSlot: number; readonly liveCount: number; readonly totalQtyLots: bigint; readonly slots: readonly (BookEntry | null)[] };
export type BookPageKey = { readonly priceTicks: bigint; readonly pageSequence: number };
export type BookPageRow = { readonly key: BookPageKey; readonly page: BookPage };
/** og BookOrderState: the RAM locator of a resting order (never committed). */
export type BookOrder = BookEntry & { readonly side: BookSide; readonly priceTicks: bigint; readonly pageSequence: number; readonly pageSlot: number };
export type BookParams = { readonly bucketWidthTicks: bigint; readonly maxOrders: number; readonly stpPolicy: 0 | 1 };
export type Book = {
  readonly params: BookParams; readonly orders: ReadonlyMap<string, BookOrder>; readonly bidPages: readonly BookPageRow[]; readonly askPages: readonly BookPageRow[];
  readonly nextSeq: number; readonly tradeCount: number; readonly tradeQtySum: bigint; readonly lastTradePriceTicks: bigint; readonly lastAcceptedUsdAskPriceTicks: bigint; readonly eventHash: bigint;
};
export type BookTif = 0 | 1 | 2;
export type OrderCmd =
  | { readonly kind: 0; readonly ownerId: string; readonly orderId: string; readonly side: BookSide; readonly tif: BookTif; readonly postOnly: boolean; readonly priceTicks: bigint; readonly qtyLots: bigint }
  | { readonly kind: 1; readonly ownerId: string; readonly orderId: string }
  | { readonly kind: 2; readonly ownerId: string; readonly orderId: string; readonly newPriceTicks: bigint | null; readonly qtyDeltaLots: bigint };
export type BookEvent =
  | { readonly type: "ACK"; readonly orderId: string; readonly ownerId: string }
  | { readonly type: "REJECT"; readonly orderId: string; readonly ownerId: string; readonly reason: string; readonly blockingOrderId?: string | undefined }
  | { readonly type: "TRADE"; readonly price: bigint; readonly qty: bigint; readonly makerOwnerId: string; readonly takerOwnerId: string; readonly makerOrderId: string; readonly takerOrderId: string; readonly makerQtyBefore: bigint; readonly takerQtyTotal: bigint }
  | { readonly type: "REDUCED"; readonly orderId: string; readonly ownerId: string; readonly delta: bigint; readonly remain: bigint }
  | { readonly type: "CANCELED"; readonly orderId: string; readonly ownerId: string };
export type MakerDisposition = "eligible" | "suspended" | "cancel";
export type BookOptions = {
  readonly suspendedOrderIds?: ReadonlySet<string> | undefined;
  /** Consulted lazily, once per maker per command (og cacheMakerDisposition). */
  readonly makerDisposition?: ((maker: BookOrder) => MakerDisposition) | undefined;
  readonly executionPriceTicksForMatch?: ((maker: bigint, taker: bigint, side: BookSide) => bigint) | undefined;
  readonly executionQtyMultipleAtPrice?: ((priceTicks: bigint) => bigint) | undefined;
};
export type BookError = Tagged<"book", { code: string }>;
export type BookStep = { readonly state: Book; readonly events: readonly BookEvent[] };
export const BOOK_PAGE_CAPACITY = 16;
export const MAX_ORDERBOOK_QTY_LOTS = 10n ** 24n;
const bookErr = (code: string): Result<never, BookError> => err({ _tag: "book", code });
const unsignedBytes = (v: bigint): Uint8Array => { const h = v.toString(16); return hexToBytes(h.length % 2 === 0 ? h : `0${h}`); };
/** og encodeBookPricePrefix / encodeBookPricePageKey: length-prefixed minimal price, then the uint16 page. */
const bookPriceKeyBytes = (k: BookPageKey): Uint8Array => { const price = unsignedBytes(k.priceTicks); return concat([Uint8Array.of(price.length), price, u16(k.pageSequence)]); };
const u16Framed = (b: Uint8Array): Uint8Array => concat([u16(b.length), b]);
const bookEntryBytes = (e: BookEntry | null): Uint8Array =>
  e === null ? Uint8Array.of(0) : concat([Uint8Array.of(1), u16Framed(utf8(e.orderId)), u16Framed(utf8(e.ownerId)), u16Framed(unsignedBytes(e.qtyLots)), u16Framed(unsignedBytes(BigInt(e.seq)))]);
/** og pageHash: the integrity digest of the page's slot layout. */
const bookPageDigest = (p: BookPage): Uint8Array =>
  sha256(concat([u16(p.headSlot), u16(p.nextSlot), u16(p.liveCount), u16Framed(unsignedBytes(p.totalQtyLots)), ...p.slots.map(bookEntryBytes)]));
/** og BookPricePageTree.rootHash(): radix-16 Patricia over key bytes, leaf value = page digest; empty is the zero word. */
export const bookPagesRoot = (rows: readonly BookPageRow[]): string =>
  sealRadix(rows.map((r) => { const key = bookPriceKeyBytes(r.key); return { nibbles: nibblesOf(key), key, digest: bookPageDigest(r.page) }; }));
/** og computeBookCommitmentHash: integrity checksum of the length-framed params, both page roots and the trade/event counters. */
export const bookCommitmentHash = (b: Book): string => {
  const parts = ["xln.orderbook.book", String(b.params.bucketWidthTicks), String(b.params.maxOrders), String(b.params.stpPolicy), bookPagesRoot(b.bidPages), bookPagesRoot(b.askPages),
    String(b.nextSeq), String(b.tradeCount), String(b.tradeQtySum), String(b.lastTradePriceTicks), String(b.lastAcceptedUsdAskPriceTicks), String(b.eventHash)].map(utf8);
  return bytesToHex(sha256(concat(parts.flatMap((p) => [u32(p.length), p]))).slice(0, 16));
};
/** og createBook. */
export const createBook = (params: BookParams): Result<Book, BookError> => {
  if (params.bucketWidthTicks <= 0n) return bookErr("bucketWidthTicks must be positive");
  if (!Number.isFinite(params.maxOrders) || params.maxOrders <= 0) return bookErr("maxOrders must be positive");
  if (params.stpPolicy !== 0 && params.stpPolicy !== 1) return bookErr("unsupported stpPolicy");
  return ok({ params: { ...params, maxOrders: Math.max(1, Math.floor(params.maxOrders)) }, orders: new Map(), bidPages: [], askPages: [], nextSeq: 1, tradeCount: 0, tradeQtySum: 0n, lastTradePriceTicks: 0n, lastAcceptedUsdAskPriceTicks: 0n, eventHash: 0n });
};
/** The working copy of one command (og forkBookState overlay). */
type BookWork = { -readonly [K in keyof Book]: Book[K] } & { orders: Map<string, BookOrder>; bidPages: BookPageRow[]; askPages: BookPageRow[] };
const forkBook = (b: Book): BookWork => ({ ...b, orders: new Map(b.orders), bidPages: [...b.bidPages], askPages: [...b.askPages] });
const sideRows = (w: Book, side: BookSide): readonly BookPageRow[] => (side === 0 ? w.bidPages : w.askPages);
const keyOrder = (a: BookPageKey, b: BookPageKey): number => (a.priceTicks !== b.priceTicks ? (a.priceTicks < b.priceTicks ? -1 : 1) : a.pageSequence - b.pageSequence);
/** og tree.updated / tree.removed (`page` undefined): one row, key order kept. */
const setPage = (w: BookWork, side: BookSide, key: BookPageKey, page: BookPage | undefined): void => {
  const rows = side === 0 ? w.bidPages : w.askPages;
  let lo = 0, hi = rows.length;
  while (lo < hi) { const mid = (lo + hi) >> 1; if (keyOrder((rows[mid] as BookPageRow).key, key) < 0) lo = mid + 1; else hi = mid; }
  const hit = lo < rows.length && keyOrder((rows[lo] as BookPageRow).key, key) === 0;
  if (page === undefined) { if (hit) rows.splice(lo, 1); } else rows.splice(lo, hit ? 1 : 0, { key, page });
};
const getPage = (w: Book, side: BookSide, key: BookPageKey): BookPage | undefined => sideRows(w, side).find((r) => keyOrder(r.key, key) === 0)?.page;
/** og orderedPages: asks ascending; bids by descending price, FIFO page sequence within a price. */
const orderedPages = (rows: readonly BookPageRow[], side: BookSide): readonly BookPageRow[] => {
  if (side === 1) return rows;
  const byPrice: BookPageRow[][] = [];
  for (const r of rows) { const last = byPrice[byPrice.length - 1]; if (last !== undefined && last[0]?.key.priceTicks === r.key.priceTicks) last.push(r); else byPrice.push([r]); }
  return byPrice.reverse().flat();
};
const pageOrder = (side: BookSide, row: BookPageRow, slot: number): BookOrder | null => {
  const e = row.page.slots[slot];
  return e ? { ...e, side, priceTicks: row.key.priceTicks, pageSequence: row.key.pageSequence, pageSlot: slot } : null;
};
const pageOrders = (side: BookSide, row: BookPageRow): readonly BookOrder[] => {
  const out: BookOrder[] = [];
  for (let slot = row.page.headSlot; slot < row.page.nextSlot; slot++) { const o = pageOrder(side, row, slot); if (o) out.push(o); }
  return out;
};
const cachedDisposition = (opts: BookOptions): BookOptions => {
  const classify = opts.makerDisposition;
  if (!classify) return opts;
  const verdicts = new Map<string, MakerDisposition>();
  return { ...opts, makerDisposition: (maker) => { const hit = verdicts.get(maker.orderId); if (hit) return hit; const v = classify(maker); verdicts.set(maker.orderId, v); return v; } };
};
/** og findBestOrder: the top of `side` (with suspensions/dispositions, the first eligible maker in price-time order). */
const findBestOrder = (w: Book, side: BookSide, opts: BookOptions): BookOrder | null => {
  const rows = sideRows(w, side);
  if (!opts.suspendedOrderIds && !opts.makerDisposition) {
    const extreme = side === 0 ? rows[rows.length - 1] : rows[0], first = extreme && rows.find((r) => r.key.priceTicks === extreme.key.priceTicks);
    return first ? pageOrders(side, first)[0] ?? null : null;
  }
  for (const row of orderedPages(rows, side)) for (const o of pageOrders(side, row)) {
    if (opts.suspendedOrderIds?.has(o.orderId)) continue;
    if ((opts.makerDisposition?.(o) ?? "eligible") === "eligible") return o;
  }
  return null;
};
const bookCrosses = (side: BookSide, taker: bigint, maker: bigint): boolean => (side === 0 ? maker <= taker : maker >= taker);
const PRIME = 0x1_0000_01n;
/** og bumpHash: the rolling event hash (JS int32 mixing kept bit-exact). */
const bumpHash = (w: BookWork, tag: number, a: number | bigint, b: number | bigint): void => {
  const a32 = Number(BigInt(a) & 0xffff_ffffn), b32 = Number(BigInt(b) & 0xffff_ffffn);
  w.eventHash = (w.eventHash * PRIME + BigInt(((tag * 2_654_435_761) >>> 0) ^ a32 ^ (b32 << 7))) & 0x1f_ffff_ffff_ffffn;
};
/** og requireEntry. */
const bookEntryCode = (e: BookEntry): string | undefined => {
  const ob = utf8(e.orderId).length, wb = utf8(e.ownerId).length;
  if (ob === 0 || ob > 323) return `BOOK_PAGE_ORDER_ID_BYTES_INVALID:${ob}`;
  if (wb === 0 || wb > 66) return `BOOK_PAGE_OWNER_ID_BYTES_INVALID:${wb}`;
  if (e.qtyLots <= 0n || e.qtyLots > MAX_ORDERBOOK_QTY_LOTS) return "BOOK_PAGE_ORDER_QTY_INVALID";
  return !Number.isSafeInteger(e.seq) || e.seq < 0 ? "BOOK_PAGE_ORDER_SEQ_INVALID" : undefined;
};
const EMPTY_BOOK_PAGE: BookPage = { headSlot: 0, nextSlot: 0, liveCount: 0, totalQtyLots: 0n, slots: Array<BookEntry | null>(BOOK_PAGE_CAPACITY).fill(null) };
/** og addOrder + appendBookPricePageOrder: FIFO append to the price's tail page, opening the next page sequence when it is full. */
const addBookOrder = (w: BookWork, o: Omit<BookOrder, "pageSequence" | "pageSlot">): string | undefined => {
  const entry: BookEntry = { orderId: o.orderId, ownerId: o.ownerId, qtyLots: o.qtyLots, seq: o.seq };
  const bad = bookEntryCode(entry);
  if (bad !== undefined) return bad;
  if (o.priceTicks <= 0n) return `BOOK_PAGE_PRICE_INVALID:${o.priceTicks}`;
  const rows = sideRows(w, o.side);
  let tail: BookPageRow | undefined;
  for (const r of rows) if (r.key.priceTicks === o.priceTicks) tail = r;
  const sequence = tail?.page.nextSlot === BOOK_PAGE_CAPACITY ? (tail?.key.pageSequence ?? 0) + 1 : tail?.key.pageSequence ?? 0;
  if (sequence > 0xffff) return "BOOK_PAGE_SEQUENCE_EXHAUSTED";
  const page = tail?.key.pageSequence === sequence ? tail.page : EMPTY_BOOK_PAGE, slot = page.nextSlot, slots = [...page.slots];
  slots[slot] = entry;
  setPage(w, o.side, { priceTicks: o.priceTicks, pageSequence: sequence }, { headSlot: page.liveCount === 0 ? slot : page.headSlot, nextSlot: slot + 1, liveCount: page.liveCount + 1, totalQtyLots: page.totalQtyLots + entry.qtyLots, slots });
  w.orders.set(o.orderId, { ...o, pageSequence: sequence, pageSlot: slot });
  return undefined;
};
/** og removeOrder + removeBookPricePageOrder. */
const removeBookOrder = (w: BookWork, orderId: string): BookOrder | null => {
  const o = w.orders.get(orderId);
  if (!o) return null;
  const key = { priceTicks: o.priceTicks, pageSequence: o.pageSequence }, page = getPage(w, o.side, key);
  if (!page) return null;
  const slots = [...page.slots], entry = slots[o.pageSlot];
  if (!entry) return null;
  slots[o.pageSlot] = null;
  const liveCount = page.liveCount - 1;
  let head = page.headSlot;
  if (o.pageSlot === page.headSlot) { head = o.pageSlot + 1; while (head < slots.length && slots[head] === null) head++; }
  setPage(w, o.side, key, liveCount === 0 ? undefined : { ...page, headSlot: head, liveCount, totalQtyLots: page.totalQtyLots - entry.qtyLots, slots });
  w.orders.delete(orderId);
  return o;
};
/** og reduceOrder + reduceBookPricePageOrder. */
const reduceBookOrder = (w: BookWork, o: BookOrder, qtyLots: bigint): string | undefined => {
  if (qtyLots <= 0n || qtyLots > MAX_ORDERBOOK_QTY_LOTS) return "BOOK_PAGE_ORDER_QTY_INVALID";
  const key = { priceTicks: o.priceTicks, pageSequence: o.pageSequence }, page = getPage(w, o.side, key), entry = page?.slots[o.pageSlot];
  if (!page || !entry || entry.orderId !== o.orderId) return "BOOK_PAGE_LOCATION_MISMATCH";
  if (qtyLots >= entry.qtyLots) return "BOOK_PAGE_REDUCTION_INVALID";
  const slots = [...page.slots];
  slots[o.pageSlot] = { ...entry, qtyLots };
  setPage(w, o.side, key, { ...page, totalQtyLots: page.totalQtyLots - entry.qtyLots + qtyLots, slots });
  const indexed = w.orders.get(o.orderId);
  if (!indexed) return `BOOK_ORDER_INDEX_MISSING:${o.orderId}`;
  w.orders.set(o.orderId, { ...indexed, qtyLots });
  return undefined;
};
type BookTaker = { readonly side: BookSide; readonly ownerId: string; readonly orderId: string; readonly priceTicks: bigint; readonly qtyLots: bigint };
type Matched = { readonly remaining: bigint; readonly blockingOrderId?: string | undefined };
/** og matchPricePages: walk the opposite side in price-time order over the pre-match snapshot, publishing each touched page once. */
const matchBook = (w: BookWork, taker: BookTaker, events: BookEvent[], opts: BookOptions): Result<Matched, BookError> => {
  let remaining = taker.qtyLots;
  const makerSide: BookSide = taker.side === 0 ? 1 : 0;
  for (const row of orderedPages([...sideRows(w, makerSide)], makerSide)) {
    if (remaining <= 0n || !bookCrosses(taker.side, taker.priceTicks, row.key.priceTicks)) break;
    const src = row.page, slots = [...src.slots];
    let live = src.liveCount, total = src.totalQtyLots, stop = false, changed = false;
    const publish = (): void => {
      if (!changed) return;
      let head = src.headSlot;
      while (head < slots.length && slots[head] === null) head++;
      setPage(w, makerSide, row.key, live === 0 ? undefined : { ...src, headSlot: head, liveCount: live, totalQtyLots: total, slots });
    };
    for (let slot = src.headSlot; slot < src.nextSlot && remaining > 0n; slot++) {
      const entry = slots[slot];
      if (!entry) continue;
      const maker: BookOrder = { ...entry, side: makerSide, priceTicks: row.key.priceTicks, pageSequence: row.key.pageSequence, pageSlot: slot };
      if (opts.suspendedOrderIds?.has(maker.orderId)) continue;
      const disposition = opts.makerDisposition?.(maker) ?? "eligible";
      if (disposition === "suspended") continue;
      if (disposition === "cancel") {
        slots[slot] = null; live -= 1; total -= maker.qtyLots; w.orders.delete(maker.orderId); bumpHash(w, 5, maker.priceTicks, 0); changed = true;
        continue;
      }
      if (maker.ownerId === taker.ownerId && w.params.stpPolicy === 1) {
        publish();
        events.push({ type: "REJECT", orderId: taker.orderId, ownerId: taker.ownerId, reason: "STP cancel taker", blockingOrderId: maker.orderId });
        return ok({ remaining, blockingOrderId: maker.orderId });
      }
      const price = opts.executionPriceTicksForMatch?.(maker.priceTicks, taker.priceTicks, taker.side) ?? maker.priceTicks;
      if (price <= 0n) return bookErr("BOOK_EXECUTION_PRICE_INVALID");
      const multiple = opts.executionQtyMultipleAtPrice?.(price) ?? 1n;
      if (multiple <= 0n) return bookErr("BOOK_EXECUTION_QTY_MULTIPLE_INVALID");
      const qty = ((maker.qtyLots < remaining ? maker.qtyLots : remaining) / multiple) * multiple;
      if (qty <= 0n) { stop = true; break; }
      w.tradeCount += 1; w.tradeQtySum += qty; w.lastTradePriceTicks = price;
      bumpHash(w, 3, price, qty);
      events.push({ type: "TRADE", price, qty, makerOwnerId: maker.ownerId, takerOwnerId: taker.ownerId, makerOrderId: maker.orderId, takerOrderId: taker.orderId, makerQtyBefore: maker.qtyLots, takerQtyTotal: taker.qtyLots });
      remaining -= qty; total -= qty; changed = true;
      if (qty === maker.qtyLots) { slots[slot] = null; live -= 1; w.orders.delete(maker.orderId); continue; }
      const next = maker.qtyLots - qty, indexed = w.orders.get(maker.orderId);
      slots[slot] = { ...entry, qtyLots: next };
      if (!indexed) return bookErr(`BOOK_ORDER_INDEX_MISSING:${maker.orderId}`);
      w.orders.set(maker.orderId, { ...indexed, qtyLots: next });
      events.push({ type: "REDUCED", orderId: maker.orderId, ownerId: maker.ownerId, delta: -qty, remain: next });
      stop = true;
      break;
    }
    publish();
    if (stop) break;
  }
  return ok({ remaining });
};
/** og applyCommand: place (GTC/IOC/FOK, post-only, STP) or cancel; replace is refused. A refusal event leaves the book unchanged. */
export const applyBookCommand = (book: Book, cmd: OrderCmd, options: BookOptions = {}): Result<BookStep, BookError> => {
  const reject = (reason: string): Result<BookStep, BookError> => ok({ state: book, events: [{ type: "REJECT", orderId: cmd.orderId, ownerId: cmd.ownerId, reason }] });
  if (cmd.kind === 2) return reject("replace unsupported");
  const w = forkBook(book);
  if (cmd.kind === 1) {
    const existing = w.orders.get(cmd.orderId);
    if (!existing) return reject("not found");
    if (existing.ownerId !== cmd.ownerId) return reject("not owner");
    if (removeBookOrder(w, cmd.orderId) === null) return bookErr("BOOK_PAGE_LOCATION_MISMATCH");
    bumpHash(w, 5, existing.priceTicks, 0);
    return ok({ state: w, events: [{ type: "CANCELED", orderId: cmd.orderId, ownerId: cmd.ownerId }] });
  }
  const { ownerId, orderId, side, tif, postOnly, priceTicks, qtyLots } = cmd, opts = cachedDisposition(options);
  if (qtyLots <= 0n || qtyLots > MAX_ORDERBOOK_QTY_LOTS) return reject("qty out of range");
  if (priceTicks <= 0n) return reject("price must be positive");
  if (w.orders.has(orderId)) return reject("duplicate orderId");
  const opposite = postOnly ? findBestOrder(w, side === 0 ? 1 : 0, opts) : null;
  if (postOnly && opposite && bookCrosses(side, priceTicks, opposite.priceTicks)) return reject("postOnly would cross");
  const events: BookEvent[] = [];
  return chain(matchBook(w, { side, ownerId, orderId, priceTicks, qtyLots }, events, opts), (matched): Result<BookStep, BookError> => {
    if (tif === 2 && matched.remaining > 0n) return reject("FOK cannot fill entirely");
    if (matched.remaining > 0n && matched.blockingOrderId === undefined && tif === 0) {
      if (w.orders.size >= w.params.maxOrders) return bookErr("Out of order slots");
      const multiple = options.executionQtyMultipleAtPrice?.(priceTicks) ?? 1n;
      if (multiple <= 0n) return bookErr("BOOK_EXECUTION_QTY_MULTIPLE_INVALID");
      const resting = (matched.remaining / multiple) * multiple;
      if (resting > 0n) {
        const bad = addBookOrder(w, { orderId, ownerId, side, priceTicks, qtyLots: resting, seq: w.nextSeq });
        if (bad !== undefined) return bookErr(bad);
        w.nextSeq += 1;
        events.push({ type: "ACK", orderId, ownerId });
        bumpHash(w, 1, priceTicks, resting);
      }
    } else if (matched.remaining === qtyLots && events.length === 0) events.push({ type: "REJECT", orderId, ownerId, reason: "no fill" });
    return ok({ state: w, events });
  });
};
export type ResumedBook = BookStep & { readonly takerOrderId: string };
/** og resumeCrossedBook: when the eligible tops cross, the younger resting order takes against the older side. */
export const resumeCrossedBook = (book: Book, options: BookOptions = {}): Result<ResumedBook | null, BookError> => {
  const w = forkBook(book), opts = cachedDisposition(options);
  const bid = findBestOrder(w, 0, opts), ask = findBestOrder(w, 1, opts);
  if (!bid || !ask || bid.priceTicks < ask.priceTicks) return ok(null);
  if (bid.seq === ask.seq) return bookErr(`BOOK_CORRUPTION: crossed top orders share seq ${bid.seq}`);
  const taker = bid.seq > ask.seq ? bid : ask, events: BookEvent[] = [];
  return chain(matchBook(w, taker, events, opts), (matched): Result<ResumedBook | null, BookError> => {
    if (matched.blockingOrderId !== undefined) { removeBookOrder(w, taker.orderId); bumpHash(w, 5, taker.priceTicks, 0); }
    else if (matched.remaining === 0n) removeBookOrder(w, taker.orderId);
    else if (matched.remaining < taker.qtyLots) { const bad = reduceBookOrder(w, taker, matched.remaining); if (bad !== undefined) return bookErr(bad); }
    return events.length === 0 ? ok(null) : ok({ state: w, events, takerOrderId: taker.orderId });
  });
};
/** og materializeCommittedRemainder: rest a committed remainder at the book's next sequence. */
export const materializeCommittedRemainder = (book: Book, o: Pick<BookOrder, "orderId" | "ownerId" | "side" | "priceTicks" | "qtyLots">): Result<Book, BookError> => {
  if (o.qtyLots <= 0n || o.qtyLots > MAX_ORDERBOOK_QTY_LOTS) return bookErr("BOOK_REMAINDER_QTY_INVALID");
  if (o.priceTicks <= 0n) return bookErr("BOOK_REMAINDER_PRICE_INVALID");
  if (book.orders.has(o.orderId)) return bookErr("BOOK_REMAINDER_DUPLICATE");
  if (book.orders.size >= book.params.maxOrders) return bookErr("Out of order slots");
  const w = forkBook(book), bad = addBookOrder(w, { ...o, seq: w.nextSeq });
  if (bad !== undefined) return bookErr(bad);
  w.nextSeq += 1;
  bumpHash(w, 1, o.priceTicks, o.qtyLots);
  return ok(w);
};
/** og reduceBookOrderQuantity: committed cross-j fill progress. */
export const reduceBookOrderQuantity = (book: Book, orderId: string, nextQtyLots: bigint): Result<Book, BookError> => {
  const current = book.orders.get(orderId);
  if (!current) return bookErr(`BOOK_ORDER_INDEX_MISSING:${orderId}`);
  if (nextQtyLots <= 0n || nextQtyLots >= current.qtyLots) return bookErr(`BOOK_ORDER_REDUCTION_INVALID:${orderId}`);
  const w = forkBook(book), bad = reduceBookOrder(w, current, nextQtyLots);
  return bad === undefined ? ok(w) : bookErr(bad);
};
/** og recordAcceptedUsdAskPrice. */
export const recordAcceptedUsdAskPrice = (book: Book, priceTicks: bigint): Result<Book, BookError> => {
  if (priceTicks <= 0n) return bookErr("BOOK_USD_ASK_PRICE_INVALID");
  if (book.lastAcceptedUsdAskPriceTicks === priceTicks) return ok(book);
  const w = forkBook(book);
  w.lastAcceptedUsdAskPriceTicks = priceTicks;
  bumpHash(w, 4, priceTicks, 0);
  return ok(w);
};
export const bestBid = (b: Book): bigint | null => { const top = b.bidPages[b.bidPages.length - 1]; return top ? top.key.priceTicks : null; };
export const bestAsk = (b: Book): bigint | null => b.askPages[0]?.key.priceTicks ?? null;
/** og getBookOrders: resting orders by sequence. */
export const bookOrders = (b: Book): readonly BookOrder[] => [...b.orders.values()].sort((l, r) => l.seq - r.seq);
export type BookLevel = { readonly priceTicks: bigint; readonly qtyLots: bigint; readonly ownerIds: readonly string[]; readonly orderIds: readonly string[] };
/** og getBookSideLevels: aggregated price levels in priority order. */
export const bookSideLevels = (b: Book, side: BookSide, depth = 10): readonly BookLevel[] => {
  const levels: { priceTicks: bigint; qtyLots: bigint; ownerIds: string[]; orderIds: string[] }[] = [];
  for (const row of orderedPages(sideRows(b, side), side)) {
    let level = levels[levels.length - 1];
    if (!level || level.priceTicks !== row.key.priceTicks) {
      if (levels.length >= depth) break;
      level = { priceTicks: row.key.priceTicks, qtyLots: 0n, ownerIds: [], orderIds: [] };
      levels.push(level);
    }
    for (const o of pageOrders(side, row)) { level.qtyLots += o.qtyLots; if (!level.ownerIds.includes(o.ownerId)) level.ownerIds.push(o.ownerId); level.orderIds.push(o.orderId); }
  }
  return levels;
};
/** og bookOrdersOutsidePriceRange: orders priced below `min` or above `max` (asks/bids, low tail first then high tail per side). */
export const bookOrdersOutsidePriceRange = (b: Book, min: bigint, max: bigint): Result<readonly BookOrder[], BookError> => {
  if (min <= 0n || max < min) return bookErr("BOOK_PRICE_RANGE_INVALID");
  return ok(([0, 1] as const).flatMap((side) => {
    const rows = sideRows(b, side), low: BookOrder[] = [], high: BookOrder[] = [];
    for (const r of rows) { if (r.key.priceTicks >= min) break; low.push(...pageOrders(side, r)); }
    for (const r of orderedPages(rows, 0)) { if (r.key.priceTicks <= max) break; high.push(...pageOrders(side, r)); }
    return [...low, ...high];
  }));
};
// ---- hub order book inside entity consensus: og entity/tx/handlers/account/orderbook/{index,queue,cancels,helpers,same/*}.ts, orderbook/cross-j/orderbook.ts
// applyCommittedSwapCancelsToOrderbook, account/utils.ts pair policy, and the cross-j book pass (cross/*), remote cancel routing and book lifecycle (see the end of this file).
// og throws halts from deep inside the pass; here every halt is an `entity_invariant` with og's message, returned through the pass. ----
export type SwapRef = { readonly offerId: string; readonly accountId: string };
/** og SwapOfferEvent for a committed same-j offer (committed-input.ts buildSameJurisdictionSwapOfferEvent); og marks it accountOutputVerified. */
export type SwapOfferEvent = SwapRef & {
  readonly makerIsLeft: boolean; readonly fromEntity: string; readonly toEntity: string; readonly createdHeight: number;
  readonly giveTokenId: number; readonly giveTokenDecimals: number; readonly giveAmount: bigint; readonly wantTokenId: number; readonly wantTokenDecimals: number; readonly wantAmount: bigint;
  readonly maxFee: bigint; readonly minNetReceive: bigint; readonly priceTicks?: bigint | undefined; readonly timeInForce?: 0 | 1 | 2 | undefined;
  /** og SwapOfferEvent.crossJurisdiction: a cross-j order the book owner admitted (book-order.ts buildCommittedCrossJurisdictionOfferEvent). */
  readonly crossJurisdiction?: CrossRoute | undefined;
  /** og same-j-swap-output.ts: the Account output is the commitment evidence for a same-j offer (orderbook-admission.ts skips the committed-state re-read). */
  readonly accountOutputVerified?: true | undefined;
};
/** og CommittedAccountEffects swapOffersCreated / swapOffersCancelled / swapCancelRequests, collected over one Entity frame. */
export type SwapEvents = { readonly created: readonly SwapOfferEvent[]; readonly cancelled: readonly SwapRef[]; readonly cancelRequests: readonly SwapRef[] };
export const joinSwapEvents = (a: SwapEvents | undefined, b: SwapEvents | undefined): SwapEvents | undefined =>
  a === undefined ? b : b === undefined ? a : { created: [...a.created, ...b.created], cancelled: [...a.cancelled, ...b.cancelled], cancelRequests: [...a.cancelRequests, ...b.cancelRequests] };
const tif = (t: number | undefined): 0 | 1 | 2 | undefined => (t === 0 || t === 1 || t === 2 ? t : undefined);
/** og buildSameJurisdictionSwapOfferEvent from the Account's swapOfferUpsert snapshot. */
export const swapOfferEvent = (accountId: string, e: Of<Effect, "swap_offer_upsert">): SwapOfferEvent => ({
  offerId: e.offer.offerId, accountId, makerIsLeft: e.offer.makerIsLeft, fromEntity: e.left, toEntity: e.right, createdHeight: e.offer.createdHeight,
  giveTokenId: Number(e.offer.giveTokenId), giveTokenDecimals: e.offer.giveTokenDecimals, giveAmount: e.offer.giveAmount, wantTokenId: Number(e.offer.wantTokenId), wantTokenDecimals: e.offer.wantTokenDecimals, wantAmount: e.offer.wantAmount,
  maxFee: e.offer.maxFee, minNetReceive: e.offer.minNetReceive, priceTicks: e.offer.priceTicks, ...opt("timeInForce", tif(e.offer.timeInForce)), accountOutputVerified: true,
});
/** The matcher's view of one hub Account (og hubState.accounts row): status, committed offers, and the resolves already queued (mempool + our pending frame). */
export type HubAccount = { readonly active: boolean; readonly left: string; readonly right: string; readonly offers: ReadonlyMap<string, SwapOffer>; readonly queued: readonly WireAccountTx[];
  /** og account.state.pulls: the committed cross-j pulls the clear lifecycle reads (not read by the matcher). */
  readonly pulls?: ReadonlyMap<string, PullRow> | undefined };
export type Hub = {
  readonly id: string; readonly ext: OrderbookExt; readonly accounts: ReadonlyMap<string, HubAccount>; readonly takerFeeBps: number;
  /** og hubState.timestamp, crossJurisdictionSwaps and crossJurisdictionBookAdmissions: what the cross-j pass reads. */
  readonly timestamp?: number | undefined; readonly crossSwaps?: ReadonlyMap<string, CrossRoute> | undefined; readonly crossAdmissions?: BookAdmissions | undefined;
};
export type BookTx = { readonly accountId: string; readonly tx: AccountTx };
/** og MatchResult (same-j): Account txs in queue order, the final book of every touched pair in first-touch order, and the committed pair dimensions. */
export type BookMatch = { readonly accountTxs: readonly BookTx[]; readonly books: ReadonlyMap<string, Book>; readonly pairDimensions: ReadonlyMap<string, PairDimensions>; readonly crossFills: readonly CrossFillInstruction[] };
/** og NormalizedOrderbookOffer (same-j). */
type BookOffer = SwapRef & {
  readonly makerIsLeft: boolean; readonly fromEntity: string; readonly toEntity: string; readonly createdHeight: number;
  readonly giveTokenId: number; readonly giveTokenDecimals: number; readonly giveAmount: bigint; readonly wantTokenId: number; readonly wantTokenDecimals: number; readonly wantAmount: bigint;
  readonly maxFee: bigint; readonly minNetReceive: bigint; readonly priceTicks: bigint; readonly timeInForce: 0 | 1 | 2;
};
const halt = (message: string): Result<never, EntityError> => err({ _tag: "entity_invariant", reason: message });
const haltMessage = (e: EntityError): string => (e._tag === "entity_invariant" ? e.reason : e._tag);
const swapKeyOf = (accountId: string, offerId: string): string => `${accountId}:${offerId}`;
/** og parseNamespacedOrderId: the account id is everything before the last colon. */
const parseOrderId = (orderId: string, code: string): Result<SwapRef, EntityError> => {
  const i = orderId.lastIndexOf(":");
  return i <= 0 || i === orderId.length - 1 ? halt(`${code}: order=${orderId}`) : ok({ accountId: orderId.slice(0, i), offerId: orderId.slice(i + 1) });
};
const dimsOf = (give: number, want: number, giveDecimals: number, wantDecimals: number): SwapDims => {
  const side = swapSide(give, want);
  return side === 1 ? { side, bd: giveDecimals, qd: wantDecimals } : { side, bd: wantDecimals, qd: giveDecimals };
};
/** og normalizeSwapOfferForOrderbook: a missing price is recomputed from the amounts; a non-positive one halts. */
const normalizeOffer = (o: Omit<BookOffer, "priceTicks" | "timeInForce" | "accountId"> & { readonly priceTicks?: bigint | undefined; readonly timeInForce?: number | undefined }, accountId: string): Result<BookOffer, EntityError> => {
  const d = dimsOf(o.giveTokenId, o.wantTokenId, o.giveTokenDecimals, o.wantTokenDecimals);
  const priceTicks = o.priceTicks !== undefined && o.priceTicks > 0n ? o.priceTicks : priceTicksOf(d, d.side === 1 ? o.giveAmount : o.wantAmount, d.side === 1 ? o.wantAmount : o.giveAmount);
  if (priceTicks <= 0n) return halt(`ORDERBOOK_NORMALIZE_INVALID_PRICE: offer=${o.offerId}`);
  return ok({
    offerId: o.offerId, accountId, makerIsLeft: o.makerIsLeft, fromEntity: o.fromEntity, toEntity: o.toEntity, createdHeight: o.createdHeight,
    giveTokenId: o.giveTokenId, giveTokenDecimals: o.giveTokenDecimals, giveAmount: o.giveAmount, wantTokenId: o.wantTokenId, wantTokenDecimals: o.wantTokenDecimals, wantAmount: o.wantAmount,
    maxFee: o.maxFee, minNetReceive: o.minNetReceive, priceTicks, timeInForce: tif(o.timeInForce) ?? 0,
  });
};
/** og account/utils.ts SWAP_PAIR_POLICY_BY_BASE_QUOTE: book bucket width and MM mid price per base/quote. */
type PairPolicy = { readonly bucket: number; readonly mid: bigint };
const PAIR_POLICIES: ReadonlyMap<string, PairPolicy> = new Map([
  ["2/1", { bucket: 10_000, mid: 25_000_000n }], ["2/3", { bucket: 10_000, mid: 25_000_000n }], ["1/3", { bucket: 10_000, mid: 10_000n }],
  ["4/1", { bucket: 100, mid: 1_200n }], ["4/3", { bucket: 100, mid: 1_200n }], ["5/1", { bucket: 10, mid: 200n }], ["5/3", { bucket: 10, mid: 200n }],
]);
const DEFAULT_PAIR_POLICY: PairPolicy = { bucket: 10_000, mid: 10_000n };
/** og getSwapPairOrientation: a reference stable is the quote; the book is keyed by the numerically ordered pair. */
const canonicalPairOf = (a: number, b: number): { readonly base: number; readonly quote: number; readonly pairId: string } => {
  const lo = Math.min(a, b), hi = Math.max(a, b), al = REFERENCE_STABLES.has(a), bl = REFERENCE_STABLES.has(b);
  const [base, quote] = al && !bl ? [b, a] : !al && bl ? [a, b] : [lo, hi];
  return { base, quote, pairId: `${lo}/${hi}` };
};
/** og hasSwapPairPolicyForDimensions: the static policy is authority only when both signed decimals match the built-in tokens. */
const pairPolicyOf = (base: number, quote: number, bd: number, qd: number): { readonly policy: PairPolicy; readonly explicit: boolean } => {
  const policy = PAIR_POLICIES.get(`${base}/${quote}`);
  const explicit = policy !== undefined && TOKEN_DECIMALS.get(base) === bd && TOKEN_DECIMALS.get(quote) === qd;
  return explicit ? { policy, explicit } : { policy: DEFAULT_PAIR_POLICY, explicit };
};
const MAX_ORDERBOOK_ORDERS_PER_PAIR = 10_000, PRICE_REJECT_BPS = 3_000n;
/** og resolvePairBandReference. */
const bandAnchor = (p: { readonly policy: PairPolicy; readonly explicit: boolean }, bid: bigint | null, ask: bigint | null): bigint | null =>
  bid !== null && ask !== null ? (bid + ask) / 2n : bid !== null ? bid : ask !== null ? ask : p.explicit ? p.policy.mid : null;
/** og deriveSameOrderbookPriceBandBounds: +/-30% around the anchor. */
const bandBounds = (anchor: bigint): Result<{ readonly min: bigint; readonly max: bigint }, EntityError> => {
  if (anchor <= 0n) return halt("ORDERBOOK_PRICE_BAND_ANCHOR_INVALID");
  const offset = (anchor * PRICE_REJECT_BPS) / 10_000n;
  return ok({ min: anchor - offset, max: anchor + offset });
};
type Materialized = {
  readonly offer: BookOffer; readonly bookKey: string; readonly bd: number; readonly qd: number; readonly side: BookSide; readonly priceTicks: bigint; readonly qtyLots: bigint;
  readonly makerId: string; readonly orderId: string; readonly pair: { readonly policy: PairPolicy; readonly explicit: boolean };
};
type Prepared = Materialized & { readonly book: Book; readonly bestBid: bigint | null; readonly bestAsk: bigint | null };
/** og deriveSameOrderbookMaterialization: direction, amounts, minimum trade size, lot and exact-quote alignment, then the order's bounds. */
const materialization = (o: BookOffer, minTradeSize: bigint): Result<Tagged<"ok", { m: Materialized }> | Tagged<"reject", { reason: string }>, EntityError> => {
  const { base, quote, pairId } = canonicalPairOf(o.giveTokenId, o.wantTokenId), side = swapSide(o.giveTokenId, o.wantTokenId);
  const amounts = o.giveTokenId === base && o.wantTokenId === quote ? { b: o.giveAmount, q: o.wantAmount } : o.giveTokenId === quote && o.wantTokenId === base ? { b: o.wantAmount, q: o.giveAmount } : undefined;
  const reject = (reason: string) => ok({ _tag: "reject" as const, reason });
  if (amounts === undefined) return reject("invalid-direction");
  const d = dimsOf(o.giveTokenId, o.wantTokenId, o.giveTokenDecimals, o.wantTokenDecimals), pair = pairPolicyOf(base, quote, d.bd, d.qd), lot = lotScale(d.bd);
  if (amounts.b <= 0n || amounts.q <= 0n) return reject("zero-amount");
  if (minTradeSize > 0n && amounts.q < minTradeSize) return reject(`below-minTradeSize:${amounts.q}`);
  if (amounts.b % lot !== 0n) return reject(`lot-misaligned:${amounts.b}`);
  if (o.priceTicks <= 0n) return halt("SWAP_EXACT_QUOTE_PRICE_INVALID");
  const qtyLots = amounts.b / lot, multiple = exactQuoteLots(d.bd, d.qd, o.priceTicks);
  if (qtyLots % multiple !== 0n) return reject(`quote-lot-misaligned:${qtyLots}:${multiple}`);
  if (qtyLots === 0n || qtyLots > MAX_ORDERBOOK_QTY_LOTS) return reject(`invalid-order:${qtyLots}:${o.priceTicks}`);
  return ok({ _tag: "ok", m: { offer: o, bookKey: pairId, bd: d.bd, qd: d.qd, side, priceTicks: o.priceTicks, qtyLots, makerId: o.makerIsLeft ? o.fromEntity : o.toEntity, orderId: swapKeyOf(o.accountId, o.offerId), pair } });
};
/** One og same-j pass: the hot book cache, queued resolves, suspended makers and committed dimensions. Local to processOrderbookSwaps. */
type Pass = {
  readonly hub: Hub; readonly accountTxs: BookTx[]; readonly queued: Set<string>; readonly suspended: Set<string>; readonly meta: Map<string, BookOffer>; readonly swept: Set<string>;
  readonly cache: Map<string, Book>; readonly updates: Map<string, Book>; readonly dims: Map<string, PairDimensions>;
};
const newPass = (hub: Hub): Pass => ({ hub, accountTxs: [], queued: new Set(), suspended: new Set(), meta: new Map(), swept: new Set(), cache: new Map(), updates: new Map(), dims: new Map(hub.ext.pairDimensions) });
/** og hasQueuedSwapResolveForEntityState. */
const hasQueuedResolve = (pass: Pick<Pass, "hub" | "queued">, accountId: string, offerId: string): boolean =>
  pass.queued.has(swapKeyOf(accountId, offerId)) || (pass.hub.accounts.get(accountId)?.queued ?? []).some((tx) => tx.type === "swap_resolve" && tx.offerId === offerId);
/** og queueUniqueSwapResolveForEntityState. */
const queueUniqueResolve = (pass: Pick<Pass, "hub" | "queued" | "accountTxs">, accountId: string, data: SwapResolveTerms): boolean => {
  if (hasQueuedResolve(pass, accountId, data.offerId)) return false;
  pass.queued.add(swapKeyOf(accountId, data.offerId));
  pass.accountTxs.push({ accountId, tx: { type: "swap_resolve", ...data } });
  return true;
};
const cancelTerms = (offerId: string, comment: string): SwapResolveTerms => ({ offerId, fillRatio: 0, cancelRemainder: true, comment });
/** og queueSameSwapResolve: the resolving row never trades again in this pass, even when an identical resolve was already queued. */
const queueSame = (pass: Pass, accountId: string, data: SwapResolveTerms): boolean => {
  const queued = queueUniqueResolve(pass, accountId, data);
  pass.suspended.add(swapKeyOf(accountId, data.offerId));
  return queued;
};
/** og buildLiveSameOfferMeta: the committed Account offer behind a book row; a cross-j or missing offer has none. */
const liveMeta = (pass: Pass, orderId: string): Result<BookOffer | null, EntityError> => chain(parseOrderId(orderId, "ORDERBOOK_MALFORMED_BOOK_ORDER"), ({ accountId, offerId }) => {
  const account = pass.hub.accounts.get(accountId), o = account?.offers.get(offerId);
  if (account === undefined || o === undefined || o.crossJurisdiction !== undefined) return ok(null);
  return normalizeOffer({ offerId, makerIsLeft: o.makerIsLeft, fromEntity: account.left, toEntity: account.right, createdHeight: o.createdHeight, giveTokenId: Number(o.giveTokenId), giveTokenDecimals: o.giveTokenDecimals,
    giveAmount: o.giveAmount, wantTokenId: Number(o.wantTokenId), wantTokenDecimals: o.wantTokenDecimals, wantAmount: o.wantAmount, maxFee: o.maxFee, minNetReceive: o.minNetReceive, priceTicks: o.priceTicks, timeInForce: o.timeInForce }, accountId);
});
/** og classifySameBookMaker: an inactive Account cancels, a resolving row is suspended, and the row must equal its committed offer. */
const classifyMaker = (pass: Pass, pairId: string, order: BookOrder): Result<MakerDisposition, EntityError> => chain(parseOrderId(order.orderId, "ORDERBOOK_MALFORMED_BOOK_ORDER"), ({ accountId }) => {
  const account = pass.hub.accounts.get(accountId);
  if (account === undefined) return halt(`ORDERBOOK_SAME_SNAPSHOT_MISSING: pair=${pairId} order=${order.orderId}`);
  if (!account.active) return ok("cancel");
  const cached = pass.meta.get(order.orderId);
  return chain(cached === undefined ? liveMeta(pass, order.orderId) : ok(cached), (meta): Result<MakerDisposition, EntityError> => {
    if (meta === null) return halt(`ORDERBOOK_SAME_SNAPSHOT_MISSING: pair=${pairId} order=${order.orderId}`);
    if (hasQueuedResolve(pass, meta.accountId, meta.offerId)) return ok("suspended");
    pass.meta.set(order.orderId, meta);
    const d = dimsOf(meta.giveTokenId, meta.wantTokenId, meta.giveTokenDecimals, meta.wantTokenDecimals), baseAmount = d.side === 1 ? meta.giveAmount : meta.wantAmount;
    const owner = meta.makerIsLeft ? meta.fromEntity : meta.toEntity, qtyLots = baseAmount / lotScale(d.bd);
    if (order.side !== d.side || order.priceTicks !== meta.priceTicks || order.ownerId !== owner || order.qtyLots !== qtyLots)
      return halt(`ORDERBOOK_CACHE_MISMATCH: pair=${pairId} order=${order.orderId} storedOwner=${order.ownerId} canonicalOwner=${owner} storedSide=${order.side} canonicalSide=${d.side} `
        + `storedPrice=${order.priceTicks} canonicalPrice=${meta.priceTicks} storedQtyLots=${order.qtyLots} canonicalQtyLots=${qtyLots}`);
    return ok("eligible");
  });
});
/** og applyCommand options for a same-j pair; a halt raised while classifying a maker is kept and surfaces after the command. */
const bookOptions = (pass: Pass, pairId: string, bd: number, qd: number): { readonly options: BookOptions; readonly fault: () => EntityError | undefined } => {
  let fault: EntityError | undefined;
  return {
    fault: () => fault,
    options: {
      suspendedOrderIds: pass.suspended,
      makerDisposition: (maker) => { if (fault !== undefined) return "suspended"; const r = classifyMaker(pass, pairId, maker); if (!r.ok) { fault = r.error; return "suspended"; } return r.value; },
      executionQtyMultipleAtPrice: (price) => exactQuoteLots(bd, qd, price),
    },
  };
};
/** og containSamePairFailure (live mode): a failed pair command halts the frame. */
const pairFailure = (pairId: string, accountId: string, offerId: string, message: string): Result<never, EntityError> =>
  halt(`ORDERBOOK_PAIR_COMMAND_FAILED: pair=${pairId} account=${accountId} offer=${offerId} error=${message}`);
/** og sweepSamePairOutOfBandOffers: once per pair and pass, every resting row outside the anchor band leaves the book. */
const sweepPair = (pass: Pass, pairId: string, pair: Materialized["pair"], book: Book): Result<Book, EntityError> => {
  const anchor = bandAnchor(pair, bestBid(book), bestAsk(book));
  if (anchor === null) return ok(book);
  return chain(bandBounds(anchor), ({ min, max }) => chain(mapErr(bookOrdersOutsidePriceRange(book, min, max), (e) => ({ _tag: "entity_invariant", reason: e.code }) as EntityError), (outside) => {
    let next = book, removed = 0;
    for (const order of outside) {
      const d = classifyMaker(pass, pairId, order);
      if (!d.ok) return d;
      if (d.value === "suspended") continue;
      const live = d.value === "eligible" ? liveMeta(pass, order.orderId) : ok(null);
      if (!live.ok) return live;
      removed += 1;
      const cancelled = applyBookCommand(next, { kind: 1, ownerId: order.ownerId, orderId: order.orderId });
      if (!cancelled.ok) return halt(cancelled.error.code);
      next = cancelled.value.state;
      if (live.value !== null) queueSame(pass, live.value.accountId, cancelTerms(live.value.offerId, `outside-anchor-band:${order.priceTicks}`));
    }
    return ok(removed === 0 ? book : next);
  }));
};
/** og materializeSameOffer: a resolving offer is skipped; a malformed one or one against the pair's committed dimensions is cancelled. */
const materializeSame = (pass: Pass, o: BookOffer, minTradeSize: bigint): Result<Materialized | null, EntityError> => {
  if (hasQueuedResolve(pass, o.accountId, o.offerId)) return ok(null);
  return map(materialization(o, minTradeSize), (r) => {
    if (r._tag === "reject") { queueSame(pass, o.accountId, cancelTerms(o.offerId, r.reason)); return null; }
    const committed = pass.dims.get(r.m.bookKey);
    if (committed !== undefined && (committed.baseTokenDecimals !== r.m.bd || committed.quoteTokenDecimals !== r.m.qd)) { queueSame(pass, o.accountId, cancelTerms(o.offerId, "pair-decimals-mismatch")); return null; }
    return r.m;
  });
};
/** og prepareSameOffer: the pair's hot book (or a fresh one), its one sweep, then the price band. */
const prepareSame = (pass: Pass, m: Materialized): Result<Prepared | null, EntityError> => {
  const cached = pass.cache.get(m.bookKey), committed = cached === undefined ? pass.hub.ext.books.get(m.bookKey) : undefined;
  if (committed !== undefined) pass.cache.set(m.bookKey, committed);
  const held = cached ?? committed;
  const fresh: Result<Book, EntityError> = held !== undefined ? ok(held)
    : mapErr(createBook({ bucketWidthTicks: BigInt(Math.max(1, m.pair.policy.bucket)), maxOrders: MAX_ORDERBOOK_ORDERS_PER_PAIR, stpPolicy: 1 }), (e) => ({ _tag: "entity_invariant", reason: e.code }) as EntityError);
  return chain(fresh, (start) => chain(pass.swept.has(m.bookKey) ? ok(start) : (pass.swept.add(m.bookKey), sweepPair(pass, m.bookKey, m.pair, start)), (book) => {
    if (book !== start) { pass.cache.set(m.bookKey, book); pass.updates.set(m.bookKey, book); }
    const bid = bestBid(book), ask = bestAsk(book), anchor = bandAnchor(m.pair, bid, ask);
    return chain(anchor === null ? ok(undefined) : bandBounds(anchor), (bounds): Result<Prepared | null, EntityError> => {
      if (bounds !== undefined && (m.priceTicks < bounds.min || m.priceTicks > bounds.max)) { queueSame(pass, m.offer.accountId, cancelTerms(m.offer.offerId, `outside-anchor-band:${m.priceTicks}`)); return ok(null); }
      pass.meta.set(m.orderId, { ...m.offer, priceTicks: m.priceTicks });
      return ok({ ...m, book, bestBid: bid, bestAsk: ask });
    });
  }));
};
/** og keepIdenticalRestingOrder: an offer already resting unchanged stays; a changed one halts. */
const keepResting = (pass: Pass, p: Prepared): Result<boolean, EntityError> => {
  const existing = p.book.orders.get(p.orderId);
  if (existing === undefined) return ok(false);
  if (existing.ownerId !== p.makerId || existing.side !== p.side || existing.qtyLots !== p.qtyLots || existing.priceTicks !== p.priceTicks) return halt(`ORDERBOOK_CACHE_MISMATCH: pair=${p.bookKey} order=${p.orderId}`);
  pass.cache.set(p.bookKey, p.book);
  return ok(true);
};
/** og applySameOfferCommand: place the offer; a full book cancels it, any other refusal halts. */
const placeSame = (pass: Pass, p: Prepared): Result<BookStep | null, EntityError> => {
  const { options, fault } = bookOptions(pass, p.bookKey, p.bd, p.qd);
  const r = applyBookCommand(p.book, { kind: 0, ownerId: p.makerId, orderId: p.orderId, side: p.side, tif: p.offer.timeInForce, postOnly: false, priceTicks: p.priceTicks, qtyLots: p.qtyLots }, options);
  const f = fault();
  if (f !== undefined) return pairFailure(p.bookKey, p.offer.accountId, p.offer.offerId, haltMessage(f));
  if (r.ok) return ok(r.value);
  if (r.error.code !== "Out of order slots") return pairFailure(p.bookKey, p.offer.accountId, p.offer.offerId, r.error.code);
  queueSame(pass, p.offer.accountId, cancelTerms(p.offer.offerId, `book-full:${p.book.params.maxOrders}`));
  return ok(null);
};
/** og resumeCrossedSameBook. */
const resumeSame = (pass: Pass, p: Prepared): Result<ResumedBook | null, EntityError> => {
  const { options, fault } = bookOptions(pass, p.bookKey, p.bd, p.qd);
  const r = resumeCrossedBook(p.book, options), f = fault();
  if (f !== undefined) return pairFailure(p.bookKey, p.offer.accountId, p.offer.offerId, haltMessage(f));
  return r.ok ? ok(r.value) : pairFailure(p.bookKey, p.offer.accountId, p.offer.offerId, r.error.code);
};
/** og SwapNetAuthorizationError (a fee-authority refusal of one fill) versus a halt. */
type FillFault = Tagged<"auth"> | Tagged<"halt", { message: string }>;
const AUTH: FillFault = { _tag: "auth" };
const fillHalt = (message: string): Result<never, FillFault> => err({ _tag: "halt", message });
const fromEntityError = <T,>(r: Result<T, EntityError>): Result<T, FillFault> => mapErr(r, (e): FillFault => ({ _tag: "halt", message: haltMessage(e) }));
type ExecOffer = { readonly giveTokenId: number; readonly giveTokenDecimals: number; readonly wantTokenId: number; readonly wantTokenDecimals: number; readonly giveAmount: bigint; readonly wantAmount: bigint; readonly quantizedGive: bigint; readonly quantizedWant: bigint; readonly maxFee: bigint; readonly minNetReceive: bigint; readonly priceTicks: bigint };
type RestingTerms = { readonly giveTokenId: number; readonly giveTokenDecimals: number; readonly wantTokenId: number; readonly wantTokenDecimals: number; readonly giveAmount: bigint; readonly wantAmount: bigint; readonly maxFee: bigint; readonly minNetReceive: bigint };
/** og materializeCanonicalRestingOffer: the resting row's original lots at its resting price, as give/want. */
const canonicalResting = (r: RestingTerms, priceTicks: bigint, qtyLots: bigint): Omit<ExecOffer, "maxFee" | "minNetReceive"> => {
  const d = dimsOf(r.giveTokenId, r.wantTokenId, r.giveTokenDecimals, r.wantTokenDecimals), base = qtyLots <= 0n ? 0n : qtyLots * lotScale(d.bd), quote = quoteAt(d.bd, d.qd, base, priceTicks);
  const [give, want] = d.side === 1 ? [base, quote] : [quote, base];
  return { giveTokenId: r.giveTokenId, giveTokenDecimals: r.giveTokenDecimals, wantTokenId: r.wantTokenId, wantTokenDecimals: r.wantTokenDecimals, giveAmount: give, wantAmount: want, quantizedGive: give, quantizedWant: want, priceTicks };
};
/** og deriveSwapFillPolicyFee: the hub's taker fee, bounded by the same pro-rata authority as the maker's signed limit. */
const policyFee = (o: { readonly giveAmount: bigint; readonly wantAmount: bigint }, fG: bigint, fW: bigint, bps: number, closes: boolean): Result<bigint, FillFault> => {
  if (o.wantAmount <= 0n || !Number.isSafeInteger(bps) || bps < 0 || bps > 10_000) return err(AUTH);
  const maxFee = (o.wantAmount * BigInt(bps)) / 10_000n, policy = { maxFee, minNetReceive: o.wantAmount - maxFee };
  if (policy.maxFee >= o.wantAmount || policy.minNetReceive <= 0n || offerAuthError({ giveAmount: 1n, wantAmount: o.wantAmount, ...policy }) !== undefined) return err(AUTH);
  const authorized = { ...o, ...policy };
  if (offerAuthError(authorized) !== undefined || fG < 0n || fG > o.giveAmount || fW < 0n) return err(AUTH);
  let num = fG, den = o.giveAmount;
  if (closes) { const capped = fW < o.wantAmount ? fW : o.wantAmount; if (capped * den > num * o.wantAmount) { num = capped; den = o.wantAmount; } }
  return ok((policy.maxFee * num) / den);
};
type SameFill = { filledLots: bigint; readonly originalLots: bigint; weightedCost: bigint };
/** og aggregateSameTradeFills: per order, maker then taker, in trade order. */
const aggregateFills = (trades: readonly Extract<BookEvent, { type: "TRADE" }>[]): ReadonlyMap<string, SameFill> => {
  const fills = new Map<string, SameFill>();
  for (const t of trades) for (const [orderId, original] of [[t.makerOrderId, t.makerQtyBefore], [t.takerOrderId, t.takerQtyTotal]] as const) {
    const e = fills.get(orderId);
    if (e === undefined) fills.set(orderId, { filledLots: t.qty, originalLots: original, weightedCost: t.price * t.qty });
    else { e.filledLots += t.qty; e.weightedCost += t.price * t.qty; }
  }
  return fills;
};
/** og buildSameFillResolvePlan: the exact execution of one fill as a swap_resolve, authorized against the executing offer. */
const fillResolve = (pass: Pass, p: Prepared, book: Book, ref: SwapRef, orderId: string, fill: SameFill, account: HubAccount, comment: string | undefined): Result<SwapResolveTerms, FillFault> => {
  const { filledLots, originalLots, weightedCost } = fill, taker = orderId === p.orderId;
  if (filledLots <= 0n || weightedCost <= 0n) return fillHalt(`ORDERBOOK_FILL_LOOKUP_FAILED: invalid fill aggregate weightedCost=${weightedCost} filledLots=${filledLots}`);
  if (!taker && weightedCost % filledLots !== 0n) return fillHalt(`ORDERBOOK_FILL_LOOKUP_FAILED: non-integral resting price weightedCost=${weightedCost} filledLots=${filledLots}`);
  const restingPrice = weightedCost / filledLots;
  const exec: Result<ExecOffer, FillFault> = (() => {
    if (taker) return ok({ ...p.offer, quantizedGive: p.offer.giveAmount, quantizedWant: p.offer.wantAmount });
    const meta = pass.meta.get(orderId), stored = account.offers.get(ref.offerId);
    const resting: RestingTerms | undefined = meta ?? (stored === undefined ? undefined : { ...stored, giveTokenId: Number(stored.giveTokenId), wantTokenId: Number(stored.wantTokenId) });
    if (resting === undefined) return fillHalt(`ORDERBOOK_FILL_SOURCE_MISSING: order=${orderId} pair=${p.bookKey} account=${ref.accountId} offer=${ref.offerId}`);
    const canonical = canonicalResting(resting, restingPrice, originalLots), auth = requantizeAuth(resting, canonical.giveAmount, canonical.wantAmount);
    return auth.ok ? ok({ ...canonical, ...auth.value }) : err(AUTH);
  })();
  return chain(exec, (o) => {
    const d = dimsOf(o.giveTokenId, o.wantTokenId, o.giveTokenDecimals, o.wantTokenDecimals), lot = lotScale(d.bd);
    const execBase = filledLots * lot, execQuote = quoteAt(d.bd, d.qd, lot, weightedCost), cancelRemainder = !book.orders.has(orderId);
    // og buildSwapResolveDataFromOrderbookFill
    const eG = d.side === 0 ? execQuote : execBase, eW = d.side === 0 ? execBase : execQuote, executed = eG > 0n && eW > 0n;
    const exact = executed ? exactFillRatio(o.quantizedGive, eG) : { n: 0n, d: 1n };
    const base: SwapResolveTerms = {
      offerId: ref.offerId, restingGiveTokenId: String(o.giveTokenId) as TokenId, restingWantTokenId: String(o.wantTokenId) as TokenId, fillRatio: Math.min(fillRatioOf(exact), MAX_FILL), fillNumerator: exact.n, fillDenominator: exact.d, cancelRemainder,
      ...(executed ? { executionGiveAmount: eG, executionWantAmount: eW } : {}), restingPriceTicks: o.priceTicks, restingGiveAmount: o.giveAmount, restingWantAmount: o.wantAmount, restingQuantizedGive: o.quantizedGive, restingQuantizedWant: o.quantizedWant,
      ...(taker && comment ? { comment } : {}),
    };
    return chain(taker ? policyFee(o, executed ? eG : 0n, executed ? eW : 0n, pass.hub.takerFeeBps, cancelRemainder) : ok(0n), (fee): Result<SwapResolveTerms, FillFault> => {
      const data: SwapResolveTerms = fee > 0n ? { ...base, feeTokenId: String(o.wantTokenId) as TokenId, feeAmount: fee } : base;
      return netAuthError(o, { give: data.executionGiveAmount ?? 0n, want: data.executionWantAmount ?? 0n, fee: data.feeAmount ?? 0n }, cancelRemainder) === undefined ? ok(data) : err(AUTH);
    });
  });
};
/** og isAuthorizedUsdReferenceAsk: only the hub's USD quote authority, selling a volatile base for the reference token, moves the USD reference. */
const usdReferenceAsk = (profile: HubProfile, p: Prepared): boolean =>
  p.side === 1 && p.offer.giveTokenId !== profile.referenceTokenId && p.offer.wantTokenId === profile.referenceTokenId && p.makerId.toLowerCase() === profile.usdQuoteAuthorityEntityId.toLowerCase();
/** og processSameCommandEvents: an unfilled reject cancels the offer; fills are all authorized before any is queued. Returns the book to commit. */
const commandEvents = (pass: Pass, p: Prepared, result: BookStep): Result<Book, FillFault> => {
  const rejects = result.events.flatMap((e) => (e.type === "REJECT" && e.orderId === p.orderId ? [e] : []));
  const trades = result.events.flatMap((e) => (e.type === "TRADE" ? [e] : []));
  const stp = rejects.find((e) => e.reason === "STP cancel taker"), comment = stp === undefined ? undefined : `STP:${String(stp.blockingOrderId || "")}`;
  if (rejects.length > 0 && trades.length === 0) {
    const reasons = rejects.map((e) => e.reason).filter(Boolean).join(", ");
    queueSame(pass, p.offer.accountId, cancelTerms(p.offer.offerId, comment ?? `post-only-reject:${reasons || "unknown"}`));
    return ok(result.state);
  }
  const recorded = usdReferenceAsk(pass.hub.ext.hubProfile, p) ? mapErr(recordAcceptedUsdAskPrice(result.state, p.priceTicks), (e): FillFault => ({ _tag: "halt", message: e.code })) : ok(result.state);
  return chain(recorded, (book) => {
    const plans: { readonly accountId: string; readonly data: SwapResolveTerms }[] = [];
    for (const [orderId, fill] of aggregateFills(trades)) {
      const ref = fromEntityError(parseOrderId(orderId, "ORDERBOOK_FILL_LOOKUP_FAILED"));
      if (!ref.ok) return ref;
      if (hasQueuedResolve(pass, ref.value.accountId, ref.value.offerId)) return fillHalt(`ORDERBOOK_TRADE_PARTICIPANT_ALREADY_RESOLVING: account=${ref.value.accountId} offer=${ref.value.offerId}`);
      const account = pass.hub.accounts.get(ref.value.accountId);
      if (account === undefined) return fillHalt(`ORDERBOOK_ACCOUNT_LOOKUP_FAILED: offer=${ref.value.offerId} accountId=${ref.value.accountId}`);
      const plan = fillResolve(pass, p, book, ref.value, orderId, fill, account, comment);
      if (!plan.ok) return plan;
      plans.push({ accountId: ref.value.accountId, data: plan.value });
    }
    for (const plan of plans) queueSame(pass, plan.accountId, plan.data);
    return ok(book);
  });
};
/** og commitSameCommandResult: a fee-authority refusal cancels the taker instead; anything else halts the pair. */
const commitSame = (pass: Pass, p: Prepared, result: BookStep): Result<void, EntityError> => {
  const r = commandEvents(pass, p, result);
  if (!r.ok) {
    if (r.error._tag === "auth") { queueSame(pass, p.offer.accountId, cancelTerms(p.offer.offerId, "fee-authorization-exceeded")); return ok(undefined); }
    return pairFailure(p.bookKey, p.offer.accountId, p.offer.offerId, r.error.message);
  }
  pass.dims.set(p.bookKey, { baseTokenDecimals: p.bd, quoteTokenDecimals: p.qd });
  pass.cache.set(p.bookKey, r.value);
  pass.updates.set(p.bookKey, r.value);
  return ok(undefined);
};
/** og prepareCrossedRestingTaker: the younger crossed row must still be its committed offer, unchanged. */
const crossedTaker = (pass: Pass, trigger: Prepared, takerOrderId: string, minTradeSize: bigint): Result<Prepared, EntityError> => chain(liveMeta(pass, takerOrderId), (live) => {
  if (live === null) return halt(`ORDERBOOK_SAME_SNAPSHOT_MISSING: pair=${trigger.bookKey} order=${takerOrderId}`);
  return chain(materializeSame(pass, live, minTradeSize), (m) => chain(m === null ? ok(null) : prepareSame(pass, m), (p) =>
    chain(p === null ? ok(false) : keepResting(pass, p), (kept): Result<Prepared, EntityError> => (p === null || !kept ? halt(`ORDERBOOK_CACHE_MISMATCH: pair=${trigger.bookKey} order=${takerOrderId}`) : ok(p)))));
});
/** og drainCrossedSameBook: settle crossed resting rows until the book no longer crosses, bounded by its order count. */
const drainSame = (pass: Pass, seed: Prepared, minTradeSize: bigint): Result<void, EntityError> => {
  let current = seed;
  const limit = seed.book.orders.size;
  for (let resumedCount = 0; ; resumedCount += 1) {
    const resumed = resumeSame(pass, current);
    if (!resumed.ok) return resumed;
    if (resumed.value === null) return ok(undefined);
    if (resumedCount >= limit) return halt(`ORDERBOOK_SAME_DRAIN_NON_TERMINATING: pair=${seed.bookKey} limit=${limit}`);
    const taker = crossedTaker(pass, current, resumed.value.takerOrderId, minTradeSize);
    if (!taker.ok) return taker;
    const committed = commitSame(pass, taker.value, resumed.value);
    if (!committed.ok) return committed;
    const next = pass.cache.get(seed.bookKey);
    if (next === undefined) return halt(`ORDERBOOK_CACHE_MISMATCH: pair=${seed.bookKey} missing-after-resume`);
    current = { ...current, book: next };
  }
};
/** og findSamePairResumeSeed: the first eligible resting row, by sequence, that is still its committed offer. */
const resumeSeed = (pass: Pass, pairId: string, minTradeSize: bigint): Result<Prepared | null, EntityError> => {
  const book = pass.cache.get(pairId) ?? pass.hub.ext.books.get(pairId);
  if (book === undefined) return ok(null);
  for (const order of bookOrders(book)) {
    const d = classifyMaker(pass, pairId, order);
    if (!d.ok) return d;
    if (d.value !== "eligible") continue;
    const live = liveMeta(pass, order.orderId);
    if (!live.ok) return live;
    if (live.value === null) continue;
    const m = materializeSame(pass, live.value, minTradeSize);
    if (!m.ok) return m;
    const p = m.value === null ? ok(null) : prepareSame(pass, m.value);
    if (!p.ok) return p;
    if (p.value === null) continue;
    const kept = keepResting(pass, p.value);
    if (!kept.ok) return kept;
    if (kept.value) return ok(p.value);
  }
  return ok(null);
};
/** og processSameOrderbookOffer. */
const processSameOffer = (pass: Pass, o: BookOffer, minTradeSize: bigint): Result<void, EntityError> =>
  chain(materializeSame(pass, o, minTradeSize), (m) => chain(m === null ? ok(null) : prepareSame(pass, m), (p) => {
    if (p === null) return ok(undefined);
    return chain(keepResting(pass, p), (kept) => (kept ? drainSame(pass, p, minTradeSize) : chain(placeSame(pass, p), (step) => (step === null ? ok(undefined) : commitSame(pass, p, step)))));
  }));
const compareText = (a: string, b: string): number => (a < b ? -1 : a > b ? 1 : 0);
/**
 * og processOrderbookSwaps (same-j pass): offers by (createdHeight, accountId, offerId), then each touched pair's crossed resting rows in pair order.
 * The committed offers come from Account outputs (og accountOutputVerified), so no second committed-state check runs here.
 */
export const processOrderbookSwaps = (hub: Hub, offers: readonly BookOfferInput[], resumePairIds: readonly string[] = []): Result<BookMatch, EntityError> => {
  const pass = newPass(hub), minTradeSize = hub.ext.hubProfile.minTradeSize;
  const sorted = [...offers].sort((l, r) => l.createdHeight - r.createdHeight || compareText(l.accountId, r.accountId) || compareText(l.offerId, r.offerId));
  // og runs the cross-j pass first, on the same hot book cache
  const cross = processCrossOffers(pass, sorted.flatMap((o) => (o.crossJurisdiction === undefined ? [] : [{ ...o, crossJurisdiction: o.crossJurisdiction }])));
  if (!cross.ok) return cross;
  for (const o of sorted) {
    if (o.crossJurisdiction !== undefined) continue;
    const done = processSameOffer(pass, o, minTradeSize);
    if (!done.ok) return done;
  }
  for (const pairId of [...new Set(resumePairIds)].sort()) {
    if (pairId.startsWith("cross:")) continue;
    const seed = resumeSeed(pass, pairId, minTradeSize);
    if (!seed.ok) return seed;
    if (seed.value === null) continue;
    const drained = drainSame(pass, seed.value, minTradeSize);
    if (!drained.ok) return drained;
  }
  return ok({ accountTxs: pass.accountTxs, books: pass.updates, pairDimensions: pass.dims, crossFills: cross.value });
};
export type BookOfferInput = BookOffer & { readonly crossJurisdiction?: CrossRoute | undefined };
/**
 * og collectOffersForMatching + admitOrderbookOfferForMatching: each committed offer once, keyed by the counterparty Account; a hub's own
 * maker offer is never listed in its own book; an inactive Account's offer is skipped; a missing Account halts. A cross-j offer is matched only by
 * its canonical book owner and only once its admission is live (admitCrossForMatching).
 */
export const offersForMatching = (hub: Hub, created: readonly SwapOfferEvent[]): Result<readonly BookOfferInput[], EntityError> => {
  const self = hub.id.toLowerCase();
  return chain(traverse(created, (e) => map(normalizeOffer(e, e.fromEntity.toLowerCase() === self ? e.toEntity.toLowerCase() : e.fromEntity.toLowerCase()),
    (o): BookOfferInput => (e.crossJurisdiction === undefined ? o : { ...o, crossJurisdiction: e.crossJurisdiction }))), (enriched) => {
    const seen = new Set<string>(), admitted: BookOfferInput[] = [];
    for (const o of enriched) {
      const key = swapKeyOf(o.accountId, o.offerId), route = o.crossJurisdiction;
      if (seen.has(key)) continue;
      seen.add(key);
      if (route === undefined && (o.makerIsLeft ? o.fromEntity : o.toEntity) === self) continue;
      // og collectOffersForMatching: a cross-j order is listed only by its canonical book owner, then admitOrderbookOfferForMatching's cross branch
      if (route !== undefined) {
        if (crossBookOwnerRef(route) !== self) continue;
        const ready = admitCrossForMatching(hub, { ...o, crossJurisdiction: route });
        if (!ready.ok) return ready;
        if (ready.value) admitted.push(o);
        continue;
      }
      const account = hub.accounts.get(o.accountId.toLowerCase());
      if (account === undefined) return halt(`ORDERBOOK_ACCOUNT_OUTPUT_ACCOUNT_MISSING: account=${o.accountId} offer=${o.offerId}`);
      if (account.active) admitted.push(o);
    }
    return ok(admitted);
  });
};
/** og getOrderbookPairsForOrder over the derived order-pair index: every pair whose book holds the row, in pair order. */
const pairsHolding = (books: ReadonlyMap<string, Book>, orderId: string): readonly string[] => [...books].filter(([, b]) => b.orders.has(orderId)).map(([pairId]) => pairId).sort();
/** og applyCommittedSwapCancelsToOrderbook: a committed offer removal takes its row off the book; returns the touched pairs, sorted. */
export const applyCommittedSwapCancels = (ext: OrderbookExt, cancels: readonly SwapRef[]): Result<{ readonly ext: OrderbookExt; readonly resumePairIds: readonly string[] }, EntityError> => {
  let books = ext.books;
  const touched = new Set<string>();
  for (const { accountId, offerId } of cancels) {
    const orderId = swapKeyOf(accountId, offerId), pairs = pairsHolding(books, orderId);
    if (pairs.length > 1) return halt(`ORDERBOOK_DUPLICATE_BOOK_ORDER: order=${orderId} matches=${pairs.length}`);
    const pairId = pairs[0], book = pairId === undefined ? undefined : books.get(pairId), order = book?.orders.get(orderId);
    if (pairId === undefined || book === undefined || order === undefined) continue;
    const r = applyBookCommand(book, { kind: 1, ownerId: order.ownerId, orderId });
    if (!r.ok) return halt(r.error.code);
    books = mapSet(books, pairId, r.value.state);
    touched.add(pairId);
  }
  return ok({ ext: books === ext.books ? ext : { ...ext, books }, resumePairIds: [...touched].sort() });
};
/** og processOrderbookCancels: take the row off its book; a same-j offer queues its zero-fill cancel resolve, a cross-j one a hub-internal cancel instruction at its admitted progress. */
export const processOrderbookCancels = (hub: Hub, cancels: readonly SwapRef[]): Result<{ readonly accountTxs: readonly BookTx[]; readonly books: ReadonlyMap<string, Book>; readonly crossFills: readonly CrossFillInstruction[] }, EntityError> => {
  const q = { hub, queued: new Set<string>(), accountTxs: [] as BookTx[] }, working = new Map<string, Book>(), crossFills: CrossFillInstruction[] = [];
  for (const { offerId, accountId } of cancels) {
    const account = hub.accounts.get(accountId);
    if (account === undefined || !account.offers.has(offerId)) continue;
    const orderId = swapKeyOf(accountId, offerId);
    const matching = pairsHolding(hub.ext.books, orderId).flatMap((pairId) => {
      const book = working.get(pairId) ?? hub.ext.books.get(pairId), order = book?.orders.get(orderId);
      return book === undefined || order === undefined ? [] : [{ pairId, book, ownerId: order.ownerId }];
    });
    if (matching.length > 1) return halt(`ORDERBOOK_DUPLICATE_BOOK_ORDER: order=${orderId} matches=${matching.length}`);
    for (const { pairId, book, ownerId } of matching) {
      const r = applyBookCommand(book, { kind: 1, ownerId, orderId });
      if (!r.ok) return halt(r.error.code);
      working.set(pairId, r.value.state);
    }
    const route = account.offers.get(offerId)?.crossJurisdiction;
    if (route !== undefined) {
      const i = crossCancelInstruction(accountId, offerId, orderId, hub.crossAdmissions?.get(bookAdmissionKey(accountId, offerId))?.route ?? route);
      if (!i.ok) return i;
      crossFills.push(i.value);
      continue;
    }
    queueUniqueResolve(q, accountId, cancelTerms(offerId, "cancel_request"));
  }
  return ok({ accountTxs: q.accountTxs, books: working, crossFills });
};
// ---- cross-j hub book: og extensions/cross-j/orderbook.ts (admissions, USD caps, market offer, fill and cancel instructions), orderbook/cross-j/*,
// entity/tx/handlers/account/orderbook/cross/* and cancels.ts (cross branch). og throws from deep inside; here each halt is an `entity_invariant` with og's message. ----
export type BookAdmissionStatus = "pending" | "admitted" | "resolving" | "closed";
/** og CrossJurisdictionBookAdmission: the book owner's record of one admitted route, keyed by source user and order. */
export type BookAdmission = {
  readonly orderId: string; readonly routeHash: string; readonly sourceEntityId: string; readonly bookOwnerEntityId: string; readonly status: BookAdmissionStatus; readonly route: CrossRoute;
  readonly admittedAt?: number | undefined; readonly resolvingAt?: number | undefined; readonly closedAt?: number | undefined; readonly closeReason?: string | undefined; readonly updatedAt: number;
};
export type BookAdmissions = ReadonlyMap<string, BookAdmission>;
/** og crossJurisdictionBookAdmissionKeyFor (also the book's namespaced order id for a cross-j row). */
export const bookAdmissionKey = (sourceEntityId: string, orderId: string): string => `${entityRef(sourceEntityId)}:${String(orderId || "")}`;
/** og crossJurisdictionBookOwnerRef. */
export const crossBookOwnerRef = (r: CrossRoute): string => entityRef(r.bookOwnerEntityId || r.source.counterpartyEntityId || r.hubEntityId || "");
/** og withCanonicalCrossJurisdictionRouteHash where a throw is a halt. */
const canonRoute = (r: CrossRoute): Result<CrossRoute, EntityError> => { const c = canonicalCrossRoute(r); return c.ok ? c : invariant(crossRouteErrorText(r, c.error.reason)); };
const cloneRouteE = (r: CrossRoute): Result<CrossRoute, EntityError> => fatalCross(cloneCrossRoute(r));
/** og cloneCrossJurisdictionBookAdmission: the collection forks a leaf through it before any in-place write. */
export const cloneBookAdmission = (a: BookAdmission): Result<BookAdmission, EntityError> => map(cloneRouteE(a.route), (route): BookAdmission => ({
  orderId: String(a.orderId || ""), routeHash: String(a.routeHash || ""), sourceEntityId: String(a.sourceEntityId || ""), bookOwnerEntityId: String(a.bookOwnerEntityId || ""), status: a.status, route,
  updatedAt: Number(a.updatedAt || 0), ...opt("admittedAt", optNum(a.admittedAt)), ...opt("resolvingAt", optNum(a.resolvingAt)), ...opt("closedAt", optNum(a.closedAt)), ...opt("closeReason", optText(a.closeReason)),
}));
/** og getEntityCollectionValueForWrite + an in-place edit: a missing key is a no-op. */
const writeAdmission = (admissions: BookAdmissions | undefined, key: string, edit: (a: BookAdmission) => BookAdmission | undefined): Result<BookAdmissions | undefined, EntityError> => {
  const current = admissions?.get(key);
  if (admissions === undefined || current === undefined) return ok(admissions);
  return map(cloneBookAdmission(current), (forked) => { const next = edit(forked); return next === undefined ? admissions : mapSet(admissions, key, next); });
};
/** og mergeAdmissionRoute: the later route wins field by field, but never lowers the status or drops a pull or the hash. */
const mergeAdmissionRoute = (existing: CrossRoute | undefined, next: CrossRoute): Result<CrossRoute, EntityError> => chain(cloneRouteE(next), (n) => existing === undefined ? ok(n) : map(cloneRouteE(existing), (e) => {
  const merged: MutableRoute = { ...e, ...n };
  if (compareCrossStatus(e.status, n.status) < 0) merged.status = e.status;
  if (e.sourcePull && !merged.sourcePull) merged.sourcePull = e.sourcePull;
  if (e.targetPull && !merged.targetPull) merged.targetPull = e.targetPull;
  if (e.routeHash && !merged.routeHash) merged.routeHash = e.routeHash;
  return merged;
}));
/** og mergeCrossJurisdictionBookAdmission: a new admission starts `pending`; an existing one keeps its status and takes the merged route. */
export const mergeBookAdmission = (admissions: BookAdmissions | undefined, route: CrossRoute, now: number): Result<{ readonly admissions: BookAdmissions; readonly admission: BookAdmission }, EntityError> =>
  chain(canonRoute(route), (c) => {
    const key = bookAdmissionKey(c.source.entityId, c.orderId), existing = admissions?.get(key);
    return map(mergeAdmissionRoute(existing?.route, c), (merged) => {
      const admission: BookAdmission = existing !== undefined ? { ...existing, route: merged, updatedAt: now }
        : { orderId: c.orderId, routeHash: c.routeHash || "", sourceEntityId: entityRef(c.source.entityId), bookOwnerEntityId: crossBookOwnerRef(c), status: "pending", route: merged, updatedAt: now };
      return { admissions: mapSet(admissions ?? new Map<string, BookAdmission>(), key, admission), admission };
    });
  });
/** og markCrossJurisdictionBookAdmissionResolving: a live (not closed) admission starts resolving. */
export const markAdmissionResolving = (admissions: BookAdmissions | undefined, route: CrossRoute, now: number): Result<BookAdmissions | undefined, EntityError> =>
  chain(canonRoute(route), (c) => writeAdmission(admissions, bookAdmissionKey(c.source.entityId, c.orderId), (a) => (a.status === "closed" ? undefined : { ...a, status: "resolving", resolvingAt: now, updatedAt: now })));
/** og markCrossJurisdictionBookAdmissionClosed. */
export const markAdmissionClosed = (admissions: BookAdmissions | undefined, sourceEntityId: string, orderId: string, now: number, reason: string): Result<BookAdmissions | undefined, EntityError> =>
  writeAdmission(admissions, bookAdmissionKey(sourceEntityId, orderId), (a) => ({ ...a, status: "closed", closedAt: now, closeReason: reason, updatedAt: now }));
/** og getCrossJurisdictionBookAdmissionError: owner, both pulls, expiry, then the admitted record for exactly this route. USD risk is not rechecked here. */
export const bookAdmissionError = (entityId: string, admissions: BookAdmissions | undefined, route: CrossRoute, now: number): Result<string | null, EntityError> => map(canonRoute(route), (c) => {
  const current = entityRef(entityId), owner = crossBookOwnerRef(c);
  if (owner !== current) return `CROSS_J_ORDER_WRONG_BOOK_OWNER: order=${c.orderId} owner=${owner} current=${current}`;
  if (!c.sourcePull || !c.targetPull) return `CROSS_J_ORDER_LOCK_REF_MISSING: order=${c.orderId}`;
  if (isCrossExpired(c, now)) return `CROSS_J_ORDER_ROUTE_EXPIRED: order=${c.orderId}`;
  const a = admissions?.get(bookAdmissionKey(c.source.entityId, c.orderId));
  if (a === undefined) return `CROSS_J_BOOK_ADMISSION_PENDING: order=${c.orderId} leg=both`;
  if (a.status === "closed") return `CROSS_J_BOOK_ADMISSION_CLOSED: order=${c.orderId} reason=${a.closeReason || ""}`;
  if (a.status === "resolving") return `CROSS_J_BOOK_ADMISSION_RESOLVING: order=${c.orderId}`;
  if (a.orderId !== c.orderId || a.routeHash.toLowerCase() !== (c.routeHash || "").toLowerCase() || entityRef(a.bookOwnerEntityId) !== owner) return `CROSS_J_BOOK_ADMISSION_ROUTE_MISMATCH: order=${c.orderId}`;
  return null;
});
export type BookAdmissionFailure = { readonly kind: "pending" | "risk_reject" | "invalid"; readonly message: string };
/** og getTypedCrossJurisdictionBookAdmissionFailure. */
export const bookAdmissionFailure = (entityId: string, admissions: BookAdmissions | undefined, route: CrossRoute, now: number): Result<BookAdmissionFailure | null, EntityError> =>
  map(bookAdmissionError(entityId, admissions, route, now), (message) => message === null ? null
    : { kind: message.startsWith("CROSS_J_BOOK_ADMISSION_PENDING:") ? "pending" : message.startsWith("CROSS_J_BOOK_USD_CAP_EXCEEDED:") ? "risk_reject" : "invalid", message });
/** og internalUsdPrice: a reference stable at par; any other token at the hub's last accepted USD authority ask against its reference token, when one exists. */
const internalUsdPrice = (ext: OrderbookExt | undefined, tokenId: number): Result<bigint | null, EntityError> => {
  if (REFERENCE_STABLES.has(tokenId)) return ok(PRICE_SCALE);
  if (ext === undefined) return ok(null);
  const reference = Number(ext.hubProfile.referenceTokenId);
  if (!REFERENCE_STABLES.has(reference)) return invariant(`CROSS_J_BOOK_USD_REFERENCE_INVALID:token=${reference}`);
  const price = ext.books.get(canonicalPairOf(tokenId, reference).pairId)?.lastAcceptedUsdAskPriceTicks ?? 0n;
  return ok(price > 0n ? price : null);
};
const usdMicrosAt = (tokenId: number, amount: bigint, price: bigint): Result<bigint, EntityError> =>
  amount <= 0n ? invariant(`CROSS_J_BOOK_USD_AMOUNT_INVALID:token=${tokenId}`) : map(tokenDecimals(tokenId), (d) => ceilDiv(amount * price * 1_000_000n, 10n ** d * PRICE_SCALE));
/** og crossJurisdictionLegUsdMicros. */
export const crossLegUsdMicros = (ext: OrderbookExt | undefined, tokenId: number, amount: bigint): Result<bigint, EntityError> =>
  chain(internalUsdPrice(ext, tokenId), (price) => (price === null ? invariant(`CROSS_J_USD_PRICE_UNAVAILABLE:token=${tokenId}`) : usdMicrosAt(tokenId, amount, price)));
/** og getCrossJurisdictionLegUsdCapError: only the leg this hub bears, and only once it has a price for it (unpriced is permissionless). */
export const crossLegUsdCapError = (ext: OrderbookExt | undefined, route: CrossRoute, role: "source" | "target"): Result<string | null, EntityError> => {
  const leg = route[role], tokenId = Number(leg.tokenId);
  return chain(internalUsdPrice(ext, tokenId), (price) => price === null ? ok(null) : map(usdMicrosAt(tokenId, BigInt(leg.amount), price), (usdMicros) =>
    usdMicros > CROSS_J_BOOK_MAX_USD_MICROS ? `CROSS_J_BOOK_USD_CAP_EXCEEDED:order=${route.orderId}:leg=${role}:usdMicros=${usdMicros}:cap=${CROSS_J_BOOK_MAX_USD_MICROS}` : null));
};
/** og getCrossJurisdictionLocalUsdCapError: exactly one local hub leg (by entity and stack), else a validator-leg error. */
export const crossLocalUsdCapError = (v: { readonly id: string; readonly jurisdiction: Domain; readonly ext?: OrderbookExt | undefined }, route: CrossRoute): Result<string | null, EntityError> => {
  const entity = entityRef(v.id), stack = stackIdOf(v.jurisdiction).toLowerCase(), roles: ("source" | "target")[] = [];
  if (entity === entityRef(route.source.counterpartyEntityId) && stack === String(route.source.jurisdiction).toLowerCase()) roles.push("source");
  if (entity === entityRef(route.target.entityId) && stack === String(route.target.jurisdiction).toLowerCase()) roles.push("target");
  const role = roles[0];
  return roles.length === 1 && role !== undefined ? crossLegUsdCapError(v.ext, route, role)
    : ok(`CROSS_J_BOOK_USD_VALIDATOR_LEG_INVALID:order=${route.orderId}:entity=${entity}:stack=${stack}:matches=${roles.length}`);
};
/** og crossJurisdictionBookQtyLots: the floor of the base remainder in book lots. */
export const crossBookQtyLots = (baseTokenId: number, baseAmount: bigint): Result<bigint, EntityError> =>
  baseAmount <= 0n ? ok(0n) : map(tokenDecimals(baseTokenId), (d) => baseAmount / lotScale(Number(d)));
export type CrossRemaining = { readonly sourceTotal: bigint; readonly targetTotal: bigint; readonly filledSourceAmount: bigint; readonly filledTargetAmount: bigint; readonly sourceRemaining: bigint; readonly targetRemaining: bigint; readonly fillRatio: number };
/** og getCrossJurisdictionRouteRemainingAmounts. */
export const crossRemaining = (r: CrossRoute): Result<CrossRemaining, EntityError> => {
  const sourceTotal = BigInt(r.source.amount), targetTotal = BigInt(r.target.amount);
  if (sourceTotal <= 0n || targetTotal <= 0n) return invariant(`CROSS_J_ROUTE_AMOUNT_INVALID: order=${r.orderId}`);
  return chain(fatalCross(crossFillAmounts(r)), (c): Result<CrossRemaining, EntityError> => c.filledSourceAmount < 0n || c.filledTargetAmount < 0n || c.filledSourceAmount > sourceTotal || c.filledTargetAmount > targetTotal
    ? invariant(`CROSS_J_ROUTE_FILL_INVALID: order=${r.orderId} source=${c.filledSourceAmount}/${sourceTotal} target=${c.filledTargetAmount}/${targetTotal}`)
    : ok({ sourceTotal, targetTotal, filledSourceAmount: c.filledSourceAmount, filledTargetAmount: c.filledTargetAmount, sourceRemaining: sourceTotal - c.filledSourceAmount, targetRemaining: targetTotal - c.filledTargetAmount, fillRatio: c.fillRatio }));
};
/** og NormalizedOrderbookOffer carrying its cross-j route. */
export type CrossBookOffer = BookOffer & { readonly crossJurisdiction: CrossRoute };
/** og CrossMarketOffer: the route's committed remainder as one book order on the canonical cross venue. */
export type CrossMarketOffer = {
  readonly offer: CrossBookOffer; readonly route: CrossRoute; readonly pairId: string; readonly side: BookSide; readonly baseTokenId: number; readonly quoteTokenId: number;
  readonly baseAmount: bigint; readonly quoteAmount: bigint; readonly priceTicks: bigint; readonly makerId: string;
};
/** og buildCrossJurisdictionMarketOffer: only this book owner's working route; side, amounts and price come from the route remainder, never from the Account offer. */
export const crossMarketOffer = (offer: CrossBookOffer, hubEntityId: string): Result<CrossMarketOffer | null, EntityError> => {
  const route = offer.crossJurisdiction, owner = entityRef(route.bookOwnerEntityId || route.source.counterpartyEntityId || route.hubEntityId);
  if (owner && owner !== entityRef(hubEntityId)) return ok(null);
  if (route.status !== "resting" && route.status !== "partially_filled") return ok(null);
  return chain(fatalCross(crossMarket(route)), (m) => {
    if (!m.sourceKey || !m.targetKey || m.sourceKey === m.targetKey) return ok(null);
    const side: BookSide = m.sourceIsBase ? 1 : 0;
    return chain(crossRemaining(route), (rem) => {
      const baseTokenId = Number(m.sourceIsBase ? route.source.tokenId : route.target.tokenId), quoteTokenId = Number(m.sourceIsBase ? route.target.tokenId : route.source.tokenId);
      const baseAmount = m.sourceIsBase ? rem.sourceRemaining : rem.targetRemaining, quoteAmount = m.sourceIsBase ? rem.targetRemaining : rem.sourceRemaining;
      return chain(tokenDecimals(baseTokenId), (bd) => map(tokenDecimals(quoteTokenId), (qd): CrossMarketOffer | null => {
        const priceTicks = priceTicksOf({ side, bd: Number(bd), qd: Number(qd) }, baseAmount, quoteAmount);
        return baseAmount <= 0n || quoteAmount <= 0n || priceTicks <= 0n ? null
          : { offer, route, pairId: m.venueId, side, baseTokenId, quoteTokenId, baseAmount, quoteAmount, priceTicks, makerId: offer.makerIsLeft ? offer.fromEntity : offer.toEntity };
      }));
    });
  });
};
/** og resolveCrossJurisdictionExecutionPriceTicks: one price-improvement lane, so a cross-j trade always executes at the ask. */
export const crossExecutionPrice = (first: CrossMarketOffer, second: CrossMarketOffer): Result<bigint, EntityError> => {
  if (first.pairId !== second.pairId || first.baseTokenId !== second.baseTokenId || first.quoteTokenId !== second.quoteTokenId) return invariant(`CROSS_J_TRADE_PAIR_MISMATCH:${first.pairId}:${second.pairId}`);
  if (first.side === second.side) return invariant(`CROSS_J_TRADE_SIDE_MISMATCH:${first.side}:${second.side}`);
  const sell = first.side === 1 ? first : second, buy = first.side === 0 ? first : second;
  return sell.priceTicks <= 0n || buy.priceTicks <= 0n || sell.priceTicks > buy.priceTicks ? invariant(`CROSS_J_TRADE_PRICE_NOT_CROSSED:ask=${sell.priceTicks}:bid=${buy.priceTicks}`) : ok(sell.priceTicks);
};
export type CrossBookFill = { readonly filledLots: bigint; readonly weightedCost: bigint; readonly cancelRemainder?: boolean | undefined };
/** og crossJurisdictionExecutionAmounts: the executed book amounts of one aggregated fill in route (source/target) terms. */
export const crossExecutionAmounts = (meta: CrossMarketOffer, fill: CrossBookFill): Result<{ readonly executionSourceAmount: bigint; readonly executionTargetAmount: bigint } | null, EntityError> => {
  if (fill.filledLots <= 0n || fill.weightedCost <= 0n) return ok(null);
  return chain(tokenDecimals(meta.baseTokenId), (bd) => map(tokenDecimals(meta.quoteTokenId), (qd) => {
    const lot = lotScale(Number(bd)), base = fill.filledLots * lot, quote = quoteAt(Number(bd), Number(qd), lot, fill.weightedCost);
    const executionSourceAmount = meta.side === 1 ? base : quote, executionTargetAmount = meta.side === 1 ? quote : base;
    return executionSourceAmount <= 0n || executionTargetAmount <= 0n ? null : { executionSourceAmount, executionTargetAmount };
  }));
};
/** og CrossJurisdictionFillInstruction: hub-internal progress of one order, one uint16 ratio; the exact executed amounts only feed conservation. */
export type CrossFillInstruction = {
  readonly accountId: string; readonly offerId: string; readonly orderId: string; readonly route: CrossRoute; readonly fillSeq: number; readonly fillRatio: number; readonly cancelRemainder: boolean;
  readonly executionSourceAmount: bigint; readonly executionTargetAmount: bigint;
};
const currentFillSeq = (r: CrossRoute): number => Math.max(0, Math.floor(Number(r.fillSeq ?? 0) || 0));
/** og buildCrossJurisdictionFillInstruction: the cumulative target-side ratio; a step that moves neither or only one leg's claim is absorbed by the hub (null). */
export const crossFillInstruction = (accountId: string, offerId: string, orderId: string, meta: CrossMarketOffer, fill: CrossBookFill): Result<CrossFillInstruction | null, EntityError> =>
  chain(crossExecutionAmounts(meta, fill), (execution) => execution === null ? ok(null) : map(fatalCross(crossFillAmounts(meta.route)), (c): CrossFillInstruction | null => {
    const { executionSourceAmount, executionTargetAmount } = execution;
    if (c.filledSourceAmount + executionSourceAmount > c.sourceTotal || c.filledTargetAmount + executionTargetAmount > c.targetTotal) return null;
    const fillRatio = fillRatioOf(exactFillRatio(c.targetTotal, c.filledTargetAmount + executionTargetAmount));
    if (fillRatio <= c.fillRatio) return null;
    const ratio = BigInt(fillRatio), max = BigInt(MAX_FILL), project = (total: bigint): bigint => (ratio >= max ? total : (total * ratio) / max);
    if (project(c.sourceTotal) <= c.filledSourceAmount || project(c.targetTotal) <= c.filledTargetAmount) return null;
    return { accountId, offerId, orderId, route: meta.route, fillSeq: currentFillSeq(meta.route) + 1, fillRatio, cancelRemainder: fill.cancelRemainder === true, executionSourceAmount, executionTargetAmount };
  }));
/** og buildCrossJurisdictionCancelInstruction: a terminal cancel of the unfilled remainder at the committed progress. */
export const crossCancelInstruction = (accountId: string, offerId: string, orderId: string, route: CrossRoute): Result<CrossFillInstruction, EntityError> =>
  map(fatalCross(crossFillAmounts(route)), (c) => ({ accountId, offerId, orderId, route, fillSeq: currentFillSeq(route), fillRatio: c.fillRatio, cancelRemainder: true, executionSourceAmount: 0n, executionTargetAmount: 0n }));
/** og normalizeSwapOfferForOrderbook for a cross-j offer: the book view keeps the route. */
const normalizeCross = (o: Parameters<typeof normalizeOffer>[0], accountId: string, route: CrossRoute): Result<CrossBookOffer, EntityError> =>
  map(normalizeOffer(o, accountId), (b) => ({ ...b, crossJurisdiction: route }));
/** og buildCrossMarketOfferFromBookOrder: the Account offer at its progressed (admitted) route, else a remote admitted route without a local Account. */
const crossMetaFromBookOrder = (hub: Hub, orderId: string): Result<CrossMarketOffer | null, EntityError> => chain(parseOrderId(orderId, "ORDERBOOK_CROSS_J_MALFORMED_BOOK_ORDER"), ({ accountId, offerId }) => {
  const account = hub.accounts.get(accountId), o = account?.offers.get(offerId), admission = hub.crossAdmissions?.get(bookAdmissionKey(accountId, offerId));
  if (account !== undefined && o?.crossJurisdiction !== undefined) {
    return chain(normalizeCross({ offerId, makerIsLeft: o.makerIsLeft, fromEntity: account.left, toEntity: account.right, createdHeight: o.createdHeight, giveTokenId: Number(o.giveTokenId), giveTokenDecimals: o.giveTokenDecimals,
      giveAmount: o.giveAmount, wantTokenId: Number(o.wantTokenId), wantTokenDecimals: o.wantTokenDecimals, wantAmount: o.wantAmount, maxFee: o.maxFee, minNetReceive: o.minNetReceive, priceTicks: o.priceTicks, timeInForce: o.timeInForce },
    accountId, admission?.route ?? o.crossJurisdiction), (n) => crossMarketOffer(n, hub.id));
  }
  if (admission === undefined || admission.status !== "admitted") return ok(null);
  const route = admission.route;
  return chain(crossRemaining(route), (rem) => chain(tokenDecimals(Number(route.source.tokenId)), (gd) => chain(tokenDecimals(Number(route.target.tokenId)), (wd) =>
    chain(normalizeCross({ offerId, makerIsLeft: true, fromEntity: route.source.entityId, toEntity: route.source.counterpartyEntityId, createdHeight: 0, giveTokenId: Number(route.source.tokenId), giveTokenDecimals: Number(gd),
      giveAmount: rem.sourceRemaining, wantTokenId: Number(route.target.tokenId), wantTokenDecimals: Number(wd), wantAmount: rem.targetRemaining, maxFee: 0n, minNetReceive: rem.targetRemaining, ...opt("priceTicks", route.priceTicks === undefined ? undefined : BigInt(route.priceTicks)) },
    accountId, route), (n) => crossMarketOffer(n, hub.id)))));
});
type CrossFillAgg = { filledLots: bigint; weightedCost: bigint; cancelRemainder: boolean };
/** og CrossOrderbookPass: shares the same-j pass's hot book cache, touched books and queued resolves; working books stay speculative until committed. */
type CrossPass = {
  readonly pass: Pass; readonly meta: Map<string, CrossMarketOffer>; readonly fills: Map<string, CrossFillAgg>; readonly suspended: Set<string>;
  readonly working: Map<string, Book>; readonly speculative: Set<string>; readonly aliased: Set<string>; readonly out: CrossFillInstruction[];
};
/** og rejectInvalidCrossOffer in live mode: every invalid cross-j offer halts the frame. */
const liveReject = (accountId: string, offerId: string, reason: string): Result<never, EntityError> => halt(`ORDERBOOK_LIVE_PROJECTION_REJECT: account=${accountId} offer=${offerId} reason=${reason}`);
/** og getCrossMarketOffer: the admitted route (or the hub's route mirror) carries the progressed remainder. */
const crossMetaOf = (cp: CrossPass, o: CrossBookOffer): Result<CrossMarketOffer | null, EntityError> => {
  const key = swapKeyOf(o.accountId, o.offerId), cached = cp.meta.get(key), hub = cp.pass.hub;
  if (cached !== undefined) return ok(cached);
  const progressed = hub.crossAdmissions?.get(bookAdmissionKey(o.accountId, o.offerId))?.route ?? hub.crossSwaps?.get(o.offerId) ?? o.crossJurisdiction;
  return map(crossMarketOffer(progressed === o.crossJurisdiction ? o : { ...o, crossJurisdiction: progressed }, hub.id), (m) => { if (m !== null) cp.meta.set(key, m); return m; });
};
const crossMeta = (cp: CrossPass, orderId: string): Result<CrossMarketOffer | null, EntityError> => { const m = cp.meta.get(orderId); return m !== undefined ? ok(m) : crossMetaFromBookOrder(cp.pass.hub, orderId); };
/** og committedCrossRouteStatus. */
const committedCrossStatus = (hub: Hub, accountId: string, offerId: string): string | undefined => {
  const admission = hub.crossAdmissions?.get(bookAdmissionKey(accountId, offerId));
  if (admission !== undefined && admission.status !== "admitted") return `admission:${admission.status}`;
  const mirror = hub.crossSwaps?.get(offerId);
  if (mirror?.status) return mirror.status;
  return hub.accounts.get(accountId)?.offers.get(offerId)?.crossJurisdiction?.status ?? admission?.route.status;
};
/** og classifyCrossBookMaker: a disputed local Account, a non-working or expired route cancels the row; a matched row is suspended; the row must equal its route remainder. */
const classifyCrossMaker = (cp: CrossPass, pairId: string, order: BookOrder): Result<MakerDisposition, EntityError> => chain(parseOrderId(order.orderId, "ORDERBOOK_CROSS_J_MALFORMED_BOOK_ORDER"), ({ accountId, offerId }) => {
  const hub = cp.pass.hub, account = hub.accounts.get(accountId);
  if (account !== undefined && !account.active) return ok("cancel");
  const status = committedCrossStatus(hub, accountId, offerId);
  if (status && status !== "resting" && status !== "partially_filled") return ok("cancel");
  return chain(crossMeta(cp, order.orderId), (meta): Result<MakerDisposition, EntityError> => {
    if (meta === null) return halt(`ORDERBOOK_CROSS_J_SNAPSHOT_MISSING: pair=${pairId} order=${order.orderId} account=${accountId} offer=${offerId}`);
    cp.meta.set(order.orderId, meta);
    if (isCrossExpired(meta.route, Number(hub.timestamp ?? 0))) return ok("cancel");
    if (cp.suspended.has(order.orderId)) return ok("suspended");
    return chain(crossBookQtyLots(meta.baseTokenId, meta.baseAmount), (qty): Result<MakerDisposition, EntityError> => {
      if (meta.pairId !== pairId || order.priceTicks !== meta.priceTicks || order.ownerId !== meta.makerId)
        return halt(`ORDERBOOK_CROSS_J_CACHE_MISMATCH: pair=${pairId} order=${order.orderId} storedPair=${pairId} canonicalPair=${meta.pairId} storedOwner=${order.ownerId} canonicalOwner=${meta.makerId} `
          + `storedQty=${order.qtyLots} canonicalQty=${qty} storedPrice=${order.priceTicks} canonicalPrice=${meta.priceTicks}`);
      if (order.qtyLots !== qty) return halt(`ORDERBOOK_CROSS_J_CACHE_MISMATCH: pair=${pairId} order=${order.orderId} storedQty=${order.qtyLots} canonicalQty=${qty} storedPrice=${order.priceTicks} canonicalPrice=${meta.priceTicks}`);
      return ok("eligible");
    });
  });
});
type PreparedCross = { readonly raw: CrossBookOffer; readonly orderId: string; readonly meta: CrossMarketOffer; readonly qtyLots: bigint; readonly bd: number; readonly qd: number; readonly book: Book };
/** og prepareCrossOrderbookOffer: dust, size and exact-quote alignment; a resolving offer or an identical resting row is skipped, a changed row halts. */
const prepareCross = (cp: CrossPass, raw: CrossBookOffer): Result<PreparedCross | null, EntityError> => {
  const { accountId, offerId } = raw, orderId = swapKeyOf(accountId, offerId), pass = cp.pass;
  return chain(crossMetaOf(cp, raw), (meta) => meta === null ? liveReject(accountId, offerId, "invalid-cross-j-route") : chain(crossBookQtyLots(meta.baseTokenId, meta.baseAmount), (qtyLots) => {
    if (qtyLots <= 0n) return liveReject(accountId, offerId, `cross-dust-remainder:${meta.baseAmount}`);
    if (qtyLots > MAX_ORDERBOOK_QTY_LOTS) return liveReject(accountId, offerId, `invalid-cross-qty:${qtyLots}`);
    const n = meta.offer, bd = meta.side === 1 ? n.giveTokenDecimals : n.wantTokenDecimals, qd = meta.side === 1 ? n.wantTokenDecimals : n.giveTokenDecimals, multiple = exactQuoteLots(bd, qd, meta.priceTicks);
    if (qtyLots % multiple !== 0n) return liveReject(accountId, offerId, `cross-quote-lot-misaligned:${qtyLots}:${multiple}`);
    cp.meta.set(orderId, meta);
    if (hasQueuedResolve(pass, accountId, offerId)) return ok(null);
    const cached = pass.cache.get(meta.pairId), published = cached === undefined ? pass.hub.ext.books.get(meta.pairId) : undefined;
    const fresh: Result<Book, EntityError> = cached !== undefined ? ok(cached) : published !== undefined ? ok(published)
      : mapErr(createBook({ bucketWidthTicks: BigInt(Math.max(1, (PAIR_POLICIES.get(`${raw.giveTokenId}/${raw.wantTokenId}`) ?? DEFAULT_PAIR_POLICY).bucket)), maxOrders: MAX_ORDERBOOK_ORDERS_PER_PAIR, stpPolicy: 1 }), (e) => ({ _tag: "entity_invariant", reason: e.code }) as EntityError);
    return chain(fresh, (book): Result<PreparedCross | null, EntityError> => {
      pass.cache.set(meta.pairId, book);
      const existing = book.orders.get(orderId);
      if (existing === undefined) return ok({ raw, orderId, meta, qtyLots, bd, qd, book });
      return existing.ownerId !== meta.makerId || existing.side !== meta.side || existing.priceTicks !== meta.priceTicks || existing.qtyLots !== qtyLots
        ? halt(`ORDERBOOK_CROSS_J_DUPLICATE_SNAPSHOT_MISMATCH: pair=${meta.pairId} order=${orderId} storedOwner=${existing.ownerId} canonicalOwner=${meta.makerId} storedQty=${existing.qtyLots} canonicalQty=${qtyLots} `
          + `storedPrice=${existing.priceTicks} canonicalPrice=${meta.priceTicks}`) : ok(null);
    });
  }));
};
/** og removeCrossBookOrderAfterFill: a terminal fill takes the row off its committed book. */
const removeCrossRowAfterFill = (cp: CrossPass, pairId: string, orderId: string): Result<void, EntityError> => {
  const pass = cp.pass, book = pass.cache.get(pairId) ?? pass.hub.ext.books.get(pairId), order = book?.orders.get(orderId);
  if (book === undefined || order === undefined) return ok(undefined);
  const r = applyBookCommand(book, { kind: 1, ownerId: order.ownerId, orderId });
  if (!r.ok) return halt(r.error.code);
  pass.cache.set(pairId, r.value.state);
  pass.updates.set(pairId, r.value.state);
  return ok(undefined);
};
const EXPECTED_CROSS_REJECTS: ReadonlySet<string> = new Set(["no fill", "FOK cannot fill entirely", "STP cancel taker"]);
/** og processCrossOrderbookOffer: speculative placement on the working book; a resting-only placement commits, trades aggregate into hub-internal fills. */
const processCrossOffer = (cp: CrossPass, raw: CrossBookOffer): Result<void, EntityError> => chain(prepareCross(cp, raw), (p): Result<void, EntityError> => {
  if (p === null) return ok(undefined);
  const { accountId, offerId } = raw, pairId = p.meta.pairId, pass = cp.pass;
  const start = cp.working.get(pairId) ?? p.book;
  cp.working.set(pairId, start);
  let fault: EntityError | undefined;
  const placed = applyBookCommand(start, { kind: 0, ownerId: p.meta.makerId, orderId: p.orderId, side: p.meta.side, tif: raw.timeInForce, postOnly: false, priceTicks: p.meta.priceTicks, qtyLots: p.qtyLots }, {
    suspendedOrderIds: cp.suspended,
    makerDisposition: (maker) => { if (fault !== undefined) return "suspended"; const d = classifyCrossMaker(cp, pairId, maker); if (!d.ok) { fault = d.error; return "suspended"; } return d.value; },
    executionPriceTicksForMatch: (maker, taker, takerSide) => (takerSide === 1 ? taker : maker),
    executionQtyMultipleAtPrice: (price) => exactQuoteLots(p.bd, p.qd, price),
  });
  if (fault !== undefined) return liveReject(accountId, offerId, `cross-pair-error:${haltMessage(fault)}`);
  if (!placed.ok) return liveReject(accountId, offerId, `cross-pair-error:${placed.error.code}`);
  const result = placed.value, rejects = result.events.flatMap((e) => (e.type === "REJECT" && e.orderId === p.orderId ? [e] : []));
  const rawTrades = result.events.flatMap((e) => (e.type === "TRADE" ? [e] : []));
  // og canonicalCrossTradeEvents: both sides' metadata must exist; the price is the ask
  const trades = traverse(rawTrades, (t) => chain(crossMeta(cp, t.makerOrderId), (maker) => chain(crossMeta(cp, t.takerOrderId), (taker) => {
    if (maker === null || taker === null) return halt(`ORDERBOOK_CROSS_J_TRADE_META_MISSING:maker=${t.makerOrderId}:taker=${t.takerOrderId}`);
    cp.meta.set(t.makerOrderId, maker); cp.meta.set(t.takerOrderId, taker);
    return map(crossExecutionPrice(maker, taker), (price) => ({ ...t, price }));
  })));
  if (!trades.ok) return trades;
  const expected = rejects.length > 0 && rejects.every((e) => EXPECTED_CROSS_REJECTS.has(e.reason)), reasons = rejects.map((e) => e.reason).join(",");
  if (rejects.length > 0 && trades.value.length === 0) {
    if (!expected) return liveReject(accountId, offerId, `cross-post-only-reject:${reasons}`);
    cp.suspended.add(p.orderId);
    return map(crossCancelInstruction(accountId, offerId, p.orderId, p.meta.route), (i) => { cp.out.push(i); });
  }
  if (rejects.length > 0 && !expected) return liveReject(accountId, offerId, `cross-order-reject:${reasons}`);
  cp.working.set(pairId, result.state);
  // og commitRestingCrossOffer publishes the working overlay itself: from then on the pair's committed book and working book are one object, so later
  // speculative trades on that pair land in the published book too (commitBookOverlay folds each step into the shared overlay)
  if ((trades.value.length === 0 && !cp.speculative.has(pairId)) || cp.aliased.has(pairId)) { pass.cache.set(pairId, result.state); pass.updates.set(pairId, result.state); cp.aliased.add(pairId); }
  if (trades.value.length > 0) cp.speculative.add(pairId);
  // og aggregateCrossTrades: a matched route never matches again in this pass; the taker remainder is cancelled for IOC/FOK or an expected reject
  const cancelTaker = raw.timeInForce !== 0 || expected;
  for (const t of trades.value) { cp.suspended.add(t.makerOrderId); cp.suspended.add(t.takerOrderId); }
  const perOrder = new Map<string, { filledLots: bigint; weightedCost: bigint }>();
  for (const t of trades.value) for (const orderId of [t.makerOrderId, t.takerOrderId]) {
    const e = perOrder.get(orderId), cost = t.price * t.qty;
    if (e === undefined) perOrder.set(orderId, { filledLots: t.qty, weightedCost: cost }); else { e.filledLots += t.qty; e.weightedCost += cost; }
  }
  for (const [orderId, fill] of perOrder) {
    const meta = crossMeta(cp, orderId);
    if (!meta.ok) return meta;
    if (meta.value === null) return halt(`ORDERBOOK_CROSS_J_FILL_META_MISSING: order=${orderId}`);
    const current = cp.fills.get(orderId), cancels = orderId === p.orderId && cancelTaker;
    if (current === undefined) cp.fills.set(orderId, { ...fill, cancelRemainder: cancels });
    else { current.filledLots += fill.filledLots; current.weightedCost += fill.weightedCost; if (cancels) current.cancelRemainder = true; }
  }
  return ok(undefined);
});
/** og planCrossFills + commitCrossFill: fills in order-id order, conserved per asset across every executed amount; a terminal one leaves the book. */
const finalizeCrossFills = (cp: CrossPass): Result<void, EntityError> => {
  const net = new Map<string, bigint>(), planned: CrossFillInstruction[] = [];
  for (const orderId of [...cp.fills.keys()].sort(compareText)) {
    const fill = cp.fills.get(orderId);
    if (fill === undefined) continue;
    const step = chain(crossMeta(cp, orderId), (meta) => meta === null ? halt(`ORDERBOOK_CROSS_J_FILL_META_MISSING: order=${orderId}`) : chain(crossExecutionAmounts(meta, fill), (execution) => {
      const netted = execution === null ? ok(undefined) : chain(fatalCross(assetKey(meta.route.source.jurisdiction, meta.route.source.tokenId)), (s) => map(fatalCross(assetKey(meta.route.target.jurisdiction, meta.route.target.tokenId)), (t) => {
        net.set(s, (net.get(s) ?? 0n) - execution.executionSourceAmount); net.set(t, (net.get(t) ?? 0n) + execution.executionTargetAmount);
      }));
      return chain(netted, () => chain(parseOrderId(orderId, "ORDERBOOK_CROSS_J_MALFORMED_FILL_ORDER"), ({ accountId, offerId }) => chain(crossFillInstruction(accountId, offerId, orderId, meta, fill), (i) =>
        i === null && fill.cancelRemainder ? map(crossCancelInstruction(accountId, offerId, orderId, meta.route), (c) => { planned.push(c); }) : ok(i === null ? undefined : (planned.push(i), undefined)))));
    }));
    if (!step.ok) return step;
  }
  const bad = [...net].filter(([, v]) => v !== 0n).sort(([a], [b]) => compareText(a, b));
  if (bad.length > 0) return halt(`CROSS_J_TRADE_CONSERVATION_FAILED:${bad.map(([asset, v]) => `${asset}=${v}`).join(",")}`);
  for (const i of planned) {
    const meta = crossMeta(cp, i.orderId);
    if (!meta.ok) return meta;
    if (meta.value === null) return halt(`ORDERBOOK_CROSS_J_FILL_META_MISSING: order=${i.orderId}`);
    if (i.cancelRemainder) { const removed = removeCrossRowAfterFill(cp, meta.value.pairId, i.orderId); if (!removed.ok) return removed; }
    cp.out.push(i);
  }
  return ok(undefined);
};
/** og processCrossJurisdictionOrderbookOffers: each cross-j offer in book order, then the pass's fills. */
const processCrossOffers = (pass: Pass, offers: readonly CrossBookOffer[]): Result<readonly CrossFillInstruction[], EntityError> => {
  const cp: CrossPass = { pass, meta: new Map(), fills: new Map(), suspended: new Set(), working: new Map(), speculative: new Set(), aliased: new Set(), out: [] };
  for (const o of offers) { const done = processCrossOffer(cp, o); if (!done.ok) return done; }
  return map(finalizeCrossFills(cp), () => cp.out);
};
// ---- cross-j book owner and source hub lifecycle: og entity/tx/handlers/cross-j/{book-order,book-removal-ack,fill}.ts, account-cross-j-followups.ts
// (applyCrossJurisdictionOrderbookFill, applySourceHubCrossJurisdictionFillProgress), orderbook/cross-j/index.ts (row resize / materialize / remove),
// account/orderbook/cancels.ts routeRemoteCrossJurisdictionBookCancels. og mutates the Entity candidate in place; each step here returns the next host. ----
/** og CrossJurisdictionFillProgressData (crossJurisdictionFillNotice data). */
export type CrossProgress = { readonly orderId: string; readonly routeHash?: string | undefined; readonly fillSeq: number; readonly cumulativeFillRatio: number; readonly cancelRemainder?: boolean | undefined };
/** The og EntityState fields the book owner / source hub lifecycle reads and writes. */
export type BookHost = {
  readonly id: string; readonly timestamp: number; readonly validators: readonly string[]; readonly ext?: OrderbookExt | undefined;
  readonly swaps?: ReadonlyMap<string, CrossRoute> | undefined; readonly admissions?: BookAdmissions | undefined; readonly accounts: ReadonlyMap<string, HubAccount>;
};
/** One lifecycle step: the next host, raw cross-j Entity outputs, frame messages, and the swap offers it created for this frame's matcher. */
export type BookHostStep = { readonly host: BookHost; readonly outputs: readonly CrossEntityOutput[]; readonly messages: readonly string[]; readonly created: readonly SwapOfferEvent[] };
const hostStep = (host: BookHost, o: Partial<Omit<BookHostStep, "host">> = {}): BookHostStep => ({ host, outputs: [], messages: [], created: [], ...o });
const shortTail = (v: unknown, chars: number): string => { const t = String(v || "").trim(); return !t ? "" : t.length > chars ? t.slice(-chars) : t; };
/** og crossJurisdictionBookOrderIdFor (orderbook/cross-j/index.ts): lowercased source, raw order id. */
const bookRowId = (sourceEntityId: string, orderId: string): string => `${lowerText(sourceEntityId)}:${String(orderId)}`;
const withBook = (h: BookHost, ext: OrderbookExt, pairId: string, book: Book): BookHost => ({ ...h, ext: { ...ext, books: mapSet(ext.books, pairId, book) } });
const bookHalt = <T,>(r: Result<T, BookError>): Result<T, EntityError> => (r.ok ? r : halt(r.error.code));
/** og removeBookOrderByIdWithPair. */
const removeRowById = (h: BookHost, id: string): Result<{ readonly host: BookHost; readonly removed: boolean }, EntityError> => {
  const ext = h.ext;
  if (ext === undefined) return ok({ host: h, removed: false });
  const pairs = pairsHolding(ext.books, id);
  if (pairs.length > 1) return halt(`ORDERBOOK_DUPLICATE_BOOK_ORDER: order=${id} matches=${pairs.length}`);
  const pairId = pairs[0], book = pairId === undefined ? undefined : ext.books.get(pairId), order = book?.orders.get(id);
  if (pairId === undefined || book === undefined || order === undefined) return ok({ host: h, removed: false });
  return map(bookHalt(applyBookCommand(book, { kind: 1, ownerId: order.ownerId, orderId: id })), (r) => ({ host: withBook(h, ext, pairId, r.state), removed: true }));
};
/** og resizeBookOrderById: a committed cross-j progress shrinks the row; never matcher repair. */
const resizeRowById = (h: BookHost, id: string, qty: bigint): Result<{ readonly host: BookHost; readonly resized: boolean }, EntityError> => {
  if (qty <= 0n || qty > MAX_ORDERBOOK_QTY_LOTS) return halt(`ORDERBOOK_RESIZE_INVALID: order=${id} qty=${qty}`);
  const ext = h.ext;
  if (ext === undefined) return ok({ host: h, resized: false });
  const pairs = pairsHolding(ext.books, id);
  if (pairs.length > 1) return halt(`ORDERBOOK_DUPLICATE_BOOK_ORDER: order=${id} matches=${pairs.length}`);
  const pairId = pairs[0], book = pairId === undefined ? undefined : ext.books.get(pairId);
  if (pairId === undefined || book === undefined) return ok({ host: h, resized: false });
  return map(bookHalt(reduceBookOrderQuantity(book, id, qty)), (next) => ({ host: withBook(h, ext, pairId, next), resized: true }));
};
/** og materializeCrossJurisdictionBookRemainder. */
const materializeRow = (h: BookHost, x: { readonly pairId: string; readonly sourceEntityId: string; readonly orderId: string; readonly ownerId: string; readonly side: BookSide; readonly priceTicks: bigint; readonly qtyLots: bigint }): Result<{ readonly host: BookHost; readonly materialized: boolean }, EntityError> => {
  const ext = h.ext;
  if (ext === undefined) return ok({ host: h, materialized: false });
  const id = bookRowId(x.sourceEntityId, x.orderId);
  if (pairsHolding(ext.books, id).length > 0) return halt(`ORDERBOOK_REMAINDER_ALREADY_PRESENT: order=${id}`);
  const book = ext.books.get(x.pairId);
  if (book === undefined) return ok({ host: h, materialized: false });
  return map(bookHalt(materializeCommittedRemainder(book, { orderId: id, ownerId: x.ownerId, side: x.side, priceTicks: x.priceTicks, qtyLots: x.qtyLots })), (next) => ({ host: withBook(h, ext, x.pairId, next), materialized: true }));
};
const proofRatioE = (r: ProofRatioInput): Result<number, EntityError> => fatalCross(crossProofRatio(r));
const transitionE = (r: CrossRoute, next: CrossStatus, at: number): Result<CrossRoute, EntityError> =>
  crossTransitionAllowed(r.status, next) ? ok({ ...r, status: next, updatedAt: at }) : halt(`CROSS_J_ROUTE_TRANSITION_INVALID: route=${r.orderId} ${r.status || "intent"}->${next}`);
/** og applyCrossJurisdictionFillProgress at a uint16 ratio (numerator = ratio, denominator = 65535); the prefix names the caller's halt. */
const applyRatioProgress = (r: CrossRoute, fillSeq: number, ratio: number, at: number, prefix: string): Result<CrossRoute, EntityError> => {
  if (!Number.isFinite(ratio)) return halt(`RangeError: The number ${ratio} cannot be converted to a BigInt because it is not an integer`);
  const next = applyCrossFill(r, { fillSeq, cumulativeFillRatio: ratio, fillNumerator: BigInt(ratio), fillDenominator: BigInt(MAX_FILL) }, at);
  return next.ok ? next : halt(`${prefix}: route=${r.orderId} ${next.error.reason}`);
};
/**
 * og buildCommittedCrossJurisdictionOfferEvent: the order at its committed remainder. A local source Account supplies its offer's shape; a remote
 * source (the book owner is the target-side hub) is exposed from the route alone.
 */
export const committedCrossOfferEvent = (h: BookHost, route: CrossRoute): Result<SwapOfferEvent, EntityError> => {
  const accountId = entityRef(route.source.entityId), account = h.accounts.get(accountId), offer = account?.offers.get(route.orderId);
  return chain(crossRemaining(route), (rem) => chain(fatalCross(cloneCrossRoute(route)), (clone): Result<SwapOfferEvent, EntityError> => {
    if (account === undefined || offer?.crossJurisdiction === undefined) {
      return chain(tokenDecimals(Number(route.source.tokenId)), (gd) => map(tokenDecimals(Number(route.target.tokenId)), (wd): SwapOfferEvent => ({
        offerId: route.orderId, accountId, makerIsLeft: true, fromEntity: accountId, toEntity: entityRef(route.source.counterpartyEntityId), createdHeight: 0,
        giveTokenId: Number(route.source.tokenId), giveTokenDecimals: Number(gd), giveAmount: rem.sourceRemaining, wantTokenId: Number(route.target.tokenId), wantTokenDecimals: Number(wd), wantAmount: rem.targetRemaining,
        maxFee: 0n, minNetReceive: rem.targetRemaining, ...opt("priceTicks", route.priceTicks === undefined ? undefined : BigInt(route.priceTicks)), crossJurisdiction: clone,
      })));
    }
    return ok({
      offerId: route.orderId, accountId, makerIsLeft: offer.makerIsLeft, fromEntity: account.left, toEntity: account.right, createdHeight: offer.createdHeight,
      giveTokenId: Number(offer.giveTokenId), giveTokenDecimals: offer.giveTokenDecimals, giveAmount: rem.sourceRemaining, wantTokenId: Number(offer.wantTokenId), wantTokenDecimals: offer.wantTokenDecimals, wantAmount: rem.targetRemaining,
      maxFee: 0n, minNetReceive: rem.targetRemaining, priceTicks: offer.priceTicks, ...opt("timeInForce", tif(offer.timeInForce)), crossJurisdiction: clone,
    });
  }));
};
/** og handleAdmitCrossJurisdictionBookOrderEntityTx: the book owner records the route, merges the admission, and exposes the committed order to this frame's matcher. */
export const admitBookOrder = (h: BookHost, data: { readonly route: CrossRoute; readonly reason?: string | undefined }): Result<BookHostStep, EntityError> => chain(canonRoute(data.route), (route) => {
  const now = h.timestamp, owner = crossBookOwnerRef(route);
  if (owner !== entityRef(h.id)) return halt(`CROSS_J_BOOK_ADMIT_WRONG_OWNER: order=${route.orderId} owner=${owner} current=${h.id}`);
  const prior = h.admissions?.get(bookAdmissionKey(route.source.entityId, route.orderId));
  if (prior?.status === "closed" || prior?.status === "resolving") {
    if ((prior.routeHash || "").toLowerCase() !== (route.routeHash || "").toLowerCase()) return halt(`CROSS_J_BOOK_ADMIT_ROUTE_INVALID: order=${route.orderId} existing admission route hash mismatch`);
    return ok(hostStep(h, { messages: [`🌉 Cross-j book admit ${route.orderId}: duplicate ${prior.status}`] }));
  }
  const swaps0 = h.swaps ?? new Map<string, CrossRoute>(), existing = swaps0.get(route.orderId);
  const recorded: Result<ReadonlyMap<string, CrossRoute>, EntityError> = existing !== undefined && isCrossTerminal(existing.status) ? ok(swaps0) : (() => {
    const transitionError = crossRouteTransitionError(existing, route), eh = existing?.routeHash?.toLowerCase(), rh = route.routeHash?.toLowerCase();
    const staleSameRoute = Boolean(eh && rh) && eh === rh && compareCrossStatus(existing?.status, route.status) < 0;
    if (transitionError && !staleSameRoute) return halt(`CROSS_J_BOOK_ADMIT_ROUTE_INVALID: order=${route.orderId} ${transitionError}`);
    return map(staleSameRoute && existing !== undefined ? mergeCrossRoute(route, existing) : mergeCrossRoute(existing, route), (merged) => mapSet(swaps0, route.orderId, merged));
  })();
  return chain(recorded, (swaps) => chain(mergeBookAdmission(h.admissions, route, now), ({ admissions, admission }) => {
    const merged: BookHost = { ...h, swaps, admissions };
    return chain(committedCrossOfferEvent(merged, admission.route), (event) => chain(bookAdmissionFailure(h.id, admissions, admission.route, now), (failure): Result<BookHostStep, EntityError> => {
      if (failure !== null) {
        if (failure.kind === "pending") return ok(hostStep(merged, { messages: [`🌉 Cross-j book admit ${route.orderId}: pending ${failure.message}`] }));
        if (failure.kind === "risk_reject")
          return map(markAdmissionClosed(admissions, admission.route.source.entityId, admission.route.orderId, now, failure.message), (closed) => hostStep({ ...merged, admissions: closed }, { messages: [`🌉 Cross-j book reject ${route.orderId}: ${failure.message}`] }));
        return halt(failure.message);
      }
      const key = bookAdmissionKey(route.source.entityId, route.orderId), admitted: BookAdmission = { ...admission, status: "admitted", admittedAt: admission.admittedAt ?? now, updatedAt: now };
      return ok(hostStep({ ...merged, admissions: mapSet(admissions, key, admitted) }, { messages: [`🌉 Cross-j book admit ${route.orderId}${data.reason ? `: ${data.reason}` : ""}`], created: [event] }));
    }));
  }));
});
/** og updateBookOrderForProgress: a partial fill resizes (or re-materializes) the row at the committed remainder; a terminal one removes what still rests. */
const updateRowForProgress = (h: BookHost, route: CrossRoute): Result<BookHost, EntityError> => {
  if (route.status !== "partially_filled") return map(removeRowById(h, bookRowId(route.source.entityId, route.orderId)), (r) => r.host);
  return chain(committedCrossOfferEvent(h, route), (event) => chain(normalizeCross(event, event.accountId || route.source.entityId, route), (n) => chain(crossMarketOffer(n, h.id), (market) => {
    if (market === null) return halt(`CROSS_J_BOOK_PROGRESS_MARKET_INVALID: order=${route.orderId}`);
    return chain(crossBookQtyLots(market.baseTokenId, market.baseAmount), (qtyLots) => chain(resizeRowById(h, bookRowId(route.source.entityId, route.orderId), qtyLots), (resized) => resized.resized ? ok(resized.host)
      : chain(materializeRow(h, { pairId: market.pairId, sourceEntityId: route.source.entityId, orderId: route.orderId, ownerId: market.makerId, side: market.side, priceTicks: market.priceTicks, qtyLots }),
        (m) => (m.materialized ? ok(m.host) : halt(`CROSS_J_BOOK_PROGRESS_ORDER_MISSING: order=${route.orderId}`)))));
  })));
};
/** og applyCrossJurisdictionBookFillToState: the book owner's admitted route and row take the progress; returns whether anything new applied. */
export const bookFillToState = (h: BookHost, sourceEntityId: string, data: CrossProgress): Result<{ readonly host: BookHost; readonly changed: boolean }, EntityError> => {
  const now = h.timestamp, key = bookAdmissionKey(sourceEntityId, data.orderId), admission = h.admissions?.get(key), admissions0 = h.admissions;
  if (admission === undefined || admissions0 === undefined) return halt(`CROSS_J_BOOK_PROGRESS_ADMISSION_MISSING: order=${data.orderId} source=${sourceEntityId}`);
  if (admission.status === "closed" && data.cancelRemainder) return ok({ host: h, changed: false });
  if (admission.status !== "admitted" && admission.status !== "resolving") return halt(`CROSS_J_BOOK_PROGRESS_ADMISSION_NOT_ADMITTED: order=${data.orderId} status=${admission.status}`);
  return chain(canonRoute(admission.route), (route) => {
    const owner = crossBookOwnerRef(route), self = entityRef(h.id), remoteSource = entityRef(route.source.counterpartyEntityId) !== self;
    if (owner !== self) return halt(`CROSS_J_BOOK_PROGRESS_WRONG_OWNER: order=${route.orderId} owner=${owner} current=${h.id}`);
    const sameSeq = Math.floor(Number(route.fillSeq ?? 0)) === Math.floor(Number(data.fillSeq));
    const same: Result<boolean, EntityError> = sameSeq ? map(proofRatioE(route), (ratio) => ratio === Math.floor(Number(data.cumulativeFillRatio))) : ok(false);
    type Filled = { readonly host: BookHost; readonly changed: boolean };
    return chain(same, (isSame): Result<Filled, EntityError> => {
      if (isSame) {
        const touched = mapSet(admissions0, key, { ...admission, updatedAt: now });
        if (!data.cancelRemainder) return ok({ host: { ...h, admissions: touched }, changed: false });
        return map(markAdmissionClosed(touched, route.source.entityId, route.orderId, now, "cancel_request"), (closed) => {
          const mirror = h.swaps?.get(route.orderId);
          const swaps = h.swaps !== undefined && mirror !== undefined && remoteSource && (mirror.status === "resting" || mirror.status === "partially_filled")
            ? mapSet(h.swaps, route.orderId, { ...mirror, status: "clear_requested" as const, clearingPolicy: "cancel_and_clear" as const, updatedAt: now }) : h.swaps;
          return { host: { ...h, admissions: closed, swaps }, changed: false };
        });
      }
      const currentSeq = Math.floor(Number(route.fillSeq ?? 0));
      if (Math.floor(Number(data.fillSeq)) <= currentSeq) return halt(`CROSS_J_BOOK_PROGRESS_STALE: order=${route.orderId} seq=${data.fillSeq} current=${currentSeq}`);
      const progressed = chain(applyRatioProgress(route, data.fillSeq, Math.floor(Number(data.cumulativeFillRatio)), now, "CROSS_J_BOOK_PROGRESS_INVALID"),
        (next) => (data.cancelRemainder ? map(transitionE(next, "clear_requested", now), (r): CrossRoute => ({ ...r, clearingPolicy: "cancel_and_clear" })) : ok(next)));
      return chain(progressed, (next) => {
        const mirror = h.swaps?.get(route.orderId);
        const swaps: Result<ReadonlyMap<string, CrossRoute> | undefined, EntityError> = h.swaps !== undefined && mirror !== undefined && remoteSource ? map(mergeCrossRoute(mirror, next), (m) => mapSet(h.swaps ?? new Map<string, CrossRoute>(), route.orderId, m)) : ok(h.swaps);
        return chain(swaps, (s) => chain(updateRowForProgress({ ...h, swaps: s, admissions: mapSet(admissions0, key, { ...admission, route: next, updatedAt: now }) }, next), (host) =>
          next.status === "partially_filled" ? ok({ host, changed: true })
            : map(markAdmissionClosed(host.admissions, next.source.entityId, next.orderId, now, data.cancelRemainder ? "cancel_request" : "fill_closed"), (admissions) => ({ host: { ...host, admissions }, changed: true }))));
      });
    });
  });
};
/** og routeBookOwnerEntityId / resolveLocalBookOwner (followups): the stored owner, else the canonical stack owner; none means this Entity. */
const localBookOwner = (h: BookHost, route: CrossRoute): Result<boolean, EntityError> =>
  map(route.bookOwnerEntityId ? ok(route.bookOwnerEntityId) : fatalCross(crossBookOwner(route)), (owner) => { const o = entityRef(owner); return !o || o === entityRef(h.id); });
/** og applyCommittedFillProgress: a cancel at or below the committed ratio only requests the clear; otherwise the ratio advances and a terminal one requests it. */
const committedFillProgress = (route: CrossRoute, fill: CrossProgress, ratio: number, at: number): Result<CrossRoute, EntityError> => chain(proofRatioE(route), (current) => {
  if (fill.cancelRemainder && ratio <= current) return map(transitionE(route, "clear_requested", at), (r): CrossRoute => ({ ...r, clearingPolicy: "cancel_and_clear" }));
  return chain(applyRatioProgress(route, fill.fillSeq, ratio, at, "CROSS_J_FILL_PROGRESS_INVALID"), (next) => chain(transitionE(route, next.status, at), () =>
    chain(fatalCross(isCrossFillTerminal(next, { nextRatio: ratio, cancelRemainder: fill.cancelRemainder })), (terminal) => !terminal ? ok(next)
      : map(transitionE(next, "clear_requested", at), (r): CrossRoute => ({ ...r, clearingPolicy: fill.cancelRemainder || ratio < MAX_FILL ? "cancel_and_clear" : "full_fill" })))));
});
/**
 * og applySourceHubCrossJurisdictionFillProgress: the source hub's route mirror takes the progress (a duplicate or late one is ignored); a terminal
 * progress removes a local book row and asks this Entity's proposer for the clear.
 */
export const sourceHubFillProgress = (h: BookHost, fill: CrossProgress): Result<{ readonly host: BookHost; readonly applied: boolean; readonly outputs: readonly CrossEntityOutput[] }, EntityError> => {
  const at = h.timestamp, route = h.swaps?.get(fill.orderId), swaps0 = h.swaps, none = { host: h, applied: false, outputs: [] as readonly CrossEntityOutput[] };
  if (route === undefined || swaps0 === undefined)
    return halt(`CROSS_J_FILL_ROUTE_MISSING: entity=${shortTail(h.id, 4)} offer=${shortTail(fill.orderId, 12)} ratio=${fill.cumulativeFillRatio} cancel=${Boolean(fill.cancelRemainder)}`);
  if (entityRef(h.id) !== entityRef(route.source.counterpartyEntityId)) return halt(`CROSS_J_FILL_SOURCE_HUB_REQUIRED: order=${fill.orderId} entity=${h.id}`);
  if (fill.routeHash && route.routeHash && fill.routeHash.toLowerCase() !== route.routeHash.toLowerCase()) return halt(`CROSS_J_FILL_ROUTE_HASH_MISMATCH: order=${fill.orderId} got=${fill.routeHash} expected=${route.routeHash}`);
  if (isCrossTerminal(route.status) || route.status === "clear_requested" || route.status === "clearing") return ok(none);
  if (!Number.isSafeInteger(fill.fillSeq) || !Number.isSafeInteger(fill.cumulativeFillRatio)) return halt(`CROSS_J_FILL_NOTICE_INVALID: order=${fill.orderId} seq=${String(fill.fillSeq)} ratio=${String(fill.cumulativeFillRatio)}`);
  const ratio = Math.max(0, Math.min(MAX_FILL, fill.cumulativeFillRatio)), currentSeq = Math.max(0, Math.floor(Number(route.fillSeq ?? 0) || 0)), incoming = fill.fillSeq;
  const isCancel = Boolean(fill.cancelRemainder) && incoming === currentSeq;
  const conflict: Result<boolean, EntityError> = incoming === currentSeq ? map(proofRatioE(route), (committed) => ratio !== committed) : ok(false);
  type Applied = { readonly host: BookHost; readonly applied: boolean; readonly outputs: readonly CrossEntityOutput[] };
  return chain(conflict, (stale): Result<Applied, EntityError> => {
    if (stale) return halt(`CROSS_J_FILL_NOTICE_STALE_CONFLICT: order=${fill.orderId} seq=${incoming} ratio=${ratio}`);
    if (!isCancel && incoming <= currentSeq) return ok(none);
    return chain(committedFillProgress(route, fill, ratio, at), (progressed) => {
      const next: CrossRoute = { ...progressed, updatedAt: at }, host: BookHost = { ...h, swaps: mapSet(swaps0, fill.orderId, next) };
      return chain(fatalCross(isCrossFillTerminal(next, { nextRatio: ratio, cancelRemainder: fill.cancelRemainder })), (terminal) => {
        if (!terminal) return ok({ host, applied: true, outputs: [] });
        const removed: Result<BookHost, EntityError> = chain(localBookOwner(host, next), (local) => !local ? ok(host)
          : chain(removeRowById(host, bookRowId(next.source.entityId, next.orderId)), (r) => map(markAdmissionClosed(r.host.admissions, next.source.entityId, next.orderId, at, "fill_closed"), (admissions) => ({ ...r.host, admissions }))));
        return chain(removed, (after) => {
          const signer = trimLower(h.validators[0] || "");
          if (!signer) return halt(`CROSS_J_SELF_SIGNER_MISSING:${next.orderId}:${h.id}`);
          const clear: CrossEntityOutput = { entityId: h.id, signerId: signer, txs: [{ type: "requestCrossJurisdictionClear", data: { orderId: next.orderId, cancelRemainder: Boolean(fill.cancelRemainder) } }] };
          return ok({ host: after, applied: true, outputs: [clear] });
        });
      });
    });
  });
};
/** og buildCrossJurisdictionFillProgressData. */
const progressOf = (i: CrossFillInstruction): CrossProgress => ({ orderId: i.offerId, ...opt("routeHash", i.route.routeHash || undefined), fillSeq: i.fillSeq, cumulativeFillRatio: i.fillRatio, cancelRemainder: i.cancelRemainder });
/** og applyCrossJurisdictionOrderbookFill: the book owner applies the progress, then hands it to the source hub (locally, or as a fill notice). */
export const orderbookFill = (h: BookHost, i: CrossFillInstruction): Result<{ readonly host: BookHost; readonly outputs: readonly CrossEntityOutput[] }, EntityError> => {
  const data = progressOf(i);
  return chain(bookFillToState(h, i.accountId, data), ({ host }) => {
    if (entityRef(i.route.source.counterpartyEntityId) === entityRef(host.id)) return map(sourceHubFillProgress(host, data), (s) => ({ host: s.host, outputs: s.outputs }));
    const sourceHub = trimLower(i.route.source.counterpartyEntityId), signer = trimLower(i.route.sourceHubSignerId || "");
    if (!sourceHub || !signer) return halt(`CROSS_J_FILL_NOTICE_SOURCE_HUB_MISSING:${i.offerId}`);
    return map(crossOutput(sourceHub, signer, [{ type: "crossJurisdictionFillNotice", data }]), (out) => ({ host, outputs: [out] }));
  });
};
/** og handleRemoveCrossJurisdictionBookOrderEntityTx: the named admitted route leaves this book; the source hub gets an ACK carrying this book's progress. */
export const removeCrossBookOrder = (h: BookHost, data: { readonly orderId: string; readonly sourceEntityId: string; readonly sourceAccountId?: string | undefined; readonly route?: CrossRoute | undefined; readonly reason?: string | undefined }): Result<BookHostStep, EntityError> => {
  if (data.route === undefined) return halt(`CROSS_J_BOOK_REMOVAL_ROUTE_MISSING:${data.orderId}`);
  return chain(canonRoute(data.route), (route) => {
    if (route.orderId !== data.orderId || entityRef(route.source.entityId) !== entityRef(data.sourceEntityId) || crossBookOwnerRef(route) !== entityRef(h.id)) return halt(`CROSS_J_BOOK_REMOVAL_ROUTE_MISMATCH:${data.orderId}`);
    const admission = h.admissions?.get(bookAdmissionKey(data.sourceEntityId, data.orderId));
    if (admission !== undefined && entityRef(admission.routeHash || "") !== entityRef(route.routeHash || "")) return halt(`CROSS_J_CANCEL_ADMISSION_ROUTE_MISMATCH:${data.orderId}`);
    return chain(removeRowById(h, bookRowId(data.sourceEntityId, data.orderId)), ({ host, removed }) => {
      const ackRoute = host.swaps?.get(data.orderId) ?? route;
      const ack: Result<readonly CrossEntityOutput[], EntityError> = !data.sourceAccountId ? ok([]) : (() => {
        const sourceHub = entityRef(ackRoute.source.counterpartyEntityId);
        if (!sourceHub || sourceHub === entityRef(host.id)) return halt(`CROSS_J_BOOK_REMOVAL_ACK_TARGET_INVALID:order=${ackRoute.orderId}:target=${sourceHub}`);
        const signer = crossRouteSigner(ackRoute, sourceHub);
        if (!signer) return halt(`CROSS_J_BOOK_REMOVAL_ACK_SIGNER_MISSING:order=${ackRoute.orderId}:target=${sourceHub}`);
        return map(crossOutput(sourceHub, signer, [{ type: "crossJurisdictionBookOrderRemoved", data: { orderId: ackRoute.orderId, sourceEntityId: ackRoute.source.entityId, sourceAccountId: data.sourceAccountId ?? "", route: ackRoute, removedAt: host.timestamp, reason: data.reason || "cancel_request" } }]), (o) => [o]);
      })();
      return chain(ack, (outputs) => map(markAdmissionClosed(host.admissions, data.sourceEntityId, data.orderId, host.timestamp, data.reason || "removeCrossJurisdictionBookOrder"), (admissions) =>
        hostStep({ ...host, admissions }, { outputs, messages: [`🌉 Cross-j book remove ${data.orderId}${data.reason ? `: ${data.reason}` : ""} ${removed ? "removed" : "not-present"}`] })));
    });
  });
};
/**
 * og handleCrossJurisdictionBookOrderRemovedEntityTx: the source hub closes the admission and requests the clear from the later of its mirror and the
 * ACK's progress. og's dispute-preparing branch (confirmDisputeBookRemoval + draftPreparedDisputeStartIfReady) belongs to the dispute port.
 */
export const bookOrderRemoved = (h: BookHost, data: { readonly orderId: string; readonly sourceEntityId: string; readonly sourceAccountId: string; readonly route: CrossRoute; readonly removedAt: number; readonly reason?: string | undefined }): Result<BookHostStep, EntityError> =>
  chain(canonRoute(data.route), (route) => {
    if (entityRef(h.id) !== entityRef(route.source.counterpartyEntityId)) return halt(`CROSS_J_BOOK_REMOVAL_ACK_SOURCE_HUB_REQUIRED:order=${route.orderId}:entity=${h.id}`);
    const visible = h.accounts.get(data.sourceAccountId), offer = visible?.offers.get(route.orderId), current = h.swaps?.get(route.orderId);
    const missing = `CROSS_J_BOOK_REMOVAL_ACK_SOURCE_STATE_MISSING:order=${route.orderId}:account=${data.sourceAccountId}`;
    if (current === undefined) return halt(missing);
    if (entityRef(current.routeHash || "") !== entityRef(route.routeHash || "")) return halt(`CROSS_J_BOOK_REMOVAL_ACK_ROUTE_HASH_MISMATCH:order=${route.orderId}`);
    if (isCrossTerminal(current.status)) return ok(hostStep(h));
    if (visible === undefined || offer?.crossJurisdiction === undefined) return halt(missing);
    return chain(markAdmissionClosed(h.admissions, route.source.entityId, route.orderId, data.removedAt, data.reason || "cancel_request"), (admissions) => {
      const seqOf = (r: CrossRoute): number => Math.max(0, Math.floor(Number(r.fillSeq ?? 0) || 0)), currentSeq = seqOf(current), carriedSeq = seqOf(route);
      return chain(proofRatioE(carriedSeq > currentSeq ? route : current), (ratio) => map(sourceHubFillProgress({ ...h, admissions }, {
        orderId: route.orderId, ...opt("routeHash", current.routeHash || undefined), fillSeq: carriedSeq > currentSeq ? currentSeq + 1 : currentSeq, cumulativeFillRatio: ratio, cancelRemainder: true,
      }), (s) => hostStep(s.host, { outputs: s.outputs, messages: [s.applied ? `🌉 Cross-j book removal committed ${route.orderId}` : `🌉 Cross-j book removal already cleared ${route.orderId}`] })));
    });
  });
/** og handleCrossJurisdictionFillNoticeEntityTx: the book owner's progress reaches the source hub. */
export const crossFillNotice = (h: BookHost, data: CrossProgress): Result<BookHostStep, EntityError> => map(sourceHubFillProgress(h, data), (s) => hostStep(s.host, {
  outputs: s.outputs, messages: [s.applied ? `🌉 Cross-j fill notice ${data.orderId} applied ${data.cumulativeFillRatio}` : `🌉 Cross-j fill notice ${data.orderId} duplicate seq ${Math.floor(Number(data.fillSeq))}`],
}));
/** og routeRemoteCrossJurisdictionBookCancels: a cross-j cancel whose book lives on a sibling hub becomes a removal request there; the admission starts resolving. */
export const routeRemoteCancels = (h: BookHost, cancels: readonly SwapRef[]): Result<{ readonly host: BookHost; readonly local: readonly SwapRef[]; readonly outputs: readonly CrossEntityOutput[] }, EntityError> =>
  foldResult<{ readonly host: BookHost; readonly local: readonly SwapRef[]; readonly outputs: readonly CrossEntityOutput[] }, SwapRef, EntityError>(cancels, { host: h, local: [], outputs: [] }, (acc, cancel) => {
    const route = acc.host.accounts.get(cancel.accountId)?.offers.get(cancel.offerId)?.crossJurisdiction;
    if (route === undefined || !acc.host.swaps?.has(cancel.offerId)) return ok({ ...acc, local: [...acc.local, cancel] });
    const current = trimLower(acc.host.id), sourceHub = trimLower(route.source.counterpartyEntityId);
    if (current !== sourceHub) return halt(`CROSS_J_CANCEL_SOURCE_HUB_REQUIRED:offer=${cancel.offerId}:entity=${current}:sourceHub=${sourceHub}`);
    const owner = crossBookOwnerRef(route);
    if (!owner) return halt(`CROSS_J_CANCEL_BOOK_OWNER_MISSING:offer=${cancel.offerId}`);
    if (owner === current) return ok({ ...acc, local: [...acc.local, cancel] });
    const signer = crossRouteSigner(route, owner);
    if (!signer) return halt(`CROSS_J_CANCEL_BOOK_OWNER_SIGNER_MISSING:offer=${cancel.offerId}:owner=${owner}`);
    return chain(markAdmissionResolving(acc.host.admissions, route, acc.host.timestamp), (admissions) => map(crossOutput(owner, signer, [{ type: "removeCrossJurisdictionBookOrder", data: {
      orderId: cancel.offerId, sourceEntityId: route.source.entityId, sourceAccountId: cancel.accountId, route, reason: "cancel_request" } }]), (out) => ({ host: { ...acc.host, admissions }, local: acc.local, outputs: [...acc.outputs, out] })));
  });
/** og applyCommittedSwapCancelsToOrderbook's cross branch: a committed removal of a still-visible cross-j offer closes its admission (`committed_cancel`). */
export const closeCommittedCrossCancels = (h: BookHost, cancels: readonly SwapRef[]): Result<BookHost, EntityError> => {
  const ext = h.ext;
  if (ext === undefined || cancels.length === 0) return ok(h);
  const gone = new Set<string>();
  return foldResult<BookHost, SwapRef, EntityError>(cancels, h, (acc, { accountId, offerId }) => {
    const id = swapKeyOf(accountId, offerId), pairs = pairsHolding(ext.books, id).filter(() => !gone.has(id));
    if (pairs.length !== 1) return ok(acc);
    gone.add(id);
    const route = acc.accounts.get(entityRef(accountId))?.offers.get(offerId)?.crossJurisdiction;
    return route === undefined ? ok(acc) : map(markAdmissionClosed(acc.admissions, route.source.entityId, offerId, acc.timestamp, "committed_cancel"), (admissions) => ({ ...acc, admissions }));
  });
};
/** og requireCommittedSwapOffer for a cross-j candidate its source Account already holds: the offer is committed, idle, and matches field for field. */
const committedCrossOfferError = (account: HubAccount, o: BookOffer & { readonly crossJurisdiction?: CrossRoute | undefined }): string | null => {
  const committed = account.offers.get(o.offerId), who = `account=${o.accountId} offer=${o.offerId}`;
  if (committed === undefined) return `ORDERBOOK_ORDER_NOT_COMMITTED: ${who}`;
  if (account.queued.some((tx) => (tx.type === "swap_resolve" || tx.type === "swap_cancel_request") && tx.offerId === o.offerId)) return `ORDERBOOK_ORDER_NOT_READY: ${who}`;
  for (const [field, value] of [["priceTicks", committed.priceTicks], ["quantizedGive", committed.quantizedGive], ["quantizedWant", committed.quantizedWant]] as const)
    if (typeof value !== "bigint" || value <= 0n) return `ORDERBOOK_ORDER_COMMITTED_INCOMPLETE: ${who} field=${field}`;
  if (committed.quantizedGive !== committed.giveAmount || committed.quantizedWant !== committed.wantAmount)
    return `ORDERBOOK_ORDER_COMMITTED_QUANTIZATION_DRIFT: ${who} give=${committed.giveAmount}/${committed.quantizedGive} want=${committed.wantAmount}/${committed.quantizedWant}`;
  return Number(committed.giveTokenId) !== o.giveTokenId || committed.giveTokenDecimals !== o.giveTokenDecimals || Number(committed.wantTokenId) !== o.wantTokenId || committed.wantTokenDecimals !== o.wantTokenDecimals
    || committed.quantizedGive !== o.giveAmount || committed.quantizedWant !== o.wantAmount || committed.maxFee !== o.maxFee || committed.minNetReceive !== o.minNetReceive || committed.priceTicks !== o.priceTicks
    || committed.makerIsLeft !== o.makerIsLeft || (committed.crossJurisdiction !== undefined) !== (o.crossJurisdiction !== undefined) ? `ORDERBOOK_ORDER_COMMITTED_MISMATCH: ${who}` : null;
};
/** og admitOrderbookOfferForMatching (cross branch): a working route only; a live Account; a pending or risk-rejected admission waits; any other failure halts. */
const admitCrossForMatching = (hub: Hub, o: BookOffer & { readonly crossJurisdiction: CrossRoute }): Result<boolean, EntityError> => {
  const status = o.crossJurisdiction.status;
  if (status !== "resting" && status !== "partially_filled") return halt(`CROSS_J_ORDERBOOK_ROUTE_NOT_WORKING: offer=${o.offerId} status=${status}`);
  const account = hub.accounts.get(entityRef(o.accountId));
  if (account !== undefined && !account.active) return ok(false);
  if (account?.offers.has(o.offerId)) { const bad = committedCrossOfferError(account, o); if (bad !== null) return halt(bad); }
  return chain(bookAdmissionFailure(hub.id, hub.crossAdmissions, o.crossJurisdiction, Number(hub.timestamp ?? 0)), (failure) => (failure === null ? ok(true) : failure.kind === "invalid" ? halt(failure.message) : ok(false)));
};
// ---- og entity/tx/handlers/cross-j/clear.ts (requestCrossJurisdictionClear, materializeCrossJurisdictionClear), payments/pull.ts (crossPullClose) and
// cross-j/sweep.ts (orderbookSweepCrossJurisdiction). og mutates the Entity candidate in place; each step here returns the next host and the Account work. ----
/** A cross-j step that also returns Account work (og AccountTxTarget[]). */
export type CrossHostStep = BookHostStep & { readonly accountTxs: readonly AccountTxTarget[] };
type CrossAcc = { readonly host: BookHost; readonly messages: readonly string[]; readonly outputs: readonly CrossEntityOutput[]; readonly accountTxs: readonly AccountTxTarget[] };
const crossAcc = (host: BookHost): CrossAcc => ({ host, messages: [], outputs: [], accountTxs: [] });
const accNote = (a: CrossAcc, message: string): CrossAcc => ({ ...a, messages: [...a.messages, message] });
const accStep = (a: CrossAcc): CrossHostStep => ({ ...hostStep(a.host, { outputs: a.outputs, messages: a.messages }), accountTxs: a.accountTxs });
const putRoute = <H extends BookHost>(h: H, route: CrossRoute): H => ({ ...h, swaps: mapSet(h.swaps ?? new Map<string, CrossRoute>(), route.orderId, route) });
/** og accountHasCrossPullCloseQueued: a cross_pull_close for this pull in the mempool or our pending frame. */
const pullCloseQueued = (a: HubAccount, pullId: string): boolean => a.queued.some((tx) => tx.type === "cross_pull_close" && tx.pullId === pullId);
/** og's proposer wake: an empty input to this Entity's validators[0], as stored. */
const selfWake = (h: BookHost): readonly CrossEntityOutput[] => { const v = h.validators[0]; return v ? [{ entityId: h.id, signerId: v, txs: [] }] : []; };
const fillRatioE = (r: CrossRoute): Result<number, EntityError> => map(fatalCross(crossFillAmounts(r)), (f) => f.fillRatio);
const closeProofE = (r: CrossRoute, binary: string): Result<CrossCloseProof, EntityError> => fatalCross(buildCrossCloseProof(r, binary));
/** og closeProofMatches / closeProofsMatch: route and binary hashes compare lowercased, the rest exactly. */
const closeProofSame = (a: CrossCloseProof, b: CrossCloseProof): boolean => a.orderId === b.orderId && lowerText(a.routeHash) === lowerText(b.routeHash) && a.sourcePullId === b.sourcePullId && a.targetPullId === b.targetPullId
  && a.fillRatio === b.fillRatio && a.cumulativeSourceAmount === b.cumulativeSourceAmount && a.cumulativeTargetAmount === b.cumulativeTargetAmount && lowerText(a.binaryHash) === lowerText(b.binaryHash) && a.closeMode === b.closeMode;
/** og requestPureCancel: nothing filled, so both pulls close at ratio 0 in one Hub cohort (the source here, the target through the sibling hub). */
const pureCancelClear = (a: CrossAcc, route: CrossRoute, accountId: string | null, orderId: string, cancel: boolean): Result<CrossHostStep, EntityError> => {
  if (!cancel) return ok(accStep(accNote(a, `🌉 Cross-j clear ${orderId} ignored: no pending fill`)));
  const at = a.host.timestamp, sourcePull = route.sourcePull, targetPull = route.targetPull;
  return chain(closeProofE(route, "0x"), (proof) => {
    const account = accountId === null ? undefined : a.host.accounts.get(accountId);
    if (accountId === null || sourcePull === undefined || targetPull === undefined || !(account?.pulls?.has(sourcePull.pullId) ?? false)) return ok(accStep(accNote(a, `🌉 Cross-j clear ${orderId} waiting for source close proof`)));
    return chain(cloneRouteE(route), (clone) => chain(transitionE({ ...clone, sourceCloseProof: proof }, "clearing", at), (command) => chain(crossOutput(route.target.entityId, route.targetHubSignerId, [{ type: "crossPullClose",
      data: { counterpartyEntityId: route.target.counterpartyEntityId, pullId: targetPull.pullId, binary: "0x", proof, route: command, description: `Cross-j ${orderId} paired pure-cancel target close` } }]), (out) =>
      map(transitionE({ ...route, sourceCloseProof: proof }, "clearing", at), (r): CrossHostStep => accStep({
        host: putRoute(a.host, { ...r, pendingClearRequestedAt: at, clearingPolicy: "cancel_and_clear" }), messages: [...a.messages, `🌉 Cross-j clear ${orderId} queued atomic Hub pure-cancel close`],
        outputs: [...a.outputs, out, ...selfWake(a.host)], accountTxs: [...a.accountTxs, { accountId, tx: { type: "cross_pull_close", pullId: sourcePull.pullId, binary: "0x", proof } }],
      })))));
  });
};
/** og requestFilledRouteReveal: a committed fill waits for the default proposer's ladder reveal (materializeCrossJurisdictionClear). */
const filledRevealClear = (a: CrossAcc, route: CrossRoute, accountId: string | null, orderId: string, cancel: boolean, ratio: number): Result<CrossHostStep, EntityError> => {
  const account = accountId === null ? undefined : a.host.accounts.get(accountId), pullId = route.sourcePull?.pullId ?? "";
  if (account === undefined) return ok(accStep(accNote(a, `❌ Cross-j clear ${orderId} blocked: no source account with ${route.source.entityId}`)));
  if (!(account.pulls?.has(pullId) ?? false)) return ok(accStep(accNote(a, `🌉 Cross-j clear ${orderId} ignored: source pull already closed`)));
  if (pullCloseQueued(account, pullId)) return ok(accStep(accNote(a, `🌉 Cross-j clear ${orderId} ignored: source pull close already queued`)));
  const at = a.host.timestamp;
  return map(transitionE(route, "clear_requested", at), (r) => accStep({
    ...a, host: putRoute(a.host, { ...r, pendingClearRequestedAt: at, clearingPolicy: cancel || ratio < MAX_FILL ? "cancel_and_clear" : "full_fill" }),
    messages: [...a.messages, `🌉 Cross-j clear ${orderId} awaiting proposer reveal ratio=${ratio}/${MAX_FILL}`], outputs: [...a.outputs, ...selfWake(a.host)],
  }));
};
/**
 * og handleRequestCrossJurisdictionClearEntityTx: a source user asks through its Account (swap_cancel_request); the source hub removes a still-resting
 * local row, then closes an unfilled route at once (pure cancel) or asks its proposer for the ladder reveal of the committed fill.
 */
export const requestCrossClear = (h: BookHost, data: { readonly orderId: string; readonly cancelRemainder?: boolean | undefined }): Result<CrossHostStep, EntityError> => {
  const orderId = data.orderId, cancel = data.cancelRemainder ?? false, stored = h.swaps?.get(orderId);
  if (stored === undefined) return halt(`CROSS_J_CLEAR_ROUTE_MISSING:${orderId}`);
  if (isCrossTerminal(stored.status)) return ok(accStep(accNote(crossAcc(h), `🌉 Cross-j clear ${orderId} ignored: route ${stored.status}`)));
  const self = entityRef(h.id), src = entityRef(stored.source.entityId), srcHub = entityRef(stored.source.counterpartyEntityId);
  // og findCrossJurisdictionOfferRoute: the Account offer's opening snapshot must name the same route
  const peer = self === src ? srcHub : self === srcHub ? src : "", offerRoute = peer ? h.accounts.get(peer)?.offers.get(orderId)?.crossJurisdiction : undefined;
  const snapshot: Result<void, EntityError> = offerRoute === undefined ? ok(undefined)
    : chain(canonRoute(offerRoute), (c) => (lowerText(c.routeHash) !== lowerText(stored.routeHash) ? halt(`CROSS_J_CLEAR_ROUTE_HASH_MISMATCH:${orderId}`) : ok(undefined)));
  return chain(snapshot, () => chain(cloneRouteE(stored), (route): Result<CrossHostStep, EntityError> => {
    if (self !== srcHub) {
      // og requestClearThroughSourceAccount: the user cannot address the remote hub; its Account carries the cancel to the source hub
      if (self !== src) return halt(`CROSS_J_CLEAR_SOURCE_PARTICIPANT_REQUIRED:${orderId}:${h.id}:${src}`);
      if (h.accounts.get(srcHub)?.offers.get(orderId)?.crossJurisdiction === undefined) return halt(`CROSS_J_CLEAR_SOURCE_OFFER_MISSING:${orderId}:${h.id}:${srcHub}`);
      return chain(cancel ? ok(1) : fillRatioE(route), (ratio) => ratio <= 0 ? ok(accStep(accNote(crossAcc(h), `🌉 Cross-j clear ${orderId} ignored: no pending fill`)))
        : map(transitionE(route, "clear_requested", h.timestamp), (r) => accStep({
          host: putRoute(h, { ...r, pendingClearRequestedAt: h.timestamp, clearingPolicy: cancel ? "cancel_and_clear" : "manual" }), messages: [`🌉 Cross-j clear ${orderId} queued through source Account`], outputs: [],
          accountTxs: [{ accountId: srcHub, tx: { type: "swap_cancel_request", offerId: orderId } }],
        })));
    }
    return chain(canonRoute(route), (canonical) => {
      if (!canonical.sourcePull || !canonical.targetPull) return halt(`CROSS_J_CLEAR_CORRUPT_ROUTE: order=${orderId} pull commitments missing`);
      if (canonical.status === "clearing") return ok(accStep(accNote(crossAcc(h), `🌉 Cross-j clear ${orderId} ignored: close already queued`)));
      return chain(fillRatioE(canonical), (ratio) => {
        const accountId = h.accounts.has(src) ? src : null;
        // og: the user's Account offer stays open until the pull close deletes it; only a still-resting local book row leaves before the reveal
        const removed = accountId === null ? ok({ host: h, removed: false }) : removeRowById(h, `${accountId}:${orderId}`);
        return chain(removed, ({ host, removed: gone }) => {
          const a = gone ? accNote(crossAcc(host), `🌉 Cross-j clear ${orderId} removed live book order`) : crossAcc(host);
          return ratio <= 0 ? pureCancelClear(a, canonical, accountId, orderId, cancel) : filledRevealClear(a, canonical, accountId, orderId, cancel, ratio);
        });
      });
    });
  }));
};
/** og materializeCrossJurisdictionClear data: the default proposer's public reveal of the committed fill. */
export type CrossClearReveal = { readonly proposerSignerId: string; readonly orderId: string; readonly binary: string; readonly proof: CrossCloseProof };
/**
 * og handleMaterializeCrossJurisdictionClearEntityTx: every validator verifies the proposer's reveal against the committed source pull (the private
 * seed never enters replay), then the hub closes its source pull and sends the paired target close to the sibling hub.
 */
export const materializeCrossClear = (h: BookHost, data: CrossClearReveal): Result<CrossHostStep, EntityError> => {
  const { orderId, binary } = data, expected = entityRef(h.validators[0] || ""), claimed = entityRef(data.proposerSignerId);
  if (!expected || claimed !== expected) return crossReject2(`CROSS_J_CLEAR_MATERIALIZE_PROPOSER_INVALID:${claimed || "missing"}:${expected || "missing"}`);
  const stored = h.swaps?.get(orderId);
  if (stored === undefined || stored.status !== "clear_requested") return halt(`CROSS_J_CLEAR_MATERIALIZE_INTENT_MISSING:${orderId}`);
  return chain(canonRoute(stored), (route) => {
    const sp = route.sourcePull;
    if (sp === undefined || route.targetPull === undefined) return halt(`CROSS_J_CLEAR_MATERIALIZE_PULLS_MISSING:${orderId}`);
    if (entityRef(route.source.counterpartyEntityId) !== entityRef(h.id)) return halt(`CROSS_J_CLEAR_MATERIALIZE_SOURCE_HUB_MISMATCH:${orderId}`);
    return chain(fillRatioE(route), (fillRatio) => {
      if (fillRatio <= 0) return halt(`CROSS_J_CLEAR_MATERIALIZE_FILL_MISSING:${orderId}`);
      const decoded = verifyHashLadderBinary({ fullHash: sp.fullHash, partialRoot: sp.partialRoot }, binary);
      if (!decoded.ok) return halt(`CROSS_J_CLEAR_MATERIALIZE_BINARY_INVALID:${orderId}:${decoded.error.reason}`);
      if (decoded.value.fillRatio !== fillRatio) return halt(`CROSS_J_CLEAR_MATERIALIZE_RATIO_MISMATCH:${orderId}:${decoded.value.fillRatio}:${fillRatio}`);
      return chain(closeProofE(route, binary), (proof) => {
        if (!closeProofSame(data.proof, proof)) return halt(`CROSS_J_CLEAR_MATERIALIZE_PROOF_MISMATCH:${orderId}`);
        const accountId = entityRef(route.source.entityId), account = h.accounts.get(accountId);
        if (account === undefined || !(account.pulls?.has(sp.pullId) ?? false)) return halt(`CROSS_J_CLEAR_MATERIALIZE_SOURCE_PULL_MISSING:${orderId}`);
        if (pullCloseQueued(account, sp.pullId)) return halt(`CROSS_J_CLEAR_MATERIALIZE_ALREADY_QUEUED:${orderId}`);
        // og writes the stored route (its fork), which must carry the canonical hash exactly
        return chain(cloneRouteE(stored), (fork) => {
          if (route.routeHash && fork.routeHash !== route.routeHash) return halt(`CROSS_J_CLEAR_MATERIALIZE_ROUTE_FORK_MISMATCH:${orderId}`);
          const source = fork.sourcePull, target = fork.targetPull, at = h.timestamp;
          if (source === undefined) return halt(`CROSS_J_CLEAR_SOURCE_PULL_MISSING:${fork.orderId}`);
          if (target === undefined) return halt(`CROSS_J_CLEAR_TARGET_PULL_MISSING:${orderId}`);
          return chain(cloneRouteE(fork), (clone) => chain(transitionE({ ...clone, sourceCloseProof: proof }, "clearing", at), (command) => chain(crossOutput(fork.target.entityId, fork.targetHubSignerId, [{ type: "crossPullClose",
            data: { counterpartyEntityId: fork.target.counterpartyEntityId, pullId: target.pullId, binary, proof, route: command, description: `Cross-j ${fork.orderId} paired target close ${fillRatio}/${MAX_FILL}` } }]), (out) =>
            map(transitionE({ ...fork, sourceCloseProof: proof }, "clearing", at), (next) => accStep({
              host: putRoute(h, next), messages: [`🌉 Cross-j clear ${orderId} queued atomic Hub source+target close ratio=${fillRatio}/${MAX_FILL}`], outputs: [out, ...selfWake(h)],
              accountTxs: [{ accountId, tx: { type: "cross_pull_close", pullId: source.pullId, binary, proof } }],
            })))));
        });
      });
    });
  });
};
/** og crossPullClose Entity tx data. */
export type CrossPullCloseData = { readonly counterpartyEntityId: string; readonly pullId: string; readonly binary: string; readonly proof: CrossCloseProof; readonly route?: CrossRoute | undefined; readonly description?: string | undefined };
/** og proofRouteError (payments/pull.ts): the close must name this route and pull, open its ladder at the proof ratio, and never roll the informed ratio back. */
const pullCloseProofError = (route: CrossRoute, proof: CrossCloseProof, binary: string, leg: "source" | "target", command: CrossRoute | undefined): Result<string | null, EntityError> => {
  const routeHash = lowerText(route.routeHash);
  if (!routeHash) return ok("route hash missing");
  if (lowerText(proof.routeHash) !== routeHash) return ok(`route hash ${proof.routeHash} != ${routeHash}`);
  if (command !== undefined) {
    if (command.orderId !== proof.orderId) return ok(`command route order ${command.orderId} != ${proof.orderId}`);
    if (lowerText(command.routeHash) !== lowerText(proof.routeHash)) return ok(`command route hash ${command.routeHash} != ${proof.routeHash}`);
    if (command.sourcePull?.pullId !== proof.sourcePullId) return ok(`command source pull ${command.sourcePull?.pullId} != ${proof.sourcePullId}`);
    if (command.targetPull?.pullId !== proof.targetPullId) return ok(`command target pull ${command.targetPull?.pullId} != ${proof.targetPullId}`);
  }
  if (proof.orderId !== route.orderId) return ok(`order ${proof.orderId} != ${route.orderId}`);
  const { sourcePull, targetPull } = route;
  if (!sourcePull || !targetPull) return ok("pull commitments missing");
  if (proof.sourcePullId !== sourcePull.pullId) return ok(`source pull ${proof.sourcePullId} != ${sourcePull.pullId}`);
  if (proof.targetPullId !== targetPull.pullId) return ok(`target pull ${proof.targetPullId} != ${targetPull.pullId}`);
  return chain(fatalCross(crossCloseBinaryHash(binary)), (binaryHash) => {
    if (lowerText(proof.binaryHash) !== binaryHash.toLowerCase()) return ok("binary hash mismatch");
    const commitment = leg === "source" ? sourcePull : targetPull;
    return chain(fatalCross(verifyHashLadderBinary({ fullHash: commitment.fullHash, partialRoot: commitment.partialRoot }, binary)), (decoded) => {
      if (decoded.fillRatio !== proof.fillRatio) return ok(`binary ratio ${decoded.fillRatio} != proof ${proof.fillRatio}`);
      if (leg === "target") {
        const sourceProof = route.sourceCloseProof ?? command?.sourceCloseProof;
        if (!sourceProof) return ok("source close proof missing");
        if (!closeProofSame(sourceProof, proof)) return ok("source close proof mismatch");
      }
      return chain(proofRatioE(route), (routeRatio) => {
        if (proof.fillRatio < routeRatio) return ok(`ratio ${proof.fillRatio} < informed ${routeRatio}`);
        if (leg !== "source" && routeRatio <= 0) return ok(null);
        // og: off-chain progress never gates the close; the expectation is built at the proof's ratio
        return map(closeProofE(withCloseProofProgress(route, proof, route.updatedAt), binary), (e) => proof.fillRatio !== e.fillRatio ? `ratio ${proof.fillRatio} != ${e.fillRatio}`
          : proof.cumulativeSourceAmount !== e.cumulativeSourceAmount ? `source amount ${proof.cumulativeSourceAmount} != ${e.cumulativeSourceAmount}`
          : proof.cumulativeTargetAmount !== e.cumulativeTargetAmount ? `target amount ${proof.cumulativeTargetAmount} != ${e.cumulativeTargetAmount}`
          : proof.closeMode !== e.closeMode ? `mode ${proof.closeMode} != ${e.closeMode}` : null);
      });
    });
  });
};
/**
 * og handleCrossPullCloseEntityTx: the source hub closes its user's source pull, or the target hub closes the paired target pull the source hub sent;
 * terminal or not-yet-clearing routes only note it, invalid close data halts, and every gate runs before the mirror moves.
 */
export const crossPullCloseTx = (h: BookHost, data: CrossPullCloseData): Result<CrossHostStep, EntityError> => {
  const { pullId, binary, proof } = data, cp = entityRef(data.counterpartyEntityId), self = entityRef(h.id);
  if (!h.accounts.has(cp)) return halt(`CROSS_J_PULL_CLOSE_ACCOUNT_MISSING:${data.counterpartyEntityId}`);
  const routes = [...(h.swaps?.values() ?? [])];
  const source = routes.find((r) => r.sourcePull?.pullId === pullId && entityRef(r.source.counterpartyEntityId) === self && entityRef(r.source.entityId) === cp);
  const found = source ?? routes.find((r) => r.targetPull?.pullId === pullId && entityRef(r.target.entityId) === self && entityRef(r.target.counterpartyEntityId) === cp);
  if (found === undefined) return halt(`CROSS_J_PULL_CLOSE_ROUTE_MISSING:${pullId}`);
  const leg = source !== undefined ? "source" : "target", short = pullId.slice(0, 8);
  if (isCrossTerminal(found.status)) return ok(accStep(accNote(crossAcc(h), `❌ Cross-j ${leg} pull close ${short} blocked: route ${found.status}`)));
  if (leg === "source" && found.status !== "clearing" && found.status !== "clear_requested") return ok(accStep(accNote(crossAcc(h), `❌ Cross-j source pull close ${short} blocked: route ${found.status}`)));
  return chain(pullCloseProofError(found, proof, binary, leg, data.route), (bad) => bad !== null ? halt(`CROSS_J_PULL_CLOSE_PROOF_INVALID:${pullId}:${bad}`)
    : chain(fatalCross(cloneCloseProof(proof)), (closeProof) => chain(cloneRouteE(found), (fork) => {
      const withProof: CrossRoute = { ...fork, sourceCloseProof: closeProof }, at = h.timestamp;
      const next: Result<CrossRoute, EntityError> = leg === "source" ? ok(withProof) : (() => {
        const progressed = withCloseProofProgress(withProof, proof, withProof.updatedAt);
        return transitionE({ ...progressed, clearingPolicy: "cancel_and_clear", pendingClearRequestedAt: progressed.pendingClearRequestedAt || at }, "clearing", at);
      })();
      return map(next, (route) => accStep({ host: putRoute(h, route), messages: [], outputs: selfWake(h), accountTxs: [{ accountId: cp, tx: { type: "cross_pull_close", pullId, binary, proof } }] }));
    })));
};
/** og handleOrderbookSweepCrossJurisdictionEntityTx: the book-TTL sweep; the source hub clears each expired route it owns (cancelling the remainder). */
export const crossSweep = (h: BookHost, reason?: string | undefined): Result<CrossHostStep, EntityError> => {
  type Sweep = CrossAcc & { readonly expired: number; readonly closed: number; readonly waiting: number };
  const start: Sweep = { ...crossAcc(h), expired: 0, closed: 0, waiting: 0 };
  return map(foldResult<Sweep, readonly [string, CrossRoute], EntityError>([...(h.swaps?.entries() ?? [])], start, (s, [orderId, route]) => {
    if (isCrossTerminal(route.status)) return ok(s);
    if (!isCrossExpired(route, h.timestamp)) return ok({ ...s, waiting: s.waiting + 1 });
    const sourceHub = lowerText(route.source.counterpartyEntityId);
    if (!sourceHub) return halt(`CROSS_J_SWEEP_SOURCE_HUB_MISSING:${orderId}`);
    if (lowerText(s.host.id) !== sourceHub) return ok({ ...s, expired: s.expired + 1, waiting: s.waiting + 1 });
    return map(requestCrossClear(s.host, { orderId, cancelRemainder: true }), (c): Sweep => ({
      host: c.host, messages: [...s.messages, ...c.messages], outputs: [...s.outputs, ...c.outputs], accountTxs: [...s.accountTxs, ...c.accountTxs],
      expired: s.expired + 1, closed: s.closed + (c.accountTxs.some((t) => t.tx.type === "cross_pull_close") ? 1 : 0), waiting: s.waiting,
    }));
  }), (s) => accStep(accNote(s, `🌉 Cross-j orderbook sweep${reason ? `: ${reason}` : ""} expired=${s.expired} closedOffers=${s.closed} waiting=${s.waiting}`)));
};
// ---- og entity/tx/handlers/account-cross-j-followups.ts applyCommittedCrossJurisdictionAccountTxFollowup: what a committed cross_pull_lock /
// cross_pull_close does to the Entity (route mirror, one-shot user authorization, expiry hook, book admission or removal). ----
/** The Entity fields a committed cross-j Account tx rewrites: the book host plus the user authorizations and the crontab. */
export type CommittedCrossHost = BookHost & { readonly auths?: ReadonlyMap<string, CrossRoute> | undefined; readonly crontab?: Crontab | undefined };
export type CommittedCrossStep = { readonly host: CommittedCrossHost; readonly outputs: readonly CrossEntityOutput[]; readonly messages: readonly string[]; readonly created: readonly SwapOfferEvent[]; readonly handled: boolean };
/** og routeBookOwnerEntityId: the stored owner, else the canonical market's. */
const bookOwnerIdE = (route: CrossRoute): Result<string, EntityError> => map(route.bookOwnerEntityId ? ok(route.bookOwnerEntityId) : fatalCross(crossBookOwner(route)), entityRef);
/** og removeOrRouteCrossJurisdictionBookOrder: a local book drops the row and closes the admission; a sibling book owner gets a removal request. */
const removeOrRouteBookOrder = (s: CommittedCrossStep, route: CrossRoute, reason: string): Result<CommittedCrossStep, EntityError> => chain(bookOwnerIdE(route), (owner): Result<CommittedCrossStep, EntityError> => {
  const h = s.host;
  if (!owner || owner === entityRef(h.id))
    return chain(removeRowById(h, bookRowId(route.source.entityId, route.orderId)), (r) => map(markAdmissionClosed(r.host.admissions, route.source.entityId, route.orderId, h.timestamp, reason), (admissions) => ({ ...s, host: { ...h, ext: r.host.ext, admissions } })));
  const signer = crossRouteSigner(route, owner);
  if (!signer) return halt(`CROSS_J_ROUTE_SIGNER_MISSING:${route.orderId}:${owner}`);
  return map(crossOutput(owner, signer, [{ type: "removeCrossJurisdictionBookOrder", data: { orderId: route.orderId, sourceEntityId: route.source.entityId, route, reason } }]), (out) => ({ ...s, outputs: [...s.outputs, out] }));
});
/** og committedPullMatchesRoute: the committed lock is exactly the route's pull on this leg. */
const committedPullMatches = (tx: TxOf<"cross_pull_lock">, route: CrossRoute, leg: "source" | "target"): boolean => {
  const pull = leg === "source" ? route.sourcePull : route.targetPull, b = tx.crossJurisdiction;
  if (pull === undefined || b.leg !== leg || b.orderId !== route.orderId || lowerText(b.routeHash) !== lowerText(route.routeHash)) return false;
  return tx.pullId === pull.pullId && Number(tx.tokenId) === pull.tokenId && tx.amount === pull.signedAmount && lowerText(tx.fullHash) === pull.fullHash.toLowerCase() && lowerText(tx.partialRoot) === pull.partialRoot.toLowerCase();
};
/** og getCommittedPullRole: which leg this pull is, and whether this Entity is the source hub committing it with the source user. */
const committedPullRole = (route: CrossRoute, self: string, cp: string, pullId: string): { readonly leg: "source" | "target"; readonly sourceHub: boolean } | null => {
  const su = entityRef(route.source.entityId), sh = entityRef(route.source.counterpartyEntityId), th = entityRef(route.target.entityId), tu = entityRef(route.target.counterpartyEntityId);
  const sp = route.sourcePull?.pullId === pullId, tp = route.targetPull?.pullId === pullId;
  if (sp && self === sh && cp === su) return { leg: "source", sourceHub: true };
  if (sp && self === su && cp === sh) return { leg: "source", sourceHub: false };
  if (tp && ((self === th && cp === tu) || (self === tu && cp === th))) return { leg: "target", sourceHub: false };
  return null;
};
/** og admitCommittedSourcePullToBook: the expiry sweep hook, then the book admission (here, or at the sibling book owner). */
const admitCommittedPull = (s: CommittedCrossStep, route: CrossRoute): Result<CommittedCrossStep, EntityError> => {
  const noted: CommittedCrossStep = { ...s, messages: [...s.messages, `🌉 Cross-j swap ${route.orderId} committed by both Account legs`] }, crontab = s.host.crontab;
  if (crontab === undefined) return halt(`CROSS_J_EXPIRY_CRONTAB_MISSING:${route.orderId}`);
  const expiresAt = Math.floor(Number(route.expiresAt || 0));
  if (!Number.isSafeInteger(expiresAt) || expiresAt <= s.host.timestamp) return halt(`CROSS_J_EXPIRY_INVALID:${route.orderId}:${String(route.expiresAt)}`);
  const hooked: CommittedCrossStep = { ...noted, host: { ...noted.host, crontab: scheduleHook(crontab, { id: `cross-j-expiry:${route.orderId}`, type: "cross_j_orderbook_sweep", triggerAt: expiresAt, data: { reason: `cross-j-expiry:${route.orderId}` } }) } };
  return chain(bookOwnerIdE(route), (owner) => chain(cloneRouteE(route), (clone) => {
    const data = { route: clone, reason: "atomic_account_pair_committed" };
    // og drops a local admission's outputs; only its created offers reach this frame's matcher
    if (owner === entityRef(s.host.id)) return map(admitBookOrder(hooked.host, data), (a): CommittedCrossStep => ({ ...hooked, host: { ...hooked.host, ...a.host }, messages: [...hooked.messages, ...a.messages], created: [...hooked.created, ...a.created] }));
    const signer = crossRouteSigner(route, owner);
    if (!signer) return halt(`CROSS_J_ROUTE_SIGNER_MISSING:${route.orderId}:${owner}`);
    return map(crossOutput(owner, signer, [{ type: "admitCrossJurisdictionBookOrder", data }]), (out) => ({ ...hooked, outputs: [...hooked.outputs, out] }));
  }));
};
/** og queueBookAdmissionOnCommittedPull: the carried route joins the mirror; each route naming this pull goes resting, a user's one-shot authorization is consumed, the source hub admits the order. */
const committedPullLock = (s0: CommittedCrossStep, cp: string, tx: TxOf<"cross_pull_lock">, committedAt: number): Result<CommittedCrossStep, EntityError> => chain(cloneRouteE(tx.crossJurisdictionRoute), (carried) => {
  const existing = s0.host.swaps?.get(carried.orderId);
  if (existing?.routeHash && carried.routeHash && existing.routeHash.toLowerCase() !== carried.routeHash.toLowerCase()) return halt(`CROSS_J_COMMITTED_PULL_ROUTE_CONFLICT:${carried.orderId}`);
  const s: CommittedCrossStep = { ...s0, host: putRoute(s0.host, { ...existing, ...carried }) }, self = entityRef(s.host.id);
  return foldResult<CommittedCrossStep, string, EntityError>([...(s.host.swaps?.keys() ?? [])], s, (acc, orderId) => {
    const route = acc.host.swaps?.get(orderId), role = route === undefined ? null : committedPullRole(route, self, entityRef(cp), tx.pullId);
    if (route === undefined || role === null) return ok(acc);
    if (!committedPullMatches(tx, route, role.leg)) return halt(`CROSS_J_COMMITTED_PULL_ROUTE_MISMATCH: route=${route.orderId} leg=${role.leg} pull=${tx.pullId}`);
    const userLeg = self === entityRef(route.source.entityId) || self === entityRef(route.target.counterpartyEntityId), auth = acc.host.auths?.get(route.orderId);
    if (userLeg && (auth === undefined || entityRef(auth.routeHash || "") !== entityRef(route.routeHash || ""))) return halt(`CROSS_J_COMMITTED_PULL_AUTH_MISSING:${route.orderId}:${self}`);
    const host: CommittedCrossHost = userLeg && acc.host.auths !== undefined ? { ...acc.host, auths: mapDelete(acc.host.auths, route.orderId) } : acc.host;
    return chain(cloneRouteE(route), (clone) => chain(transitionE(clone, "resting", committedAt), (resting) => {
      const next: CommittedCrossStep = { ...acc, host: putRoute(host, { ...route, ...resting }), handled: true };
      return role.sourceHub ? admitCommittedPull(next, resting) : ok(next);
    }));
  });
});
/** og assertTerminalPullReplay: a close replayed against a terminal mirror must be the recorded close exactly. */
const terminalPullReplay = (route: CrossRoute, fillRatio: number, binary: string, supplied: CrossCloseProof): Result<boolean, EntityError> => {
  if (!isCrossTerminal(route.status)) return ok(false);
  const proof = route.sourceCloseProof;
  return chain(fatalCross(crossFillAmounts(route)), (c) => chain(fatalCross(crossCloseBinaryHash(binary)), (binaryHash) => {
    if (!proof || fillRatio !== c.fillRatio || proof.fillRatio !== c.fillRatio || entityRef(proof.binaryHash) !== entityRef(binaryHash) || proof.cumulativeSourceAmount !== c.filledSourceAmount || proof.cumulativeTargetAmount !== c.filledTargetAmount)
      return halt(`CROSS_J_TERMINAL_PULL_REPLAY_MISMATCH: route=${route.orderId} ratio=${fillRatio}`);
    const same = supplied.orderId === proof.orderId && entityRef(supplied.routeHash) === entityRef(proof.routeHash) && supplied.sourcePullId === proof.sourcePullId && supplied.targetPullId === proof.targetPullId
      && supplied.closeMode === proof.closeMode && supplied.fillRatio === proof.fillRatio && supplied.cumulativeSourceAmount === proof.cumulativeSourceAmount && supplied.cumulativeTargetAmount === proof.cumulativeTargetAmount && entityRef(supplied.binaryHash) === entityRef(proof.binaryHash);
    return same ? ok(true) : halt(`CROSS_J_TERMINAL_PULL_PROOF_REPLAY_MISMATCH: route=${route.orderId}`);
  }));
};
/** og assertCrossPullCloseAllowed: the proof names this route, its amounts are the ratio's projection, it never rolls back, and a hub mirror is already clearing. */
const pullCloseAllowed = (route: CrossRoute, fillRatio: number, leg: "source" | "target", hubCommitted: boolean, proof: CrossCloseProof): Result<void, EntityError> => {
  if (proof.orderId !== route.orderId || entityRef(proof.routeHash) !== entityRef(route.routeHash || "") || proof.sourcePullId !== route.sourcePull?.pullId || proof.targetPullId !== route.targetPull?.pullId)
    return halt(`CROSS_J_PULL_CLOSE_PROOF_MISMATCH: route=${route.orderId}`);
  const ratio = BigInt(fillRatio), max = BigInt(MAX_FILL), project = (total: bigint): bigint => (ratio >= max ? total : (total * ratio) / max);
  if (proof.cumulativeSourceAmount !== project(BigInt(route.source.amount)) || proof.cumulativeTargetAmount !== project(BigInt(route.target.amount))) return halt(`CROSS_J_PULL_CLOSE_ECONOMICS_MISMATCH: route=${route.orderId} ratio=${fillRatio}`);
  return chain(proofRatioE(route), (committed): Result<void, EntityError> => {
    if (fillRatio < committed) return halt(`CROSS_J_PULL_CLOSE_ROLLBACK: route=${route.orderId} ratio=${fillRatio} informed=${committed}`);
    if (fillRatio <= 0 || !hubCommitted) return ok(undefined);
    const allowed = leg === "source" ? route.status === "clearing" || route.status === "clear_requested"
      : route.status === "resting" || route.status === "partially_filled" || route.status === "clear_requested" || route.status === "clearing";
    return allowed ? ok(undefined) : halt(`CROSS_J_PULL_CLOSE_STATE_INVALID: route=${route.orderId} leg=${leg} status=${route.status}`);
  });
};
/** og applyCrossPullCloseProgress + transitionTargetLegTerminal: the mirror takes the close at its ratio and ends settled, expired or cancelled; the expiry hook goes. */
const closeProgress = (s: CommittedCrossStep, orderId: string, proof: CrossCloseProof, fillRatio: number): Result<{ readonly step: CommittedCrossStep; readonly route: CrossRoute; readonly terminal: CrossStatus }, EntityError> => {
  const stored = s.host.swaps?.get(orderId), at = s.host.timestamp;
  if (stored === undefined) return halt(`CROSS_J_PULL_CLOSE_ROUTE_FORK_MISSING:${orderId}`);
  return chain(fatalCross(cloneCloseProof(proof)), (closeProof) => {
    const progressed: CrossRoute = { ...withCloseProofProgress(stored, proof, at), sourceCloseProof: closeProof, targetCloseProof: closeProof };
    return chain(progressed.status !== "clearing" ? transitionE(progressed, "clearing", at) : ok(progressed), (clearing) => {
      const terminal: CrossStatus = fillRatio > 0 ? "settled" : isCrossExpired(clearing, at) ? "expired" : "cancelled";
      return map(transitionE(clearing, terminal, at), (ended) => {
        const route: CrossRoute = terminal === "settled" ? { ...ended, settledAt: at } : ended, crontab = s.host.crontab;
        return { step: { ...s, host: { ...putRoute(s.host, route), ...opt("crontab", crontab === undefined ? undefined : cancelHook(crontab, `cross-j-expiry:${orderId}`)) } }, route, terminal };
      });
    });
  });
};
/** og applyCrossPullCloseFollowup: every mirrored route naming this pull on this Account projects the committed close; the source hub then drops or routes the book order. */
const committedPullClose = (s0: CommittedCrossStep, cp: string, tx: TxOf<"cross_pull_close">): Result<CommittedCrossStep, EntityError> => {
  const fillRatio = tx.proof.fillRatio;
  if (!Number.isSafeInteger(fillRatio) || fillRatio < 0 || fillRatio > MAX_FILL) return halt(`CROSS_J_CLOSE_PROOF_RATIO_INVALID:${fillRatio}`);
  return chain(fatalCross(decodeHashLadderBinary(tx.binary)), (decoded) => {
    if (decoded.fillRatio !== fillRatio) return halt(`CROSS_J_CLOSE_BINARY_RATIO_MISMATCH: pull=${tx.pullId} binary=${decoded.fillRatio} proof=${fillRatio}`);
    const self = entityRef(s0.host.id), peer = entityRef(cp);
    type Walk = { readonly step: CommittedCrossStep; readonly matched: boolean };
    return chain(foldResult<Walk, string, EntityError>([...(s0.host.swaps?.keys() ?? [])], { step: s0, matched: false }, (w, orderId) => {
      const route = w.step.host.swaps?.get(orderId);
      if (route === undefined) return ok(w);
      const su = entityRef(route.source.entityId), sh = entityRef(route.source.counterpartyEntityId), th = entityRef(route.target.entityId), tu = entityRef(route.target.counterpartyEntityId);
      const sourceHub = route.sourcePull?.pullId === tx.pullId && route.targetPull?.pullId !== undefined && self === sh && peer === su;
      const sourceUser = route.sourcePull?.pullId === tx.pullId && self === su && peer === sh;
      if (sourceHub || sourceUser) return chain(terminalPullReplay(route, fillRatio, tx.binary, tx.proof), (replay): Result<Walk, EntityError> => {
        if (replay) return map(sourceHub ? removeOrRouteBookOrder(w.step, route, "settled") : ok(w.step), (step) => ({ step, matched: true }));
        return chain(pullCloseAllowed(route, fillRatio, "source", sourceHub, tx.proof), () => chain(closeProgress(w.step, route.orderId, tx.proof, fillRatio), (p) =>
          map(sourceHub ? removeOrRouteBookOrder(p.step, p.route, p.terminal) : ok(p.step), (step) => ({ step, matched: true }))));
      });
      const targetUser = route.targetPull?.pullId === tx.pullId && self === tu && peer === th, targetHub = route.targetPull?.pullId === tx.pullId && self === th && peer === tu;
      if (!targetUser && !targetHub) return ok(w);
      return chain(terminalPullReplay(route, fillRatio, tx.binary, tx.proof), (replay) => replay ? ok({ step: w.step, matched: true })
        : chain(pullCloseAllowed(route, fillRatio, "target", targetHub, tx.proof), () => map(closeProgress(w.step, route.orderId, tx.proof, fillRatio), (p) => ({ step: p.step, matched: true }))));
    }), (w) => (w.matched ? ok({ ...w.step, handled: true }) : halt(`CROSS_J_PULL_CLOSE_ROUTE_MISSING: pull=${tx.pullId} order=${tx.proof.orderId}`)));
  });
};
/** og applyCommittedCrossJurisdictionAccountTxFollowup: `handled` is og's return (a cross-j tx that the HTLC followup must skip). */
export const committedCrossFollowup = (h: CommittedCrossHost, counterpartyId: string, tx: WireAccountTx, committedAt: number): Result<CommittedCrossStep, EntityError> => {
  const start: CommittedCrossStep = { host: h, outputs: [], messages: [], created: [], handled: false };
  return tx.type === "cross_pull_lock" ? committedPullLock(start, counterpartyId, tx, committedAt) : tx.type === "cross_pull_close" ? committedPullClose(start, counterpartyId, tx) : ok(start);
};
