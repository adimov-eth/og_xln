// STUB for oracle.test.ts: xln-pure's lexical accountId (string order), not present in this repo.
export const accountId = (a: string, b: string) => (a < b ? { left: a, right: b } : { left: b, right: a });
