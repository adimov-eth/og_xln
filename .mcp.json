{
  "mcpServers": {
    "ast-grep": {
      "type": "stdio",
      "command": "uvx",
      "args": ["--python", "3.13", "--from", "git+https://github.com/ast-grep/ast-grep-mcp", "ast-grep-server"],
      "env": {
        "AST_GREP_PATH": "uvx --from ast-grep-cli ast-grep"
      }
    }
  }
}
