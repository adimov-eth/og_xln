# q2-reject-granularity.grok-latest.out

harness: pi 0.84 --mode json, provider openrouter, packet-only, 2026-09-07 14:40 UTC, cost unknown (not reconciled)

{"verdict":"per-tx","answer":"TS is the reference; per-tx eviction is canonical. Owner canon forbids sender-caused Runtime halt, not extra drops. Rust’s lane retain and the comment “TS discards the whole origin lane” contradict start.ts:562-575 (evict frameTx only, retry, certify remaining same-signer txs). Per-lane changes committed proposal contents/roots and silently drops valid work.\n\n(b) Yes: remove reject-vs-halt from RJEA. WAL journals the input, not the env decision. rejectFailFast()/OnceLock inside the transition makes replay diverge (production drop vs unset halt). Always return a typed reject disposition; the Runtime loop applies fail-fast only outside production, once, after WAL commit. Scenario short-circuits and no NODE_ENV=production gate currently hide both bugs. Financial/Control `?` → InvalidLocalEntityTx fail_stop also violates the owner canon and must become the same typed reject.\n\n(c) Pin both engines: (1) reject.proposal.evict_only_malformed_tx — one signer, two txs, second MalformedEntityFrameInputError; mempool/proposal keep the first; equal roots. (2) reject.replay.env_independent_roots — same checkpoint+WAL under NODE_ENV=production and unset; equal per-frame roots/outbox. (3) reject.user_invalid_financial_control_typed_reject — those arms yield typed reject, replica continues, never InvalidLocalEntityTx halt.","references":["AGENTS.md RJEA purity; TS reference","owner canon 2026-09-05 never halt on sender fault","core/entity/consensus/proposal/start.ts:562-575 per-tx evict","rscore/crates/entity-kernel/src/kernel.rs:973-1040 lane drop + Financial/Control fail_stop","core/entity/tx/handlers/account/input-phases.ts:157-177","core/support/process/runtime-process.ts:82-86","rscore error.rs:54-69 OnceLock","WAL journals input not decision; no NODE_ENV=production gate; discard.ts:38 entity-input-staging.ts:306"]}
