# q3-rust-scope.grok-latest.out

harness: pi 0.84 --mode json, provider openrouter, packet-only, 2026-09-07 14:40 UTC, cost unknown (not reconciled)

{"verdict_a":"1","verdict_b":"i","answer":"Q3a: choose (1). Launch hubs/MMs are single-signer. Porting TS BFT (PROPOSE→PRECOMMIT→COMMIT, leader timeout, prepared-frame relay) into Rust before H1 is unused consensus surface. Safety: Rust already fail-stops multi-validator restore (SINGLE_SIGNER_REQUIRED) and strips proposedFrame/hashPrecommits/leaderTimeoutVote. Remaining holes are engine selection and the predicate split (TS: len==1 && threshold===1n vs Rust: len==1 && threshold==share). Unify on the intersection both engines already agree on: len==1 && threshold===1n && share===1n. Rust engine selector must refuse any other board; document single-signer-only H1 in AGENTS.md. Do not run TS BFT entities on the Rust H1 path.\n\nQ3b: choose (i). Chain is the dispute oracle. _boardMatches already accepts current plus two grace slots; activateBoard already shifts both. Off-chain CertifiedBoardRecord with one previousBoardHash rejects a Hanko signed under the first-retired board that chain would verify (HANKO_BOARD_AUTHORITY_INVALID). That is settlement-evidence divergence, not a docs issue. (ii) needs a pre-deploy Solidity cut and shrinks intended grace. (iii) leaves the mismatch. Add previousBoardHash2/previousBoardValidUntil2 and accept both when currentOnly=false.\n\nTests: (a) Rust reject 2-of-3; predicate vectors (1,1), (100,100), (100,1). (b) double rotate inside both grace windows; Hanko under first-retired board verifies off-chain iff chain would; expired slot2 rejected.","references":["HEAD f7ba455fc","core/entity/consensus (TS BFT)","core/entity/consensus/replica-validation.ts:142-149","rscore/crates/entity-kernel/src/consensus/single_signer.rs:319-321","entity-kernel/consensus/authority.rs:331-345","runtime/src/restore/entity/entity_consensus.rs:176-181","machine/types.rs:189-203","HankoVerifier.sol:346-357 _boardMatches","EntityProvider.activateBoard","core/hanko/signing.ts:663-666","engine/consensus/signing.rs:388-393","CertifiedBoardRecord previousBoardHash"]}
