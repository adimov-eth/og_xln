You are a senior protocol reviewer. Answer ONLY from this packet; no tools. Project: xln (TS reference; Rust port used for the H1 hub only). HEAD f7ba455fc. Two scope questions with code facts.
Q3a Rust Entity consensus scope. TS implements multi-validator BFT (PROPOSE→PRECOMMIT→COMMIT, leader timeout, prepared-frame relay) in core/entity/consensus. Rust implements ONLY single-signer certification: rscore/crates/entity-kernel/src/consensus/single_signer.rs:319-321 `if !authority.is_single_signer()? { return Err(SingleSignerAuthorityRequired) }`; restore rejects multi-validator boards (runtime/src/restore/entity/entity_consensus.rs:176-181 SINGLE_SIGNER_REQUIRED); the transport decoder rejects proposedFrame/hashPrecommits/leaderTimeoutVote fields (machine/types.rs:189-203). The single-signer predicate also differs: TS `validators.length===1 && threshold===1n` (core/entity/consensus/replica-validation.ts:142-149) vs Rust `len==1 && threshold == share` (entity-kernel/consensus/authority.rs:331-345). Launch plan: our own H1–H3 hubs + 2 market makers; hubs are single-signer entities today; multisig entities (2-of-3 company board, 3-of-4 scenario) exist in TS scenarios. Options: (1) declare Rust H1 single-signer-only for launch: engine selector refuses any multi-validator board on Rust, document in AGENTS.md, unify the predicate; (2) port BFT to Rust before launch.
Q3b previous-board grace. On-chain HankoVerifier.sol:346-357 `_boardMatches` accepts currentBoardHash, previousBoardHash (until previousBoardValidUntil) AND previousBoardHash2 (until previousBoardValidUntil2) when currentOnly=false; EntityProvider.activateBoard shifts two historical slots. Off-chain TS core/hanko/signing.ts:663-666 and Rust engine/consensus/signing.rs:388-393 accept only ONE previous board (CertifiedBoardRecord has a single previousBoardHash). Scenario: entity rotates its board twice inside the grace window; a counterparty holds a dispute Hanko signed under the first-retired board; chain verifies it, TS/Rust reject it (HANKO_BOARD_AUTHORITY_INVALID). Options: (i) add previousBoardHash2/previousBoardValidUntil2 to the off-chain certified record and accept both slots; (ii) restrict the chain to one slot (contracts are deploy-once; would need a Solidity change before deploy); (iii) leave as is and document.
Required output: JSON {verdict_a: "1"|"2", verdict_b: "i"|"ii"|"iii", answer (≤250 words covering safety, launch risk, minimal tests), references}.
