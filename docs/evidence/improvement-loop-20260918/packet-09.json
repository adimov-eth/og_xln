{"baseSha":"a8210663e8378f76c387359f59f69eb35d3f4324","dirty":true,"scope":"fixed evidence, read only; no tools or repository writes","facts":["Goal iPhone wallet pay/swap and fresh-device history. Native balances restore, earlier Sent absent. Export canonical signed checkpoint+WAL recording fixed: sparse full roots derived only after verified replay; canonical portable projection. Real WebKit isolated import test passed21frames: snapshotimport atR1 into private namespace, per frame replay exits (write guard intact) then normal saveEnvToDB. Compared original/imported replicaMetaDigest,postStateHash,outboxDigest,count for every frame. Close/reopen and rebuildhistory gives one exact HtlcFinalized. No new receipt store. 39 source gates pass.","Now need publish complete stage atomically. Proposed fresh-device-only import API: validate signed recording against locally derived seed, construct privately using existing snapshot import/replay/normal writer, validate tip and each stored commitment. Under existing target storage writer lock, require current/previous/WAL all empty, copy complete canonical staged WAL rows with head in ONE sync batch. Never chunk-publish. Then close target and reload via canonical loader to rebuild sparse overlays before exposing wallet. No new durable field or format. Namespace random staging, cleanup only exact own stage handles. Existing wallet import always rejects, including retry after successful publication; normal unlock can load committed import. Crash pre-batch leaves no committed wallet, crash post-batch gives complete tip/history. Never resume old input env after swapping sparse WAL.","Bounded decision: strongest correctness counterexample and smallest adversarial test for this algorithm. Avoid another framework, model loop, alternative receipt store or migration. Account Rust portable import remains explicitly rejected. Quiescent fresh-device assumption already exists in UI; low-level target lock must independently enforce emptiness. Local stage uses actual DBs, no mocks. Need implementation next, not repeated audit."]}
